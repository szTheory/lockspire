# Next Release Notes Draft

This file holds a human-written summary of user-facing changes sitting on `main`
that have **not** yet been published to Hex. It is a drafting space for a future
release review; it does not publish anything by itself.

Published releases are owned by `CHANGELOG.md`, which Release Please maintains.
Once a version ships, its notes belong there and should be cleared from here so
this file never restates already-released work.

## Unreleased Changes On `main`

None. `main` is at `1.4.0`, and everything after it is release-hygiene chore
work that Release Please does not treat as user facing.

The previous contents of this file described changes after `1.2.0`. All of that
shipped in `1.3.0` and `1.4.0` on 2026-07-28 and now lives in `CHANGELOG.md`:

- `1.3.0` — dependency security advisories and release-lane hygiene.
- `1.4.0` — the host-owned demo 404 page and working Disconnect, the
  remembered-consent duplication fix, the `phoenix_live_view` range widening,
  and the Release Please unblock.
