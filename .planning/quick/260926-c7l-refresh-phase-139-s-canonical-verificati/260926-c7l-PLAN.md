---
quick_id: 260926-c7l
status: in_progress
---

# Quick Task: Refresh Phase 139 Verification

## Objective

Refresh Phase 139's canonical verification over all 13 PLAN/SUMMARY pairs, then reconcile Phase 139 state only after that report passes. Keep the Phase 140 exact-SHA `plan:pre` gate unchanged and do not repeat completed plans or UAT.

## Tasks

1. Align the Phase 139 completion classifier and its integration fixture with the actual live parent: 13 matching plan/summary pairs, the checked `9/11` roadmap row, and `STATE.md` `current_plan: 12`. Accept only the exact canonical GSD completion child and retain the hostile mutation matrix.
2. Run the Plan 139-13 selector in its pinned Elixir/Erlang environment, using ExUnit trace mode because the full matrix exceeds its five-minute per-test timeout; run the portable Phase 140 router/lifecycle fixture and shell/diff checks. Commit only the focused classifier/test change.
3. Refresh Phase 139's preverification ledger through the installed GSD finalizer only when `.git/phase-139-finalizer.lock` is absent and the worktree is clean. Create and fingerprint a fresh verification report covering all 13 plan/summary pairs. Only after `status: passed`, invoke canonical `phase.complete 139` and verify the resulting state transition; do not invoke conversational UAT.

## Scope

- `scripts/maintainer/baseline_inventory.sh`
- `test/support/lockspire/release_proof/package_assertions.ex`
- `test/lockspire/release/repository_hygiene_contract_test.exs`
- `.planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md`
- `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `.planning/state.json` only after the fresh verification gate passes

## Completion Criteria

- The exact live Phase 139 parent state is accepted by the classifier; malformed counts, unrelated paths, and premature Phase 140 advancement remain rejected.
- The pinned Plan 139-13 integration selector and portable Phase 140 router/lifecycle suite pass.
- The refreshed report is `passed`, includes all 13 PLAN/SUMMARY pairs in its fingerprint, and keeps live CI-06/CI-07 evidence deferred to the unchanged Phase 140 gate.
- Canonical `phase.complete 139` reconciles state after the passing report, and the next specific GSD command is reported.
