---
quick_id: 260925-w7l
slug: make-phase-139-completion-classification
phase: 139-required-truth-reconciliation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/support/lockspire/release_proof/package_assertions.ex
  - test/lockspire/release/repository_hygiene_contract_test.exs
autonomous: true
status: planned
created: 2026-09-25
description: Accept GSD's canonical Phase 139 state.json update and completed plan count in the inventory relation
isolation: none
requirements: [CI-08, HYGIENE-06, TRUTH-03, TRUTH-05]
must_haves:
  truths:
    - The Phase 139 completion classifier accepts the exact `.planning/state.json` transition emitted by canonical GSD `phase.complete 139` alongside its bounded planning documents.
    - The Phase 139 completion roadmap row uses the exact number of committed, paired Phase 139 plans and summaries; the current 11/11 closeout succeeds without a numeric hardcode.
    - A missing, malformed, extra-field, wrong-phase, premature-Phase-140, stale-next-action, or unrelated JSON change remains `refresh_required`.
    - The immutable inventory relation, synchronized same-SHA CI and Release no-publication proof, and blocking Phase 140 `plan:pre` acceptance gate retain their current authority.
  artifacts:
    - scripts/maintainer/baseline_inventory.sh
    - test/support/lockspire/release_proof/package_assertions.ex
    - test/lockspire/release/repository_hygiene_contract_test.exs
  key_links:
    - The lifecycle classifier routes the exact Phase 139 completion subject to `validate_phase_139_completion_commit`, whose allowed path set includes `.planning/state.json` only when its old-to-new contract passes.
    - The JSON validator binds the child contract to the parent contract, the completed 139 and pending 140 roadmap state, and the GSD-emitted next action.
    - The roadmap validator derives N from the completion parent's committed 139-PLAN and 139-SUMMARY pairs and requires the parent and child Phase 139 rows to show N/N.
    - The existing post-transition relation and Phase 140 `plan:pre` hook still require the sealed candidate and external exact-SHA receipt before later planning.
---

# Quick Task 260925-w7l: Repair Phase 139 completion classification

## Objective

Repair the repository-owned inventory relation so its Phase 139 completion validator recognizes the state contract that installed GSD writes during `phase.complete 139` and derives its completed plan count from committed artifacts. Keep the change local to the completion commit's old-to-new semantic validation. This quick task does not rerun completed Phase 139 plans, advance live planning state, or claim external acceptance.

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Add canonical and hostile state-contract and plan-count fixtures</name>
  <files>test/support/lockspire/release_proof/package_assertions.ex, test/lockspire/release/repository_hygiene_contract_test.exs</files>
  <action>Extend the existing Phase 139 inventory-relation fixture, `write_phase_139_completion!`, and the completion mutation matrix. Seed the parent commit with all eleven numbered 139-PLAN/139-SUMMARY pairs, update its verification-position STATE and ROADMAP fixture from 9/9 to 11/11, and make the child completion ROADMAP row 11/11. Start with an isolated passing fixture whose `.planning/state.json` before/after bytes are produced by the installed GSD `phase.complete 139` contract publisher, or by a fixture captured from that command and checked against its current output. Commit the JSON only in the completion commit, alongside STATE, ROADMAP, and REQUIREMENTS. Assert that the relation returns `authorized_bookkeeping` before and after synchronized local/remote main movement, while the same existing sealed-receipt checks stay active. Add focused hostile mutations of the child JSON and parent-to-child relationship: absent or extra top-level keys, malformed timestamp or JSON, altered contract/flavor/milestone, changed phase identity/order, 139 not complete, 140 advanced early, added or changed unrelated phase, wrong or stale `next`, and a contract-only completion commit. Update the existing forged-count case from 9/9 to 11/11; add missing/extra/unpaired plan or summary and forged parent/child roadmap-count cases. Require each to return `refresh_required` with no receipt or ref movement. Reuse the existing temporary repository and fake remote; do not access live GitHub or publish a release. Keep the historic 1.5.0 evidence and supplemental OIDF classification unchanged.</action>
  <verify><automated>GSD_TOOLS=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-core/bin/gsd-tools.cjs ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_inventory_relation</automated></verify>
  <done>The new 11/11 positive fixture fails against the current classifier for the missing JSON allowance and stale 9/9 roadmap check; hostile fixtures specify the old-to-new, exact-action, and count-rejection contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate canonical JSON and derive the completion count from committed artifacts</name>
  <files>scripts/maintainer/baseline_inventory.sh, test/support/lockspire/release_proof/package_assertions.ex</files>
  <action>In `validate_phase_139_completion_commit`, require `.planning/state.json` in the changed path set and validate it as a regular write with an unchanged, valid parent contract through a dedicated `validate_phase_139_completion_state_contract` call. Validate exact schema/key sets, phase number/name/order and unchanged sibling phase records, a sole 139 `in_progress` to `complete` change, 140 still `pending`, unchanged `contract`, `flavor`, and milestone, bounded canonical UTC `updated_at`, and only the GSD `phase.complete 139` next-action change. Bind the new JSON to the same completion commit's STATE/ROADMAP and reject a JSON-only or JSON-plus-unrelated-path commit. Use the current installed GSD output from Task 1 to settle the literal accepted `next` command, label, and reason; do not infer them from prose or admit arbitrary Phase 140 routing text. In `validate_phase_139_completion_roadmap`, remove the 9/9 literals: enumerate regular 139-NN-PLAN.md and matching 139-NN-SUMMARY.md blobs from the completion parent, require a nonzero exact one-to-one set, and use that committed N to require exactly one parent In Progress N/N row and one child Complete N/N row with the existing two-line scoped diff. Reject missing, extra, duplicate, non-regular, or unpaired artifacts and any forged count. Keep the Phase 139 checkbox transition exact. Preserve the existing verification, requirements, state, inventory-ledger, exact-SHA synchronized-main, Release no-publication, historical release, and Phase 140 `plan:pre` predicates verbatim. Do not modify GSD runtime, workflow dispatch, main refs, current `.planning/STATE.md`, current `.planning/state.json`, or the Phase 140 gate.</action>
  <verify><automated>GSD_TOOLS=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-core/bin/gsd-tools.cjs ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_inventory_relation &amp;&amp; bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; git diff --check HEAD</automated></verify>
  <done>The canonical 11/11 completion is `authorized_bookkeeping`; every hostile JSON or count mutation is `refresh_required`; the existing sealed post-transition relation, same-SHA no-publish acceptance, and Phase 140 entry gate remain strict.</done>
</task>

## Verification

Run the focused ExUnit selector with its exact fixture runtime and record its test count and failures. Inspect the diff to confirm that only the classifier and regression fixtures changed. Do not run the live finalizer as part of this repair. The normal later GSD verification and Phase 140 `plan:pre` gate remain responsible for live currentness and external acceptance.

## Source coverage audit

| Source | Item | Task | Disposition |
|---|---|---:|---|
| GOAL | Accept only the GSD-emitted Phase 139 JSON completion change and the committed N/N plan count | 1, 2 | COVERED |
| REQ | CI-08, HYGIENE-06, TRUTH-03, TRUTH-05 repository relation and lifecycle truth | 1, 2 | COVERED |
| REQ | CI-06/CI-07 exact-SHA synchronized CI/Release no-publish receipt remains a blocking Phase 140 entry requirement | 1, 2 | PRESERVED |
| RESEARCH | Existing shell, jq, ExUnit, GSD runtime, and temporary Git fixture; no new dependency or external API integration | 1, 2 | COVERED |
| CONTEXT | Immutable inventory and proposal-only dispositions; no publication, release rewrite, or Phase 140 cleanup | 1, 2 | PRESERVED |

<threat_model>
## Trust boundaries

| Boundary | Description |
|---|---|
| GSD planning commit to inventory classifier | The commit's JSON and companion documents are mutable input to the currentness decision. |
| Repository proof to external acceptance | A passing fixture cannot substitute for synchronized main or the live CI/Release receipt. |

## STRIDE threat register

| Threat ID | Category | Component | Severity | Disposition | Mitigation |
|---|---|---|---|---|---|
| T-Q-w7l-01 | Tampering | `.planning/state.json` transition | high | mitigate | Strict old-to-new schema, phase, action, and companion-document validation; hostile fixture matrix. |
| T-Q-w7l-03 | Tampering | Phase 139 ROADMAP count | medium | mitigate | Derive N from committed regular plan/summary pairs and require exact N/N in parent and child rows. |
| T-Q-w7l-02 | Spoofing | Phase 140 readiness | high | mitigate | Retain the blocking `plan:pre` sealed receipt and same-SHA external proof requirements. |
</threat_model>

## Success criteria

- Canonical GSD `phase.complete 139` JSON bookkeeping is accepted by the Phase 139 completion classifier.
- The current 11/11 Phase 139 closeout is accepted from committed plan/summary artifacts without a 9/9 hardcode.
- All named malformed or unrelated variants remain rejected.
- No live external receipt, publication, ref update, or Phase 140 planning claim is created by this repair.
