---
phase: 102-generated-host-scaffolding-telemetry-migration
plan: 03
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - docs/upgrading/v1.27.md
  - test/lockspire/release_readiness_contract_test.exs
autonomous: true
requirements: [MIGRATE-01]
must_haves:
  truths:
    - "An operator upgrading from v1.26 to v1.27 reads docs/upgrading/v1.27.md and learns the default issuance format flipped from opaque to :jwt."
    - "The guide shows the HONEST one-line opt-back-to-opaque: Lockspire.Admin.ServerPolicy.put_access_token_format(:opaque), and states explicitly there is NO config :lockspire key for it."
    - "The guide names affected clients as exactly those whose access_token_format is nil (they inherit the new :jwt default); explicit :opaque-override clients are not affected."
    - "A contract-test clause fails if the guide's opt-out mechanism or affected-client naming drifts from shipped behavior."
  artifacts:
    - path: "docs/upgrading/v1.27.md"
      provides: "v1.27 migration guide (issuance flip + runtime opt-out + nil-inherit affected-client set)"
      contains: "put_access_token_format(:opaque)"
      min_lines: 20
    - path: "test/lockspire/release_readiness_contract_test.exs"
      provides: "@upgrading_v1_27_path constant + migration-guide pin clause"
      contains: "@upgrading_v1_27_path"
  key_links:
    - from: "test/lockspire/release_readiness_contract_test.exs"
      to: "docs/upgrading/v1.27.md"
      via: "File.read! + assert honest opt-out / nil-inherit + refute phantom config key"
      pattern: "put_access_token_format"
---

<objective>
Create the net-new `docs/upgrading/v1.27.md` migration guide (MIGRATE-01) and pin its truth with contract-test assertions so the migration prose cannot drift from shipped behavior.

Purpose: Operators upgrading from v1.26 must understand the opaque→`:jwt` issuance-default flip (shipped Phase 99), the HONEST runtime opt-out (`ServerPolicy.put_access_token_format(:opaque)` — NOT a `config :lockspire` key, which would be a silent no-op per D-09), and exactly which clients are affected (those with `access_token_format: nil`, per D-10).
Output: New `docs/upgrading/v1.27.md` + `@upgrading_v1_27_path` constant and a pin clause in the release-readiness contract test.

Note (wave dependency): this plan also edits `test/lockspire/release_readiness_contract_test.exs`, which Plan 102-01 owns in Wave 1. This plan runs in Wave 2 after 102-01 to avoid a same-file conflict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-CONTEXT.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-RESEARCH.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/upgrading/v1.27.md migration guide (MIGRATE-01, D-08/D-09/D-10)</name>
  <files>docs/upgrading/v1.27.md</files>
  <read_first>
    - lib/lockspire/admin/server_policy.ex (lines 40-43 `get_server_policy/0`; lines 65-73 `put_access_token_format/1` — the one-call runtime opt-out the guide documents; confirm it accepts :jwt|:opaque)
    - lib/lockspire/protocol/access_token_signer.ex (lines 88-98 `resolve_format/2` — the authoritative per-client → server-default → :jwt precedence the guide cross-references; note it is `defp`/private)
    - lib/lockspire/domain/server_policy.ex (line 38 — server default `access_token_format: :jwt`)
    - .planning/phases/99-signer-extraction-jwt-default-issuance/99-CONTEXT.md (D-04: server-wide format is durable runtime ServerPolicy state, NOT boot-time Config — this is WHY the opt-out is a runtime call, not a config key)
  </read_first>
  <action>
    Create the net-new directory `docs/upgrading/` and file `docs/upgrading/v1.27.md` (D-08; no in-repo precedent — prose/headings/ordering are Claude's discretion provided the locked content below holds). Required content: (1) Explain the default issuance flip — the server-wide default `access_token_format` is now `:jwt` (was opaque); the authorization-code, refresh, device, and CIBA paths mint RFC 9068 `at+jwt` by default. (2) Document the HONEST one-line opt-the-whole-deployment-back-to-opaque mechanism as a RUNTIME call: `Lockspire.Admin.ServerPolicy.put_access_token_format(:opaque)` (D-09) — and state EXPLICITLY that there is NO `config :lockspire` key for this, because the server-wide format is durable runtime `ServerPolicy` state (Phase 99 D-04), so editing `config/*.exs` would be a silent no-op. (3) Name affected clients as "every client whose `access_token_format` is `nil`" (D-10) — these inherit the new `:jwt` server default; cross-reference the precedence (per-client `:jwt|:opaque` wins → `nil` inherits `ServerPolicy.access_token_format`, default `:jwt`). State explicitly that clients with an explicit `:opaque` override are NOT affected and their behavior is unchanged. The doc MUST contain the literal strings `put_access_token_format(:opaque)`, `access_token_format` adjacent to `nil`, `:jwt`, and `opaque`, plus a flip narrative. The doc MUST NOT contain a `config :lockspire ... access_token_format` key (it would be a phantom no-op).
  </action>
  <verify>
    <automated>test -f docs/upgrading/v1.27.md && grep -q "put_access_token_format(:opaque)" docs/upgrading/v1.27.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/upgrading/v1.27.md` exists (net-new directory created).
    - Contains the literal `put_access_token_format(:opaque)`.
    - Contains `access_token_format` within ~40 chars of `nil` (the affected-client naming).
    - Contains both `:jwt` and `opaque` and a flip-narrative phrase (e.g. "default" + "change"/"flip").
    - Does NOT contain a `config :lockspire` line referencing `access_token_format` (grep -v match): `! grep -E "config :lockspire.*access_token_format" docs/upgrading/v1.27.md`.
    - States explicitly that explicit-`:opaque` clients are unaffected.
  </acceptance_criteria>
  <done>The migration guide exists with the honest runtime opt-out, the nil-inherit affected-client naming, the flip narrative, and no phantom config key.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the migration guide with contract-test assertions (MIGRATE-01, D-11)</name>
  <files>test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - test/lockspire/release_readiness_contract_test.exs (read lines 78-92 for the `Path.expand(... __DIR__)` constant idiom; read the new SCAFFOLD clauses added by Plan 102-01 to match conventions and avoid duplicate constants)
    - docs/upgrading/v1.27.md (the doc just written — confirm the pinned strings are present before asserting on them)
  </read_first>
  <action>
    Add constant `@upgrading_v1_27_path Path.expand("../../docs/upgrading/v1.27.md", __DIR__)` beside the other path constants (~line 88). Add a new test clause (D-11) that reads `doc = File.read!(@upgrading_v1_27_path)` and asserts: `assert doc =~ "put_access_token_format(:opaque)"` (honest runtime opt-out, D-09); `assert doc =~ ~r/access_token_format.{0,40}nil/` (nil-inherit affected-client naming, D-10); `assert doc =~ "opaque" and doc =~ ":jwt"` (the flip); and `refute doc =~ ~r/config :lockspire.*access_token_format/` (no phantom config key, D-09). Give the clause a name/message that names MIGRATE-01 and D-09/D-10 so a failure points the maintainer at the drift.
  </action>
  <verify>
    <automated>mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/lockspire/release_readiness_contract_test.exs` contains `@upgrading_v1_27_path`.
    - The new clause contains `assert doc =~ "put_access_token_format(:opaque)"`, the `~r/access_token_format.{0,40}nil/` assert, and the `refute doc =~ ~r/config :lockspire.*access_token_format/` line.
    - `mix test test/lockspire/release_readiness_contract_test.exs` exits 0.
    - Mutation check: temporarily removing the `put_access_token_format(:opaque)` line from the guide makes the clause FAIL (revert after confirming).
  </acceptance_criteria>
  <done>The migration guide is pinned by a contract-test clause that asserts the honest opt-out + nil-inherit naming + flip and refutes the phantom config key; the suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration guide prose → upgrading operator | Operator acts on documented opt-out; a wrong (no-op) instruction is a false-security boundary. |
| docs/upgrading/v1.27.md → contract test | The test reads the doc bytes; no untrusted runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-08 | Misconfiguration / false security | guide pointing operators at a no-op `config :lockspire` key | mitigate | D-09 documents the runtime `put_access_token_format(:opaque)` call; contract-test `refute` on `config :lockspire ... access_token_format` keeps the guide pointed at the real setter. |
| T-102-09 | Repudiation | guide drifts from shipped affected-client semantics | mitigate | D-11 pin asserts the `nil`-inherit naming and the honest opt-out so the migration truth cannot silently diverge. |
</threat_model>

<verification>
- `docs/upgrading/v1.27.md` exists with the required strings and no phantom config key.
- `mix test test/lockspire/release_readiness_contract_test.exs` exits 0 with the new pin clause present.
- Mutation check confirms the pin FAILS if the opt-out instruction is removed from the guide.
</verification>

<success_criteria>
MIGRATE-01 is satisfied: the v1.27 migration guide explains the issuance flip, shows the honest runtime opt-out (and denies a phantom config key), names the `nil`-inherit affected clients, and is fenced by a contract-test pin against drift.
</success_criteria>

<output>
Create `.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-03-SUMMARY.md` when done.
</output>
