---
phase: 102-generated-host-scaffolding-telemetry-migration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/lockspire.doctor.token_format.ex
  - lib/mix/tasks/lockspire.doctor.ex
  - test/mix/tasks/lockspire_doctor_token_format_test.exs
autonomous: true
requirements: [MIGRATE-02]
must_haves:
  truths:
    - "An operator running `mix lockspire.doctor token_format` sees a per-client report of each client's effective access-token format."
    - "Every client with access_token_format: nil is flagged as changed-semantics (now inherits the :jwt server default); explicit-:opaque clients are NOT flagged."
    - "The task is read-only and diagnostic-only: no mutation, no Mix.raise on flagged clients, no non-zero exit."
    - "The doctor's effective-format computation matches AccessTokenSigner.resolve_format/2's precedence exactly."
    - "`mix lockspire.doctor token_format` is routed by the dispatcher and listed in its fallback help."
  artifacts:
    - path: "lib/mix/tasks/lockspire.doctor.token_format.ex"
      provides: "Read-only per-client effective-format diagnostic subtask"
      contains: "Mix.Tasks.Lockspire.Doctor.TokenFormat"
    - path: "lib/mix/tasks/lockspire.doctor.ex"
      provides: "run([\"token_format\" | rest]) dispatch clause + updated fallback help"
      contains: "token_format"
    - path: "test/mix/tasks/lockspire_doctor_token_format_test.exs"
      provides: "Doctor subtask diagnostic + precedence-parity + dispatcher/help test"
      exports: []
  key_links:
    - from: "lib/mix/tasks/lockspire.doctor.ex"
      to: "lib/mix/tasks/lockspire.doctor.token_format.ex"
      via: "Mix.Task.run(\"lockspire.doctor.token_format\", rest)"
      pattern: "token_format"
    - from: "lib/mix/tasks/lockspire.doctor.token_format.ex"
      to: "Clients.list_clients/1 + ServerPolicy.get_server_policy/0"
      via: "{:ok, _}-tuple reads + reproduced resolve_format precedence"
      pattern: "list_clients"
---

<objective>
Add a read-only `mix lockspire.doctor token_format` diagnostic subtask (MIGRATE-02) that reports each client's effective access-token format and flags every `access_token_format: nil` client whose inherited default changed semantics — diagnostic, not enforcement.

Purpose: Operators need a one-command read-only view of which clients inherit the new `:jwt` default after the v1.27 issuance flip, computed with the SAME precedence the signer uses, without any mutation or CI-breaking exit.
Output: New `Mix.Tasks.Lockspire.Doctor.TokenFormat` subtask, a dispatch clause + fallback-help update in the existing doctor dispatcher, and a new doctor test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-CONTEXT.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-RESEARCH.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the TokenFormat doctor subtask + dispatcher wiring (MIGRATE-02, D-12/D-13/D-14)</name>
  <files>lib/mix/tasks/lockspire.doctor.token_format.ex, lib/mix/tasks/lockspire.doctor.ex</files>
  <read_first>
    - lib/mix/tasks/lockspire.doctor.remote_jwks.ex (the subtask shape to mirror shape-for-shape: `use Mix.Task`, `@requirements ["app.config"]`, `@switches`, `OptionParser.parse(args, strict: @switches)`, invalid-opts `Mix.raise`, `--help` branch, `print_result`-style helper building a line list → `Enum.each(&Mix.shell().info/1)`)
    - lib/mix/tasks/lockspire.doctor.ex (the 23-line dispatcher — existing `run(["remote-jwks" | rest])` clause + the `run(_args)` fallback whose "Supported commands:" help block must be updated, Pitfall 5)
    - lib/lockspire/protocol/access_token_signer.ex (lines 35-36 aliases `Lockspire.Domain.Client` / `Lockspire.Domain.ServerPolicy`; lines 88-98 the `defp resolve_format/2` three-clause precedence to REPRODUCE — it is private, cannot be called)
    - lib/lockspire/admin/clients.ex (lines 82-85 `list_clients/1` → `{:ok, [Client.t()]} | {:error, term()}`)
    - lib/lockspire/admin/server_policy.ex (lines 40-43 `get_server_policy/0` → `{:ok, ServerPolicy.t()} | {:error, term()}`)
    - lib/lockspire/domain/server_policy.ex (line 38 — default `access_token_format: :jwt`)
  </read_first>
  <action>
    Create `lib/mix/tasks/lockspire.doctor.token_format.ex` defining `Mix.Tasks.Lockspire.Doctor.TokenFormat` (D-12), mirroring `lockspire.doctor.remote_jwks.ex` shape-for-shape: `use Mix.Task`, `@shortdoc`, `@requirements ["app.config"]`, `@switches [help: :boolean]` (NO required `--client` — this task enumerates ALL clients), `run/1` with `OptionParser.parse(args, strict: @switches)`, invalid-opts `Mix.raise`, and a `--help` branch printing a `help/0` string that includes the literal usage line `mix lockspire.doctor token_format`. The report path (D-13): `with {:ok, policy} <- ServerPolicy.get_server_policy(), {:ok, clients} <- Clients.list_clients() do ... else {:error, reason} -> Mix.shell().info("Could not inspect token formats: #{inspect(reason)}")` — report errors CALMLY, never crash (Pitfall 6; both reads return `{:ok, _}` tuples). Compute each client's effective format by REPRODUCING `resolve_format/2`'s three clauses as a private `effective_format/2` (Pitfall 1 / LANDMINE-1 — `resolve_format/2` is `defp` and cannot be called): per-client `:jwt|:opaque` wins; `nil` + `%ServerPolicy{access_token_format: server_fmt}` → `server_fmt || :jwt`; `nil` + no policy → `:jwt`. Match on `Lockspire.Domain.Client` / `Lockspire.Domain.ServerPolicy` (alias them) and add a code comment pointing to `access_token_signer.ex:88-98` as the authority that this must stay byte-equivalent to. Output (D-14): one `Mix.shell().info/1` line per client naming `client_id` + effective format, plus a flag line/marker on EVERY client with `access_token_format: nil` indicating its semantics changed (now inherits `:jwt`); explicit-`:opaque` clients are NOT flagged. STRICTLY read-only: no mutation, NO `Mix.raise` on flagged clients, no non-zero exit (raising would break operator CI). Then wire the dispatcher in `lib/mix/tasks/lockspire.doctor.ex`: add `def run(["token_format" | rest]), do: Mix.Task.run("lockspire.doctor.token_format", rest)` BEFORE the `run(_args)` fallback, and add the line `mix lockspire.doctor token_format` to the fallback's "Supported commands:" help block (Pitfall 5 — otherwise the help lies). NOTE on A1 (Claude's discretion area): default is the reproduce-inline approach above (lowest blast radius). Promoting `resolve_format/2` to a public `def` on the shipped Phase-99 signer module is the user-confirmable alternative — do NOT widen that module's public surface without explicit user confirmation; reproduce inline.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; mix test test/mix/tasks/lockspire_doctor_token_format_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `lib/mix/tasks/lockspire.doctor.token_format.ex` defines `Mix.Tasks.Lockspire.Doctor.TokenFormat` with `use Mix.Task` and `@requirements ["app.config"]`.
    - It does NOT call `AccessTokenSigner.resolve_format/2` (it is private; grep: no such call) — it reproduces the three-clause precedence as a private `effective_format/2` with a comment citing `access_token_signer.ex:88-98`.
    - It pattern-matches `{:ok, _}` from both `get_server_policy/0` and `list_clients/1` and handles `{:error, _}` without crashing.
    - It contains NO `Mix.raise` in the report/flag path (only the OptionParser invalid-opts guard may raise) and no `System.halt`/non-zero exit on flagged clients.
    - `lib/mix/tasks/lockspire.doctor.ex` contains `run(["token_format" | rest])` and its fallback help lists `mix lockspire.doctor token_format`.
    - `mix compile --warnings-as-errors` succeeds (no undefined/private function error).
  </acceptance_criteria>
  <done>The read-only TokenFormat subtask compiles, reproduces signer precedence, flags only nil clients without raising, and is dispatched + listed in help.</done>
</task>

<task type="auto">
  <name>Task 2: Doctor subtask test — diagnostic, precedence parity, dispatcher/help (MIGRATE-02)</name>
  <files>test/mix/tasks/lockspire_doctor_token_format_test.exs</files>
  <read_first>
    - test/mix/tasks/lockspire_doctor_remote_jwks_test.exs (the test structure to mirror: `use ExUnit.Case, async: false`, `import ExUnit.CaptureIO`, `setup_all` TestRepo + sandbox, `setup` checkout + `Mix.Task.reenable`, `capture_io(fn -> Mix.Task.run(...) end)` assertions)
    - lib/lockspire/admin/server_policy.ex (lines 65-73 `put_access_token_format/1` — used to flip the server default for the precedence-parity test)
    - lib/lockspire/admin/clients.ex (client-create path used by other tests for seeding a nil-format and an explicit-:opaque client)
    - test/support (locate the existing client-seed / TestRepo helpers used by the remote_jwks test before adding new ones)
  </read_first>
  <action>
    Create `test/mix/tasks/lockspire_doctor_token_format_test.exs` mirroring `lockspire_doctor_remote_jwks_test.exs`: `use ExUnit.Case, async: false`, `import ExUnit.CaptureIO`, `setup_all` putting `Lockspire.TestRepo`, `start_supervised!`, sandbox `:manual`; `setup` sandbox checkout + `Mix.Task.reenable("lockspire.doctor")` + `Mix.Task.reenable("lockspire.doctor.token_format")`. Seed two clients: one with `access_token_format: nil` and one with explicit `access_token_format: :opaque`. Tests: (1) `output = capture_io(fn -> Mix.Task.run("lockspire.doctor", ["token_format"]) end)` — assert the output names the nil client with effective `:jwt` and marks it changed/flagged, and names the :opaque client with effective `:opaque` and does NOT flag it; assert the run did not raise (the capture returning normally is the proof — add an explicit assertion that no exception propagates, D-14). (2) Precedence parity: with server default `:jwt`, the nil client reports `:jwt`; then call `Lockspire.Admin.ServerPolicy.put_access_token_format(:opaque)`, re-run via `capture_io`, and assert the SAME nil client now reports `:opaque` — proving `effective_format/2` matches the signer's `resolve_format/2` resolution. (3) Dispatcher + help (Pitfall 5): `help = capture_io(fn -> Mix.Task.run("lockspire.doctor", ["token_format", "--help"]) end)` and assert `help =~ "mix lockspire.doctor token_format"`.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/lockspire_doctor_token_format_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File `test/mix/tasks/lockspire_doctor_token_format_test.exs` exists and uses `capture_io` + `Mix.Task.run("lockspire.doctor", ["token_format"])`.
    - A test asserts the nil-format client is reported with effective `:jwt` AND flagged changed.
    - A test asserts the explicit-`:opaque` client is reported and NOT flagged.
    - A test flips the server default via `put_access_token_format(:opaque)` and asserts the nil client's reported effective format becomes `:opaque` (precedence parity).
    - A test asserts the `--help` output contains `mix lockspire.doctor token_format`.
    - `mix test test/mix/tasks/lockspire_doctor_token_format_test.exs` exits 0.
  </acceptance_criteria>
  <done>The doctor test proves the diagnostic report, the nil-only flagging, signer-precedence parity, and dispatcher/help routing; the suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI args → doctor subtask | OptionParser strict switches; only CLI args cross here (V5 input validation, minimal). |
| live ServerPolicy + client rows → doctor (read-only) | The task reads runtime state; it must not mutate or break operator CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-10 | Tampering / Denial of Service | doctor mutating state or breaking operator CI via Mix.raise / non-zero exit on flagged clients | mitigate | D-14: read-only, no mutation, no `Mix.raise` in the report path, no non-zero exit; doctor test asserts no raise on flagged clients. |
| T-102-11 | Repudiation | doctor reporting a different effective format than the signer issues ("a lying doctor") | mitigate | D-13: reproduce `resolve_format/2`'s exact three clauses (it is private), comment-anchored to `access_token_signer.ex:88-98`; precedence-parity test flips the server default and asserts the report tracks it. |
| T-102-12 | Information Disclosure | doctor leaking secrets in the per-client report | accept | Report names only `client_id` + effective format atom; no secrets/token material is read or printed (low risk, no PII beyond client_id which is operator-visible by definition). |
| T-102-13 | Denial of Service | doctor crashing on `{:error, _}` from admin reads | mitigate | Pitfall 6: pattern-match `{:ok, _}` and report `{:error, reason}` calmly via `Mix.shell().info`, never crash. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` succeeds (no private-function call).
- `mix test test/mix/tasks/lockspire_doctor_token_format_test.exs` exits 0 (diagnostic + parity + dispatcher/help).
- Grep confirms no `AccessTokenSigner.resolve_format` call and no `Mix.raise`/`System.halt` in the report path.
</verification>

<success_criteria>
MIGRATE-02 is satisfied: `mix lockspire.doctor token_format` is a routed, read-only, diagnostic-only subtask that reports each client's effective format (matching the signer's precedence) and flags every `access_token_format: nil` client, without mutation or non-zero exit.
</success_criteria>

<output>
Create `.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-04-SUMMARY.md` when done.
</output>
