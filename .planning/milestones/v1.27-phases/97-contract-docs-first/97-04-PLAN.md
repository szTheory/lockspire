---
phase: 97-contract-docs-first
plan: 04
type: execute
wave: 3
depends_on:
  - 97-02
files_modified:
  - examples/adoption_demo/lib/adoption_demo_web/router.ex
  - priv/templates/lockspire.install/router.ex
  - scripts/demo/adoption_smoke.py
autonomous: true
requirements:
  - RECIPE-01
tags:
  - phase-97
  - recipe-01
  - canonical-block-carriers
  - demo-router
  - install-template
  - python-smoke
user_setup: []

must_haves:
  truths:
    - "The live demo router `examples/adoption_demo/lib/adoption_demo_web/router.ex` carries the canonical pipeline declaration block between `# BEGIN LOCKSPIRE_PROTECTED_PIPELINE` and `# END LOCKSPIRE_PROTECTED_PIPELINE` marker comments, replacing the existing L23-27 declaration block in place; the interior bytes match the canonical bytes in `docs/protect-phoenix-api-routes.md` after the D-02 normalization step (strip-uniform-indent + trim-trailing-whitespace + LF normalization)."
    - "The install template `priv/templates/lockspire.install/router.ex` carries the canonical block as commented-out Elixir inside the existing heredoc string, BEFORE the first `scope \"/\", <%= @web_module %> do` block (placement is module-level per RESEARCH Pitfall 7); after EEx interpolation, host routers see inert Elixir line comments per D-10."
    - "The Python smoke script `scripts/demo/adoption_smoke.py` carries the canonical block as a Python-comment carrier (every interior `.ex` line prefixed with `# `) between the same `# BEGIN LOCKSPIRE_PROTECTED_PIPELINE` and `# END LOCKSPIRE_PROTECTED_PIPELINE` markers, placed inside `exercise_authorization_code` immediately above L244 per D-14."
    - "All four canonical-block sites (the doc page from Plan 02 plus the three sites from this plan) produce byte-identical extracted regions after the D-02 normalization step (strip-uniform-indent + Python-only `# ` per-line prefix strip + LF normalization + trailing-whitespace normalization)."
    - "Demo runtime still works after the placeholder swap: `dpop_replay_store: AdoptionDemo.Repo` → `dpop_replay_store: MyAppWeb.ProtectedApiReplayStore` in the canonical block is a placeholder per D-04; the demo's current `AdoptionDemo.Repo` is removed from the canonical region but a thin adopter-side alias will be added in Phase 101 DEMO-01/02/03. For Phase 97 purposes, the placeholder name is documentation-canonical and does not yet need a runtime referent in the demo BEAM — the demo app's `mix compile` may emit a warning about an undefined module reference; that warning is expected and is reconciled in Phase 101."
    - "The install template stays compile-clean after the canonical block insertion: commented-out Elixir lines inside a heredoc are inert; `mix compile` of Lockspire itself succeeds; no EEx tags (`<%`, `<%=`) appear inside the canonical region."
    - "Python file stays runnable: comment-prefixed lines inside `exercise_authorization_code` do not change the function's runtime behavior; `python -c 'import ast; ast.parse(open(\"scripts/demo/adoption_smoke.py\").read())'` exits 0."
  artifacts:
    - path: "examples/adoption_demo/lib/adoption_demo_web/router.ex"
      provides: "Second of four canonical-block carrier sites; live Phoenix router pipeline marker-wrapped"
      contains: "# BEGIN LOCKSPIRE_PROTECTED_PIPELINE"
    - path: "priv/templates/lockspire.install/router.ex"
      provides: "Third of four canonical-block carrier sites; install-template heredoc with commented-out canonical block per D-10"
      contains: "# BEGIN LOCKSPIRE_PROTECTED_PIPELINE"
    - path: "scripts/demo/adoption_smoke.py"
      provides: "Fourth of four canonical-block carrier sites; Python-comment carrier adjacent to protected-API smoke per D-14"
      contains: "# BEGIN LOCKSPIRE_PROTECTED_PIPELINE"
  key_links:
    - from: "examples/adoption_demo/lib/adoption_demo_web/router.ex (canonical region)"
      to: "docs/protect-phoenix-api-routes.md (canonical region from Plan 02)"
      via: "byte-identical interior after D-02 normalization"
      pattern: "BEGIN LOCKSPIRE_PROTECTED_PIPELINE.*END LOCKSPIRE_PROTECTED_PIPELINE"
    - from: "priv/templates/lockspire.install/router.ex (heredoc canonical region)"
      to: "docs/protect-phoenix-api-routes.md (canonical region)"
      via: "byte-identical interior after strip-uniform-indent normalization"
      pattern: "BEGIN LOCKSPIRE_PROTECTED_PIPELINE.*END LOCKSPIRE_PROTECTED_PIPELINE"
    - from: "scripts/demo/adoption_smoke.py (Python-comment canonical region)"
      to: "docs/protect-phoenix-api-routes.md (canonical region)"
      via: "byte-identical interior after Python `# ` prefix strip + strip-uniform-indent normalization"
      pattern: "BEGIN LOCKSPIRE_PROTECTED_PIPELINE.*END LOCKSPIRE_PROTECTED_PIPELINE"
---

<objective>
Carry the canonical pipeline block into the three remaining RECIPE-01 sites — the live demo router, the install template heredoc (commented-out per D-10), and the Python smoke script (as a Python-comment carrier per D-03 + D-14). The canonical bytes are the ones established in Plan 02's `docs/protect-phoenix-api-routes.md` rewrite; this plan mirrors them into three different host syntaxes such that, after the D-02 normalization step, all four files produce byte-identical extracted regions.

Purpose: Complete the four-site canonical-block ground truth that Plan 05's `release_readiness_contract_test` hash-compare clause will enforce. After this plan, the four files all carry markers and a canonical region; Plan 05 adds the test that proves they cannot silently drift.

Output: Three modified files (one Elixir router, one EEx-interpolated heredoc template, one Python script). No test file modified in this plan; Plan 05 owns the test clause.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-contract-docs-first/97-CONTEXT.md
@.planning/phases/97-contract-docs-first/97-RESEARCH.md
@.planning/phases/97-contract-docs-first/97-PATTERNS.md
@.planning/phases/97-contract-docs-first/97-VALIDATION.md
@.planning/phases/97-contract-docs-first/97-02-SUMMARY.md
@.planning/phases/92-advanced-setup-support-truth/92-CONTEXT.md

<interfaces>
<!-- The canonical bytes (interior between BEGIN/END markers in the .md file) that all three sites in this plan must mirror. -->

Canonical interior bytes (verbatim — established in Plan 02's `docs/protect-phoenix-api-routes.md` rewrite; same byte sequence in all four files after D-02 normalization). NO surrounding whitespace; NO leading or trailing blank lines inside the BEGIN/END region.

Canonical bytes (8 lines, LF line endings, no trailing whitespace on any line):

```
pipeline :lockspire_protected_api do
  plug Lockspire.Plug.VerifyToken, scopes: ["read:billing"], audience: "billing-api"
  plug Lockspire.Plug.EnforceSenderConstraints,
    dpop_replay_store: MyAppWeb.ProtectedApiReplayStore
  plug Lockspire.Plug.RequireToken
end
```

(Six lines total in the canonical region — 1 `pipeline :lockspire_protected_api do` line, 4 `plug ...` lines (lines 2 and 3 of plug are the multi-line `EnforceSenderConstraints` form with `dpop_replay_store:` on the continuation line), 1 `end` line. The canonical region does NOT include the BEGIN/END marker lines themselves — those are visible adjacent to the region but excluded from the hashed bytes.)

Per-file marker comment shapes (these wrap the canonical region in each carrier file; the markers are visible but not part of the hashed region):

```
Elixir (.ex) files (demo router, install template):
# BEGIN LOCKSPIRE_PROTECTED_PIPELINE
... canonical bytes ...
# END LOCKSPIRE_PROTECTED_PIPELINE

Python (.py) file (smoke script):
# BEGIN LOCKSPIRE_PROTECTED_PIPELINE
... canonical bytes with each line prefixed by `# ` ...
# END LOCKSPIRE_PROTECTED_PIPELINE
```

D-02 normalization (this is what makes the four sites' extracted regions byte-identical):
1. CRLF → LF (`String.replace(bytes, "\r\n", "\n")`)
2. Strip the leading `# ` from each interior line — applied to TWO carrier kinds: `:python_commented` (the Python file) AND `:elixir_in_commented_heredoc` (the install template, whose canonical block is commented-out Elixir per D-10). NOT applied to `:elixir_in_markdown_fence` (the doc page) or `:elixir` (the live demo router).
3. Strip uniform left-indent (compute minimum leading-whitespace count across non-blank lines; strip that many spaces from every line)
4. Strip trailing whitespace per line (`String.replace(bytes, ~r/[ \t]+$/m, "")`)

The carrier-file interior indent is the only difference Plan 05's helper has to absorb:
- `docs/protect-phoenix-api-routes.md` canonical region: zero leading spaces (fenced block interior is at column 0)
- `examples/adoption_demo/lib/adoption_demo_web/router.ex` canonical region: 2 spaces of leading indent (matches module-level in a `defmodule ... do` body)
- `priv/templates/lockspire.install/router.ex` canonical region: 4 spaces of leading indent (matches heredoc-interior indent — verified at L14-19 of current file)
- `scripts/demo/adoption_smoke.py` canonical region: 4 spaces of leading indent + `# ` Python comment prefix on each interior line (matches Python function-body indent inside `exercise_authorization_code`)

After D-02 normalization steps 1-4, all four files produce identical bytes equal to the canonical bytes shown above (modulo zero leading-indent — the strip-uniform-indent step takes each carrier down to zero).

Current state of the three carrier files (verified by direct file read 2026-05-27):

`examples/adoption_demo/lib/adoption_demo_web/router.ex` current L23-27 (parens style; missing `audience:`; uses `AdoptionDemo.Repo` not `MyAppWeb.ProtectedApiReplayStore`):
```
  pipeline :lockspire_protected_api do
    plug(Lockspire.Plug.VerifyToken, scopes: ["read:billing"])
    plug(Lockspire.Plug.EnforceSenderConstraints, dpop_replay_store: AdoptionDemo.Repo)
    plug(Lockspire.Plug.RequireToken)
  end
```

Style note for the demo router rewrite: switch from `plug(Foo, ...)` parens style to `plug Foo, ...` no-parens style to match the docs page canonical block (D-01's "Exact Elixir formatting" Claude's-discretion bullet says: pick one style; apply identically to all four sites; the docs page is the seed). The demo router edit must also re-indent the canonical region to 2-space leading indent (was already 2-space; preserve) and switch the `EnforceSenderConstraints` line from single-line parens form to multi-line no-parens form per the canonical bytes shown above.

Pre-edit `plug(` parens count for the demo router (verified 2026-05-27 via `grep -cE 'plug\(' examples/adoption_demo/lib/adoption_demo_web/router.ex`): exactly `11` parens-form plug calls (3 in :browser, 1 in :operator, 1 in :api, 3 in :lockspire_protected_api, 3 in scope/pipe_through declarations). After this task drops parens on the 3 :lockspire_protected_api plug calls, the post-edit count is exactly `8` (= 11 − 3). A post-edit count equal to 11 means the parens-drop did not happen.

`priv/templates/lockspire.install/router.ex` current heredoc structure (verified 55 lines total):
- L1-8: module wrapper + moduledoc
- L9: `def lockspire_routes do`
- L10: `"""` (heredoc open)
- L11: `scope "/", <%= @web_module %> do` (first heredoc-interior line; verified at column 4 — i.e., 4-space leading indent inside the heredoc)
- L12-28: scope body lines (4-space and deeper indent)
- L30-43: blank line then commented-out admin-mount example block (4-space indent, `# ` prefix)
- L44-51: admin scope and OAuth forward
- L52: `"""` (heredoc close)
- L53: `end` (function close)

The canonical block goes BETWEEN L10 (`"""`) and L11 (`scope "/", ...`). This is the safe placement per RESEARCH Pitfall 7 — pipelines must be at module level, BEFORE any `scope` declaration.

`scripts/demo/adoption_smoke.py` current relevant region (verified L240-246):
```
    assert_status(userinfo, 200, "userinfo accepts issued access token")
    userinfo_json = json_body(userinfo, "userinfo")
    assert userinfo_json["email"] == "alice@acme.test"

    anonymous_api = Browser(BASE_URL).request("GET", "/api/billing/summary")
    assert_status(anonymous_api, 401, "protected API rejects anonymous request")
```

The Python-comment carrier block goes IMMEDIATELY ABOVE L244 (the `anonymous_api` line) inside the `exercise_authorization_code` function (verified function defined at L173-246). Indent: 4 spaces (matches Python function-body indent). Each canonical-region interior line is prefixed with `# ` (Python comment) so the lines are syntactically inert.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wrap the demo router pipeline in BEGIN/END markers and reconcile to the canonical bytes (per D-04 and D-13)</name>
  <files>examples/adoption_demo/lib/adoption_demo_web/router.ex</files>
  <read_first>
    - examples/adoption_demo/lib/adoption_demo_web/router.ex (the file being modified — read in full; the existing pipeline at L23-27 is the target)
    - .planning/phases/97-contract-docs-first/97-PATTERNS.md (the `examples/adoption_demo/lib/adoption_demo_web/router.ex` row — exact target-state form, exact placeholder reconciliations)
    - .planning/phases/97-contract-docs-first/97-CONTEXT.md decisions D-01 (canonical block shape), D-04 (`MyAppWeb.ProtectedApiReplayStore` placeholder), D-13 (`audience: "billing-api"` placeholder)
    - .planning/phases/97-contract-docs-first/97-02-SUMMARY.md (Plan 02's record of the canonical bytes — the exact byte sequence between BEGIN/END markers in docs/protect-phoenix-api-routes.md is the seed this task mirrors)
    - docs/protect-phoenix-api-routes.md (the post-Plan-02 state — the fenced Elixir block in the canonical-plug-order section IS the byte-equality target)
  </read_first>
  <behavior>
    - After this edit, `examples/adoption_demo/lib/adoption_demo_web/router.ex` carries `# BEGIN LOCKSPIRE_PROTECTED_PIPELINE` and `# END LOCKSPIRE_PROTECTED_PIPELINE` lines around the pipeline declaration; the interior 6 lines match the canonical bytes shown in `<interfaces>` after D-02 normalization (strip 2-space uniform indent → zero leading indent).
    - The current single-line parens-style `plug(Foo, ...)` declarations at L24-26 are replaced with the no-parens canonical-byte form (matching the docs page style chosen in Plan 02).
    - `dpop_replay_store: AdoptionDemo.Repo` is replaced with `dpop_replay_store: MyAppWeb.ProtectedApiReplayStore` per D-04 (placeholder; reconciled in Phase 101).
    - `audience: "billing-api"` is added per D-13 (previously absent; canonical block requires it).
    - The rest of the router file is unchanged (other pipeline declarations, scope blocks, route declarations all preserved verbatim).
    - `mix compile --force --warnings-as-errors` for the demo app emits at most ONE warning related to `MyAppWeb.ProtectedApiReplayStore` being an undefined module reference (this is the expected placeholder gap that Phase 101 DEMO-01/02/03 reconciles via a demo-side alias; the warning does not block compilation). If `mix compile` fails entirely with `--warnings-as-errors`, the executor relaxes to plain `mix compile` for this file to confirm compilation success despite the warning.
  </behavior>
  <action>
    Edit `examples/adoption_demo/lib/adoption_demo_web/router.ex` in place. Replace L23-27 (the current pipeline declaration) with a marker-wrapped canonical-byte form. Total replacement is 8 lines (2 marker comment lines + 6 canonical body lines), replacing 5 lines.

    Find the existing block at L23-27 (verified current state):

    Line 23 reads: `  pipeline :lockspire_protected_api do`
    Line 24 reads: `    plug(Lockspire.Plug.VerifyToken, scopes: ["read:billing"])`
    Line 25 reads: `    plug(Lockspire.Plug.EnforceSenderConstraints, dpop_replay_store: AdoptionDemo.Repo)`
    Line 26 reads: `    plug(Lockspire.Plug.RequireToken)`
    Line 27 reads: `  end`

    Replace with (each line shown with its exact column-0 leading whitespace — the new region has 2 spaces of leading indent for the marker lines and 2 spaces for the `pipeline`/`end` lines, with the `plug` lines at 4 spaces of leading indent inside the pipeline):

    New L23: `  # BEGIN LOCKSPIRE_PROTECTED_PIPELINE`
    New L24: `  pipeline :lockspire_protected_api do`
    New L25: `    plug Lockspire.Plug.VerifyToken, scopes: ["read:billing"], audience: "billing-api"`
    New L26: `    plug Lockspire.Plug.EnforceSenderConstraints,`
    New L27: `      dpop_replay_store: MyAppWeb.ProtectedApiReplayStore`
    New L28: `    plug Lockspire.Plug.RequireToken`
    New L29: `  end`
    New L30: `  # END LOCKSPIRE_PROTECTED_PIPELINE`

    Key style points:
    - Marker lines use 2-space leading indent (matches the surrounding module-body indent).
    - `pipeline` and `end` use 2-space leading indent (matches the existing L23 and L27 form).
    - `plug` lines use 4-space leading indent (2 module-body + 2 pipeline-body), no parens (drop the parens to match the canonical docs-page form), no trailing whitespace.
    - The `plug Lockspire.Plug.EnforceSenderConstraints,` line ends with a comma — it is the first line of a multi-line plug call. The continuation line `      dpop_replay_store: MyAppWeb.ProtectedApiReplayStore` uses 6-space leading indent (2 module-body + 4 continuation indent, matching the canonical bytes shown in `<interfaces>` which have 4-space continuation indent after the strip-uniform-indent step takes the 2-space module-body offset away).
    - The four `plug` calls in the canonical bytes are: `plug Lockspire.Plug.VerifyToken, scopes: ["read:billing"], audience: "billing-api"` (one line); `plug Lockspire.Plug.EnforceSenderConstraints,` + continuation `    dpop_replay_store: MyAppWeb.ProtectedApiReplayStore` (two lines); `plug Lockspire.Plug.RequireToken` (one line). Total 4 plug call lines.

    Do NOT touch any other line in the file. Do NOT modify the other pipelines (`:browser` at L6-13, `:operator` at L15-17, `:api` at L19-21). Do NOT modify the scopes or routes.

    After the edit, the demo app may emit a `MyAppWeb.ProtectedApiReplayStore` undefined-module warning at compile time. This is expected; do NOT add an alias or stub module in this plan — Phase 101 owns that work per D-04.

    No fenced code blocks in this action — every byte change is enumerated line-by-line above.
  </action>
  <verify>
    <automated>grep -c "BEGIN LOCKSPIRE_PROTECTED_PIPELINE" examples/adoption_demo/lib/adoption_demo_web/router.ex ; grep -c "END LOCKSPIRE_PROTECTED_PIPELINE" examples/adoption_demo/lib/adoption_demo_web/router.ex ; grep -c "MyAppWeb.ProtectedApiReplayStore" examples/adoption_demo/lib/adoption_demo_web/router.ex ; grep -c "AdoptionDemo.Repo" examples/adoption_demo/lib/adoption_demo_web/router.ex ; grep -c 'audience: "billing-api"' examples/adoption_demo/lib/adoption_demo_web/router.ex ; grep -cE 'plug\(' examples/adoption_demo/lib/adoption_demo_web/router.ex ; (cd examples/adoption_demo &amp;&amp; mix compile) 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BEGIN LOCKSPIRE_PROTECTED_PIPELINE" examples/adoption_demo/lib/adoption_demo_web/router.ex` returns exactly `1`.
    - `grep -c "END LOCKSPIRE_PROTECTED_PIPELINE" examples/adoption_demo/lib/adoption_demo_web/router.ex` returns exactly `1`.
    - `grep -c "MyAppWeb.ProtectedApiReplayStore" examples/adoption_demo/lib/adoption_demo_web/router.ex` returns exactly `1` (the canonical-block placeholder is present).
    - `grep -c "AdoptionDemo.Repo" examples/adoption_demo/lib/adoption_demo_web/router.ex` returns exactly `0` inside the canonical region. (NOTE: `AdoptionDemo.Repo` may still appear in other parts of the router file legitimately — e.g., if it is referenced in a scope or alias; verify by inspecting any non-zero matches and confirming they are outside the canonical region. The existing file at L25 was the ONLY occurrence, so the expected count post-edit is `0`. If the count is non-zero, inspect each line to confirm none of them is the swapped-out canonical-block line.)
    - `grep -c 'audience: "billing-api"' examples/adoption_demo/lib/adoption_demo_web/router.ex` returns exactly `1` (D-13 placeholder added).
    - `grep -cE 'plug\(' examples/adoption_demo/lib/adoption_demo_web/router.ex` returns EXACTLY `8` post-edit. Ground truth: pre-edit count is `11` (verified 2026-05-27); the parens-drop on the three `:lockspire_protected_api` plug calls is the only change to parens-form plug calls in this file; 11 − 3 = 8. A post-edit count of 11 means the parens-drop did not happen → the canonical block is not in no-parens form → FAIL. A post-edit count of anything other than 8 means an unintended parens change leaked into another pipeline → investigate. (This acceptance criterion was tightened in revision iteration 1 to close BLOCKER #2 — the prior threshold of "≤5" was unsatisfiable because the three non-target pipelines `:browser`, `:operator`, `:api` plus the three scope `pipe_through` calls contribute 8 unrelated parens-form plug calls that survive this edit.)
    - `(cd examples/adoption_demo && mix compile)` exits 0 (warnings about `MyAppWeb.ProtectedApiReplayStore` are expected and acceptable; outright compilation failure is not). Note: the `cd` runs inside a subshell so the parent shell's cwd is automatically restored regardless of inner exit status (fix from revision WARNING #5 — prior form `cd ... && mix compile ; cd ../..` left cwd in `examples/adoption_demo` on partial failure).
  </acceptance_criteria>
  <done>
    The live demo router carries the canonical block marker-wrapped in 2-space module-body indent with `audience:` and `MyAppWeb.ProtectedApiReplayStore` placeholders; the demo app compiles (with the expected placeholder-warning); no other pipeline or route is touched; the post-edit `plug(` count is exactly 8 (BLOCKER #2 closure from revision iteration 1); the verify command uses subshell `cd` scope (WARNING #5 closure from revision iteration 1).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Insert the commented-out canonical block into the install-template heredoc before the first scope (per D-10 and RESEARCH Pitfall 7)</name>
  <files>priv/templates/lockspire.install/router.ex</files>
  <read_first>
    - priv/templates/lockspire.install/router.ex (the file being modified — read in full; 55 lines; the heredoc body runs L10-52)
    - .planning/phases/97-contract-docs-first/97-PATTERNS.md (the `priv/templates/lockspire.install/router.ex` row — exact insertion point, exact comment-prefix shape, exact marker placement)
    - .planning/phases/97-contract-docs-first/97-CONTEXT.md decision D-10 (commented-out Elixir inside the existing heredoc; Phase 102 SCAFFOLD-01 uncomments) AND decision D-02's clarifying note about the `:elixir_in_commented_heredoc` carrier kind also receiving the `# ` strip per-line during normalization
    - .planning/phases/97-contract-docs-first/97-RESEARCH.md `## Common Pitfalls` Pitfall 3 (no EEx tags inside canonical region) and Pitfall 7 (pipeline must be at module level, BEFORE the first scope)
    - .planning/phases/97-contract-docs-first/97-02-SUMMARY.md (canonical bytes seed)
  </read_first>
  <behavior>
    - After this edit, `priv/templates/lockspire.install/router.ex` heredoc body carries a commented-out canonical block between `# BEGIN LOCKSPIRE_PROTECTED_PIPELINE` and `# END LOCKSPIRE_PROTECTED_PIPELINE` markers, placed BEFORE the first `scope "/", <%= @web_module %> do` block (i.e., between heredoc-open `"""` at current L10 and current L11).
    - Each canonical-region interior line carries a 4-space heredoc-interior indent + `# ` Python-style comment prefix; the leading-space-and-`#` shape is identical to the existing commented admin-mount example at current L34-43.
    - The canonical region contains NO EEx tags (no `<%`, no `<%= ... %>`) — RESEARCH Pitfall 3 guard.
    - `mix compile --force --warnings-as-errors` of Lockspire itself exits 0 (the template file is not compiled at install time of Lockspire — it is read by the generator; compile-cleanness applies to Lockspire's own `mix compile` which treats this file as a plain `.ex` source).
    - The install-time generator output (after EEx renders) produces commented-out canonical lines in the generated host router, which is the desired Phase 97 → Phase 102 transition state (D-10).
  </behavior>
  <action>
    Edit `priv/templates/lockspire.install/router.ex` in place. Insert 9 lines between the current L10 (`    """`) and the current L11 (`    scope "/", <%= @web_module %> do`).

    Insertion content (each line shown with exact column-0 leading whitespace; the heredoc-interior indent is 4 spaces from column 0):

    New line A: `    # BEGIN LOCKSPIRE_PROTECTED_PIPELINE`
    New line B: `    # pipeline :lockspire_protected_api do`
    New line C: `    #   plug Lockspire.Plug.VerifyToken, scopes: ["read:billing"], audience: "billing-api"`
    New line D: `    #   plug Lockspire.Plug.EnforceSenderConstraints,`
    New line E: `    #     dpop_replay_store: MyAppWeb.ProtectedApiReplayStore`
    New line F: `    #   plug Lockspire.Plug.RequireToken`
    New line G: `    # end`
    New line H: `    # END LOCKSPIRE_PROTECTED_PIPELINE`
    New line I: blank line (preserves visual separation from the next heredoc content `scope "/", <%= @web_module %> do`)

    Key style points:
    - All 8 marker+content lines have a 4-space heredoc-interior leading indent (matches the existing heredoc body's column-4 leading indent at L11 and subsequent lines).
    - Marker lines `# BEGIN ...` and `# END ...` carry only the `# ` Python-style comment prefix — they are inert in any host syntax.
    - Content lines (B-G) carry the `# ` comment prefix PLUS the canonical-region body. The D-02 normalization helper strips the leading `# ` per line from THIS carrier (kind `:elixir_in_commented_heredoc` — per the D-02 clarifying note, the `# ` strip is applied to two carrier kinds: `:python_commented` and `:elixir_in_commented_heredoc`). After the strip, the extracted region for this file equals the canonical bytes.

    **D-02 normalization for the install-template file (clarified in revision iteration 1):** Plan 05's helper applies the leading-`# ` strip to TWO carrier kinds, NOT only the Python file. The two kinds that get the strip are `:python_commented` (the `.py` file) AND `:elixir_in_commented_heredoc` (this file, because D-10 makes its canonical block commented-out Elixir inside a heredoc). After the strip-uniform-indent step also runs, the extracted region for this file equals the canonical bytes byte-for-byte. The other two carriers (`:elixir_in_markdown_fence` for the doc page, `:elixir` for the live demo router) do NOT get the `# ` strip — their canonical regions are not comment-prefixed.

    (Recap of Plan 05's normalization map per the PATTERNS.md test snippet: `docs/protect-phoenix-api-routes.md` → kind `:elixir_in_markdown_fence` → no comment strip; `examples/adoption_demo/lib/adoption_demo_web/router.ex` → kind `:elixir` → no comment strip; `priv/templates/lockspire.install/router.ex` → kind `:elixir_in_commented_heredoc` → strip `# ` per line; `scripts/demo/adoption_smoke.py` → kind `:python_commented` → strip `# ` per line. The two strip-eligible kinds map to identical normalize-helper behavior in Plan 05.)

    Do NOT add EEx tags (`<%`, `<%=`) inside the canonical region. Do NOT remove the `# ` prefix on any content line (Phase 102 SCAFFOLD-01 owns the prefix removal). Do NOT touch any other line in the heredoc (admin-mount example at current L34-43, scope blocks, etc. all stay verbatim). Do NOT change the module wrapper or `lockspire_routes/0` function signature.

    No fenced code blocks in this action — every byte change is enumerated line-by-line above.
  </action>
  <verify>
    <automated>grep -c "BEGIN LOCKSPIRE_PROTECTED_PIPELINE" priv/templates/lockspire.install/router.ex ; grep -c "END LOCKSPIRE_PROTECTED_PIPELINE" priv/templates/lockspire.install/router.ex ; grep -c "# pipeline :lockspire_protected_api do" priv/templates/lockspire.install/router.ex ; grep -c "MyAppWeb.ProtectedApiReplayStore" priv/templates/lockspire.install/router.ex ; awk '/# BEGIN LOCKSPIRE_PROTECTED_PIPELINE/,/# END LOCKSPIRE_PROTECTED_PIPELINE/' priv/templates/lockspire.install/router.ex | grep -E '&lt;%|&lt;%=' ; mix compile --force 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BEGIN LOCKSPIRE_PROTECTED_PIPELINE" priv/templates/lockspire.install/router.ex` returns exactly `1`.
    - `grep -c "END LOCKSPIRE_PROTECTED_PIPELINE" priv/templates/lockspire.install/router.ex` returns exactly `1`.
    - `grep -c "# pipeline :lockspire_protected_api do" priv/templates/lockspire.install/router.ex` returns exactly `1` (the canonical-region body's first line, with `# ` prefix).
    - `grep -c "MyAppWeb.ProtectedApiReplayStore" priv/templates/lockspire.install/router.ex` returns exactly `1` (placeholder present, commented-out).
    - The EEx-tag pre-flight uses the range-pattern awk filter `awk '/# BEGIN LOCKSPIRE_PROTECTED_PIPELINE/,/# END LOCKSPIRE_PROTECTED_PIPELINE/' priv/templates/lockspire.install/router.ex | grep -E '<%|<%='` (revision iteration 1 fix for WARNING #3 — the prior `awk '... $2>=1 && $2<=99 ...' -F:` form filtered on `-F:` grep CONTENT field, not line numbers, so the range filter was a no-op). Expected: EMPTY OUTPUT (grep exits 1 because awk's range-extract returns the BEGIN/END region, none of which contains `<%` or `<%=`). Non-empty output means an EEx tag leaked into the canonical region → FAIL → executor must remove the EEx interpolation and re-verify.
    - `mix compile --force` exits 0 (Lockspire's `mix compile` treats this template file as a plain `.ex` file; the heredoc string contents are inert at Lockspire-compile-time).
  </acceptance_criteria>
  <done>
    The install template heredoc carries the commented-out canonical block between BEGIN/END markers at the safe pre-scope insertion point; the range-pattern awk pre-flight confirms no EEx tags inside the region (WARNING #3 closure from revision iteration 1); `mix compile` succeeds.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Insert the Python-comment canonical-block carrier into the smoke script adjacent to the protected-API exercise (per D-03 and D-14)</name>
  <files>scripts/demo/adoption_smoke.py</files>
  <read_first>
    - scripts/demo/adoption_smoke.py (the file being modified — read L235-250 to confirm the insertion-point context; the `exercise_authorization_code` function spans L173-246; the protected-API line is L244)
    - .planning/phases/97-contract-docs-first/97-PATTERNS.md (the `scripts/demo/adoption_smoke.py` row — exact target shape, exact 4-space + `# ` per-line prefix, exact insertion point above L244)
    - .planning/phases/97-contract-docs-first/97-CONTEXT.md decisions D-03 (Python-comment carrier shape) and D-14 (adjacent to `exercise_authorization_code` at L244)
    - .planning/phases/97-contract-docs-first/97-02-SUMMARY.md (canonical bytes seed)
  </read_first>
  <behavior>
    - After this edit, `scripts/demo/adoption_smoke.py` carries a Python-comment block between `# BEGIN LOCKSPIRE_PROTECTED_PIPELINE` and `# END LOCKSPIRE_PROTECTED_PIPELINE` markers, placed immediately above the current L244 `anonymous_api = Browser(BASE_URL).request(...)` line inside the `exercise_authorization_code` function.
    - Each interior canonical-region line carries a 4-space leading indent (Python function-body indent) + `# ` Python comment prefix.
    - The script remains valid Python: `python -c 'import ast; ast.parse(open("scripts/demo/adoption_smoke.py").read())'` exits 0.
    - The smoke's runtime behavior is unchanged — comment-prefixed lines have no execution effect.
    - The function `exercise_authorization_code` continues to terminate normally (no syntax error from the insertion).
  </behavior>
  <action>
    Edit `scripts/demo/adoption_smoke.py` in place. Insert 9 lines between the current L243 (the `assert userinfo_json["email"] == "alice@acme.test"` line that closes the userinfo block) and the current L244 (the `anonymous_api = Browser(BASE_URL).request(...)` line that starts the protected-API exercise).

    Insertion content (each line shown with exact column-0 leading whitespace; the Python function-body indent is 4 spaces):

    New line A: blank line (preserves visual separation from the preceding userinfo block)
    New line B: `    # BEGIN LOCKSPIRE_PROTECTED_PIPELINE`
    New line C: `    # pipeline :lockspire_protected_api do`
    New line D: `    #   plug Lockspire.Plug.VerifyToken, scopes: ["read:billing"], audience: "billing-api"`
    New line E: `    #   plug Lockspire.Plug.EnforceSenderConstraints,`
    New line F: `    #     dpop_replay_store: MyAppWeb.ProtectedApiReplayStore`
    New line G: `    #   plug Lockspire.Plug.RequireToken`
    New line H: `    # end`
    New line I: `    # END LOCKSPIRE_PROTECTED_PIPELINE`

    Then a blank line (which is essentially preserved — the current L244 already follows a blank line at L243; the structure post-insertion is: closing userinfo line → blank → BEGIN marker → 6 content lines → END marker → blank → `anonymous_api = ...`).

    Key style points:
    - All marker+content lines have a 4-space leading indent (matches the existing function-body indent at L240-244).
    - Marker lines `# BEGIN ...` and `# END ...` are pure Python comments (column-4 `# ` + marker token), inert.
    - Content lines (C-H) are pure Python comments (column-4 `# ` + canonical-region body line); they cause no runtime behavior change.
    - After D-02 normalization for THIS file (strip `# ` prefix per line; strip 4-space uniform indent; LF normalize; trailing whitespace strip), the extracted region matches the canonical bytes shown in `<interfaces>` byte-for-byte.

    The canonical bytes shown in `<interfaces>` have a 2-space indent on the `plug ...` lines (matches a `pipeline ... do ... end` body). After the Python normalization stripping `# ` then strip-uniform-indent, the 4-space Python-function-body indent disappears, the `# ` prefix disappears, but the 2-space pipeline-body indent on the `plug` lines REMAINS — which matches the canonical bytes exactly.

    Self-check: line D in the insertion has the content `# pipeline :lockspire_protected_api do` (after the `# ` strip: `pipeline :lockspire_protected_api do`); this matches the canonical bytes' line 1 exactly. Line F has `# &nbsp;&nbsp;plug Lockspire.Plug.VerifyToken, ...` (after the strip: `&nbsp;&nbsp;plug Lockspire.Plug.VerifyToken, ...`); this matches canonical line 2 exactly. And so on for each line.

    Do NOT touch any other line in the file. Do NOT modify the `exercise_authorization_code` function signature or any of its other body lines. Do NOT add a separate Python-comment block at the top of the file or anywhere else — the canonical region is SINGLE-OCCURRENCE per file.

    No fenced code blocks in this action — every byte change is enumerated line-by-line above.
  </action>
  <verify>
    <automated>grep -c "BEGIN LOCKSPIRE_PROTECTED_PIPELINE" scripts/demo/adoption_smoke.py ; grep -c "END LOCKSPIRE_PROTECTED_PIPELINE" scripts/demo/adoption_smoke.py ; grep -c "# pipeline :lockspire_protected_api do" scripts/demo/adoption_smoke.py ; grep -c "MyAppWeb.ProtectedApiReplayStore" scripts/demo/adoption_smoke.py ; python3 -c "import ast; ast.parse(open('scripts/demo/adoption_smoke.py').read()); print('python_parse_ok')" 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BEGIN LOCKSPIRE_PROTECTED_PIPELINE" scripts/demo/adoption_smoke.py` returns exactly `1`.
    - `grep -c "END LOCKSPIRE_PROTECTED_PIPELINE" scripts/demo/adoption_smoke.py` returns exactly `1`.
    - `grep -c "# pipeline :lockspire_protected_api do" scripts/demo/adoption_smoke.py` returns exactly `1` (the canonical-region body's first line, prefixed `# `).
    - `grep -c "MyAppWeb.ProtectedApiReplayStore" scripts/demo/adoption_smoke.py` returns exactly `1`.
    - `python3 -c "import ast; ast.parse(open('scripts/demo/adoption_smoke.py').read()); print('python_parse_ok')"` prints `python_parse_ok` and exits 0 (the script is still parseable Python after the insertion).
  </acceptance_criteria>
  <done>
    The Python smoke carries the canonical-block Python-comment carrier inside `exercise_authorization_code` immediately above the protected-API exercise line; the script still parses as valid Python; the `# ` prefix on every content line allows D-02 normalization to produce the canonical bytes.
  </done>
</task>

</tasks>

<threat_model>
Phase 97 does not introduce new attack surface (per 97-RESEARCH.md `## Security Domain`). This plan edits three files: a live Phoenix router (no auth/input handling changes; only the canonical pipeline declaration is restructured into marker-wrapped form with a placeholder module name), an EEx-interpolated install template (commented-out content is inert in any generated host; no EEx tag introduced inside the canonical region — RESEARCH Pitfall 3 guard), and a Python smoke script (comment-prefixed lines change no runtime behavior). The `MyAppWeb.ProtectedApiReplayStore` placeholder is a documentation-canonical name; Phase 101 wires the demo-side alias. Phase 98 (V5 Input Validation for the hardened plug) and Phase 99 (V6 Cryptography for the extracted `Protocol.AccessTokenSigner`) carry the security work for v1.27.
</threat_model>

<verification>
- All three carrier files carry exactly one BEGIN/END marker pair each.
- The demo app compiles (with the expected `MyAppWeb.ProtectedApiReplayStore` placeholder warning).
- Lockspire itself compiles cleanly (`mix compile --force` exits 0).
- The Python script still parses (`ast.parse` succeeds).
- No EEx tags inside the install-template canonical region (verified via the range-pattern awk pre-flight introduced in revision iteration 1).
- The canonical-region interiors, after D-02 normalization, are byte-identical across all four files (this is verified for-real by Plan 05's content-hash test; Plan 04 establishes the bytes).
</verification>

<success_criteria>
- Plan 04 is COMPLETE when:
  1. The three carrier files each carry one `# BEGIN LOCKSPIRE_PROTECTED_PIPELINE` line and one `# END LOCKSPIRE_PROTECTED_PIPELINE` line surrounding the canonical-region interior.
  2. The interior bytes in each file, after D-02 normalization (strip `# ` for `:python_commented`-kind AND `:elixir_in_commented_heredoc`-kind carriers; strip uniform leading indent for all carriers; LF normalization; trailing whitespace strip), are byte-identical to the docs page canonical bytes from Plan 02.
  3. The demo app compiles (warnings about `MyAppWeb.ProtectedApiReplayStore` are expected; outright failure is not). The post-edit `plug(` count for the demo router file is exactly `8` (= pre-edit 11 − 3 parens-dropped). The verify command uses subshell `cd` scope.
  4. Lockspire's own `mix compile` is clean.
  5. The Python smoke script remains parseable.
  6. The range-pattern awk pre-flight on the install template confirms no EEx tags inside the canonical region.
  7. Plan 05 can now build the content-hash test clause against the four established sites.
</success_criteria>

<output>
Create `.planning/phases/97-contract-docs-first/97-04-SUMMARY.md` when done. The summary MUST record: (a) the exact post-edit line numbers of each BEGIN/END marker pair in each of the three carrier files; (b) the exact byte sequence of the canonical region interior in each carrier file (so Plan 05's executor can sanity-check the four-file byte-equality before running the test); (c) the per-file normalization that Plan 05 must apply to each carrier kind (the kind name and the strip transformations); (d) confirmation that each carrier file's compile/parse acceptance test passed; (e) note the `MyAppWeb.ProtectedApiReplayStore` undefined-module warning in the demo app's compile output as expected behavior (not a regression); (f) the observed `grep -cE 'plug\(' examples/adoption_demo/lib/adoption_demo_web/router.ex` count post-edit (expected: `8`) and confirmation that the parens-drop landed on exactly three :lockspire_protected_api plug calls; (g) the observed output of the range-pattern awk EEx pre-flight on the install template (expected: empty).
</output>
</output>
