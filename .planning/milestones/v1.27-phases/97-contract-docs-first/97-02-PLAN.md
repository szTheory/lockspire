---
phase: 97-contract-docs-first
plan: 02
type: execute
wave: 2
depends_on:
  - 97-01
files_modified:
  - docs/protect-phoenix-api-routes.md
autonomous: true
requirements:
  - DOCS-01
  - RECIPE-01
tags:
  - phase-97
  - docs-01
  - canonical-page
  - recipe-01
user_setup: []

must_haves:
  truths:
    - "An adopter reading `docs/protect-phoenix-api-routes.md` finds the D-06 contract sentence verbatim as the lead paragraph (immediately after the H1 title)."
    - "Immediately after the D-06 contract lead, the page carries the D-07 forward-reference caveat sentence verbatim, preceded by the HTML-comment sweep marker `<!-- PHASE-102: delete this caveat sentence when issuance flip ships -->` for Phase 102's planner."
    - "The Phase 92 cross-link line `For the public support contract around this surface, see [\`docs/supported-surface.md\`](supported-surface.md).` still appears verbatim on the page (D-08)."
    - "The Phase 92 substring `error=\"use_dpop_nonce\"` still appears in the rewritten failure-behavior table."
    - "The Phase 92 substrings `no-op for unconstrained bearer tokens`, `business authorization`, `tenant checks`, and the three plug names `Lockspire.Plug.VerifyToken` / `Lockspire.Plug.EnforceSenderConstraints` / `Lockspire.Plug.RequireToken` all survive the rewrite. (Ground truth note 2026-05-27: `tenant checks` exists today ONLY at L3 of the file in the literal lowercase form, and L3 is the line this plan REWRITES — so this plan MUST re-inject the lowercase substring `tenant checks` in the introductory prose of the canonical-plug-order section per Task 1 Step 3 below. `business authorization` exists today at L3 AND L22; L22 is preserved per D-05, so re-injection of `business authorization` at Step 3 is belt-and-suspenders. The Ownership boundary section at L89-104 carries CAPITALIZED `Business authorization` / `Tenant and account policy` forms which do NOT match the literal lowercase Phase 92 substrings.)"
    - "The first fenced Elixir code block on the page (the canonical pipeline) is wrapped in `# BEGIN LOCKSPIRE_PROTECTED_PIPELINE` / `# END LOCKSPIRE_PROTECTED_PIPELINE` marker comment lines inside the fence; interior bytes carry the canonical pipeline declaration with `audience: \"billing-api\"` (per D-13) and `dpop_replay_store: MyAppWeb.ProtectedApiReplayStore` (per D-04)."
    - "The two SECONDARY fenced Elixir blocks (currently the scope-restricted example and the audience-restricted example) are rewritten to REFERENCE the canonical block (cross-link prose) and do NOT restate the three plug names verbatim (per D-15)."
    - "Running `mix test test/lockspire/release_readiness_contract_test.exs:642` after this plan (and Plan 03) turns GREEN — every D-06/D-07 substring added by Plan 01 is now present in this file."
  artifacts:
    - path: "docs/protect-phoenix-api-routes.md"
      provides: "Single authoritative protected-route doc page anchoring the v1.27 plug contract; first content-hashed canonical pipeline site of four."
      contains: "# BEGIN LOCKSPIRE_PROTECTED_PIPELINE"
  key_links:
    - from: "docs/protect-phoenix-api-routes.md (lead paragraph)"
      to: ".planning/phases/97-contract-docs-first/97-CONTEXT.md D-06"
      via: "verbatim contract sentence"
      pattern: "Lockspire issues RFC 9068 .at\\+jwt. access tokens by default"
    - from: "docs/protect-phoenix-api-routes.md (canonical fenced Elixir block, ~L11-18 today)"
      to: "examples/adoption_demo/lib/adoption_demo_web/router.ex, priv/templates/lockspire.install/router.ex, scripts/demo/adoption_smoke.py (Plan 04)"
      via: "byte-identical interior (after D-02 normalization) between BEGIN/END markers"
      pattern: "BEGIN LOCKSPIRE_PROTECTED_PIPELINE.*END LOCKSPIRE_PROTECTED_PIPELINE"
---

<objective>
Rewrite `docs/protect-phoenix-api-routes.md` per D-05 (section-level rewrite of lead, canonical-plug-order, and failure table; assigns-contract, ownership-boundary, and repo-owned-proof sections stay intact). Insert the D-06 contract sentence verbatim as the lead, append the D-07 caveat sentence with HTML-comment sweep marker, and wrap the first fenced Elixir canonical pipeline block in `BEGIN`/`END` marker comment lines. Rewrite the two secondary fenced blocks per D-15 to reference the canonical pipeline (cross-link prose) rather than restate the three plug names.

Purpose: Make this page the single authoritative protected-route contract for v1.27. After this plan plus Plan 03, the test that Plan 01 RED-fails turns GREEN.

Output: One modified doc file. First of four canonical-block sites established; remaining three carriers land in Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-contract-docs-first/97-CONTEXT.md
@.planning/phases/97-contract-docs-first/97-RESEARCH.md
@.planning/phases/97-contract-docs-first/97-PATTERNS.md
@.planning/phases/97-contract-docs-first/97-VALIDATION.md
@.planning/phases/97-contract-docs-first/97-01-SUMMARY.md
@.planning/phases/92-advanced-setup-support-truth/92-CONTEXT.md

<interfaces>
<!-- Phase 92 contract substrings that MUST survive the rewrite. Source: test/support/advanced_setup_support_truth.ex `assert_protected_routes_guide!/1` L69-80. -->

Phase 92 substrings (8) — these MUST remain anywhere on the page after the rewrite:

```
"For the public support contract around this surface, see [`docs/supported-surface.md`](supported-surface.md)."
"Lockspire.Plug.VerifyToken"
"Lockspire.Plug.EnforceSenderConstraints"
"Lockspire.Plug.RequireToken"
"no-op for unconstrained bearer tokens"
"error=\"use_dpop_nonce\""
"business authorization"
"tenant checks"
```

Phase 97 contract substrings (Plan 01 already added these to the helper; Plan 02 makes them present in the doc):

```
"Lockspire issues RFC 9068 `at+jwt` access tokens by default."                        (D-06 sentence 1, verbatim)
"`Lockspire.Plug.VerifyToken` accepts JWT bearer tokens for host Phoenix API routes." (D-06 sentence 2, verbatim)
"Lockspire-owned `/userinfo` and `/introspect` use stored opaque tokens; those are not interchangeable." (D-06 sentence 3, verbatim)
"To opt a client back to opaque, see the admin Client Detail page."                   (D-06 sentence 4, verbatim)
"the runtime narrowing and the default-issuance flip land in v1.27"                   (D-07 caveat midphrase)
"opaque tokens may still be silently accepted on these routes"                        (D-07 caveat tail)
```

Phase 97 marker comments inside the first fenced Elixir code block (wrap the interior pipeline declaration; markers are visible inside the fenced block but are inert Elixir line comments so the rendered example reads cleanly):

```
# BEGIN LOCKSPIRE_PROTECTED_PIPELINE
pipeline :lockspire_protected_api do
  plug Lockspire.Plug.VerifyToken, scopes: ["read:billing"], audience: "billing-api"
  plug Lockspire.Plug.EnforceSenderConstraints,
    dpop_replay_store: MyAppWeb.ProtectedApiReplayStore
  plug Lockspire.Plug.RequireToken
end
# END LOCKSPIRE_PROTECTED_PIPELINE
```

Phase 97 HTML-comment Phase-102 sweep marker (precedes the D-07 caveat sentence in the markdown source; invisible in rendered output; greppable for Phase 102's planner):

```
<!-- PHASE-102: delete this caveat sentence when issuance flip ships -->
```

Current page structure (verified by direct file read, 2026-05-27; preserved per D-05 except where noted):

- L1: H1 title `# Protect Phoenix API Routes` (PRESERVE)
- L3: lead paragraph (REWRITE — replace with D-06 contract sentence; immediately followed by HTML sweep marker + D-07 caveat sentence)
  IMPORTANT GROUND TRUTH: L3 is the ONLY line in the current file carrying the literal lowercase substring `tenant checks`. Because the L3 lead is wholesale replaced by the D-06 contract sentence, the lowercase substring `tenant checks` will VANISH from the page unless re-injected. Task 1 Step 3 below re-injects it (and re-injects `business authorization`) in the introductory prose of the canonical-plug-order section.
- L5: existing cross-link `For the public support contract around this surface, see ...` (PRESERVE verbatim per D-08)
- L7-18: `## Canonical plug order` section (REWRITE the section but the first fenced Elixir block at L11-18 becomes the canonical-block carrier with BEGIN/END markers; the canonical pipeline declaration body is byte-identical to the snippet shown above)
- L20-24: prose about each plug's responsibility (PRESERVE — carries `no-op for unconstrained bearer tokens` substring and the lowercase `business authorization` substring at L22)
- L26-37: `## Example route` section (PRESERVE)
- L39-49: `## Scope-restricted route example` (REWRITE per D-15 — replace fenced block restating three plug names with a reference-to-canonical cross-link sentence; keep the section heading)
- L51-62: `## Audience-restricted route example` (REWRITE per D-15 — same treatment)
- L64-77: `## Access-token assigns contract` (PRESERVE; L77 sentence uses the phrase `this tenant` and `internal policy checks` — NOT the literal lowercase substring `tenant checks`)
- L79-87: `## Failure behavior` table (REWRITE per D-05 BUT the substring `error="use_dpop_nonce"` MUST survive somewhere in the table)
- L89-104: `## Ownership boundary` (PRESERVE; uses CAPITALIZED `Business authorization` at L100 and `Tenant and account policy` at L101 — these are NOT the literal lowercase Phase 92 substrings `business authorization` / `tenant checks`)
- L106-112: `## Repo-owned proof` (PRESERVE)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert D-06 contract lead + D-07 caveat + canonical-block marker wrap into the rewritten lead and canonical-plug-order sections</name>
  <files>docs/protect-phoenix-api-routes.md</files>
  <read_first>
    - docs/protect-phoenix-api-routes.md (the file being modified — read in full; current structure verified at L1-113)
    - .planning/phases/97-contract-docs-first/97-PATTERNS.md (the `docs/protect-phoenix-api-routes.md` row — exact substrings to insert, exact substrings to preserve, exact placement of D-06 contract sentence and D-07 caveat)
    - .planning/phases/97-contract-docs-first/97-CONTEXT.md decisions D-05, D-06, D-07, D-08 (verbatim source of the contract sentence and caveat; section-rewrite-depth lock)
    - .planning/phases/97-contract-docs-first/97-RESEARCH.md `## Common Pitfalls` Pitfall 5 (Phase 92 substring regression risk) and Pitfall 6 (D-07 caveat sweep marker) — both directly relevant to this task
    - test/support/advanced_setup_support_truth.ex (read the post-Plan-01 substring lists in `assert_protected_routes_guide!/1`; each substring there MUST end up somewhere in this doc after the rewrite)
    - .planning/phases/97-contract-docs-first/97-01-SUMMARY.md (Plan 01 records the first RED-failing substring — start by making THAT one true, then verify by running the test, then proceed through the rest)
  </read_first>
  <behavior>
    - After edit: `mix test test/lockspire/release_readiness_contract_test.exs:642` MOVES PAST the assertions against `assert_protected_routes_guide!/1` (i.e., it stops failing on substrings from this doc). It will still RED-fail until Plan 03 lands the four D-09 non-goal bullets in `docs/supported-surface.md`. Acceptance is "the failure cited substring is no longer one of the D-06 or D-07 substrings nor any Phase 92 substring".
    - The D-06 contract sentence appears verbatim as the lead paragraph (the only paragraph between the H1 title at L1 and the existing cross-link line at L5 today). Sentence is four discrete sentences joined by spaces; total length ~280 characters.
    - The D-07 caveat sentence appears verbatim immediately after the D-06 contract lead, in its own paragraph, preceded by the HTML-comment sweep marker `<!-- PHASE-102: delete this caveat sentence when issuance flip ships -->` on its own line.
    - The existing cross-link `For the public support contract around this surface, see [\`docs/supported-surface.md\`](supported-surface.md).` is preserved verbatim.
    - The first fenced Elixir code block (currently L11-18, the canonical pipeline) now has `# BEGIN LOCKSPIRE_PROTECTED_PIPELINE` and `# END LOCKSPIRE_PROTECTED_PIPELINE` lines inside the fence wrapping the `pipeline :lockspire_protected_api do ... end` declaration. The declaration body uses `audience: "billing-api"` and `dpop_replay_store: MyAppWeb.ProtectedApiReplayStore` (these are already present at L13 and L15 today — verified by direct file read; rewrite preserves them).
    - Style normalization for the canonical pipeline block: NO parentheses on `plug` calls (matches current docs L12-16 form). Two-space indent inside the `pipeline do ... end` block. Continuation lines for multi-line plug calls indented 4 spaces (matches current L15 form).
    - Page contains exactly ONE occurrence of the string `BEGIN LOCKSPIRE_PROTECTED_PIPELINE` and exactly ONE occurrence of `END LOCKSPIRE_PROTECTED_PIPELINE` after this task (Plan 04 adds the other three sites; Plan 02 only touches this doc).
    - The substring `no-op for unconstrained bearer tokens` (Phase 92 contract) is preserved somewhere on the page — the simplest preservation is to keep L22's prose paragraph about `EnforceSenderConstraints` largely intact; D-05 marks that paragraph as PRESERVE.
    - Failure-behavior table at L79-87 may be rewritten but the substring `error="use_dpop_nonce"` (Phase 92) MUST appear somewhere in the table after the rewrite.
    - The literal lowercase substring `tenant checks` appears at least once on the rewritten page (re-injected in Step 3's introductory prose; see Step 3 below). Without re-injection, this substring vanishes because L3 (its only current home in lowercase form) is wholesale replaced.
    - The literal lowercase substring `business authorization` appears at least once on the rewritten page (preserved at L22 by D-05; ALSO re-injected in Step 3's introductory prose as belt-and-suspenders).
  </behavior>
  <action>
    Edit `docs/protect-phoenix-api-routes.md` in place. Apply the section-level rewrite per D-05.

    Step 1 (lead paragraph rewrite, replaces current L3): Replace the existing one-paragraph lead with TWO consecutive paragraphs separated by one blank line:

    Paragraph 1 (the D-06 contract sentence, verbatim, on a single line — line-wrap is the renderer's job):
    `Lockspire issues RFC 9068 \`at+jwt\` access tokens by default. \`Lockspire.Plug.VerifyToken\` accepts JWT bearer tokens for host Phoenix API routes. Lockspire-owned \`/userinfo\` and \`/introspect\` use stored opaque tokens; those are not interchangeable. To opt a client back to opaque, see the admin Client Detail page.`

    Paragraph 2 (the HTML-comment sweep marker on its own line, then a blank line, then the D-07 caveat sentence verbatim):
    Line A: `<!-- PHASE-102: delete this caveat sentence when issuance flip ships -->`
    Line B: blank
    Line C: `This page describes the contract \`Lockspire.Plug.VerifyToken\` enforces; the runtime narrowing and the default-issuance flip land in v1.27. Until v1.27 is fully shipped, opaque tokens may still be silently accepted on these routes.`

    Step 2 (preserve the existing cross-link line, per D-08): Leave the existing `For the public support contract around this surface, see [\`docs/supported-surface.md\`](supported-surface.md).` line unchanged in its current position (will be at the new L7-ish after Step 1 inserts new lines above it).

    Step 3 (canonical-plug-order section rewrite + marker wrap): Keep the `## Canonical plug order` heading.

    Replace the body up to the end of the first fenced Elixir code block with the following sequence:

    First, ONE prose sentence introducing the canonical pipeline. This sentence has THREE load-bearing substring requirements (planner's discretion on exact wording, but ALL THREE substring requirements MUST hold):

    Requirement R1: The introductory paragraph MUST contain all three plug-name substrings: `Lockspire.Plug.VerifyToken`, `Lockspire.Plug.EnforceSenderConstraints`, `Lockspire.Plug.RequireToken` (Phase 92 contract).

    Requirement R2: The introductory paragraph MUST contain the literal lowercase substring `business authorization` (Phase 92 contract).

    Requirement R3: The introductory paragraph MUST contain the literal lowercase substring `tenant checks` (Phase 92 contract). Without this, the lowercase substring vanishes from the page entirely because L3 (its only current home in lowercase form, in the soon-to-be-rewritten lead) is wholesale replaced by the D-06 contract sentence.

    Cleanest discharge of R1+R2+R3 in a single sentence (recommended wording — matches the cadence of the L22 prose already in the file): `Lockspire enforces the token contract via \`Lockspire.Plug.VerifyToken\`, \`Lockspire.Plug.EnforceSenderConstraints\`, and \`Lockspire.Plug.RequireToken\`; your host application keeps ownership of business authorization and tenant checks.`

    Alternative two-sentence wording (also fine): `The canonical pipeline applies \`Lockspire.Plug.VerifyToken\`, \`Lockspire.Plug.EnforceSenderConstraints\`, and \`Lockspire.Plug.RequireToken\` in that order. Lockspire enforces token validity; your host app retains business authorization and tenant checks as host-owned concerns.`

    Either wording satisfies R1+R2+R3. The executor picks one; do not invent a wording that omits the lowercase `tenant checks` or `business authorization` substrings.

    Then the fenced Elixir block, which becomes:

    Line A: ```` ```elixir ```` (fence open with elixir language tag, unchanged from today)
    Line B: `# BEGIN LOCKSPIRE_PROTECTED_PIPELINE`
    Line C: `pipeline :lockspire_protected_api do`
    Line D: `  plug Lockspire.Plug.VerifyToken, scopes: ["read:billing"], audience: "billing-api"`
    Line E: `  plug Lockspire.Plug.EnforceSenderConstraints,`
    Line F: `    dpop_replay_store: MyAppWeb.ProtectedApiReplayStore`
    Line G: `  plug Lockspire.Plug.RequireToken`
    Line H: `end`
    Line I: `# END LOCKSPIRE_PROTECTED_PIPELINE`
    Line J: ```` ``` ```` (fence close, unchanged from today)

    Note that lines B and I are Elixir line-comments inside the fenced code block — they render as part of the example but are inert (any reader copying the example to their router gets two comment lines they can delete). Lines C-H are the canonical bytes that will be hashed by Plan 05; they exactly match the current declaration at L12-17 of the existing file (audience already present per D-13; placeholder name already present per D-04).

    Step 4 (preserve the EnforceSenderConstraints prose at current L22 — D-05 marks this as PRESERVE): Leave the paragraph that begins `Lockspire.Plug.EnforceSenderConstraints is part of the canonical shipped path...` intact. This paragraph carries the Phase 92 substrings `no-op for unconstrained bearer tokens`, `error="use_dpop_nonce"` (also in the failure table), and the literal lowercase `business authorization` substring at L22; preserving the prose is the surest preservation.

    Step 5 (preserve `## Example route` at current L26-37): No change.

    Step 6 (rewrite secondary fenced blocks per D-15 — `## Scope-restricted route example` currently L39-49): Keep the heading. Replace the entire body (the fenced Elixir block at current L41-47 plus the prose at L49) with ONE prose sentence — planner's discretion on exact wording, but the body MUST NOT contain a fenced ` ```elixir ... ``` ` block restating any of the three plug names. Recommended wording: `See the canonical pipeline above; this example narrows it to a single \`scopes:\` value (e.g., \`scopes: ["read:billing"]\`) with no \`audience:\` restriction. Keep \`Lockspire.Plug.EnforceSenderConstraints\` in the pipeline even on bearer-only routes so the route stays correct when sender-constrained tokens arrive later.` The clause `Lockspire.Plug.EnforceSenderConstraints` mention here is fine — it is mentioning a plug name in cross-reference prose, not restating a three-plug pipeline declaration. The D-15 spirit is "no second fenced-block restatement of the three-plug declaration block within this file".

    Step 7 (rewrite secondary fenced blocks per D-15 — `## Audience-restricted route example` currently L51-62): Same treatment as Step 6. Recommended wording: `See the canonical pipeline above; this example pins \`audience:\` (e.g., \`audience: "billing-api"\`) to constrain the route to tokens minted for a specific resource server. Route-level audience checks are exact-match against the token \`aud\` set.`

    Step 8 (preserve `## Access-token assigns contract` at current L64-77): No change. Ground truth note (verified 2026-05-27): the L77 sentence in this section uses the phrases `this tenant` and `internal policy checks` — it does NOT carry the literal lowercase substring `tenant checks`. The literal-lowercase `tenant checks` substring is preserved on the page exclusively via Step 3's R3 re-injection.

    Step 9 (rewrite `## Failure behavior` table per D-05): Keep the heading and the markdown table structure. The table may be reordered, reworded, or restructured BUT one cell in the table MUST contain the substring `error="use_dpop_nonce"` (Phase 92 contract). Recommendation: keep the current table as-is — it already reads cleanly and contains the substring; D-05 marks the table as REWRITE-eligible, not REWRITE-mandatory, so a minimal-touch preservation is acceptable. If a rewrite is desired, the simplest change is to add a clarifying header note above the table reinforcing the D-06 contract; the table rows stay verbatim.

    Step 10 (preserve `## Ownership boundary` at current L89-104): No change. Ground truth note (verified 2026-05-27): this section uses CAPITALIZED `Business authorization` (L100) and `Tenant and account policy` (L101). These do NOT match the literal lowercase Phase 92 substrings `business authorization` / `tenant checks`. The lowercase substrings are preserved via Step 3's R2+R3 re-injection plus the L22 paragraph carrying `business authorization` (preserved per Step 4).

    Step 11 (preserve `## Repo-owned proof` at current L106-112): No change.

    Do NOT add a new section. Do NOT add additional fenced Elixir code blocks anywhere on the page. After this task, the page contains EXACTLY ONE fenced Elixir code block (the canonical one in the canonical-plug-order section). Secondary "example" sections become prose-only.

    No fenced code blocks in this action — the implementation values appear inline above; the executor reads them from `<interfaces>` and the patterns file.
  </action>
  <verify>
    <automated>grep -c "BEGIN LOCKSPIRE_PROTECTED_PIPELINE" docs/protect-phoenix-api-routes.md ; grep -c "END LOCKSPIRE_PROTECTED_PIPELINE" docs/protect-phoenix-api-routes.md ; grep -c "Lockspire issues RFC 9068" docs/protect-phoenix-api-routes.md ; grep -c "the runtime narrowing and the default-issuance flip land in v1.27" docs/protect-phoenix-api-routes.md ; grep -c "PHASE-102: delete this caveat sentence when issuance flip ships" docs/protect-phoenix-api-routes.md ; grep -c "error=\"use_dpop_nonce\"" docs/protect-phoenix-api-routes.md ; grep -c "no-op for unconstrained bearer tokens" docs/protect-phoenix-api-routes.md ; grep -c "tenant checks" docs/protect-phoenix-api-routes.md ; grep -c "business authorization" docs/protect-phoenix-api-routes.md ; grep -c '^```elixir' docs/protect-phoenix-api-routes.md ; mix test test/lockspire/release_readiness_contract_test.exs:642 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BEGIN LOCKSPIRE_PROTECTED_PIPELINE" docs/protect-phoenix-api-routes.md` returns exactly `1`.
    - `grep -c "END LOCKSPIRE_PROTECTED_PIPELINE" docs/protect-phoenix-api-routes.md` returns exactly `1`.
    - `grep -c "Lockspire issues RFC 9068" docs/protect-phoenix-api-routes.md` returns exactly `1` (D-06 sentence 1).
    - `grep -c "the runtime narrowing and the default-issuance flip land in v1.27" docs/protect-phoenix-api-routes.md` returns exactly `1` (D-07 caveat midphrase).
    - `grep -c "PHASE-102: delete this caveat sentence when issuance flip ships" docs/protect-phoenix-api-routes.md` returns exactly `1` (HTML sweep marker).
    - `grep -c 'error=\"use_dpop_nonce\"' docs/protect-phoenix-api-routes.md` returns at least `1` (Phase 92 substring preserved).
    - `grep -c "no-op for unconstrained bearer tokens" docs/protect-phoenix-api-routes.md` returns exactly `1` (Phase 92 substring preserved).
    - `grep -c "tenant checks" docs/protect-phoenix-api-routes.md` returns AT LEAST `1` (Phase 92 substring re-injected via Step 3 R3). This is the load-bearing acceptance criterion for BLOCKER #1 from the revision context: without the Step 3 re-injection, this count is `0` because the only pre-edit home of the lowercase substring was L3, which Step 1 wholesale replaces.
    - `grep -c "business authorization" docs/protect-phoenix-api-routes.md` returns AT LEAST `1` (Phase 92 substring; preserved at L22 by Step 4 AND re-injected at Step 3 R2 as belt-and-suspenders).
    - `grep -c '^```elixir' docs/protect-phoenix-api-routes.md` returns exactly `1` (only the canonical block remains as a fenced Elixir example; D-15 secondary-block restatements gone).
    - After Plan 03 also lands, `mix test test/lockspire/release_readiness_contract_test.exs:642` exits 0; after THIS plan alone, the failure (if any) is on a `docs/supported-surface.md` substring (D-09), never on a `docs/protect-phoenix-api-routes.md` substring.
    - The Phase 92 substrings `Lockspire.Plug.VerifyToken`, `Lockspire.Plug.EnforceSenderConstraints`, `Lockspire.Plug.RequireToken`, `business authorization`, `tenant checks` all return at least `1` from `grep -c` against the file.
  </acceptance_criteria>
  <done>
    The page lead carries the D-06 contract sentence verbatim and the D-07 caveat with its sweep marker; the canonical pipeline block is marker-wrapped with `audience:` and the `MyAppWeb.ProtectedApiReplayStore` placeholder; the two secondary fenced blocks are gone (replaced by reference-to-canonical prose); every Phase 92 substring survives, including the lowercase `tenant checks` re-injected by Step 3 R3 (BLOCKER #1 closure from revision iteration 1).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify D-12 / D-15 spirit (zero fifth-restatement on this page) and confirm Plan 01's red test now passes for protected-routes-guide assertions</name>
  <files>docs/protect-phoenix-api-routes.md</files>
  <read_first>
    - docs/protect-phoenix-api-routes.md (the file just modified by Task 1)
    - .planning/phases/97-contract-docs-first/97-CONTEXT.md decisions D-12 and D-15 (no fifth restatement; secondary blocks reference, not restate)
    - .planning/phases/97-contract-docs-first/97-RESEARCH.md `### Evidence 9` (the existing-grep snapshot to compare against)
    - test/support/advanced_setup_support_truth.ex (`assert_protected_routes_guide!/1` should now be GREEN against this file after Task 1)
  </read_first>
  <behavior>
    - The page now contains EXACTLY ONE fenced Elixir code block (the canonical pipeline at the canonical-plug-order section). All other Elixir-styled prose mentions of plug names appear in cross-link sentences or canonical-block references, NOT in restated `pipeline ... do ... end` declarations.
    - `git grep` for the regex `pipeline :lockspire_protected_api do` within `docs/protect-phoenix-api-routes.md` returns exactly one match (inside the canonical fenced block).
    - The composite restatement pattern (three plug names in restated form near each other) does NOT appear outside the canonical block.
    - Running just the protected-routes guide assertion against the rewritten file passes.
  </behavior>
  <action>
    Run a self-audit against the freshly-modified file. Do NOT make further edits unless the audit fails. The audit is mechanical and grep-based; report each result.

    Audit step A: Confirm exactly one canonical-block declaration on the page by running `grep -c 'pipeline :lockspire_protected_api do' docs/protect-phoenix-api-routes.md`. Expected: `1`. If the result is greater than 1, a restatement survived the Task 1 rewrite — fix by replacing the extra block with reference-to-canonical prose, then re-audit.

    Audit step B: Confirm exactly one fenced Elixir code block on the page by running `grep -c '^```elixir' docs/protect-phoenix-api-routes.md`. Expected: `1`. If the result is greater than 1, an additional Elixir fence survived (probably from the Step 6 or Step 7 rewrites in Task 1) — fix by collapsing the fence to prose, then re-audit.

    Audit step C: Confirm the composite three-plug restatement pattern does not appear outside the canonical block. Run `grep -nE 'Lockspire\.Plug\.VerifyToken.*Lockspire\.Plug\.EnforceSenderConstraints.*Lockspire\.Plug\.RequireToken' docs/protect-phoenix-api-routes.md`. The expected result is one match — the line that ends Task 1 Step 3's introductory prose (which we deliberately allowed to mention all three plug names in cross-reference prose to preserve the Phase 92 substrings). If grep returns more than one match, inspect each match and convert any additional one to single-plug or cross-link prose. Multi-match in the canonical block lines is fine; multi-match on PROSE lines outside the canonical block is the D-15 spirit violation we are policing.

    Audit step D: Confirm the Phase 92 + Phase 97 substring contract via the helper. Run `MIX_QUIET=1 mix run -e 'IO.puts(Lockspire.TestSupport.AdvancedSetupSupportTruth.assert_protected_routes_guide!(File.read!("docs/protect-phoenix-api-routes.md")))'`. Expected: prints `:ok` (or runs silently with no exception). If it raises with an `expected content to include` message, the named substring is missing — go back to Task 1 and add it.

    Audit step E: Confirm the full release-readiness test no longer cites a protect-phoenix-api-routes.md substring in any failure. Run `mix test test/lockspire/release_readiness_contract_test.exs:642 2>&amp;1 | tail -20`. If the test passes, this plan plus Plan 03 are both done (Plan 03 may have landed in parallel). If the test fails, the failure message MUST cite a `docs/supported-surface.md` substring (D-09) and NOT any D-06/D-07/Phase-92 substring — that case means Plan 03 has not landed yet and is expected; this plan is still done.

    If any audit step fails, the executor returns to Task 1, applies the minimal edit to fix it, re-runs the audit, and only marks Task 2 done when all five audit steps pass.

    No fenced code blocks in this action — audit commands are inline shell strings.
  </action>
  <verify>
    <automated>grep -c 'pipeline :lockspire_protected_api do' docs/protect-phoenix-api-routes.md ; grep -c '^```elixir' docs/protect-phoenix-api-routes.md ; grep -nE 'Lockspire\.Plug\.VerifyToken.*Lockspire\.Plug\.EnforceSenderConstraints.*Lockspire\.Plug\.RequireToken' docs/protect-phoenix-api-routes.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pipeline :lockspire_protected_api do' docs/protect-phoenix-api-routes.md` returns exactly `1`.
    - `grep -c '^```elixir' docs/protect-phoenix-api-routes.md` returns exactly `1`.
    - The composite three-plug grep returns at most `1` match, and that single match (if present) is on a prose line inside the canonical-plug-order section (introductory paragraph), NOT inside a fenced code block other than the canonical one.
    - `mix test test/lockspire/release_readiness_contract_test.exs:642` either passes outright or fails on a `docs/supported-surface.md` D-09 substring; the failure message MUST NOT cite any D-06 substring, any D-07 substring, or any of the 8 Phase 92 protected-routes-guide substrings.
  </acceptance_criteria>
  <done>
    The audit passes all five steps. `docs/protect-phoenix-api-routes.md` is now the single authoritative protected-route doc page, carries the canonical block in marker-wrapped form, and exhibits zero D-15-spirit violations (no fifth restatement within the file).
  </done>
</task>

</tasks>

<threat_model>
Phase 97 does not introduce new attack surface (per 97-RESEARCH.md `## Security Domain`). This plan edits only a markdown doc file — no runtime path, no input handling, no crypto, no auth boundary. Documentation precision is a defense-in-depth aid (adopters who copy the canonical pipeline get the contract right), but is not itself a security primitive. Phase 98 (V5 Input Validation for the hardened plug) and Phase 99 (V6 Cryptography for the extracted `Protocol.AccessTokenSigner`) carry the security work for v1.27.
</threat_model>

<verification>
- All `grep -c` invariants in Task 2's audit pass.
- `mix test test/lockspire/release_readiness_contract_test.exs:642` failure messages no longer cite this file's substrings; failure (if any) cites only `docs/supported-surface.md` D-09 substrings until Plan 03 lands.
- The canonical pipeline block in this file is byte-stable — Plan 04 will mirror its interior bytes in three other files; Plan 05 will hash-compare all four.
</verification>

<success_criteria>
- Plan 02 is COMPLETE when:
  1. `docs/protect-phoenix-api-routes.md` lead carries the D-06 contract sentence verbatim followed by the D-07 caveat sentence (with HTML sweep marker preceding it).
  2. The canonical pipeline fenced Elixir block carries `# BEGIN LOCKSPIRE_PROTECTED_PIPELINE` / `# END LOCKSPIRE_PROTECTED_PIPELINE` markers around the existing canonical declaration (with `audience: "billing-api"` and `dpop_replay_store: MyAppWeb.ProtectedApiReplayStore` already present).
  3. The two secondary fenced Elixir blocks (scope-restricted, audience-restricted) are rewritten to reference-to-canonical prose; only one fenced Elixir block remains on the page.
  4. All 8 Phase 92 substrings asserted by `assert_protected_routes_guide!/1` survive verbatim, INCLUDING the lowercase `tenant checks` re-injected by Task 1 Step 3 R3.
  5. All 6 Phase 97 substrings added in Plan 01 are now present in the file.
  6. The release-readiness test no longer fails on this file's substrings (it may still fail on `docs/supported-surface.md` D-09 substrings — that is Plan 03's job).
</success_criteria>

<output>
Create `.planning/phases/97-contract-docs-first/97-02-SUMMARY.md` when done. The summary MUST record: (a) the new line numbers of the D-06 contract lead, the D-07 caveat, and the canonical-pipeline fenced block; (b) the exact text of the introductory prose sentence in the canonical-plug-order section (Task 1 Step 3) — including confirmation that the literal lowercase substrings `tenant checks` and `business authorization` are both present in that sentence; (c) the exact wording chosen for the rewritten scope-restricted and audience-restricted prose paragraphs; (d) confirmation that all Task 2 audit steps passed; (e) the exact byte sequence between the BEGIN/END markers (so Plan 04 can mirror it byte-identically across the other three carrier files).
</output>
</output>
