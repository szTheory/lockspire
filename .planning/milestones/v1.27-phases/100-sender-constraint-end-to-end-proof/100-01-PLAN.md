---
phase: 100-sender-constraint-end-to-end-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/access_token.ex
  - lib/lockspire/plug/enforce_sender_constraints.ex
  - lib/lockspire/plug/require_token.ex
  - test/lockspire/access_token_test.exs
  - test/lockspire/plug/enforce_sender_constraints_test.exs
  - test/lockspire/plug/require_token_test.exs
autonomous: true
requirements: [BIND-03]
must_haves:
  truths:
    - "%Lockspire.AccessToken{} carries a binding_verified field defaulting to false (fail-closed)"
    - "EnforceSenderConstraints sets binding_verified: true on every binding-validated success path (DPoP-only, mTLS, both); the unbound no-op path leaves it false"
    - "A bound token (error: nil, binding_requirements != nil, binding_verified: false) reaching RequireToken halts fail-closed with 403 and a binding-derived WWW-Authenticate challenge"
    - "A bearer (unbound) token (binding_requirements: nil) passes through RequireToken exactly as before — zero false-positive surface"
    - "Existing bound-token error tests (verify_token_test.exs error-carrying tokens) stay green because the D-03 guard is gated on error: nil"
  artifacts:
    - path: "lib/lockspire/access_token.ex"
      provides: "binding_verified: false struct field + boolean() type entry"
      contains: "binding_verified: false"
    - path: "lib/lockspire/plug/enforce_sender_constraints.ex"
      provides: "mark_binding_verified breadcrumb set on success paths"
      contains: "binding_verified: true"
    - path: "lib/lockspire/plug/require_token.ex"
      provides: "fail-closed sender-constraint-bypass clause + 403 handler"
      contains: "binding_verified: false"
    - path: "test/lockspire/plug/require_token_test.exs"
      provides: "bound-but-unverified->403 and bearer->pass clauses"
      contains: "binding_verified"
  key_links:
    - from: "lib/lockspire/plug/enforce_sender_constraints.ex"
      to: "lib/lockspire/access_token.ex"
      via: "sets binding_verified: true on access_token assign"
      pattern: "binding_verified: true"
    - from: "lib/lockspire/plug/require_token.ex"
      to: "lib/lockspire/access_token.ex"
      via: "reads binding_verified + binding_requirements off the assign"
      pattern: "binding_verified: false"
    - from: "lib/lockspire/plug/require_token.ex"
      to: "lib/lockspire/web/protected_resource_challenge.ex"
      via: "handle_structured_error -> 403 path -> put_dpop_challenge"
      pattern: "category: :sender_constraint"
---

<objective>
Land the BIND-03 runtime fail-closed guard: the cross-plug `binding_verified` breadcrumb (D-01), the `EnforceSenderConstraints` success-path set (D-02), and the `RequireToken` bound-but-unverified rejection clause (D-03), with the plug-unit/unit tests that prove all three (the BIND-03 negative + bearer-still-passes + positive-set + struct-default proofs from D-10).

Purpose: Close the sender-constraint bypass class (RFC 9449 §7.2 MUST-reject; CVE-2024-49755 class) so a pipeline that omits `EnforceSenderConstraints` after `VerifyToken` can no longer silently accept a bound token. This is the one piece of new runtime code in the phase — a deliberate, scoped exception to "proof only" that BIND-03's wording permits.

Output: 3 runtime files edited (struct + enforcer + guard) + 3 test files extended (defaults assertion, positive-set assertions, negative/bearer clauses).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-CONTEXT.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-RESEARCH.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-PATTERNS.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-VALIDATION.md

<interfaces>
<!-- Live contracts the executor needs. Verified against the tree this session. -->
<!-- Use these directly — no codebase exploration needed beyond <read_first>. -->

From lib/lockspire/access_token.ex (the 7-field struct D-01 extends — all default nil today):
  defstruct [:token, :claims, :client_id, :authorization_scheme, :binding_type, :binding_requirements, :error]
  @type t with binding_requirements: %{optional(:dpop_jkt) => String.t(), optional(:mtls_x5t_s256) => String.t()} | nil

From lib/lockspire/plug/enforce_sender_constraints.ex:
  - call/2 entry (lines 56-65): matches %AccessToken{error: nil, binding_requirements: requirements} when is_map(requirements) -> enforce_constraints; `_other -> conn` is the UNBOUND no-op path (MUST stay untouched).
  - enforce_constraints/3 (67-78): {:ok, _proof} and :skip arms call maybe_validate_mtls (success); {:error, sender_error} arm assigns error (failure — unchanged).
  - maybe_validate_mtls/3 success body (line 118) returns `conn`; the catch-all `defp maybe_validate_mtls(conn, _access_token, _opts), do: conn` (line 128) is the DPoP-only-success path.
  - established re-assign idiom (lines 76, 121): assign(conn, :access_token, %AccessToken{access_token | error: ...})
  - sender_error/2 (130-139) and mtls_error/0 (141-149) produce maps with category: :sender_constraint, challenge: :dpop | :bearer.

From lib/lockspire/plug/require_token.ex:
  - call/2 (19-36): the pass-through clause is FIRST today: %AccessToken{error: nil, claims: claims} when not is_nil(claims) -> conn. D-03 new clause goes BEFORE it.
  - handle_structured_error/2 (86-93): %{category: :sender_constraint} -> handle_invalid_token(normalize_sender_error(error)) [emits 401]; %{category: :insufficient_scope} -> handle_insufficient_scope [emits 403].
  - handle_invalid_token/2 (48-61): sends 401; challenge-aware (challenge: :dpop -> put_dpop_challenge; else www-authenticate header).
  - handle_insufficient_scope/2 (63-84): sends 403; SAME challenge-aware routing — the 403 template to mirror.
  - normalize_sender_error/1 (95-103): reads challenge (default :bearer), error, error_description, dpop_nonce via Map.get.
  - ProtectedResourceChallenge.put_dpop_challenge/3 is status-agnostic (sets headers only); send_json/3 sets the status.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add binding_verified: false to %AccessToken{} (D-01) and update the struct-defaults test</name>
  <files>lib/lockspire/access_token.ex, test/lockspire/access_token_test.exs</files>
  <read_first>
    - lib/lockspire/access_token.ex (the full 7-field defstruct + @type, lines 6-29)
    - test/lockspire/access_token_test.exs (defaults test lines 7-17; "allows setting fields" test lines 19-37)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-PATTERNS.md (the access_token.ex section, lines 27-68 — the defstruct kw-form caveat)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-RESEARCH.md (Pitfall 5, lines 322-326)
  </read_first>
  <behavior>
    - %AccessToken{}.binding_verified == false (the new field's fail-closed default)
    - %AccessToken{binding_verified: true}.binding_verified == true (the field is settable)
    - All seven pre-existing fields still default to nil (no regression)
  </behavior>
  <action>
    Append `binding_verified: false` to the `defstruct` list per D-01. CRITICAL (Elixir parser): the `key: value` form must come AFTER all bare-atom `:field` entries — place `binding_verified: false` LAST, do NOT interleave it among the `:field` atoms (convert nothing else; the existing seven stay bare atoms defaulting to nil). Add `binding_verified: boolean()` to the `@type t` map. Do NOT remove or repurpose any of the existing seven fields (D-01).
    In test/lockspire/access_token_test.exs add `assert token.binding_verified == false` to the defaults test (Pitfall 5: the ONLY existing test that must change for D-01 — this is an additive reconcile, not a workaround). Use `== false`, NOT `== nil`. Update the test name/comment so "defaults all fields to nil" no longer misleads about the one non-nil field.
  </action>
  <verify>
    <automated>mix test test/lockspire/access_token_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lockspire/access_token_test.exs` exits 0
    - Source assertion: `lib/lockspire/access_token.ex` contains `binding_verified: false` in defstruct AND `binding_verified: boolean()` in @type
    - Behavior: `%Lockspire.AccessToken{}.binding_verified` evaluates to `false` (not nil)
    - No existing field default changed (the seven bare-atom fields still default to nil)
  </acceptance_criteria>
  <done>The struct carries the 8th field defaulting to false; the defaults test asserts it; `mix test test/lockspire/access_token_test.exs` is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Set binding_verified: true on EnforceSenderConstraints success paths (D-02) + positive/negative plug-unit assertions</name>
  <files>lib/lockspire/plug/enforce_sender_constraints.ex, test/lockspire/plug/enforce_sender_constraints_test.exs</files>
  <read_first>
    - lib/lockspire/plug/enforce_sender_constraints.ex (call/2 entry 55-65; enforce_constraints/3 67-78; maybe_validate_dpop/3 80-109; maybe_validate_mtls/4 111-128; sender_error/2 130-139; mtls_error/0 141-149)
    - test/lockspire/plug/enforce_sender_constraints_test.exs (no-op path test 33-43; DPoP success 57-80; mTLS success 192-216; dual-bound success 236-270; dpop_fixture/2 279-311; request_conn/0 272-277)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-PATTERNS.md (the enforce_sender_constraints.ex section, lines 72-135 — the mark_binding_verified/1 helper shape and the "pull from conn.assigns" anchor note)
  </read_first>
  <behavior>
    - DPoP-only success: conn.assigns.access_token.binding_verified == true
    - mTLS success: conn.assigns.access_token.binding_verified == true
    - DPoP + mTLS dual-bound success: conn.assigns.access_token.binding_verified == true
    - Unbound no-op path (binding_requirements nil): conn.assigns.access_token.binding_verified == false (never set)
    - Every failure arm (DPoP error, mTLS error) leaves error != nil and is otherwise unchanged
  </behavior>
  <action>
    Per D-02, set `binding_verified: true` on `conn.assigns.access_token` at EVERY binding-validated success return, and NOWHERE else. Add one shared private helper `mark_binding_verified/1` (Claude's-Discretion shape per D-02): it reads the CURRENT struct from `conn.assigns[:access_token]` (NOT the closed-over `access_token` param — the anchor note: `maybe_validate_mtls/4` returns the unmodified conn whose assign already holds the token), re-assigns `%AccessToken{at | binding_verified: true}` using the established `assign(conn, :access_token, ...)` idiom (lines 76/121), and falls through to `conn` for any non-struct assign. Funnel through it the two success returns: the `conn` in the `maybe_validate_mtls/4` success `with` body (line 118) AND the DPoP-only-success catch-all `defp maybe_validate_mtls(conn, _access_token, _opts), do: conn` (line 128). Do NOT touch: the two error arms (`assign(... | error: mtls_error())`, lines 120-124), the `{:error, sender_error}` arm in enforce_constraints (line 75-76), or the unbound `_other -> conn` entry clause (lines 62-63) — only binding-validated successes get the breadcrumb.
    In the test file: add `assert conn.assigns.access_token.binding_verified == true` to each of the three `%AccessToken{error: nil}` success-path tests (DPoP success ~57-80, mTLS success ~192-216, dual-bound success ~236-270) after their existing `error: nil` assertion. Add `assert conn.assigns.access_token.binding_verified == false` to the no-op/unconstrained pass-through test (~33-43). Reuse `dpop_fixture/2` and `request_conn/0` as-is.
  </action>
  <verify>
    <automated>mix test test/lockspire/plug/enforce_sender_constraints_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lockspire/plug/enforce_sender_constraints_test.exs` exits 0
    - Source assertion: `enforce_sender_constraints.ex` contains a `binding_verified: true` set reachable from both the mTLS-success `with` body AND the DPoP-only-success catch-all clause
    - Behavior (faithful, not shallow): the three success tests assert `binding_verified == true`; the unbound test asserts `binding_verified == false`; the error arms are untouched (their tests still assert `error != nil`)
    - The unbound `_other -> conn` entry clause and both mTLS/DPoP failure arms contain NO `binding_verified: true` set
  </acceptance_criteria>
  <done>EnforceSenderConstraints marks every binding-validated success and only those; the four assertions (3 positive + 1 negative no-op) pass; file is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add the RequireToken bound-but-unverified fail-closed clause + 403 handler (D-03) and its negative/bearer plug-unit clauses</name>
  <files>lib/lockspire/plug/require_token.ex, test/lockspire/plug/require_token_test.exs</files>
  <read_first>
    - lib/lockspire/plug/require_token.ex (call/2 19-36; handle_invalid_token/2 48-61; handle_insufficient_scope/2 63-84 — the 403 template; handle_structured_error/2 86-93; normalize_sender_error/1 95-103; default_invalid_error/0 105-111; the send_json/3, oauth_body/1, www_authenticate/1 helpers below line 111)
    - lib/lockspire/plug/enforce_sender_constraints.ex (sender_error/2 130-139, mtls_error/0 141-149 — the maps normalize_sender_error/1 already consumes; copy this shape)
    - test/lockspire/plug/require_token_test.exs (pass-through test 14-22; sender-constraint challenge tests 69-96; build_conn/0 line 9)
    - lib/lockspire/web/protected_resource_challenge.ex (put_dpop_challenge/3 — confirm it is status-agnostic; sets headers only)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-PATTERNS.md (the require_token.ex section, lines 139-217 — the new-clause placement, the 403 template to copy verbatim, the challenge-derivation rule; and the require_token_test.exs section 357-408)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-RESEARCH.md (Pitfall 4 — the error: nil gate; A3 — 403 vs 401)
  </read_first>
  <behavior>
    - Bound-but-unverified DPoP token (error: nil, binding_requirements: %{dpop_jkt: _}, binding_verified: false) -> conn.halted, conn.status == 403, WWW-Authenticate carries `DPoP realm="Lockspire"` and `error="invalid_token"`
    - Bound-but-unverified mTLS token (binding_requirements: %{mtls_x5t_s256: _}, binding_verified: false) -> conn.halted, conn.status == 403, WWW-Authenticate carries `Bearer realm="Lockspire"`
    - Bearer/unbound token (binding_requirements: nil) -> refute conn.halted, conn.status == nil (passes through unchanged)
    - A bound token that PASSED enforcement (binding_verified: true) passes through unchanged
    - An error-carrying token (error != nil) is NOT intercepted by the new clause — it falls to the existing error clauses (Pitfall 4)
  </behavior>
  <action>
    Per D-03, add a NEW `call/2` clause ordered BEFORE the existing pass-through (`%AccessToken{error: nil, claims: claims} when not is_nil(claims) -> conn`): match `%AccessToken{error: nil, binding_requirements: req, binding_verified: false} when not is_nil(req)` and route it through `handle_structured_error(conn, sender_constraint_bypass_error(req))`. The `error: nil` gate is NON-NEGOTIABLE (Pitfall 4: it prevents intercepting the verify_token_test.exs:947-1037 error-carrying bound tokens — those carry error != nil and must keep hitting the existing error clauses; RESEARCH verified zero existing tests newly fail-close with this gate).
    Build the bypass error map (private `sender_constraint_bypass_error/1`) mirroring `EnforceSenderConstraints.sender_error/2`/`mtls_error/0`: `%{category: :sender_constraint, challenge: challenge, error: "invalid_token", error_description: <sender-constraint description>, dpop_nonce: nil}`. Derive `challenge` from `req`: `%{dpop_jkt: _}` -> `:dpop`; otherwise (`%{mtls_x5t_s256: _}`) -> `:bearer` (keeps the WWW-Authenticate story coherent with Phase 98's challenge taxonomy — DPoP-bound -> DPoP, mTLS-bound -> Bearer).
    D-03 requires status 403 (A3): `handle_structured_error/2`'s sender-constraint arm currently calls `handle_invalid_token/2` which sends 401. Add a status-explicit 403 path. Cleanest fit (Claude's-Discretion): a new `handle_sender_constraint_bypass/2` modeled VERBATIM on `handle_insufficient_scope/2` (lines 63-84 — challenge-aware: `%{challenge: :dpop}` -> `ProtectedResourceChallenge.put_dpop_challenge(conn, error, realm: "Lockspire")`; else `put_resp_header(conn, "www-authenticate", www_authenticate(error))`; then `send_json(403, oauth_body(error))` |> halt()). Route the bypass error to it — either via a new `handle_structured_error/2` head matching a distinct marker on the bypass map, or by calling `handle_sender_constraint_bypass/2` directly from the new call/2 clause. Do NOT alter the existing sender-constraint 401 path used by EnforceSenderConstraints failures (those stay 401 — they are a different case). Do NOT clear `binding_requirements` (D-04 — the host controller reads it).
    In the test file: add three clauses using `build_conn/0` — (a) bound DPoP `binding_requirements: %{dpop_jkt: "x"}, binding_verified: false` -> assert halted, status 403, www-authenticate =~ `DPoP realm="Lockspire"` and =~ `error="invalid_token"`; (b) bound mTLS `binding_requirements: %{mtls_x5t_s256: "x"}` -> assert 403 and www-authenticate =~ `Bearer realm="Lockspire"`; (c) bearer `binding_requirements: nil` -> refute halted, status nil (the surprise-free guarantee). Mirror the existing pass-through (14-22) and sender-constraint challenge tests (69-96) for assertion shape.
  </action>
  <verify>
    <automated>mix test test/lockspire/plug/require_token_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lockspire/plug/require_token_test.exs` exits 0
    - Source assertion: the new `call/2` clause is ordered BEFORE the `%AccessToken{error: nil, claims: claims}` pass-through AND is gated on `error: nil` and `binding_verified: false` and `not is_nil(req)`
    - Behavior (faithful, not shallow): the guard fires ONLY for `error: nil, binding_requirements != nil, binding_verified: false` (status 403); a bearer token (binding_requirements: nil) still passes (refute halted); both DPoP (`DPoP realm`) and mTLS (`Bearer realm`) challenge branches asserted
    - Status is 403 (not 401) for the bound-but-unverified case (D-03)
    - `binding_requirements` is NOT cleared anywhere (D-04)
  </acceptance_criteria>
  <done>RequireToken fail-closes bound-but-unverified tokens with 403 and the correct binding-derived challenge, passes bearer tokens unchanged, and never intercepts error-carrying tokens; file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RS plug pipeline | Untrusted `Authorization` header + binding proof (DPoP header / mTLS cert) crosses into `VerifyToken → EnforceSenderConstraints → RequireToken` |
| EnforceSenderConstraints → RequireToken | Intra-pipeline trust signal: the `binding_verified` breadcrumb on `conn.assigns.access_token` (this plan creates it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Elevation / Spoofing | RequireToken on a pipeline missing EnforceSenderConstraints | mitigate | D-03 fail-closed clause: bound token (binding_requirements != nil) with binding_verified == false halts 403. This is the RFC 9449 §7.2 MUST-reject / CVE-2024-49755-class bypass closure — the core threat the plan closes. |
| T-100-02 | Spoofing | binding_verified default | mitigate | D-01 fail-closed default `false`: a token is "not verified until something proves otherwise". A struct constructed without the enforcer running cannot claim verification. |
| T-100-03 | Tampering / Repudiation | EnforceSenderConstraints success set | mitigate | D-02: binding_verified: true set ONLY on binding-validated success paths; failure arms and the unbound no-op path never set it, so a failed/skipped binding cannot mark itself verified. |
| T-100-04 | Denial of Service (false-positive) | RequireToken guard on legitimate bearer routes | accept | Disambiguator: the guard is gated on `binding_requirements != nil`; bearer-only routes (nil) pass through exactly as today. Zero false-positive surface; no legitimate config trips it (verified: only error-carrying bound tokens exist in current tests, excluded by the `error: nil` gate). |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (verified RESEARCH Package Legitimacy Audit: not applicable — no new external packages). |
</threat_model>

<verification>
After all three tasks:
- `mix test test/lockspire/access_token_test.exs test/lockspire/plug/enforce_sender_constraints_test.exs test/lockspire/plug/require_token_test.exs` all green.
- `mix test` (full default non-integration suite) green — confirms the reconciliation analysis (no existing test newly fail-closes): in particular `test/lockspire/plug/verify_token_test.exs` "challenge derivation from binding" block (947-1037) stays green because those tokens carry `error != nil`.
- `mix compile --warnings-as-errors` clean.
</verification>

<success_criteria>
- BIND-03 runtime guard exists and is proven: bound-but-unverified → 403, bearer → pass, success-path → binding_verified: true, struct default → false.
- No existing test regresses (the `error: nil` gate keeps the verify_token_test.exs error-carrying bound tokens on their existing clauses).
- All five files compile and their test files are green.
</success_criteria>

<output>
Create `.planning/phases/100-sender-constraint-end-to-end-proof/100-01-SUMMARY.md` when done
</output>
