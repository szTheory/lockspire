---
milestone: v1.27
audited: 2026-05-29T12:29:35Z
status: tech_debt
incomplete_milestone: false
scores:
  requirements: 28/28
  phases: 6/6
  integration: 12/12   # all cross-phase connections WIRED
  flows: 5/5           # all critical E2E flows complete with executable proof
gaps:
  requirements: []     # no unsatisfied or orphaned requirements
  integration: []      # no broken cross-phase wiring
  flows: []            # no broken flows
tech_debt:
  - phase: "98/99/100 (milestone-induced)"
    severity: medium
    items:
      - "9 failing tests from stale fixtures the milestone's own hardening invalidated — suite is RED until updated. Documented in .planning/phases/100-sender-constraint-end-to-end-proof/deferred-items.md."
      - "test/integration/phase81_generated_host_route_protection_e2e_test.exs (5 failures): hand-signs JWTs at :295-298 without typ:at+jwt header → correctly rejected by hardened plug (invalid_typ). Fix: mint via AccessTokenSigner.issue/3."
      - "test/integration/phase32_device_flow_token_exchange_e2e_test.exs (2 failures): device flow returns 500 :signing_key_not_found — test setup seeds no active signing key, now load-bearing under JWT-default issuance. Fix: seed an active signing key in setup."
      - "test/lockspire/audit/audit_writer_test.exs (2 failures): same missing-active-key root cause → token_signing_failed. Fix: seed an active signing key in setup."
  - phase: "99-signer-extraction (deferred)"
    severity: low
    items:
      - "token_exchange.ex:1232 fetch_signing_key/1 (ID-token path) calls host key_store at arity 0 — same WR-01 class as the fixed signer path, but pre-existing and outside the approved Phase 99 remediation scope. Tracked for follow-up."
      - "refresh_exchange.ex:332 rotated refresh token account_id: nil → audit subject_id: nil (WR-05). Observability gap, pre-existing; repository back-fills at persistence."
nyquist:
  compliant_phases: ["99", "100"]
  partial_phases: ["97", "102"]
  missing_phases: ["98", "101"]
  overall: incomplete
---

# Milestone v1.27 — Phoenix Resource Server Token Acceptance — Audit Report

**Audited:** 2026-05-29
**Status:** `tech_debt` — all requirements met, no critical blockers, accumulated deferred items need review before completion.
**Phases:** 97–102 (all complete, all individually verified `passed`)

> Supersedes the 2026-05-28 audit snapshot (which captured the milestone mid-flight at 4/6 phases / 20/28 requirements). All six phases are now complete and verified.

## Definition of Done

**Milestone goal:** Make it obvious which Lockspire-issued token shape a host Phoenix API should accept, how that relates to `Lockspire.Plug.VerifyToken`, and what CI proof backs the blessed path — without conflating stored opaque access tokens with JWT bearer route-protection fixtures.

**Verdict:** Achieved. `VerifyToken` narrows to RFC 9068 `at+jwt`, default issuance flips to `:jwt` across all grant paths through one shared signer, opaque stays an explicit opt-in for `/userinfo` + `/introspect`, and CI proves the auth-code → `at+jwt` → host-route → 200 round-trip executably.

## Requirements Coverage — 28/28 satisfied

3-source cross-reference (REQUIREMENTS.md traceability `[x]` + phase VERIFICATION.md `passed` + SUMMARY `requirements-completed` frontmatter) agrees on all 28. No `unsatisfied`, no `partial`, no orphaned requirements.

| Phase | Requirements | Verification | Status |
|-------|--------------|--------------|--------|
| 97 Contract + Docs First | RECIPE-01, DOCS-01, DOCS-02 | passed (14/14) | ✅ satisfied |
| 98 Plug Hardening | VERIFIER-01..06 | passed (4/4) | ✅ satisfied |
| 99 Signer + JWT-Default | SIGNER-01/02, FORMAT-01/02, AUD-01/02/03, DISCOVERY-01 | passed (8/8) | ✅ satisfied |
| 100 Sender-Constraint Proof | BIND-01/02/03 | passed (11/11) | ✅ satisfied |
| 101 Adoption-Demo Re-Wire | DEMO-01/02/03 | passed (3/3) | ✅ satisfied |
| 102 Scaffolding + Telemetry + Migration | SCAFFOLD-01/02, TELEMETRY-01, MIGRATE-01/02 | passed (4/4 SC + 5/5 REQ) | ✅ satisfied |

## Cross-Phase Integration — 12/12 WIRED

Verified by `gsd-integration-checker` against the live codebase with executable proof. All four critical seams plus supporting wiring resolve to **WIRED**:

1. **Issuance → Verification round-trip (P99→P98→P100):** WIRED. `AccessTokenSigner.sign_jwt/2` emits `typ:at+jwt` header + `iss/sub/iat/exp` + list `aud` + `cnf` carry-through (`access_token_signer.ex:121-148,170-174`); `VerifyToken` accepts exactly that shape (`verify_token.ex:698-780`); signer `alg` ∈ `{RS256,ES256,PS256}` = discovery-advertised algs = `VerifyToken.@allowed_algs`. All 5 grant paths (AC/refresh/device/CIBA/RFC8693) funnel through the single signer; no access-token signing bypass remains. Proven green by `phase100_sender_constraint_e2e_test.exs` (DPoP + mTLS → 200).
2. **Canonical pipeline hash-pin (P97/P100/P101):** WIRED. 4 RECIPE-01 sites byte-identical (normalized SHA-256) and ordered `VerifyToken → EnforceSenderConstraints → RequireToken`; `release_readiness_contract_test.exs` green (34/34).
3. **Audience coherence (P99 resource= → P101 audience:):** WIRED. `derive_aud/2` → `aud=[resource]`; demo declares + smoke uses the same absolute URI `https://billing.acme-ledger.test`; smoke asserts it in `access_token.audience`.
4. **Telemetry (P102→P98):** WIRED. `[:lockspire, :rs, :token_format]` emitted from the two real `verify_token.ex` paths (JWT-success :149-154, opaque-rejection :120-125).

## End-to-End Flows — 5/5 complete

| Flow | Status | Proof |
|------|--------|-------|
| Issuance → verification (default JWT) | ✅ | phase100 e2e (real signer → 200) |
| DPoP-bound + mTLS-bound traversal | ✅ | phase100 e2e (binding_verified set + gated) |
| Canonical pipeline hash/order integrity | ✅ | release_readiness_contract_test (green) |
| audience binding coherence | ✅ | adoption_smoke audience assertion |
| Adoption-demo full round-trip | ✅ | adoption_smoke: auth-code → at+jwt → /api/billing/summary → 200; anon-401; /userinfo opaque-200 |

## Tech Debt (needs review — not a blocker)

### 1. Nine stale-fixture test failures (milestone-induced) — MEDIUM

The full suite is **RED**: 9 failures across 3 files, all caused by the milestone's *own correct* behavior changes, not by broken production wiring. The integration checker confirmed at source level these mask **no** real cross-phase gap — each is the system correctly rejecting a token shape/state the hardening made invalid, while the parallel real-signer proofs (phase100, adoption_smoke, contract test) are green.

| File | Failures | Root cause | Fix |
|------|----------|-----------|-----|
| `test/integration/phase81_generated_host_route_protection_e2e_test.exs` | 5 | Hand-signs JWTs (`:295-298`) without `typ:at+jwt` → `invalid_typ` rejection | Mint via `AccessTokenSigner.issue/3` |
| `test/integration/phase32_device_flow_token_exchange_e2e_test.exs` | 2 | 500 `:signing_key_not_found` — no active key seeded, now load-bearing under JWT default | Seed active signing key in setup |
| `test/lockspire/audit/audit_writer_test.exs` | 2 | `token_signing_failed` — same missing-key cause | Seed active signing key in setup |

Documented in `.planning/phases/100-sender-constraint-end-to-end-proof/deferred-items.md`. Recommended fix is the same `AccessTokenSigner.issue/3` / key-seeding pattern Phase 100 demonstrates.

### 2. Phase 99 deferred items — LOW

- `token_exchange.ex:1232` `fetch_signing_key/1` (ID-token path) calls host `key_store` at arity 0 — same WR-01 class as the fixed access-token signer path, but pre-existing and outside the approved Phase 99 remediation scope.
- `refresh_exchange.ex:332` rotated refresh token `account_id: nil` → audit `subject_id: nil` (WR-05). Pre-existing observability gap; repository back-fills at persistence.

## Nyquist Validation Coverage — incomplete

Nyquist validation enabled (`workflow.nyquist_validation: true`).

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Status | Action |
|-------|---------------|-------------------|-----------------|--------|--------|
| 97 | exists (draft) | false | false | PARTIAL | `/gsd-validate-phase 97` |
| 98 | missing | — | — | MISSING | `/gsd-validate-phase 98` |
| 99 | exists (approved) | true | true | COMPLIANT | — |
| 100 | exists (approved) | true | true | COMPLIANT | — |
| 101 | missing | — | — | MISSING | `/gsd-validate-phase 101` |
| 102 | exists (planned) | true | false | PARTIAL | `/gsd-validate-phase 102` |

2 compliant, 2 partial, 2 missing. Discovery-only — does not block; consider closing before/after completion.

## Recommendation

No requirement or wiring blocks completion. The single decision point is the 9 RED tests: they are stale fixtures, not production defects, but leaving the suite red is meaningful for "definition of done." Recommended: land one small cleanup plan that updates the three fixtures (use `AccessTokenSigner.issue/3` for phase81; seed an active signing key for phase32 + audit_writer) before `/gsd-complete-milestone v1.27`. Optionally run `/gsd-validate-phase` for 98/101 (missing) and 97/102 (partial) to close Nyquist coverage.
