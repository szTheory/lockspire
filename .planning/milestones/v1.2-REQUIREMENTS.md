# Requirements Archive: v1.2

**Archived:** 2026-04-24
**Milestone:** v1.2
**Release posture at archive:** Planning milestone complete; package version `0.2.0` exists, and the shipped preview surface now includes the narrow PAR wedge plus warning-free checked-in release automation on a supported runtime.

## v1.2 Requirements

### PAR Intake

- [x] **PAR-01**: OAuth clients can submit a pushed authorization request to a dedicated PAR endpoint using Lockspire's supported direct-call client authentication rules and receive a server-issued `request_uri` plus `expires_in`.

### Authorization Lifecycle

- [x] **PAR-02**: OAuth clients can complete the existing authorization code + PKCE flow by presenting a PAR-issued `request_uri`, and Lockspire enforces expiry, client binding, and replay-resistant single use for that reference.

### Discovery and Support Truth

- [x] **PAR-03**: Integrators can discover PAR support through truthful metadata and docs that advertise only the implemented PAR slice and do not imply request-object-by-value, dynamic registration, or device-flow support.

### Verification

- [x] **PAR-04**: Maintainers have automated protocol, security, and integration coverage for PAR success, expiry, wrong-client usage, replay rejection, and discovery truth before the milestone can close.

### Release Hygiene

- [x] **RELS-04**: Maintainers can run the checked-in preview release path without the known deprecated GitHub Actions runtime warning while keeping release automation and maintainer docs aligned.

## Requirement Outcomes

| Requirement | Phase | Final Status | Notes |
|-------------|-------|--------------|-------|
| PAR-01 | Phase 14 | Complete | `POST /par` now persists Lockspire-issued opaque references durably, returns `request_uri` plus `expires_in`, and rejects invalid submissions without partial state. |
| PAR-02 | Phase 15 | Complete | `/authorize` consumes Lockspire-issued PAR references with expiry, client binding, replay resistance, and canonical auth-code + PKCE continuation. |
| PAR-03 | Phase 15 | Complete | Discovery, README, supported-surface, and SECURITY text advertise only the mounted narrow PAR slice. |
| PAR-04 | Phase 16 | Complete | Milestone closure reuses the existing protocol, browser, discovery, and end-to-end proof stack with explicit verification artifacts. |
| RELS-04 | Phase 16 | Complete | Release Please now runs through a checked-in Node 24 composite action while the protected publish lane and maintainer guidance remain aligned. |

## Deferred to Next Milestone Planning

### v2+ Candidate Requirements

- **JAR-01**: Clients can send signed or encrypted request objects by value or through richer PAR/JAR interoperability modes.
- **PAR-05**: Operators can require PAR globally or per client when stronger request-path policy is needed.
- **DCR-01**: Developers can self-register OAuth clients through dynamic client registration.
- **DEVICE-01**: Devices can complete OAuth authorization through the device authorization grant.

## Residual Tech Debt at Archive

| Item | Status |
|------|--------|
| `gsd-sdk query milestone.complete` archive automation is still unreliable | Deferred |

## Out of Scope Preserved

| Feature | Reason |
|---------|--------|
| Dynamic client registration in v1.2 | Expands client trust and operator scope beyond the narrow PAR wedge. |
| Device authorization flow in v1.2 | Introduces a separate interaction model instead of extending the existing browser authorization path. |
| Sender-constrained token modes in v1.2 | Valuable later, but not required to prove the PAR extension cleanly. |
| Generic external `request_uri` support in v1.2 | The milestone should only support request references issued by Lockspire's PAR endpoint. |
| JAR-by-value support in v1.2 | PAR is the intended narrow first step; broader request-object work would blur scope and docs. |
