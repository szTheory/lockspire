---
milestone: v1.36
name: Structural Quality Ratchet
verified: 2026-08-26T16:40:30Z
status: passed
score:
  requirements: 25/25
  roadmap_success_criteria: 21/21
warnings:
  - "Focused ExUnit invocations emit a KeyCache startup error before Lockspire.TestRepo is available, plus Telemetry local-handler performance notices. They did not fail tests, but they are avoidable proof noise."
---

# Milestone v1.36 Verification: Structural Quality Ratchet

**Verdict:** `passed` — all 25 requirements have live implementation and executable evidence. The sole prior `READ-02` documentation contradiction is closed on current HEAD.

## Method

This review started from the roadmap outcomes and checked current source, workflow wiring, and focused executable contracts. It did not use phase summaries as proof.

Commands run independently:

- `MIX_ENV=test mix test` for 11 release/CI/architecture/docs/artifact contract suites — **66 tests, 0 failures**.
- `MIX_ENV=test mix test` for CIBA Push and all split token-exchange capability suites — **42 tests, 0 failures**.
- Re-verification: `MIX_ENV=test mix test test/lockspire/documentation_contract_test.exs test/lockspire/ci_static_contract_test.exs test/lockspire/release_ci_evidence_contract_test.exs test/lockspire/release/release_automation_contract_test.exs test/lockspire/protocol/token_exchange_test.exs` — **24 tests, 0 failures**.

## Requirement Matrix

| Requirement | Verdict | Live evidence |
| --- | --- | --- |
| RUNTIME-01 | VERIFIED | Protocol modules default storage behavior to `Lockspire.Storage.Ecto.Repository`; `architecture_fitness_test.exs` parses every protocol module and rejects `Config.repo!/0` and Ecto-record reach-through. |
| RUNTIME-02 | VERIFIED | `phase53_ciba_delivery_modes_e2e_test.exs` asserts TestRepo has no storage delegates, then exercises Push delivery with JWT-shaped access and ID tokens. |
| RUNTIME-03 | VERIFIED | `TokenLifetime` is the single 3600/3600/2592000 policy and all five current lifetime consumers call it. |
| RUNTIME-04 | VERIFIED | `PrivateJwk.decode/1` only accepts JSON/safe binary term maps and fail-closes; access, ID, JARM, introspection, logout, and JAR paths call it. |
| ARCH-01 | VERIFIED | AST fitness contract covers all protocol files, admin LiveViews, explicit Repository ports, and ordinary TestRepo behavior. |
| ARCH-02 | VERIFIED | Ecto Repository implements client-registration, initial-access-token, logout, transaction, and audit port operations; fitness contract asserts the public port functions. |
| ARCH-03 | VERIFIED | Fitness test rejects Ecto adapter reach-through by the relevant LiveViews; protocol module AST scan rejects Ecto records/query imports. |
| ARCH-04 | VERIFIED | `TokenExchange` remains the stable facade for authorization-code, device-code, and CIBA coordinators. The duplicate `GrantSupport.exchange/1` router was removed; the contract proves no such export remains. |
| STATIC-01 | VERIFIED | `scripts/ci/run_credo.sh` exits on Credo failure and additionally rejects parser timeout output; CI invokes `mix qa`. |
| STATIC-02 | VERIFIED | CI has ordinary required `dialyzer` job, cache keyed by lock/version, 20-minute bound, and a fail-closed runner with no warning filter. |
| TEST-01 | VERIFIED | `Lockspire.DataCase` owns SQL sandbox lifecycle; `Lockspire.ConfigCase` snapshots and restores application config, and split suites use them. |
| TEST-02 | VERIFIED | Token proof is capability-split (39 unique inventory tests); release proof is split automation/hygiene/support; admin design proof is split CSS/routes/proof. |
| COVER-01 | VERIFIED | `mix.exs` configures ExUnit threshold 73; `test.coverage` runs `test --cover`; Fast CI lane invokes it through the timed matrix. |
| RELEASE-01 | VERIFIED | Release Please workflow merges then exits; a later successful canonical `CI` push for current main dispatches exact SHA and source run ID. Validator checks the CI path and canonical workflow ID as well as SHA metadata. |
| RELEASE-02 | VERIFIED | `workflow_dispatch` accepts typed inputs only through environment variables, validates 40-hex SHA and numeric run ID, fetches main, checks ancestry/current head, and validates canonical CI identity with quoted expansions. |
| RELEASE-03 | VERIFIED | Exact ref is detached before publish; GitHub release target is checked/created at that SHA; Hex publish is followed by the install-truth job and retained log artifact. |
| SUPPLY-01 | VERIFIED | Local checked-in Release Please action is used by `release.yml`; release runtime and its lock are included in CI lock verification and covered by release runtime contracts. |
| SUPPLY-02 | VERIFIED | Dependency review is present fail-closed; workflow contract checks immutable PostgreSQL images; all reviewed workflow jobs carry timeouts. |
| CI-01 | VERIFIED | CI runs repository hygiene, checksum-verified actionlint/ShellCheck, and non-mutating lock diff verification. |
| CI-02 | VERIFIED | `run_test_matrix.sh` records partition command, elapsed seconds, exit code, and timestamps; default CI executes fast coverage and integration once each, leaving `test.phase3` as a focused alias. |
| COMPAT-01 | VERIFIED | Minimum compatibility CI uses the pinned PostgreSQL 14 digest and minimum OTP/Elixir variables. |
| COMPAT-02 | VERIFIED | Committed compatibility fixture pins Phoenix `1.8.5` and LiveView `1.1.28`, mounts Lockspire by host seam, and CI installs/compiles it with lock-drift checking. |
| READ-01 | VERIFIED | `readability_contract_test.exs` scans all runtime `lib/**/*.{ex,exs}` against planning-marker patterns and passes. |
| READ-02 | VERIFIED | Architecture/walkthrough contracts pass, and `docs/maintainer-release.md` now accurately states that Dialyzer is a required cached PR CI job with a zero-warning, unsuppressed baseline while remaining separately runnable locally. |
| CLEAN-01 | VERIFIED | Artifact policy contract confirms named scratch files absent and retained `tmp/admin-ui-polish/*.png` evidence; maintainer guide specifies repo-only/redaction-safe/no-runtime-or-package lifecycle. |

## Goal-Backward Success Criteria

| Phase | Outcome status | Evidence |
| --- | --- | --- |
| 126 | VERIFIED | Exact post-merge canonical-CI evidence, exact immutable ref validation, publish/install flow, and release/supply-chain contracts all exist and pass. |
| 127 | VERIFIED | Fail-closed Credo, coverage threshold, PostgreSQL 14 job, and exact Phoenix/LiveView fixture are CI-wired. |
| 128 | VERIFIED | Ordinary Repo CIBA Push is behaviorally exercised; explicit persistence ports and AST boundary guard are live. |
| 129 | VERIFIED | Stable token facade/coordinator routing with no duplicate GrantSupport router, one lifetime policy, one fail-closed decoder, and a required cached Dialyzer lane are live. |
| 130 | VERIFIED | Test consolidation/splitting, timing proof, archaeology cleanup, artifact policy, and synchronized documentation are live. |

## Disconfirmation Findings

The review specifically searched for a partial requirement, a misleading passing test, and an uncovered failure path:

- **Prior partial requirement, now closed:** the maintainer guide now describes the required cached zero-warning Dialyzer CI lane consistently with `.github/workflows/ci.yml`.
- **Prior misleading-test risk, reduced:** focused re-verification covered both the documentation and CI static/release contracts after the wording update. A future dedicated assertion of that exact prose would strengthen drift resistance further, but no current requirement is unmet.
- **Proof-noise error path:** focused ExUnit startup logs `Failed to refresh KeyCache` before `Lockspire.TestRepo` is started. The tests still pass, but the failure path is not made quiet or explicitly asserted.

## Re-verification Closure

Commit `a52c30d` revised the contributor-gate guidance. The current guide and workflow now agree: Dialyzer is required in cached PR CI, has a zero-warning unsuppressed baseline, and remains separate from the fast local `mix ci` loop. The post-fix documentation, CI-static, canonical release-identity, and token-facade contracts all pass.
