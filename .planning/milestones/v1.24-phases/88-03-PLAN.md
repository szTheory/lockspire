---
phase: 88
plan: 3
title: Prove Shared Direct-Client `client_secret_jwt` Runtime Behavior
type: execute
wave: 3
depends_on:
  - "88-01"
  - "88-02"
files_modified:
  - test/lockspire/protocol/client_auth_test.exs
  - test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs
  - test/lockspire/audit/event_test.exs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "Representative shared direct-client surfaces accept valid client_secret_jwt callers through one runtime path."
    - "Invalid signature, audience, replay, algorithm, and method-mismatch cases fail consistently as invalid_client."
    - "Phase 88 proof remains runtime-only and does not change PAR, discovery, admin, or docs truth."
  artifacts:
    - path: "test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs"
      provides: "Cross-surface success and failure proof"
      contains: "client_secret_jwt"
    - path: "test/lockspire/protocol/client_auth_test.exs"
      provides: "Token-endpoint-side runtime and verifier proof"
      min_lines: 240
  key_links:
    - from: "test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs"
      to: "lib/lockspire/protocol/client_auth.ex"
      via: "shared direct-client auth path"
    - from: "test/lockspire/protocol/client_auth_test.exs"
      to: "lib/lockspire/protocol/client_auth/client_secret_jwt.ex"
      via: "verifier and token-endpoint-side proof"
---

<objective>
Add repo-native proof that valid `client_secret_jwt` authentication works across the shipped shared direct-client surfaces and that invalid inputs fail consistently without widening the milestone boundary.

Purpose: Phase 88 closes only when the shared runtime path is proven across representative consumers, not just at the verifier unit layer.
Output: a companion cross-surface test module plus aligned unit and audit proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-shared-client-secret-jwt-runtime/88-CONTEXT.md
@test/lockspire/protocol/client_auth_test.exs
@test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs
@test/lockspire/audit/event_test.exs
@lib/lockspire/protocol/introspection.ex
@lib/lockspire/protocol/revocation.ex
@lib/lockspire/protocol/device_authorization.ex
@lib/lockspire/protocol/backchannel_authentication.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add representative cross-surface success proof</name>
  <files>test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs, test/lockspire/protocol/client_auth_test.exs</files>
  <read_first>test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs, test/lockspire/protocol/client_auth_test.exs</read_first>
  <action>Create a direct companion to the existing `private_key_jwt` runtime suite that proves valid `client_secret_jwt` callers succeed across `POST /revoke`, `POST /introspect`, `POST /device/code`, and `POST /bc-authorize`, while `test/lockspire/protocol/client_auth_test.exs` continues to prove the token-endpoint-side runtime path. Keep `POST /par` out of scope.</action>
  <verify>mix test test/lockspire/protocol/client_auth_test.exs test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs</verify>
  <acceptance_criteria>
    - The new direct-client suite covers the representative non-PAR shared-auth consumers.
    - Token-endpoint-side proof remains in `client_auth_test.exs`, and the verify command runs both files.
  </acceptance_criteria>
  <done>Success proof matches the claimed runtime scope and does not overstate what one test file covers.</done>
</task>

<task type="auto">
  <name>Task 2: Prove consistent failure behavior across representative surfaces</name>
  <files>test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs, test/lockspire/protocol/client_auth_test.exs</files>
  <read_first>lib/lockspire/protocol/client_auth.ex, test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs</read_first>
  <action>Cover invalid signature, audience mismatch, replay, disallowed algorithm, and auth-method mismatch across the representative shared-auth consumers. Assert generic `invalid_client` behavior everywhere and prove that JWT assertion attempts never fall back to another auth method.</action>
  <verify>mix test test/lockspire/protocol/client_auth_test.exs test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs</verify>
  <acceptance_criteria>
    - Negative cases return consistent `invalid_client` outcomes across the representative surfaces.
    - Tests explicitly prove there is no fallback to `client_secret_basic`, `client_secret_post`, or `private_key_jwt`.
  </acceptance_criteria>
  <done>Cross-surface negative-path proof matches the Phase 88 security and support boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Keep audit normalization aligned with the symmetric runtime slice</name>
  <files>test/lockspire/audit/event_test.exs</files>
  <read_first>test/lockspire/audit/event_test.exs, lib/lockspire/redaction.ex</read_first>
  <action>Extend audit normalization proof only where the new runtime emits additional metadata, and keep assertions strict that raw JWT material, decoded claims, sealed verifier material, or secret-derived bytes are absent from normalized output.</action>
  <verify>mix test test/lockspire/audit/event_test.exs</verify>
  <acceptance_criteria>
    - Audit normalization stays green with the symmetric verifier path covered.
    - No new test requires Phase 89 discovery, DCR, admin, or docs changes.
  </acceptance_criteria>
  <done>Audit proof remains runtime-only and aligned with the new shared auth slice.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mix test test/lockspire/protocol/client_auth_test.exs test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs test/lockspire/audit/event_test.exs`
- [ ] Representative direct-client surfaces all authenticate through the shared runtime path.
- [ ] No Phase 89 or 90 truth files change as part of this proof plan.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Repo-native tests prove valid and invalid `client_secret_jwt` behavior across the representative shared direct-client surfaces.
- Proof stays runtime-only and keeps `POST /par`, discovery, admin, and docs out of scope.

</success_criteria>

<output>
After completion, create `.planning/phases/88-03-SUMMARY.md`
</output>
