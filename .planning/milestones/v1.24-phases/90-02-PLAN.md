---
phase: 90
plan: 2
title: Add Repo-Native Support-Truth Contract Proof For `client_secret_jwt`
type: execute
wave: 2
depends_on:
  - "90-01"
files_modified:
  - test/support/client_secret_jwt_support_truth.ex
  - test/lockspire/release_readiness_contract_test.exs
  - test/lockspire/protocol/discovery_test.exs
  - test/lockspire/web/discovery_controller_test.exs
  - test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs
autonomous: true
requirements:
  - PROOF-01
  - META-02
user_setup: []
must_haves:
  truths:
    - "Release/docs contract tests pin the same narrow support facts the docs now claim: direct-client only, `HS256` only, issuer-string `aud`, no `PAR`, and no FAPI equivalence."
    - "Existing runtime and discovery proof remains part of the closure story instead of being bypassed by docs-only assertions."
    - "One small test-only helper defines the semantic anchors reused by proof code without becoming a runtime source of truth."
  artifacts:
    - path: "test/support/client_secret_jwt_support_truth.ex"
      provides: "Shared semantic assertions for the shipped client_secret_jwt support facts"
      contains: "client_secret_jwt"
    - path: "test/lockspire/release_readiness_contract_test.exs"
      provides: "Release/docs contract proof tied to the canonical support surface and adjacent guides"
      contains: "client_secret_jwt"
    - path: "test/lockspire/protocol/discovery_test.exs"
      provides: "Builder-level discovery proof remains aligned with documented endpoint truth"
      contains: "client_secret_jwt"
    - path: "test/lockspire/web/discovery_controller_test.exs"
      provides: "HTTP discovery proof remains aligned with documented endpoint truth"
      contains: "client_secret_jwt"
  key_links:
    - from: "test/support/client_secret_jwt_support_truth.ex"
      to: "test/lockspire/release_readiness_contract_test.exs"
      via: "shared semantic anchors for support-truth assertions"
    - from: "docs/supported-surface.md"
      to: "test/lockspire/protocol/discovery_test.exs"
      via: "documented endpoint scope matches published discovery metadata"
    - from: "docs/client-secret-jwt-host-guide.md"
      to: "test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs"
      via: "guide claims match existing runtime proof"
---

<objective>
Add a repo-native proof layer that ties the new support-truth wording to the already-shipped runtime and discovery behavior, using semantic assertions rather than brittle prose snapshots.

Purpose: updating docs is not enough for milestone closure. The repo needs executable proof that the canonical support page, the new guide, discovery metadata, and runtime tests all describe the same narrow `client_secret_jwt` slice.
Output: one test-only support-truth helper, extended release/docs contract assertions, and refreshed runtime/discovery tests that pin the documented truth.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/90-support-truth-and-milestone-closure/90-CONTEXT.md
@.planning/phases/90-support-truth-and-milestone-closure/90-RESEARCH.md
@docs/supported-surface.md
@docs/client-secret-jwt-host-guide.md
@test/lockspire/release_readiness_contract_test.exs
@test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs
@test/lockspire/protocol/discovery_test.exs
@test/lockspire/web/discovery_controller_test.exs
</context>

<threat_model>
## Threats

- `T-90-04` Docs could claim a narrow support slice that no test asserts, allowing future drift between wording and shipped behavior.
- `T-90-05` Release-contract tests could become large prose snapshots that are expensive to maintain and still miss the real support facts.
- `T-90-06` Runtime or discovery truth could drift later if Phase 90 verifies only docs and not the existing protocol proof surfaces.

## Mitigations

- Add one small helper under `test/support/` that asserts the concrete support facts: `client_secret_jwt`, `HS256`, direct-client endpoint scope, issuer-string `aud`, `POST /par` exclusion, and FAPI denial.
- Reuse that helper in `test/lockspire/release_readiness_contract_test.exs` so release/docs proof stays semantic instead of paragraph-shaped.
- Re-run and extend the existing runtime and discovery tests so the phase proof includes builder-level discovery, HTTP discovery, and representative direct-client runtime behavior.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add one shared semantic helper for client_secret_jwt support truth</name>
  <files>test/support/client_secret_jwt_support_truth.ex</files>
  <read_first>.planning/phases/90-support-truth-and-milestone-closure/90-CONTEXT.md, docs/supported-surface.md, docs/client-secret-jwt-host-guide.md</read_first>
  <action>Create `test/support/client_secret_jwt_support_truth.ex` with helper functions that assert the shipped semantic anchors directly from file contents: `client_secret_jwt`, `HS256`, issuer-string `aud`, confidential-client direct-client scope, `POST /par` exclusion, and no FAPI or mTLS equivalence claim. Keep this helper test-only and free of runtime dependencies.</action>
  <verify>mix test test/lockspire/release_readiness_contract_test.exs</verify>
  <acceptance_criteria>
    - A single helper exists for the semantic support facts Phase 90 needs to pin.
    - The helper is test-only and does not become a runtime source of truth.
  </acceptance_criteria>
  <done>Support-truth assertions can now be reused without duplicating strings across tests.</done>
</task>

<task type="auto">
  <name>Task 2: Extend release/docs contract proof with semantic client_secret_jwt assertions</name>
  <files>test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>test/lockspire/release_readiness_contract_test.exs, test/support/client_secret_jwt_support_truth.ex, docs/supported-surface.md, docs/client-secret-jwt-host-guide.md, docs/maintainer-release.md</read_first>
  <action>Extend `test/lockspire/release_readiness_contract_test.exs` minimally so it asserts the canonical support page and adjacent docs tell one coherent `client_secret_jwt` story: narrow direct-client scope, `HS256` only, issuer-string `aud`, no `PAR`, and no broader FAPI or mTLS claim. Use the shared helper instead of large prose snapshots, and keep maintainer-release checks deferential to `docs/supported-surface.md`.</action>
  <verify>mix test test/lockspire/release_readiness_contract_test.exs</verify>
  <acceptance_criteria>
    - Release-readiness tests fail if canonical docs lose the narrow `client_secret_jwt` support facts.
    - The contract test still defers to `docs/supported-surface.md` instead of creating a second support matrix.
  </acceptance_criteria>
  <done>Release/docs contract proof now guards the support-truth wording for the shipped symmetric JWT slice.</done>
</task>

<task type="auto">
  <name>Task 3: Keep runtime and discovery proof aligned with the documented support story</name>
  <files>test/lockspire/protocol/discovery_test.exs, test/lockspire/web/discovery_controller_test.exs, test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs</files>
  <read_first>test/lockspire/protocol/discovery_test.exs, test/lockspire/web/discovery_controller_test.exs, test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs, docs/supported-surface.md, docs/client-secret-jwt-host-guide.md</read_first>
  <action>Add or tighten assertions in the existing discovery and representative runtime tests so they explicitly pin the documented endpoint truth: `client_secret_jwt` appears only on the shipped direct-client surfaces, `POST /par` stays excluded, and the discovery/routing story remains FAPI-sensitive and route-local. Do not invent a new runtime test module; reuse the existing Phase 88/89 proof surfaces.</action>
  <verify>mix test test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs</verify>
  <acceptance_criteria>
    - Discovery tests and representative runtime tests encode the same endpoint-scope truth the docs now claim.
    - `POST /par` remains explicitly excluded by proof, not just by prose.
  </acceptance_criteria>
  <done>Docs, runtime, and discovery are now pinned together by repo-native automated proof.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mix test test/lockspire/release_readiness_contract_test.exs`
- [ ] `mix test test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs`
- [ ] Release/docs proof uses semantic anchors rather than broad prose snapshots.
- [ ] Runtime and discovery tests still prove the documented direct-client scope and `POST /par` exclusion.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Repo-native proof now ties support docs, discovery metadata, and representative runtime behavior together.
- The phase adds durable contract coverage without creating a second runtime truth store or brittle snapshot tests.

</success_criteria>

<output>
After completion, create `.planning/phases/90-02-SUMMARY.md`
</output>
