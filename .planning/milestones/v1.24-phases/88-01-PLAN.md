---
phase: 88
plan: 1
title: Route Shared Direct-Client JWT Auth By Registered Runtime Method
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/client_auth.ex
  - lib/lockspire/protocol/client_auth/client_secret_jwt.ex
  - test/lockspire/protocol/client_auth_test.exs
  - test/lockspire/protocol/discovery_test.exs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "JWT bearer client assertions are routed from the stored client auth method, not hard-coded to private_key_jwt."
    - "client_secret_jwt runtime support does not change published discovery auth-method truth in Phase 88."
    - "Method mismatch still fails closed as invalid_client with no fallback to other auth methods."
  artifacts:
    - path: "lib/lockspire/protocol/client_auth.ex"
      provides: "Runtime-only JWT method resolution and verifier dispatch"
      contains: "resolve_jwt_auth_method"
    - path: "lib/lockspire/protocol/client_auth/client_secret_jwt.ex"
      provides: "Symmetric verifier entry-point module"
      contains: "def verify("
    - path: "test/lockspire/protocol/client_auth_test.exs"
      provides: "Routing and mismatch proof"
      min_lines: 200
  key_links:
    - from: "lib/lockspire/protocol/client_auth.ex"
      to: "lib/lockspire/protocol/client_auth/client_secret_jwt.ex"
      via: "explicit runtime dispatch"
      pattern: "ClientSecretJwt\\.verify"
    - from: "lib/lockspire/protocol/client_auth.ex"
      to: "test/lockspire/protocol/discovery_test.exs"
      via: "public auth-method truth remains unchanged in Phase 88"
---

<objective>
Route shared direct-client JWT authentication explicitly from the registered client method while keeping Phase 88 runtime-only and preventing discovery/publication drift.

Purpose: remove the current implicit `private_key_jwt` routing bug without pulling Phase 89 metadata truth into the runtime slice.
Output: a runtime-only JWT routing seam in `ClientAuth`, a `ClientSecretJwt.verify/3` dispatch target, and proof that discovery truth stays unchanged.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-shared-client-secret-jwt-runtime/88-CONTEXT.md
@.planning/phases/88-shared-client-secret-jwt-runtime/88-RESEARCH.md
@.planning/phases/88-shared-client-secret-jwt-runtime/88-PATTERNS.md
@lib/lockspire/protocol/client_auth.ex
@lib/lockspire/protocol/discovery.ex
@test/lockspire/protocol/client_auth_test.exs
@test/lockspire/protocol/discovery_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split runtime JWT routing from published auth-method truth</name>
  <files>lib/lockspire/protocol/client_auth.ex, test/lockspire/protocol/discovery_test.exs</files>
  <read_first>lib/lockspire/protocol/discovery.ex, .planning/phases/88-shared-client-secret-jwt-runtime/88-CONTEXT.md</read_first>
  <action>Change `ClientAuth` so JWT bearer assertions parse into a neutral attempted mode such as `:jwt_client_assertion`, then resolve the concrete verifier only after client lookup. Add a dedicated runtime-only auth-method registry or resolver for `client_secret_jwt`; do not change `supported_auth_method_names/0` or any public discovery list in this phase. The end state must let runtime code recognize `client_secret_jwt` without making discovery publish it before Phase 89.</action>
  <verify>mix test test/lockspire/protocol/discovery_test.exs test/lockspire/protocol/client_auth_test.exs</verify>
  <acceptance_criteria>
    - `lib/lockspire/protocol/client_auth.ex` no longer returns `:private_key_jwt` directly from JWT assertion parsing.
    - Discovery tests remain green without adding `client_secret_jwt` to published metadata.
  </acceptance_criteria>
  <done>`ClientAuth` resolves JWT verifier choice from stored client state, and Phase 88 does not change discovery/public support truth.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit symmetric verifier dispatch with fail-closed mismatch behavior</name>
  <files>lib/lockspire/protocol/client_auth.ex, lib/lockspire/protocol/client_auth/client_secret_jwt.ex</files>
  <read_first>lib/lockspire/protocol/client_auth/private_key_jwt.ex, .planning/phases/88-shared-client-secret-jwt-runtime/88-PATTERNS.md</read_first>
  <action>Create `Lockspire.Protocol.ClientAuth.ClientSecretJwt.verify/3` as the symmetric verifier entry point and dispatch to it only when the stored client method is `:client_secret_jwt`. Keep endpoint-facing behavior generic `invalid_client`, preserve the existing `private_key_jwt` path, and reject method mismatch with the same fail-closed posture used today. Do not add endpoint-local branching.</action>
  <verify>mix test test/lockspire/protocol/client_auth_test.exs</verify>
  <acceptance_criteria>
    - `lib/lockspire/protocol/client_auth.ex` calls `ClientSecretJwt.verify/3` only for registered `:client_secret_jwt` clients.
    - No code path falls back from a JWT assertion to `client_secret_basic`, `client_secret_post`, or `private_key_jwt`.
  </acceptance_criteria>
  <done>The shared auth pipeline has explicit verifier dispatch for `client_secret_jwt` and preserves fail-closed mismatch behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Prove routing and mismatch behavior without leaking assertion material</name>
  <files>test/lockspire/protocol/client_auth_test.exs</files>
  <read_first>test/lockspire/protocol/client_auth_test.exs, test/lockspire/audit/event_test.exs</read_first>
  <action>Add focused tests that prove JWT assertions for `client_secret_jwt` clients no longer route through the asymmetric verifier and that mismatched auth-method attempts still fail as `invalid_client`. Keep the assertions at the existing `ClientAuth.Error.reason_code` layer and do not assert on leaked JWT payload or secret-derived values.</action>
  <verify>mix test test/lockspire/protocol/client_auth_test.exs</verify>
  <acceptance_criteria>
    - Tests cover successful dispatch selection for `client_secret_jwt`.
    - Tests cover method mismatch failure without raw assertion leakage.
  </acceptance_criteria>
  <done>Unit proof pins the routing contract before stricter verifier and cross-surface proof work begins.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mix test test/lockspire/protocol/client_auth_test.exs test/lockspire/protocol/discovery_test.exs`
- [ ] `ClientAuth.authenticate/3` remains the only shared direct-client auth entry point.
- [ ] `client_secret_jwt` runtime support does not appear in published discovery metadata yet.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- JWT bearer assertions are no longer implicitly routed to `private_key_jwt`.
- Phase 88 runtime support is isolated from Phase 89 discovery/support-truth publication.

</success_criteria>

<output>
After completion, create `.planning/phases/88-01-SUMMARY.md`
</output>
