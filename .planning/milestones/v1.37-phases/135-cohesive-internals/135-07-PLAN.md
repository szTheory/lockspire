---
phase: 135-cohesive-internals
plan: 07
type: execute
wave: 3
depends_on: [135-06]
files_modified:
  - lib/lockspire/protocol/token_exchange/internal/client_authentication.ex
  - lib/lockspire/protocol/token_exchange/internal/resource_selection.ex
  - lib/lockspire/protocol/token_exchange/internal/grant_polling.ex
  - lib/lockspire/protocol/token_exchange/internal/grant_support.ex
  - lib/lockspire/protocol/token_exchange/internal/token_endpoint_dpop.ex
  - test/lockspire/protocol/token_exchange/client_authentication_test.exs
  - test/lockspire/protocol/token_exchange/resource_selection_test.exs
  - test/lockspire/protocol/token_exchange/polling_test.exs
autonomous: true
requirements: [COH-03, COH-04, COH-05]
must_haves:
  truths:
    - "Client authentication, resource narrowing, and device/CIBA polling each have one focused internal owner receiving explicit dependencies."
    - "Security ordering, error classifications, and grant state matrices remain identical through the stable token facade."
  artifacts:
    - path: lib/lockspire/protocol/token_exchange/internal/client_authentication.ex
      provides: client credential/JWT/mTLS authentication coordination
    - path: lib/lockspire/protocol/token_exchange/internal/resource_selection.ex
      provides: grant resource and audience narrowing validation
    - path: lib/lockspire/protocol/token_exchange/internal/grant_polling.ex
      provides: device and CIBA poll-result translation
  key_links:
    - from: lib/lockspire/protocol/token_exchange/internal/grant_support.ex
      to: lib/lockspire/protocol/token_exchange/internal/client_authentication.ex
      via: explicit dependency bundle and neutral result
      pattern: "ClientAuthentication"
---

<objective>
Split the validation and polling half of grant orchestration into focused collaborators.

Purpose: Make security decisions navigable and independently characterized while preserving coordinator ordering and public outcomes.
Output: Client-authentication, resource-selection, and polling collaborators behind neutral grants.
</objective>

<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/135-cohesive-internals/135-06-SUMMARY.md
@lib/lockspire/protocol/token_exchange/internal/grant_support.ex
@lib/lockspire/protocol/token_exchange/internal/token_endpoint_dpop.ex
@test/lockspire/protocol/token_exchange/characterization_test.exs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extract client authentication and sender-constraint coordination</name>
  <files>lib/lockspire/protocol/token_exchange/internal/client_authentication.ex, lib/lockspire/protocol/token_exchange/internal/grant_support.ex, lib/lockspire/protocol/token_exchange/internal/token_endpoint_dpop.ex, test/lockspire/protocol/token_exchange/client_authentication_test.exs</files>
  <behavior>
    - Basic/POST/private-key-JWT/client-secret-JWT/none authentication retains current precedence, client binding, and safe errors.
    - mTLS/DPoP policy and replay dependencies come from the bundle and preserve nonce/error behavior.
    - Authentication completes before any grant polling or token persistence.
  </behavior>
  <action>Move client extraction/auth-option construction and the shared authentication step into `ClientAuthentication`, accepting only params, authorization material, and the explicit dependency subset. Update TokenEndpointDPoP to consume explicit policy/replay/clock/config fields instead of reading request options. Keep grant coordinators responsible for order: authentication and sender constraints precede state lookup, resource validation, and persistence. Preserve every auth method and public TokenResult error.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/client_authentication_test.exs test/lockspire/protocol/client_auth_test.exs test/lockspire/protocol/token_endpoint_dpop_test.exs test/lockspire/protocol/token_exchange/characterization_test.exs</automated></verify>
  <done>Authentication and sender constraints are explicitly injected, independently testable, and ordered before issuance.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Extract grant resource narrowing</name>
  <files>lib/lockspire/protocol/token_exchange/internal/resource_selection.ex, lib/lockspire/protocol/token_exchange/internal/grant_support.ex, test/lockspire/protocol/token_exchange/resource_selection_test.exs</files>
  <behavior>
    - No requested resource preserves the grant audience; valid narrowing returns only allowed resources.
    - Empty, malformed, duplicated, or broader resources retain current `invalid_target`/reason results.
    - The retained public test helper keeps its exact arity and output shape.
  </behavior>
  <action>Move requested-resource normalization, grant-audience comparison, duplicate handling, and safe OAuth errors into a pure `ResourceSelection` collaborator. Keep the public GrantSupport helper delegating through its neutral internal boundary and Phase 134 compatibility adapter. Do not couple this module to token issuance, polling, persistence, or request option bags.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/resource_selection_test.exs test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs test/lockspire/protocol/rfc8693_exchange_test.exs</automated></verify>
  <done>Resource selection is pure, focused, and retains every grant/public helper result.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Extract device and CIBA polling translation</name>
  <files>lib/lockspire/protocol/token_exchange/internal/grant_polling.ex, lib/lockspire/protocol/token_exchange/internal/grant_support.ex, test/lockspire/protocol/token_exchange/polling_test.exs</files>
  <behavior>
    - Device and CIBA pending, slow-down, approved, denied, expired, consumed, mismatch, and malformed outcomes map to existing neutral errors/domains.
    - Polling invokes only the explicit device/CIBA store and clock dependencies.
  </behavior>
  <action>Move presented-code extraction, store polling calls, and result-map translation into `GrantPolling`, retaining separate device and CIBA entry points so their state semantics remain obvious. Pass only the relevant store/clock fields. Keep redemption, issuance, audit, and telemetry outside this collaborator.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/polling_test.exs test/lockspire/protocol/token_exchange/device_code_test.exs test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs</automated></verify>
  <done>Polling is a focused explicitly injected state translator and all device/CIBA outcomes remain stable.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Client credentials/proofs → authenticated client | Spoofed identity would authorize token issuance. |
| Requested resources → token audience | Over-broad selection would escalate access. |
| Device/CIBA poll input → approved state | State confusion could issue before approval. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-135-18 | Spoofing | client authentication | critical | mitigate | Full method matrix, explicit dependencies, and pre-persistence ordering. |
| T-135-19 | Elevation of privilege | resource selection | critical | mitigate | Pure narrowing rules and invalid-target characterization. |
| T-135-20 | Tampering | polling translation | critical | mitigate | Complete state matrices through real aggregate stores. |
</threat_model>
<verification>`mix test test/lockspire/protocol/token_exchange/client_authentication_test.exs test/lockspire/protocol/token_exchange/resource_selection_test.exs test/lockspire/protocol/token_exchange/polling_test.exs test/lockspire/protocol/token_exchange/characterization_test.exs`</verification>
<success_criteria>Authentication, resource selection, and polling are focused explicit collaborators with unchanged five-flow behavior.</success_criteria>
<output>Create `.planning/phases/135-cohesive-internals/135-07-SUMMARY.md` when done.</output>
