---
phase: 134-architecture-topology
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/discovery_routes.ex
  - lib/lockspire/protocol/discovery.ex
  - lib/lockspire/web/controllers/discovery_controller.ex
  - test/lockspire/discovery_routes_test.exs
  - test/lockspire/protocol/discovery_test.exs
  - test/lockspire/web/discovery_controller_test.exs
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "Protocol discovery accepts neutral mounted-route capability and has no compile/runtime/export dependency on Lockspire.Web (D-01, D-09)."
    - "Discovery remains mount-truthful, preserves openid_configuration/0 and current metadata, and honors the existing :discovery_router override through a compatibility resolver at the delivery/configuration edge (D-02, D-09)."
    - "The discovery/controller/router cycle is absent from Mix xref output (D-03)."
  artifacts:
    - path: lib/lockspire/discovery_routes.ex
      provides: delivery-edge resolution of mounted route paths and legacy router configuration
    - path: lib/lockspire/protocol/discovery.ex
      provides: delivery-neutral metadata calculation from supplied route paths
  key_links:
    - from: lib/lockspire/web/controllers/discovery_controller.ex
      to: lib/lockspire/protocol/discovery.ex
      via: resolved neutral mounted-route input
      pattern: "DiscoveryRoutes|route_paths"
---

<objective>
Invert mounted-route discovery so Phoenix reflection stays outside protocol while public discovery behavior remains compatible.

Purpose: Remove the concrete discovery/web/router cycle without replacing it with a transitive delivery dependency or static over-advertisement.
Output: Neutral route-capability resolver, delivery-aware controller call, and regression coverage.

Decision trace: Implements D-01 through D-04, D-09, D-12, and D-13.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/134-architecture-topology/134-CONTEXT.md
@.planning/phases/134-architecture-topology/134-RESEARCH.md
@lib/lockspire/protocol/discovery.ex
@lib/lockspire/web/controllers/discovery_controller.ex
@lib/lockspire/web/router.ex
@test/lockspire/protocol/discovery_test.exs
@test/lockspire/web/discovery_controller_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize mounted discovery and introduce the delivery-edge capability</name>
  <files>lib/lockspire/discovery_routes.ex, lib/lockspire/protocol/discovery.ex, test/lockspire/discovery_routes_test.exs, test/lockspire/protocol/discovery_test.exs</files>
  <behavior>
    - Supplied route paths advertise only mounted endpoints and retain registration-disabled and authorization-surface invariants.
    - openid_configuration/0 and published_token_endpoint_auth_methods_supported/0 remain exported with unchanged result shapes.
    - Existing :discovery_router overrides continue to resolve through a compatibility path outside protocol.
  </behavior>
  <action>Write RED characterization for default metadata, a router missing selected endpoints, alternate router configuration, registration policy, resource indicators/RAR, and both public zero-arity exports. Add a non-protocol `Lockspire.DiscoveryRoutes` capability that owns Phoenix route reflection and the legacy `Application.get_env(:lockspire, :discovery_router, ...)` compatibility behavior without introducing a hard compile reference from protocol to the default router. Refactor Discovery's internal endpoint selection to accept a normalized path set; retain zero-arity behavior by consuming neutral configured capability rather than naming a Web module. Keep repository-backed policy truth and all documented metadata unchanged.</action>
  <verify><automated>mix test test/lockspire/discovery_routes_test.exs test/lockspire/protocol/discovery_test.exs</automated></verify>
  <done>Discovery calculation is route-input driven, existing zero-arity/configuration behavior is characterized, and protocol source contains no Web reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the Phoenix controller and prove the cycle removed</name>
  <files>lib/lockspire/web/controllers/discovery_controller.ex, lib/lockspire/discovery_routes.ex, test/lockspire/web/discovery_controller_test.exs, test/lockspire/discovery_routes_test.exs</files>
  <behavior>
    - The controller supplies concrete mounted paths and returns the same cache/status/JSON contract.
    - Mounted and unmounted endpoint cases are truthful through the real delivery adapter.
    - Mix xref no longer reports the three-node discovery cycle.
  </behavior>
  <action>Make DiscoveryController resolve the concrete router/capability at the delivery edge and pass only normalized paths to Discovery. Preserve its cache-control, status, and JSON representation. Extend controller tests for default and alternate mounted route sets. Run the focused xref assertion and inspect the complete output so the former Discovery/DiscoveryController/Router path is absent without weakening other cycle visibility.</action>
  <verify><automated>mix test test/lockspire/discovery_routes_test.exs test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs &amp;&amp; mix xref graph --format cycles</automated></verify>
  <done>Web owns route reflection, protocol consumes neutral input, compatibility remains green, and the discovery cycle is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mounted Phoenix routes → public metadata | Incorrect capability input can over-advertise security endpoints. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-134-08 | Spoofing | discovery endpoint advertisement | high | mitigate | Real route-path characterization for mounted/unmounted endpoints and policy gates. |
| T-134-09 | Tampering | legacy router override | medium | mitigate | Compatibility resolver tests alternate router inputs without protocol Web dependency. |
</threat_model>

<verification>
- `mix test test/lockspire/discovery_routes_test.exs test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs`
- `mix xref graph --format cycles`
</verification>

<success_criteria>
- Protocol discovery has no Web reference.
- Discovery remains mounted-route truthful and public APIs/configuration remain compatible.
- The discovery/web/router cycle is removed.
</success_criteria>

<output>Create `.planning/phases/134-architecture-topology/134-03-SUMMARY.md` when done.</output>
