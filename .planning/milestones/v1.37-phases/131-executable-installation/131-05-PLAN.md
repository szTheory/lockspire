---
phase: 131-executable-installation
plan: 05
type: execute
wave: 3
depends_on:
  - 131-01
  - 131-04
files_modified:
  - lib/lockspire/generators/templates.ex
  - lib/lockspire/generators/install.ex
  - lib/mix/tasks/lockspire.install.ex
  - priv/templates/lockspire.install/default_smoke_e2e_test.exs
  - priv/templates/lockspire.install/fapi_smoke_e2e_test.exs
  - test/integration/install_generator_test.exs
autonomous: true
requirements:
  - INST-05
must_haves:
  truths:
    - "Ordinary generation produces a behavioral smoke test that runs against actual generated modules under the default secure profile (D-15, D-16)."
    - "FAPI assertions are generated only through an explicit documented opt-in and do not redefine the server's default profile (D-15)."
    - "Generated authorization examples keep PKCE S256 and exact redirect matching and do not register protocol-reserved openid as an application scope."
  artifacts:
    - path: "priv/templates/lockspire.install/default_smoke_e2e_test.exs"
      provides: "Default-profile generated behavioral smoke"
    - path: "priv/templates/lockspire.install/fapi_smoke_e2e_test.exs"
      provides: "Explicit opt-in FAPI proof"
    - path: "lib/lockspire/generators/templates.ex"
      provides: "Profile-aware generated test inventory"
    - path: "test/integration/install_generator_test.exs"
      provides: "Real render/compile/execute and option-selection proof"
  key_links:
    - from: "lib/mix/tasks/lockspire.install.ex"
      to: "lib/lockspire/generators/install.ex"
      via: "strict --with-fapi-smoke option"
      pattern: "with_fapi_smoke"
    - from: "lib/lockspire/generators/install.ex"
      to: "lib/lockspire/generators/templates.ex"
      via: "assign-aware template selection"
      pattern: "Templates"
---

<objective>
Separate generated baseline proof from optional FAPI proof and execute both against real generated host artifacts.

Purpose: Ensure a new adopter's normal test suite is truthful under Lockspire defaults without weakening OAuth/OIDC security or overstating FAPI posture.
Output: A default smoke template, opt-in FAPI template selection, strict installer option/help, and compiled behavioral integration proof.

Decision trace: Implements D-15 and D-16 while preserving all AGENTS.md OAuth/OIDC security defaults.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/131-executable-installation/131-CONTEXT.md
@.planning/phases/131-executable-installation/131-RESEARCH.md
@.planning/phases/131-executable-installation/131-PATTERNS.md
@.planning/phases/131-executable-installation/131-VALIDATION.md
@.planning/phases/131-executable-installation/131-01-SUMMARY.md
@.planning/phases/131-executable-installation/131-04-SUMMARY.md
@priv/templates/lockspire.install/fapi_smoke_e2e_test.exs
@test/integration/phase6_onboarding_e2e_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make generated smoke inventory profile-aware</name>
  <files>lib/lockspire/generators/templates.ex, lib/lockspire/generators/install.ex, lib/mix/tasks/lockspire.install.ex, priv/templates/lockspire.install/default_smoke_e2e_test.exs, test/integration/install_generator_test.exs</files>
  <behavior>
    - Default install always generates the default smoke and does not generate FAPI proof.
    - --with-fapi-smoke is a strict boolean option that additionally generates the FAPI test.
    - Unknown options still fail; help and final instructions name exact default and opt-in commands.
    - Template inventory and install manifest match the selected outputs on fresh/repeat generation.
  </behavior>
  <action>Add a default_smoke_e2e_test.exs managed template and make Templates/Install select optional entries from assigns without changing ownership semantics. Parse and document strict --with-fapi-smoke in Mix.Tasks.Lockspire.Install, propagate with_fapi_smoke through build_assigns, and make ordinary output exclude the FAPI file per D-15. Keep the default smoke enabled under standard `_test.exs` discovery. Emit opted-in FAPI proof with a non-default-discovery `.exs` filename and document an explicit `mix test path/to/lockspire_fapi_smoke_e2e.exs --include fapi` command, so a normal `mix test` cannot accidentally assert the opt-in profile. Update inventory, manifest, repeat-install, and option tests in install_generator_test.exs. Account for Plan 04's migration-aware manifest signature when updating Install.</action>
  <verify>
    <automated>mix test test/integration/install_generator_test.exs</automated>
  </verify>
  <done>Default and opt-in template sets are deterministic, documented, manifest-tracked, and selected only by the explicit strict option.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute default-secure and explicit FAPI generated behavior</name>
  <files>priv/templates/lockspire.install/default_smoke_e2e_test.exs, priv/templates/lockspire.install/fapi_smoke_e2e_test.exs, test/integration/install_generator_test.exs</files>
  <behavior>
    - Rendered default smoke compiles and executes discovery/JWKS plus authorization-code S256 routing under security_profile :none.
    - Default smoke proves exact redirect mismatch rejection and never disables PKCE or changes secure defaults.
    - Client registration uses application scopes such as profile while authorization may request openid profile.
    - Rendered FAPI proof executes only under an explicitly selected FAPI server/client profile.
  </behavior>
  <action>Build the default template from the existing Phase 6 endpoint/client/PKCE/session patterns, using the actual generated endpoint/router/seam modules and profile :none. Prove discovery and JWKS availability, authorization-code request routing with S256, and exact redirect rejection; keep allowed_scopes application-only while requesting openid separately. Revise the FAPI template so its setup explicitly enables the intended server/client profile and its filename/tag/documented command prevents ordinary default execution. In install_generator_test.exs render, compile, and execute both selected outputs rather than inspecting strings or compiling substitute modules per D-16. Preserve PKCE S256, exact redirect validation, no implicit flow, and no alg=none; do not broaden into Phase 133's separate-origin lifecycle.</action>
  <verify>
    <automated>mix test test/integration/install_generator_test.exs</automated>
  </verify>
  <done>The unchanged rendered default smoke runs under the default profile, optional FAPI behavior runs only when explicitly selected/configured, and neither proof weakens security defaults.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generator options → emitted security proof | Install flags decide which adopter tests and claims are created. |
| Generated test → OAuth/OIDC endpoint | Example parameters can normalize safe or unsafe integration behavior for adopters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-131-18 | Tampering | default security profile | high | mitigate | Execute baseline proof under profile :none and assert real S256/exact-redirect behavior without config overrides that weaken defaults. |
| T-131-19 | Spoofing | generated authorization example | high | mitigate | Use random state/nonce/PKCE inputs and the real registered redirect/client path in generated behavior. |
| T-131-20 | Elevation of privilege | FAPI option boundary | high | mitigate | Require explicit --with-fapi-smoke selection plus explicit FAPI runtime/client configuration. |
| T-131-21 | Repudiation | generated proof substitution | medium | mitigate | Render, compile, and execute selected templates against actual generated modules and persist inventory in the manifest. |

## ASVS L1 Control-to-Automated-Test Evidence

| ASVS L1 control | Threats | Required automated evidence |
|---|---|---|
| V6 Stored Cryptography / protocol cryptography configuration — secure algorithms and proof profile cannot be weakened implicitly | T-131-18 | `mix test test/integration/install_generator_test.exs` must execute the rendered default smoke under `security_profile: :none` and prove PKCE S256 plus exact redirect rejection without overrides that weaken defaults. |
| V2 Authentication and V4 Access Control — authorization requests bind unpredictable browser state to the registered client and redirect | T-131-19 | `mix test test/integration/install_generator_test.exs` must execute the generated request using random state/nonce/verifier and the registered client/redirect path. |
| V14 Configuration — stronger profile evidence is explicit and isolated from defaults | T-131-20 | `mix test test/integration/install_generator_test.exs` must prove ordinary generation excludes FAPI proof and `--with-fapi-smoke` plus explicit server/client configuration is required to execute it. |
</threat_model>

<verification>
- `mix test test/integration/install_generator_test.exs`
- `mix test.fast`
</verification>

<success_criteria>
- Default generation/test execution is profile-neutral and secure.
- FAPI proof is opt-in in generation, setup, and command.
- Actual generated modules and templates—not substitutes—provide the evidence.
- Plan completion is blocked unless every high-severity threat above has green named automated evidence; any unresolved high-severity threat prohibits creating 131-05-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/131-executable-installation/131-05-SUMMARY.md` when done.
</output>
