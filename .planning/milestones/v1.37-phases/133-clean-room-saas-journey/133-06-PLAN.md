---
phase: 133-clean-room-saas-journey
plan: 06
type: execute
wave: 6
depends_on: [133-05]
files_modified:
  - scripts/acceptance/clean_room_saas_journey.py
  - test/integration/phase133_clean_room_saas_journey_test.exs
  - mix.exs
  - scripts/ci/run_test_matrix.sh
  - .github/workflows/ci.yml
autonomous: true
requirements: [E2E-01, E2E-05, E2E-06]
must_haves:
  truths:
    - "The same confidential-client/provider lab completes Plan 03's dedicated DPoP login through token and userinfo nonce retries, then uses only its opaque server-side session to receive a resource nonce challenge, succeed with a new proof, and reject byte-identical replay (D-15)."
    - "The provider has no replay override; replay rejection remains observable after a provider restart and therefore exercises the configured Ecto repository (D-16)."
    - "One named Mix/CI command runs the full fresh journey, scans all evidence, and leaves no process, database, plaintext channel, or temp tree (D-04, D-17)."
  artifacts:
    - path: scripts/acceptance/clean_room_saas_journey.py
      provides: black-box orchestration of the client-owned DPoP session's resource nonce/retry/replay operations
    - path: mix.exs
      provides: discoverable `test.clean-room.e2e` alias
    - path: scripts/ci/run_test_matrix.sh
      provides: integration-lane invocation and timing evidence
  key_links:
    - from: scripts/acceptance/clean_room_saas_journey.py
      to: provider protected API
      via: opaque client session commanding client-owned exact htu/htm/ath/nonce proofs without exposing credentials/key/token/proof
      pattern: "DPoP-Nonce|use_dpop_nonce"
    - from: .github/workflows/ci.yml
      to: mix.exs
      via: deterministic clean-room command in PostgreSQL integration environment
      pattern: "test.clean-room.e2e"
---

<objective>
Close the journey with durable DPoP replay proof, full evidence hygiene, and a discoverable CI gate.

Purpose: Demonstrate the highest-risk sender-constraint behavior under the same package-clean composition and make the proof sustainable.
Output: DPoP nonce/replay steps, restart durability assertion, maintained Mix alias, and CI integration.

Decision trace: Implements D-04, D-13 through D-18 and completes E2E-06 without entering Phase 137 publication proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-clean-room-saas-journey/133-CONTEXT.md
@.planning/phases/133-clean-room-saas-journey/133-RESEARCH.md
@.planning/phases/133-clean-room-saas-journey/133-VALIDATION.md
@.planning/phases/133-clean-room-saas-journey/133-05-SUMMARY.md
@test/integration/phase81_generated_host_route_protection_e2e_test.exs
@test/integration/protected_resource_dpop_default_store_test.exs
@mix.exs
@scripts/ci/run_test_matrix.sh
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Complete nonce retry and byte-identical durable DPoP replay rejection</name>
  <files>scripts/acceptance/clean_room_saas_journey.py, test/integration/phase133_clean_room_saas_journey_test.exs</files>
  <behavior>
    - The DPoP journey completes the DPoP-specific start/callback and safe receipt proves the server-selected profile, token-endpoint nonce retry, DPoP token type, same-key userinfo nonce retry, subject match, and opaque session handoff; bearer credentials remain unused.
    - The first client-owned resource operation sends a fresh proof without a resource nonce, observes upstream 401/use_dpop_nonce and DPoP-Nonce presence, stores the nonce server-side, and returns only safe status/challenge-present evidence.
    - A newly signed proof with exact GET, externally visible htu, ath, and challenged nonce returns 200 with semantic confirmation.dpop_jkt.
    - Resubmitting the exact accepted compact proof bytes returns documented rejection; regenerating jti/iat is not accepted as replay proof.
    - After provider restart with the same dedicated database, the accepted proof remains rejected without any configured replay override or internal row inspection.
  </behavior>
  <action>Drive the client app's completed `/oauth/dpop/start` flow across provider login/consent and callback, then assert only its safe receipt booleans/handles show that Plan 03 performed the authorization-server token nonce retry, received DPoP token type, completed same-key DPoP userinfo nonce retry, validated OIDC, and handed off an opaque session. From the same browser session, call client-owned acceptance operations that make the protected-resource request: first without a resource nonce, then with a fresh proof carrying the server-side stored nonce, then replay the exact accepted proof ciphertext owned by the client session. On success the client must internally compare semantic `confirmation.dpop_jkt` to its owned key and expose only a matching safe handle/boolean. The operation may expose upstream status, OAuth challenge code, `dpop_nonce_present`, allowlisted semantic response, and safe JKT handle only; the outer runner must never construct or receive the client secret, private key, access token, nonce value, nonce-bearing proof, or proof bytes. Restart only the provider against its same database and command the client to replay those same stored bytes again. Assert documented HTTP-derived safe statuses/challenges per D-15/D-16 and provider durable rejection; never query replay rows or inject a store. Keep the independent bearer happy/lifecycle path green.</action>
  <verify><automated>mix test test/integration/phase133_clean_room_saas_journey_test.exs --only dpop</automated></verify>
  <done>The same-byte proof succeeds once and is rejected before and after provider restart through the package-clean host's default repository-backed path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ratchet full-run redaction, teardown, alias, and CI execution</name>
  <files>scripts/acceptance/clean_room_saas_journey.py, test/integration/phase133_clean_room_saas_journey_test.exs, mix.exs, scripts/ci/run_test_matrix.sh, .github/workflows/ci.yml</files>
  <behavior>
    - `mix test.clean-room.e2e` performs environment probe, fresh package/provider/client provision, all named journey groups, sentinel scan, and teardown.
    - The full command fails if any secret sentinel or raw Authorization/Cookie value appears in console, redacted evidence, or retained CI artifacts.
    - Success and induced failure both leave no child processes, unique databases, credential file, or run directory.
    - CI invokes the command once in its PostgreSQL-backed integration lane and records timing/status without Docker or publication assertions.
    - CI caches only external child dependency directories under `tmp/clean-room-cache/{provider,client}/deps`, keyed by OS, OTP, Elixir, role, and the corresponding checked-in lock hash; child builds and run data are never cached.
  </behavior>
  <action>Add the maintained alias and preferred environment, then integrate it into the existing test matrix without duplicating fast/integration execution. Add one pinned `actions/cache` step covering only `tmp/clean-room-cache/provider/deps` and `tmp/clean-room-cache/client/deps`, with a key containing runner OS, OTP, Elixir, role/format version, and `hashFiles` for both child locks; do not use broad restore keys across runtime versions and do not cache child `_build`, logs, credentials, databases, or run roots. Pass that bounded cache root to child dependency setup, which still runs `mix deps.get --check-locked` and provenance validation every time. Exercise a deliberately failing redaction-safe case before the green full run and assert teardown by resolved identifiers, not broad deletion targets. Retain redacted logs only on failure. Preserve D-03's boundary by leaving publication-specific release proof to Phase 137.</action>
  <verify><automated>mix test.clean-room.e2e &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix test.fast &amp;&amp; mix test.integration &amp;&amp; mix qa &amp;&amp; mix docs.verify</automated></verify>
  <done>The clean-room command is discoverable, deterministic, CI-executed, entirely redacted, and cleanup-complete, with the full repository gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Client DPoP key/proof → provider resource | Sender proof authorizes use of a bound access token. |
| Provider process → durable database | Replay uniqueness must survive process restart. |
| CI runner → retained artifacts | Failure evidence may outlive the ephemeral run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-133-20 | Spoofing/Elevation of privilege | DPoP proof | critical | mitigate | Exact htu/htm/ath/nonce proof success and byte-identical replay rejection. |
| T-133-21 | Tampering | replay persistence | high | mitigate | No override, configured Ecto repo, and post-provider-restart HTTP rejection. |
| T-133-22 | Information disclosure | CI evidence | high | mitigate | Sentinel/raw-header scans before retention; only redacted bounded failure artifacts. |
| T-133-23 | Denial of service | CI teardown | medium | mitigate | Exit/signal traps, bounded waits, explicit per-run database/process/path cleanup tests. |
| T-133-24 | Elevation of privilege | DPoP client selection | high | mitigate | Complete the fixed DPoP route/profile and consume only its opaque server-side session; never substitute bearer credentials. |
| T-133-25 | Tampering | child dependency cache | high | mitigate | Lock-hash/runtime-keyed dependency-only paths, `--check-locked`, provenance checks, and fresh child compilation. |
| T-133-26 | Information disclosure | DPoP resource operation | critical | mitigate | Outer runner receives only safe statuses/handles; client backend retains encrypted key/token/nonce/proof and clears them terminally. |
| T-133-27 | Spoofing | resource nonce domain | critical | mitigate | Client backend uses only the resource challenge nonce with fresh exact htu/htm/ath proof and never reuses AS/userinfo nonce state. |
| T-133-28 | Elevation of privilege | opaque DPoP session | high | mitigate | Resource operations resolve only the rotated server-side session handle and reject caller-provided key/token/proof inputs. |
</threat_model>

<verification>
- `mix test.clean-room.e2e`
- `mix compile --warnings-as-errors`
- `mix test.fast && mix test.integration && mix qa && mix docs.verify`
</verification>

<success_criteria>
- E2E-06 proves nonce handling and durable exact-proof replay rejection over HTTP.
- The DPoP proof consumes only the separately bootstrapped DPoP client identity/secret; ordinary lifecycle remains on the bearer client.
- The full journey is one deterministic maintainer/CI command with no secret retention or leaked resources.
- All six E2E requirements and all high-severity threats have current automated evidence.
</success_criteria>

<output>Create `.planning/phases/133-clean-room-saas-journey/133-06-SUMMARY.md` when done.</output>
