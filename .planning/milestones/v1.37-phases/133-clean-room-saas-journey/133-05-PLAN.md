---
phase: 133-clean-room-saas-journey
plan: 05
type: execute
wave: 5
depends_on: [133-04]
files_modified:
  - scripts/acceptance/clean_room_saas_journey.py
  - test/integration/phase133_clean_room_saas_journey_test.exs
autonomous: true
requirements: [E2E-04, E2E-05]
must_haves:
  truths:
    - "One refresh rotates the token; reuse of the prior refresh token returns invalid_grant and revokes the family, which authenticated introspection reports inactive (D-11)."
    - "Authenticated revocation is idempotent and lifecycle evidence never claims an issued self-contained JWT is instantly offline-invalid before expiry (D-11, D-12)."
    - "Redirect drift, code reuse, state mismatch, nonce mismatch, missing token, wrong audience, and insufficient scope fail over real HTTP with stable documented outcomes (D-13, D-14)."
  artifacts:
    - path: scripts/acceptance/clean_room_saas_journey.py
      provides: named lifecycle and negative wire steps
    - path: test/integration/phase133_clean_room_saas_journey_test.exs
      provides: E2E-04/E2E-05 acceptance matrix
  key_links:
    - from: scripts/acceptance/clean_room_saas_journey.py
      to: provider token/introspection/revocation endpoints
      via: confidential Basic-authenticated lifecycle requests
      pattern: "introspect|revoke|refresh_token"
    - from: scripts/acceptance/clean_room_saas_journey.py
      to: provider authorization/protected API endpoints
      via: independent negative transactions and stable wire assertions
      pattern: "invalid_grant|invalid_token|insufficient_scope"
---

<objective>
Extend the clean-room journey with truthful token lifecycle proof and the complete non-DPoP negative matrix.

Purpose: Turn the positive composition proof into an adversarial acceptance contract without relying on internal state.
Output: Named lifecycle and failure steps with stable HTTP assertions.

Decision trace: Implements D-11 through D-14 and carries D-05, D-09, D-12, and D-17 through failure handling.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-clean-room-saas-journey/133-CONTEXT.md
@.planning/phases/133-clean-room-saas-journey/133-RESEARCH.md
@.planning/phases/133-clean-room-saas-journey/133-VALIDATION.md
@.planning/phases/133-clean-room-saas-journey/133-04-SUMMARY.md
@test/integration/phase3_oidc_token_lifecycle_e2e_test.exs
@test/integration/phase81_generated_host_route_protection_e2e_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Rotate, reuse-contain, introspect, and revoke over HTTP</name>
  <files>scripts/acceptance/clean_room_saas_journey.py, test/integration/phase133_clean_room_saas_journey_test.exs</files>
  <behavior>
    - First refresh returns a distinct access/refresh pair; replaying the old refresh returns 400 invalid_grant.
    - Authenticated introspection after reuse reports only inactive lifecycle truth for affected family material.
    - Revocation accepts current or already revoked material idempotently with its documented empty success response.
    - Test names/copy distinguish durable authorization-server lifecycle truth from expiry-bound offline JWT validation.
  </behavior>
  <action>Use a fresh successful bearer-client transaction from Plan 04 and that client's confidential `client_secret_basic` credentials for token, introspection, and revocation; assert the DPoP client identity is not selected. Keep token bytes only in memory. Assert rotation inequality without printing values, old-token reuse status/error, family inactivity through authenticated introspection, and two successful revocation calls per D-11. Explicitly encode D-12 in test names and assertions: do not use protected-resource rejection of a previously issued self-contained JWT as immediate revocation evidence.</action>
  <verify><automated>mix test --include integration test/integration/phase133_clean_room_saas_journey_test.exs --only lifecycle</automated></verify>
  <done>Lifecycle endpoints demonstrate rotation, family-wide reuse containment, introspection truth, and idempotent revocation without overstating JWT revocation semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise the redirect, callback, code, token, audience, and scope failure matrix</name>
  <files>scripts/acceptance/clean_room_saas_journey.py, test/integration/phase133_clean_room_saas_journey_test.exs</files>
  <behavior>
    - Redirect drift is rejected; a redeemed authorization code returns invalid_grant on byte-identical reuse.
    - Wrong callback state is rejected before exchange and terminally consumes its transaction; nonce mismatch blocks client completion.
    - Missing and wrong-audience tokens return 401 invalid_token with documented challenge; under-scoped valid token returns 403 insufficient_scope with required scope challenge.
    - Each case owns fresh setup except the explicit code replay and exposes status/error/challenge only.
  </behavior>
  <action>Add individually named real-listener cases for every non-DPoP item in D-13. Arrange nonce mismatch by using an acceptance-only client endpoint to replace the stored expected nonce after authorization start; the provider must still issue and sign its ordinary ID token, and the client's full signature/claim verifier must reject it. Prove state failure preceded token exchange using the client's safe request counter/stage receipt rather than provider rows. Assert only public status, OAuth/OIDC JSON error, and documented authentication challenges per D-14. Route all failures through sentinel-safe assertion helpers.</action>
  <verify><automated>mix test --include integration test/integration/phase133_clean_room_saas_journey_test.exs --only negative</automated></verify>
  <done>Every E2E-05 failure has an independent, stable, black-box assertion and no failure output contains secret material or private implementation reasons.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Client credentials → lifecycle endpoints | Confidential authentication authorizes introspection/revocation state reads and writes. |
| Malicious OAuth/resource input → live endpoints | Replayed/drifted/malformed credentials attempt to cross protocol and resource gates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-133-16 | Elevation of privilege | refresh family | critical | mitigate | Real rotation, identical old-token replay, invalid_grant, and inactive introspection evidence. |
| T-133-17 | Tampering | code/state/nonce/redirect | critical | mitigate | Independent live negative cases and terminal transaction consumption before exchange. |
| T-133-18 | Elevation of privilege | audience/scope gates | high | mitigate | Assert documented 401/403 and challenges through canonical protected route. |
| T-133-19 | Repudiation | lifecycle claims | medium | mitigate | Name evidence as authorization-server truth and explicitly avoid an offline JWT invalidation claim. |
</threat_model>

<verification>
- `mix test --include integration test/integration/phase133_clean_room_saas_journey_test.exs --only lifecycle`
- `mix test --include integration test/integration/phase133_clean_room_saas_journey_test.exs --only negative`
</verification>

<success_criteria>
- Refresh-family and lifecycle endpoint behavior satisfies E2E-04 without a false JWT claim.
- Every E2E-05 failure is visible through actual HTTP and stable public semantics.
- Negative diagnostics remain redacted and independent across cases.
</success_criteria>

<output>Create `.planning/phases/133-clean-room-saas-journey/133-05-SUMMARY.md` when done.</output>
