---
phase: 137-ci-conformance-and-release-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .sobelow-conf
  - scripts/ci/check_sobelow_routers.sh
  - scripts/ci/check_dependency_truth.sh
  - scripts/ci/check_architecture_topology.sh
  - test/lockspire/ci_security_dependency_contract_test.exs
autonomous: true
requirements: [CI-01, CI-03]
must_haves:
  truths:
    - "One repository command fails when either shipped Lockspire router has a low-severity Sobelow finding."
    - "The same local CI policy fails on an unused locked dependency or a compile-connected dependency cycle."
    - "Sobelow exclusions remain narrow, named, and reviewable; no broad source tree is hidden."
  artifacts:
    - path: "scripts/ci/check_sobelow_routers.sh"
      provides: "Fail-closed scans of the public and admin router entry points"
    - path: "scripts/ci/check_dependency_truth.sh"
      provides: "Unused-lock and architecture topology policy"
    - path: "test/lockspire/ci_security_dependency_contract_test.exs"
      provides: "Executable policy characterization and bypass resistance"
  key_links:
    - from: "scripts/ci/check_sobelow_routers.sh"
      to: "lib/lockspire/web/router.ex and lib/lockspire/web/admin_router.ex"
      via: "two explicit Sobelow --router invocations with low threshold and non-zero exit"
    - from: "scripts/ci/check_dependency_truth.sh"
      to: "scripts/ci/check_architecture_topology.sh"
      via: "fail-closed dependency quality command"
---

<objective>
Create the first end-to-end release-quality tracer: one local policy path that explicitly scans both HTTP routers and rejects dependency/topology drift.

Purpose: Make CI-01 and the dependency/cycle half of CI-03 observable before workflow orchestration is changed.
Output: Narrow security and dependency scripts plus contracts that prevent quiet bypasses.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/137-ci-conformance-and-release-proof/137-ASSUMPTIONS.md
@.planning/phases/137-ci-conformance-and-release-proof/137-RESEARCH.md
@mix.exs
@.sobelow-conf
@scripts/ci/check_architecture_topology.sh
@test/lockspire/ci_static_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove both router scans through one fail-closed command</name>
  <files>.sobelow-conf, scripts/ci/check_sobelow_routers.sh, test/lockspire/ci_security_dependency_contract_test.exs</files>
  <behavior>
    - The command invokes Sobelow independently for `lib/lockspire/web/router.ex` and `lib/lockspire/web/admin_router.ex`.
    - Each invocation includes private checks, low severity threshold, and non-zero exit behavior.
    - A missing router, omitted required flag, command failure, or broad ignore fails the contract.
  </behavior>
  <action>Write the contract first, then add a strict Bash wrapper using the locked Sobelow dependency. Keep `.sobelow-conf` limited to named findings with adjacent reasons and reject directory-wide/file-wide ignores. Do not rely on router auto-discovery or the current `mix qa` Sobelow call, because this embedded library has two separate shipped router surfaces.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/ci_security_dependency_contract_test.exs &amp;&amp; bash scripts/ci/check_sobelow_routers.sh</automated>
  </verify>
  <done>Both concrete router files are scanned at low severity and every bypass characterized by the contract fails closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject unused locked dependencies and compile-connected cycles</name>
  <files>scripts/ci/check_dependency_truth.sh, scripts/ci/check_architecture_topology.sh, test/lockspire/ci_security_dependency_contract_test.exs</files>
  <behavior>
    - The dependency command uses the locally supported Mix unused-lock check and propagates failure.
    - The topology command asks Mix for compile-connected cycles and rejects any reported cycle.
    - Neither command edits a lockfile or silently downgrades an unsupported option.
  </behavior>
  <action>Confirm the exact command with `mix help deps.unlock` and `mix help xref`, then implement a side-effect-free dependency truth wrapper. Tighten the existing topology script to the compile-connected label. Extend the contract with fixture-safe command/flag assertions and failure propagation; preserve all four existing lockfiles and do not refresh dependency versions in this task.</action>
  <verify>
    <automated>bash scripts/ci/check_dependency_truth.sh &amp;&amp; MIX_ENV=test mix test test/lockspire/ci_security_dependency_contract_test.exs test/lockspire/architecture_fitness_test.exs</automated>
  </verify>
  <done>Unused locked dependencies and compile-connected cycles are independently actionable failures with no lockfile mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| CI policy -> Phoenix source | Static analysis must cover both shipped HTTP entry points. |
| Lockfile/source graph -> quality verdict | Repository metadata can conceal dead dependencies or cycles if commands are incomplete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-137-01 | Tampering | Sobelow invocation | high | mitigate | Assert both exact router paths and all fail-closed flags in script and contract. |
| T-137-02 | Repudiation | Sobelow exclusions | medium | mitigate | Reject broad ignores and require named, reasoned exclusions. |
| T-137-03 | Tampering | dependency lock gate | medium | mitigate | Use Mix's read-only unused-lock check and verify locks remain unchanged. |
| T-137-04 | Denial of service | architecture cycle gate | medium | mitigate | Restrict xref to compile-connected cycles and propagate tool failures distinctly. |
</threat_model>

<verification>
Run both scripts, the focused contract, and `mix qa.architecture`; inspect `git diff --exit-code -- mix.lock examples/adoption_demo/mix.lock compatibility/phoenix_1_8_live_view_1_1/mix.lock`.
</verification>

<success_criteria>
Both router surfaces, unused dependency truth, and compile-connected cycle truth are executable locally without workflow YAML.
</success_criteria>

<output>
Create `.planning/phases/137-ci-conformance-and-release-proof/137-01-SUMMARY.md` when done.
</output>
