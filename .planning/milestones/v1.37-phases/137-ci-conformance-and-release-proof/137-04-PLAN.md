---
phase: 137-ci-conformance-and-release-proof
plan: 04
type: execute
wave: 3
depends_on: [137-01, 137-02, 137-03]
files_modified:
  - .github/workflows/ci.yml
  - test/lockspire/ci_workflow_evidence_contract_test.exs
autonomous: true
requirements: [CI-01, CI-02, CI-03]
must_haves:
  truths:
    - "Required CI transports fast and integration coverdata from one revision into a non-executing 84 percent aggregate job."
    - "Required CI invokes explicit dual-router Sobelow and dependency/cycle gates."
    - "Adoption-demo updates remain controlled while the minimum-version fixture lock is immutable."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Least-privilege required-job orchestration for all CI evidence"
    - path: "test/lockspire/ci_workflow_evidence_contract_test.exs"
      provides: "Workflow topology, permissions, artifact, and lock-boundary proof"
  key_links:
    - from: ".github/workflows/ci.yml fast/integration jobs"
      to: ".github/workflows/ci.yml coverage aggregate job"
      via: "explicit SHA-bound artifact names and needs edges"
    - from: ".github/workflows/ci.yml fast job"
      to: "Plan 01 policy scripts"
      via: "required fail-closed steps"
---

<objective>
Wire the proven local quality policies into the required GitHub Actions topology.

Purpose: Complete CI-01..03 with immutable artifact transport and visible failure ownership.
Output: Updated CI workflow and a static contract that prevents orchestration drift.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-ci-conformance-and-release-proof/137-01-SUMMARY.md
@.planning/phases/137-ci-conformance-and-release-proof/137-02-SUMMARY.md
@.planning/phases/137-ci-conformance-and-release-proof/137-03-SUMMARY.md
@.github/workflows/ci.yml
@scripts/ci/lint_workflows.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry once-per-partition coverage into a required aggregate job</name>
  <files>.github/workflows/ci.yml, test/lockspire/ci_workflow_evidence_contract_test.exs</files>
  <behavior>
    - Fast and integration jobs upload uniquely named coverdata/receipt artifacts for `github.sha`.
    - Aggregate needs both jobs, downloads only those names, invokes the Plan 02 script, and never runs tests or executable artifact content.
    - Artifact absence or aggregate below 84 fails the required job.
  </behavior>
  <action>Fetch/recheck official GitHub workflow syntax and artifact guidance before editing. Add least-privilege upload/download steps pinned to existing action SHA conventions. Give coverage aggregation an explicit `needs: [fast, integration]`, clean checkout/dependency setup, immutable artifact names containing `github.sha`, and bounded retention. Test the YAML text/topology so a missing producer, wrong SHA, broad artifact path, or test rerun is rejected.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/ci_workflow_evidence_contract_test.exs &amp;&amp; bash scripts/ci/lint_workflows.sh</automated>
  </verify>
  <done>Required CI has one same-revision complete-suite coverage verdict and no partition is rerun in the aggregate job.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make security, dependency, cycle, and fixture boundaries required</name>
  <files>.github/workflows/ci.yml, test/lockspire/ci_workflow_evidence_contract_test.exs</files>
  <behavior>
    - CI invokes the two-router Sobelow and dependency truth scripts without continue-on-error.
    - Existing architecture and all lockfile checks remain required.
    - Adoption-demo dependency handling is explicit and cannot rewrite the minimum Phoenix/LiveView fixture lock.
  </behavior>
  <action>Replace the ambiguous `mix qa` Sobelow signal with the explicit Plan 01 script while retaining formatting, compile, Credo, architecture, docs, audit, and package gates. Add the unused-lock/cycle script in an authoritative job. Keep repository and demo lock drift checks; preserve the compatibility job's exact minimum-version lock as a separate immutable boundary. Do not add write permissions or automated dependency mutation to pull-request CI.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/ci_workflow_evidence_contract_test.exs test/lockspire/ci_static_contract_test.exs test/lockspire/workflow_supply_chain_contract_test.exs &amp;&amp; bash scripts/ci/lint_workflows.sh</automated>
  </verify>
  <done>All CI-01..03 evidence is required, least privilege is preserved, and dependency fixture ownership remains explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub event -> workflow | Fork/push metadata selects the revision and permissions. |
| Job artifact -> aggregate job | Cross-job data can be substituted or broadened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-137-13 | Tampering | artifact handoff | high | mitigate | SHA-bound names, exact download paths, receipt checksum validation, needs edges. |
| T-137-14 | Elevation of privilege | workflow permissions | high | mitigate | Retain top-level contents:read and no unneeded job permissions. |
| T-137-15 | Repudiation | skipped quality step | high | mitigate | Contract required step names/scripts and reject continue-on-error. |
| T-137-16 | Tampering | minimum-version fixture | medium | mitigate | Separate immutable lock check from controlled demo update ownership. |
</threat_model>

<verification>
Run focused workflow contracts and checksum-pinned actionlint/shellcheck; inspect action pins and permissions in the diff.
</verification>

<success_criteria>
The repository's required CI graph carries reproducible security, coverage, dependency, and cycle evidence for the exact revision.
</success_criteria>

<output>
Create `.planning/phases/137-ci-conformance-and-release-proof/137-04-SUMMARY.md` when done.
</output>
