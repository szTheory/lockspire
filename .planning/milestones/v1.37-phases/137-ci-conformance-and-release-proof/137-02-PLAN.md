---
phase: 137-ci-conformance-and-release-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci/run_test_matrix.sh
  - scripts/ci/aggregate_coverage.sh
  - test/lockspire/ci_coverage_aggregation_contract_test.exs
autonomous: true
requirements: [CI-02]
must_haves:
  truths:
    - "Fast and integration ExUnit partitions each run once and export distinct native coverdata."
    - "Aggregation accepts exactly those two exports from one source SHA and enforces 84 percent without executing tests."
    - "The existing local fast-suite threshold remains 73 percent and the clean-room Python journey is reported separately."
  artifacts:
    - path: "scripts/ci/run_test_matrix.sh"
      provides: "Once-per-partition native coverage export"
    - path: "scripts/ci/aggregate_coverage.sh"
      provides: "Strict coverdata inventory and >=84 aggregate gate"
    - path: "test/lockspire/ci_coverage_aggregation_contract_test.exs"
      provides: "Missing, extra, foreign-SHA, duplicate, and rerun rejection proof"
  key_links:
    - from: "scripts/ci/run_test_matrix.sh"
      to: "Mix.Tasks.Test.Coverage"
      via: "--cover --export-coverage with unique partition names"
    - from: "scripts/ci/aggregate_coverage.sh"
      to: "exported fast and integration coverdata"
      via: "exact manifest/SHA validation before native Mix aggregation"
---

<objective>
Build a truthful native coverage evidence path without changing the current test ownership model.

Purpose: Turn CI-02 into a reusable script contract before wiring artifact transport in GitHub Actions.
Output: Partition exports, strict aggregation, source receipts, and focused failure tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-ci-conformance-and-release-proof/137-RESEARCH.md
@mix.exs
@scripts/ci/run_test_matrix.sh
@test/lockspire/ci_test_matrix_contract_test.exs
@test/lockspire/coverage_baseline_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Export each existing ExUnit partition exactly once</name>
  <files>scripts/ci/run_test_matrix.sh, test/lockspire/ci_coverage_aggregation_contract_test.exs</files>
  <behavior>
    - `--fast` runs only the ordinary non-integration partition and exports `fast` coverdata.
    - `--integration` runs only tagged integration tests with coverage export, then runs the uninstrumented clean-room journey separately.
    - Each export emits a bounded receipt containing partition, source SHA, filename, and checksum; timings still persist on failure.
  </behavior>
  <action>Start with source/fixture contract failures, then extend the existing runner with explicit coverage output and source-SHA arguments. Use Mix's native `--export-coverage`; never create a percentage parser or rerun a partition in the aggregation job. Preserve the existing `mix.exs` fast-only threshold of 73 and label clean-room evidence separately because its child VMs do not contribute coverdata.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/ci_coverage_aggregation_contract_test.exs test/lockspire/ci_test_matrix_contract_test.exs test/lockspire/coverage_baseline_contract_test.exs</automated>
  </verify>
  <done>The runner has two disjoint native coverage exports and one separate clean-room proof, with no duplicate partition execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggregate only complete same-SHA native coverage evidence</name>
  <files>scripts/ci/aggregate_coverage.sh, test/lockspire/ci_coverage_aggregation_contract_test.exs</files>
  <behavior>
    - Missing, duplicate, extra, malformed, checksum-mismatched, or foreign-SHA inputs fail before aggregation.
    - The aggregator invokes Mix's native coverage report without invoking `mix test`.
    - Aggregate coverage below 84.00 fails; 84.00 or higher passes and emits a bounded report receipt.
  </behavior>
  <action>Implement an argument-validating Bash aggregator around Mix's exported-coverdata workflow. Require exactly `fast` and `integration` manifests and coverdata for the expected 40-hex source SHA, verify file hashes, copy only those data files into a fresh coverage directory, and invoke the locally verified Mix coverage threshold option at 84. Fail if Mix's installed interface cannot enforce the threshold. Treat downloaded artifacts only as data and never source or commands.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/ci_coverage_aggregation_contract_test.exs &amp;&amp; bash -n scripts/ci/aggregate_coverage.sh scripts/ci/run_test_matrix.sh</automated>
  </verify>
  <done>Aggregation proves exactly one fast plus one integration export from the same revision and enforces >=84 without executing a test partition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Test process -> coverdata artifact | Coverage evidence leaves the process that measured it. |
| Downloaded artifacts -> aggregate verdict | Untrusted artifact contents influence a required CI result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-137-05 | Tampering | coverage artifacts | high | mitigate | Validate exact names, checksums, and source SHA before native aggregation. |
| T-137-06 | Repudiation | partition ownership | high | mitigate | Record disjoint commands and reject missing or duplicate exports. |
| T-137-07 | Elevation of privilege | downloaded artifact | high | mitigate | Accept coverdata/JSON only and never execute artifact-carried files. |
| T-137-08 | Information disclosure | timing/coverage receipts | low | accept | Receipts contain bounded paths, hashes, percentages, and revision only. |
</threat_model>

<verification>
Focused contracts prove inventory failure modes; a local complete-suite run will be performed in Plan 03 after behavior coverage closes the measured 78.03% baseline.
</verification>

<success_criteria>
The complete Mix test partition has one precise denominator, one pair of immutable inputs, and one 84 percent gate.
</success_criteria>

<output>
Create `.planning/phases/137-ci-conformance-and-release-proof/137-02-SUMMARY.md` when done.
</output>
