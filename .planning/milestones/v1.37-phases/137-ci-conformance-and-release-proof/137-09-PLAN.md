---
phase: 137-ci-conformance-and-release-proof
plan: 09
type: execute
wave: 2
depends_on: [137-08]
files_modified:
  - scripts/publish/release_artifact.py
  - scripts/publish/publish_hex_idempotently.sh
  - scripts/publish/verify_install_truth.sh
  - test/lockspire/release_artifact_chain_contract_test.exs
autonomous: true
requirements: [REL-01, REL-02]
must_haves:
  truths:
    - "A schema-versioned redacted manifest binds release SHA, package name/version, tar checksum, and pinned runtime/tool versions."
    - "Publish rejects a different tar, manifest, source SHA, or Hex checksum on first publication and retries."
    - "Post-publish verification checks exact Hex checksum/docs and runs the exact-version clean-room HTTP journey."
  artifacts:
    - path: "scripts/publish/release_artifact.py"
      provides: "Create/validate manifest and verify Hex checksum"
    - path: "scripts/publish/publish_hex_idempotently.sh"
      provides: "Manifest-bound idempotent package publication"
    - path: "scripts/publish/verify_install_truth.sh"
      provides: "Exact public version/checksum/docs/HTTP proof"
  key_links:
    - from: "release manifest"
      to: "local tar and Hex release API"
      via: "same SHA-256 required before publish and after availability"
    - from: "verify_install_truth.sh"
      to: "Plan 08 exact-version source"
      via: "minimal clean-room happy HTTP journey"
---

<objective>
Create a single-byte release evidence contract around the clean-room source seam.

Purpose: Ensure the reviewed artifact, published package, retained receipt, and post-publish journey all identify the same bytes/version.
Output: Redacted manifest tool, manifest-bound publisher, and exact public verification.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-ci-conformance-and-release-proof/137-08-SUMMARY.md
@scripts/publish/publish_hex_idempotently.sh
@scripts/publish/verify_install_truth.sh
@test/lockspire/release/release_automation_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind a built tar to a redacted release manifest and publisher</name>
  <files>scripts/publish/release_artifact.py, scripts/publish/publish_hex_idempotently.sh, test/lockspire/release_artifact_chain_contract_test.exs</files>
  <behavior>
    - Manifest creation accepts exact detached 40-hex SHA and regular tar, derives exact name/version/checksum, and records Elixir/OTP/Mix/Hex/Phoenix/LiveView/PostgreSQL tooling identities without environment secrets.
    - Validation rejects schema drift, path escape, malformed versions/checksums, source mismatch, tar replacement, unknown fields, or secret-shaped keys/values.
    - Publisher requires a valid manifest and uses/rebuilds only bytes that compare equal to its recorded tar before contacting Hex.
  </behavior>
  <action>Build a standard-library Python CLI with `create`, `verify-local`, and `verify-hex` subcommands and deterministic JSON serialization. Allowlist fields and values; do not serialize environment, command output, URLs with credentials, tokens, or absolute temp paths. Update the publisher to accept explicit tar/manifest/SHA inputs, verify them immediately before publication, and use the supported Hex publish path confirmed by local help. If Hex rebuilds, byte-compare that output with the reviewed artifact before publish; never attest merely because a version exists.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/release_artifact_chain_contract_test.exs &amp;&amp; bash -n scripts/publish/publish_hex_idempotently.sh &amp;&amp; python3 scripts/publish/release_artifact.py --help</automated>
  </verify>
  <done>A different revision, tar, manifest, or rebuilt payload cannot cross the publish boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify exact public bytes and repeat the real HTTP journey</name>
  <files>scripts/publish/release_artifact.py, scripts/publish/verify_install_truth.sh, test/lockspire/release_artifact_chain_contract_test.exs</files>
  <behavior>
    - Verification polls the exact release endpoint, requires checksum equality with the manifest, and confirms versioned HexDocs.
    - It invokes Plan 08 with `--hex-version` for install, migration, verification, boot, and happy HTTP flow.
    - HTTP/API retries are bounded; metadata mismatch, timeout, journey failure, or unsafe receipt fails closed.
  </behavior>
  <action>Replace the current generated compile-only host path with manifest-bound exact release verification and the established clean-room journey. Query the release-specific Hex API endpoint, use bounded retry/backoff only for propagation, verify checksum before executing the public package, then run the happy-path HTTP proof and validate its redacted receipt. Keep infrastructure failures distinct from package/protocol failures.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/release_artifact_chain_contract_test.exs test/lockspire/release/release_automation_contract_test.exs &amp;&amp; bash -n scripts/publish/verify_install_truth.sh</automated>
  </verify>
  <done>Post-publish success means exact checksum/docs availability plus a real installed HTTP journey, not version-list presence and compile alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Detached checkout/tar -> protected publish | Reviewed source becomes a public package. |
| Hex API/public package -> verification runtime | External metadata and package bytes are consumed after publish. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-137-33 | Tampering | release tar/manifest | critical | mitigate | Verify SHA/version/checksum immediately before publish and reject byte drift. |
| T-137-34 | Spoofing | Hex release response | high | mitigate | HTTPS release-specific API plus exact package/version/checksum comparison. |
| T-137-35 | Information disclosure | retained manifest | high | mitigate | Strict allowlist schema and secret-shaped field/value rejection. |
| T-137-36 | Repudiation | first publish | high | mitigate | Always perform post-publish Hex checksum verification, including initial publish. |
| T-137-37 | Denial of service | registry propagation | low | accept | Bounded retries with classified timeout; protected publish remains non-canceling. |
</threat_model>

<verification>
Fixture-driven contract exercises local/Hex mismatch and redaction failures without publishing; shell syntax and existing release contracts remain green.
</verification>

<success_criteria>
The produced tar, retained manifest, Hex checksum, and post-publish SaaS journey share one exact package identity.
</success_criteria>

<output>
Create `.planning/phases/137-ci-conformance-and-release-proof/137-09-SUMMARY.md` when done.
</output>
