---
phase: 111-demo-url-contract-config-unification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/config/config.exs
  - examples/adoption_demo/docker-compose.yml
autonomous: true
requirements:
  - URL-01
  - URL-02
  - URL-05
user_setup: []
must_haves:
  truths:
    - "D-01/D-02: LOCKSPIRE_DEMO_BASE_URL is the only browser-visible URL input in adoption demo config."
    - "D-01: AdoptionDemoWeb.Endpoint url and config :lockspire issuer derive from the same normalized base URL."
    - "D-07/D-08: LOCKSPIRE_DEMO_BIND_IP controls only the listener socket, with 127.0.0.1 default and 0.0.0.0 Docker opt-in."
    - "D-09: No app-plus-Postgres Compose topology, conflict-control routing, startup banner, smoke wrapper, docs expansion, cleanup lane, or protocol behavior is added."
  artifacts:
    - path: "examples/adoption_demo/config/config.exs"
      provides: "normalized demo base URL, endpoint url, issuer, and bind IP config"
      contains: "LOCKSPIRE_DEMO_BASE_URL"
    - path: "examples/adoption_demo/docker-compose.yml"
      provides: "Docker bind-IP opt-in environment"
      contains: "LOCKSPIRE_DEMO_BIND_IP=0.0.0.0"
  key_links:
    - from: "LOCKSPIRE_DEMO_BASE_URL"
      to: "AdoptionDemoWeb.Endpoint url"
      via: "URI.parse/1-derived scheme/host/port"
      pattern: "LOCKSPIRE_DEMO_BASE_URL"
    - from: "LOCKSPIRE_DEMO_BASE_URL"
      to: "config :lockspire, issuer"
      via: "demo_base_url <> \"/lockspire\""
      pattern: "issuer:.*demo_base_url"
    - from: "LOCKSPIRE_DEMO_BIND_IP"
      to: "AdoptionDemoWeb.Endpoint http ip"
      via: "explicit allowlist mapping"
      pattern: "LOCKSPIRE_DEMO_BIND_IP"
---

<objective>
Make `LOCKSPIRE_DEMO_BASE_URL` the adoption demo's canonical browser-visible URL contract and add an explicit bind-interface option for Docker.

Purpose: This removes issuer and Phoenix endpoint drift before seed and smoke proof work consumes the contract.
Output: Updated adoption demo config and compose environment wiring.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/111-demo-url-contract-config-unification/111-CONTEXT.md
@.planning/phases/111-demo-url-contract-config-unification/111-RESEARCH.md
@.planning/phases/111-demo-url-contract-config-unification/111-VALIDATION.md
@examples/adoption_demo/config/config.exs
@examples/adoption_demo/docker-compose.yml
@lib/lockspire/config.ex
@lib/lockspire/protocol/discovery.ex

<interfaces>
Existing contracts the executor must preserve:
- `Lockspire.Config.issuer!/0` reads `config :lockspire, :issuer`, validates it with the configured `mount_path`, and returns the issuer string.
- `Lockspire.Config.device_verification_uri/0` derives `/verify` from `issuer!/0`; do not modify this protocol helper for Phase 111.
- `Lockspire.Protocol.Discovery.openid_configuration/0` builds discovery endpoint URLs from `Config.issuer!/0`; do not add discovery-specific URL rewrites.
- `examples/adoption_demo/config/config.exs` currently sets `AdoptionDemoWeb.Endpoint` `http: [ip:, port:]`, `url: [scheme:, host:, port:]`, and `config :lockspire, issuer:` in the same config file.
</interfaces>
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | - | Make `LOCKSPIRE_DEMO_BASE_URL` drive endpoint URL generation, Lockspire issuer, seeded redirects, smoke proof, and Docker bind behavior | 111-01, 111-02 | COVERED | Config contract is created here; consumers and proof are in Plan 111-02.
REQ | URL-01 | One canonical `LOCKSPIRE_DEMO_BASE_URL` for browser-visible origin | 111-01, 111-02 | COVERED | This plan removes `LOCKSPIRE_DEMO_HOST` from config.
REQ | URL-02 | Phoenix endpoint URL generation and Lockspire issuer derive from same base URL | 111-01 | COVERED | Endpoint `url:` and issuer both derive from normalized `demo_base_url`.
REQ | URL-03 | Seeded redirect, callback, and verification URLs align after setup | 111-02 | COVERED | Seed/UI plan consumes `config :adoption_demo, :demo_base_url`.
REQ | URL-04 | Smoke remains base-URL driven and fails clearly on drift | 111-02 | COVERED | Smoke plan keeps only `LOCKSPIRE_DEMO_BASE_URL`.
REQ | URL-05 | Docker binds to container-reachable interface without changing host-local loopback default | 111-01 | COVERED | `LOCKSPIRE_DEMO_BIND_IP` default remains `127.0.0.1`; compose sets `0.0.0.0`.
RESEARCH | R-01 | Parse base URL once in config, normalize trailing slash, derive endpoint and issuer | 111-01 | COVERED | Task 1 implements this directly.
RESEARCH | R-02 | Separate listener bind IP from browser-visible base URL | 111-01 | COVERED | Task 2 implements explicit bind env.
RESEARCH | R-03 | Seed/controller local URLs derive from base URL while external fixtures stay external | 111-02 | COVERED | Plan 111-02 owns seeds and developer UI.
RESEARCH | R-04 | Improve smoke drift assertions without new packages or wrapper | 111-02 | COVERED | Plan 111-02 owns Python smoke helper.
CONTEXT | D-01 | Parse base URL once in adoption demo config; issuer is exactly `{base_url}/lockspire` | 111-01 | COVERED | Task 1.
CONTEXT | D-02 | Base URL is external origin, not bind or DB config; normalize trailing slash drift | 111-01 | COVERED | Task 1 and Task 2.
CONTEXT | D-03 | Local seeded/browser URLs derive from base URL | 111-02 | COVERED | Plan 111-02.
CONTEXT | D-04 | Preserve external partner fixtures | 111-02 | COVERED | Plan 111-02 acceptance criteria.
CONTEXT | D-05 | Keep `scripts/demo/adoption_smoke.py` as drift fence | 111-02 | COVERED | Plan 111-02.
CONTEXT | D-06 | Improve smoke failure clarity; no new wrapper/startup path | 111-02 | COVERED | Plan 111-02.
CONTEXT | D-07 | Add explicit bind-interface env option | 111-01 | COVERED | Task 2.
CONTEXT | D-08 | Do not infer bind IP from base URL | 111-01 | COVERED | Task 2.
CONTEXT | D-09 | Keep Phase 111 narrow; later phase work excluded | 111-01, 111-02 | COVERED | Both plans include exclusion criteria.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive endpoint URL and issuer from the normalized demo base URL</name>
  <files>examples/adoption_demo/config/config.exs</files>
  <read_first>
    - `.planning/phases/111-demo-url-contract-config-unification/111-CONTEXT.md`
    - `.planning/phases/111-demo-url-contract-config-unification/111-RESEARCH.md`
    - `.planning/phases/111-demo-url-contract-config-unification/111-VALIDATION.md`
    - `examples/adoption_demo/config/config.exs`
    - `lib/lockspire/config.ex`
    - `lib/lockspire/protocol/discovery.ex`
  </read_first>
  <behavior>
    - With no env override, the normalized demo base URL is `http://127.0.0.1:4100`.
    - With `LOCKSPIRE_DEMO_BASE_URL=http://localhost:4200/`, endpoint `url:` uses scheme `http`, host `localhost`, port `4200`, and Lockspire issuer is `http://localhost:4200/lockspire`.
    - With a base URL containing a query, fragment, blank host, blank scheme, or path other than empty string or `/`, config raises `ArgumentError`.
  </behavior>
  <action>In `examples/adoption_demo/config/config.exs`, remove `LOCKSPIRE_DEMO_HOST` as a public URL input. Read `LOCKSPIRE_DEMO_BASE_URL` with default `http://127.0.0.1:4100`, trim whitespace, remove trailing `/`, parse with `URI.parse/1`, and raise `ArgumentError` when scheme or host is missing, query or fragment is present, or path is not `nil`, empty string, or `/`. Store the normalized string with `config :adoption_demo, :demo_base_url, demo_base_url`. Derive `AdoptionDemoWeb.Endpoint` `url:` from the parsed URI using `scheme: demo_uri.scheme`, `host: demo_uri.host`, and `port: demo_uri.port`. Derive `config :lockspire, :issuer` from `demo_base_url <> "/lockspire"` and keep `mount_path: "/lockspire"`. Leave repo/database config, `secret_key_base`, `known_scopes`, `account_resolver`, signing algorithm, and Oban settings unchanged. Do not modify `lib/lockspire/config.ex`, `lib/lockspire/protocol/discovery.ex`, OAuth flow code, redirect validation, PKCE behavior, token issuance, admin routes, or host-owned seams.</action>
  <verify>
    <automated>mix format --check-formatted examples/adoption_demo/config/config.exs</automated>
    <automated>cd examples/adoption_demo && mix compile --warnings-as-errors</automated>
    <automated>test "$(rg -n "LOCKSPIRE_DEMO_HOST|issuer: \"http://127\\.0\\.0\\.1:4100/lockspire\"" examples/adoption_demo/config/config.exs | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `examples/adoption_demo/config/config.exs` contains `LOCKSPIRE_DEMO_BASE_URL`.
    - Source assertion: `examples/adoption_demo/config/config.exs` contains `config :adoption_demo, :demo_base_url`.
    - Source assertion: `examples/adoption_demo/config/config.exs` does not contain `LOCKSPIRE_DEMO_HOST`.
    - Source assertion: `examples/adoption_demo/config/config.exs` does not contain `issuer: "http://127.0.0.1:4100/lockspire"`.
    - Behavior assertion: `cd examples/adoption_demo && mix compile --warnings-as-errors` exits 0 with default env.
  </acceptance_criteria>
  <done>The adoption demo exposes one normalized `LOCKSPIRE_DEMO_BASE_URL` config contract, and both Phoenix endpoint URL generation and Lockspire issuer derive from it per D-01 and D-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit bind IP config and Docker opt-in</name>
  <files>examples/adoption_demo/config/config.exs, examples/adoption_demo/docker-compose.yml</files>
  <read_first>
    - `.planning/phases/111-demo-url-contract-config-unification/111-CONTEXT.md`
    - `.planning/phases/111-demo-url-contract-config-unification/111-RESEARCH.md`
    - `.planning/phases/111-demo-url-contract-config-unification/111-VALIDATION.md`
    - `examples/adoption_demo/config/config.exs`
    - `examples/adoption_demo/docker-compose.yml`
  </read_first>
  <behavior>
    - With no `LOCKSPIRE_DEMO_BIND_IP`, the endpoint listener uses `{127, 0, 0, 1}`.
    - With `LOCKSPIRE_DEMO_BIND_IP=0.0.0.0`, the endpoint listener uses `{0, 0, 0, 0}`.
    - `LOCKSPIRE_DEMO_BASE_URL` never controls the listener bind tuple.
  </behavior>
  <action>In `examples/adoption_demo/config/config.exs`, add a bind-IP helper that reads `LOCKSPIRE_DEMO_BIND_IP` with default `127.0.0.1`. Map exactly `127.0.0.1` to `{127, 0, 0, 1}` and exactly `0.0.0.0` to `{0, 0, 0, 0}`; raise `ArgumentError` for any other value and include `LOCKSPIRE_DEMO_BIND_IP` in the error message. Use that helper for `AdoptionDemoWeb.Endpoint` `http: [ip:, port:]` while keeping listener `port:` from `System.get_env("PORT") || "4100"`. In `examples/adoption_demo/docker-compose.yml`, add the exact environment entry `LOCKSPIRE_DEMO_BIND_IP=0.0.0.0` to the existing `web` service. Keep `PHX_SERVER=true`, `PORT=4000`, existing Traefik labels, existing volumes, and existing networks unchanged. Do not add a PostgreSQL service, healthcheck, project-scoped volume names, configurable project name, public app port controls, startup banner, smoke wrapper, cleanup lane, or docs expansion in this phase.</action>
  <verify>
    <automated>mix format --check-formatted examples/adoption_demo/config/config.exs</automated>
    <automated>cd examples/adoption_demo && mix compile --warnings-as-errors</automated>
    <automated>cd examples/adoption_demo && LOCKSPIRE_DEMO_BIND_IP=0.0.0.0 mix compile --warnings-as-errors</automated>
    <automated>cd examples/adoption_demo && docker compose config >/tmp/lockspire-phase111-compose.txt && rg -n "LOCKSPIRE_DEMO_BIND_IP=0\\.0\\.0\\.0" /tmp/lockspire-phase111-compose.txt</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `examples/adoption_demo/config/config.exs` contains `LOCKSPIRE_DEMO_BIND_IP`.
    - Source assertion: `examples/adoption_demo/config/config.exs` contains both `127.0.0.1` and `0.0.0.0`.
    - Source assertion: `examples/adoption_demo/docker-compose.yml` contains `LOCKSPIRE_DEMO_BIND_IP=0.0.0.0`.
    - CLI assertion: default compile and `LOCKSPIRE_DEMO_BIND_IP=0.0.0.0` compile both exit 0.
    - Source assertion: `examples/adoption_demo/docker-compose.yml` does not add a `postgres` service or new named database volume.
  </acceptance_criteria>
  <done>Host-local runs keep safe loopback binding by default, and Docker can opt into container-reachable `0.0.0.0` without coupling bind behavior to `LOCKSPIRE_DEMO_BASE_URL` per D-07 and D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| environment -> Phoenix config | Env vars enter endpoint URL, issuer, listener bind, DB, and server config. |
| Phoenix config -> OIDC metadata | Lockspire issuer config is published through discovery and consumed by clients. |
| Docker compose -> container listener | Compose env changes network exposure of the demo Phoenix process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Spoofing | `config :lockspire, :issuer` | mitigate | Derive issuer exactly from normalized `LOCKSPIRE_DEMO_BASE_URL <> "/lockspire"` and leave discovery code unchanged. |
| T-111-02 | Tampering | `LOCKSPIRE_DEMO_BASE_URL` parsing | mitigate | Reject missing scheme/host, query, fragment, and non-root path so malicious or malformed URL inputs fail at boot. |
| T-111-03 | Information Disclosure | Endpoint listener bind | mitigate | Keep default bind `127.0.0.1`; require explicit `LOCKSPIRE_DEMO_BIND_IP=0.0.0.0` for Docker. |
| T-111-04 | Information Disclosure | Config and compose output | mitigate | Do not add startup banners or print secrets; do not alter secret-bearing config values. |
| T-111-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager installs are allowed in this plan; if an executor adds one, stop for slopcheck and human verification. |
</threat_model>

<verification>
Run after both tasks:
- `mix format --check-formatted examples/adoption_demo/config/config.exs`
- `cd examples/adoption_demo && mix compile --warnings-as-errors`
- `cd examples/adoption_demo && LOCKSPIRE_DEMO_BIND_IP=0.0.0.0 mix compile --warnings-as-errors`
- `cd examples/adoption_demo && docker compose config >/tmp/lockspire-phase111-compose.txt && rg -n "LOCKSPIRE_DEMO_BIND_IP=0\\.0\\.0\\.0" /tmp/lockspire-phase111-compose.txt`
- `test "$(rg -n "LOCKSPIRE_DEMO_HOST|issuer: \"http://127\\.0\\.0\\.1:4100/lockspire\"" examples/adoption_demo/config/config.exs | wc -l | tr -d ' ')" = "0"`
</verification>

<success_criteria>
- URL-01 is partially complete: adoption demo config has one canonical browser-visible base URL env.
- URL-02 is complete: endpoint URL generation and Lockspire issuer derive from the same normalized base URL.
- URL-05 is complete: Docker can opt into `0.0.0.0` bind while host-local default remains loopback.
- No Lockspire protocol module, admin workflow, public support claim, startup wrapper, cleanup lane, or default app-plus-Postgres Docker topology is changed.
</success_criteria>

<output>
Create `.planning/phases/111-demo-url-contract-config-unification/111-01-SUMMARY.md` when done.
</output>
