---
phase: 129-token-endpoint-cohesion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/token_lifetime.ex
  - lib/lockspire/protocol/token_exchange/authorization_code_grant.ex
  - lib/lockspire/protocol/token_exchange.ex
  - test/lockspire/protocol/token_exchange_test.exs
autonomous: true
requirements: [RUNTIME-03, ARCH-04]
must_haves:
  truths:
    - "Existing authorization-code callers receive byte-compatible Success and Error structs through TokenExchange."
    - "Authorization-code redemption remains single-use, atomic, audited, and observable."
    - "The shared lifetime policy reports the existing access, ID, and refresh defaults."
  artifacts:
    - path: "lib/lockspire/protocol/token_exchange/authorization_code_grant.ex"
      provides: "Authorization-code grant coordinator"
    - path: "lib/lockspire/protocol/token_lifetime.ex"
      provides: "Internal lifetime policy"
  key_links:
    - from: "lib/lockspire/protocol/token_exchange.ex"
      to: "lib/lockspire/protocol/token_exchange/authorization_code_grant.ex"
      via: "exchange_authorization_code/1 delegation"
    - from: "lib/lockspire/protocol/token_exchange/authorization_code_grant.ex"
      to: "Lockspire storage transaction and audit ports"
      via: "unchanged request option precedence"
---

<objective>
Extract one complete authorization-code redemption path behind the unchanged token-exchange facade and establish the shared lifetime contract.

Purpose: Prove the coordinator boundary end to end before moving the remaining grants.
Output: Production authorization-code coordinator, stable facade, and characterized policy values.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-token-endpoint-cohesion/129-RESEARCH.md
@.planning/phases/128-runtime-dependency-truth/128-02-SUMMARY.md
@lib/lockspire/protocol/token_exchange.ex
@test/lockspire/protocol/token_exchange_test.exs

<interfaces>
Preserve `TokenExchange.result()`, `exchange/1`, `exchange_authorization_code/1`, `%TokenExchange.Success{access_token, refresh_token, id_token, token_type, issued_token_type, expires_in, scope}`, and `%TokenExchange.Error{status, error, error_description, reason_code, dpop_nonce}` exactly. `TokenLifetime.access_token/0` and `id_token/0` return 3600; `refresh_token/0` returns 2_592_000.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Route one authorization-code exchange through its coordinator</name>
  <files>lib/lockspire/protocol/token_lifetime.ex, lib/lockspire/protocol/token_exchange/authorization_code_grant.ex, lib/lockspire/protocol/token_exchange.ex, test/lockspire/protocol/token_exchange_test.exs</files>
  <behavior>
    - A valid confidential-client code still issues the same token response, persists token hashes, consumes the code, appends its durable audit, and emits existing telemetry.
    - Replay, binding, PKCE, resource, DPoP, signing, and transaction failures retain status, OAuth error text, reason code, nonce, audit, and rollback behavior.
    - Lifetime functions return the existing three integer defaults.
  </behavior>
  <action>First add focused characterization assertions around the public facade. Create internal `TokenLifetime` and `TokenExchange.AuthorizationCodeGrant`; move authorization-code-only authentication, validation, issuance, persistence, audit, and telemetry coordination into it. Keep `TokenExchange` as the public owner of Success/Error structs and delegate `exchange_authorization_code/1` without changing request maps or store-option precedence. Move functions, do not duplicate them, and keep Phase 128 Repository/transaction/audit seams intact.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange_test.exs --seed 0</automated></verify>
  <done>The public authorization-code path traverses the coordinator with unchanged successful and failing behavior, and shared lifetime values are characterized.</done>
</task>

<task type="auto">
  <name>Task 2: Ratchet facade ownership and extraction boundaries</name>
  <files>lib/lockspire/protocol/token_exchange.ex, test/lockspire/protocol/token_exchange_test.exs</files>
  <action>Add source/behavior assertions that the facade continues to define the public structs/functions and delegates authorization-code coordination, while the coordinator remains internal and owns the moved grant logic. Remove now-dead aliases, attributes, and private helpers from the facade only after focused tests prove their behavior is represented in the coordinator.</action>
  <verify><automated>mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix test test/lockspire/protocol/token_exchange_test.exs</automated></verify>
  <done>The facade is smaller, public contracts are unchanged, and authorization-code implementation has one owner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Token request → grant coordinator | Untrusted grant, client credentials, PKCE, resource, and DPoP inputs cross into redemption. |
| Coordinator → persistence | Single-use state, tokens, and audit events cross an atomicity boundary. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-129-01 | Tampering | Authorization-code extraction | high | mitigate | Characterize binding, replay, transaction rollback, durable audit, and exact error structs before moving code. |
| T-129-02 | Repudiation | Audit/telemetry movement | medium | mitigate | Assert existing durable audit and telemetry metadata on success and replay. |
| T-129-03 | Information disclosure | Error handling | medium | mitigate | Preserve generic OAuth errors and ensure no token/key material enters metadata. |
</threat_model>

<verification>
Run the focused exchange suite, compile with warnings as errors, and inspect that only facade APIs—not coordinator APIs—are documented publicly.
</verification>

<success_criteria>
Authorization-code redemption is end-to-end behind `AuthorizationCodeGrant`; public/wire/storage/audit/telemetry behavior and lifetime values remain unchanged.
</success_criteria>

<output>Create `.planning/phases/129-token-endpoint-cohesion/129-01-SUMMARY.md` when done.</output>

