---
phase: 129-token-endpoint-cohesion
plan: 03
type: execute
wave: 3
depends_on: [129-02]
files_modified:
  - lib/lockspire/protocol/private_jwk.ex
  - lib/lockspire/protocol/access_token_signer.ex
  - lib/lockspire/protocol/id_token.ex
  - lib/lockspire/protocol/jarm.ex
  - test/lockspire/protocol/private_jwk_test.exs
  - test/lockspire/protocol/access_token_signer_test.exs
  - test/lockspire/protocol/id_token_test.exs
  - test/lockspire/protocol/jarm_test.exs
autonomous: true
requirements: [RUNTIME-04]
must_haves:
  truths:
    - "Valid JSON and safe-term private JWK maps remain usable."
    - "Malformed, non-map, unsafe, or non-binary material fails closed as invalid_signing_key."
    - "No failure path logs private key material."
  artifacts:
    - path: "lib/lockspire/protocol/private_jwk.ex"
      provides: "Single fail-closed decoder"
  key_links:
    - from: "AccessTokenSigner, IdToken, and Jarm"
      to: "PrivateJwk.decode/1"
      via: "shared decode before JOSE construction"
---

<objective>
Introduce the one fail-closed private-key decoder and route three high-volume signing paths through it.

Purpose: Eliminate divergent cryptographic key parsing without altering algorithm or signing behavior.
Output: Characterized decoder used by access, ID, and JARM signing.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-token-endpoint-cohesion/129-RESEARCH.md
@lib/lockspire/protocol/access_token_signer.ex
@lib/lockspire/protocol/id_token.ex
@lib/lockspire/protocol/jarm.ex

<interfaces>`Lockspire.Protocol.PrivateJwk.decode(binary()) :: {:ok, map()} | {:error, :invalid_signing_key}`. It accepts JSON object maps first, then safe non-executable Erlang-term maps; every other input or decode exception returns the error tuple.</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement the fail-closed decoder</name>
  <files>lib/lockspire/protocol/private_jwk.ex, test/lockspire/protocol/private_jwk_test.exs</files>
  <behavior>
    - JSON object and safely encoded term maps return `{:ok, map}`.
    - JSON scalars/lists, ordinary text, truncated terms, unsafe terms, atoms, maps, and nil return `{:error, :invalid_signing_key}`.
    - Exceptions, throws, and exits from malformed term parsing do not escape.
  </behavior>
  <action>Write red tests, then implement the internal decoder with Jason object decoding and `Plug.Crypto.non_executable_binary_to_term/2` safe mode. Normalize all parse failures to the existing atom, catch all decoder failure classes at this boundary, and never inspect or log key contents.</action>
  <verify><automated>mix test test/lockspire/protocol/private_jwk_test.exs</automated></verify>
  <done>The decoder has one total, fail-closed contract over arbitrary inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate access and ID-token signing</name>
  <files>lib/lockspire/protocol/access_token_signer.ex, lib/lockspire/protocol/id_token.ex, test/lockspire/protocol/access_token_signer_test.exs, test/lockspire/protocol/id_token_test.exs</files>
  <behavior>
    - Valid keys produce the same compact JWT headers and claims.
    - Malformed keys retain each module's existing outward error shape and redacted logs.
  </behavior>
  <action>Replace local decode helpers with `PrivateJwk.decode/1`; keep JOSE signing, allowed-algorithm checks, claims, and each module's public error mapping unchanged. Add safe-term compatibility and malformed-input parity cases before deleting duplicated helpers.</action>
  <verify><automated>mix test test/lockspire/protocol/access_token_signer_test.exs test/lockspire/protocol/id_token_test.exs</automated></verify>
  <done>Access and ID token paths share one decoder without wire or logging drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate JARM signing</name>
  <files>lib/lockspire/protocol/jarm.ex, test/lockspire/protocol/jarm_test.exs</files>
  <behavior>
    - Signed and nested-encrypted JARM output remains valid for allowed algorithms.
    - Invalid key material returns the same signing error without attempting encryption.
  </behavior>
  <action>Delegate JARM signing-key parsing to `PrivateJwk`; preserve key-store selection, FAPI algorithm rules, response claims, and nested encryption order. Remove only the local decode implementation and strengthen invalid/safe-term cases.</action>
  <verify><automated>mix test test/lockspire/protocol/jarm_test.exs</automated></verify>
  <done>JARM uses the shared decoder and retains its signing/encryption contracts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Encrypted key bytes → JOSE | Stored untrusted/corrupt bytes become cryptographic key objects. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-129-06 | Elevation of privilege | Private JWK parsing | high | mitigate | Accept only object maps from JSON or safe non-executable terms; normalize all failures. |
| T-129-07 | Information disclosure | Decode errors | high | mitigate | Test redacted logs and return only stable error atoms/structs. |
| T-129-08 | Denial of service | Malformed term | medium | mitigate | Catch decoder exceptions, throws, and exits at the shared boundary. |
</threat_model>

<verification>All four focused suites pass and `mix compile --warnings-as-errors` succeeds.</verification>
<success_criteria>The decoder is total and three signing paths use it with no behavioral drift.</success_criteria>
<output>Create `.planning/phases/129-token-endpoint-cohesion/129-03-SUMMARY.md` when done.</output>

