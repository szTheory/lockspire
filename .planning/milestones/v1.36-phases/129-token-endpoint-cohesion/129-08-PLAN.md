---
phase: 129-token-endpoint-cohesion
plan: 08
type: execute
wave: 6
depends_on: [129-07]
files_modified:
  - scripts/ci/run_dialyzer.sh
  - .github/workflows/ci.yml
  - test/lockspire/ci_static_contract_test.exs
autonomous: true
requirements: [STATIC-02]
must_haves:
  truths:
    - "CI contains a required, bounded Dialyzer job."
    - "The job restores keyed PLT/build caches and fails on any warning."
    - "Repository contracts reject ignore files and non-required/suppressed analysis."
  artifacts:
    - path: "scripts/ci/run_dialyzer.sh"
      provides: "One local/CI zero-warning entrypoint"
    - path: ".github/workflows/ci.yml"
      provides: "Required cached Dialyzer job"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/run_dialyzer.sh"
      via: "analysis job step"
---

<objective>
Make the zero-warning baseline a cached, required CI gate with executable anti-suppression contracts.

Purpose: Prevent the repository from returning to an ignored or optional static-analysis state.
Output: Reusable analysis script, CI job, and workflow contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/129-token-endpoint-cohesion/129-07-SUMMARY.md
@mix.exs
@.github/workflows/ci.yml
@test/lockspire/ci_static_contract_test.exs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add the deterministic Dialyzer entrypoint and policy contract</name>
  <files>scripts/ci/run_dialyzer.sh, test/lockspire/ci_static_contract_test.exs</files>
  <behavior>
    - Script exits nonzero on compile or Dialyzer warnings and does not filter output.
    - Contract fails if a blanket ignore file/config, skipped analysis, or warning-discarding shell construct is introduced.
  </behavior>
  <action>Create a strict shell entrypoint that checks locked dependencies, compiles with warnings denied, and runs the existing `mix qa.dialyzer` alias. Extend CI contract tests with parsed/source assertions for the entrypoint and absence of blanket suppression; keep checks specific to configured ignore mechanisms rather than comments.</action>
  <verify><automated>mix test test/lockspire/ci_static_contract_test.exs &amp;&amp; bash scripts/ci/run_dialyzer.sh</automated></verify>
  <done>One command enforces zero warnings locally and CI contracts forbid masking them.</done>
</task>
<task type="auto">
  <name>Task 2: Add a cached required CI analysis job</name>
  <files>.github/workflows/ci.yml, test/lockspire/ci_static_contract_test.exs</files>
  <action>Add a named top-level CI job with repository checkout, the primary pinned BEAM setup action, dependency install, a bounded timeout, and `actions/cache` for `priv/plts`, `deps`, and `_build`. Key the cache with runner OS, OTP, Elixir, and `mix.lock`; invoke only the repository script. Do not mark the job continue-on-error or hide it behind path/branch conditions. Extend workflow contracts to assert these properties so branch protection can require the stable job name.</action>
  <verify><automated>mix test test/lockspire/ci_static_contract_test.exs &amp;&amp; bash scripts/ci/lint_workflows.sh &amp;&amp; git diff --exit-code -- mix.lock</automated></verify>
  <done>The CI workflow exposes a stable, cache-backed, required-by-default Dialyzer check.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Repository changes → CI quality gate | Contributors can alter workflow/script configuration that decides pass/fail. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-129-20 | Tampering | CI analysis job | high | mitigate | Pin actions, bound timeout, call strict repo script, and test workflow properties. |
| T-129-21 | Repudiation | Warning output | medium | mitigate | Preserve raw Dialyzer exit/output and reject continue-on-error/filtering. |
| T-129-22 | Denial of service | PLT construction | low | accept | Bounded timeout plus exact-version keyed cache limits repeated build cost. |
</threat_model>
<verification>Local entrypoint, CI contracts, workflow lint, and lock immutability all pass.</verification>
<success_criteria>Dialyzer is zero-warning, cached, bounded, unsuppressed, and present as a normal required CI job.</success_criteria>
<output>Create `.planning/phases/129-token-endpoint-cohesion/129-08-SUMMARY.md` when done.</output>
