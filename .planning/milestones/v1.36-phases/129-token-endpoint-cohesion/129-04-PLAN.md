---
phase: 129-token-endpoint-cohesion
plan: 04
type: execute
wave: 4
depends_on: [129-03]
files_modified:
  - lib/lockspire/protocol/introspection_jwt.ex
  - lib/lockspire/protocol/logout_token.ex
  - lib/lockspire/protocol/jar.ex
  - test/lockspire/protocol/introspection_jwt_test.exs
  - test/lockspire/protocol/logout_token_test.exs
  - test/lockspire/protocol/jar_test.exs
autonomous: true
requirements: [RUNTIME-04]
must_haves:
  truths:
    - "Every supported private-key signing/decryption path uses PrivateJwk."
    - "Introspection JWT, logout token, and JAR outputs/errors remain compatible."
  artifacts:
    - path: "lib/lockspire/protocol/private_jwk.ex"
      provides: "Only private JWK byte decoder in protocol runtime"
  key_links:
    - from: "IntrospectionJwt, LogoutToken, Jar"
      to: "PrivateJwk.decode/1"
      via: "decode before JOSE sign/decrypt"
---

<objective>
Complete shared decoder adoption across introspection, logout, and JAR paths.

Purpose: Close alternate key-parsing paths so fail-closed behavior cannot drift.
Output: Six-of-six protocol consumers on the common decoder and a source fence against reintroduction.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-token-endpoint-cohesion/129-03-SUMMARY.md
@lib/lockspire/protocol/private_jwk.ex
@lib/lockspire/protocol/introspection_jwt.ex
@lib/lockspire/protocol/logout_token.ex
@lib/lockspire/protocol/jar.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate signed introspection</name>
  <files>lib/lockspire/protocol/introspection_jwt.ex, test/lockspire/protocol/introspection_jwt_test.exs</files>
  <behavior>- Valid JSON/safe-term keys sign the same RFC 9701 payload; malformed keys and unsupported algorithms retain their distinct errors.</behavior>
  <action>Use `PrivateJwk.decode/1`, preserve key-store/profile/algorithm selection and compact JWT headers, and test malformed plus safe-term parity before removing local parsing.</action>
  <verify><automated>mix test test/lockspire/protocol/introspection_jwt_test.exs</automated></verify>
  <done>Signed introspection shares the decoder without collapsing algorithm errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate back-channel logout signing</name>
  <files>lib/lockspire/protocol/logout_token.ex, test/lockspire/protocol/logout_token_test.exs</files>
  <behavior>- Valid logout tokens retain claims/JTI; invalid key, algorithm, and logout-event errors remain distinguishable.</behavior>
  <action>Replace local parsing with the shared decoder and retain event, subject/sid, profile, and algorithm validation order. Add compatibility assertions and ensure private material never appears in errors.</action>
  <verify><automated>mix test test/lockspire/protocol/logout_token_test.exs</automated></verify>
  <done>Logout signing shares the decoder with unchanged claims and errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate JAR decryption and fence decoder ownership</name>
  <files>lib/lockspire/protocol/jar.ex, test/lockspire/protocol/jar_test.exs</files>
  <behavior>- Five-part JWE request objects decrypt with valid JSON/safe-term keys; malformed keys continue to produce decryption_failed; three-part JWS input passes through.</behavior>
  <action>Use `PrivateJwk.decode/1` in JAR decryption while preserving the intentionally generic outward decryption error. Add a source contract enumerating the six approved consumers and asserting that protocol runtime defines private-key byte parsing only in `PrivateJwk`; use AST/source inspection rather than a brittle unscoped comment grep.</action>
  <verify><automated>mix test test/lockspire/protocol/jar_test.exs &amp;&amp; mix compile --warnings-as-errors</automated></verify>
  <done>All supported paths use the shared decoder and executable proof prevents duplicate parsers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Stored key bytes → signing/JWE decryption | Corrupt key material reaches protocol cryptography. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-129-09 | Tampering | Alternate key consumers | high | mitigate | Migrate all three and add executable ownership fence. |
| T-129-10 | Information disclosure | JAR/logout errors | high | mitigate | Preserve generic outward errors and test absence of key material. |
</threat_model>

<verification>All decoder consumer suites pass; source fence identifies exactly one parser implementation.</verification>
<success_criteria>AccessTokenSigner, IdToken, Jarm, IntrospectionJwt, LogoutToken, and Jar all use `PrivateJwk`.</success_criteria>
<output>Create `.planning/phases/129-token-endpoint-cohesion/129-04-SUMMARY.md` when done.</output>

