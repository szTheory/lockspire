---
phase: 129-token-endpoint-cohesion
plan: 05
type: execute
wave: 2
depends_on: [129-01]
files_modified:
  - lib/lockspire/protocol/token_exchange/device_code_grant.ex
  - lib/lockspire/protocol/token_exchange.ex
  - test/lockspire/protocol/token_exchange_test.exs
autonomous: true
requirements: [ARCH-04]
must_haves:
  truths:
    - "Device-code polling and redemption retain every current OAuth outcome."
    - "Successful device grants remain single-use, atomic, audited, and observable through TokenExchange."
  artifacts:
    - path: "lib/lockspire/protocol/token_exchange/device_code_grant.ex"
      provides: "Device grant coordinator"
  key_links:
    - from: "TokenExchange.exchange/1"
      to: "DeviceCodeGrant.exchange/1"
      via: "device grant dispatch"
---

<objective>
Move the complete device-code grant behind a dedicated coordinator while preserving the facade and protocol behavior.

Purpose: Give device polling, validation, issuance, and redemption one readable owner.
Output: `DeviceCodeGrant` and a thinner stable facade.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/129-token-endpoint-cohesion/129-01-SUMMARY.md
@.planning/phases/128-runtime-dependency-truth/128-02-SUMMARY.md
@lib/lockspire/protocol/token_exchange.ex
@test/lockspire/protocol/token_exchange_test.exs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extract device polling and outcome mapping</name>
  <files>lib/lockspire/protocol/token_exchange/device_code_grant.ex, lib/lockspire/protocol/token_exchange.ex, test/lockspire/protocol/token_exchange_test.exs</files>
  <behavior>
    - Missing, pending, slow_down, denied, expired, mismatched, consumed, and ready outcomes retain exact status/error/reason behavior.
    - Public clients and confidential clients retain authentication and DPoP behavior.
  </behavior>
  <action>Move device request parsing, poll recording, outcome mapping, client/DPoP coordination, and failure telemetry/audit into `DeviceCodeGrant`. Delegate from the unchanged facade. Preserve storage option precedence and exact mapping of each durable poll result.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange_test.exs --seed 0</automated></verify>
  <done>All device polling states traverse the coordinator with unchanged public errors.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Extract atomic device issuance and replay handling</name>
  <files>lib/lockspire/protocol/token_exchange/device_code_grant.ex, lib/lockspire/protocol/token_exchange.ex, test/lockspire/protocol/token_exchange_test.exs</files>
  <behavior>
    - Ready grants issue the same access/optional refresh/ID tokens and consume once.
    - Persistence failure rolls back; replay appends the same durable audit and emits the same telemetry.
  </behavior>
  <action>Move grant construction, token issuance, transactional persistence, audit event construction, replay handling, and success telemetry. Reuse shared facade structs and lifetime policy; remove moved helpers and aliases from `TokenExchange` after characterization passes.</action>
  <verify><automated>mix format --check-formatted &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix test test/lockspire/protocol/token_exchange_test.exs</automated></verify>
  <done>Device coordination has one owner and the facade retains only dispatch/public contracts.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Device code → poll state | Untrusted bearer code selects durable authorization state. |
| Ready grant → transaction | Token issuance and single-use consumption must commit atomically. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-129-11 | Spoofing | Device client binding | high | mitigate | Characterize public/confidential auth, client mismatch, and DPoP paths. |
| T-129-12 | Tampering | Device replay | high | mitigate | Preserve atomic consume and replay audit tests. |
| T-129-13 | Denial of service | Poll throttling | medium | mitigate | Preserve slow_down and next-poll state transitions exactly. |
</threat_model>
<verification>Focused token-exchange suite passes with compile warnings denied.</verification>
<success_criteria>`DeviceCodeGrant` owns device flow coordination with no public/wire/transaction/audit/telemetry drift.</success_criteria>
<output>Create `.planning/phases/129-token-endpoint-cohesion/129-05-SUMMARY.md` when done.</output>

