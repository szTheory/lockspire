---
phase: 127-executable-quality-baselines
plan: 03
type: execute
wave: 2
depends_on: [127-01, 127-02]
files_modified:
  - mix.exs
  - .github/workflows/ci.yml
  - test/lockspire/coverage_baseline_contract_test.exs
autonomous: true
requirements: [COVER-01]

must_haves:
  truths:
    - "The required Fast Checks job fails when built-in ExUnit coverage falls below 73%."
    - "The 73% floor is the rounded-down value of the freshly measured 73.11% repository baseline."
    - "Coverage runs the existing non-integration fast suite instead of adding a duplicate test pass."
    - "No production module is excluded from the coverage denominator and no external coverage dependency is added."
  artifacts:
    - path: "mix.exs"
      provides: "Built-in coverage threshold and test.coverage alias"
      contains: "threshold: 73"
    - path: ".github/workflows/ci.yml"
      provides: "Required coverage execution in Fast Checks"
      contains: "mix test.coverage"
    - path: "test/lockspire/coverage_baseline_contract_test.exs"
      provides: "Executable drift fence for threshold, suite selection, and dependency/exclusion boundaries"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "mix.exs"
      via: "Fast Checks invokes the test.coverage alias"
      pattern: "mix test.coverage"
    - from: "mix.exs"
      to: "test/test_helper.exs"
      via: "test.coverage runs ExUnit with the existing default integration exclusion"
      pattern: "test --cover"
---

<objective>
Convert the measured repository coverage baseline into a required, built-in CI ratchet.

Purpose: Make test coverage regressions visible without adding a service, third-party package, production exclusion list, or duplicate fast-suite execution.
Output: Mix coverage configuration, a coverage alias replacing the existing Fast Checks test invocation, and a focused contract test.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-executable-quality-baselines/127-RESEARCH.md
@.planning/phases/127-executable-quality-baselines/127-01-SUMMARY.md
@.planning/phases/127-executable-quality-baselines/127-02-SUMMARY.md
@mix.exs
@.github/workflows/ci.yml
@test/test_helper.exs

<interfaces>
Mix 1.19 accepts `test_coverage: [summary: [threshold: number]]` in `project/0`; its built-in cover tool exits non-zero when total coverage is below that threshold. `test_helper.exs` excludes `integration: true` unless integration or an explicit test target is requested. The fresh repository measurement is 73.11%, so the approved rounded-down floor is 73%.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Carry the measured built-in coverage floor through the existing Fast Checks suite</name>
  <files>mix.exs, .github/workflows/ci.yml, test/lockspire/coverage_baseline_contract_test.exs</files>
  <action>Add Mix's built-in `test_coverage` summary with threshold 73 and no module exclusions or alternate tool. Add a `test.coverage` alias that performs the same test database setup as `test.fast` and runs the same default non-integration suite with `--cover`; add its preferred test environment. In the Fast Checks job, replace the existing `mix test.fast` invocation with `mix test.coverage` so coverage instruments the already-required test pass rather than creating another pass. Add an async contract test that loads Mix project configuration and CI source to prove the exact floor, built-in tool, absence of coverage exclusions/external coverage packages, setup-before-test ordering, required CI invocation, and absence of a second fast-suite invocation in that job. Record the 73.11% measured baseline and rounding rationale in test diagnostics or adjacent durable maintainer-facing configuration commentary without turning the threshold into a dynamic value.</action>
  <verify>
    <automated>mix test test/lockspire/coverage_baseline_contract_test.exs &amp;&amp; MIX_ENV=test mix test.coverage</automated>
  </verify>
  <done>The focused contract passes and the full built-in coverage command exits successfully at or above the committed 73% floor; dropping below it makes the same required Fast Checks step fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test execution → coverage result | The measured denominator and threshold must represent the normal required non-integration suite. |
| Mix coverage result → CI status | A below-floor result must propagate as a failed required job. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-07 | Tampering | `test_coverage` denominator/floor | high | mitigate | Contract-test the 73% threshold, built-in tool, and absence of production exclusions or an external coverage package. |
| T-127-08 | Repudiation | Fast Checks suite selection | medium | mitigate | Replace the existing fast test invocation with one coverage alias and prove database setup plus default suite ordering. |
| T-127-09 | Denial of service | coverage instrumentation overhead | low | accept | The measured local run completes within the existing 30-minute job bound and does not add a second suite execution. |
</threat_model>

<verification>
- `mix test test/lockspire/coverage_baseline_contract_test.exs`
- `MIX_ENV=test mix test.coverage`
- `bash scripts/ci/lint_workflows.sh`
</verification>

<success_criteria>
- Built-in Mix coverage is configured at the measured rounded-down 73% baseline.
- Fast Checks runs the ordinary non-integration suite once, under coverage, after test database setup.
- A below-floor result fails CI; no production exclusions, external coverage dependency, service, or public/runtime behavior is added.
</success_criteria>

<output>
Create `.planning/phases/127-executable-quality-baselines/127-03-SUMMARY.md` when done.
</output>
