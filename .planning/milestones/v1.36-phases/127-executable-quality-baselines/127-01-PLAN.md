---
phase: 127-executable-quality-baselines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .credo.exs
  - scripts/ci/run_credo.sh
  - mix.exs
  - test/lockspire/static_analysis_baseline_contract_test.exs
autonomous: true
requirements: [STATIC-01]

must_haves:
  truths:
    - "Every intended lib/ and test/ source file receives enough time for Credo parsing."
    - "A timed-out Credo source makes the local qa command and required Fast Checks job fail."
    - "Credo findings and Credo process failures retain their normal non-zero status."
  artifacts:
    - path: ".credo.exs"
      provides: "Thirty-second parse budget for the full lib/test source set"
      contains: "parse_timeout: 30_000"
    - path: "scripts/ci/run_credo.sh"
      provides: "Fail-closed Credo execution wrapper"
    - path: "test/lockspire/static_analysis_baseline_contract_test.exs"
      provides: "Executable source/configuration contract for the Credo gate"
  key_links:
    - from: "mix.exs"
      to: "scripts/ci/run_credo.sh"
      via: "the qa alias invokes the repo-owned wrapper"
      pattern: "cmd bash scripts/ci/run_credo.sh"
    - from: ".github/workflows/ci.yml"
      to: "mix.exs"
      via: "Fast Checks continues to run mix qa"
      pattern: "mix qa"
---

<objective>
Make Credo's full-source analysis a fail-closed executable gate.

Purpose: Prevent large security- and protocol-sensitive modules from disappearing silently from static analysis on slower runners.
Output: A bounded Credo parse budget, a warning-aware wrapper, and a contract test wired through the existing local and CI qa path.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-executable-quality-baselines/127-RESEARCH.md
@.credo.exs
@mix.exs
@.github/workflows/ci.yml

<interfaces>
Credo 1.7 reads `parse_timeout` from each config entry; its default is 5,000 ms. Timed-out sources are reported with the stable warning text `Some source files were not parsed in the time allotted` while the command may otherwise exit successfully. The existing `qa` Mix alias runs format, compile, Credo, and Sobelow, and the `Fast Checks` CI job invokes `mix qa`.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Carry one fail-closed Credo run from mix qa through all intended sources</name>
  <files>.credo.exs, scripts/ci/run_credo.sh, mix.exs, test/lockspire/static_analysis_baseline_contract_test.exs</files>
  <action>Set `parse_timeout: 30_000` on the existing strict Credo config without narrowing the `lib/` or `test/` include set. Add a Bash wrapper that uses a private `mktemp` output file with cleanup, runs `mix credo --strict` while preserving visible output and the Credo process status, and returns failure when Credo emits its timed-out-source warning even if Credo returned zero. Replace the direct Credo entry in the `qa` alias with `cmd bash scripts/ci/run_credo.sh`, leaving the other qa checks and the public/runtime dependency graph unchanged. Add an async contract test that reads these files, proves the 30-second timeout and full include set, proves `qa` reaches the wrapper, and proves the wrapper checks both the underlying status and the timeout warning.</action>
  <verify>
    <automated>mix test test/lockspire/static_analysis_baseline_contract_test.exs &amp;&amp; bash scripts/ci/run_credo.sh</automated>
  </verify>
  <done>The focused contract passes, a real strict Credo run analyzes the complete configured source set without timeout warnings, and `mix qa` owns the same fail-closed path used by CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source → static-analysis report | Security-sensitive Elixir files must not be omitted because a parser task timed out. |
| Credo subprocess → qa/CI exit status | Tool warnings and failures must propagate as a blocking quality result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-01 | Tampering | `.credo.exs` source selection | high | mitigate | Contract-test the unchanged full `lib/` and `test/` include set plus the 30-second parse budget. |
| T-127-02 | Repudiation | `scripts/ci/run_credo.sh` status handling | medium | mitigate | Preserve Credo output and exit status, and explicitly turn the timed-out-source warning into failure. |
| T-127-03 | Denial of service | Credo parse budget | low | accept | A bounded 30-second per-source budget is intentionally finite; a source exceeding it blocks rather than silently disappearing. |
</threat_model>

<verification>
- `mix test test/lockspire/static_analysis_baseline_contract_test.exs`
- `bash scripts/ci/run_credo.sh`
- `mix qa`
</verification>

<success_criteria>
- Credo includes all intended `lib/` and `test/` files and has a 30,000 ms parse timeout.
- Timeout warnings and native Credo failures both make the wrapper non-zero.
- The existing `mix qa` → Fast Checks path executes the wrapper with no new runtime dependency or public behavior.
</success_criteria>

<output>
Create `.planning/phases/127-executable-quality-baselines/127-01-SUMMARY.md` when done.
</output>
