---
phase: 127-executable-quality-baselines
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - test/fixtures/phoenix_1_8_live_view_1_1/mix.exs
  - test/fixtures/phoenix_1_8_live_view_1_1/mix.lock
  - test/fixtures/phoenix_1_8_live_view_1_1/lib/lockspire_compatibility_fixture.ex
  - test/lockspire/compatibility_baseline_contract_test.exs
autonomous: true
requirements: [COMPAT-01, COMPAT-02]

must_haves:
  truths:
    - "The minimum Elixir/OTP lane runs Lockspire's fast suite against PostgreSQL 14."
    - "A committed fixture resolves exactly Phoenix 1.8.5 and LiveView 1.1.28 with Lockspire as a path dependency."
    - "The minimum-version CI lane compiles the fixture, so lower-bound dependency drift is required-check visible."
    - "All PostgreSQL service and external action references remain immutable, and dependency setup cannot rewrite committed locks."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Minimum BEAM/PostgreSQL 14 and exact Phoenix/LiveView compatibility lane"
    - path: "test/fixtures/phoenix_1_8_live_view_1_1/mix.exs"
      provides: "Exact lower-bound dependency fixture"
      contains: "== 1.1.28"
    - path: "test/fixtures/phoenix_1_8_live_view_1_1/mix.lock"
      provides: "Reproducible lower-bound dependency resolution"
    - path: "test/fixtures/phoenix_1_8_live_view_1_1/lib/lockspire_compatibility_fixture.ex"
      provides: "Compile-time Phoenix router and LiveView integration proof"
    - path: "test/lockspire/compatibility_baseline_contract_test.exs"
      provides: "Executable drift fence for declared minimum versions and CI wiring"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "test/fixtures/phoenix_1_8_live_view_1_1/mix.lock"
      via: "fixture dependency restore/install and compile steps use the committed lock"
      pattern: "phoenix_1_8_live_view_1_1"
    - from: "test/fixtures/phoenix_1_8_live_view_1_1/mix.exs"
      to: "mix.exs"
      via: "path dependency compiles the current Lockspire checkout against exact lower bounds"
      pattern: "lockspire.*path"
    - from: "test/fixtures/phoenix_1_8_live_view_1_1/lib/lockspire_compatibility_fixture.ex"
      to: "Lockspire.Web.Router"
      via: "Phoenix router forwards protocol and admin mounts to shipped Lockspire routers"
      pattern: "Lockspire.Web.(Admin)?Router"
---

<objective>
Turn Lockspire's declared database and Phoenix/LiveView lower bounds into one reproducible required compatibility lane.

Purpose: Detect breakage at the supported floor while the change is still a pull request, without downgrading the maintainer lock or changing package dependency ranges.
Output: PostgreSQL 14 on the minimum BEAM lane, an exact-version path-dependency fixture, and executable CI/contract proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-trusted-release-path/126-SUMMARY.md
@.planning/phases/127-executable-quality-baselines/127-RESEARCH.md
@mix.exs
@.github/workflows/ci.yml
@test/lockspire/workflow_supply_chain_contract_test.exs

<interfaces>
The root project requires Elixir `~&gt; 1.18`, Phoenix `~&gt; 1.8.5`, and LiveView `&gt;= 1.1.28 and &lt; 2.0.0`. The current compatibility job uses Elixir 1.18.4 / OTP 27, runs `mix compile --warnings-as-errors` and `mix test.fast`, and currently points at PostgreSQL 16. Phase 126 requires every PostgreSQL image to use a verified `@sha256:` digest and every dependency lane to use `--check-locked` plus a lockfile diff guard.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Compile an exact Phoenix/LiveView host seam in the PostgreSQL 14 minimum lane</name>
  <files>.github/workflows/ci.yml, test/fixtures/phoenix_1_8_live_view_1_1/mix.exs, test/fixtures/phoenix_1_8_live_view_1_1/mix.lock, test/fixtures/phoenix_1_8_live_view_1_1/lib/lockspire_compatibility_fixture.ex</files>
  <action>Change only the `compatibility` job's service to the official PostgreSQL 14 image at a registry-resolved immutable sha256 digest, retaining its health check and the Phase 126 immutable-reference comment convention. Create a standalone Mix fixture whose application is not part of Lockspire's normal compilation, depends on the repository root via `{:lockspire, path: ...}`, and pins Phoenix exactly to 1.8.5 and LiveView exactly to 1.1.28. Its source must compile a small host router that forwards protocol and host-guarded admin scopes to `Lockspire.Web.Router` and `Lockspire.Web.AdminRouter`, plus a LiveView using the 1.1.28 API; do not add a runtime service, database schema, or public Lockspire API. Generate and commit the fixture lockfile. Extend the existing minimum-version CI job cache/install path to include the fixture, run fixture `mix deps.get --check-locked`, compile it with warnings as errors, and include its lockfile in the non-mutating lock diff guard. Keep all non-minimum jobs on their current PostgreSQL images.</action>
  <verify>
    <automated>cd test/fixtures/phoenix_1_8_live_view_1_1 &amp;&amp; mix deps.get --check-locked &amp;&amp; mix compile --warnings-as-errors</automated>
  </verify>
  <done>The committed fixture compiles the current Lockspire checkout against the exact supported Phoenix/LiveView floor, and the required minimum BEAM lane runs root tests on immutable PostgreSQL 14 before compiling the fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Ratchet minimum-version and immutable-lock truth with a focused contract</name>
  <files>test/lockspire/compatibility_baseline_contract_test.exs</files>
  <action>Add an async source contract that isolates the `compatibility` job from the CI workflow and asserts its minimum Elixir/OTP environment, PostgreSQL 14 immutable digest, root compile/test commands, exact-version fixture install/compile commands, and fixture lock cache key. Read the fixture Mixfile and lockfile to assert exact Phoenix 1.8.5 and LiveView 1.1.28 resolution plus the Lockspire path dependency. Assert the repository lock diff guard includes the fixture lock and that other CI PostgreSQL services retain immutable digest references. Keep the assertions semantic enough to allow digest rotation without weakening the PostgreSQL 14 or sha256 requirements.</action>
  <verify>
    <automated>mix test test/lockspire/compatibility_baseline_contract_test.exs test/lockspire/workflow_supply_chain_contract_test.exs test/lockspire/ci_static_contract_test.exs</automated>
  </verify>
  <done>Focused contracts fail on supported-version drift, a mutable or wrong-major minimum database image, skipped fixture compilation, lock mutation risk, or loss of Phase 126 supply-chain invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutable CI environment → supported-version claim | Runner toolchains, service images, and dependency resolution must match the versions Lockspire promises. |
| compatibility fixture → Lockspire host seam | The fixture consumes the package as a Phoenix host would, through a path dependency and shipped router modules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-04 | Tampering | PostgreSQL service reference | high | mitigate | Use PostgreSQL 14 with a verified immutable digest and retain the Phase 126 workflow contract. |
| T-127-05 | Repudiation | fixture dependency resolution | medium | mitigate | Commit `mix.lock`, use exact Phoenix/LiveView constraints, run `--check-locked`, and include the lock in the diff guard. |
| T-127-06 | Elevation of privilege | fixture host boundary | low | accept | The fixture compiles router forwarding only and adds no authentication claim or production route; host ownership remains explicit. |
</threat_model>

<verification>
- Fixture: `mix deps.get --check-locked &amp;&amp; mix compile --warnings-as-errors` from its directory.
- Contracts: `mix test test/lockspire/compatibility_baseline_contract_test.exs test/lockspire/workflow_supply_chain_contract_test.exs test/lockspire/ci_static_contract_test.exs`.
- Workflow lint: `bash scripts/ci/lint_workflows.sh`.
</verification>

<success_criteria>
- The minimum Elixir 1.18.4 / OTP 27 job runs the root fast suite on immutable PostgreSQL 14.
- The committed fixture locks and compiles Phoenix 1.8.5, LiveView 1.1.28, and the current Lockspire path dependency.
- CI installs the fixture without rewriting its lock and compiles it with warnings as errors.
- No root dependency range, public API, runtime behavior, or non-minimum database lane changes.
</success_criteria>

<output>
Create `.planning/phases/127-executable-quality-baselines/127-02-SUMMARY.md` when done.
</output>
