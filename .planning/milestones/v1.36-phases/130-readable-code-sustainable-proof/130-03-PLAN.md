---
phase: 130-readable-code-sustainable-proof
plan: 03
type: execute
wave: 3
depends_on: ["130-02"]
files_modified:
  - test/support/token_exchange_case.ex
  - test/lockspire/protocol/token_exchange_test.exs
  - test/lockspire/protocol/token_exchange/authorization_code_test.exs
  - test/lockspire/protocol/token_exchange/device_code_test.exs
  - test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs
autonomous: true
requirements: [TEST-02]
must_haves:
  truths:
    - "Token endpoint tests are grouped by grant capability instead of one 2,000-line module."
    - "Authorization code, device code, CIBA, resource/audience, DPoP, audit, replay, refresh, and facade behavior remain covered."
    - "The split suites share one readable setup/fixture vocabulary and pass together."
  artifacts:
    - path: "test/support/token_exchange_case.ex"
      provides: "Shared token-exchange setup and fixture helpers"
    - path: "test/lockspire/protocol/token_exchange/authorization_code_test.exs"
      provides: "Authorization-code capability coverage"
    - path: "test/lockspire/protocol/token_exchange/device_code_test.exs"
      provides: "Device-code capability coverage"
    - path: "test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs"
      provides: "CIBA and cross-grant resource/audience coverage"
  key_links:
    - from: "test/support/token_exchange_case.ex"
      to: "test/support/data_case.ex"
      via: "shared sandbox/config setup"
      pattern: "Lockspire.DataCase"
---

<objective>
Split the oversized token endpoint suite by grant capability while preserving every behavioral assertion.

Purpose: Make the most security-sensitive tests navigable and cheaper to extend.
Output: Capability-focused token-exchange test modules backed by one shared test case.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/129-token-endpoint-cohesion/129-08-SUMMARY.md
@.planning/phases/130-readable-code-sustainable-proof/130-02-SUMMARY.md
@test/lockspire/protocol/token_exchange_test.exs
@lib/lockspire/protocol/token_exchange_facade.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract a shared token-exchange case and authorization-code capability suite</name>
  <files>test/support/token_exchange_case.ex, test/lockspire/protocol/token_exchange_test.exs, test/lockspire/protocol/token_exchange/authorization_code_test.exs</files>
  <behavior>
    - Authorization code happy paths, PKCE/client/redirect failures, OIDC ID token/auth_time behavior, DPoP nonce/replay, refresh issuance, audit, and code replay retain their assertions.
    - Shared key/client/request fixtures are defined once and clean up through DataCase/ConfigCase patterns.
  </behavior>
  <action>Move reusable setup, signing-key, client, request, telemetry, and decode helpers into `Lockspire.TokenExchangeCase`. Move authorization-code scenarios into the capability file without rewriting assertions into broader or weaker forms. Leave the original module temporarily containing only unsplit grant/cross-grant cases so each intermediate commit stays runnable.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/protocol/token_exchange_test.exs test/lockspire/protocol/token_exchange/authorization_code_test.exs</automated>
  </verify>
  <done>The authorization-code suite is independently readable and all moved assertions pass against the public facade.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split device, CIBA, resource, and facade ownership coverage</name>
  <files>test/lockspire/protocol/token_exchange_test.exs, test/lockspire/protocol/token_exchange/device_code_test.exs, test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs</files>
  <behavior>
    - Device polling states, single redemption, DPoP, refresh, ID token, and audit behavior retain exact coverage.
    - CIBA polling/Push issuance, DPoP nonce, resource/audience defaults, invalid target, and client mismatch retain exact coverage.
    - Facade dispatch and stable result structs remain explicitly covered.
  </behavior>
  <action>Move the remaining cases into device and CIBA/resource modules by capability. Keep a concise facade-focused `token_exchange_test.exs` for unsupported grant, refresh/RFC8693 delegation, public entry points, stable result shapes, and coordinator ownership. Record an old-to-new test-name inventory in the plan summary so coverage movement is auditable.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/protocol/token_exchange_test.exs test/lockspire/protocol/token_exchange &amp;&amp; MIX_ENV=test mix test --cover test/lockspire/protocol/token_exchange_test.exs test/lockspire/protocol/token_exchange</automated>
  </verify>
  <done>The token endpoint contract is capability-shaped, all original scenarios are mapped, and focused coverage remains above the repository floor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OAuth inputs → token endpoint | Split tests protect client auth, PKCE, replay, DPoP, audience, and token issuance behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-07 | Spoofing | client authentication tests | high | mitigate | Preserve invalid auth method, credential parsing, and client mismatch cases by name. |
| T-130-08 | Tampering | code/device/CIBA redemption | high | mitigate | Preserve replay, expiry, binding, nonce, and single-use tests in capability suites. |
| T-130-09 | Information Disclosure | tokens and audit fixtures | high | mitigate | Keep opaque/JWT hash, redaction, and durable audit assertions unchanged during moves. |
</threat_model>

<verification>
Run every token-exchange capability file together and produce focused built-in coverage.
</verification>

<success_criteria>
No token-exchange assertion is lost, and contributors can find grant behavior in a capability-named test file.
</success_criteria>

<output>
Create `.planning/phases/130-readable-code-sustainable-proof/130-03-SUMMARY.md` when done.
</output>
