---
phase: 130-readable-code-sustainable-proof
plan: 06
type: execute
wave: 2
depends_on: ["130-01"]
files_modified:
  - scripts/ci/run_test_matrix.sh
  - .github/workflows/ci.yml
  - mix.exs
  - test/lockspire/ci_test_matrix_contract_test.exs
  - .planning/phases/130-readable-code-sustainable-proof/130-CI-TIMING-BASELINE.md
autonomous: true
requirements: [CI-02]
must_haves:
  truths:
    - "CI emits a machine-readable timing artifact for each required test partition."
    - "The Phase 3 aggregate gate is removed from default CI only after executable membership proof shows all of its files already run in fast or integration."
    - "Focused developer aliases remain available while default CI runs each test file once."
  artifacts:
    - path: "scripts/ci/run_test_matrix.sh"
      provides: "Timed test-partition runner and membership proof"
    - path: ".planning/phases/130-readable-code-sustainable-proof/130-CI-TIMING-BASELINE.md"
      provides: "Measured before/after decision evidence"
    - path: "test/lockspire/ci_test_matrix_contract_test.exs"
      provides: "Executable no-duplicate CI matrix contract"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/ci/run_test_matrix.sh"
      via: "integration job execution and artifact upload"
      pattern: "run_test_matrix.sh"
---

<objective>
Measure CI test partitions, preserve the evidence as an artifact, and remove only the aggregate work proven redundant.

Purpose: Lower CI cost without trading away coverage or making timing decisions anecdotal.
Output: A timed runner, baseline decision record, no-duplicate matrix contract, and uploaded CI evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@mix.exs
@.github/workflows/ci.yml
@test/test_helper.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture timed partition evidence and executable test-set membership</name>
  <files>scripts/ci/run_test_matrix.sh, test/lockspire/ci_test_matrix_contract_test.exs, .planning/phases/130-readable-code-sustainable-proof/130-CI-TIMING-BASELINE.md</files>
  <behavior>
    - The runner records command, elapsed seconds, exit status, and UTC timestamp for fast, integration, and the current aggregate gate.
    - Membership proof shows the aggregate integration file belongs to `test.integration` and its ordinary unit files belong to `test.fast`.
    - Any command failure fails the runner and cannot produce a green evidence result.
  </behavior>
  <action>Create a portable Bash runner using fail-fast execution and elapsed-time measurement, emitting JSON to a caller-selected path plus a concise job summary. Add an ExUnit contract that derives inclusion from alias arguments and `test_helper.exs` tag behavior rather than hard-coded test counts. Run the current three-part matrix once and record measured durations, file-membership proof, environment, and the explicit removal decision in the baseline markdown.</action>
  <verify>
    <automated>MIX_ENV=test bash scripts/ci/run_test_matrix.sh --baseline /tmp/lockspire-ci-timings.json &amp;&amp; test -s /tmp/lockspire-ci-timings.json &amp;&amp; MIX_ENV=test mix test test/lockspire/ci_test_matrix_contract_test.exs</automated>
  </verify>
  <done>The repository contains measured baseline evidence and an executable proof of which aggregate work is duplicate.</done>
</task>

<task type="auto">
  <name>Task 2: Run each CI test partition once and upload timing truth</name>
  <files>.github/workflows/ci.yml, mix.exs, scripts/ci/run_test_matrix.sh, test/lockspire/ci_test_matrix_contract_test.exs</files>
  <action>Replace the integration job's separate integration and aggregate commands with the timed runner's non-baseline mode, which runs fast-owned tests only in the fast job and integration-tagged tests only in the integration job. Remove the aggregate alias from the default `mix ci` chain after the baseline and membership contract prove redundancy; retain focused aliases for maintainer use. Upload the JSON timing output with the repository's already pinned artifact action and a bounded retention period. Add the timing summary to `$GITHUB_STEP_SUMMARY` when present and keep local execution independent of GitHub variables.</action>
  <verify>
    <automated>MIX_ENV=test bash scripts/ci/run_test_matrix.sh --ci /tmp/lockspire-ci-timings.json &amp;&amp; MIX_ENV=test mix test test/lockspire/ci_test_matrix_contract_test.exs &amp;&amp; bash scripts/ci/lint_workflows.sh</automated>
  </verify>
  <done>Default CI has no proven duplicate aggregate run and publishes timing evidence for the required partitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test selection → CI quality claim | Alias/tag semantics determine whether removal preserves full coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-16 | Tampering | timing JSON | medium | mitigate | Fail fast on test failure and emit command/status/timestamp from the runner itself. |
| T-130-17 | Repudiation | duplicate removal | high | mitigate | Commit measured baseline plus executable path/tag membership proof before editing default CI. |
| T-130-18 | Denial of service | CI test matrix | medium | mitigate | Preserve fast and integration partitions and existing job timeout while removing only the redundant aggregate. |
</threat_model>

<verification>
Run baseline and final modes locally, validate the contract, and lint the edited workflow.
</verification>

<success_criteria>
CI runs every intended test file, no proven duplicate aggregate, and emits durable machine-readable timing evidence.
</success_criteria>

<output>
Create `.planning/phases/130-readable-code-sustainable-proof/130-06-SUMMARY.md` when done.
</output>
