---
phase: 128-runtime-dependency-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/authorization_flow.ex
  - lib/lockspire/protocol/userinfo.ex
  - lib/lockspire/protocol/introspection.ex
  - lib/lockspire/protocol/revocation.ex
  - lib/lockspire/protocol/rfc8693_exchange.ex
autonomous: true
requirements: [RUNTIME-01]

must_haves:
  truths:
    - "Authorization, userinfo, introspection, revocation, and RFC 8693 requests without explicit stores use Lockspire's default storage adapter."
    - "Explicit test or host-provided storage overrides retain their current precedence."
    - "Opaque/JWT token lookup, revocation, consent, interaction, client, key, and server-policy behavior is unchanged apart from the corrected default module."
  artifacts:
    - path: "lib/lockspire/protocol/authorization_flow.ex"
      provides: "Repository defaults for the authorization interaction transaction"
    - path: "lib/lockspire/protocol/introspection.ex"
      provides: "Repository defaults for client and token lookup"
    - path: "lib/lockspire/protocol/rfc8693_exchange.ex"
      provides: "Repository defaults for server policy and token persistence"
  key_links:
    - from: "protocol request opts"
      to: "lib/lockspire/storage/ecto/repository.ex"
      via: "Keyword.get/3 and Keyword.put_new/3 retain explicit override precedence and use Repository only when absent"
      pattern: "Repository"
---

<objective>
Correct the storage defaults across the primary authorization and token-lifecycle request paths.

Purpose: Ensure an ordinary configured host Repo is used only by the Ecto adapter, never invoked as a protocol behavior implementation.
Output: Repository-backed defaults for authorization, userinfo, introspection, revocation, and token exchange with all request-level seams preserved.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/lockspire/storage/ecto/repository.ex
@lib/lockspire/protocol/authorization_flow.ex
@lib/lockspire/protocol/userinfo.ex
@lib/lockspire/protocol/introspection.ex
@lib/lockspire/protocol/revocation.ex
@lib/lockspire/protocol/rfc8693_exchange.ex

<interfaces>
Repository implements `InteractionStore`, `ConsentStore`, `TokenStore`, `ClientStore`, `KeyStore`, and `ServerPolicyStore`. Each affected request already accepts an explicit module through its existing `opts`; this plan changes only the absent-option default and does not alter the option names or public request structs.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Carry an authorization and userinfo path through default Repository stores</name>
  <files>lib/lockspire/protocol/authorization_flow.ex, lib/lockspire/protocol/userinfo.ex</files>
  <action>Alias `Lockspire.Storage.Ecto.Repository` and make every absent interaction, consent, token, client, key, and server-policy option in these two modules resolve to that adapter. Preserve explicit override modules exactly, including their current option keys and callback arities. Do not change transaction ordering, authorization-code single-use behavior, consent reuse, subject/claim resolution, userinfo audience/scope checks, response shapes, telemetry, or error collapse.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/authorization_flow_test.exs test/lockspire/web/userinfo_controller_test.exs</automated>
  </verify>
  <done>Default authorization and userinfo flows use Repository behavior callbacks and their focused protocol suites pass without any public or wire change.</done>
</task>

<task type="auto">
  <name>Task 2: Expand adapter defaults across token inspection, revocation, and exchange</name>
  <files>lib/lockspire/protocol/introspection.ex, lib/lockspire/protocol/revocation.ex, lib/lockspire/protocol/rfc8693_exchange.ex</files>
  <action>Apply the same behavior-correct default to every absent client, token, and server-policy store in introspection, revocation, and RFC 8693 exchange. Retain `Keyword.put_new/3` versus `Keyword.get/3` semantics so caller-provided modules continue to win. Preserve client authentication, token hashing, active/revoked checks, RFC 7662 inactive responses, family revocation, exchange audience shape, custom-claim restrictions, audit/telemetry, and error mapping.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/introspection_test.exs test/lockspire/protocol/revocation_test.exs test/lockspire/protocol/rfc8693_exchange_test.exs</automated>
  </verify>
  <done>All three token lifecycle modules use Repository when no store is supplied, explicit overrides remain honored, and focused protocol regressions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted OAuth request → protocol store selection | Request data must never influence which trusted persistence implementation runs. |
| protocol core → storage behavior | Token, client, consent, interaction, key, and policy callbacks must execute against a module implementing the expected contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-04 | Elevation of privilege | authorization/token store defaults | high | mitigate | Bind absent options to Repository while preserving only existing trusted explicit injection seams. |
| T-128-05 | Tampering | revocation and exchange persistence | high | mitigate | Change module selection only; retain current atomic callbacks, token hashes, and state-transition tests. |
| T-128-06 | Information disclosure | introspection/userinfo errors | high | mitigate | Preserve existing inactive/error collapse and run focused response-contract tests. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/authorization_flow_test.exs test/lockspire/web/userinfo_controller_test.exs`
- `mix test test/lockspire/protocol/introspection_test.exs test/lockspire/protocol/revocation_test.exs test/lockspire/protocol/rfc8693_exchange_test.exs`
- `mix format --check-formatted lib/lockspire/protocol/authorization_flow.ex lib/lockspire/protocol/userinfo.ex lib/lockspire/protocol/introspection.ex lib/lockspire/protocol/revocation.ex lib/lockspire/protocol/rfc8693_exchange.ex`
</verification>

<success_criteria>
- No affected protocol-store default resolves to `Config.repo!()`.
- Explicit store injection continues to take precedence.
- Focused authorization, userinfo, introspection, revocation, and RFC 8693 tests pass unchanged in behavior.
</success_criteria>

<output>
Create `.planning/phases/128-runtime-dependency-truth/128-02-SUMMARY.md` when done.
</output>
