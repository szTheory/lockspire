---
phase: 128-runtime-dependency-truth
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/jarm.ex
  - lib/lockspire/protocol/jarm/client_key_resolver.ex
  - lib/lockspire/protocol/introspection_jwt.ex
  - lib/lockspire/protocol/client_auth/client_secret_jwt.ex
  - lib/lockspire/protocol/client_auth/private_key_jwt.ex
  - lib/lockspire/protocol/token_endpoint_dpop.ex
  - lib/lockspire/protocol/protected_resource_dpop.ex
autonomous: true
requirements: [RUNTIME-01]

must_haves:
  truths:
    - "JARM, signed introspection, JWT client authentication, and DPoP paths default to the Lockspire adapter for client, key, policy, and token operations."
    - "Explicit request-level stores continue to override defaults without changing supported seams."
    - "Signing, verification, replay, sender-constraint, and error behavior remains fail closed."
  artifacts:
    - path: "lib/lockspire/protocol/jarm.ex"
      provides: "Adapter-backed signing-key default"
    - path: "lib/lockspire/protocol/client_auth/private_key_jwt.ex"
      provides: "Adapter-backed client and server-policy defaults for asymmetric client assertions"
    - path: "lib/lockspire/protocol/protected_resource_dpop.ex"
      provides: "Adapter-backed token lookup for DPoP-protected resources"
  key_links:
    - from: "JARM/client-auth/DPoP request options"
      to: "lib/lockspire/storage/ecto/repository.ex"
      via: "absent behavior options resolve to Repository while explicit modules win"
      pattern: "Repository"
---

<objective>
Correct the remaining security-sensitive protocol storage fallbacks.

Purpose: Remove the host-repo/storage ambiguity from signing, JWT client authentication, introspection JWT, and DPoP enforcement without weakening any security profile.
Output: Behavior-correct Repository defaults throughout these paths and focused regression proof for both asymmetric and sender-constrained flows.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/lockspire/storage/ecto/repository.ex
@lib/lockspire/protocol/jarm.ex
@lib/lockspire/protocol/jarm/client_key_resolver.ex
@lib/lockspire/protocol/introspection_jwt.ex
@lib/lockspire/protocol/client_auth/client_secret_jwt.ex
@lib/lockspire/protocol/client_auth/private_key_jwt.ex
@lib/lockspire/protocol/token_endpoint_dpop.ex
@lib/lockspire/protocol/protected_resource_dpop.ex

<interfaces>
The existing request options name `:key_store`, `:client_store`, `:server_policy_store`, and `:token_store`. Repository implements those domain callbacks. `Config.repo!()` remains the underlying raw Ecto repo used inside the adapter, not a protocol callback target.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Carry signed response and introspection key lookup through Repository</name>
  <files>lib/lockspire/protocol/jarm.ex, lib/lockspire/protocol/jarm/client_key_resolver.ex, lib/lockspire/protocol/introspection_jwt.ex</files>
  <action>Alias Repository and replace absent key/client-store defaults in JARM signing, JARM client key resolution, and JWT introspection signing. Keep explicit context/option modules authoritative and preserve algorithm allowlists, active-key requirements, remote-JWKS behavior, encryption decisions, private-key redaction, compact response shapes, and normalized failure atoms.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/jarm_test.exs test/lockspire/protocol/introspection_jwt_test.exs</automated>
  </verify>
  <done>JARM and signed introspection use Repository by default, retain all explicit injection and cryptographic behavior, and pass their focused suites.</done>
</task>

<task type="auto">
  <name>Task 2: Expand correct defaults across JWT client authentication and DPoP</name>
  <files>lib/lockspire/protocol/client_auth/client_secret_jwt.ex, lib/lockspire/protocol/client_auth/private_key_jwt.ex, lib/lockspire/protocol/token_endpoint_dpop.ex, lib/lockspire/protocol/protected_resource_dpop.ex</files>
  <action>Make fallback client, policy, and token behavior calls resolve to Repository in both JWT client-auth strategies and both DPoP enforcement modules. Preserve the existing rule that a provided store is accepted only for callbacks it actually exports, but use Repository—not the host Repo—when it does not. Do not alter assertion audience/time/JTI validation, secret hashing, JWKS resolution, DPoP proof verification, nonce handling, replay protection, binding checks, challenge scheme, or error/telemetry shapes.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs test/lockspire/protocol/token_endpoint_dpop_test.exs test/lockspire/protocol/protected_resource_dpop_test.exs</automated>
  </verify>
  <done>JWT client authentication and DPoP paths invoke valid behavior modules by default and all focused security regressions pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted JWT/DPoP input → key/client/token lookup | Attacker-controlled assertions and proofs cross into trusted persistence and cryptographic validation. |
| persisted key material → signed response | Only active, allowed keys may sign JARM and introspection responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-07 | Spoofing | JWT client authentication | critical | mitigate | Preserve assertion validation and bind fallback client/policy lookup to behavior-correct Repository callbacks. |
| T-128-08 | Tampering | DPoP token binding and replay state | critical | mitigate | Change only store selection and run both token-endpoint and protected-resource DPoP suites. |
| T-128-09 | Information disclosure | JARM/introspection signing failures | high | mitigate | Preserve fail-closed normalized errors and current private-key redaction. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/jarm_test.exs test/lockspire/protocol/introspection_jwt_test.exs`
- `mix test test/lockspire/protocol/direct_client_auth_client_secret_jwt_test.exs test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs test/lockspire/protocol/token_endpoint_dpop_test.exs test/lockspire/protocol/protected_resource_dpop_test.exs`
- `mix format --check-formatted lib/lockspire/protocol/jarm.ex lib/lockspire/protocol/jarm/client_key_resolver.ex lib/lockspire/protocol/introspection_jwt.ex lib/lockspire/protocol/client_auth/client_secret_jwt.ex lib/lockspire/protocol/client_auth/private_key_jwt.ex lib/lockspire/protocol/token_endpoint_dpop.ex lib/lockspire/protocol/protected_resource_dpop.ex`
</verification>

<success_criteria>
- No affected signing, client-authentication, or DPoP behavior default targets the configured host Repo.
- Explicit store overrides and every existing cryptographic/security check remain intact.
- All focused JARM, introspection JWT, client-authentication, and DPoP suites pass.
</success_criteria>

<output>
Create `.planning/phases/128-runtime-dependency-truth/128-03-SUMMARY.md` when done.
</output>
