---
phase: 128-runtime-dependency-truth
plan: 05
type: execute
wave: 2
depends_on: [128-04]
files_modified:
  - lib/lockspire/storage/transaction_store.ex
  - lib/lockspire/storage/audit_store.ex
  - lib/lockspire/storage/interaction_store.ex
  - lib/lockspire/storage/logout_store.ex
  - lib/lockspire/storage/ecto/repository.ex
  - lib/lockspire/protocol/logout_propagation.ex
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "Transaction and audit operations are explicit capabilities rather than incidental callbacks on an unrelated store."
    - "Logout propagation persists state, enqueues on the named Lockspire Oban instance, records audit events, marks delivery state, and revokes the sid in one rollback-safe transaction."
    - "Logout read and delivery-state operations are declared by LogoutStore and implemented by Repository."
  artifacts:
    - path: "lib/lockspire/storage/transaction_store.ex"
      provides: "Narrow transaction capability"
    - path: "lib/lockspire/storage/audit_store.ex"
      provides: "Narrow audit append capability"
    - path: "lib/lockspire/storage/logout_store.ex"
      provides: "Complete durable logout persistence/read/update contract"
    - path: "lib/lockspire/protocol/logout_propagation.ex"
      provides: "Port-driven propagation orchestration and named Oban insertion"
  key_links:
    - from: "lib/lockspire/protocol/logout_propagation.ex"
      to: "lib/lockspire/storage/transaction_store.ex"
      via: "complete/1 wraps all persistence and enqueue state changes in Repository.transact/1"
      pattern: "transact"
    - from: "lib/lockspire/protocol/logout_propagation.ex"
      to: "lib/lockspire/oban.ex"
      via: "Oban.insert names Lockspire.Oban explicitly"
      pattern: "Oban.insert(Lockspire.Oban"
---

<objective>
Make transaction, audit, and logout persistence explicit and keep logout job insertion on Lockspire's named Oban instance.

Purpose: Prevent orchestration code from depending on raw Repo mechanics or accidental behavior placement while preserving atomic logout guarantees.
Output: TransactionStore, AuditStore, an expanded LogoutStore, Repository implementations, and a port-driven LogoutPropagation flow.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-runtime-dependency-truth/128-04-SUMMARY.md
@lib/lockspire/storage/interaction_store.ex
@lib/lockspire/storage/logout_store.ex
@lib/lockspire/storage/ecto/repository.ex
@lib/lockspire/protocol/logout_propagation.ex
@lib/lockspire/ciba.ex
@lib/lockspire/oban.ex
@test/lockspire/protocol/logout_propagation_test.exs

<interfaces>
`TransactionStore.transact/1` returns `{:ok, value} | {:error, reason}` with rollback initiated by the Repository implementation. `AuditStore.append_audit_event/1` accepts `Lockspire.Audit.Event` or normalized attrs, and `transact_with_audit/2` preserves the current atomic helper. LogoutStore gains the existing Repository read/update signatures in addition to persistence. The public `LogoutPropagation.complete/1` signature and `Result` remain unchanged.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Carry one logout completion through explicit transaction and audit ports</name>
  <files>lib/lockspire/storage/transaction_store.ex, lib/lockspire/storage/audit_store.ex, lib/lockspire/storage/interaction_store.ex, lib/lockspire/storage/ecto/repository.ex, lib/lockspire/protocol/logout_propagation.ex</files>
  <action>Create narrow TransactionStore and AuditStore behaviors matching the existing Repository functions, declare them on Repository, and mark implementations. Remove `transact/1` from InteractionStore because transaction capability is not interaction-specific. Refactor LogoutPropagation's transaction and audit calls to depend on the explicit capabilities implemented by Repository and to roll back through that adapter rather than `Config.repo!()`. Preserve transaction result normalization, idempotent replay, audit event construction, post-commit telemetry ordering, error tuples, and all-or-nothing mutation behavior.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/logout_propagation_test.exs test/lockspire/storage/ecto/repository_logout_propagation_test.exs test/lockspire/audit/audit_writer_test.exs</automated>
  </verify>
  <done>A complete logout path reaches transaction and audit callbacks through explicit ports, replay/rollback/audit behavior is unchanged, and focused tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete LogoutStore and bind enqueueing to Lockspire.Oban</name>
  <files>lib/lockspire/storage/logout_store.ex, lib/lockspire/storage/ecto/repository.ex, lib/lockspire/protocol/logout_propagation.ex</files>
  <behavior>
    - Back-channel jobs are inserted through the named `Lockspire.Oban` instance and their returned job IDs mark the matching delivery enqueued.
    - Existing-event replay creates no duplicate deliveries, jobs, audit events, or sid revocation.
    - Logout reads and delivery-state updates satisfy typed LogoutStore callbacks.
  </behavior>
  <action>Extend LogoutStore with the existing event lookup, all-delivery list, per-event list, and delivery-enqueued update operations and mark corresponding Repository functions as implementations. Replace raw Repo/Oban changeset insertion in LogoutPropagation with `Oban.insert(Lockspire.Oban, changeset)`, following the established `Lockspire.Ciba` pattern so the configured host Repo remains an internal concern of the named Oban supervisor. Preserve prefix handling through Oban runtime configuration, uniqueness, job args, rollback semantics, delivery status, front-channel behavior, and observability/audit metadata.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/logout_propagation_test.exs test/integration/phase39_logout_propagation_e2e_test.exs test/lockspire/workers/backchannel_logout_delivery_worker_test.exs</automated>
  </verify>
  <done>LogoutStore declares the complete used persistence surface, named Oban insertion works inside the existing atomic flow, and focused protocol/E2E/worker suites pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| logout orchestration → database transaction | Event, deliveries, job state, audits, and sid revocation must commit or roll back together. |
| protocol library → Oban runtime | Jobs must target Lockspire's named instance and configured prefix, not an ambient/default Oban. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-128-13 | Tampering | logout completion transaction | critical | mitigate | Keep all current mutations in one TransactionStore operation and retain rollback/replay tests. |
| T-128-14 | Repudiation | logout audit lifecycle | high | mitigate | Route append through AuditStore and preserve requested/enqueued audit metadata and atomicity. |
| T-128-15 | Denial of service | Oban insertion | high | mitigate | Insert through the named configured instance and preserve unique job configuration and rollback on insertion failure. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/logout_propagation_test.exs test/lockspire/storage/ecto/repository_logout_propagation_test.exs test/lockspire/audit/audit_writer_test.exs`
- `mix test test/integration/phase39_logout_propagation_e2e_test.exs test/lockspire/workers/backchannel_logout_delivery_worker_test.exs`
- `mix format --check-formatted lib/lockspire/storage/transaction_store.ex lib/lockspire/storage/audit_store.ex lib/lockspire/storage/interaction_store.ex lib/lockspire/storage/logout_store.ex lib/lockspire/storage/ecto/repository.ex lib/lockspire/protocol/logout_propagation.ex`
</verification>

<success_criteria>
- Repository explicitly implements transaction, audit, and complete logout-store contracts.
- LogoutPropagation contains no raw host Repo access and inserts through `Lockspire.Oban`.
- Atomicity, idempotency, delivery state, sid revocation, audit, telemetry, and wire behavior remain unchanged.
- Focused logout, audit, E2E, and worker tests pass.
</success_criteria>

<output>
Create `.planning/phases/128-runtime-dependency-truth/128-05-SUMMARY.md` when done.
</output>
