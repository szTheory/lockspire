---
phase: 126-trusted-release-path
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/actions/release-please/runtime/package.json
  - .github/actions/release-please/runtime/package-lock.json
  - .github/dependabot.yml
  - test/lockspire/release_please_runtime_contract_test.exs
autonomous: true
requirements: [SUPPLY-01]
must_haves:
  truths:
    - "The checked-in Release Please runtime has no known moderate-or-higher npm audit finding."
    - "Lockspire's local composite action still creates release PRs and exposes the existing root release outputs."
    - "Dependabot monitors the nested runtime lockfile as its own npm ecosystem."
  artifacts:
    - path: ".github/actions/release-please/runtime/package-lock.json"
      provides: "Reproducible audit-clean Release Please runtime dependency graph"
    - path: ".github/dependabot.yml"
      provides: "Nested npm runtime update coverage"
    - path: "test/lockspire/release_please_runtime_contract_test.exs"
      provides: "Focused runtime version, lock, action API, and Dependabot contract"
  key_links:
    - from: ".github/actions/release-please/action.yml"
      to: ".github/actions/release-please/runtime/package-lock.json"
      via: "npm ci --ignore-scripts installs exactly the audited lock"
      pattern: "npm ci"
    - from: ".github/dependabot.yml"
      to: ".github/actions/release-please/runtime/package.json"
      via: "npm update entry points at the nested runtime directory"
      pattern: "release-please/runtime"
---

<objective>
Make the repo-controlled Release Please runtime reproducible, audited, and continuously maintained.

Purpose: Remove known vulnerable transitive dependencies from code that executes with release permissions.
Output: An audit-clean lockfile, preserved local action behavior, and nested npm Dependabot coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-trusted-release-path/126-RESEARCH.md
@.github/actions/release-please/action.yml
@.github/actions/release-please/runtime/index.js
@.github/actions/release-please/runtime/package.json
@.github/dependabot.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Upgrade the local release runtime without changing its action contract</name>
  <reversibility rating="costly">A major `@actions/core` runtime update is internal but affects the privileged composite action and its full locked dependency graph.</reversibility>
  <files>.github/actions/release-please/runtime/package.json, .github/actions/release-please/runtime/package-lock.json, test/lockspire/release_please_runtime_contract_test.exs</files>
  <behavior>
    - `npm ci --ignore-scripts` succeeds under the action's checked-in Node 24 setup.
    - `npm audit --omit=dev --audit-level=moderate` exits zero.
    - The runtime still loads, creates/grooms release PRs, and exposes `release_created`, `tag_name`, `sha`, and other existing outputs through the composite action.
  </behavior>
  <action>Use the VERIFIED packages in `126-RESEARCH.md`. Begin with focused tests that parse package/lock/action/index contracts without executing a real release. Update direct pins to the lowest currently verified compatible versions that yield a zero moderate-or-higher audit result (the researched pair is `@actions/core` 3.0.1 and `release-please` 17.11.2), regenerate the lockfile with `npm install --package-lock-only --ignore-scripts`, then install with `npm ci --ignore-scripts`. Do not add packages, install scripts, generated bundles, or a remote Release Please action. If registry advisories changed, choose the lowest compatible audit-clean pair and record the exact versions and audit output in the summary.</action>
  <verify>
    <automated>npm ci --prefix .github/actions/release-please/runtime --ignore-scripts &amp;&amp; npm audit --prefix .github/actions/release-please/runtime --omit=dev --audit-level=moderate &amp;&amp; mix test test/lockspire/release_please_runtime_contract_test.exs</automated>
  </verify>
  <done>The privileged local runtime installs reproducibly, audits cleanly, and preserves the supported composite-action output contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Dependabot coverage for the nested npm runtime</name>
  <files>.github/dependabot.yml, test/lockspire/release_please_runtime_contract_test.exs</files>
  <behavior>
    - Dependabot has an npm entry whose directory is exactly the checked-in Release Please runtime.
    - Existing GitHub Actions and Mix entries remain intact.
    - The nested entry has a bounded open-PR limit and weekly schedule consistent with repository policy.
  </behavior>
  <action>Add a `package-ecosystem: npm` entry for `/.github/actions/release-please/runtime`, keeping the established weekly cadence and bounded PR limit. Extend the focused contract test to parse all three ecosystem/directory pairs and reject a root npm entry that would miss the nested lockfile.</action>
  <verify>
    <automated>mix test test/lockspire/release_please_runtime_contract_test.exs</automated>
  </verify>
  <done>Dependabot continuously covers the exact nested package manifest and lockfile used by the release action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → checked-in privileged runtime lock | Registry packages become code executed with release workflow permissions. |
| Dependabot → repository pull requests | Automated dependency updates propose changes to privileged runtime code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-09 | Tampering | npm dependency graph | high | mitigate | Use only VERIFIED existing packages, exact direct pins, a checked-in lockfile, scripts-disabled install, and zero-audit gate. |
| T-126-10 | Elevation of privilege | release action runtime API | high | mitigate | Preserve and test the narrow composite-action inputs/outputs; do not switch to remote executable action code. |
| T-126-11 | Denial of service | automated update volume | low | accept | Weekly cadence and bounded open PRs make temporary update churn tolerable and reviewable. |
| T-126-SC | Tampering | npm package installs | high | mitigate | Package Legitimacy Audit in `126-RESEARCH.md` classifies both existing direct dependencies VERIFIED; no ASSUMED, SUS, or SLOP package is installed. |
</threat_model>

<verification>
- `npm ci --prefix .github/actions/release-please/runtime --ignore-scripts`
- `npm audit --prefix .github/actions/release-please/runtime --omit=dev --audit-level=moderate`
- `mix test test/lockspire/release_please_runtime_contract_test.exs test/lockspire/release_readiness_contract_test.exs`
</verification>

<success_criteria>
- The release runtime's production dependency audit reports zero moderate-or-higher findings.
- Runtime behavior and composite-action outputs remain intact.
- Dependabot owns the nested npm directory explicitly.
</success_criteria>

<output>
Create `.planning/phases/126-trusted-release-path/126-03-SUMMARY.md` when done.
</output>
