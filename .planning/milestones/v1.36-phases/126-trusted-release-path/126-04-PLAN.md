---
phase: 126-trusted-release-path
plan: 04
type: execute
wave: 3
depends_on: [126-02]
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/dependency-review.yml
  - .github/workflows/oidf-conformance.yml
  - .github/workflows/release-please-automerge.yml
  - .github/workflows/release.yml
  - test/lockspire/workflow_supply_chain_contract_test.exs
autonomous: true
requirements: [SUPPLY-02]
must_haves:
  truths:
    - "Dependency review fails when the repository dependency graph is unavailable."
    - "Every third-party action is selected by a full commit SHA and every PostgreSQL service is selected by a reviewed digest."
    - "Every workflow job has an explicit bounded timeout appropriate to its work."
  artifacts:
    - path: ".github/workflows/dependency-review.yml"
      provides: "Fail-closed dependency-change review"
    - path: ".github/workflows/oidf-conformance.yml"
      provides: "Immutable conformance action and service inputs"
    - path: "test/lockspire/workflow_supply_chain_contract_test.exs"
      provides: "Repository-wide immutable-reference and timeout contract"
  key_links:
    - from: "test/lockspire/workflow_supply_chain_contract_test.exs"
      to: ".github/workflows/*.yml"
      via: "YAML/source contract enumerates every job, uses reference, and service image"
      pattern: "timeout-minutes"
---

<objective>
Make workflow dependencies immutable, bounded, and fail-closed.

Purpose: Prevent unavailable security infrastructure, mutable upstream references, or unbounded jobs from silently weakening release integrity.
Output: Hardened dependency review, immutable workflow inputs, bounded jobs, and repository-wide regression proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-trusted-release-path/126-RESEARCH.md
@.planning/phases/126-trusted-release-path/126-02-SUMMARY.md
@.github/workflows/ci.yml
@.github/workflows/dependency-review.yml
@.github/workflows/oidf-conformance.yml
@.github/workflows/release-please-automerge.yml
@.github/workflows/release.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fail closed on dependency review and immutable conformance inputs</name>
  <files>.github/workflows/dependency-review.yml, .github/workflows/oidf-conformance.yml, test/lockspire/workflow_supply_chain_contract_test.exs</files>
  <behavior>
    - Dependency-graph API failure or unavailability makes the dependency-review job fail.
    - Dependency review always reaches the full-SHA pinned review action when its prerequisite is available.
    - OIDF artifact uploads use a reviewed full commit SHA and PostgreSQL service images include a reviewed sha256 digest.
  </behavior>
  <action>Write the repository-wide supply-chain contract first. Replace the dependency-review skip path with an explicit failing prerequisite or direct fail-closed action execution; retain least-privilege permissions and the existing full-SHA action pin. Resolve `actions/upload-artifact` v7 to its reviewed upstream full commit SHA and replace all OIDF upload tags. Resolve the official PostgreSQL service image used by these jobs to a sha256 digest and retain the human-readable tag plus digest syntax. Record source tag and digest in comments for Dependabot/reviewer clarity.</action>
  <verify>
    <automated>mix test test/lockspire/workflow_supply_chain_contract_test.exs</automated>
  </verify>
  <done>Dependency security cannot silently skip, and the first complete conformance path contains no mutable third-party executable or service reference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply immutable service references and bounded timeouts repository-wide</name>
  <files>.github/workflows/ci.yml, .github/workflows/release-please-automerge.yml, .github/workflows/release.yml, test/lockspire/workflow_supply_chain_contract_test.exs</files>
  <behavior>
    - Every job in every workflow declares `timeout-minutes`.
    - Every remaining PostgreSQL service image uses a reviewed digest.
    - Local composite actions remain allowed while every external `uses:` reference is a 40-character commit SHA.
  </behavior>
  <action>Add explicit job timeouts to CI, dependency review, OIDF conformance, release automerge, release validation/publish/install truth, choosing larger bounds only for compilation, conformance, publication propagation, and clean-room install jobs. Apply the same reviewed PostgreSQL digest to remaining service jobs, except Phase 127 may intentionally replace the minimum compatibility lane with PostgreSQL 14 and its own digest. Extend the contract test to enumerate all workflow files and jobs, accept local `./` actions, require full 40-character SHAs for external actions, require sha256 service digests, and assert every job has a positive timeout. Do not weaken permissions or change test selection.</action>
  <verify>
    <automated>mix test test/lockspire/workflow_supply_chain_contract_test.exs test/lockspire/release_ci_evidence_contract_test.exs test/lockspire/publish_verification_test.exs</automated>
  </verify>
  <done>All current workflow jobs are time-bounded and every executable external action/service reference is immutable under focused proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Marketplace / container registry → CI runner | External executable code and service images run inside trusted workflows. |
| dependency graph service → merge gate | Availability of dependency metadata controls whether vulnerable changes are reviewed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-12 | Tampering | external action references | high | mitigate | Require reviewed full commit SHAs for every external action. |
| T-126-13 | Tampering | PostgreSQL service image | high | mitigate | Pin official images by sha256 digest and test the reference shape. |
| T-126-14 | Elevation of privilege | dependency-review skip path | high | mitigate | Convert unavailable dependency metadata into a failing required job. |
| T-126-15 | Denial of service | unbounded workflow jobs | medium | mitigate | Give every job an explicit timeout sized to its workload. |
| T-126-SC | Tampering | npm package installs | high | mitigate | No npm package install or manifest change occurs in this plan. |
</threat_model>

<verification>
- `mix test test/lockspire/workflow_supply_chain_contract_test.exs`
- `actionlint -no-color .github/workflows/*.yml` (final zero-warning enforcement lands in Plan 05)
</verification>

<success_criteria>
- Dependency review never reports success by skipping unavailable graph evidence.
- No mutable external action tag or undigested service image remains.
- Every job is bounded and the contract scans the whole workflow directory.
</success_criteria>

<output>
Create `.planning/phases/126-trusted-release-path/126-04-SUMMARY.md` when done.
</output>
