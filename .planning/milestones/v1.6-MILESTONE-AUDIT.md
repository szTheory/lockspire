---
milestone: v1.6
audited: 2026-04-28T13:46:55Z
status: passed
scores:
  requirements: 9/9
  phases: 3/3
  integration: 4/4
  flows: 4/4
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt: []
nyquist:
  compliant_phases:
    - "30"
    - "31"
    - "32"
  partial_phases: []
  missing_phases: []
  overall: "complete"
---

# Milestone v1.6 Audit

## Verdict

`v1.6 Device Authorization Grant (RFC 8628)` clears the milestone audit gate.

The shipped path `POST /device/code -> host-owned /verify -> POST /token` is intact, re-verified, and now reconciled with the milestone ledger, supported-surface contract, and Nyquist validation metadata.

## Scope

- Milestone: `v1.6 Device Authorization Grant (RFC 8628)`
- Phases in scope: `30-32`
- Plans in scope: `10`
- Completed summaries found: `10`
- Phase verifications found: `3` (`30`, `31`, `32`)

## Phase Verification Status

| Phase | Name | Verification | Result | Notes |
|-------|------|--------------|--------|-------|
| 30 | Core Device Authorization Endpoint & Storage | present | passed | `30-VERIFICATION.md` now closes `DEV-01` through `DEV-03` with controller, repository, generator, and mounted-route proof. |
| 31 | Host-Owned Verification UI Seam | present | passed | `31-VERIFICATION.md` closes `DEV-04` through `DEV-06` and records the re-verification that fixed the review-step gap. |
| 32 | Polling & Token Issuance | present | passed | `32-VERIFICATION.md` closes `DEV-07` through `DEV-09` and proves polling, token redemption, discovery truth, and the E2E flow. |

## Requirements Coverage

Three-source cross-reference used:

- `.planning/REQUIREMENTS.md` checklist and traceability table
- phase `*-VERIFICATION.md` requirements tables
- summary frontmatter `requirements-completed`

| Requirement | Assigned Phase | REQUIREMENTS.md | Verification Evidence | Summary Frontmatter | Final Status | Notes |
|-------------|----------------|-----------------|-----------------------|---------------------|--------------|-------|
| `DEV-01` | 30 | complete | passed (`30`) | missing | satisfied | Runtime route/controller/protocol plus mounted-route proof now exist in `30-VERIFICATION.md`. |
| `DEV-02` | 30 | complete | passed (`30`) | missing | satisfied | Code generation and protocol proof are now recorded in `30-VERIFICATION.md`. |
| `DEV-03` | 30 | complete | passed (`30`) | missing | satisfied | Durable storage, TTL, and persistence proof are now recorded in `30-VERIFICATION.md`. |
| `DEV-04` | 31 | complete | passed (`31`) | listed (`31-01`, `31-02`, `31-04`) | satisfied | The host-owned `/verify` seam is verified and the traceability table is reconciled. |
| `DEV-05` | 31 | complete | passed (`31`) | listed (`31-02`) | satisfied | Prefill-only `verification_uri_complete` handling is verified and the traceability table is reconciled. |
| `DEV-06` | 31 | complete | passed (`31`) | listed (`31-03`) | satisfied | Host rate-limit docs are verified and the traceability table is reconciled. |
| `DEV-07` | 32 | complete | passed (`32`) | listed (`32-02`, `32-03`) | satisfied | `/token` device grant routing is verified in protocol, controller, and E2E evidence. |
| `DEV-08` | 32 | complete | passed (`32`) | listed (`32-02`, `32-03`) | satisfied | Pending, `slow_down`, and success mappings are verified end to end. |
| `DEV-09` | 32 | complete (`32-01`) | passed (`32`) | listed (`32-01`, `32-02`) | satisfied | Durable polling enforcement is verified in repository, protocol, and controller evidence. |

No cross-phase orphaned requirement IDs were found.

## Cross-Phase Integration

Integration verdict: runtime wiring is intact.

### Findings

- `POST /device/code` is mounted in `lib/lockspire/web/router.ex`, handled by `lib/lockspire/web/controllers/device_authorization_controller.ex`, and flows through `lib/lockspire/protocol/device_authorization.ex` into repository-backed storage.
- Phase 31 consumes that output through `verification_uri_complete`, the generated host-owned `/verify` seam, and `lib/lockspire/protocol/device_verification.ex`.
- Phase 32 consumes approved device authorizations through `POST /token`, `lib/lockspire/protocol/token_exchange.ex`, and row-locked polling/consume callbacks in `lib/lockspire/storage/ecto/repository.ex`.
- Discovery truth publishes the device grant and `device_authorization_endpoint`, and the generated-host E2E flow proves `/device/code -> /verify -> /token`.

No runtime integration blocker or broken handoff was found.

## End-to-End Flows

| Flow | Status | Result |
|------|--------|--------|
| `/device/code` issuance | complete | Device authorization responses are mounted and verified by controller tests plus Phase 32 E2E proof. |
| Host-owned `/verify` lookup and approval | complete | Generated-host integration proof and Phase 31 verification confirm explicit user-action lookup, review, and approval. |
| `/token` device polling and single-use redemption | complete | Phase 32 verification and E2E proof confirm pending, `slow_down`, success, and replay collapse. |
| Discovery and docs truth surface | complete | Discovery protocol/controller tests and release-readiness docs checks confirm the shipped device-flow surface. |

Fresh scoped integration evidence: `MIX_ENV=test mix test test/integration/phase31_generated_host_verification_e2e_test.exs test/integration/phase32_device_flow_token_exchange_e2e_test.exs test/lockspire/web/token_controller_test.exs test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs` passed with `23 tests, 0 failures`.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 30 | exists | true | none |
| 31 | exists | true | none |
| 32 | exists | true | none |

## Conclusion

The milestone is ready for `$gsd-complete-milestone`.

Resolved since the earlier audit pass:

- `.planning/STATE.md` now reports `v1.6`
- `docs/supported-surface.md` now reflects preview `v0.2.0`
- `30-VALIDATION.md` and `32-VALIDATION.md` now carry complete Nyquist metadata
- `audit-open` is clear across UAT, verification, and other planning artifact types
