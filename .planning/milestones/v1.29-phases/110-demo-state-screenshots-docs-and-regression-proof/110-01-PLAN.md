---
phase: 110-demo-state-screenshots-docs-and-regression-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/adoption_demo/priv/repo/seeds.exs
  - test/lockspire/web/live/admin/design_system_contract_test.exs
autonomous: true
requirements:
  - CONFIG-03
user_setup: []
security_gate:
  standard: "OWASP ASVS 5.0.0 Level 1"
  block_on: high
  categories:
    - V4 Access Control
    - V5 Input Validation
    - V6 Cryptography
must_haves:
  truths:
    - "D-01: Phase 110 is a milestone closeout and proof phase, not another UI polish or route redesign phase; Phase 107 route contracts, Phase 108 primitives, Phase 109 polish, and `110-UI-SPEC.md` are locked inputs."
    - "D-02: `examples/adoption_demo/priv/repo/seeds.exs` remains the single repeatable source of screenshot and click-through demo state."
    - "D-03: Demo state exercises healthy, warning, incident, disabled, self-registered, retryable, revoked, expired, long-value, and copy-once states."
    - "D-04: Demo seed values are deterministic, artificial, and redaction-safe; no real-looking secrets or recoverable plaintext credentials are persisted into docs, inventories, logs, or later screenshots."
  artifacts:
    - path: "examples/adoption_demo/priv/repo/seeds.exs"
      provides: "repeatable admin proof state for screenshots and browser click-through"
      contains: "northstar-dcr-self-registered"
    - path: "test/lockspire/web/live/admin/design_system_contract_test.exs"
      provides: "deterministic seed-state coverage assertions"
      contains: "Phase 110"
  key_links:
    - from: "examples/adoption_demo/priv/repo/seeds.exs"
      to: ".planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-SCREENSHOTS.md"
      via: "inventory rows name the seeded state exercised by each route"
      pattern: "demo"
---

<objective>
Make the adoption demo seed state complete enough for final v1.29 route proof.

Purpose: Cover CONFIG-03 by ensuring the demo can exercise healthy, warning, incident, disabled, self-registered, retryable, revoked, expired, long-value, and copy-once admin states without adding protocol behavior or exposing secret material.
Output: Updated demo seed script and deterministic seed coverage assertions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-CONTEXT.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-RESEARCH.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-VALIDATION.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-PATTERNS.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-UI-SPEC.md
@examples/adoption_demo/priv/repo/seeds.exs
@test/lockspire/web/live/admin/design_system_contract_test.exs

<interfaces>
Use existing domain structs and `Lockspire.Storage.Ecto.Repository` calls in `examples/adoption_demo/priv/repo/seeds.exs`. Do not add migrations, schemas, protocol operations, admin capabilities, or dependencies. Preserve existing seeded clients and state unless a value must be expanded for proof.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inventory and fill the Phase 110 demo state matrix</name>
  <files>examples/adoption_demo/priv/repo/seeds.exs, test/lockspire/web/live/admin/design_system_contract_test.exs</files>
  <read_first>
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-CONTEXT.md
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-UI-SPEC.md
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-RESEARCH.md
    - examples/adoption_demo/priv/repo/seeds.exs
    - test/lockspire/web/live/admin/design_system_contract_test.exs
    - lib/lockspire/domain/client.ex
    - lib/lockspire/domain/token.ex
    - lib/lockspire/domain/consent_grant.ex
    - lib/lockspire/domain/device_authorization.ex
    - lib/lockspire/domain/interaction.ex
    - lib/lockspire/domain/logout_delivery.ex
    - lib/lockspire/domain/initial_access_token.ex
    - lib/lockspire/domain/signing_key.ex
  </read_first>
  <behavior>
    - Test 1: The seed source contains deterministic artificial examples for healthy, warning, incident, disabled, self-registered, retryable, revoked, expired, long-value, and copy-once proof states.
    - Test 2: The seed source does not persist real-looking plaintext client secrets, access tokens, refresh tokens, initial access tokens, registration access tokens, user codes, verifier material, or raw credentials into inventory/doc-facing values.
    - Test 3: The existing seeded happy-path client state remains present for adoption-demo smoke flows.
  </behavior>
  <action>
    Extend `design_system_contract_test.exs` first with a Phase 110 seed-state coverage test that reads `examples/adoption_demo/priv/repo/seeds.exs`. Assert the source includes the required proof-state vocabulary or concrete domain states from `110-UI-SPEC.md`: healthy or active, warning or retryable or pending, incident or failed or reuse-detected, disabled, self-registered, retryable, revoked, expired, long-value via long client IDs/URLs/families/endpoints, and copy-once via IAT minting, client secret rotation, or RAT rotation proof language. Then update `seeds.exs` only where the test exposes real gaps. Preserve existing clients `acme-ledger-public`, `acme-tv-device`, `acme-ledger-backend`, `northstar-dcr-self-registered`, and `legacy-disabled-reporter`. Add missing artificial state with obvious demo IDs and URLs under `.example.com` or loopback domains. Do not add new domain APIs, migrations, protocol behavior, or storage semantics.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1</automated>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `design_system_contract_test.exs` contains `Phase 110` and reads `examples/adoption_demo/priv/repo/seeds.exs`.
    - `seeds.exs` contains artificial proof state for healthy/active, warning/pending/retryable, incident/failed/reuse-detected, disabled, self-registered, revoked, expired, long-value, and copy-once states.
    - `seeds.exs` still contains `acme-ledger-public`, `acme-tv-device`, `acme-ledger-backend`, `northstar-dcr-self-registered`, and `legacy-disabled-reporter`.
    - The automated test commands exit 0.
  </acceptance_criteria>
  <done>The adoption demo seed matrix is complete enough for Phase 110 proof and fenced by deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fence copy-once and redaction-safe demo proof values</name>
  <files>examples/adoption_demo/priv/repo/seeds.exs, test/lockspire/web/live/admin/design_system_contract_test.exs</files>
  <read_first>
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-CONTEXT.md
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-UI-SPEC.md
    - examples/adoption_demo/priv/repo/seeds.exs
    - test/lockspire/web/live/admin/design_system_contract_test.exs
    - lib/lockspire/security/policy.ex
    - lib/lockspire/web/components/admin_components.ex
  </read_first>
  <behavior>
    - Test 1: Seeded sensitive values are hashed or redacted through existing helpers such as `Lockspire.Security.Policy.hash_client_secret/1`, `hash_token/1`, or existing copy-once UI flows.
    - Test 2: Seed comments or metadata identify copy-once proof without storing later plaintext in screenshot inventory or docs.
    - Test 3: The seed source remains free of realistic production tenant names, domains, credentials, and opaque token-looking values outside artificial demo placeholders.
  </behavior>
  <action>
    Add deterministic source assertions for redaction-safe seed values. Check that seeded client secrets, registration access tokens, IATs, device codes, and token values go through `Lockspire.Security.Policy.hash_client_secret/1` or `Lockspire.Security.Policy.hash_token/1` rather than being rendered as durable plaintext fields. If additional seed state is needed, use values beginning with `demo-`, accounts such as `acct-alice` or `acct-bob`, and `.example.com` domains. Do not put generated IAT, RAT, client secret, user code, verifier, or access/refresh token plaintext into `docs/operator-admin.md`, `110-SCREENSHOTS.md`, `110-BROWSER-EVIDENCE.md`, or any committed proof inventory. Preserve copy-once proof as a UI/browser workflow to be captured later, not as persisted secret content.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1</automated>
    <automated>rg "tmp/admin-ui-polish" lib >/tmp/lockspire-phase110-runtime-screenshot-ref.txt; test ! -s /tmp/lockspire-phase110-runtime-screenshot-ref.txt</automated>
  </verify>
  <acceptance_criteria>
    - `design_system_contract_test.exs` asserts seed redaction/copy-once safety or forbidden plaintext patterns.
    - `seeds.exs` hashes seeded secret/token values through existing security helpers.
    - No source file under `lib/` references `tmp/admin-ui-polish`.
    - The automated commands exit 0.
  </acceptance_criteria>
  <done>Demo seed proof remains artificial, redaction-safe, and compatible with copy-once screenshots.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Demo seed source -> screenshot/browser proof | Artificial demo data becomes visible to operators and reviewers during screenshot capture |
| Seeded secrets -> durable storage | Sensitive demo placeholders must be hashed or shown only through copy-once flows |
| Proof artifacts -> public repo | Inventories and docs must not persist secret plaintext or realistic credentials |

## ASVS Level 1 / High-Block Gate

| Gate | Handling |
|------|----------|
| Baseline | OWASP ASVS 5.0.0 Level 1 |
| Block condition | Block plan completion on unresolved HIGH information-disclosure or auth-boundary issues. |
| Applicable categories | V4 Access Control for host-owned admin boundary preservation; V5 Input Validation for deterministic demo values; V6 Cryptography for hashed secret/token material. |
| Executor handling | If a required proof state appears to require exposing real secret material, stop and replace it with redacted/artificial demo state or record an explicit proof gap. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Information Disclosure | `examples/adoption_demo/priv/repo/seeds.exs` | mitigate | hash token/secret placeholders, keep values artificial, and add source tests forbidding durable plaintext proof values |
| T-110-02 | Spoofing | demo tenant/client names | mitigate | use obvious demo IDs, loopback URLs, and `.example.com` domains rather than production-like tenant data |
| T-110-03 | Tampering | demo seed semantics | accept | this plan changes only repeatable demo proof state, not protocol enforcement or storage schemas |
| T-110-SC | Tampering | package manager installs | accept | no dependency installation or package-manager task is allowed in this plan |
</threat_model>

<verification>
- `mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1`
- `MIX_ENV=test mix compile --warnings-as-errors`
- `rg "tmp/admin-ui-polish" lib >/tmp/lockspire-phase110-runtime-screenshot-ref.txt; test ! -s /tmp/lockspire-phase110-runtime-screenshot-ref.txt`
</verification>

<success_criteria>
- Adoption demo seeds exercise all CONFIG-03 state categories.
- Sensitive seeded values remain hashed, artificial, or copy-once only.
- No runtime source references screenshot evidence files.
</success_criteria>

<output>
Create `.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-01-SUMMARY.md` when done.
</output>
