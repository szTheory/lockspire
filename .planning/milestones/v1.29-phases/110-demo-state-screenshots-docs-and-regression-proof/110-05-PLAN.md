---
phase: 110-demo-state-screenshots-docs-and-regression-proof
plan: 05
type: execute
wave: 4
depends_on:
  - "110-04"
files_modified:
  - lib/lockspire/web/admin_css.ex
  - test/lockspire/web/live/admin/design_system_contract_test.exs
  - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-SCREENSHOTS.md
  - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-BROWSER-EVIDENCE.md
  - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-VERIFICATION.md
autonomous: true
requirements:
  - PROOF-04
security_gate:
  standard: "OWASP ASVS 5.0.0 Level 1"
  block_on: high
  categories:
    - V4 Access Control
    - V5 Input Validation
    - V6 Cryptography
must_haves:
  truths:
    - "D-10: Mobile proof targets 390px and catches page-level horizontal overflow, overlapping text, clipped controls, long-value wrapping failures, and incoherent action stacking."
    - "D-15: Phase 110 closure records compile proof, docs/diff truth, admin LiveView tests, design-system contract tests, seeded browser click-through, screenshot inventory proof, and mobile no-page-overflow proof."
    - "D-16: Environment gaps are concrete and route-specific."
  artifacts:
    - path: "lib/lockspire/web/admin_css.ex"
      provides: "embedded admin responsive CSS contract for client workspace routes"
      contains: "lockspire-admin-client-workspace"
    - path: "test/lockspire/web/live/admin/design_system_contract_test.exs"
      provides: "deterministic responsive source contract for client route overflow fix"
      contains: "390px"
    - path: ".planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-BROWSER-EVIDENCE.md"
      provides: "rerun evidence for all previously failing 390px routes"
      contains: "390px no-page-overflow"
---

<objective>
Close the Phase 110 verification gap by fixing the 390px page-level overflow on client workspace and client workflow routes, pinning the responsive CSS contract in deterministic tests, and updating browser evidence only after the nine failing routes return `false` for page overflow.

Purpose: Convert Phase 110 from `status: gaps_found` to passed without redesigning admin routes or broadening Lockspire behavior.
Output: CSS responsive fix, deterministic contract test, rerun browser evidence, and updated Phase 110 verification artifacts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-CONTEXT.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-VALIDATION.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-PATTERNS.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-VERIFICATION.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-BROWSER-EVIDENCE.md
@.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-SCREENSHOTS.md
@lib/lockspire/web/admin_css.ex
@lib/lockspire/web/live/admin/clients_live/show.ex
@test/lockspire/web/live/admin/design_system_contract_test.exs

<interfaces>
The failing browser proof is isolated to `Lockspire.Web.Live.Admin.ClientsLive.Show` routes rendered through `.lockspire-admin-client-workspace`, `.lockspire-admin-form-shell`, client description/value lists, action groups, copy-once panels, and rotation forms. Keep the fix inside the embedded admin CSS/design-system contract; do not add a visual-regression stack, new browser dependency, route redesign, or screenshot-only CSS.
</interfaces>
</context>

<tasks>

<task type="execute" tdd="false">
  <name>Task 1: Patch the client route responsive overflow contract</name>
  <files>lib/lockspire/web/admin_css.ex</files>
  <read_first>
    - lib/lockspire/web/admin_css.ex
    - lib/lockspire/web/live/admin/clients_live/show.ex
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-VERIFICATION.md
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-BROWSER-EVIDENCE.md
  </read_first>
  <behavior>
    - At 390px viewport width, the client detail and workflow routes must not create page-level horizontal overflow.
    - Long client IDs, redirect URIs, logout URIs, registration client URIs, PAR/security labels, copy-once values, form fields, and action buttons wrap or shrink inside the viewport instead of forcing the document wider.
  </behavior>
  <action>
    In `lib/lockspire/web/admin_css.ex`, add the missing shrink/wrap constraints to the existing admin CSS contract. Set `.lockspire-admin-card`, `.lockspire-admin-form-shell`, `.lockspire-admin-client-workspace`, `.lockspire-admin-client-workspace > *`, `.lockspire-admin-description-list`, `.lockspire-admin-description-list div`, `.lockspire-admin-value-list`, `.lockspire-admin-copy-once-secret`, `.lockspire-admin-copy-once-secret__value`, `.lockspire-admin-code-block`, `.lockspire-admin-display-value`, `.lockspire-admin-action-bar`, `.lockspire-admin-actions`, `.lockspire-admin-action-group`, `.lockspire-admin-action-group__primary`, `.lockspire-admin-action-group__secondary`, `.lockspire-admin-action-group__destructive`, and `.lockspire-admin-confirmation-panel__actions` so their min-content widths cannot push the page wider than their parent. Use concrete CSS properties already used in this file: `min-width: 0`, `max-width: 100%`, `overflow-wrap: anywhere`, and `word-break: break-word` where text/value content needs wrapping. In the existing `@media (max-width: 720px)` block, make `.lockspire-admin-client-workspace` use `grid-template-columns: minmax(0, 1fr)`, make `.lockspire-admin-form-shell` use `width: 100%` and `max-width: 100%`, and ensure `.lockspire-admin-btn-primary`, `.lockspire-admin-btn-secondary`, and `.lockspire-admin-btn-danger` retain `width: 100%` without causing overflow. Preserve token usage, BEM naming, reduced-motion behavior, and the current visual hierarchy.
  </action>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
    <automated>mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lockspire/web/admin_css.ex` contains `.lockspire-admin-client-workspace` with `grid-template-columns: repeat(auto-fit, minmax(260px, 1fr))` outside the mobile media block and `grid-template-columns: minmax(0, 1fr)` inside `@media (max-width: 720px)`.
    - `lib/lockspire/web/admin_css.ex` contains `min-width: 0` for `.lockspire-admin-card`, `.lockspire-admin-form-shell`, `.lockspire-admin-client-workspace`, and `.lockspire-admin-client-workspace > *`.
    - `lib/lockspire/web/admin_css.ex` contains `max-width: 100%` for `.lockspire-admin-card`, `.lockspire-admin-form-shell`, `.lockspire-admin-copy-once-secret__value`, and `.lockspire-admin-code-block`.
    - `lib/lockspire/web/admin_css.ex` contains `overflow-wrap: anywhere` for long-value surfaces used by client routes: `.lockspire-admin-description-list dd`, `.lockspire-admin-value-list`, `.lockspire-admin-display-value`, `.lockspire-admin-code-block`, and `.lockspire-admin-copy-once-secret__value`.
    - The automated commands exit 0.
  </acceptance_criteria>
  <done>The embedded admin CSS has a deterministic shrink/wrap contract for the failing client workspace and workflow routes.</done>
</task>

<task type="execute" tdd="false">
  <name>Task 2: Pin the client-route responsive contract in deterministic tests</name>
  <files>test/lockspire/web/live/admin/design_system_contract_test.exs</files>
  <read_first>
    - test/lockspire/web/live/admin/design_system_contract_test.exs
    - lib/lockspire/web/admin_css.ex
    - lib/lockspire/web/live/admin/clients_live/show.ex
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-VERIFICATION.md
  </read_first>
  <behavior>
    - Source-level regression tests fail if the client-route CSS loses the specific shrink/wrap contract needed for 390px proof.
    - The test remains deterministic ExUnit/source proof and does not depend on screenshot paths, external browser tooling, or runtime CSS parsing libraries.
  </behavior>
  <action>
    Extend `test/lockspire/web/live/admin/design_system_contract_test.exs` with a focused test named `phase 110 client workspace CSS prevents 390px page overflow regressions` or equivalent. The test must read `lib/lockspire/web/admin_css.ex`, assert that `.lockspire-admin-client-workspace`, `.lockspire-admin-form-shell`, `.lockspire-admin-copy-once-secret__value`, `.lockspire-admin-code-block`, `.lockspire-admin-action-group`, and `.lockspire-admin-description-list dd` have the required `min-width: 0`, `max-width: 100%`, `overflow-wrap: anywhere`, or `grid-template-columns: minmax(0, 1fr)` strings. Keep helper code local to this module if needed; do not introduce a CSS parser dependency.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1</automated>
  </verify>
  <acceptance_criteria>
    - `test/lockspire/web/live/admin/design_system_contract_test.exs` contains a Phase 110 responsive contract test that mentions `390px`.
    - The test asserts `.lockspire-admin-client-workspace` and `grid-template-columns: minmax(0, 1fr)`.
    - The test asserts `.lockspire-admin-form-shell` and `max-width: 100%`.
    - The test asserts `.lockspire-admin-copy-once-secret__value` and `overflow-wrap: anywhere`.
    - The test asserts `.lockspire-admin-code-block` and `max-width: 100%`.
    - The automated command exits 0.
  </acceptance_criteria>
  <done>The responsive overflow fix is fenced by deterministic design-system contract coverage.</done>
</task>

<task type="manual" tdd="false">
  <name>Task 3: Re-run 390px browser proof and update Phase 110 evidence</name>
  <files>.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-SCREENSHOTS.md, .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-BROWSER-EVIDENCE.md, .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-SCREENSHOTS.md
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-BROWSER-EVIDENCE.md
    - .planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-VERIFICATION.md
    - docs/adoption-demo.md
    - examples/adoption_demo/priv/repo/seeds.exs
  </read_first>
  <behavior>
    - Browser proof: each previously failing route returns `false` for page-level overflow at 390px.
    - Evidence proof: Phase 110 artifacts name the rerun route, viewport, result, and remaining gap if any route still fails.
    - Verification proof: `110-VERIFICATION.md` uses passed status only after automated tests and browser overflow proof are green.
  </behavior>
  <action>
    Start the seeded adoption demo and sign in as the seeded `ops` operator. Set viewport width to 390px and re-check exactly these routes: `/admin/clients/:client_id`, `/admin/clients/:client_id/edit`, `/admin/clients/:client_id/redirects`, `/admin/clients/:client_id/logout-uris`, `/admin/clients/:client_id/edit?workflow=logout-propagation`, `/admin/clients/:client_id/par-policy`, `/admin/clients/:client_id/security-profile`, `/admin/clients/:client_id/rotate-secret`, and `/admin/clients/:client_id/rotate-registration-access-token`. For each route, run `document.documentElement.scrollWidth > document.documentElement.clientWidth` in the browser. Update `110-BROWSER-EVIDENCE.md` and the matching `110-SCREENSHOTS.md` notes so each route records `390px no-page-overflow returned false` only when the browser result is false. If any route returns true, keep `Status: Gaps`, list the exact failing route, and do not mark `110-VERIFICATION.md` as passed. After all nine routes return false, update `110-VERIFICATION.md` frontmatter to `status: passed` and replace the gap section with the passing rerun evidence, command results, and screenshot/browser status.
  </action>
  <verify>
    <automated>MIX_ENV=test mix compile --warnings-as-errors</automated>
    <automated>git diff --check</automated>
    <automated>mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1</automated>
    <automated>mix test test/lockspire/web/live/admin --max-failures 1</automated>
    <automated>mix test</automated>
    <manual>At 390px, every route listed in this task returns `false` for `document.documentElement.scrollWidth > document.documentElement.clientWidth`.</manual>
  </verify>
  <acceptance_criteria>
    - `110-BROWSER-EVIDENCE.md` no longer says the nine client workspace/workflow routes returned `true` after the rerun.
    - `110-SCREENSHOTS.md` browser notes for the nine client workspace/workflow routes record the passing 390px no-page-overflow rerun or a route-specific remaining gap.
    - `110-VERIFICATION.md` frontmatter is `status: passed` only when all nine listed routes return `false` and every automated command exits 0.
    - `110-VERIFICATION.md` records `MIX_ENV=test mix compile --warnings-as-errors`, `git diff --check`, `mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1`, `mix test test/lockspire/web/live/admin --max-failures 1`, and `mix test` results.
    - No artifact records plaintext IATs, RATs, client secrets, user codes, verifier material, access tokens, refresh tokens, or token hashes.
  </acceptance_criteria>
  <done>Phase 110 evidence reflects the rerun browser result and only claims passed verification when the overflow gap is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS fix -> admin route rendering | Responsive changes must not obscure controls, break focusable actions, or create new clipped content |
| Browser rerun -> verification artifact | Manual proof must not overclaim pass status when a route still overflows |
| Screenshot/browser evidence -> persisted docs | Evidence artifacts must not persist copy-once or credential plaintext |

## ASVS Level 1 / High-Block Gate

| Gate | Handling |
|------|----------|
| Baseline | OWASP ASVS 5.0.0 Level 1 |
| Block condition | Block on HIGH information-disclosure, false-verification, or host-boundary issues. |
| Applicable categories | V4 Access Control, V5 Input Validation, V6 Cryptography |
| Executor handling | If any route still returns `true` for page-level overflow at 390px, keep Phase 110 in `status: gaps`; do not mark verification passed. If browser evidence exposes secret plaintext, discard the evidence and recapture with redacted/artificial state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-21 | Tampering | responsive CSS contract | mitigate | add deterministic source assertions for the client-route shrink/wrap rules |
| T-110-22 | Information Disclosure | screenshot and browser evidence | mitigate | record durable handles and route results only; do not persist copy-once or credential plaintext |
| T-110-23 | Repudiation | manual overflow rerun | mitigate | record exact routes, viewport width, JavaScript expression, and pass/fail result in `110-BROWSER-EVIDENCE.md` |
| T-110-24 | Denial of Service | mobile admin usability | mitigate | require no page-level horizontal overflow for the nine client routes at 390px |
| T-110-SC | Tampering | package manager installs | accept | no dependency installation or package-manager task is allowed in this plan |
</threat_model>

<verification>
- `MIX_ENV=test mix compile --warnings-as-errors`
- `git diff --check`
- `mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1`
- `mix test test/lockspire/web/live/admin --max-failures 1`
- `mix test`
- Browser proof at 390px for the nine previously failing client workspace/workflow routes using `document.documentElement.scrollWidth > document.documentElement.clientWidth`
</verification>

<success_criteria>
- The nine client workspace/workflow routes listed in `110-VERIFICATION.md` return `false` for page-level overflow at 390px.
- The embedded admin CSS has explicit shrink/wrap rules for client workspace, form shell, description/value lists, action groups, copy-once panels, and code/value blocks.
- Deterministic design-system contract tests fail if the Phase 110 responsive CSS contract is removed.
- `110-BROWSER-EVIDENCE.md`, `110-SCREENSHOTS.md`, and `110-VERIFICATION.md` report the rerun truth without overclaiming pass status.
</success_criteria>

<output>
Create `.planning/phases/110-demo-state-screenshots-docs-and-regression-proof/110-05-SUMMARY.md` when done.
</output>
