---
phase: 108-design-system-token-component-upgrade
plan: 02
type: execute
wave: 1
depends_on: ["108-01"]
files_modified:
  - lib/lockspire/web/components/admin_components.ex
  - lib/lockspire/web/admin_css.ex
  - test/lockspire/web/live/admin/design_system_contract_test.exs
autonomous: true
requirements:
  - DESIGN-01
  - DESIGN-02
  - DESIGN-04
must_haves:
  truths:
    - "D-06/D-07: Lockspire.Web.Components.AdminComponents expands as the library-owned admin component API using Phoenix function components with explicit attr/slot declarations while preserving all existing component APIs."
    - "D-08/D-09: Shared structural and safety primitives cover page hero, metric grid/card, task/attention card, filter bar, resource row slots, copy-once secret panel, action group, help/result count, empty notice/state, long-value display, status clusters, and confirmation primitives without becoming domain workflow components."
    - "D-10: Components remain library-owned after the host-mounted admin route; no host-editable admin components are generated and the host keeps operator auth, layouts, branding, tenant policy, and product authorization before the admin router."
    - "D-15/D-24/D-25: Row/list/table, long-value, timestamp/mono, badge/status cluster, compact filter, metric/attention, pivot-link, and safe action primitives are added for Phase 109 to consume without treating tables as categorically wrong or making horizontal table scrolling the long-term mobile answer."
  artifacts:
    - path: "lib/lockspire/web/components/admin_components.ex"
      provides: "expanded shared admin component API"
      contains: "def page_hero"
    - path: "lib/lockspire/web/admin_css.ex"
      provides: "CSS classes for new shared primitives"
      contains: ".lockspire-admin-filter-bar"
    - path: "test/lockspire/web/live/admin/design_system_contract_test.exs"
      provides: "component primitive coverage fence"
      contains: "shared component primitives"
  key_links:
    - from: ".planning/phases/108-design-system-token-component-upgrade/108-UI-SPEC.md"
      to: "lib/lockspire/web/components/admin_components.ex"
      via: "Required Component Primitives table"
      pattern: "page_hero|metric_grid|task_card|filter_bar|resource_item|copy_once_secret_panel|action_group|empty_state|long_value"
---

<objective>
Expand the shared Phoenix admin component API around the repeated structural primitives Phase 108 locked, while preserving every existing component API.

Purpose: Give future admin routes reusable, tested primitives before route-specific weak-page polish starts in Phase 109.
Output: Updated `AdminComponents`, supporting admin CSS classes, and component coverage tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/108-design-system-token-component-upgrade/108-CONTEXT.md
@.planning/phases/108-design-system-token-component-upgrade/108-UI-SPEC.md
@.planning/phases/108-design-system-token-component-upgrade/108-RESEARCH.md
@.planning/phases/108-design-system-token-component-upgrade/108-VALIDATION.md
@.planning/phases/108-design-system-token-component-upgrade/108-PATTERNS.md
@lib/lockspire/web/components/admin_components.ex
@lib/lockspire/web/admin_css.ex
@test/lockspire/web/live/admin/design_system_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add structural page, metric, task, and filter primitives</name>
  <files>lib/lockspire/web/components/admin_components.ex, lib/lockspire/web/admin_css.ex</files>
  <read_first>
    - lib/lockspire/web/components/admin_components.ex
    - lib/lockspire/web/admin_css.ex
    - lib/lockspire/web/live/admin/overview_live/index.ex
    - lib/lockspire/web/live/admin/dcr_live/index.ex
    - lib/lockspire/web/live/admin/clients_live/index.ex
    - lib/lockspire/web/live/admin/tokens_live/index.ex
    - lib/lockspire/web/live/admin/consents_live/index.ex
    - .planning/phases/108-design-system-token-component-upgrade/108-UI-SPEC.md
  </read_first>
  <action>
    Add backward-compatible structural components to `AdminComponents`: `page_hero/1`, `metric_grid/1`, `task_card/1`, and `filter_bar/1`. `page_hero/1` must accept `eyebrow`, `title`, optional `body`, optional `class`, and slots for `summary` and `actions`. `metric_grid/1` must accept optional `wide` and `class` attrs and render an inner slot around existing `summary_stat/1`. `task_card/1` must accept `title`, optional `subtitle`, optional `state`, optional `class`, an inner/body slot, and optional `actions`. `filter_bar/1` must be layout-only: accept `action`, optional `method` defaulting to `get`, optional `class`, a required `fields` slot, optional `help` slot, and optional `actions` slot; it must not parse or mutate URL params. Add matching classes in `admin_css.ex`: `lockspire-admin-page-hero` as an alias-compatible extension of the current hero styling, `lockspire-admin-metric-grid`, `lockspire-admin-task-card`, `lockspire-admin-filter-bar`, `lockspire-admin-filter-bar__fields`, `lockspire-admin-filter-bar__help`, and `lockspire-admin-filter-bar__actions`. Keep existing `section_card/1`, `summary_stat/1`, and `action_bar/1` signatures unchanged.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/design_system_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `admin_components.ex` contains `def page_hero`, `def metric_grid`, `def task_card`, and `def filter_bar`.
    - Each new component has at least one `attr` declaration and any flexible content uses `slot`.
    - `filter_bar/1` renders a `<form` with `method={@method}` and `action={@action}` but does not call `push_patch`, `handle_params`, or any route-specific filter helper.
    - `admin_css.ex` defines `.lockspire-admin-filter-bar`, `.lockspire-admin-filter-bar__fields`, and `.lockspire-admin-task-card`.
    - Existing functions `section_card`, `summary_stat`, and `action_bar` are still present.
  </acceptance_criteria>
  <done>Shared structural page, metric, task, and filter primitives exist without taking over route state.</done>
</task>

<task type="auto">
  <name>Task 2: Add safety, row, long-value, and action primitives</name>
  <files>lib/lockspire/web/components/admin_components.ex, lib/lockspire/web/admin_css.ex</files>
  <read_first>
    - lib/lockspire/web/components/admin_components.ex
    - lib/lockspire/web/admin_css.ex
    - lib/lockspire/web/live/admin/clients_live/index.ex
    - lib/lockspire/web/live/admin/clients_live/rotate_secret_component.ex
    - lib/lockspire/web/live/admin/clients_live/show.ex
    - lib/lockspire/web/live/admin/device_authorizations_live/index.ex
    - lib/lockspire/web/live/admin/logout_deliveries_live/index.ex
    - .planning/phases/108-design-system-token-component-upgrade/108-PATTERNS.md
  </read_first>
  <action>
    Add `copy_once_secret_panel/1`, `action_group/1`, `long_value/1`, and any small row/status helper needed to make `resource_item/1` satisfy the Phase 108 row contract without breaking its existing attrs. `copy_once_secret_panel/1` must accept `title`, optional `body`, optional `value`, optional `redacted` defaulting to false, optional `label`, and optional `class`; render the plaintext value only when `value` is present and `redacted` is false, and render redacted text otherwise. `long_value/1` must accept `value`, optional `kind` defaulting to `:text`, optional `redacted` defaulting to false, optional `class`, and render a namespaced mono/wrapping class. `action_group/1` must provide layout slots for primary, secondary, and destructive actions and render destructive actions in a visually separated namespaced container. Improve `resource_item/1` only by adding optional slots/classes for status clusters/actions/long wrapping; do not remove `href`, `title`, `subtitle`, `meta`, or `actions`.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/design_system_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `admin_components.ex` contains `def copy_once_secret_panel`, `def action_group`, and `def long_value`.
    - `copy_once_secret_panel/1` does not render `@value` when `@redacted` is true.
    - `long_value/1` renders a class containing `lockspire-admin-long-value`.
    - `action_group/1` has separate slots or containers for destructive actions and includes `lockspire-admin-action-group__destructive`.
    - `resource_item/1` still accepts `href`, `title`, `subtitle`, `meta`, and `actions`.
    - `admin_css.ex` defines `.lockspire-admin-copy-once-secret`, `.lockspire-admin-long-value`, `.lockspire-admin-status-cluster`, `.lockspire-admin-action-group`, and `.lockspire-admin-action-group__destructive`.
  </acceptance_criteria>
  <done>Secret, long-value, row/status, and action grouping primitives exist with safety-first structure.</done>
</task>

<task type="auto">
  <name>Task 3: Fence shared component primitive coverage</name>
  <files>test/lockspire/web/live/admin/design_system_contract_test.exs</files>
  <read_first>
    - test/lockspire/web/live/admin/design_system_contract_test.exs
    - lib/lockspire/web/components/admin_components.ex
    - lib/lockspire/web/admin_css.ex
    - .planning/phases/108-design-system-token-component-upgrade/108-UI-SPEC.md
  </read_first>
  <action>
    Extend `design_system_contract_test.exs` with deterministic component primitive coverage. Read `admin_components.ex` and assert it contains function definitions for `page_hero`, `metric_grid`, `task_card`, `filter_bar`, `copy_once_secret_panel`, `action_group`, `long_value`, `empty_state`, `confirmation_panel`, `resource_item`, and `status_badge`. Assert the component source contains `attr(` and `slot(` declarations near the new primitives. Read `admin_css.ex` and assert primitive classes exist for hero/page hero, metric grid/stat, task card, filter bar, resource item/row, empty state, confirmation panel, copy-once secret panel, long value, status cluster/badge group, and action group. Keep this source-level unless a small rendered assertion is already easy to add without full snapshots.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/design_system_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `design_system_contract_test.exs` contains a test whose name includes `shared component primitives`.
    - The test asserts function coverage for the required primitive list.
    - The test asserts CSS class coverage for `lockspire-admin-filter-bar`, `lockspire-admin-copy-once-secret`, `lockspire-admin-long-value`, and `lockspire-admin-action-group`.
    - `mix test test/lockspire/web/live/admin/design_system_contract_test.exs` exits 0.
  </acceptance_criteria>
  <done>Shared primitive coverage is fenced by the deterministic admin design-system contract test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LiveView route state -> shared components | Route-owned params and workflow intent pass through structural slots without being hidden in generic components |
| copy-once values -> rendered HTML | Secret/plaintext material must render only in the intended reveal panel state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-11 | Information Disclosure | `copy_once_secret_panel/1` | mitigate | render plaintext only when explicitly passed and not redacted; centralize copy-once structure so pages stop hand-rolling reveal markup |
| T-108-12 | Tampering | shared component API | mitigate | preserve existing component function signatures and add contract tests for required primitive coverage |
| T-108-13 | Elevation of Privilege | generic filter/action components | mitigate | keep filters layout-only and actions slot-based; LiveViews remain responsible for URL state, events, and authorization boundary before mount |
| T-108-SC | Tampering | package manager installs | accept | no external UI libraries or package manager changes are allowed |
</threat_model>

<verification>
- `mix test test/lockspire/web/live/admin/design_system_contract_test.exs`
- `MIX_ENV=test mix compile --warnings-as-errors`
</verification>

<success_criteria>
- Required shared admin component primitives exist and are backward-compatible
- CSS supports the primitive classes with namespaced selectors
- Contract tests fence component and class coverage
</success_criteria>

<output>
Create `.planning/phases/108-design-system-token-component-upgrade/108-02-SUMMARY.md` when done.
</output>
