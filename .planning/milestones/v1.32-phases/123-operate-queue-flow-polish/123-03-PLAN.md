---
phase: 123-operate-queue-flow-polish
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/web/live/admin/logout_deliveries_live/index.ex
  - test/lockspire/web/live/admin/logout_deliveries_live_test.exs
autonomous: true
requirements:
  - OPERATE-01
  - OPERATE-02
  - OPERATE-03
requirements_addressed:
  - OPERATE-01
  - OPERATE-02
  - OPERATE-03
user_setup: []
must_haves:
  truths:
    - "OPERATE-01 D-04 D-09: /admin/logouts exposes delivery id, redacted client, channel, endpoint, attempts, status pressure, last activity, optional concise failure context, and support note."
    - "OPERATE-02 D-01 D-03 D-17: logout deliveries remain read-only support truth with no worker, retry, discard, requeue, or logout-now controls."
    - "OPERATE-03 D-05 D-12 D-14 D-15 D-18 D-24: retryable, discarded, skipped, rendered, completed, long URL, dense, mobile-safe, focus/theme/reduced-motion proof exists."
  artifacts:
    - path: "lib/lockspire/web/live/admin/logout_deliveries_live/index.ex"
      provides: "Strongest Operate pressure/support-note pattern, extended for terminal/completed/incident clarity."
      contains: "delivery_support_note"
    - path: "test/lockspire/web/live/admin/logout_deliveries_live_test.exs"
      provides: "Rendered proof for logout delivery states, endpoint wrapping, worker/internal secrecy, no tables, and unsupported-control absence."
      contains: "Retryable delivery failure"
  key_links:
    - from: "lib/lockspire/web/live/admin/logout_deliveries_live/index.ex"
      to: "Lockspire.Storage.Ecto.Repository.list_all_logout_deliveries/0"
      via: "existing mount read path"
      pattern: "Repository\\.list_all_logout_deliveries"
    - from: "lib/lockspire/web/live/admin/logout_deliveries_live/index.ex"
      to: "Lockspire.Web.Components.AdminComponents"
      via: "resource_list, dense_resource_row, status_badge, long_value, support note class"
      pattern: "AdminComponents\\.(dense_resource_row|long_value|status_badge)"
---

<objective>
Finish the logout propagation queue as the reference Operate scan pattern for status pressure, endpoint/attempt context, terminal/completed state clarity, and support-note truth.

Purpose: Deliver OPERATE-01, OPERATE-02, and OPERATE-03 for `/admin/logouts` while preserving read-only support truth and excluding worker/internal leakage per D-01, D-03, D-04, D-05, D-07, D-08, D-09, D-12, D-13, D-16, D-17, D-18, D-21, D-22, and D-24.
Output: Focused logout delivery LiveView tests and a route-local logout queue implementation that remains the model for the other Operate pages.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-operate-queue-flow-polish/123-CONTEXT.md
@.planning/phases/123-operate-queue-flow-polish/123-RESEARCH.md
@.planning/phases/123-operate-queue-flow-polish/123-PATTERNS.md
@.planning/phases/123-operate-queue-flow-polish/123-UI-SPEC.md
@.planning/phases/123-operate-queue-flow-polish/123-VALIDATION.md
@.planning/phases/122-support-investigation-flow-polish/122-01-SUMMARY.md
@lib/lockspire/web/admin_router.ex
@lib/lockspire/web/live/admin/logout_deliveries_live/index.ex
@lib/lockspire/domain/logout_delivery.ex
@lib/lockspire/web/components/admin_components.ex
@lib/lockspire/web/admin_css.ex
@test/lockspire/web/live/admin/logout_deliveries_live_test.exs
@test/support/lockspire/web/admin_proof/html_assertions.ex
</context>

## Artifacts this phase produces

- Extended `delivery_pressure/1`, `delivery_support_note/1`, `delivery_timestamp/1`, and optional concise failure-context private helper inside `lib/lockspire/web/live/admin/logout_deliveries_live/index.ex`.
- Expanded rendered logout delivery tests in `test/lockspire/web/live/admin/logout_deliveries_live_test.exs`.
- No new route, no new component module, no public Admin API, no schema file, no migration file, no package dependency, and no browser tooling artifact.

<tasks>

<task id="123-03-01" type="auto" tdd="true">
  <name>Task 123-03-01: Add logout delivery terminal, retry, and leakage tests</name>
  <files>test/lockspire/web/live/admin/logout_deliveries_live_test.exs</files>
  <read_first>
    - .planning/phases/123-operate-queue-flow-polish/123-CONTEXT.md
    - .planning/phases/123-operate-queue-flow-polish/123-UI-SPEC.md
    - .planning/phases/123-operate-queue-flow-polish/123-VALIDATION.md
    - .planning/phases/123-operate-queue-flow-polish/123-PATTERNS.md
    - lib/lockspire/web/live/admin/logout_deliveries_live/index.ex
    - lib/lockspire/domain/logout_delivery.ex
    - test/lockspire/web/live/admin/logout_deliveries_live_test.exs
    - test/support/lockspire/web/admin_proof/html_assertions.ex
  </read_first>
  <behavior>
    - Rendered rows cover pending/enqueued, attempted, retryable, discarded, skipped, rendered, and succeeded/completed delivery outcomes.
    - Rows expose delivery id, redacted client, channel, endpoint URL, attempts, last activity, status pressure, and support note.
    - Retryable/incident state exposes concise endpoint/support context without raw response body, cookie, endpoint secret, Oban job id, SQL row, or worker-internal leakage.
    - Long endpoint URL wraps through `long_value`; rendered HTML has no table surface and no interactive queue command events.
  </behavior>
  <action>
    Expand `logout_deliveries_live_test.exs` before production edits. Replace or extend the SQL fixture loop with deterministic artificial delivery rows for pending, attempted, retryable, discarded, skipped, rendered, and succeeded statuses. Include one long safe endpoint URL, one retryable delivery with HTTP status or concise failure context if implemented, and fixture values for logout token JTI, Oban job ID, and raw response/failure-like text that must remain absent from rendered HTML. Extend rendered assertions for status metrics, route-specific titles, pressure copy, endpoint, attempts, last activity, support note, `lockspire-admin-dense-resource-row`, `lockspire-admin-long-value`, no table surface, no interactive event attributes, no generic CTA text, no duplicate IDs, and no unresolved described-by targets. Use the UI-SPEC unsupported-control contract for no-command assertions per D-03, D-09, D-17, and D-18.
  </action>
  <acceptance_criteria>
    - `test/lockspire/web/live/admin/logout_deliveries_live_test.exs` includes fixture rows for `pending`, `attempted`, `retryable`, `discarded`, `skipped`, `rendered`, and `succeeded`.
    - The rendered test asserts `Waiting`, `Retrying`, `Failed`, `Discarded`, `Completed`, `Delivery`, `Client`, `Channel`, `Endpoint`, `Attempts`, `Last activity`, and `Support note`.
    - The rendered test asserts `lockspire-admin-dense-resource-row`, `lockspire-admin-long-value`, and absence of `<table`, `lockspire-admin-table-wrap`, `phx-click`, and `phx-submit`.
    - The rendered test denies `logout_token_jti`, `oban_job_id`, `raw response`, `cookie`, `endpoint secret`, `SQL row`, and `worker internals`.
    - Running the focused command after this task fails only on missing planned logout delivery terminal/retry/rendering behavior.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/web/live/admin/logout_deliveries_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Logout delivery RED coverage exists for retryable, discarded, skipped, rendered, completed, long-url, worker-leakage, and no-command states.</done>
</task>

<task id="123-03-02" type="auto" tdd="true">
  <name>Task 123-03-02: Implement complete logout delivery support truth</name>
  <files>lib/lockspire/web/live/admin/logout_deliveries_live/index.ex</files>
  <read_first>
    - .planning/phases/123-operate-queue-flow-polish/123-CONTEXT.md
    - .planning/phases/123-operate-queue-flow-polish/123-UI-SPEC.md
    - .planning/phases/123-operate-queue-flow-polish/123-PATTERNS.md
    - lib/lockspire/web/live/admin/logout_deliveries_live/index.ex
    - lib/lockspire/domain/logout_delivery.ex
    - lib/lockspire/web/components/admin_components.ex
    - lib/lockspire/web/admin_css.ex
    - test/lockspire/web/live/admin/logout_deliveries_live_test.exs
  </read_first>
  <behavior>
    - The page spine remains `page_hero`, `pane`, `metric_grid`, `resource_list`, `dense_resource_row`, `status_badge`, `long_value`, and `empty_state`.
    - Row subtitles and support notes distinguish waiting, attempted, retryable, discarded, skipped, rendered, and succeeded/completed support truth.
    - Optional HTTP status or concise failure class appears only when it clarifies retryable/incident state and does not expose raw response content.
    - The LiveView has no `handle_event/3`, no worker command controls, and no storage/schema/protocol changes.
  </behavior>
  <action>
    Update `logout_deliveries_live/index.ex` to keep the current logout delivery page as the strongest Operate pattern. Extend private pressure/support-note helpers so retryable, discarded, skipped, rendered, succeeded, and attempted states each produce calm support-oriented copy. If adding failure context, render only concise HTTP status or sanitized failure class from D-09 allowed data; never render raw response content, cookies, endpoint secrets, worker internals, SQL details, logout token JTI, or Oban job IDs. Keep delivery id, redacted client, channel, endpoint URL, attempts, and last activity in `dense_resource_row` meta through `long_value`. Keep `Repository.list_all_logout_deliveries()` as the read path. Do not add route, API, event handler, queue mutation, storage schema, CSS, shared component, public support-surface, or host-seam changes per D-01, D-02, D-03, D-05, D-06, D-09, D-12, D-16, and D-23.
  </action>
  <acceptance_criteria>
    - `lib/lockspire/web/live/admin/logout_deliveries_live/index.ex` still calls `Repository.list_all_logout_deliveries`.
    - The source contains private helper names or equivalent logic for delivery pressure, delivery support notes, delivery timestamp selection, and optional sanitized failure context.
    - The source contains `AdminComponents.dense_resource_row`, `AdminComponents.status_badge`, `AdminComponents.long_value`, and `lockspire-admin-dense-resource-row__note`.
    - The source does not contain `def handle_event`, `phx-click`, `phx-submit`, `responsive_table`, `lockspire-admin-table-wrap`, raw response rendering, cookie rendering, endpoint secret rendering, worker-control rendering, logout token JTI rendering, or Oban job ID rendering.
    - Focused logout delivery tests pass.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/web/live/admin/logout_deliveries_live_test.exs --max-failures 1</automated>
    <automated>mix format --check-formatted lib/lockspire/web/live/admin/logout_deliveries_live/index.ex test/lockspire/web/live/admin/logout_deliveries_live_test.exs</automated>
  </verify>
  <done>`/admin/logouts` renders complete pressure-first, redaction-safe, non-table logout delivery rows and remains read-only support truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host app -> AdminRouter | Host-owned operator authentication and tenant policy remain outside Lockspire. |
| LiveView -> Repository | Logout delivery data is read from existing storage paths and shaped for presentation inside the LiveView. |
| Logout delivery struct -> rendered HTML | Delivery structs include support-safe fields adjacent to logout token, worker, and failure internals. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Information Disclosure | `LogoutDeliveriesLive.Index` rows | mitigate | Render only D-09 allowed fields, redacted handles, and `long_value`; add rendered forbidden-text assertions. |
| T-123-02 | Elevation of Privilege | `/admin/logouts` controls | mitigate | Keep page read-only: no `handle_event/3`, LiveView command attributes, mutation labels, new routes, or new Admin delegates. |
| T-123-03 | Information Disclosure | worker/failure context | mitigate | Show endpoint/channel/attempt/status pressure and calm support notes instead of raw worker, SQL, Oban, cookie, or response-body internals. |
| T-123-04 | Denial of Service | Long endpoint URLs | mitigate | Use `long_value`, existing wrapping CSS, and rendered no-table/no-overflow proxies. |
| T-123-SC | Tampering | package installs | mitigate | No package-manager install tasks; RESEARCH.md package audit says no external packages are needed. |
</threat_model>

<verification>
Focused:
- `MIX_ENV=test mix test test/lockspire/web/live/admin/logout_deliveries_live_test.exs --max-failures 1`
- `mix format --check-formatted lib/lockspire/web/live/admin/logout_deliveries_live/index.ex test/lockspire/web/live/admin/logout_deliveries_live_test.exs`

Plan handoff:
- This plan modifies no schema-relevant files (`priv/repo/migrations/*.exs`, Ecto schemas, storage records), so no schema push or migration task is required.
</verification>

<success_criteria>
`/admin/logouts` lets an operator scan logout delivery pressure, channel, endpoint, attempts, last activity, terminal/completed states, retryable incidents, and support notes without table overload, unsupported worker controls, raw backend internals, or host-boundary expansion.
</success_criteria>

<output>
Create `.planning/phases/123-operate-queue-flow-polish/123-03-SUMMARY.md` when done.
</output>
