---
phase: 135-cohesive-internals
plan: 02
type: execute
wave: 2
depends_on: [135-01]
files_modified:
  - lib/lockspire/storage/ecto/repository.ex
  - lib/lockspire/storage/ecto/repository/support.ex
  - lib/lockspire/storage/ecto/repository/transaction_store.ex
  - lib/lockspire/storage/ecto/repository/audit_store.ex
  - lib/lockspire/storage/ecto/repository/client_store.ex
  - lib/lockspire/storage/ecto/repository/server_policy_store.ex
  - test/lockspire/storage/repository_test.exs
  - test/lockspire/storage/repository_atomicity_test.exs
autonomous: true
requirements: [COH-01, COH-02]
must_haves:
  truths:
    - "Repository keeps all current public behaviors/arities while client, policy, transaction, and audit Ecto ownership is navigable by aggregate."
    - "DCR replacement and RAT rotation retain one lock/write/audit rollback domain."
  artifacts:
    - path: lib/lockspire/storage/ecto/repository/client_store.ex
      provides: complete client record/query/lifecycle Ecto ownership
    - path: lib/lockspire/storage/ecto/repository/support.ex
      provides: one controlled Ecto repo/query option boundary
  key_links:
    - from: lib/lockspire/storage/ecto/repository.ex
      to: lib/lockspire/storage/ecto/repository/client_store.ex
      via: compatible facade delegation with explicit repo
      pattern: "Repository.ClientStore"
---

<objective>
Establish the aggregate-delegate pattern and extract the audited client lifecycle intact.

Purpose: Make the highest-risk audited aggregate the production-quality storage tracer for every later extraction.
Output: Shared Ecto support plus client, policy, transaction, and audit collaborators behind Repository.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-cohesive-internals/135-01-SUMMARY.md
@.planning/phases/135-cohesive-internals/PATTERNS.md
@lib/lockspire/storage/ecto/repository.ex
@test/lockspire/storage/repository_atomicity_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract explicit Ecto support, transaction, and audit ownership</name>
  <files>lib/lockspire/storage/ecto/repository.ex, lib/lockspire/storage/ecto/repository/support.ex, lib/lockspire/storage/ecto/repository/transaction_store.ex, lib/lockspire/storage/ecto/repository/audit_store.ex, test/lockspire/storage/repository_test.exs</files>
  <behavior>
    - `Repository.transact/1`, `rollback/1`, `append_audit_event/1`, and `transact_with_audit/2` retain exact results and rollback both work and audit failures.
    - Sensitive Ecto calls retain the existing prefix/logging options through one support owner.
  </behavior>
  <action>Move configured-repo access and low-level all/one/insert/update/delete/options helpers into `Repository.Support`, accepting the concrete repo explicitly and preserving prefix plus sensitive-query logging behavior. Move transaction and audit composition as complete operations; the facade continues declaring all behaviors and delegates with the configured repo. Do not expose collaborators as a new advertised API and do not let them resolve application configuration independently.</action>
  <verify><automated>mix test test/lockspire/storage/repository_test.exs</automated></verify>
  <done>The common Ecto boundary and audit rollback semantics are centralized without changing Repository callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract clients and server policy with locks and audit intact</name>
  <files>lib/lockspire/storage/ecto/repository.ex, lib/lockspire/storage/ecto/repository/client_store.ex, lib/lockspire/storage/ecto/repository/server_policy_store.ex, test/lockspire/storage/repository_test.exs, test/lockspire/storage/repository_atomicity_test.exs</files>
  <behavior>
    - Client CRUD, secret/status changes, DCR replacement, and RAT rotation retain current domain/error results.
    - Replacement and RAT rotation lock the client and append audit inside the same transaction.
    - Server-policy reads and locked updates retain defaults and concurrent-update behavior.
  </behavior>
  <action>Move complete client and server-policy clusters, including filters, record mapping, locked queries, changesets, audit rollback helpers, and lifecycle error mapping, into their aggregate owners. Preserve each public facade arity and have the facade supply one repo/support dependency. Keep `ClientLifecycle` calling Repository rather than new internal modules. Re-run the Plan 01 rollback characterization after every move.</action>
  <verify><automated>mix test test/lockspire/storage/repository_test.exs test/lockspire/storage/repository_atomicity_test.exs test/lockspire/client_lifecycle_test.exs</automated></verify>
  <done>Client and policy behavior is aggregate-owned and audited mutations remain one atomic operation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Facade → Ecto collaborators | Delegation must not weaken configured storage or transaction ownership. |
| DCR mutation → audit row | Registration changes require attributable atomic audit. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-135-04 | Repudiation/Tampering | client lifecycle | high | mitigate | One locked collaborator operation plus audit-failure rollback proof. |
| T-135-05 | Information disclosure | Ecto query options | high | mitigate | Single support owner preserving sensitive logging suppression. |
</threat_model>

<verification>`mix test test/lockspire/storage/repository_test.exs test/lockspire/storage/repository_atomicity_test.exs test/lockspire/client_lifecycle_test.exs`</verification>
<success_criteria>Repository remains compatible and audited client/policy Ecto behavior has one obvious aggregate owner.</success_criteria>
<output>Create `.planning/phases/135-cohesive-internals/135-02-SUMMARY.md` when done.</output>
