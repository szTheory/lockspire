---
phase: 135-cohesive-internals
plan: 06
type: execute
wave: 2
depends_on: [135-01]
files_modified:
  - lib/lockspire/protocol/token_exchange.ex
  - lib/lockspire/protocol/token_exchange/internal/dependencies.ex
  - lib/lockspire/protocol/token_exchange/internal/legacy_options.ex
  - lib/lockspire/protocol/token_exchange/internal/authorization_code_grant.ex
  - lib/lockspire/protocol/token_exchange/internal/device_code_grant.ex
  - lib/lockspire/protocol/token_exchange/internal/ciba_grant.ex
  - lib/lockspire/protocol/token_exchange/internal/refresh_exchange.ex
  - lib/lockspire/protocol/token_exchange/internal/rfc8693_exchange.ex
  - test/lockspire/protocol/token_exchange/dependencies_test.exs
  - test/lockspire/protocol/token_exchange_test.exs
autonomous: true
requirements: [COH-03, COH-04, COH-05]
must_haves:
  truths:
    - "Token internals receive a validated typed dependency bundle built once at a stable facade/coordinator boundary."
    - "Every currently accepted request option injection key maps exactly to the new bundle with the same production default."
    - "Public TokenExchange and lower compatibility facade arities/results remain unchanged."
  artifacts:
    - path: lib/lockspire/protocol/token_exchange/internal/dependencies.ex
      provides: typed explicit token runtime dependencies
    - path: lib/lockspire/protocol/token_exchange/internal/legacy_options.ex
      provides: exhaustive compatibility mapping from existing request opts
  key_links:
    - from: lib/lockspire/protocol/token_exchange.ex
      to: lib/lockspire/protocol/token_exchange/internal/dependencies.ex
      via: one normalization before neutral grant dispatch
      pattern: "Dependencies"
---

<objective>
Introduce explicit token dependencies without changing any public request or result contract.

Purpose: Replace scattered option reads and runtime capability negotiation with construction-time dependencies while preserving existing injection.
Output: Private dependency struct, exhaustive legacy adapter, and dependency-aware grant coordinator entry points.
</objective>

<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/135-cohesive-internals/135-01-SUMMARY.md
@.planning/phases/134-architecture-topology/134-10-SUMMARY.md
@lib/lockspire/protocol/token_exchange.ex
@test/support/token_exchange_case.ex
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define and exhaustively characterize the private dependency bundle</name>
  <files>lib/lockspire/protocol/token_exchange/internal/dependencies.ex, lib/lockspire/protocol/token_exchange/internal/legacy_options.ex, test/lockspire/protocol/token_exchange/dependencies_test.exs</files>
  <behavior>
    - The bundle names client/token/device/CIBA/interaction/key/server-policy/DPoP-replay/audit/transaction dependencies, clock, generators, signer/config, telemetry, sender-constraint inputs, and token-exchange validator used by current internals.
    - Every option key read anywhere under token-exchange internals maps to the same injected value or established production default.
    - Required transactional/audit capabilities validate before mutation and produce a deterministic internal safe error rather than selecting a degraded branch.
  </behavior>
  <action>Inventory all request-option reads across Internal.GrantSupport, AccessTokenSigner, TokenEndpointDPoP, RefreshExchange, and Rfc8693Exchange, including nested auth/DPoP/token formatting inputs. Define a private struct and `LegacyOptions.from_request/1` mapping table with explicit defaults to Repository/current services. Preserve functions, MFA/generator values, nil semantics, and injected plain-method test stores. Validate capability groups needed by each grant at construction; compatibility failures must use existing TokenResult error categories and occur before durable work.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/dependencies_test.exs</automated></verify>
  <done>The compatibility matrix proves every existing injection seam and deterministic dependency validation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Build dependencies once and thread them through every neutral grant coordinator</name>
  <files>lib/lockspire/protocol/token_exchange.ex, lib/lockspire/protocol/token_exchange/internal/authorization_code_grant.ex, lib/lockspire/protocol/token_exchange/internal/device_code_grant.ex, lib/lockspire/protocol/token_exchange/internal/ciba_grant.ex, lib/lockspire/protocol/token_exchange/internal/refresh_exchange.ex, lib/lockspire/protocol/token_exchange/internal/rfc8693_exchange.ex, test/lockspire/protocol/token_exchange_test.exs</files>
  <behavior>
    - Stable public dispatch constructs dependencies once and internal coordinators accept the bundle explicitly.
    - Authorization code, device, CIBA, refresh, and RFC 8693 retain neutral TokenResult and public conversion behavior.
    - Retained lower public facades continue accepting current requests and adapt them through the same compatibility boundary.
  </behavior>
  <action>Update internal coordinator signatures to accept `%Dependencies{}` and pass it toward shared operations. Keep `TokenExchange` as the sole public result converter established in Phase 134. For separately callable lower compatibility facades, normalize legacy request options at their entry before calling neutral implementations; do not introduce an internal edge back to TokenExchange or Compatibility. At this stage collaborators may still be broad, but they must receive the explicit bundle rather than discover stores from request opts.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange_test.exs test/lockspire/protocol/token_exchange/delegation_test.exs test/lockspire/protocol/token_exchange/characterization_test.exs &amp;&amp; mix xref graph --format cycles</automated></verify>
  <done>All five dispatch paths carry one explicit bundle and every public result boundary remains compatible and cycle free.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Legacy request opts → internal runtime | Host/test injection controls security-critical stores and clocks. |
| Internal result → public facade | OAuth response modules and fields are compatibility/security contracts. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-135-15 | Spoofing | dependency fallback | critical | mitigate | Exhaustive mapping matrix and eager capability validation. |
| T-135-16 | Tampering | transaction/audit dependency | high | mitigate | Required bundle groups fail before mutation. |
| T-135-17 | Information disclosure | public errors | high | mitigate | Reuse neutral safe errors and five-flow characterization. |
</threat_model>
<verification>`mix test test/lockspire/protocol/token_exchange/dependencies_test.exs test/lockspire/protocol/token_exchange_test.exs test/lockspire/protocol/token_exchange/characterization_test.exs &amp;&amp; mix xref graph --format cycles`</verification>
<success_criteria>Explicit token dependencies replace scattered discovery at the dispatch boundary without public or injection drift.</success_criteria>
<output>Create `.planning/phases/135-cohesive-internals/135-06-SUMMARY.md` when done.</output>
