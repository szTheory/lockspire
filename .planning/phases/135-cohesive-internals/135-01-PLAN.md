---
phase: 135-cohesive-internals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/token_exchange_characterization.ex
  - test/lockspire/protocol/token_exchange/characterization_test.exs
  - test/lockspire/storage/repository_atomicity_test.exs
autonomous: true
requirements: [COH-02, COH-05]
must_haves:
  truths:
    - "All five supported token grants have one stable-facade characterization covering public result, durable token state, audit, and telemetry."
    - "Authorization-code redemption, refresh reuse, DCR mutation plus audit, and signing-key transitions have DB-backed rollback or concurrency proof before implementation moves."
  artifacts:
    - path: test/lockspire/protocol/token_exchange/characterization_test.exs
      provides: five-flow observable compatibility spine
    - path: test/lockspire/storage/repository_atomicity_test.exs
      provides: transaction and row-lock characterization
  key_links:
    - from: test/lockspire/protocol/token_exchange/characterization_test.exs
      to: lib/lockspire/protocol/token_exchange.ex
      via: stable public exchange entry points
      pattern: "TokenExchange"
---

<objective>
Freeze the externally observable grant and storage transaction contracts before structural movement.

Purpose: Give every extraction a behavioral oracle that catches wire, persistence, audit, telemetry, rollback, and concurrency drift.
Output: A reusable five-flow characterization harness and DB-backed atomicity tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/135-cohesive-internals/135-ASSUMPTIONS.md
@.planning/phases/135-cohesive-internals/135-RESEARCH.md
@test/support/token_exchange_case.ex
@test/lockspire/storage/repository_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Characterize one authorization-code journey across every observable seam</name>
  <files>test/support/token_exchange_characterization.ex, test/lockspire/protocol/token_exchange/characterization_test.exs</files>
  <behavior>
    - A real authorization-code plus S256 PKCE request through `TokenExchange.exchange/1` returns the retained public success struct and persists the access/refresh outcome.
    - Replaying the code returns the retained OAuth error/reason and emits the existing replay audit and telemetry metadata without secret or raw-token values.
  </behavior>
  <action>Create a production-contract test helper that observes, but does not reimplement, real token results, persisted records, audit records, and telemetry. Wire the authorization-code success and replay path end to end through the stable facade. Reuse `TokenExchangeCase` fixture conventions and real Ecto storage; normalize only nondeterministic IDs/times in assertions. This tracer is the permanent skeleton for the other four grant cases.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/characterization_test.exs --trace</automated></verify>
  <done>The code flow proves public response, tokens, audit, telemetry, and replay behavior through one executable seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand characterization to four grants and critical storage boundaries</name>
  <files>test/support/token_exchange_characterization.ex, test/lockspire/protocol/token_exchange/characterization_test.exs, test/lockspire/storage/repository_atomicity_test.exs</files>
  <behavior>
    - Refresh success and reuse prove rotation plus family-wide revocation and matching audit/telemetry.
    - Device and CIBA cover success plus pending/slow-down or terminal failure classifications.
    - RFC 8693 covers allowed exchange and policy denial with retained token/result fields.
    - Two authorization-code contenders yield exactly one redemption; injected audit failure rolls back DCR replacement/RAT mutation; signing-key lifecycle rollback and serialization remain intact.
  </behavior>
  <action>Complete the five-flow table through existing public TokenExchange entry points. Add PostgreSQL-backed transaction tests using the established SQL Sandbox shared-process pattern: concurrent code contenders must exercise the real `FOR UPDATE` path, DCR replacement/RAT audit failure must leave both client metadata and token hash unchanged, refresh reuse must revoke its family atomically, and key activation/retirement must retain guided state transitions. Assert safe public errors and durable state rather than private call order.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/characterization_test.exs test/lockspire/storage/repository_atomicity_test.exs</automated></verify>
  <done>All five grant contracts and all COH-02 atomicity boundaries are pinned before refactoring.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| OAuth request → token facade | Untrusted grant material can authorize token issuance. |
| Concurrent caller → PostgreSQL state | Single-use and lifecycle transitions depend on locks and rollback. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-135-01 | Tampering/Elevation of privilege | code and refresh redemption | critical | mitigate | DB-backed single-winner and family-revocation characterization. |
| T-135-02 | Repudiation | DCR/token audit | high | mitigate | Persisted audit assertions plus injected failure rollback. |
| T-135-03 | Information disclosure | results/telemetry | high | mitigate | Explicit redaction assertions over captured observations. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/token_exchange/characterization_test.exs test/lockspire/storage/repository_atomicity_test.exs`
</verification>

<success_criteria>
- Five grant paths are characterized at the stable facade.
- Critical storage transaction/lock behavior is DB-proven before extraction.
</success_criteria>

<output>Create `.planning/phases/135-cohesive-internals/135-01-SUMMARY.md` when done.</output>
