---
phase: 135-cohesive-internals
plan: 08
type: execute
wave: 4
depends_on: [135-07]
files_modified:
  - lib/lockspire/protocol/token_exchange/internal/token_issuer.ex
  - lib/lockspire/protocol/token_exchange/internal/grant_persistence.ex
  - lib/lockspire/protocol/token_exchange/internal/grant_observability.ex
  - lib/lockspire/protocol/token_exchange/internal/grant_support.ex
  - lib/lockspire/protocol/token_exchange/internal/access_token_signer.ex
  - lib/lockspire/protocol/token_exchange/internal/authorization_code_grant.ex
  - lib/lockspire/protocol/token_exchange/internal/device_code_grant.ex
  - lib/lockspire/protocol/token_exchange/internal/ciba_grant.ex
  - lib/lockspire/protocol/token_exchange/internal/refresh_exchange.ex
  - lib/lockspire/protocol/token_exchange/internal/rfc8693_exchange.ex
  - test/lockspire/protocol/token_exchange/issuance_test.exs
  - test/lockspire/protocol/token_exchange/persistence_test.exs
  - test/lockspire/protocol/token_exchange/observability_test.exs
autonomous: true
requirements: [COH-02, COH-03, COH-04, COH-05]
must_haves:
  truths:
    - "Token construction/signing, atomic persistence, and audit/telemetry emission each have one focused internal owner."
    - "All five grant coordinators compose focused collaborators and return unchanged neutral/public results."
    - "No token collaborator reads legacy request options or negotiates behavior from module exports at runtime."
  artifacts:
    - path: lib/lockspire/protocol/token_exchange/internal/token_issuer.ex
      provides: access/refresh/ID token construction and signing coordination
    - path: lib/lockspire/protocol/token_exchange/internal/grant_persistence.ex
      provides: explicit atomic storage operations and audit intent
    - path: lib/lockspire/protocol/token_exchange/internal/grant_observability.ex
      provides: established audit and telemetry success/failure contracts
  key_links:
    - from: lib/lockspire/protocol/token_exchange/internal/authorization_code_grant.ex
      to: lib/lockspire/protocol/token_exchange/internal/grant_persistence.ex
      via: one atomic redemption intent using explicit dependencies
      pattern: "GrantPersistence"
---

<objective>
Split issuance, persistence, and observability, then compose every grant through the focused collaborator set.

Purpose: Replace the broad GrantSupport implementation without moving public conversion or weakening durable transitions.
Output: Three focused collaborators, thin grant coordinators, and a substantially reduced GrantSupport compatibility surface.
</objective>

<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>
@.planning/phases/135-cohesive-internals/135-07-SUMMARY.md
@lib/lockspire/protocol/token_exchange/internal/grant_support.ex
@lib/lockspire/protocol/token_exchange/internal/access_token_signer.ex
@test/lockspire/protocol/token_exchange/characterization_test.exs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extract explicit token issuance and signing</name>
  <files>lib/lockspire/protocol/token_exchange/internal/token_issuer.ex, lib/lockspire/protocol/token_exchange/internal/access_token_signer.ex, lib/lockspire/protocol/token_exchange/internal/grant_support.ex, test/lockspire/protocol/token_exchange/issuance_test.exs</files>
  <behavior>
    - Access, refresh, and ID token values retain format, lifetime, scopes, audience, cnf, nonce/auth_time, and public response fields.
    - Opaque/JWT format, signing key, server policy, generators, and clock come only from explicit dependencies.
    - Issuance constructs values but performs no durable mutation or telemetry emission.
  </behavior>
  <action>Move token domain construction, format options, optional refresh decisions, ID-token claims, signer invocation, and success payload assembly into `TokenIssuer`. Update AccessTokenSigner to take the explicit key/policy/config dependency subset instead of inspecting requests or module exports. Keep key selection and cryptographic error mapping identical; retain the public AccessTokenSigner compatibility facade established in Phase 134.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/issuance_test.exs test/lockspire/protocol/access_token_signer_test.exs test/lockspire/protocol/id_token_test.exs</automated></verify>
  <done>Issuance/signing is explicit, mutation-free, and preserves exact token contents/results.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Extract atomic grant persistence and observability</name>
  <files>lib/lockspire/protocol/token_exchange/internal/grant_persistence.ex, lib/lockspire/protocol/token_exchange/internal/grant_observability.ex, lib/lockspire/protocol/token_exchange/internal/grant_support.ex, lib/lockspire/protocol/token_exchange/internal/refresh_exchange.ex, test/lockspire/protocol/token_exchange/persistence_test.exs, test/lockspire/protocol/token_exchange/observability_test.exs</files>
  <behavior>
    - Code/device/CIBA redemption and refresh rotation call explicit complete storage operations with audit in the same rollback domain.
    - Success, replay/reuse, invalid-resource, pending/slow-down, and exchange-denial audit/telemetry names and metadata remain exact and redacted.
    - Missing required capabilities fail before mutation rather than falling through to split writes.
  </behavior>
  <action>Move audited persistence intents and error normalization into `GrantPersistence`; call required bundle capabilities directly and keep aggregate transaction operations complete. Move audit-event construction plus telemetry success/failure emission into `GrantObservability`, accepting explicit audit/telemetry dependencies and safe domain metadata. Remove duplicated refresh transaction/audit negotiation and route it through these owners without changing the TokenStore atomic rotation operation.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/persistence_test.exs test/lockspire/protocol/token_exchange/observability_test.exs test/lockspire/storage/repository_atomicity_test.exs test/lockspire/protocol/refresh_exchange_test.exs</automated></verify>
  <done>Persistence is atomic and observability retains exact audited/telemetry behavior through explicit dependencies.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Compose all five grants and reduce GrantSupport to compatibility delegation</name>
  <files>lib/lockspire/protocol/token_exchange/internal/grant_support.ex, lib/lockspire/protocol/token_exchange/internal/authorization_code_grant.ex, lib/lockspire/protocol/token_exchange/internal/device_code_grant.ex, lib/lockspire/protocol/token_exchange/internal/ciba_grant.ex, lib/lockspire/protocol/token_exchange/internal/refresh_exchange.ex, lib/lockspire/protocol/token_exchange/internal/rfc8693_exchange.ex, test/lockspire/protocol/token_exchange/characterization_test.exs</files>
  <behavior>
    - Each grant visibly sequences authentication/policy, polling or grant validation, resource selection, issuance, persistence, and observability as applicable.
    - GrantSupport's retained named arities remain callable but implementation ownership delegates to focused collaborators.
    - All five characterization rows preserve responses, errors, token records, audit, and telemetry.
  </behavior>
  <action>Rewrite the neutral grant coordinators as readable compositions of the explicit collaborators while preserving security order and TokenResult. RFC 8693 keeps its distinct validator/delegation policy but uses shared resource/issuer/observability owners; refresh keeps distinct family semantics but uses shared auth/issuer/persistence/observability. Retain every lower public helper arity and public result conversion through existing compatibility facades. Remove moved private clusters and stale aliases from GrantSupport.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/characterization_test.exs test/lockspire/protocol/token_exchange/authorization_code_test.exs test/lockspire/protocol/refresh_exchange_test.exs test/lockspire/protocol/token_exchange/device_code_test.exs test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs test/lockspire/protocol/rfc8693_exchange_test.exs</automated></verify>
  <done>All five grants are cohesive compositions with stable results and GrantSupport no longer owns unrelated responsibilities.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Validated grant → signed token | Claim/format drift changes authorization semantics. |
| Issued values → durable mutation/audit | Partial writes can mint untracked or replayable credentials. |
| Protocol outcome → operator telemetry | Missing or secret-bearing observations harm response and confidentiality. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-135-21 | Spoofing/Elevation of privilege | token issuer | critical | mitigate | Exact claims/format/cnf characterization through real signers. |
| T-135-22 | Tampering | grant persistence | critical | mitigate | Complete aggregate operations and DB rollback/concurrency proof. |
| T-135-23 | Repudiation | grant observability | high | mitigate | Exact audit/telemetry event and attribution assertions. |
| T-135-24 | Information disclosure | observations/errors | high | mitigate | Redaction assertions and safe neutral error reuse. |
</threat_model>
<verification>`mix test test/lockspire/protocol/token_exchange/characterization_test.exs test/lockspire/protocol/token_exchange/issuance_test.exs test/lockspire/protocol/token_exchange/persistence_test.exs test/lockspire/protocol/token_exchange/observability_test.exs`</verification>
<success_criteria>All grant responsibilities are focused, explicit, behaviorally stable, and composed behind the stable facade.</success_criteria>
<output>Create `.planning/phases/135-cohesive-internals/135-08-SUMMARY.md` when done.</output>
