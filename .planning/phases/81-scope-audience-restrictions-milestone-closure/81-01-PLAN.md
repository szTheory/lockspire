---
phase: 81-scope-audience-restrictions-milestone-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - lib/lockspire/plug/verify_token.ex
  - test/lockspire/plug/verify_token_test.exs
autonomous: true
requirements:
  - VAL-PLUG-04
  - VAL-DX-01
  - VAL-DX-03
must_haves:
  truths:
    - `VerifyToken` accepts explicit per-route `scopes:` and `audience:` / `audiences:` options without introducing global policy.
    - Audience validation failures remain token-invalidity failures while missing required scopes remain authorization denials that later map to `403 insufficient_scope`.
    - Valid requests still assign one `%Lockspire.AccessToken{}` carrying claims, `client_id`, authorization scheme, and sender-binding metadata for the downstream plug pipeline.
    - Restriction failures emit actionable, redaction-safe developer-facing log messages that distinguish invalid audience, insufficient scope, and pre-existing JWT failure classes without exposing raw tokens, proofs, certificates, or full claim dumps.
  artifacts:
    - path: mix.exs
      provides: Direct `nimble_options` dependency for first-class plug option validation
    - path: lib/lockspire/plug/verify_token.ex
      provides: Route-option validation, scope normalization, audience normalization, and structured restriction failures
    - path: test/lockspire/plug/verify_token_test.exs
      provides: Unit proof for option parsing, audience semantics, and scope semantics
  key_links:
    - from: lib/lockspire/plug/verify_token.ex
      to: lib/lockspire/plug/require_token.ex
      via: structured `%AccessToken{error: ...}` for invalid-token vs insufficient-scope decisions
      pattern: "assign\\(conn, :access_token"
    - from: lib/lockspire/plug/verify_token.ex
      to: mix.exs
      via: `NimbleOptions.validate!/2` in `init/1`
      pattern: "NimbleOptions"
---

<objective>
Add route-level scope and audience restrictions to `Lockspire.Plug.VerifyToken` while preserving the split pipeline from Phases 79-80.

Purpose: Per D-01 through D-03, Phase 81 must stay inside the existing `VerifyToken -> EnforceSenderConstraints -> RequireToken` pipeline and expose route-level restrictions as explicit plug options, not a second policy system or hidden app config.
Output: `VerifyToken` validates/normalizes `scopes:` and `audience:` / `audiences:` options, evaluates those restrictions after JWT verification, and emits typed `%AccessToken{}` failures that downstream plugs can render without reparsing claims.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/81-scope-audience-restrictions-milestone-closure/81-CONTEXT.md
@.planning/phases/81-scope-audience-restrictions-milestone-closure/81-RESEARCH.md
@.planning/phases/81-scope-audience-restrictions-milestone-closure/81-PATTERNS.md
@.planning/phases/79-core-validation-plug/79-02-SUMMARY.md
@.planning/phases/80-sender-constraining-integration/80-01-SUMMARY.md
@lib/lockspire/plug/verify_token.ex
@lib/lockspire/access_token.ex
@test/lockspire/plug/verify_token_test.exs

<interfaces>
From lib/lockspire/access_token.ex:
```elixir
defstruct [
  :token,
  :claims,
  :client_id,
  :authorization_scheme,
  :binding_type,
  :binding_requirements,
  :error
]
```

From lib/lockspire/plug/verify_token.ex:
```elixir
def call(conn, _opts) do
  case extract_token(conn) do
    {:ok, authorization_scheme, token} ->
      access_token = verify_token(token, authorization_scheme)
      assign(conn, :access_token, access_token)

    {:error, reason} ->
      assign(conn, :access_token, %AccessToken{error: reason})
  end
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validate and normalize explicit route restriction options in `VerifyToken.init/1`</name>
  <files>mix.exs, lib/lockspire/plug/verify_token.ex, test/lockspire/plug/verify_token_test.exs</files>
  <behavior>
    - `scopes:` is the only supported scope option; omitted or `[]` means no scope restriction.
    - `audience:` accepts exactly one non-empty string; `audiences:` accepts a non-empty list of non-empty strings.
    - Setting both `:audience` and `:audiences` raises immediately during plug init.
    - Normalized opts preserve exact case-sensitive values and do not introduce singular aliases or grouped-expression syntax.
  </behavior>
  <action>
    Add `{:nimble_options, "~> 1.1"}` as a direct dependency in `mix.exs` so `VerifyToken` can use the same repo-supported options-validation pattern already used by `EnforceSenderConstraints`. Then update `VerifyToken.init/1` to validate and normalize route options per D-03 through D-11: support `scopes:` only (per D-04, D-08), reject simultaneous `audience:` and `audiences:` (per D-11), and preserve exact configured values without canonicalization or OR-style DSLs (per D-05, D-09, D-10, D-16). Keep all option work local to the plug; do not add application env or a second policy abstraction.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/plug/verify_token_test.exs --warnings-as-errors</automated>
  </verify>
  <done>`VerifyToken.init/1` rejects invalid route configuration up front and the repo compiles against a direct `NimbleOptions` dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce normalized audience and scope restrictions in the soft verification step</name>
  <files>lib/lockspire/plug/verify_token.ex, test/lockspire/plug/verify_token_test.exs</files>
  <behavior>
    - JWT verification still handles Bearer and DPoP schemes, signature/time checks, and sender-binding metadata assignment exactly as before.
    - If route audience restrictions are configured, token `aud` is normalized as string -> one-item list, list -> all non-empty strings, anything else -> invalid token.
    - Missing, malformed, or mismatched audience under configured restrictions becomes a structured `invalid_token` failure for later `401` rendering.
    - Token `scope` is normalized by splitting the RFC 6749 space-delimited claim, trimming, and de-duplicating before exact all-of comparison against configured `scopes:`.
    - Valid token with insufficient scopes becomes a structured insufficient-scope failure that carries the required scope set without halting the connection.
    - Developer-facing logs distinguish route restriction failures from missing token, invalid signature/time claims, and sender-binding mismatches using typed, redaction-safe metadata only.
  </behavior>
  <action>
    Extend `VerifyToken.call/2` and the post-JWT success path so restrictions are evaluated only after signature/time validation succeeds, per D-17 through D-21. Normalize the token `aud` claim exactly as locked in D-12 through D-16, treating missing/malformed/mismatched audience as token-invalidity (`401 invalid_token` later, not business authorization). Normalize the token `scope` claim per D-05 through D-07 into exact case-sensitive values and enforce all-of matching only; do not add `scope:` aliasing, nested expressions, or any-of scope semantics. Emit typed, redaction-safe structured errors onto `%AccessToken{}` for audience failures and insufficient-scope failures so `RequireToken` can later choose `401` vs `403` without reparsing claims. Extend the existing logging path so developers can distinguish missing token, expired/time-invalid token, signature failure, audience failure, insufficient scope, and sender-binding mismatch from redaction-safe log metadata or messages, satisfying VAL-DX-03 without logging raw tokens, proofs, certificates, or full claim dumps per D-19 and D-27.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/plug/verify_token_test.exs --warnings-as-errors</automated>
  </verify>
  <done>`VerifyToken` remains non-halting, preserves sender-binding metadata, produces deterministic structured failures for malformed/missing/mismatched audience and insufficient scopes, and proves differentiated developer-facing logging for the new restriction paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> `VerifyToken` | Untrusted JWT claims and route-targeting metadata cross into Lockspire’s protected-resource validation path. |
| Host router config -> `VerifyToken.init/1` | Explicit route options define security policy for a given Phoenix route and must be validated before request handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-01 | Spoofing / Elevation of Privilege | `VerifyToken` audience checks | mitigate | Enforce exact audience matching only after strict `aud` normalization; malformed or missing `aud` under configured restrictions becomes `invalid_token` rather than silently passing. |
| T-81-02 | Elevation of Privilege | `VerifyToken` scope checks | mitigate | Normalize token scopes deterministically and require exact case-sensitive all-of matching for `scopes:` per D-05 through D-07. |
| T-81-03 | Tampering | Plug option surface | mitigate | Validate `scopes:` / `audience:` / `audiences:` in `init/1` with `NimbleOptions` and reject ambiguous `audience` + `audiences` configuration per D-11. |
| T-81-04 | Information Disclosure | Restriction failure logging | mitigate | Keep structured failure metadata redaction-safe and do not log raw tokens, proofs, certificates, or full claim dumps per D-27. |
</threat_model>

<verification>
Primary proof is `test/lockspire/plug/verify_token_test.exs` expanded to cover option validation, `aud` normalization, audience mismatch, malformed `aud`, scope normalization, exact all-of matching, differentiated developer-facing log assertions for restriction failures, and continued Bearer/DPoP assignment behavior.
</verification>

<success_criteria>
`VerifyToken` now owns the full route-restriction intake and soft evaluation surface for Phase 81 while remaining a composable, non-halting validation plug in the existing three-plug pipeline.
</success_criteria>

<output>
After completion, create `.planning/phases/81-scope-audience-restrictions-milestone-closure/81-01-SUMMARY.md`
</output>
