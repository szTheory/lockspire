---
phase: 81-scope-audience-restrictions-milestone-closure
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/lockspire/plug/require_token.ex
  - test/lockspire/plug/require_token_test.exs
  - test/support/generated_host_app_web/controllers/protected_api_controller.ex
  - test/support/generated_host_app_web/router.ex
  - test/integration/phase81_generated_host_route_protection_e2e_test.exs
autonomous: true
requirements:
  - VAL-DX-01
  - VAL-DX-02
  - VAL-BIND-03
must_haves:
  truths:
    - `RequireToken` remains the only strict HTTP boundary and renders `401 invalid_token` for token, audience, and sender-constraint failures while rendering `403 insufficient_scope` for scope-only denials.
    - Routed Phoenix requests can prove successful access, missing-token failure, audience mismatch failure, insufficient-scope denial, and sender-constrained success/failure against real route dispatch.
    - Generated-host proof demonstrates the narrow shipped DX: protecting host Phoenix API routes with Lockspire-issued tokens inside the same embedded host shape.
  artifacts:
    - path: lib/lockspire/plug/require_token.ex
      provides: Final HTTP semantics for invalid-token vs insufficient-scope failures while preserving DPoP-vs-Bearer challenge selection
    - path: test/support/generated_host_app_web/controllers/protected_api_controller.ex
      provides: Minimal generated-host protected API endpoint that exposes the assigns contract to routed tests
    - path: test/integration/phase81_generated_host_route_protection_e2e_test.exs
      provides: Real Phoenix router and generated-host proof for Phase 81 milestone closure
  key_links:
    - from: test/support/generated_host_app_web/router.ex
      to: lib/lockspire/plug/verify_token.ex
      via: explicit `scopes:` / `audience:` route options in the generated-host pipeline
      pattern: "VerifyToken"
    - from: lib/lockspire/plug/require_token.ex
      to: lib/lockspire/plug/enforce_sender_constraints.ex
      via: scheme-aware challenge rendering from typed sender-constraint failures
      pattern: "challenge: :dpop"
    - from: test/integration/phase81_generated_host_route_protection_e2e_test.exs
      to: test/support/generated_host_app_web/controllers/protected_api_controller.ex
      via: routed JSON responses proving the assigns contract
      pattern: "access_token"
---

<objective>
Complete Phase 81’s HTTP semantics and prove the end-to-end route-protection DX through the generated-host Phoenix fixture.

Purpose: Per D-21 through D-25 and D-38 through D-40, Phase 81 is not done when `VerifyToken` alone understands restrictions. `RequireToken` must preserve the strict 401 boundary for token/audience/sender failures, add `403 insufficient_scope` for valid-but-under-scoped tokens, and the repo must prove those semantics through real routed requests in the embedded host fixture.
Output: Updated `RequireToken`, a generated-host protected API fixture route/controller, and end-to-end integration tests for valid, missing, audience-mismatch, insufficient-scope, and sender-constrained restricted-route cases.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/81-scope-audience-restrictions-milestone-closure/81-CONTEXT.md
@.planning/phases/81-scope-audience-restrictions-milestone-closure/81-RESEARCH.md
@.planning/phases/81-scope-audience-restrictions-milestone-closure/81-PATTERNS.md
@.planning/phases/80-sender-constraining-integration/80-03-SUMMARY.md
@lib/lockspire/plug/require_token.ex
@lib/lockspire/plug/enforce_sender_constraints.ex
@test/lockspire/plug/require_token_test.exs
@test/support/generated_host_app_web/router.ex
@test/integration/phase31_generated_host_verification_e2e_test.exs

<interfaces>
From lib/lockspire/plug/require_token.ex:
```elixir
case conn.assigns[:access_token] do
  %AccessToken{error: nil, claims: claims} when not is_nil(claims) -> conn
  %AccessToken{error: :missing_token} -> handle_missing_token(conn)
  %AccessToken{error: error} when is_map(error) -> handle_structured_error(conn, error)
  %AccessToken{error: _reason} -> handle_invalid_token(conn, default_invalid_error())
end
```

Expected protected route shape per Phase 81:
```elixir
plug Lockspire.Plug.VerifyToken, scopes: ["read:billing"], audience: "billing-api"
plug Lockspire.Plug.EnforceSenderConstraints, dpop_replay_store: ReplayStore
plug Lockspire.Plug.RequireToken
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep `RequireToken` as the single strict boundary while adding insufficient-scope semantics</name>
  <files>lib/lockspire/plug/require_token.ex, test/lockspire/plug/require_token_test.exs</files>
  <behavior>
    - `:missing_token`, malformed JWT, signature/time failures, malformed/missing/mismatched audience, and sender-constraint failures still halt with `401`.
    - Typed sender-constraint failures continue to choose DPoP vs Bearer `WWW-Authenticate` based on the structured error map.
    - Typed insufficient-scope failures halt with `403`, `error="insufficient_scope"`, and include the required scopes in `WWW-Authenticate` when practical.
    - Response bodies remain minimal JSON and do not leak extra token material.
  </behavior>
  <action>
    Extend `RequireToken` so per D-21 through D-26 it remains the only HTTP response plug, but branches on a new structured insufficient-scope error category separately from sender-constraint failures. Preserve the existing DPoP-vs-Bearer challenge selection for sender failures exactly as locked in D-25 and the user’s explicit constraint. Audience failures from Plan 01 must map to `401 invalid_token`, not `403`, because audience is a token-validity/resource-targeting check per D-18. Scope failures must map to `403 insufficient_scope` with a redaction-safe challenge carrying the required scope set per D-24. Do not move any JWT parsing or sender enforcement logic into `RequireToken`; it should only render based on the typed `%AccessToken{error: ...}` contract.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/plug/require_token_test.exs --warnings-as-errors</automated>
  </verify>
  <done>`RequireToken` cleanly distinguishes invalid token vs insufficient scope while preserving scheme-aware DPoP/Bearer challenges.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove route protection through the generated-host Phoenix fixture</name>
  <files>test/support/generated_host_app_web/controllers/protected_api_controller.ex, test/support/generated_host_app_web/router.ex, test/integration/phase81_generated_host_route_protection_e2e_test.exs</files>
  <behavior>
    - A protected generated-host API route returns `200` and exposes the `%AccessToken{}` assigns contract when token, route restrictions, and sender constraints all pass.
    - Missing token returns `401 invalid_token`.
    - Audience mismatch returns `401 invalid_token`.
    - Valid token lacking required scopes returns `403 insufficient_scope` with the expected challenge/body semantics.
    - At least one combined sender-constraint plus route-restriction case proves that restricted routes still work correctly with DPoP or MTLS-bound Lockspire-issued tokens.
  </behavior>
  <action>
    Add a minimal generated-host protected API controller that returns JSON built from `conn.assigns[:access_token]` and demonstrates the documented assigns contract from D-33 without inventing business-policy behavior. Update `test/support/generated_host_app_web/router.ex` to add a narrow API pipeline that uses the canonical plug order from D-01 and D-31: `VerifyToken` with explicit route options, `EnforceSenderConstraints`, then `RequireToken`. Build `test/integration/phase81_generated_host_route_protection_e2e_test.exs` on the Phase 31 generated-host fixture style so it boots the endpoint, publishes a signing key, issues Lockspire-shaped JWTs, and proves the real route-dispatch matrix from D-38 through D-40. Keep the public proof narrow: same host app, same issuer family, Phoenix API route protection only; do not imply multi-issuer or gateway support.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/plug/require_token_test.exs test/integration/phase81_generated_host_route_protection_e2e_test.exs --include integration --warnings-as-errors</automated>
  </verify>
  <done>The repo has executable generated-host proof that Lockspire-issued tokens can protect Phoenix API routes with correct 200/401/403 semantics under scope, audience, and sender-constraint rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `VerifyToken` / `EnforceSenderConstraints` -> `RequireToken` | Structured token errors become the sole input to final HTTP response semantics. |
| Generated host router -> protected API controller | A host-owned Phoenix route depends on Lockspire’s plug pipeline to reject invalid or under-scoped tokens before controller logic runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-81-05 | Elevation of Privilege | `RequireToken` | mitigate | Keep `RequireToken` as the only strict boundary and halt on all invalid-token and insufficient-scope failures before controller execution. |
| T-81-06 | Spoofing | Challenge selection | mitigate | Preserve DPoP-vs-Bearer challenge rendering from typed sender-constraint errors so proof-specific failures are not downgraded to generic bearer semantics. |
| T-81-07 | Information Disclosure | Protected API fixture | mitigate | Return minimal JSON showing assigns contract without echoing raw access tokens, proofs, certificates, or full claims dumps. |
| T-81-08 | Repudiation | Routed milestone proof | mitigate | Exercise the generated-host fixture through real Phoenix dispatch so route protection claims stay repo-provable and traceable to executable tests. |
</threat_model>

<verification>
The unit seam is `test/lockspire/plug/require_token_test.exs`; the milestone-proof seam is `test/integration/phase81_generated_host_route_protection_e2e_test.exs`, which must cover 200 success, 401 missing token, 401 audience mismatch, 403 insufficient scope, and at least one sender-constrained restricted-route success/failure pair.
</verification>

<success_criteria>
Phase 81 has real routed proof that the Lockspire plug pipeline protects host Phoenix API routes with correct HTTP semantics and without widening the product boundary beyond Lockspire-issued tokens inside the same embedded host.
</success_criteria>

<output>
After completion, create `.planning/phases/81-scope-audience-restrictions-milestone-closure/81-02-SUMMARY.md`
</output>
