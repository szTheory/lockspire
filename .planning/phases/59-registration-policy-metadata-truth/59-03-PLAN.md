---
phase: 59-registration-policy-metadata-truth
plan: 03
type: execute
wave: 2
depends_on:
  - "59-01"
  - "59-02"
files_modified:
  - lib/lockspire/protocol/discovery.ex
  - lib/lockspire/protocol/client_auth.ex
  - test/lockspire/protocol/discovery_test.exs
  - test/lockspire/web/discovery_controller_test.exs
autonomous: true
requirements:
  - META-01
  - META-02
must_haves:
  truths:
    - "Discovery publishes token, revocation, and introspection client-auth metadata from one shared direct-client-auth capability source plus endpoint-specific truth predicates."
    - "`private_key_jwt` is published only on endpoints whose current behavior genuinely supports the method for this phase."
    - "JWT client-auth signing-algorithm metadata is published only when the corresponding endpoint publishes `private_key_jwt`."
    - "Revocation and introspection may diverge automatically when their runtime behavior diverges; metadata-only override knobs are not added."
  artifacts:
    - path: lib/lockspire/protocol/discovery.ex
      provides: Centralized endpoint auth-method and signing-alg metadata truth.
    - path: lib/lockspire/protocol/client_auth.ex
      provides: Shared capability source used by discovery helpers instead of duplicated method lists.
    - path: test/lockspire/protocol/discovery_test.exs
      provides: Contract tests for token/revocation/introspection metadata presence and omission.
    - path: test/lockspire/web/discovery_controller_test.exs
      provides: HTTP-level proof that the published document matches the protocol helpers.
  key_links:
    - from: lib/lockspire/protocol/discovery.ex
      to: lib/lockspire/protocol/client_auth.ex
      via: shared auth-method capability source
      pattern: supported_auth_methods|private_key_jwt
    - from: lib/lockspire/protocol/discovery.ex
      to: lib/lockspire/protocol/security_profile.ex
      via: derived signing algorithm publication
      pattern: allowed_signing_algorithms
    - from: lib/lockspire/protocol/discovery.ex
      to: lib/lockspire/protocol/introspection.ex, lib/lockspire/protocol/revocation.ex
      via: endpoint-specific truth predicates
      pattern: validate_confidential_caller|authenticate_client
---

<objective>
Align discovery, revocation, and introspection metadata with the runtime truth of Lockspire's current direct-client authentication surface.

Purpose: Satisfy `META-01` and `META-02` by publishing auth-method and signing-algorithm metadata only where the repo's current behavior supports it, while keeping all metadata assembly centralized in `Discovery`.
Output: One focused plan covering shared capability helpers, endpoint-specific metadata predicates, and discovery/controller contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-registration-policy-metadata-truth/59-CONTEXT.md
@.planning/phases/59-registration-policy-metadata-truth/59-RESEARCH.md
@.planning/phases/59-registration-policy-metadata-truth/59-PATTERNS.md
@lib/lockspire/protocol/discovery.ex
@lib/lockspire/protocol/client_auth.ex
@lib/lockspire/protocol/security_profile.ex
@lib/lockspire/protocol/introspection.ex
@lib/lockspire/protocol/revocation.ex
@test/lockspire/protocol/discovery_test.exs
@test/lockspire/web/discovery_controller_test.exs

<interfaces>
From `lib/lockspire/protocol/discovery.ex`:
```elixir
@spec openid_configuration() :: map()
def openid_configuration do
  issuer = Config.issuer!()
  endpoint_metadata = mounted_endpoint_metadata()
  ...
end
```

From `lib/lockspire/protocol/client_auth.ex`:
```elixir
@supported_auth_methods [:none, :client_secret_basic, :client_secret_post, :private_key_jwt]
@spec supported_auth_methods() :: [atom()]
def supported_auth_methods, do: @supported_auth_methods
```

From `lib/lockspire/protocol/introspection.ex`:
```elixir
defp validate_confidential_caller(%Client{
       client_type: :confidential,
       token_endpoint_auth_method: method
     })
     when method in [:client_secret_basic, :client_secret_post] do
  {:ok, true}
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared endpoint-auth metadata helpers in Discovery</name>
  <read_first>
    - lib/lockspire/protocol/discovery.ex
    - lib/lockspire/protocol/client_auth.ex
    - lib/lockspire/protocol/security_profile.ex
    - .planning/phases/59-registration-policy-metadata-truth/59-RESEARCH.md
    - .planning/phases/59-registration-policy-metadata-truth/59-PATTERNS.md
  </read_first>
  <files>lib/lockspire/protocol/discovery.ex, lib/lockspire/protocol/client_auth.ex</files>
  <behavior>
    - Discovery remains the only place that assembles public metadata.
    - Token, revocation, and introspection auth-method fields are computed from one shared capability source plus discovery-side endpoint truth predicates that reflect current runtime behavior without modifying endpoint auth behavior in this phase.
    - Signing algorithm fields are emitted only when the corresponding method list includes `private_key_jwt`.
  </behavior>
  <action>Refactor `Discovery` so endpoint auth-method metadata is no longer hardcoded as a single token-endpoint list. Introduce small helpers that start from the shared `ClientAuth` capability source and then narrow per endpoint according to current runtime truth, but keep that narrowing in `Discovery` for Phase 59 rather than changing endpoint auth behavior. Concretely: derive token-endpoint publication from the current shared client-auth seam, derive revocation publication from the same seam plus mounted-route truth, and derive introspection publication from the existing current limitation that confidential callers are narrowed beyond raw `ClientAuth` support. Reuse `SecurityProfile.allowed_signing_algorithms/1` for the algorithm fields. Keep all published keys assembled in `Discovery`; do not move metadata logic into controllers and do not pull signature-verification or endpoint-behavior changes forward from Phase 61.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/protocol/discovery_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "token_endpoint_auth_methods_supported|revocation_endpoint_auth_methods_supported|introspection_endpoint_auth_methods_supported|signing_alg_values_supported|private_key_jwt" lib/lockspire/protocol/discovery.ex` finds centralized helpers for all published endpoint auth metadata.
    - The protocol discovery test command passes.
    - `lib/lockspire/protocol/introspection.ex` and `lib/lockspire/protocol/revocation.ex` remain behavior-neutral inputs to metadata truth unless execution finds a tiny read-only helper is strictly necessary.
    - No controller or LiveView module computes auth-method metadata directly.
  </acceptance_criteria>
  <done>Discovery can now publish endpoint auth-method and signing-algorithm metadata from shared capability truth instead of hardcoded one-off lists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the published document against current endpoint behavior</name>
  <read_first>
    - test/lockspire/protocol/discovery_test.exs
    - test/lockspire/web/discovery_controller_test.exs
    - lib/lockspire/protocol/introspection.ex
    - lib/lockspire/protocol/revocation.ex
    - .planning/ROADMAP.md
  </read_first>
  <files>test/lockspire/protocol/discovery_test.exs, test/lockspire/web/discovery_controller_test.exs</files>
  <behavior>
    - Protocol tests pin the presence and omission rules for token, revocation, and introspection auth metadata.
    - Controller tests prove the HTTP document matches the same truth.
    - The tests allow endpoint divergence when runtime behavior diverges, instead of assuming all endpoints publish the same set.
  </behavior>
  <action>Extend `discovery_test.exs` with contract coverage for the new Phase 59 metadata keys. The assertions should explicitly pin both positive and negative cases: endpoint mounted/unmounted behavior, publication of signing algorithms only when `private_key_jwt` is actually published, and narrower introspection/revocation method sets when runtime behavior is narrower. Then update `discovery_controller_test.exs` so the `.well-known` response includes the same auth-method/signing-alg truth over HTTP. Keep the tests strict enough to catch future drift when Phase 60 or 61 changes runtime behavior.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "revocation_endpoint_auth|introspection_endpoint_auth|token_endpoint_auth_signing_alg_values_supported|private_key_jwt" test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs` shows contract coverage for all new metadata keys.
    - The protocol + controller discovery test command passes.
    - No test treats revocation/introspection as equal to token metadata unless the underlying code actually makes them equal.
  </acceptance_criteria>
  <done>The published discovery document is pinned to current endpoint behavior and cannot silently drift from runtime truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime endpoint behavior -> discovery document | Integrators depend on published metadata to choose auth methods and algorithms. |
| shared client-auth seam -> endpoint-specific publication | A broad shared capability must still be narrowed when individual endpoints impose stricter behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-05 | I | `Discovery` publication | mitigate | Publish endpoint auth metadata only from centralized truth helpers tied to actual runtime behavior. |
| T-59-06 | T | token/revocation/introspection divergence | mitigate | Permit automatic narrowing per endpoint and pin it in tests instead of forcing metadata uniformity through UI/config. |
</threat_model>

<verification>
Run discovery protocol and controller tests with warnings-as-errors and confirm the public document matches the phase's current runtime truth.
</verification>

<success_criteria>
Discovery, revocation, and introspection metadata become truthful, centralized, and drift-resistant for the current direct-client auth surface.
</success_criteria>

<output>
After completion, create `.planning/phases/59-registration-policy-metadata-truth/59-03-SUMMARY.md`
</output>
