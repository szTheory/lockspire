---
phase: 59-registration-policy-metadata-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/admin/server_policy.ex
  - lib/lockspire/web/live/admin/policies_live/dcr.ex
  - lib/lockspire/web/live/admin/policies_live/dcr.html.heex
  - lib/lockspire/web/live/admin/clients_live/show.ex
  - test/lockspire/admin/server_policy_test.exs
  - test/lockspire/web/live/admin/policies_live/dcr_test.exs
  - test/lockspire/web/live/admin/clients_live/show_test.exs
autonomous: true
requirements:
  - REG-03
must_haves:
  truths:
    - "Operator-facing policy truth for `private_key_jwt` is derived from existing server policy and security profile, not from a new editable crypto plane."
    - "Admin DCR policy surfaces explain whether self-registered `private_key_jwt` is currently allowed and which assertion signing algorithms the issuer posture implies."
    - "Per-client admin surfaces remain read-only about this slice in Phase 59; they do not become a new key-management workflow."
  artifacts:
    - path: lib/lockspire/admin/server_policy.ex
      provides: A derived policy/truth helper for the admin surface.
    - path: lib/lockspire/web/live/admin/policies_live/dcr.html.heex
      provides: Explanatory DCR policy truth for the supported `private_key_jwt` slice.
    - path: lib/lockspire/web/live/admin/clients_live/show.ex
      provides: Read-only client-level visibility without widening edit workflows.
  key_links:
    - from: lib/lockspire/admin/server_policy.ex
      to: lib/lockspire/protocol/security_profile.ex
      via: effective assertion signing algorithms
      pattern: allowed_signing_algorithms|security_profile
    - from: lib/lockspire/web/live/admin/policies_live/dcr.ex
      to: lib/lockspire/domain/server_policy.ex
      via: DCR allowlist truth for `private_key_jwt`
      pattern: dcr_allowed_token_endpoint_auth_methods
---

<objective>
Expose truthful operator-facing policy for the `private_key_jwt` registration slice without expanding Lockspire into a standalone security console.

Purpose: Satisfy `REG-03` by making admin policy and client detail surfaces explain what Lockspire will accept for self-registered `private_key_jwt` clients and which signing algorithms the issuer posture implies.
Output: One narrow plan covering derived policy helpers, DCR policy UI truth, and read-only client visibility.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-registration-policy-metadata-truth/59-CONTEXT.md
@.planning/phases/59-registration-policy-metadata-truth/59-RESEARCH.md
@.planning/phases/59-registration-policy-metadata-truth/59-PATTERNS.md
@lib/lockspire/admin/server_policy.ex
@lib/lockspire/domain/server_policy.ex
@lib/lockspire/protocol/security_profile.ex
@lib/lockspire/web/live/admin/policies_live/dcr.ex
@lib/lockspire/web/live/admin/policies_live/dcr.html.heex
@lib/lockspire/web/live/admin/clients_live/show.ex
@test/lockspire/admin/server_policy_test.exs
@test/lockspire/web/live/admin/policies_live/dcr_test.exs
@test/lockspire/web/live/admin/clients_live/show_test.exs

<interfaces>
From `lib/lockspire/protocol/security_profile.ex`:
```elixir
@spec allowed_signing_algorithms(ServerPolicy.security_profile()) :: [String.t()]
def allowed_signing_algorithms(:fapi_2_0_security), do: ["ES256", "PS256"]
def allowed_signing_algorithms(:none), do: ["RS256", "ES256", "PS256", "EdDSA"]
```

From `lib/lockspire/domain/server_policy.ex`:
```elixir
%ServerPolicy{
  security_profile: :none,
  dcr_allowed_token_endpoint_auth_methods: []
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a derived admin truth helper for `private_key_jwt` policy</name>
  <read_first>
    - lib/lockspire/admin/server_policy.ex
    - lib/lockspire/domain/server_policy.ex
    - lib/lockspire/protocol/security_profile.ex
    - test/lockspire/admin/server_policy_test.exs
    - .planning/phases/59-registration-policy-metadata-truth/59-RESEARCH.md
  </read_first>
  <files>lib/lockspire/admin/server_policy.ex, test/lockspire/admin/server_policy_test.exs</files>
  <behavior>
    - The helper reports whether self-registered `private_key_jwt` is allowed by current DCR policy.
    - The helper reports the effective JWT client-assertion signing algorithms implied by server posture.
    - No new persisted field or operator-configured algorithm allowlist is introduced.
  </behavior>
  <action>Add a small read-only helper in `Admin.ServerPolicy` that derives Phase 59 policy truth from the existing `%ServerPolicy{}` singleton plus `SecurityProfile.allowed_signing_algorithms/1`. The returned shape should be simple enough for LiveView rendering, for example: whether `private_key_jwt` is currently allowed for self-registered clients and the effective supported assertion algorithms. Keep it computed, not stored. Add focused unit tests that pin both security-profile branches and the `dcr_allowed_token_endpoint_auth_methods` gate.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/admin/server_policy_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "private_key_jwt|allowed_signing_algorithms" lib/lockspire/admin/server_policy.ex test/lockspire/admin/server_policy_test.exs` shows a derived helper and coverage.
    - The admin server-policy test command passes.
    - No new durable field or config knob is added for assertion algorithms.
  </acceptance_criteria>
  <done>The admin layer has one derived truth helper for the Phase 59 `private_key_jwt` policy slice.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface narrow policy truth in DCR and client admin views</name>
  <read_first>
    - lib/lockspire/web/live/admin/policies_live/dcr.ex
    - lib/lockspire/web/live/admin/policies_live/dcr.html.heex
    - lib/lockspire/web/live/admin/clients_live/show.ex
    - test/lockspire/web/live/admin/policies_live/dcr_test.exs
    - test/lockspire/web/live/admin/clients_live/show_test.exs
    - .planning/phases/59-registration-policy-metadata-truth/59-CONTEXT.md
  </read_first>
  <files>lib/lockspire/web/live/admin/policies_live/dcr.ex, lib/lockspire/web/live/admin/policies_live/dcr.html.heex, lib/lockspire/web/live/admin/clients_live/show.ex, test/lockspire/web/live/admin/policies_live/dcr_test.exs, test/lockspire/web/live/admin/clients_live/show_test.exs</files>
  <behavior>
    - The DCR policy page explains whether self-registered `private_key_jwt` is currently allowed and which signing algorithms are effective.
    - The client detail page can show read-only posture clues for configured `private_key_jwt` / `jwks_uri` clients without adding edit actions.
    - The UI stays explanatory and narrow; no operator-managed key workflow or test-fetch affordance appears.
  </behavior>
  <action>Wire the derived helper into the DCR policy LiveView so the page renders a concise explanation of the supported `private_key_jwt` self-registration slice and the effective signing algorithms implied by issuer posture. Update `clients_live/show.ex` only enough to keep read-only client posture truthful for this slice, such as surfacing configured auth method and `jwks_uri` presence or clear explanatory copy. Extend the existing LiveView tests to pin the new truth text and verify the views remain narrow. Do not add edit controls for `jwks_uri`, operator-created JWT clients, or assertion algorithm lists.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/web/live/admin/policies_live/dcr_test.exs test/lockspire/web/live/admin/clients_live/show_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "private_key_jwt|jwks_uri|ES256|PS256|EdDSA|RS256" lib/lockspire/web/live/admin/policies_live/dcr.html.heex lib/lockspire/web/live/admin/clients_live/show.ex test/lockspire/web/live/admin/policies_live/dcr_test.exs test/lockspire/web/live/admin/clients_live/show_test.exs` shows the rendered truth and coverage.
    - The LiveView test command passes.
    - No new admin write path is introduced for JWT client key material or algorithm policy.
  </acceptance_criteria>
  <done>Operator-facing surfaces explain the supported `private_key_jwt` slice truthfully without widening Lockspire's admin scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| durable server policy -> operator UI | Operators infer security support from the admin surface and must not be misled by stale or non-derived values. |
| client detail page -> operator action | Read-only posture cues must not imply unsupported edit workflows or preview guarantees. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-03 | I | admin DCR policy UI | mitigate | Derive support and algorithms from existing server policy plus security profile, not from handwritten UI strings. |
| T-59-04 | E | admin client UI | mitigate | Keep the surface read-only for this slice and avoid controls that imply key-management or remote-fetch support before later phases. |
</threat_model>

<verification>
Run admin unit and LiveView tests with warnings-as-errors and confirm the policy text stays derived and narrow.
</verification>

<success_criteria>
Operators can tell whether self-registered `private_key_jwt` is allowed and which assertion algorithms apply, without being offered a broader security console.
</success_criteria>

<output>
After completion, create `.planning/phases/59-registration-policy-metadata-truth/59-02-SUMMARY.md`
</output>
