---
phase: 59-registration-policy-metadata-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/registration.ex
  - lib/lockspire/protocol/registration_management.ex
  - test/support/fixtures/dcr_fixtures.ex
  - test/lockspire/protocol/registration_test.exs
  - test/lockspire/protocol/registration_management_test.exs
autonomous: true
requirements:
  - REG-01
  - REG-02
must_haves:
  truths:
    - "`jwks_uri` is accepted only for confidential clients using `token_endpoint_auth_method=private_key_jwt`."
    - "`jwks` and `jwks_uri` remain strictly mutually exclusive across registration and RFC 7592 full-replace updates."
    - "Unsupported `jwks_uri` combinations fail as explicit `invalid_client_metadata` reasons, not generic persistence errors."
    - "RFC 7592 updates persist the same `jwks_uri` truth as RFC 7591 registration intake."
  artifacts:
    - path: lib/lockspire/protocol/registration.ex
      provides: Narrow `jwks_uri` intake validation and self-registered client persistence truth.
    - path: lib/lockspire/protocol/registration_management.ex
      provides: RFC 7592 full-replace persistence that mirrors registration truth, including `jwks_uri`.
    - path: test/lockspire/protocol/registration_test.exs
      provides: Explicit DCR contract tests for the admitted and rejected `jwks_uri` slice.
    - path: test/lockspire/protocol/registration_management_test.exs
      provides: RFC 7592 regression coverage proving update-path parity with registration intake.
  key_links:
    - from: lib/lockspire/protocol/registration.ex
      to: lib/lockspire/protocol/registration_management.ex
      via: shared validate_intake_metadata truth
      pattern: validate_intake_metadata|validate_jwks|jwks_uri
    - from: lib/lockspire/protocol/registration_management.ex
      to: lib/lockspire/domain/client.ex
      via: first-class durable client fields
      pattern: jwks|jwks_uri|token_endpoint_auth_method
---

<objective>
Implement the narrow `jwks_uri` registration and RFC 7592 update slice for `private_key_jwt` without widening Lockspire into general remote metadata ingestion.

Purpose: Satisfy `REG-01` and `REG-02` by making DCR and management flows truthful, explicit, and durable for the supported `private_key_jwt` wedge only.
Output: One focused plan covering intake validation, persistence parity, and protocol tests for the admitted/rejected `jwks_uri` slice.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/59-registration-policy-metadata-truth/59-CONTEXT.md
@.planning/phases/59-registration-policy-metadata-truth/59-RESEARCH.md
@.planning/phases/59-registration-policy-metadata-truth/59-PATTERNS.md
@lib/lockspire/protocol/registration.ex
@lib/lockspire/protocol/registration_management.ex
@lib/lockspire/domain/client.ex
@test/support/fixtures/dcr_fixtures.ex
@test/lockspire/protocol/registration_test.exs
@test/lockspire/protocol/registration_management_test.exs

<interfaces>
From `lib/lockspire/protocol/registration.ex`:
```elixir
@spec validate_intake_metadata(map(), Resolved.t(), ServerPolicy.t()) ::
        :ok | {:error, Error.t()}
def validate_intake_metadata(metadata, %Resolved{} = _resolved, server_policy)

defp validate_jwks(metadata) do
  has_jwks = Map.has_key?(metadata, "jwks")
  has_jwks_uri = Map.has_key?(metadata, "jwks_uri")
  auth_method = Map.get(metadata, "token_endpoint_auth_method", "client_secret_basic")
end
```

From `lib/lockspire/protocol/registration_management.ex`:
```elixir
with {:ok, resolved} <- DcrPolicy.resolve(server_policy, nil, metadata),
     :ok <- Registration.validate_intake_metadata(metadata, resolved, server_policy),
     {new_rat_plaintext, new_rat_hash} <- RegistrationAccessToken.generate(),
     {:ok, updated_client} <- persist_update(client, metadata, new_rat_hash) do
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten RFC 7591 `jwks_uri` intake and durable persistence</name>
  <read_first>
    - lib/lockspire/protocol/registration.ex
    - lib/lockspire/domain/client.ex
    - test/support/fixtures/dcr_fixtures.ex
    - test/lockspire/protocol/registration_test.exs
    - .planning/phases/59-registration-policy-metadata-truth/59-RESEARCH.md
  </read_first>
  <files>lib/lockspire/protocol/registration.ex, test/support/fixtures/dcr_fixtures.ex, test/lockspire/protocol/registration_test.exs</files>
  <behavior>
    - `jwks_uri` is accepted only when `token_endpoint_auth_method == "private_key_jwt"` and the resulting client is confidential.
    - `jwks_uri` must be `https` and remain mutually exclusive with inline `jwks`.
    - Missing cryptographic material for `private_key_jwt` remains an explicit `invalid_client_metadata` error.
    - Persisted self-registered clients retain `jwks_uri` as a first-class field.
  </behavior>
  <action>Extend `validate_jwks/1` in `Registration` to cover the full Phase 59 slice: reject `jwks_uri` outside the narrow `private_key_jwt` path, reject non-`https` values, and keep the current explicit `%Registration.Error{field, reason}` style. Then mirror the admitted truth in the registration persistence mapping so newly created DCR clients durably carry `jwks_uri`. Add or update DCR fixtures to make the supported `private_key_jwt` + `jwks_uri` path easy to exercise in tests. Extend `registration_test.exs` with focused cases for the admitted happy path and the main rejected combinations: `jwks` xor `jwks_uri`, non-`private_key_jwt`, missing material, and non-`https` URIs. Do not add fetch behavior, reachability checks, or preview actions in this phase.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/protocol/registration_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "jwks_uri|private_key_jwt|invalid_client_metadata" lib/lockspire/protocol/registration.ex test/lockspire/protocol/registration_test.exs` shows the new slice-specific validation and contract tests.
    - The registration test command passes.
    - No code performs an outbound fetch or attempts to validate remote key contents in Phase 59.
  </acceptance_criteria>
  <done>DCR intake truthfully admits only the supported `private_key_jwt` + `jwks_uri` slice and persists it durably.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make RFC 7592 updates mirror registration truth</name>
  <read_first>
    - lib/lockspire/protocol/registration_management.ex
    - lib/lockspire/protocol/registration.ex
    - test/lockspire/protocol/registration_management_test.exs
    - test/lockspire/protocol/registration_test.exs
    - .planning/phases/59-registration-policy-metadata-truth/59-PATTERNS.md
  </read_first>
  <files>lib/lockspire/protocol/registration_management.ex, test/lockspire/protocol/registration_management_test.exs</files>
  <behavior>
    - RFC 7592 full-replace updates accept and reject the same `jwks_uri` combinations as registration intake.
    - `apply_metadata_to_client/2` mirrors the registration field mapping, including `jwks_uri`.
    - RAT rotation and audit behavior remain unchanged.
  </behavior>
  <action>Update `RegistrationManagement.apply_metadata_to_client/2` so it no longer drops `jwks_uri`, and ensure the management path continues to reuse `Registration.validate_intake_metadata/3` rather than introducing parallel rules. Add RFC 7592 tests covering: updating a self-registered confidential client from inline `jwks` to `jwks_uri`, preserving xor semantics, rejecting unsupported auth-method combinations, and persisting the new `jwks_uri` value after a successful full replace. Keep the existing RAT rotation and transaction/audit flow intact.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/protocol/registration_management_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "jwks_uri" lib/lockspire/protocol/registration_management.ex test/lockspire/protocol/registration_management_test.exs` finds both persistence logic and regression coverage.
    - The registration-management test command passes.
    - The update path still routes through `Registration.validate_intake_metadata/3`.
  </acceptance_criteria>
  <done>RFC 7592 management no longer drifts from registration truth and durably preserves the admitted `jwks_uri` slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party DCR metadata -> durable client row | Untrusted metadata must not silently widen Lockspire into general remote-ingestion support. |
| RFC 7592 update payload -> existing client state | Full-replace updates must not drift from registration truth or lose durable key material fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-59-01 | T | `registration.ex` validation | mitigate | Admit `jwks_uri` only for the narrow `private_key_jwt` slice and keep xor errors explicit. |
| T-59-02 | I | `registration_management.ex` mapping | mitigate | Mirror registration persistence exactly, including `jwks_uri`, so management updates cannot understate or lose configured key material. |
</threat_model>

<verification>
Run the registration and registration-management protocol tests with warnings-as-errors and confirm the admitted/rejected `jwks_uri` matrix is pinned.
</verification>

<success_criteria>
Lockspire can truthfully store and update the narrow `private_key_jwt` + `jwks_uri` slice, while all unsupported combinations fail explicitly and durably.
</success_criteria>

<output>
After completion, create `.planning/phases/59-registration-policy-metadata-truth/59-01-SUMMARY.md`
</output>
