---
phase: 68
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["scripts/publish/verify_install_truth.sh"]
autonomous: true
requirements: ["PUB-01", "PUB-02"]

must_haves:
  truths:
    - "Maintainer can run a single script to verify post-publish truth"
    - "Script queries Hex API for the published version and confirms metadata"
    - "Script validates that HexDocs are live and accessible"
    - "Script generates an isolated Phoenix host app and compiles Lockspire to prove installability"
  artifacts:
    - path: "scripts/publish/verify_install_truth.sh"
      provides: "Automated install verification and Hex API checking"
  key_links:
    - from: "scripts/publish/verify_install_truth.sh"
      to: "hex.pm"
      via: "curl Hex API"
      pattern: "hex.pm/api/packages"
---

<objective>
Implement a post-publish script (`scripts/publish/verify_install_truth.sh`) that asserts Hex metadata via the HTTP API, validates Hexdocs availability, and generates a fresh Phoenix host application to prove dependency resolution and compilation of the released package.

Purpose: To ensure the public release is verifiable from a clean-room perspective, matching the canonical repository intent and installability without internal path pollution.
Output: A bash script capable of validating Hex publish state and Phoenix integration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-publish-verification-and-install-truth/68-PATTERNS.md
@.planning/phases/68-publish-verification-and-install-truth/68-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold verify_install_truth.sh</name>
  <files>scripts/publish/verify_install_truth.sh</files>
  <action>
    Create a new bash script `scripts/publish/verify_install_truth.sh`. Set standard bash strictness (`set -euo pipefail`).
    Implement an isolated environment using `mktemp -d` and trap `EXIT` to clean up the temporary directory.
    Ensure required commands like `curl`, `jq`, and `mix` are present.
    Follow the script structure analog established in `scripts/conformance/run_phase37_suite.sh`.
    Implement logic to extract the expected Lockspire version from the repository `mix.exs` and query the Hex API (`https://hex.pm/api/packages/lockspire`) using a retry loop (e.g. up to 12 retries with 10s sleep) to overcome Fastly CDN cache delay. Assert `latest_stable_version` matches the expected version.
  </action>
  <verify>
    <automated>bash -n scripts/publish/verify_install_truth.sh</automated>
  </verify>
  <done>Bash script exists, handles temp directory cleanly, and checks the Hex API for correct version metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Add docs verification and clean-room Phoenix generator</name>
  <files>scripts/publish/verify_install_truth.sh</files>
  <action>
    Extend `scripts/publish/verify_install_truth.sh`:
    1. Verify Hexdocs. Fetch the HTTP status code of `https://hexdocs.pm/lockspire/$EXPECTED_VERSION/supported-surface.html` and ensure it's a 200 OK.
    2. Inside the temp directory, generate a fresh Phoenix host application using `mix phx.new host_app --no-assets --no-ecto --no-html --no-mailer`. Ensure it forces the `phx_new` archive if needed.
    3. Modify the new host app's `mix.exs` via `sed` to inject `{:lockspire, "$EXPECTED_VERSION"}`. Do NOT use local paths (`path: "../"`).
    4. Run `mix deps.get` and `mix compile` within the `host_app` context to definitively prove installation and compilation truth.
    Make the script executable (`chmod +x scripts/publish/verify_install_truth.sh`).
  </action>
  <verify>
    <automated>bash -n scripts/publish/verify_install_truth.sh</automated>
  </verify>
  <done>The script verifies the documentation URL and completely tests a pristine Phoenix generator installation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Maintainer→Hex | The script trusts the Hex API to return accurate metadata, requiring parsed validation instead of regex. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | scripts/publish/verify_install_truth.sh | mitigate | Parse Hex API JSON safely via `jq` to prevent unexpected payload injection or formatting changes. |
</threat_model>

<verification>
Ensure the bash script adheres to standard execution patterns and cleanly handles temporary directory state without side-effects on the surrounding repository environment.
</verification>

<success_criteria>
- The publish script accurately orchestrates Hex API metadata polling.
- The script checks the live support contract endpoint.
- The script scaffolds a real Phoenix app to prove package compatibility.
</success_criteria>

<output>
After completion, create `.planning/phases/68-publish-verification-and-install-truth/68-01-SUMMARY.md`
</output>