---
phase: 68
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["docs/maintainer-release.md", "test/lockspire/publish_verification_test.exs"]
autonomous: true
requirements: ["PUB-01", "PUB-02"]

must_haves:
  truths:
    - "Maintainer guide explicitly outlines running the post-publish verification script"
    - "Test suite enforces that the guide maintains this explicit proof bucket"
  artifacts:
    - path: "docs/maintainer-release.md"
      provides: "Maintainer execution steps"
    - path: "test/lockspire/publish_verification_test.exs"
      provides: "Assertions for the maintainer release guide"
  key_links:
    - from: "test/lockspire/publish_verification_test.exs"
      to: "docs/maintainer-release.md"
      via: "file read and string match"
      pattern: "File.read"
---

<objective>
Update the repository's maintainer release guidance to require the execution of `verify_install_truth.sh` post-publish, and write assertions in a dedicated verification test to enforce this posture.

Purpose: To ensure the new install truth bash script isn't just an orphan file, but an enforced step in the real public release process.
Output: An updated maintainer guide and a new ExUnit test proving the guide's posture.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/68-publish-verification-and-install-truth/68-PATTERNS.md
@.planning/phases/68-publish-verification-and-install-truth/68-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update maintainer release documentation</name>
  <files>docs/maintainer-release.md</files>
  <action>
    Append post-publish verification instructions to `docs/maintainer-release.md`. 
    Create a new section (e.g., `## Post-Publish Verification`) detailing how to run `scripts/publish/verify_install_truth.sh`.
    Explain that this step verifies the published Hex artifact and docs against the canonical support contract and proves clean Phoenix installability.
  </action>
  <verify>
    <automated>grep -q "verify_install_truth.sh" docs/maintainer-release.md</automated>
  </verify>
  <done>Maintainer guide includes explicitly documented execution instructions for the new script.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert maintainer guide posture in ExUnit test</name>
  <files>test/lockspire/publish_verification_test.exs</files>
  <behavior>
    - Test 1: Assert `docs/maintainer-release.md` includes references to running `scripts/publish/verify_install_truth.sh`.
    - Test 2: Assert the test correctly reads the maintainer guide path using the `Path.expand("../../docs/maintainer-release.md", __DIR__)` pattern.
  </behavior>
  <action>
    Create `test/lockspire/publish_verification_test.exs`. Add `use ExUnit.Case, async: true`.
    Implement test to load `docs/maintainer-release.md` and check that it explicitly dictates running `scripts/publish/verify_install_truth.sh` and validating "Install Truth". 
    Look at `test/lockspire/release_readiness_contract_test.exs` for exact structure on how to load markdown documentation.
  </action>
  <verify>
    <automated>mix test test/lockspire/publish_verification_test.exs</automated>
  </verify>
  <done>ExUnit tests pass, structurally validating the presence of publish verification instructions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation→Action | Ensuring manual operators correctly copy and execute trusted release-verification workflows instead of hand-rolling ad-hoc checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-02 | Spoofing | test/lockspire/publish_verification_test.exs | mitigate | Use ExUnit to enforce documentation presence, minimizing risk that crucial install verification steps are silently removed over time. |
</threat_model>

<verification>
Ensure the test suite passes and explicitly guards the instructions for running the publish verification script in the maintainer guides.
</verification>

<success_criteria>
- The maintainer guide directs users to run the post-publish script.
- The new `publish_verification_test.exs` runs green and structurally locks the presence of this post-publish truth in the guide.
</success_criteria>

<output>
After completion, create `.planning/phases/68-publish-verification-and-install-truth/68-02-SUMMARY.md`
</output>