---
phase: 123-operate-queue-flow-polish
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/web/live/admin/device_authorizations_live/index.ex
  - test/lockspire/web/live/admin/device_authorizations_live_test.exs
autonomous: true
requirements:
  - OPERATE-01
  - OPERATE-02
  - OPERATE-03
requirements_addressed:
  - OPERATE-01
  - OPERATE-02
  - OPERATE-03
user_setup: []
must_haves:
  truths:
    - "OPERATE-01 D-07 D-11: /admin/device_authorizations exposes status pressure, redacted client/account, redacted durable authorization handle, expiry, poll interval, and next-poll or lifecycle activity."
    - "OPERATE-02 D-01 D-03 D-17: the device authorization page remains read-only and adds no approve, deny, queue, or worker command controls."
    - "OPERATE-03 D-05 D-12 D-14 D-15 D-18 D-24: empty, dense, approved, denied, expired, consumed, long-value, theme/focus/reduced-motion, and mobile-safe proof exists."
  artifacts:
    - path: "lib/lockspire/web/live/admin/device_authorizations_live/index.ex"
      provides: "Device authorization pressure copy, poll/activity helpers, and redaction-safe dense rows."
      contains: "device_authorization_pressure"
    - path: "test/lockspire/web/live/admin/device_authorizations_live_test.exs"
      provides: "Rendered proof for device queue states, redaction, no raw code/hash leakage, no tables, and unsupported-control absence."
      contains: "Approved, waiting"
  key_links:
    - from: "lib/lockspire/web/live/admin/device_authorizations_live/index.ex"
      to: "Lockspire.Admin.list_device_authorizations/1"
      via: "existing read path"
      pattern: "Admin\\.list_device_authorizations"
    - from: "lib/lockspire/web/live/admin/device_authorizations_live/index.ex"
      to: "Lockspire.Web.Components.AdminComponents"
      via: "dense_resource_row, status_badge with device domain, long_value"
      pattern: "AdminComponents\\.(dense_resource_row|status_badge|long_value)"
---

<objective>
Polish the device authorization queue so operators can understand pending, approved, denied, expired, and consumed device-flow pressure without revealing device/user codes or implying approval controls.

Purpose: Deliver OPERATE-01, OPERATE-02, and OPERATE-03 for `/admin/device_authorizations` using existing domain fields and existing AdminComponents primitives per D-01, D-02, D-03, D-04, D-05, D-07, D-08, D-11, D-12, D-13, D-14, D-15, D-17, D-18, D-21, D-22, and D-24.
Output: Focused device authorization LiveView tests and a page-local pressure-first implementation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-operate-queue-flow-polish/123-CONTEXT.md
@.planning/phases/123-operate-queue-flow-polish/123-RESEARCH.md
@.planning/phases/123-operate-queue-flow-polish/123-PATTERNS.md
@.planning/phases/123-operate-queue-flow-polish/123-UI-SPEC.md
@.planning/phases/123-operate-queue-flow-polish/123-VALIDATION.md
@.planning/phases/122-support-investigation-flow-polish/122-01-SUMMARY.md
@lib/lockspire/web/admin_router.ex
@lib/lockspire/web/live/admin/device_authorizations_live/index.ex
@lib/lockspire/domain/device_authorization.ex
@lib/lockspire/admin.ex
@lib/lockspire/web/components/admin_components.ex
@lib/lockspire/web/admin_css.ex
@test/lockspire/web/live/admin/device_authorizations_live_test.exs
@test/support/lockspire/web/admin_proof/html_assertions.ex
</context>

## Artifacts this phase produces

- `device_authorization_pressure/1`, `device_authorization_title/1`, `device_activity_context/1`, and `redacted_authorization_handle/1` or equivalent private helpers inside `lib/lockspire/web/live/admin/device_authorizations_live/index.ex`.
- Expanded rendered device authorization tests in `test/lockspire/web/live/admin/device_authorizations_live_test.exs`.
- No new route, no new component module, no public Admin API, no schema file, no migration file, no package dependency, and no browser tooling artifact.

<tasks>

<task id="123-02-01" type="auto" tdd="true">
  <name>Task 123-02-01: Add device authorization pressure and secrecy tests</name>
  <files>test/lockspire/web/live/admin/device_authorizations_live_test.exs</files>
  <read_first>
    - .planning/phases/123-operate-queue-flow-polish/123-CONTEXT.md
    - .planning/phases/123-operate-queue-flow-polish/123-UI-SPEC.md
    - .planning/phases/123-operate-queue-flow-polish/123-VALIDATION.md
    - .planning/phases/123-operate-queue-flow-polish/123-PATTERNS.md
    - lib/lockspire/web/live/admin/device_authorizations_live/index.ex
    - lib/lockspire/domain/device_authorization.ex
    - test/lockspire/web/live/admin/device_authorizations_live_test.exs
    - test/support/lockspire/web/admin_proof/html_assertions.ex
  </read_first>
  <behavior>
    - Rendered rows expose redacted client, redacted subject/account, redacted durable authorization handle, status, expiry, poll interval, next poll or lifecycle activity, and pressure copy.
    - Fixtures cover pending, approved, denied, expired, consumed, dense rows, and long handle/client/account values.
    - Rendered HTML denies raw device-flow and backend fields: raw device code, raw user code, hashes, raw verification handle, authorization code, token material, PKCE material, state, nonce, raw params, and storage detail terms.
    - Rendered HTML contains no table surface and no interactive queue command events.
  </behavior>
  <action>
    Expand `device_authorizations_live_test.exs` before production edits. Seed multiple `DeviceAuthorization` records through the existing repository path with artificial values for pending, approved, denied, expired, and consumed states. Include poll interval and next-poll/lifecycle fields already present on `Lockspire.Domain.DeviceAuthorization`; do not expect `updated_at` on the domain struct. Extend rendered assertions for UI-SPEC pressure phrases, redacted client/account/authorization handle display, expiry, poll/activity context, status buckets, `lockspire-admin-dense-resource-row`, `lockspire-admin-long-value`, no table surface, no interactive event attributes, no generic CTA text, no duplicate IDs, and no unresolved described-by targets. Add denial assertions for raw code/hash/verification-handle and backend-storage terms per D-11 and D-18. Use the UI-SPEC unsupported-control contract without adding those labels to production code.
  </action>
  <acceptance_criteria>
    - `test/lockspire/web/live/admin/device_authorizations_live_test.exs` includes fixtures or assigns for `:pending`, `:approved`, `:denied`, `:expired`, and `:consumed`.
    - The rendered test asserts `Pending`, `Approved`, `Denied`, `Expired`, `Completed`, `Client`, `Subject`, `Handle`, `Expires`, poll interval or next-poll text, and device-flow pressure copy.
    - The rendered test asserts `lockspire-admin-dense-resource-row`, `lockspire-admin-long-value`, and absence of `<table`, `lockspire-admin-table-wrap`, `phx-click`, and `phx-submit`.
    - The rendered test denies `device_code`, `user_code`, raw hashes, `verification_handle`, `authorization_code`, token material, `pkce`, `state`, `nonce`, `raw params`, and backend storage detail terms.
    - Running the focused command after this task fails only on missing planned device authorization pressure/rendering behavior.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/web/live/admin/device_authorizations_live_test.exs --max-failures 1</automated>
  </verify>
  <done>Device authorization queue RED coverage exists for OPERATE-01, OPERATE-02, and OPERATE-03 without syntax/setup failures.</done>
</task>

<task id="123-02-02" type="auto" tdd="true">
  <name>Task 123-02-02: Implement pressure-first device authorization rows</name>
  <files>lib/lockspire/web/live/admin/device_authorizations_live/index.ex</files>
  <read_first>
    - .planning/phases/123-operate-queue-flow-polish/123-CONTEXT.md
    - .planning/phases/123-operate-queue-flow-polish/123-UI-SPEC.md
    - .planning/phases/123-operate-queue-flow-polish/123-PATTERNS.md
    - lib/lockspire/web/live/admin/device_authorizations_live/index.ex
    - lib/lockspire/web/live/admin/logout_deliveries_live/index.ex
    - lib/lockspire/domain/device_authorization.ex
    - lib/lockspire/admin.ex
    - lib/lockspire/web/components/admin_components.ex
    - lib/lockspire/web/admin_css.ex
    - test/lockspire/web/live/admin/device_authorizations_live_test.exs
  </read_first>
  <behavior>
    - The page spine remains `page_hero`, `pane`, `metric_grid`, `resource_list`, `dense_resource_row`, `status_badge`, `long_value`, and `empty_state`.
    - Row subtitles communicate pending, approved waiting, denied, expired, or consumed pressure.
    - Poll/activity copy uses `effective_poll_interval_seconds`, `next_poll_allowed_at`, `approved_at`, `denied_at`, `consumed_at`, `expired_at`, and `expires_at`; it does not use `auth.updated_at`.
    - The LiveView has no `handle_event/3`, no approval/denial command controls, and no storage/schema/protocol changes.
  </behavior>
  <action>
    Update `device_authorizations_live/index.ex` with page-local private helpers for row title, pressure copy, durable redacted authorization handle, poll/activity context, redacted handles, and timestamp formatting. Keep `Admin.list_device_authorizations()` as the existing read path per D-08. Render rows with `dense_resource_row`; title should identify device authorization state, subtitle should explain device-flow pressure, meta should include redacted client, redacted subject, redacted durable authorization handle, expiry, and poll/activity context through `long_value`, and status should use `status_badge` with `domain={:device_authorization}`. Use only the domain fields named in the UI-SPEC and D-11. Keep empty-state copy from the UI-SPEC. Do not add route, API, event handler, queue mutation, raw code/hash field, `auth.updated_at`, storage schema, CSS, shared component, public support-surface, or host-seam changes per D-01, D-02, D-03, D-05, D-06, D-11, D-12, D-16, and D-23.
  </action>
  <acceptance_criteria>
    - `lib/lockspire/web/live/admin/device_authorizations_live/index.ex` still calls `Admin.list_device_authorizations`.
    - The source contains private helper names or equivalent logic for device authorization pressure and poll/activity context.
    - The source contains `effective_poll_interval_seconds`, `next_poll_allowed_at`, and lifecycle timestamp handling where activity copy is built.
    - The source contains `AdminComponents.dense_resource_row`, `AdminComponents.status_badge`, `AdminComponents.long_value`, and `AdminComponents.empty_state`.
    - The source does not contain `auth.updated_at`, `def handle_event`, `phx-click`, `phx-submit`, `responsive_table`, `lockspire-admin-table-wrap`, raw `device_code`, raw `user_code`, or raw `verification_handle` rendering.
    - Focused device authorization tests pass.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/web/live/admin/device_authorizations_live_test.exs --max-failures 1</automated>
    <automated>mix format --check-formatted lib/lockspire/web/live/admin/device_authorizations_live/index.ex test/lockspire/web/live/admin/device_authorizations_live_test.exs</automated>
  </verify>
  <done>`/admin/device_authorizations` renders pressure-first, redaction-safe, non-table rows and remains a read-only support-review surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host app -> AdminRouter | Host-owned operator authentication and tenant policy remain outside Lockspire. |
| LiveView -> Admin API | Device authorization data is read through the existing `Lockspire.Admin` boundary. |
| Device authorization struct -> rendered HTML | Device-flow fields include sensitive code/hash material adjacent to safe display fields. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Information Disclosure | `DeviceAuthorizationsLive.Index` rows | mitigate | Render only D-11 allowed fields, redacted handles, and `long_value`; add rendered forbidden-text assertions. |
| T-123-02 | Elevation of Privilege | `/admin/device_authorizations` controls | mitigate | Keep page read-only: no `handle_event/3`, LiveView command attributes, mutation labels, new routes, or new Admin delegates. |
| T-123-04 | Denial of Service | Long handles/client/account values | mitigate | Use `dense_resource_row`, `long_value`, existing wrapping CSS, and rendered no-table/no-overflow proxies. |
| T-123-05 | Information Disclosure | Status rendering | mitigate | Use text status labels and pressure copy; do not rely on color alone. |
| T-123-SC | Tampering | package installs | mitigate | No package-manager install tasks; RESEARCH.md package audit says no external packages are needed. |
</threat_model>

<verification>
Focused:
- `MIX_ENV=test mix test test/lockspire/web/live/admin/device_authorizations_live_test.exs --max-failures 1`
- `mix format --check-formatted lib/lockspire/web/live/admin/device_authorizations_live/index.ex test/lockspire/web/live/admin/device_authorizations_live_test.exs`

Plan handoff:
- This plan modifies no schema-relevant files (`priv/repo/migrations/*.exs`, Ecto schemas, storage records), so no schema push or migration task is required.
</verification>

<success_criteria>
`/admin/device_authorizations` lets an operator scan status pressure, redacted client/account, durable non-secret authorization handle, expiry, poll/activity context, and terminal states without table overload, unsupported controls, raw code/hash exposure, or host-boundary expansion.
</success_criteria>

<output>
Create `.planning/phases/123-operate-queue-flow-polish/123-02-SUMMARY.md` when done.
</output>
