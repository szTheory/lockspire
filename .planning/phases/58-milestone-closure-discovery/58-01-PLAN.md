---
phase: 58-milestone-closure-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/discovery.ex
  - test/lockspire/protocol/discovery_test.exs
  - docs/rar-consent-host-guide.md
  - docs/install-and-onboard.md
  - docs/supported-surface.md
  - README.md
  - mix.exs
  - test/lockspire/release_readiness_contract_test.exs
  - .planning/phases/58-milestone-closure-discovery/58-VERIFICATION.md
autonomous: true
requirements:
  - META-01
  - META-02
  - DOC-01
must_haves:
  truths:
    - "Discovery publishes `resource_indicators_supported: true` only when the effective authorization/token surface that handles `resource` is actually mounted and usable."
    - "Discovery publishes `authorization_details_types_supported` only when the RAR-capable authorization surface is usable and `Lockspire.Config.rar_types_supported/0` is non-empty."
    - "Discovery omits `authorization_details_types_supported` instead of publishing `[]` when no RAR types are configured."
    - "The public docs show a concrete host-owned customization path for RAR consent using the existing generated seam and structural `authorization_details` data."
    - "README, supported-surface docs, HexDocs wiring, and release-readiness contract all agree on the shipped v1.14 Resource Indicators + RAR closure surface."
    - "Phase 58 leaves a verification handoff proving the live planning set is ready for `$gsd-complete-milestone`, while not trying to archive milestone snapshots itself."
  artifacts:
    - path: "lib/lockspire/protocol/discovery.ex"
      provides: "Shared truth predicates and published metadata for Resource Indicators and RAR supported types."
    - path: "test/lockspire/protocol/discovery_test.exs"
      provides: "Contract tests for metadata presence/omission across mounted-surface and config states."
    - path: "docs/rar-consent-host-guide.md"
      provides: "Executable host-owned custom RAR consent walkthrough with one illustrative `payment_initiation` example."
    - path: "test/lockspire/release_readiness_contract_test.exs"
      provides: "Repo-owned drift fence for Phase 58 public claims."
    - path: ".planning/phases/58-milestone-closure-discovery/58-VERIFICATION.md"
      provides: "Phase-level closure proof and explicit handoff to `$gsd-complete-milestone`."
  key_links:
    - from: "lib/lockspire/protocol/discovery.ex"
      to: "lib/lockspire/config.ex"
      via: "configured RAR-type truth"
      pattern: "rar_types_supported"
    - from: "lib/lockspire/protocol/discovery.ex"
      to: "mounted route metadata"
      via: "shared capability predicate for `/authorize` and `/token`"
      pattern: "authorization_endpoint|token_endpoint|resource_indicators_supported|authorization_details_types_supported"
    - from: "docs/rar-consent-host-guide.md"
      to: "lib/lockspire/web/live/consent_live.ex"
      via: "host seam explanation built on current structural consent data"
      pattern: "authorization_details|authorization_detail_types|payment_initiation"
    - from: "test/lockspire/release_readiness_contract_test.exs"
      to: "README.md, docs/supported-surface.md, docs/install-and-onboard.md, docs/rar-consent-host-guide.md, mix.exs"
      via: "public-claim and docs-publication contract checks"
      pattern: "resource_indicators_supported|authorization_details_types_supported|RAR|consent"
---

<objective>
Close v1.14 truthfully by advertising the shipped Resource Indicators and RAR discovery surface, documenting the host-owned custom RAR consent seam with executable repo-backed guidance, and aligning claim-bearing docs/tests for milestone verification.

Purpose: Satisfy `META-01`, `META-02`, and `DOC-01` without widening Lockspire's product shape. Phase 58 is a closure pass over discovery truth, host guidance, and repo-proof integrity, not a new protocol implementation slice.
Output: One narrow execute plan covering discovery metadata, executable RAR consent docs, support-contract updates, and a phase verification handoff ready for later archival.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/58-milestone-closure-discovery/58-CONTEXT.md
@.planning/phases/58-milestone-closure-discovery/58-RESEARCH.md
@.planning/phases/57-rar-introspection-and-verification/57-01-SUMMARY.md
@lib/lockspire/protocol/discovery.ex
@lib/lockspire/config.ex
@lib/lockspire/web/live/consent_live.ex
@test/lockspire/protocol/discovery_test.exs
@test/integration/phase57_rar_introspection_verification_e2e_test.exs
@README.md
@docs/install-and-onboard.md
@docs/supported-surface.md
@mix.exs
@test/lockspire/release_readiness_contract_test.exs

<interfaces>
From `lib/lockspire/protocol/discovery.ex`:
```elixir
@spec openid_configuration() :: map()
def openid_configuration do
  issuer = Config.issuer!()
  endpoint_metadata = mounted_endpoint_metadata()
  ...
  |> Map.merge(endpoint_metadata)
  |> maybe_put_dpop_metadata(endpoint_metadata)
  |> maybe_put_ciba_metadata(endpoint_metadata)
  |> put_bcl_fcl_metadata()
  |> put_iss_parameter_metadata()
  |> maybe_put_par_required_metadata()
end
```

From `lib/lockspire/config.ex`:
```elixir
@spec rar_types_supported() :: [String.t()]
def rar_types_supported do
  rar_validators()
  |> Map.keys()
  |> Enum.sort()
end
```

From `lib/lockspire/web/live/consent_live.ex`:
```elixir
authorization_details: interaction.authorization_details,
authorization_detail_types: authorization_detail_types(interaction.authorization_details),
```

From `mix.exs`:
```elixir
docs: [
  extras: [...],
  groups_for_extras: [...]
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish truthful discovery metadata for Resource Indicators and RAR types</name>
  <read_first>
    - lib/lockspire/protocol/discovery.ex
    - lib/lockspire/config.ex
    - test/lockspire/protocol/discovery_test.exs
    - .planning/phases/58-milestone-closure-discovery/58-CONTEXT.md
    - .planning/phases/58-milestone-closure-discovery/58-RESEARCH.md
  </read_first>
  <files>lib/lockspire/protocol/discovery.ex, test/lockspire/protocol/discovery_test.exs</files>
  <behavior>
    - `resource_indicators_supported` is published only when the effective authorization and token endpoints are both mounted so the host can actually request and redeem targeted resources.
    - `authorization_details_types_supported` is published only when the same usable authorization surface exists and `Lockspire.Config.rar_types_supported/0` returns a non-empty list.
    - Empty configured type lists omit the key rather than publishing `[]`.
    - The truth predicates are shared internally rather than duplicated across one-off branches.
  </behavior>
  <action>Extend `Lockspire.Protocol.Discovery` with one or more small private helpers that treat the mounted authorization and token surface as the truth gate for both Resource Indicators and RAR metadata. Keep the pattern consistent with existing conditional discovery fields like DPoP, registration, and FAPI. Reuse `Lockspire.Config.rar_types_supported/0` directly as the configured type source, and only merge `authorization_details_types_supported` when the list is non-empty and the mounted-capability predicate passes. Add discovery tests that cover: the default fully mounted surface publishes `resource_indicators_supported: true`; the default fully mounted surface publishes configured RAR types in sorted order when validators exist; a token-only or otherwise incomplete router omits both new keys; and an empty `:rar_validators` config omits `authorization_details_types_supported` while leaving `resource_indicators_supported` truthful. Do not add controller logic or route probing outside `Discovery`.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/protocol/discovery_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "resource_indicators_supported|authorization_details_types_supported" lib/lockspire/protocol/discovery.ex test/lockspire/protocol/discovery_test.exs` finds implementation and tests for both keys.
    - The discovery test command passes.
    - No test asserts `authorization_details_types_supported == []`.
  </acceptance_criteria>
  <done>Discovery truthfully advertises the new v1.14 metadata only when the mounted/configured host surface can really support it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the executable host-owned RAR consent guide and wire it into HexDocs/onboarding</name>
  <read_first>
    - docs/install-and-onboard.md
    - lib/lockspire/web/live/consent_live.ex
    - test/integration/phase57_rar_introspection_verification_e2e_test.exs
    - .planning/phases/58-milestone-closure-discovery/58-CONTEXT.md
    - .planning/phases/58-milestone-closure-discovery/58-RESEARCH.md
  </read_first>
  <files>docs/rar-consent-host-guide.md, docs/install-and-onboard.md, mix.exs</files>
  <behavior>
    - The guide is narrow, host-owned, and executable: concrete file targets, copy-pasteable snippets, and verification pointers.
    - The guide shows one illustrative `payment_initiation` rendering example built on normalized `authorization_details`, while clearly stating that semantics, copy, and branding belong to the host.
    - The guide does not introduce a renderer registry, Lockspire-owned semantic UI, or a sample host app.
    - HexDocs and onboarding make the guide discoverable.
  </behavior>
  <action>Create a dedicated doc file, preferably `docs/rar-consent-host-guide.md`, that explains how a Phoenix host customizes the generated consent seam after `mix lockspire.install`. Point directly at the generated host-owned consent files and the structural data already available from Lockspire, using one `payment_initiation` example that converts normalized `authorization_details` into human copy through an ordinary helper/component pattern. Include a short verification section that tells maintainers what repo proof already exists and what a host should test locally. Update `docs/install-and-onboard.md` to link to this guide at the moment consent/UI ownership is discussed, and update `mix.exs` `extras` / `groups_for_extras` so the guide is published in HexDocs. Keep the tone strict about boundaries: illustrative example only, host-owned semantics, Lockspire owns protocol validity and final redirect.</action>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `docs/rar-consent-host-guide.md` exists and references `payment_initiation`, host-owned customization, and `authorization_details`.
    - `rg -n "rar-consent-host-guide" docs/install-and-onboard.md mix.exs` shows onboarding and HexDocs wiring.
    - `mix docs --warnings-as-errors` passes.
    - No new sample app, behaviour, or renderer registry is introduced.
  </acceptance_criteria>
  <done>The repo publishes a focused, executable RAR host-consent guide that is easy to find and aligned with Lockspire's embedded-library boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Align public claims, release-readiness proof, and phase verification handoff</name>
  <read_first>
    - README.md
    - docs/supported-surface.md
    - test/lockspire/release_readiness_contract_test.exs
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/29-truthful-discovery-security-docs-and-milestone-closure/29-VERIFICATION.md
    - .planning/phases/36-end-to-end-proof-and-milestone-closure/36-VERIFICATION.md
  </read_first>
  <files>README.md, docs/supported-surface.md, test/lockspire/release_readiness_contract_test.exs, .planning/phases/58-milestone-closure-discovery/58-VERIFICATION.md</files>
  <behavior>
    - README and supported-surface docs truthfully describe the newly shipped Resource Indicators and RAR consent/discovery surface without implying broader vertical support.
    - Release-readiness assertions pin the new public claims and the existence/wiring of the RAR host guide so docs cannot drift silently.
    - Phase 58 leaves a verification artifact showing the live planning set is ready for milestone completion, while explicitly delegating archive-snapshot creation to `$gsd-complete-milestone`.
  </behavior>
  <action>Update `README.md` and `docs/supported-surface.md` to mention the shipped Resource Indicators support, truthful discovery metadata for Resource Indicators and configured RAR types, and the existence of the new host-owned RAR consent guide. Keep wording narrow and structural; do not claim built-in semantic consent UI or a general payments product surface. Extend `test/lockspire/release_readiness_contract_test.exs` so it asserts the new guide is listed in HexDocs, onboarding points to it, and the top-level support contract mentions the new truthful v1.14 surfaces. Then add `.planning/phases/58-milestone-closure-discovery/58-VERIFICATION.md` summarizing the phase goal, evidence, requirement closure for `META-01`, `META-02`, and `DOC-01`, and an explicit note that milestone archive snapshots are created afterward by `$gsd-complete-milestone`, not by this plan.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/release_readiness_contract_test.exs --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "resource indicators|authorization_details_types_supported|rar-consent-host-guide|custom RAR consent" README.md docs/supported-surface.md test/lockspire/release_readiness_contract_test.exs` finds aligned public claims and contract assertions.
    - `58-VERIFICATION.md` exists and includes a handoff note to `$gsd-complete-milestone`.
    - The release-readiness contract test passes.
    - `SECURITY.md` is unchanged unless execution finds a real support-boundary mismatch that must be corrected.
  </acceptance_criteria>
  <done>The public support contract, repo-owned drift tests, and phase-level verification all agree on the closed v1.14 surface and are ready for milestone completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public discovery document -> integrators | Unauthenticated clients infer supported capabilities from discovery metadata. |
| repo docs -> host implementer | Host teams rely on docs to shape real consent UX and supported-surface expectations. |
| public claims -> release contract tests | Release-readiness assertions guard against docs and discovery drifting apart. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-58-01 | I | `lib/lockspire/protocol/discovery.ex` | mitigate | Publish new metadata only when the mounted authorization/token surface is truly usable and omit empty RAR-type lists. |
| T-58-02 | T | docs / README support claims | mitigate | Keep claims bound to repo-owned proof and extend `release_readiness_contract_test.exs` to pin them. |
| T-58-03 | E | `docs/rar-consent-host-guide.md` | mitigate | Keep the guide host-owned and structural; do not introduce new Lockspire-owned semantic rendering or policy seams. |
| T-58-04 | R | milestone closure handoff | mitigate | Add `58-VERIFICATION.md` with explicit evidence and an explicit boundary that archival is the next workflow's job. |
</threat_model>

<verification>
- `MIX_ENV=test mix test test/lockspire/protocol/discovery_test.exs --warnings-as-errors`
- `mix docs --warnings-as-errors`
- `MIX_ENV=test mix test test/lockspire/release_readiness_contract_test.exs --warnings-as-errors`
</verification>

<success_criteria>
- `.well-known/openid-configuration` truthfully includes `resource_indicators_supported` and, when configured, `authorization_details_types_supported`.
- The repo contains a focused, executable host-owned custom RAR consent guide that is published in HexDocs and linked from onboarding.
- README, supported-surface docs, and release-readiness contract tests all agree on the shipped v1.14 surface.
- Phase 58 leaves a verification artifact ready for milestone completion without trying to perform archival itself.
</success_criteria>

<output>
After completion, create `.planning/phases/58-milestone-closure-discovery/58-01-SUMMARY.md`
</output>
