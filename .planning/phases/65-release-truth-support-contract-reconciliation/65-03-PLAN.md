---
phase: 65-release-truth-support-contract-reconciliation
plan: 65-03
type: execute
wave: 3
depends_on:
  - 65-01
  - 65-02
files_modified:
  - docs/maintainer-release.md
  - test/lockspire/release_readiness_contract_test.exs
autonomous: true
requirements:
  - TRUTH-01
  - TRUTH-02
must_haves:
  truths:
    - "Maintainer release guidance is clearly secondary to the canonical public support contract and stays focused on release operations plus evidence boundaries."
    - "Release-readiness contract tests fail when package metadata, changelog posture, release workflow truth, README, SECURITY, and the canonical support contract drift apart."
    - "Executable drift fences prevent shadow support contracts and claims that outrun repo-owned proof."
  artifacts:
    - path: docs/maintainer-release.md
      provides: "maintainer-only release contract aligned with canonical public support truth"
    - path: test/lockspire/release_readiness_contract_test.exs
      provides: "cross-file executable drift fence for version truth, contract hierarchy, and proof boundaries"
  key_links:
    - from: docs/maintainer-release.md
      to: docs/supported-surface.md
      via: "maintainer runbook defers public support scope to the canonical contract"
      pattern: "supported-surface"
    - from: test/lockspire/release_readiness_contract_test.exs
      to: mix.exs
      via: "tests parse or assert package version truth against docs and manifest"
      pattern: "version|manifest|changelog"
---

<objective>
Turn Phase 65 release truth into an executable cross-file contract.

Purpose: The first two plans align artifacts and public docs, but the phase is incomplete unless the maintainer runbook and the checked-in test fence enforce that hierarchy. This plan makes `docs/maintainer-release.md` secondary to the canonical support contract per D-08 and extends `test/lockspire/release_readiness_contract_test.exs` so future drift is caught automatically per D-13 through D-16.
Output: a maintainer-only release guide that does not become a shadow support contract, plus final release-contract test tightening that binds maintainer guidance and public-doc hierarchy into the artifact/workflow drift fence established in 65-01.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/65-release-truth-support-contract-reconciliation/65-CONTEXT.md
@.planning/phases/65-release-truth-support-contract-reconciliation/65-RESEARCH.md
@.planning/phases/65-release-truth-support-contract-reconciliation/65-01-PLAN.md
@.planning/phases/65-release-truth-support-contract-reconciliation/65-02-PLAN.md
@docs/maintainer-release.md
@docs/supported-surface.md
@README.md
@SECURITY.md
@mix.exs
@.release-please-manifest.json
@CHANGELOG.md
@release-please-config.json
@.github/workflows/release.yml
@.github/actions/release-please/action.yml
@test/lockspire/release_readiness_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Narrow the maintainer guide to release operations and evidence boundaries only</name>
  <files>docs/maintainer-release.md</files>
  <read_first>
    - docs/maintainer-release.md
    - docs/supported-surface.md
    - .github/workflows/release.yml
    - .planning/phases/65-release-truth-support-contract-reconciliation/65-CONTEXT.md
  </read_first>
  <action>Update `docs/maintainer-release.md` so it remains a maintainer-only operations document per D-08. It should explain the repo-owned proof bucket, the GitHub-settings proof bucket, and the workflow-run proof bucket without redefining the public support surface. Make the guide explicit that `docs/supported-surface.md` is authoritative for public claims, the release workflow is authoritative for checked-in automation truth, and live environment evidence remains secondary per D-13 and D-14.</action>
  <acceptance_criteria>
    - `docs/maintainer-release.md` points to `docs/supported-surface.md` as the public authority
    - the guide clearly distinguishes repo-owned proof, GitHub settings proof, and workflow-run proof
    - the guide does not add a second public feature inventory or broader product posture
    - maintainer-only release operations remain tied to the protected `hex-publish` lane
  </acceptance_criteria>
  <verify>
    <automated>rg 'supported-surface|Repo-owned proof|GitHub settings proof|Workflow-run proof|hex-publish' docs/maintainer-release.md</automated>
  </verify>
  <done>The maintainer guide explains release operations and evidence boundaries without becoming a second support contract.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the release-readiness contract test into a cross-file drift fence</name>
  <files>test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - test/lockspire/release_readiness_contract_test.exs
    - mix.exs
    - .release-please-manifest.json
    - CHANGELOG.md
    - README.md
    - docs/supported-surface.md
    - SECURITY.md
    - docs/maintainer-release.md
    - .github/workflows/release.yml
  </read_first>
  <action>Finalize the D-16 drift fence on top of the 65-01 baseline. Add or tighten assertions that `docs/supported-surface.md` is the canonical contract while README and SECURITY remain subordinate, that `docs/maintainer-release.md` is maintainer-facing and not a second support contract, that `0.x` history remains intact, and that no document claims a broader host shape, release readiness, or protocol surface than the repo names as proof today. Preserve the workflow/config assertions added in 65-01 and extend them only where the maintainer-guide or doc-hierarchy work needs more coverage. Where useful, add small helpers to parse the version and newest changelog heading instead of relying only on substring checks.</action>
  <acceptance_criteria>
    - `test/lockspire/release_readiness_contract_test.exs` compares package version truth across `mix.exs`, `.release-please-manifest.json`, and `CHANGELOG.md`
    - the test suite asserts `docs/supported-surface.md` is canonical and README/SECURITY are subordinate
    - the test suite asserts the maintainer guide does not define a second support contract
    - the test suite asserts workflow/config contract consistency across `release-please-config.json`, `.github/workflows/release.yml`, and `.github/actions/release-please/action.yml`
    - the test suite fails if `0.x` history is rewritten or if claims outrun named repo-owned proof
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <done>Release truth is now executable: future drift across metadata, docs, workflow, and support hierarchy fails in tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer docs -> public support posture | Maintainer guidance can accidentally become a shadow public contract |
| test fence -> release truth | If executable checks stay too narrow, contradictions can reappear unnoticed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-03-01 | R | maintainer guide | mitigate | Keep `docs/maintainer-release.md` explicitly subordinate to `docs/supported-surface.md` and focused on evidence buckets only |
| T-65-03-02 | R | release_readiness_contract_test.exs | mitigate | Add cross-file assertions for version truth, changelog posture, contract hierarchy, and repo-owned proof boundaries |
| T-65-03-03 | I | support claims | mitigate | Fail tests when README, SECURITY, or maintainer docs broaden claims beyond the canonical contract or named proof |
</threat_model>

<verification>
Run the release-readiness contract test after updating the maintainer guide and again after the cross-file assertions land. Completion requires the test to prove the hierarchy, not merely mention the files.
</verification>

<success_criteria>
- The maintainer guide is clearly secondary to the canonical public support contract.
- The release-readiness contract test enforces version truth, support hierarchy, and proof boundaries across files.
- Phase 65 cannot silently regress into mixed `0.x` versus `1.0.0` posture or shadow support contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/65-release-truth-support-contract-reconciliation/65-03-SUMMARY.md`
</output>
