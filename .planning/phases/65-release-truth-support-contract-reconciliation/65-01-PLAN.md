---
phase: 65-release-truth-support-contract-reconciliation
plan: 65-01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/lockspire/release_readiness_contract_test.exs
  - mix.exs
  - .release-please-manifest.json
  - CHANGELOG.md
  - release-please-config.json
  - .github/workflows/release.yml
  - .github/actions/release-please/action.yml
autonomous: true
requirements:
  - TRUTH-01
must_haves:
  truths:
    - "Package metadata, release manifest state, and changelog posture agree on one public release baseline."
    - "The next protected publish from the repo-controlled release lane is the first authoritative `1.0.0` artifact unless execution discovers a material proof gap and explicitly falls back to truthful `0.x` posture per D-04."
    - "Checked-in release automation tells the same artifact-first story as the package and changelog files."
  artifacts:
    - path: mix.exs
      provides: "authoritative package version truth for the next release"
    - path: .release-please-manifest.json
      provides: "Release Please tracked current version aligned with package truth"
    - path: CHANGELOG.md
      provides: "factual `0.x` history plus explicit `1.0.0` transition posture"
    - path: .github/workflows/release.yml
      provides: "protected publish-lane contract for the authoritative release"
  key_links:
    - from: mix.exs
      to: .release-please-manifest.json
      via: "same semantic version for the next authoritative release"
      pattern: "1\\.0\\.0|0\\.[0-9]+\\.[0-9]+"
    - from: CHANGELOG.md
      to: .github/workflows/release.yml
      via: "release notes and protected publish lane describe the same artifact-first release posture"
      pattern: "1\\.0\\.0|hex-publish|release.preflight"
---

<objective>
Reconcile Lockspire's package and release artifacts around one truthful public version story.

Purpose: TRUTH-01 fails today because `mix.exs`, the Release Please manifest, and the changelog still reflect `0.x` while public docs already speak in `1.0.0` terms. This plan makes the next protected publish the first authoritative `1.0.0` artifact per D-01, D-02, and D-03, while preserving factual `0.x` history per D-10 through D-12.
Output: aligned package version metadata, aligned Release Please manifest state, an explicit `1.0.0` changelog transition entry, and checked-in release-lane wording that matches the artifact-first story.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/METHODOLOGY.md
@.planning/phases/65-release-truth-support-contract-reconciliation/65-CONTEXT.md
@.planning/phases/65-release-truth-support-contract-reconciliation/65-RESEARCH.md
@.planning/phases/47-1.0-ga-release-readiness/47-01-SUMMARY.md
@mix.exs
@release-please-config.json
@.release-please-manifest.json
@CHANGELOG.md
@test/lockspire/release_readiness_contract_test.exs
@.github/workflows/release.yml
@.github/actions/release-please/action.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the release-truth drift fence before changing artifact posture</name>
  <files>test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - test/lockspire/release_readiness_contract_test.exs
    - mix.exs
    - .release-please-manifest.json
    - CHANGELOG.md
    - release-please-config.json
    - .github/workflows/release.yml
    - .github/actions/release-please/action.yml
    - .planning/phases/65-release-truth-support-contract-reconciliation/65-VALIDATION.md
  </read_first>
  <action>Strengthen `test/lockspire/release_readiness_contract_test.exs` before any release-posture edits land so Wave 1 sampling is meaningful. Add explicit assertions that compare `mix.exs`, `.release-please-manifest.json`, the newest `CHANGELOG.md` heading, `release-please-config.json`, `.github/workflows/release.yml`, and `.github/actions/release-please/action.yml` as one checked-in release-truth story. The strengthened baseline only needs to cover artifact-version agreement and workflow/config contract integrity needed for 65-01 and 65-02; 65-03 can add the final maintainer-guide and doc-hierarchy tightening afterward.</action>
  <acceptance_criteria>
    - `test/lockspire/release_readiness_contract_test.exs` compares package version truth across `mix.exs`, `.release-please-manifest.json`, and `CHANGELOG.md`
    - the test suite asserts workflow/config contract consistency across `release-please-config.json`, `.github/workflows/release.yml`, and `.github/actions/release-please/action.yml`
    - the test suite still passes against the pre-edit repo state before artifact posture changes begin
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <done>Wave 1 now starts with an executable drift fence that can validate artifact and workflow truth changes as they land.</done>
</task>

<task type="auto">
  <name>Task 2: Converge package version truth on the first authoritative `1.0.0` artifact</name>
  <files>mix.exs, .release-please-manifest.json, CHANGELOG.md</files>
  <read_first>
    - mix.exs
    - .release-please-manifest.json
    - CHANGELOG.md
    - test/lockspire/release_readiness_contract_test.exs
    - .planning/phases/65-release-truth-support-contract-reconciliation/65-CONTEXT.md
    - .planning/phases/65-release-truth-support-contract-reconciliation/65-RESEARCH.md
  </read_first>
  <action>Update `mix.exs` and `.release-please-manifest.json` so the repo's next release artifact is `1.0.0` per D-01 and D-02, not an indefinite `0.x` package with `1.0.0` docs. Add a new `1.0.0` changelog section that explicitly states the GA support contract becomes authoritative with this release per D-11, while preserving the published `0.1.x` and `0.2.0` chronology verbatim per D-10 and D-12. Before finalizing, validate the checked-in proof surfaces named in D-13 still support the `1.0.0` claim; if execution uncovers a material proof gap large enough to make `1.0.0` dishonest, do not invent limbo language and instead route the touched release artifacts back to truthful `0.x` posture per D-04 in the same change set.</action>
  <acceptance_criteria>
    - `mix.exs` declares the same next public version as `.release-please-manifest.json`
    - `CHANGELOG.md` adds either a `1.0.0` entry or an explicit fallback-aligned `0.x` correction in the same change set, not a `1.0.0-rc` or "GA-ready" posture
    - `CHANGELOG.md` preserves existing `0.1.1`, `0.1.2`, and `0.2.0` entries unchanged in chronology
    - the release note makes the release-truth shift explicit for whichever allowed posture is chosen instead of relying on earlier overstated docs
  </acceptance_criteria>
  <verify>
    <automated>VERSION=$(sed -n 's/.*version: "\\([0-9][0-9.]*\\)".*/\\1/p' mix.exs | head -1) && MANIFEST=$(sed -n 's/.*"\\.": "\\([0-9][0-9.]*\\)".*/\\1/p' .release-please-manifest.json | head -1) && test -n "$VERSION" && test "$VERSION" = "$MANIFEST" && rg "^## \\[$VERSION\\]" CHANGELOG.md && ! rg '1\\.0\\.0-rc|GA-ready' CHANGELOG.md</automated>
  </verify>
  <done>The package file, manifest, and changelog now tell one release story and preserve factual `0.x` history.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the repo-controlled release lane aligned with the artifact-first `1.0.0` posture</name>
  <files>release-please-config.json, .github/workflows/release.yml, .github/actions/release-please/action.yml</files>
  <read_first>
    - release-please-config.json
    - .github/workflows/release.yml
    - .github/actions/release-please/action.yml
    - docs/maintainer-release.md
    - test/lockspire/release_readiness_contract_test.exs
    - test/lockspire/release_readiness_contract_test.exs
  </read_first>
  <action>Adjust checked-in release automation only where needed so it describes the same first-authoritative-`1.0.0` story as the package artifacts. Preserve the protected `hex-publish` lane, the repo-controlled composite Release Please action, and recovery-only `workflow_dispatch` posture from D-13 and D-14. Remove any wording that leaves a preview or ambiguous pre-GA contract in the release lane, but do not broaden the release story into live GitHub settings guarantees that exist outside git.</action>
  <acceptance_criteria>
    - `release-please-config.json` remains Elixir manifest-mode configuration with no pre-major limbo language introduced
    - `.github/workflows/release.yml` still makes the protected `hex-publish` environment the authoritative publish boundary
    - `.github/actions/release-please/action.yml` remains the repo-controlled Release Please shim rather than a direct third-party action call
    - release workflow wording is consistent with the next authoritative `1.0.0` artifact
  </acceptance_criteria>
  <verify>
    <automated>rg 'release-type": "elixir"' release-please-config.json && rg 'bump-minor-pre-major' release-please-config.json && rg 'environment: hex-publish|mix release.preflight|mix hex.publish --yes' .github/workflows/release.yml && rg 'node-version: "24"|node \\.github/actions/release-please/runtime/index.js' .github/actions/release-please/action.yml</automated>
  </verify>
  <done>The checked-in release lane contract matches the artifact truth and stays narrow to repo-owned proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package metadata -> public release claim | Version drift here creates a false public release posture |
| repo workflow contract -> protected publish lane | If checked-in automation says something different from the package artifacts, the trusted release story becomes non-falsifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-01-01 | R | version truth | mitigate | Align `mix.exs`, `.release-please-manifest.json`, and `CHANGELOG.md` on one authoritative version per D-01, D-02, and D-11 |
| T-65-01-02 | T | release workflow contract | mitigate | Keep `.github/workflows/release.yml` and the repo-controlled Release Please action aligned with the same artifact-first posture |
| T-65-01-03 | R | fallback handling | mitigate | If a material proof gap appears during execution, use explicit D-04 fallback to truthful `0.x` posture rather than leaving mixed claims in git |
</threat_model>

<verification>
Completion requires the package file, manifest, changelog, and release-lane contract to agree on one public release posture, with the strengthened test fence in place first. Do not treat passing workflow structure checks alone as sufficient if the version artifacts still drift.
</verification>

<success_criteria>
- `mix.exs`, `.release-please-manifest.json`, and `CHANGELOG.md` agree on the next authoritative public version.
- The `1.0.0` transition is explicit and factual, with `0.x` history preserved.
- Checked-in release automation still points to one protected publish lane and does not imply a second truth source.
</success_criteria>

<output>
After completion, create `.planning/phases/65-release-truth-support-contract-reconciliation/65-01-SUMMARY.md`
</output>
