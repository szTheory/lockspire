---
phase: 134-architecture-topology
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/request_object/result.ex
  - lib/lockspire/protocol/request_object.ex
  - lib/lockspire/protocol/authorization_request.ex
  - lib/lockspire/protocol/pushed_authorization_request.ex
  - test/lockspire/protocol/request_object_test.exs
  - test/lockspire/protocol/authorization_request_test.exs
  - test/lockspire/protocol/pushed_authorization_request_test.exs
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "RequestObject no longer imports AuthorizationRequest.Error, and AuthorizationRequest owns conversion to its retained public browser/redirect error struct (D-02, D-11)."
    - "JAR decryption/signature/claim/conflict and redirect-safety outcomes remain unchanged for /authorize and /par (D-11, D-13)."
    - "The authorization-request/request-object cycle is absent from Mix xref output (D-03)."
  artifacts:
    - path: lib/lockspire/protocol/request_object/result.ex
      provides: narrow neutral JAR consumption issue/result values
    - path: lib/lockspire/protocol/authorization_request.ex
      provides: retained public error/result translation at authorization boundary
  key_links:
    - from: lib/lockspire/protocol/authorization_request.ex
      to: lib/lockspire/protocol/request_object.ex
      via: neutral request-object result adapted to AuthorizationRequest.Error
      pattern: "RequestObject.Result"
---

<objective>
Break the authorization-request/request-object cycle with a narrow neutral result seam.

Purpose: Preserve browser-safe and redirect-safe JAR behavior without mutual public-struct imports.
Output: Neutral request-object result, boundary adapters, and endpoint characterization.

Decision trace: Implements D-02 through D-04, D-11 through D-13.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/134-architecture-topology/134-CONTEXT.md
@.planning/phases/134-architecture-topology/134-RESEARCH.md
@lib/lockspire/protocol/authorization_request.ex
@lib/lockspire/protocol/request_object.ex
@lib/lockspire/protocol/pushed_authorization_request.ex
@test/lockspire/protocol/authorization_request_test.exs
@test/lockspire/protocol/request_object_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize JAR errors and introduce a neutral request-object result</name>
  <files>lib/lockspire/protocol/request_object/result.ex, lib/lockspire/protocol/request_object.ex, test/lockspire/protocol/request_object_test.exs</files>
  <behavior>
    - Outer-parameter conflict, request/request_uri collision, missing keys, decryption, signature, claim, and projection failures retain code, description, reason, state, and redirect-safety facts.
    - Successful consume returns the same projected parameter map.
    - RequestObject does not name AuthorizationRequest or its nested Error type.
  </behavior>
  <action>Write RED table-driven RequestObject cases for the successful projection and each security-relevant failure. Add a small internal result/issue module under RequestObject containing only neutral disposition and safe fields. Make RequestObject return those neutral values and remove its alias/type dependency on AuthorizationRequest.Error. Keep JAR cryptography, repository key use, maximum-age policy, and fail-closed messages unchanged; do not move endpoint orchestration into the collaborator.</action>
  <verify><automated>mix test test/lockspire/protocol/request_object_test.exs</automated></verify>
  <done>RequestObject is dependency-neutral relative to AuthorizationRequest and its full security behavior is characterized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adapt neutral JAR results at /authorize and /par boundaries</name>
  <files>lib/lockspire/protocol/authorization_request.ex, lib/lockspire/protocol/pushed_authorization_request.ex, test/lockspire/protocol/authorization_request_test.exs, test/lockspire/protocol/pushed_authorization_request_test.exs</files>
  <behavior>
    - AuthorizationRequest.validate/1 still returns its exact Validated/Error structs and browser_error/redirect_error tuples.
    - PAR wraps JAR errors into its existing public error contract without changing redirect disclosure behavior.
    - Existing AuthorizationRequest.Error exports and struct fields remain available.
  </behavior>
  <action>Add failing public-boundary cases for JAR success, outer conflict, decryption/signature failure, safe redirect, and PAR wrapping. Translate RequestObject's neutral issue into the existing `%AuthorizationRequest.Error{}` only in AuthorizationRequest, and into existing PAR output at its owning boundary. Retain all nested public modules/fields and endpoint tuple tags. Verify Mix no longer reports the two-node cycle.</action>
  <verify><automated>mix test test/lockspire/protocol/request_object_test.exs test/lockspire/protocol/authorization_request_test.exs test/lockspire/protocol/pushed_authorization_request_test.exs &amp;&amp; mix xref graph --format cycles</automated></verify>
  <done>Authorization and PAR wire results are unchanged, the public Error struct remains compatible, and the request-object cycle is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Signed/encrypted request object → authorization state | Untrusted JWT claims influence redirects and authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-134-12 | Tampering/Spoofing | JAR result translation | critical | mitigate | Characterize signature/decryption/claim/conflict outcomes and safe redirect disposition. |
| T-134-13 | Information disclosure | browser/redirect errors | high | mitigate | Preserve boundary-owned Error shape and redirect safety; neutral issues carry only safe facts. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/request_object_test.exs test/lockspire/protocol/authorization_request_test.exs test/lockspire/protocol/pushed_authorization_request_test.exs`
- `mix xref graph --format cycles`
</verification>

<success_criteria>
- RequestObject has no AuthorizationRequest dependency.
- Public authorization/PAR result structures and security outcomes remain unchanged.
- The two-node cycle is removed.
</success_criteria>

<output>Create `.planning/phases/134-architecture-topology/134-05-SUMMARY.md` when done.</output>
