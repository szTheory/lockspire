---
phase: 134-architecture-topology
plan: 10
type: execute
wave: 4
depends_on: [134-09]
files_modified:
  - lib/lockspire/protocol/token_result.ex
  - lib/lockspire/protocol/token_exchange.ex
  - test/lockspire/protocol/token_exchange_test.exs
  - test/lockspire/documentation_contract_test.exs
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "TokenExchange.exchange/1, exchange_authorization_code/1, issue_ciba_tokens/4, and validate_grant_resources_for_test/2 retain exact arities and result contracts (D-02)."
    - "TokenExchange calls only Internal.* implementations and performs its own private neutral-to-public conversion; it never calls lower compatibility facades or TokenExchange.Compatibility (D-03, D-11)."
    - "Every retained helper facade from Plans 07-09 remains outside the dispatch graph, and the nine-node token cycle is absent (D-03)."
  artifacts:
    - path: lib/lockspire/protocol/token_exchange.ex
      provides: stable public facade and sole result conversion boundary
    - path: test/lockspire/documentation_contract_test.exs
      provides: public nested-module/struct compatibility proof
  key_links:
    - from: lib/lockspire/protocol/token_exchange.ex
      to: lib/lockspire/protocol/token_result.ex
      via: final conversion for every exchange entry point
      pattern: "TokenResult"
---

<objective>
Close the token group with one stable compatible public facade and zero cycles.

Purpose: Preserve every public token result while completing the compile/export direction inversion.
Output: Sole facade conversion, all-grant characterization, and zero token-cycle evidence.

Decision trace: Completes D-02 through D-04 and D-11 through D-13 for token exchange.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/134-architecture-topology/134-CONTEXT.md
@.planning/phases/134-architecture-topology/134-RESEARCH.md
@.planning/phases/134-architecture-topology/134-09-SUMMARY.md
@lib/lockspire/protocol/token_exchange.ex
@test/lockspire/protocol/token_exchange_test.exs
@test/lockspire/documentation_contract_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make TokenExchange the sole compatible result boundary</name>
  <files>lib/lockspire/protocol/token_result.ex, lib/lockspire/protocol/token_exchange.ex, test/lockspire/protocol/token_exchange_test.exs, test/lockspire/documentation_contract_test.exs</files>
  <behavior>
    - exchange/1 dispatches authorization-code, refresh, device, CIBA, and RFC 8693 and returns public Success/Error structs.
    - exchange_authorization_code/1 and issue_ciba_tokens/4 retain callable arities and public results.
    - validate_grant_resources_for_test/2 retains its existing list/error shape.
    - Unsupported grant and every converted error retain exact status/code/description/reason/DPoP nonce.
    - Success/Error module names and struct keys remain unchanged.
  </behavior>
  <action>Start RED with all four named TokenExchange arities, every grant, unsupported/error paths, CIBA worker entry, resource test helper, and exact Success/Error struct keys. Dispatch directly to `TokenExchange.Internal.AuthorizationCodeGrant`, `.RefreshExchange`, `.DeviceCodeGrant`, `.CibaGrant`, `.Rfc8693Exchange`, and `.GrantSupport`. Convert TokenResult to `%TokenExchange.Success{}`/`%TokenExchange.Error{}` with private functions inside TokenExchange; do not call Compatibility or any retained lower facade. Assert the complete helper manifest from Plans 07-09 remains loaded/callable with representative exact results, and assert neutral/internal source has no TokenExchange or Compatibility reference. Run the full token capability set and require Mix xref output to omit the former nine-node cycle.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange_test.exs test/lockspire/protocol/token_exchange/authorization_code_test.exs test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs test/lockspire/protocol/token_exchange/device_code_test.exs test/lockspire/protocol/token_exchange/delegation_test.exs test/lockspire/documentation_contract_test.exs &amp;&amp; mix xref graph --format cycles</automated></verify>
  <done>All token entry points retain public runtime shapes and the complete token exchange cycle is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Neutral grant result → public response | Conversion controls externally visible OAuth response semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-134-26 | Tampering | facade result conversion | critical | mitigate | Execute all grants and assert exact public fields/tuple tags. |
| T-134-27 | Spoofing | public compatibility claim | high | mitigate | Module/arity/struct-key and representative-result contracts. |
| T-134-28 | Information disclosure | public error conversion | high | mitigate | Exact safe fields and existing documentation/redaction tests. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/token_exchange_test.exs test/lockspire/protocol/token_exchange/authorization_code_test.exs test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs test/lockspire/protocol/token_exchange/device_code_test.exs test/lockspire/protocol/token_exchange/delegation_test.exs test/lockspire/documentation_contract_test.exs`
- `mix xref graph --format cycles`
</verification>

<success_criteria>
- TokenExchange remains the exact stable public facade for every grant.
- Internal token modules have no back-edge to the facade/Compatibility; retained lower facades remain outside the dispatcher graph.
- The nine-node token cycle is gone.
</success_criteria>

<output>Create `.planning/phases/134-architecture-topology/134-10-SUMMARY.md` when done.</output>
