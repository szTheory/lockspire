---
phase: 134-architecture-topology
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/storage/prefix.ex
  - lib/lockspire/storage/ecto/prefix.ex
  - lib/lockspire/config.ex
  - lib/lockspire/security/policy.ex
  - test/lockspire/storage/prefix_test.exs
  - test/lockspire/storage/ecto/prefix_test.exs
  - test/lockspire/config_test.exs
  - test/lockspire/security/policy_test.exs
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "Prefix normalization and option construction are pure functions of supplied values and never call Config (D-10)."
    - "Config.storage_prefix/0, Config.oban_prefix/0, issuer/config validation, and Policy secret sealing retain their public behavior (D-02, D-10)."
    - "The config/security/prefix cycle is absent from Mix xref output (D-03)."
  artifacts:
    - path: lib/lockspire/storage/prefix.ex
      provides: dependency-neutral prefix normalization and explicit option builders
    - path: lib/lockspire/storage/ecto/prefix.ex
      provides: compatible adapter facade over explicit configured values
  key_links:
    - from: lib/lockspire/config.ex
      to: lib/lockspire/storage/prefix.ex
      via: application config value passed explicitly to pure normalizer
      pattern: "Storage.Prefix"
---

<objective>
Break the configuration/security/prefix loop with explicit inputs and compatible wrappers.

Purpose: Make configuration direction unambiguous without changing host prefix, issuer, or cryptographic behavior.
Output: Pure prefix utility, explicit policy secret input, compatible Config/Ecto facades, and focused regression proof.

Decision trace: Implements D-01 through D-04, D-10 through D-12.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/134-architecture-topology/134-CONTEXT.md
@.planning/phases/134-architecture-topology/134-RESEARCH.md
@lib/lockspire/config.ex
@lib/lockspire/security/policy.ex
@lib/lockspire/storage/ecto/prefix.ex
@test/lockspire/config_test.exs
@test/lockspire/storage/ecto/prefix_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract pure prefix normalization behind compatible Config methods</name>
  <files>lib/lockspire/storage/prefix.ex, lib/lockspire/storage/ecto/prefix.ex, test/lockspire/storage/prefix_test.exs, test/lockspire/storage/ecto/prefix_test.exs</files>
  <behavior>
    - normalize/1 retains nil, blank/public, valid identifier, and invalid identifier outcomes.
    - prefix_opts/1 and oban_opts/1 derive options only from their argument.
  </behavior>
  <action>Start with RED table-driven tests for every current prefix input and Ecto Prefix wrapper. Create dependency-neutral `Lockspire.Storage.Prefix` with explicit `normalize/1`, `prefix_opts/1`, and `oban_opts/1`. Keep `Lockspire.Storage.Ecto.Prefix` as a compatible adapter-facing facade whose pure arities delegate inward. Ensure the neutral utility imports neither Config nor Ecto.</action>
  <verify><automated>mix test test/lockspire/storage/prefix_test.exs test/lockspire/storage/ecto/prefix_test.exs</automated></verify>
  <done>Prefix normalization and option computation are argument-only and compatible at the Ecto adapter boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve Config prefix accessors over explicit values</name>
  <files>lib/lockspire/config.ex, lib/lockspire/storage/prefix.ex, lib/lockspire/storage/ecto/prefix.ex, test/lockspire/config_test.exs, test/lockspire/storage/ecto/prefix_test.exs</files>
  <behavior>
    - Config.storage_prefix/0 and oban_prefix/0 retain existing environment/default behavior.
    - Ecto Prefix public zero-arity accessors remain callable without the pure utility reaching Config.
  </behavior>
  <action>Add RED compatibility cases for missing, public, and custom storage/Oban prefix configuration. Make Config read application environment and pass raw values to the pure utility. Preserve Ecto Prefix zero-arity wrappers at the adapter boundary while eliminating its back-edge into Config through explicit argument flow or Config-owned option construction.</action>
  <verify><automated>mix test test/lockspire/config_test.exs test/lockspire/storage/prefix_test.exs test/lockspire/storage/ecto/prefix_test.exs</automated></verify>
  <done>Config and Ecto public accessors retain all prefix outcomes without a utility-to-Config cycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove Policy's runtime Config back-edge</name>
  <files>lib/lockspire/security/policy.ex, lib/lockspire/config.ex, test/lockspire/security/policy_test.exs, test/lockspire/config_test.exs</files>
  <behavior>
    - Secret verifier sealing succeeds with an explicit secret_key_base and fails closed when required material is unavailable.
    - Existing public callers that omit options retain compatibility through an outer Config-fed wrapper.
    - Issuer, signing algorithm, required config, storage prefix, and Oban prefix behavior remain unchanged.
  </behavior>
  <action>Add failing Policy/Config compatibility tests around `seal_client_secret_jwt_verifier/2`, missing key material, issuer/mount validation, and prefix wrappers. Refactor the cryptographic helper so the pure Policy operation receives `secret_key_base` explicitly and no longer reaches back to Config; keep any default resolution at a public/application boundary that does not recreate the cycle. Do not weaken failure behavior or expose secret material in exceptions. Verify the complete xref output no longer includes Config/Policy/Prefix as a cycle.</action>
  <verify><automated>mix test test/lockspire/security/policy_test.exs test/lockspire/config_test.exs test/lockspire/storage/prefix_test.exs test/lockspire/storage/ecto/prefix_test.exs &amp;&amp; mix xref graph --format cycles</automated></verify>
  <done>The config/security/prefix cycle is removed with public configuration and cryptographic behavior intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host configuration → storage adapters | Prefix controls database object location. |
| Host secret config → credential sealing | Missing/wrong key material must fail closed without disclosure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-134-10 | Tampering | storage/Oban prefix | high | mitigate | Preserve identifier validation/default tests and explicit value flow. |
| T-134-11 | Information disclosure/Tampering | verifier sealing | high | mitigate | Explicit secret input, fail-closed missing-key tests, no secret-bearing errors. |
</threat_model>

<verification>
- `mix test test/lockspire/storage/prefix_test.exs test/lockspire/storage/ecto/prefix_test.exs test/lockspire/config_test.exs test/lockspire/security/policy_test.exs`
- `mix xref graph --format cycles`
</verification>

<success_criteria>
- The neutral prefix utility has no Config/Ecto dependency.
- Config and Policy no longer mutually depend while public behavior remains compatible.
- The three-node configuration cycle is gone.
</success_criteria>

<output>Create `.planning/phases/134-architecture-topology/134-04-SUMMARY.md` when done.</output>
