---
phase: 134-architecture-topology
plan: 09
type: execute
wave: 3
depends_on: [134-08]
files_modified:
  - lib/lockspire/protocol/token_result.ex
  - lib/lockspire/protocol/token_exchange/compatibility.ex
  - lib/lockspire/protocol/token_exchange/internal/authorization_code_grant.ex
  - lib/lockspire/protocol/token_exchange/internal/ciba_grant.ex
  - lib/lockspire/protocol/token_exchange/internal/device_code_grant.ex
  - lib/lockspire/protocol/token_exchange/internal/grant_support.ex
  - lib/lockspire/protocol/token_exchange/authorization_code_grant.ex
  - lib/lockspire/protocol/token_exchange/ciba_grant.ex
  - lib/lockspire/protocol/token_exchange/device_code_grant.ex
  - lib/lockspire/protocol/token_exchange/grant_support.ex
  - test/lockspire/protocol/token_exchange/authorization_code_test.exs
  - test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs
  - test/lockspire/protocol/token_exchange/device_code_test.exs
  - test/lockspire/protocol/token_exchange/delegation_test.exs
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "Neutral authorization-code, device, and CIBA leaf grants plus GrantSupport depend on TokenResult, never the public TokenExchange facade or Compatibility adapter (D-11)."
    - "The retained AuthorizationCodeGrant.exchange/1, CibaGrant.exchange/1 and issue_tokens/4, DeviceCodeGrant.exchange/1, and nine named GrantSupport arities remain callable with exact existing public/domain/error result shapes (D-02)."
    - "Authentication, PKCE, resource selection, polling, persistence, DPoP, delegation, and observability behavior remain unchanged (D-08, D-13)."
  artifacts:
    - path: lib/lockspire/protocol/token_exchange/grant_support.ex
      provides: retained public helper facade over neutral Internal.GrantSupport
    - path: lib/lockspire/protocol/token_exchange/internal/grant_support.ex
      provides: shared grant orchestration on neutral results
    - path: lib/lockspire/protocol/token_exchange/authorization_code_grant.ex
      provides: retained authorization-code compatibility facade
  key_links:
    - from: lib/lockspire/protocol/token_exchange/internal/grant_support.ex
      to: lib/lockspire/protocol/token_result.ex
      via: neutral grant success/error values
      pattern: "TokenResult"
---

<objective>
Convert all token grant leaves and shared support to the neutral result spine.

Purpose: Remove the remaining internal back-edges before TokenExchange becomes the sole converter on the dispatch path.
Output: Facade-independent authorization-code, device, CIBA, resource, and delegation orchestration.

Decision trace: Implements D-03, D-08, D-11, and D-13.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/134-architecture-topology/134-CONTEXT.md
@.planning/phases/134-architecture-topology/134-RESEARCH.md
@.planning/phases/134-architecture-topology/134-08-SUMMARY.md
@lib/lockspire/protocol/token_exchange/grant_support.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert authorization-code and shared grant support</name>
  <files>lib/lockspire/protocol/token_result.ex, lib/lockspire/protocol/token_exchange/compatibility.ex, lib/lockspire/protocol/token_exchange/internal/authorization_code_grant.ex, lib/lockspire/protocol/token_exchange/internal/grant_support.ex, lib/lockspire/protocol/token_exchange/authorization_code_grant.ex, lib/lockspire/protocol/token_exchange/grant_support.ex, test/lockspire/protocol/token_exchange/authorization_code_test.exs, test/lockspire/protocol/token_exchange/delegation_test.exs</files>
  <behavior>
    - `AuthorizationCodeGrant.exchange/1` retains public TokenExchange success/error while `Internal.AuthorizationCodeGrant.exchange/1` returns TokenResult.
    - GrantSupport retains `handle_code_exchange/6`, `authenticate_client/3`, `fetch_authorization_code/2`, `fetch_device_authorization_for_exchange/3`, `fetch_ciba_authorization_for_exchange/3`, `validate_grant_resources_for_test/2`, `redeem_device_authorization/4`, `redeem_ciba_authorization/4`, and `emit_failure/3` with exact current domain/success/error tuples.
    - Internal GrantSupport constructs/consumes TokenResult and does not alias TokenExchange.Success/Error or Compatibility.
    - Code redemption and token persistence retain atomic single-use behavior.
  </behavior>
  <action>Start RED with module/arity/result checks for `AuthorizationCodeGrant.exchange/1` and all nine exported GrantSupport functions, then the authorization-code capability suite and shared resource/delegation failures. Move orchestration to `TokenExchange.Internal.AuthorizationCodeGrant` and `.GrantSupport`, which use TokenResult and the Plan 07-08 internal signer/DPoP collaborators only. Keep the existing modules as retained facades: delegate to internals, pass through domain/non-token successes, and convert TokenResult errors/successes only through Compatibility. Preserve client authentication ordering, PKCE S256, exact redirect match, one-time code redemption, resource authorization, DPoP, token persistence, and telemetry. Plan 10 calls internal modules directly.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/authorization_code_test.exs test/lockspire/protocol/token_exchange/delegation_test.exs</automated></verify>
  <done>Authorization-code and shared grant support are facade-independent with atomic/security behavior intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert device and CIBA grant leaves</name>
  <files>lib/lockspire/protocol/token_exchange/compatibility.ex, lib/lockspire/protocol/token_exchange/internal/ciba_grant.ex, lib/lockspire/protocol/token_exchange/internal/device_code_grant.ex, lib/lockspire/protocol/token_exchange/internal/grant_support.ex, lib/lockspire/protocol/token_exchange/ciba_grant.ex, lib/lockspire/protocol/token_exchange/device_code_grant.ex, lib/lockspire/protocol/token_exchange/grant_support.ex, test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs, test/lockspire/protocol/token_exchange/device_code_test.exs</files>
  <behavior>
    - `DeviceCodeGrant.exchange/1`, `CibaGrant.exchange/1`, and `CibaGrant.issue_tokens/4` retain public TokenExchange success/error results.
    - Internal device/CIBA pending, slow-down, denied, expired, consumed, poll/ping/push, resource, and success paths use TokenResult only.
    - Internal CIBA/Device leaves do not alias TokenExchange or Compatibility.
  </behavior>
  <action>Add RED module/arity/result checks for the three named facade functions plus device and CIBA state matrices, resource, and sender-constraint cases. Move behavior to `TokenExchange.Internal.DeviceCodeGrant` and `.CibaGrant`, both using Internal.GrantSupport and TokenResult. Retain the current leaf modules as facades that delegate inward and convert only through Compatibility. Preserve polling intervals, single-use redemption, Push worker contract inputs, resource selection, token issuance/persistence, telemetry, and safe errors. Plan 10 dispatches to the internal leaves, never these facades.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs test/lockspire/protocol/token_exchange/device_code_test.exs test/lockspire/protocol/token_exchange/delegation_test.exs</automated></verify>
  <done>Device and CIBA leaves are facade-independent and retain their complete state/security behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated grant request → grant support | Inputs authorize single-use token issuance. |
| Device/CIBA state → redemption | Polling and approval state constrain issuance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-134-23 | Spoofing/Elevation of privilege | authorization-code auth/PKCE | critical | mitigate | Full auth/code/redirect/PKCE/resource/DPoP suite. |
| T-134-24 | Tampering | code/device/CIBA redemption | critical | mitigate | Preserve atomic single-use repository operations and state matrices. |
| T-134-25 | Information disclosure | grant errors/telemetry | high | mitigate | Safe neutral results and existing redaction assertions. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/token_exchange/authorization_code_test.exs test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs test/lockspire/protocol/token_exchange/device_code_test.exs test/lockspire/protocol/token_exchange/delegation_test.exs`
</verification>

<success_criteria>
- All neutral grant leaves and Internal.GrantSupport depend only on neutral results.
- Every named exported helper module/arity/result remains compatible through a facade outside the dispatch graph.
</success_criteria>

<output>Create `.planning/phases/134-architecture-topology/134-09-SUMMARY.md` when done.</output>
