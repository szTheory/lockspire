---
phase: 134-architecture-topology
plan: 09
type: execute
wave: 3
depends_on: [134-08]
files_modified:
  - lib/lockspire/protocol/token_result.ex
  - lib/lockspire/protocol/token_exchange/authorization_code_grant.ex
  - lib/lockspire/protocol/token_exchange/ciba_grant.ex
  - lib/lockspire/protocol/token_exchange/device_code_grant.ex
  - lib/lockspire/protocol/token_exchange/grant_support.ex
  - test/lockspire/protocol/token_exchange/authorization_code_test.exs
  - test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs
  - test/lockspire/protocol/token_exchange/device_code_test.exs
  - test/lockspire/protocol/token_exchange/delegation_test.exs
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "Authorization-code, device, and CIBA leaf grants plus GrantSupport depend on neutral TokenResult, never the public TokenExchange facade (D-11)."
    - "Authentication, PKCE, resource selection, polling, persistence, DPoP, delegation, and observability behavior remain unchanged (D-08, D-13)."
  artifacts:
    - path: lib/lockspire/protocol/token_exchange/grant_support.ex
      provides: shared grant orchestration on neutral results
    - path: lib/lockspire/protocol/token_exchange/authorization_code_grant.ex
      provides: facade-independent authorization-code grant
  key_links:
    - from: lib/lockspire/protocol/token_exchange/grant_support.ex
      to: lib/lockspire/protocol/token_result.ex
      via: neutral grant success/error values
      pattern: "TokenResult"
---

<objective>
Convert all token grant leaves and shared support to the neutral result spine.

Purpose: Remove the remaining internal back-edges before the stable facade becomes the sole public converter.
Output: Facade-independent authorization-code, device, CIBA, resource, and delegation orchestration.

Decision trace: Implements D-03, D-08, D-11, and D-13.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/134-architecture-topology/134-CONTEXT.md
@.planning/phases/134-architecture-topology/134-RESEARCH.md
@.planning/phases/134-architecture-topology/134-08-SUMMARY.md
@lib/lockspire/protocol/token_exchange/grant_support.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert authorization-code and shared grant support</name>
  <files>lib/lockspire/protocol/token_result.ex, lib/lockspire/protocol/token_exchange/authorization_code_grant.ex, lib/lockspire/protocol/token_exchange/grant_support.ex, test/lockspire/protocol/token_exchange/authorization_code_test.exs, test/lockspire/protocol/token_exchange/delegation_test.exs</files>
  <behavior>
    - Authorization-code success and every authentication, grant-type, code, redirect, PKCE, resource, DPoP, signing, and persistence failure retain exact semantics.
    - Shared helpers construct/consume TokenResult and do not alias TokenExchange.Success/Error.
    - Code redemption and token persistence retain atomic single-use behavior.
  </behavior>
  <action>Start RED with the authorization-code capability suite and shared resource/delegation failures. Replace TokenExchange struct construction/specs in AuthorizationCodeGrant and GrantSupport with TokenResult while preserving client authentication ordering, PKCE S256, exact redirect match, one-time code redemption, resource authorization, DPoP, token persistence, and telemetry. Keep public conversion outside these modules.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/authorization_code_test.exs test/lockspire/protocol/token_exchange/delegation_test.exs</automated></verify>
  <done>Authorization-code and shared grant support are facade-independent with atomic/security behavior intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert device and CIBA grant leaves</name>
  <files>lib/lockspire/protocol/token_exchange/ciba_grant.ex, lib/lockspire/protocol/token_exchange/device_code_grant.ex, lib/lockspire/protocol/token_exchange/grant_support.ex, test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs, test/lockspire/protocol/token_exchange/device_code_test.exs</files>
  <behavior>
    - Device pending/slow-down/denied/expired/consumed/success states retain exact outcomes.
    - CIBA poll/ping/push and resource success/failure states retain exact outcomes.
    - CIBA/Device leaves do not alias TokenExchange or nested result structs.
  </behavior>
  <action>Add RED device and CIBA state matrices, including resource and sender-constraint cases. Convert both leaf modules and their shared GrantSupport paths to TokenResult. Preserve polling intervals, single-use redemption, Push worker contract inputs, resource selection, token issuance/persistence, telemetry, and safe errors.</action>
  <verify><automated>mix test test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs test/lockspire/protocol/token_exchange/device_code_test.exs test/lockspire/protocol/token_exchange/delegation_test.exs</automated></verify>
  <done>Device and CIBA leaves are facade-independent and retain their complete state/security behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Authenticated grant request → grant support | Inputs authorize single-use token issuance. |
| Device/CIBA state → redemption | Polling and approval state constrain issuance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-134-23 | Spoofing/Elevation of privilege | authorization-code auth/PKCE | critical | mitigate | Full auth/code/redirect/PKCE/resource/DPoP suite. |
| T-134-24 | Tampering | code/device/CIBA redemption | critical | mitigate | Preserve atomic single-use repository operations and state matrices. |
| T-134-25 | Information disclosure | grant errors/telemetry | high | mitigate | Safe neutral results and existing redaction assertions. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/token_exchange/authorization_code_test.exs test/lockspire/protocol/token_exchange/ciba_and_resource_test.exs test/lockspire/protocol/token_exchange/device_code_test.exs test/lockspire/protocol/token_exchange/delegation_test.exs`
</verification>

<success_criteria>
- All grant leaves and GrantSupport depend only on neutral results.
- Grant authentication, atomic redemption, sender constraints, polling, and observability remain compatible.
</success_criteria>

<output>Create `.planning/phases/134-architecture-topology/134-09-SUMMARY.md` when done.</output>
