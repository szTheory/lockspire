---
phase: 134-architecture-topology
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/token_result.ex
  - lib/lockspire/protocol/token_exchange/compatibility.ex
  - lib/lockspire/protocol/token_exchange/internal/access_token_signer.ex
  - lib/lockspire/protocol/token_exchange/internal/token_endpoint_dpop.ex
  - lib/lockspire/protocol/access_token_signer.ex
  - lib/lockspire/protocol/token_endpoint_dpop.ex
  - test/lockspire/protocol/token_result_test.exs
  - test/lockspire/protocol/access_token_signer_test.exs
  - test/lockspire/protocol/token_endpoint_dpop_test.exs
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "Signing and token-endpoint DPoP collaborators build and consume one neutral token success/error value rather than importing TokenExchange nested structs (D-11)."
    - "AccessTokenSigner.issue/3 and issue_exchange/4 plus TokenEndpointDPoP.resolve_context/2 and resolve_refresh_context/3 remain public-compatible facades returning their exact current tuples and TokenExchange.Error values (D-02, D-13)."
    - "TokenResult and both neutral implementation modules never alias/call TokenExchange or its Compatibility adapter; the retained helper facades alone call the one-way adapter (D-03, D-11)."
    - "Token errors contain no client secrets, tokens, proofs, or key material after conversion (D-08)."
  artifacts:
    - path: lib/lockspire/protocol/token_result.ex
      provides: dependency-neutral token endpoint success/error data with no public-struct conversion
    - path: lib/lockspire/protocol/token_exchange/compatibility.ex
      provides: one-way neutral-to-public conversion used only by retained lower helper facades
    - path: test/lockspire/protocol/token_result_test.exs
      provides: exact compatibility field and redaction contract
  key_links:
    - from: lib/lockspire/protocol/token_exchange/internal/access_token_signer.ex
      to: lib/lockspire/protocol/token_result.ex
      via: neutral signing failure construction
      pattern: "TokenResult"
---

<objective>
Introduce the neutral result spine needed to untangle the nine-node token-exchange cycle.

Purpose: Remove lower-level imports of the public TokenExchange facade while keeping every existing collaborator behavior callable for the dispatcher inversion completed through Plans 09-10.
Output: Neutral token result/error values and migrated signing/DPoP collaborators.

Decision trace: Implements D-02 through D-04, D-08, D-11, and D-13; Plans 09-10 complete final token-cycle removal.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/134-architecture-topology/134-CONTEXT.md
@.planning/phases/134-architecture-topology/134-RESEARCH.md
@lib/lockspire/protocol/token_exchange.ex
@lib/lockspire/protocol/access_token_signer.ex
@lib/lockspire/protocol/refresh_exchange.ex
@lib/lockspire/protocol/rfc8693_exchange.ex
@lib/lockspire/protocol/token_endpoint_dpop.ex
@test/lockspire/protocol/token_exchange_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define neutral token success/error values with exact compatibility conversion</name>
  <reversibility rating="costly">The internal result seam spans every token grant and signer, so changing it later requires coordinated protocol collaborators.</reversibility>
  <files>lib/lockspire/protocol/token_result.ex, lib/lockspire/protocol/token_exchange/compatibility.ex, test/lockspire/protocol/token_result_test.exs, test/lockspire/protocol/token_exchange_test.exs</files>
  <behavior>
    - Neutral success carries the exact access, refresh, ID token, token type, issued token type, expiry, and scope fields.
    - Neutral error carries the exact status, OAuth code/description, reason code, and optional DPoP nonce fields.
    - TokenResult contains no conversion to or reference to TokenExchange.Success/Error.
    - TokenExchange.Compatibility losslessly converts neutral success/error only for retained lower helper facades.
    - Safe inspection/event metadata excludes credential/token/proof/key values.
  </behavior>
  <action>Start RED with exact neutral fields, compatibility conversion, tuple-tag, and redaction tests. Add `Lockspire.Protocol.TokenResult` as a pure neutral success/error owner which never references TokenExchange. Add `Lockspire.Protocol.TokenExchange.Compatibility` as the one-way adapter that constructs the retained `%TokenExchange.Success{}` and `%TokenExchange.Error{}` only for separately callable lower facades. The TokenExchange dispatcher must never call this adapter; Plan 10 performs its own private conversion at its public entries. Do not move or rename either public nested struct.</action>
  <verify><automated>mix test test/lockspire/protocol/token_result_test.exs test/lockspire/protocol/token_exchange_test.exs</automated></verify>
  <done>Neutral values can represent every current token result and convert losslessly to the retained public structs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate signing and token DPoP collaborators off the public facade</name>
  <files>lib/lockspire/protocol/token_exchange/internal/access_token_signer.ex, lib/lockspire/protocol/token_exchange/internal/token_endpoint_dpop.ex, lib/lockspire/protocol/access_token_signer.ex, lib/lockspire/protocol/token_endpoint_dpop.ex, test/lockspire/protocol/access_token_signer_test.exs, test/lockspire/protocol/token_endpoint_dpop_test.exs</files>
  <behavior>
    - Access-token signing success/failure and DPoP nonce challenge produce unchanged OAuth semantics.
    - `AccessTokenSigner.issue/3` retains `{:ok, raw, hash} | {:error, %TokenExchange.Error{}}`; `issue_exchange/4` retains the same shape.
    - `TokenEndpointDPoP.resolve_context/2` and `resolve_refresh_context/3` retain context success or `%TokenExchange.Error{}`.
    - `TokenExchange.Internal.AccessTokenSigner` and `.TokenEndpointDPoP` use TokenResult only; public facades delegate inward then call Compatibility.
  </behavior>
  <action>Add failing module/arity/result characterization for all four named public functions plus signer failures and DPoP nonce/error outcomes. Move implementations to `TokenExchange.Internal.AccessTokenSigner` and `TokenExchange.Internal.TokenEndpointDPoP`, returning TokenResult on error. Keep the existing `AccessTokenSigner` and `TokenEndpointDPoP` modules as public-compatible facades which delegate inward and call Compatibility only when adapting an error; successful raw/hash and context values pass through unchanged. Neutral implementations must not import TokenExchange or Compatibility. Plans 09-10 must call these internal implementations, never the retained facades.</action>
  <verify><automated>mix test test/lockspire/protocol/token_result_test.exs test/lockspire/protocol/access_token_signer_test.exs test/lockspire/protocol/token_endpoint_dpop_test.exs test/lockspire/protocol/token_exchange_test.exs</automated></verify>
  <done>Signing and token DPoP use the neutral result spine with security behavior and callable boundaries preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Grant/signing state → neutral result | Sensitive token material crosses internal orchestration. |
| Neutral result → public TokenExchange struct | Conversion must not change OAuth semantics or disclosure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-134-17 | Tampering | token result conversion | critical | mitigate | Exact field/tuple conversion tests across success and error paths. |
| T-134-18 | Elevation of privilege | signing/DPoP behavior | critical | mitigate | Preserve signing, proof, replay, nonce, and sender-binding tests. |
| T-134-19 | Information disclosure | neutral errors/events | critical | mitigate | Redaction sentinels and safe metadata-only conversion assertions. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/token_result_test.exs test/lockspire/protocol/access_token_signer_test.exs test/lockspire/protocol/token_endpoint_dpop_test.exs test/lockspire/protocol/token_exchange_test.exs`
</verification>

<success_criteria>
- The neutral token result is lossless and not a new advertised API.
- Neutral signing/DPoP implementations no longer require public TokenExchange structs; retained helper facades preserve every named arity/result.
- Token security and lifecycle characterization stays green.
</success_criteria>

<output>Create `.planning/phases/134-architecture-topology/134-07-SUMMARY.md` when done.</output>
