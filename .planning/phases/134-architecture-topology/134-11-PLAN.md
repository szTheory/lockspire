---
phase: 134-architecture-topology
plan: 11
type: execute
wave: 5
depends_on: [134-02, 134-03, 134-04, 134-05, 134-06, 134-10]
files_modified:
  - scripts/ci/check_architecture_topology.sh
  - test/lockspire/architecture_fitness_test.exs
  - test/lockspire/compatibility_baseline_contract_test.exs
  - mix.exs
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04]
must_haves:
  truths:
    - "One contributor command fails with exact cycle diagnostics unless Mix reports zero runtime/export cycles (D-03, D-04, D-12)."
    - "Permanent AST fitness rejects protocol-to-Web/Admin, delivery-to-Ecto reach-through, and duplicated DCR/admin lifecycle ownership (D-01, D-12)."
    - "Compatibility fitness proves retained public facade modules, arities, nested structs, and representative result shapes (D-02, D-12)."
    - "The complete repository gate remains green after all five cycle inversions and client-service extraction."
  artifacts:
    - path: scripts/ci/check_architecture_topology.sh
      provides: deterministic zero-cycle command with full Mix diagnostics
    - path: test/lockspire/architecture_fitness_test.exs
      provides: permanent directional and ownership AST invariants
    - path: mix.exs
      provides: discoverable qa.architecture alias included in standard qa
  key_links:
    - from: mix.exs
      to: scripts/ci/check_architecture_topology.sh
      via: qa.architecture standard contributor gate
      pattern: "qa.architecture"
    - from: test/lockspire/architecture_fitness_test.exs
      to: lib/lockspire/protocol
      via: parse-once deterministic AST traversal with violating file/module diagnostics
      pattern: "protocol.*Web|Admin"
---

<objective>
Make the cycle-free directional topology and compatibility boundary permanent executable invariants.

Purpose: Turn the refactor's graph result into a contributor/CI fitness contract that identifies the exact violation and prevents architectural drift.
Output: Zero-cycle command, expanded AST/export/ownership fitness, standard QA wiring, and full phase gate evidence.

Decision trace: Completes D-01 through D-04 and D-12 through D-13 across ARCH-01 through ARCH-04.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/134-architecture-topology/134-CONTEXT.md
@.planning/phases/134-architecture-topology/134-RESEARCH.md
@.planning/phases/134-architecture-topology/134-VALIDATION.md
@test/lockspire/architecture_fitness_test.exs
@test/lockspire/compatibility_baseline_contract_test.exs
@docs/supported-surface.md
@mix.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic topology, direction, ownership, and compatibility fitness</name>
  <files>scripts/ci/check_architecture_topology.sh, test/lockspire/architecture_fitness_test.exs, test/lockspire/compatibility_baseline_contract_test.exs</files>
  <behavior>
    - The topology command runs Mix xref cycles, exits nonzero for any cycle, and prints the complete exact path/edge diagnostic.
    - Architecture fitness scans all protocol production sources for aliases and remote calls into Lockspire.Web/Admin, and scans delivery sources for direct Ecto implementation reach-through outside owning boundaries.
    - Fitness proves Clients/Admin/DCR delegate to ClientLifecycle/ClientMetadata and rejects duplicate lifecycle/metadata ownership by structural predicates.
    - Public modules/functions and nested Success/Error/Validated/UpdateSuccess/RegistrationResult struct fields remain exported.
  </behavior>
  <action>Start RED by extracting pure AST predicates that can be exercised against inline violating/allowed snippets, then apply them to production files with exact path/module diagnostics. Add a checked shell command that captures `mix xref graph --format cycles`, preserves its exit status/output, and fails on any reported cycle without parsing aliases itself. Extend compatibility tests with `Code.ensure_loaded?`, `function_exported?`, struct module/key, and representative public result checks for all compatibility-sensitive modules touched by the five cycles and client extraction. Add ownership rules that require Registration, RegistrationManagement, Clients, and Admin.Clients to delegate to the neutral service while forbidding copies of moved normalization/audit transaction implementation. Keep current protocol-Ecto and LiveView-repository rules.</action>
  <verify><automated>sh scripts/ci/check_architecture_topology.sh &amp;&amp; mix test test/lockspire/architecture_fitness_test.exs test/lockspire/compatibility_baseline_contract_test.exs</automated></verify>
  <done>The permanent tests detect synthetic direction/ownership violations, scan real production sources, preserve public exports, and the topology command reports zero cycles.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the maintained architecture gate and run the complete phase proof</name>
  <files>mix.exs, scripts/ci/check_architecture_topology.sh</files>
  <action>Add a discoverable `mix qa.architecture` alias that runs the zero-cycle script plus focused architecture/compatibility tests, and invoke it from the existing `mix qa` sequence exactly once without changing fast/integration suite composition. Run warnings-as-errors compile, focused architecture gate, fast tests, integration tests, QA, and docs verification from the final tree. If any compatibility or endpoint test fails, repair the responsible earlier slice rather than weakening the fitness predicate or allowing a new cycle/back-edge.</action>
  <verify><automated>mix qa.architecture &amp;&amp; mix compile --warnings-as-errors &amp;&amp; mix test.fast &amp;&amp; mix test.integration &amp;&amp; mix qa &amp;&amp; mix docs.verify</automated></verify>
  <done>The standard contributor gate enforces zero cycles/direction/compatibility and the complete repository verification suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Source graph → contributor gate | A false-green checker would permit architectural security regression. |
| Internal relocation → public consumers | Removed modules/arities/struct shapes would break existing adopters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-134-29 | Tampering/Elevation of privilege | dependency direction | high | mitigate | Authoritative Mix xref gate plus AST alias/remote-call rules with synthetic predicate tests. |
| T-134-30 | Tampering | duplicated lifecycle owner | high | mitigate | Structural delegation/ownership fitness across all four public callers. |
| T-134-31 | Denial of service | recursive/unstable checker | medium | mitigate | Dedicated direct script/alias, deterministic output, no recursive Mix invocation from ExUnit. |
| T-134-32 | Spoofing | compatibility claim | high | mitigate | Export, arity, module, struct-key, and representative-result assertions. |
</threat_model>

<verification>
- `mix qa.architecture`
- `mix compile --warnings-as-errors`
- `mix test.fast`
- `mix test.integration`
- `mix qa`
- `mix docs.verify`
</verification>

<success_criteria>
- `mix xref graph --format cycles` reports zero cycles through a maintained failing gate.
- Protocol/delivery/neutral-service direction and public compatibility are permanently tested.
- The full repository gate passes after the topology refactor.
</success_criteria>

<output>Create `.planning/phases/134-architecture-topology/134-11-SUMMARY.md` when done.</output>
