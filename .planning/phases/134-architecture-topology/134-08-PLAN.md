---
phase: 134-architecture-topology
plan: 08
type: execute
wave: 2
depends_on: [134-07]
files_modified:
  - lib/lockspire/protocol/token_result.ex
  - lib/lockspire/protocol/token_exchange/compatibility.ex
  - lib/lockspire/protocol/token_exchange/internal/refresh_exchange.ex
  - lib/lockspire/protocol/token_exchange/internal/rfc8693_exchange.ex
  - lib/lockspire/protocol/refresh_exchange.ex
  - lib/lockspire/protocol/rfc8693_exchange.ex
  - test/lockspire/protocol/refresh_exchange_test.exs
  - test/lockspire/protocol/rfc8693_exchange_test.exs
autonomous: true
requirements: [ARCH-01]
must_haves:
  truths:
    - "Refresh rotation/reuse and RFC 8693 exchange use the neutral result spine without importing TokenExchange.Success/Error (D-11)."
    - "RefreshExchange.exchange_refresh_token/2 and Rfc8693Exchange.exchange/2 remain retained facades returning exact TokenExchange.Success/Error results (D-02, D-08, D-13)."
    - "TokenExchange.Internal.RefreshExchange and .Rfc8693Exchange use TokenResult only; the public facades alone adapt through Compatibility, and Plan 10 dispatches directly to internals (D-03, D-11)."
  artifacts:
    - path: lib/lockspire/protocol/refresh_exchange.ex
      provides: retained exchange_refresh_token/2 facade over neutral internal implementation
    - path: lib/lockspire/protocol/rfc8693_exchange.ex
      provides: neutral internal RFC 8693 result with compatible public wrapper
  key_links:
    - from: lib/lockspire/protocol/token_exchange/internal/refresh_exchange.ex
      to: lib/lockspire/protocol/token_result.ex
      via: neutral rotation/reuse outcomes
      pattern: "TokenResult"
---

<objective>
Move refresh and RFC 8693 collaborators onto the neutral token result spine.

Purpose: Remove two major back-edges into TokenExchange while preserving atomic lifecycle and delegation behavior.
Output: Compatible refresh/RFC 8693 modules with facade-independent internals.

Decision trace: Implements D-02, D-03, D-08, D-11, and D-13.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/134-architecture-topology/134-CONTEXT.md
@.planning/phases/134-architecture-topology/134-RESEARCH.md
@.planning/phases/134-architecture-topology/134-07-SUMMARY.md
@lib/lockspire/protocol/refresh_exchange.ex
@lib/lockspire/protocol/rfc8693_exchange.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate refresh rotation and reuse outcomes</name>
  <files>lib/lockspire/protocol/token_result.ex, lib/lockspire/protocol/token_exchange/compatibility.ex, lib/lockspire/protocol/token_exchange/internal/refresh_exchange.ex, lib/lockspire/protocol/refresh_exchange.ex, test/lockspire/protocol/refresh_exchange_test.exs</files>
  <behavior>
    - Successful refresh rotation retains access/refresh/ID token, expiry, scope, and token type fields.
    - Old-token reuse still revokes the family atomically and returns the same safe OAuth error/reason.
    - Refresh public facade retains TokenExchange success/error; its internal implementation never imports them.
  </behavior>
  <action>Start RED with explicit `RefreshExchange.exchange_refresh_token/2` module/arity/result compatibility plus refresh success, rotation, reuse containment, expired/revoked token, resource, DPoP, and persistence-failure characterization. Move implementation to `TokenExchange.Internal.RefreshExchange`, returning TokenResult. Keep `RefreshExchange.exchange_refresh_token/2` as a facade that delegates inward and converts only through Compatibility. Preserve repository atomicity, family-wide reuse revocation, truthful expiry, telemetry, and redaction. Plan 10 calls the internal implementation directly.</action>
  <verify><automated>mix test test/lockspire/protocol/refresh_exchange_test.exs test/lockspire/protocol/token_result_test.exs</automated></verify>
  <done>Refresh internals are facade-independent and retain atomic rotation/reuse behavior plus compatible results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate RFC 8693 exchange outcomes</name>
  <files>lib/lockspire/protocol/token_result.ex, lib/lockspire/protocol/token_exchange/compatibility.ex, lib/lockspire/protocol/token_exchange/internal/rfc8693_exchange.ex, lib/lockspire/protocol/rfc8693_exchange.ex, test/lockspire/protocol/rfc8693_exchange_test.exs</files>
  <behavior>
    - Valid subject/actor/resource/audience/delegation exchanges retain exact success fields.
    - Invalid token type, subject, actor, delegation depth, resource, and persistence paths retain exact safe errors.
    - Rfc8693Exchange public facade retains TokenExchange success/error; its internal implementation never imports them.
  </behavior>
  <action>Add RED `Rfc8693Exchange.exchange/2` module/arity/result compatibility plus success and failure matrix coverage for validation, resource/audience selection, delegation depth, signing, and persistence. Move implementation to `TokenExchange.Internal.Rfc8693Exchange`, returning TokenResult. Keep the existing module as a facade delegating inward and converting only through Compatibility. Keep host token-exchange validator, authorization boundaries, token lifetime, audit/telemetry, and redaction unchanged. Plan 10 calls the internal implementation directly.</action>
  <verify><automated>mix test test/lockspire/protocol/rfc8693_exchange_test.exs test/lockspire/protocol/token_result_test.exs</automated></verify>
  <done>RFC 8693 internals are facade-independent with exact grant and error behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Refresh credential → token family state | Rotation and reuse determine family authorization. |
| RFC 8693 input → delegated token | Subject/actor/resource policy controls privilege delegation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-134-20 | Tampering/Elevation of privilege | refresh reuse | critical | mitigate | Preserve atomic rotation and family-wide revocation tests. |
| T-134-21 | Spoofing/Elevation of privilege | RFC 8693 validation | critical | mitigate | Full subject/actor/resource/depth success/failure matrix. |
| T-134-22 | Information disclosure | token errors | high | mitigate | Safe TokenResult errors and sentinel redaction assertions. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/refresh_exchange_test.exs test/lockspire/protocol/rfc8693_exchange_test.exs test/lockspire/protocol/token_result_test.exs`
</verification>

<success_criteria>
- Neutral refresh/RFC 8693 implementations no longer depend on TokenExchange nested structs.
- `RefreshExchange.exchange_refresh_token/2` and `Rfc8693Exchange.exchange/2` remain exact compatible facades.
</success_criteria>

<output>Create `.planning/phases/134-architecture-topology/134-08-SUMMARY.md` when done.</output>
