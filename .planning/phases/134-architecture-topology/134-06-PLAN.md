---
phase: 134-architecture-topology
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/protected_resource_error.ex
  - lib/lockspire/protocol/protected_resource_dpop.ex
  - lib/lockspire/protocol/userinfo.ex
  - lib/lockspire/plug/enforce_sender_constraints.ex
  - test/lockspire/protocol/protected_resource_dpop_test.exs
  - test/lockspire/protocol/userinfo_test.exs
  - test/lockspire/plug/enforce_sender_constraints_test.exs
  - test/lockspire/web/userinfo_controller_test.exs
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "ProtectedResourceDPoP returns a neutral protected-resource error and no longer imports Userinfo.Error (D-11)."
    - "Userinfo and the host sender-constraint plug translate the neutral error into their unchanged public/status/header behavior, including DPoP nonce challenges and replay rejection (D-02, D-13)."
    - "The protected-resource-DPoP/userinfo cycle is absent from Mix xref output (D-03)."
  artifacts:
    - path: lib/lockspire/protocol/protected_resource_error.ex
      provides: narrow safe sender-constraint failure value
    - path: lib/lockspire/protocol/userinfo.ex
      provides: retained Userinfo.Error translation and endpoint contract
  key_links:
    - from: lib/lockspire/protocol/userinfo.ex
      to: lib/lockspire/protocol/protected_resource_dpop.ex
      via: neutral result translated to Userinfo.Error
      pattern: "ProtectedResourceError"
---

<objective>
Break the DPoP/userinfo type cycle while retaining both Lockspire-owned and host-resource sender-constraint behavior.

Purpose: Make the shared DPoP validator endpoint-neutral without changing nonce, replay, status, or public error contracts.
Output: Neutral protected-resource error and compatible userinfo/plug adapters.

Decision trace: Implements D-02 through D-04 and D-11 through D-13.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/134-architecture-topology/134-CONTEXT.md
@.planning/phases/134-architecture-topology/134-RESEARCH.md
@.planning/phases/132-public-api-and-resource-server-truth/132-CONTEXT.md
@lib/lockspire/protocol/protected_resource_dpop.ex
@lib/lockspire/protocol/userinfo.ex
@lib/lockspire/plug/enforce_sender_constraints.ex
@test/lockspire/protocol/protected_resource_dpop_test.exs
@test/lockspire/web/userinfo_controller_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce endpoint-neutral DPoP validation failures</name>
  <files>lib/lockspire/protocol/protected_resource_error.ex, lib/lockspire/protocol/protected_resource_dpop.ex, test/lockspire/protocol/protected_resource_dpop_test.exs</files>
  <behavior>
    - Missing/wrong authorization scheme, proof, nonce, htu/htm, ath, key binding, and replay return the same safe status/code/description/reason/nonce facts.
    - Successful validation still returns the validated DPoP proof and records replay use through the configured repository/store.
    - ProtectedResourceDPoP does not name Userinfo or its Error struct.
  </behavior>
  <action>Start RED by characterizing every current success/failure tuple and nonce/replay side effect at the ProtectedResourceDPoP boundary. Add a narrow internal `%Lockspire.Protocol.ProtectedResourceError{}` with the safe fields required by endpoint adapters. Return it from the validator and remove the Userinfo.Error alias/type edge. Preserve DPoP verification, nonce purpose, repository-backed replay, telemetry redaction, and fail-closed custom-store behavior exactly.</action>
  <verify><automated>mix test test/lockspire/protocol/protected_resource_dpop_test.exs</automated></verify>
  <done>The shared validator is endpoint-neutral and its sender-constraint success/failure semantics remain fully characterized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve userinfo and host-plug boundary contracts</name>
  <files>lib/lockspire/protocol/userinfo.ex, lib/lockspire/plug/enforce_sender_constraints.ex, test/lockspire/protocol/userinfo_test.exs, test/lockspire/plug/enforce_sender_constraints_test.exs, test/lockspire/web/userinfo_controller_test.exs</files>
  <behavior>
    - Userinfo.fetch_claims/1 still returns %Userinfo.Error{} with identical fields, HTTP status, WWW-Authenticate/DPoP-Nonce delivery, and claims behavior.
    - EnforceSenderConstraints preserves bearer/DPoP/mTLS ordering, challenge headers, assigned semantic token, and halt behavior.
    - Existing Userinfo.Error module/struct remains exported and callable.
  </behavior>
  <action>Add failing protocol/controller/plug cases for bearer, DPoP nonce retry, replay, wrong key/ath, mTLS, and error headers. Translate `ProtectedResourceError` to `%Userinfo.Error{}` inside Userinfo and to the plug's current response at the host-resource boundary; do not make either adapter depend on the other. Preserve exact status/code/headers and semantic access-token assignment. Verify the former two-node cycle is absent.</action>
  <verify><automated>mix test test/lockspire/protocol/protected_resource_dpop_test.exs test/lockspire/protocol/userinfo_test.exs test/lockspire/plug/enforce_sender_constraints_test.exs test/lockspire/web/userinfo_controller_test.exs &amp;&amp; mix xref graph --format cycles</automated></verify>
  <done>Userinfo and host API protection remain compatible, DPoP security behavior is unchanged, and the DPoP/userinfo cycle is gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| DPoP proof/token → shared validator | Sender possession and replay uniqueness authorize token use. |
| Neutral failure → endpoint response | Adapter must preserve nonce/challenge semantics without leaking proof/token. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-134-14 | Spoofing/Elevation of privilege | DPoP binding/replay | critical | mitigate | Full proof, key, ath, nonce, and replay characterization across userinfo and host plug. |
| T-134-15 | Information disclosure | error adapter | high | mitigate | Safe neutral fields only; exact header/status tests; no token/proof in telemetry/errors. |
| T-134-16 | Tampering | sender-constraint pipeline | high | mitigate | Preserve bearer/DPoP/mTLS ordering and halt/assign behavior in plug tests. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/protected_resource_dpop_test.exs test/lockspire/protocol/userinfo_test.exs test/lockspire/plug/enforce_sender_constraints_test.exs test/lockspire/web/userinfo_controller_test.exs`
- `mix xref graph --format cycles`
</verification>

<success_criteria>
- ProtectedResourceDPoP has no Userinfo dependency.
- Userinfo and host plug retain exact sender-constraint behavior and public shapes.
- The two-node cycle is removed.
</success_criteria>

<output>Create `.planning/phases/134-architecture-topology/134-06-SUMMARY.md` when done.</output>
