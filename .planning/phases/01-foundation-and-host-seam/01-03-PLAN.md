---
phase: 01-foundation-and-host-seam
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - lib/mix/tasks/lockspire.install.ex
  - lib/lockspire/generators/install.ex
  - lib/lockspire/generators/templates.ex
  - lib/lockspire/web/router.ex
  - lib/lockspire/web/controllers/interaction_controller.ex
  - lib/lockspire/web/live/consent_live.ex
  - priv/templates/lockspire.install/router.ex
  - priv/templates/lockspire.install/config.exs
  - priv/templates/lockspire.install/account_resolver.ex
  - priv/templates/lockspire.install/interaction_handler.ex
  - priv/templates/lockspire.install/consent_live.ex
  - test/integration/install_generator_test.exs
  - test/support/fixtures/generated_host_app/.keep
autonomous: true
requirements:
  - INTE-03
  - INTE-04
user_setup: []
must_haves:
  truths:
    - A host Phoenix app gets an editable integration path through generated modules and templates rather than hidden callbacks or macros.
    - Generated code covers only host-owned glue such as router mount, resolver stub, interaction completion, and consent surface.
    - The host keeps layout, branding, login UX, and policy ownership while Lockspire keeps protocol and storage internals inside the library.
  artifacts:
    - A `mix lockspire.install` task and generator modules exist.
    - Generator templates exist for router mount, config scaffold, `AccountResolver`, interaction handoff, and consent LiveView.
    - Library-owned web entrypoints exist for the mountable router and reference interaction/consent flow.
    - Integration tests prove the generator writes normal Phoenix files into a host fixture.
  key_links:
    - `mix lockspire.install` writes the templates from `priv/templates/lockspire.install` into the host app.
    - Generated `AccountResolver` and consent files reference the behaviour/types introduced in `01-01`.
    - Generated router/config glue mounts the library path without moving host branding or session ownership into Lockspire.
---

<objective>
Create the first install and host integration path: generator task, editable host glue, and mountable Phoenix entrypoints for login/consent handoff.

Purpose: Fulfill the product promise that a host Phoenix team can embed Lockspire through normal generated files while keeping ownership of app-facing UX and policy.
Output: A `mix lockspire.install` task, generator templates, mountable web entrypoints, and integration tests for the generated host code.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-host-seam/01-CONTEXT.md
@prompts/lockspire-host-app-integration-seam.md
@prompts/lockspire-oauth-oidc-implementation-playbook.md
@prompts/Oauth server jtbd and domain.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the install task and generator pipeline</name>
  <files>lib/mix/tasks/lockspire.install.ex, lib/lockspire/generators/install.ex, lib/lockspire/generators/templates.ex</files>
  <read_first>mix.exs, lib/lockspire.ex, lib/lockspire/config.ex, prompts/lockspire-oauth-oidc-implementation-playbook.md, .planning/phases/01-foundation-and-host-seam/01-CONTEXT.md</read_first>
  <action>Create a Mix task `Mix.Tasks.Lockspire.Install` that becomes invokable as `mix lockspire.install`. The task should delegate to `Lockspire.Generators.Install.run/1`, accept `--web` and `--scope` options for the host namespace, and emit clear file-change output. Implement `Lockspire.Generators.Templates` to enumerate the exact templates Phase 1 supports: `router.ex`, `config.exs`, `account_resolver.ex`, `interaction_handler.ex`, and `consent_live.ex`. Make generator writes idempotent or conflict-aware by refusing to overwrite modified files silently. Keep generator logic in the library; do not inline templates in the Mix task.</action>
  <verify>rg -n 'defmodule Mix.Tasks.Lockspire.Install|mix lockspire.install|def run\\(|router.ex|account_resolver.ex' lib/mix/tasks/lockspire.install.ex lib/lockspire/generators/install.ex lib/lockspire/generators/templates.ex</verify>
  <acceptance_criteria>
    - `lib/mix/tasks/lockspire.install.ex` contains `defmodule Mix.Tasks.Lockspire.Install`
    - `lib/mix/tasks/lockspire.install.ex` contains `mix lockspire.install`
    - `lib/lockspire/generators/install.ex` contains `def run`
    - `lib/lockspire/generators/templates.ex` contains `account_resolver.ex`
    - `lib/lockspire/generators/templates.ex` contains `consent_live.ex`
  </acceptance_criteria>
  <done>The host install flow exists as a real Mix task and generator pipeline with explicit template inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Author the generated host templates and mountable library web entrypoints</name>
  <files>lib/lockspire/web/router.ex, lib/lockspire/web/controllers/interaction_controller.ex, lib/lockspire/web/live/consent_live.ex, priv/templates/lockspire.install/router.ex, priv/templates/lockspire.install/config.exs, priv/templates/lockspire.install/account_resolver.ex, priv/templates/lockspire.install/interaction_handler.ex, priv/templates/lockspire.install/consent_live.ex</files>
  <read_first>lib/lockspire/host/account_resolver.ex, lib/lockspire/host/interaction_result.ex, lib/lockspire/storage/interaction_store.ex, prompts/lockspire-host-app-integration-seam.md, prompts/lockspire-oauth-oidc-implementation-playbook.md</read_first>
  <action>Implement a library router module that can be mounted by a host Phoenix router under the configured `:mount_path`, plus reference interaction and consent entrypoints in `Lockspire.Web.InteractionController` and `Lockspire.Web.ConsentLive`. Author generator templates that produce normal host modules: a router mount snippet using `forward "/lockspire", Lockspire.Web.Router`, a `config.exs` scaffold setting `config :lockspire, repo: ..., account_resolver: ..., mount_path: "/lockspire"`, an `AccountResolver` stub that implements `Lockspire.Host.AccountResolver`, an interaction handler module that calls the future `finish_interaction/2` flow, and a host-owned consent LiveView shell that wraps its own layout/copy around Lockspire data. Do not generate protocol services, storage schemas, or any host user schema code.</action>
  <verify>rg -n 'forward \"/lockspire\"|config :lockspire|defmodule .*AccountResolver|use Phoenix.LiveView|defmodule Lockspire.Web.Router' lib/lockspire/web priv/templates/lockspire.install</verify>
  <acceptance_criteria>
    - `lib/lockspire/web/router.ex` contains `defmodule Lockspire.Web.Router`
    - `priv/templates/lockspire.install/router.ex` contains `forward "/lockspire", Lockspire.Web.Router`
    - `priv/templates/lockspire.install/config.exs` contains `mount_path: "/lockspire"`
    - `priv/templates/lockspire.install/account_resolver.ex` contains `@behaviour Lockspire.Host.AccountResolver`
    - `priv/templates/lockspire.install/consent_live.ex` contains `use Phoenix.LiveView`
  </acceptance_criteria>
  <done>The library can be mounted and the generated host files keep UX ownership on the host side.</done>
</task>

<task type="auto">
  <name>Task 3: Verify generator output with an integration fixture</name>
  <files>test/integration/install_generator_test.exs, test/support/fixtures/generated_host_app/.keep</files>
  <read_first>lib/mix/tasks/lockspire.install.ex, lib/lockspire/generators/install.ex, lib/lockspire/generators/templates.ex, priv/templates/lockspire.install/router.ex, priv/templates/lockspire.install/account_resolver.ex</read_first>
  <action>Create `test/integration/install_generator_test.exs` to run the install generator against a fixture host app under `test/support/fixtures/generated_host_app`. The test should assert the generator writes the router snippet, config scaffold, resolver stub, interaction handler, and consent LiveView as plain files. Include assertions that the generated files contain the exact behaviour/module references from Plans 01 and 02 and that rerunning the generator is conflict-aware instead of silently replacing host edits. Keep the fixture minimal; `.keep` is enough until the test writes the rest during execution.</action>
  <verify>test -f test/integration/install_generator_test.exs && rg -n 'generated_host_app|Mix.Tasks.Lockspire.Install|AccountResolver|consent_live' test/integration/install_generator_test.exs</verify>
  <acceptance_criteria>
    - `test/integration/install_generator_test.exs` contains `generated_host_app`
    - `test/integration/install_generator_test.exs` contains `Mix.Tasks.Lockspire.Install`
    - `test/integration/install_generator_test.exs` contains `AccountResolver`
    - `test/integration/install_generator_test.exs` contains `consent_live`
    - `test/support/fixtures/generated_host_app/.keep` exists
  </acceptance_criteria>
  <done>The install path is testable and protects the promise that generated host glue stays editable and explicit.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mix test test/integration/install_generator_test.exs`
- [ ] `mix compile`
- [ ] `mix lockspire.install --help`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Generated files are normal Phoenix modules/templates owned by the host app after generation
- Host branding, layouts, login UX, and product policy remain outside Lockspire internals
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-host-seam/01-03-SUMMARY.md`
</output>
