---
phase: 01-foundation-and-host-seam
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - lib/lockspire/domain/client.ex
  - lib/lockspire/domain/interaction.ex
  - lib/lockspire/domain/consent_grant.ex
  - lib/lockspire/domain/token.ex
  - lib/lockspire/domain/signing_key.ex
  - lib/lockspire/storage/client_store.ex
  - lib/lockspire/storage/interaction_store.ex
  - lib/lockspire/storage/consent_store.ex
  - lib/lockspire/storage/token_store.ex
  - lib/lockspire/storage/key_store.ex
  - lib/lockspire/storage/ecto/client_record.ex
  - lib/lockspire/storage/ecto/interaction_record.ex
  - lib/lockspire/storage/ecto/consent_grant_record.ex
  - lib/lockspire/storage/ecto/token_record.ex
  - lib/lockspire/storage/ecto/signing_key_record.ex
  - lib/lockspire/storage/ecto/repository.ex
  - priv/repo/migrations/20260422000100_create_lockspire_core_tables.exs
  - test/lockspire/storage/repository_test.exs
autonomous: true
requirements:
  - INTE-01
  - INTE-02
user_setup: []
must_haves:
  truths:
    - Lockspire owns durable OAuth domain records through Ecto/Postgres without exposing raw `Repo` operations as the public contract.
    - Storage boundaries are domain-specific behaviours for clients, interactions, consents, tokens, and keys.
    - The default Ecto implementation is future-extensible but not falsely marketed as backend-agnostic in Phase 1.
  artifacts:
    - Domain structs exist for clients, interactions, consent grants, tokens, and signing keys.
    - Storage behaviour modules and Ecto record modules exist for each core aggregate.
    - A migration creates the core Lockspire tables with indexes and constraints suited to durable protocol truth.
    - Storage tests verify behaviour-level operations instead of generic CRUD access.
  key_links:
    - `Lockspire.Storage.Ecto.Repository` implements the behaviour modules from `Lockspire.Storage.*`.
    - The migration schema matches the fields expected by the domain structs and behaviour contracts.
    - Tests exercise the Ecto repository through store behaviours, not direct `Repo.insert!` calls from the API surface.
---

<objective>
Define the Phase 1 domain model and the default Ecto/Postgres persistence seam for clients, interactions, consents, tokens, and keys.

Purpose: Establish durable protocol truth and domain-oriented storage boundaries before any authorization or OIDC flow is implemented.
Output: Domain structs, storage behaviours, Ecto record modules, a first migration, and tests proving the adapter contract.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-host-seam/01-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@prompts/lockspire-oauth-oidc-implementation-playbook.md
@prompts/Oauth server jtbd and domain.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Model the core OAuth/OIDC aggregates as library-owned domain structs</name>
  <files>lib/lockspire/domain/client.ex, lib/lockspire/domain/interaction.ex, lib/lockspire/domain/consent_grant.ex, lib/lockspire/domain/token.ex, lib/lockspire/domain/signing_key.ex</files>
  <read_first>lib/lockspire.ex, lib/lockspire/config.ex, prompts/Oauth server jtbd and domain.md, .planning/phases/01-foundation-and-host-seam/01-CONTEXT.md</read_first>
  <action>Create domain modules for the Phase 1 aggregates using explicit structs and typespecs. `Lockspire.Domain.Client` must include fields such as `client_id`, `client_type`, `redirect_uris`, `allowed_scopes`, `allowed_grant_types`, `token_endpoint_auth_method`, and `pkce_required`. `Lockspire.Domain.Interaction` must carry `interaction_id`, `client_id`, `scopes_requested`, `prompt`, `account_id`, `expires_at`, and `return_to`. `Lockspire.Domain.ConsentGrant`, `Lockspire.Domain.Token`, and `Lockspire.Domain.SigningKey` should capture the fields the roadmap and prompt corpus identify as durable truth. Keep these modules free of Ecto macros so the domain layer remains independent from the default adapter.</action>
  <verify>rg -n 'defstruct|@type t :: %__MODULE__' lib/lockspire/domain/client.ex lib/lockspire/domain/interaction.ex lib/lockspire/domain/consent_grant.ex lib/lockspire/domain/token.ex lib/lockspire/domain/signing_key.ex</verify>
  <acceptance_criteria>
    - `lib/lockspire/domain/client.ex` contains `client_id`
    - `lib/lockspire/domain/interaction.ex` contains `interaction_id`
    - `lib/lockspire/domain/consent_grant.ex` contains `granted_at`
    - `lib/lockspire/domain/token.ex` contains `token_hash`
    - `lib/lockspire/domain/signing_key.ex` contains `kid`
  </acceptance_criteria>
  <done>The library has explicit domain structs for every core Phase 1 aggregate and none of them depend on Ecto.</done>
</task>

<task type="auto">
  <name>Task 2: Build domain-level storage behaviours and the default Ecto repository implementation</name>
  <files>lib/lockspire/storage/client_store.ex, lib/lockspire/storage/interaction_store.ex, lib/lockspire/storage/consent_store.ex, lib/lockspire/storage/token_store.ex, lib/lockspire/storage/key_store.ex, lib/lockspire/storage/ecto/client_record.ex, lib/lockspire/storage/ecto/interaction_record.ex, lib/lockspire/storage/ecto/consent_grant_record.ex, lib/lockspire/storage/ecto/token_record.ex, lib/lockspire/storage/ecto/signing_key_record.ex, lib/lockspire/storage/ecto/repository.ex</files>
  <read_first>lib/lockspire/domain/client.ex, lib/lockspire/domain/interaction.ex, lib/lockspire/domain/consent_grant.ex, lib/lockspire/domain/token.ex, lib/lockspire/domain/signing_key.ex, .planning/research/ARCHITECTURE.md, .planning/phases/01-foundation-and-host-seam/01-CONTEXT.md</read_first>
  <action>Define one behaviour module per aggregate with concrete operations, for example `register_client/1`, `fetch_client_by_id/1`, `put_interaction/1`, `fetch_active_interaction/1`, `grant_consent/1`, `list_consents_for_account/1`, `store_token/1`, `revoke_token_family/1`, `publish_key/1`, and `list_active_keys/0`. Then implement `Lockspire.Storage.Ecto.Repository` that satisfies those behaviours using Ecto schemas in `lib/lockspire/storage/ecto/*_record.ex`. The Ecto record modules must own table names, changesets, unique constraints, and casting logic. Do not create a generic `Lockspire.RepoAdapter` or expose generic CRUD functions; the only public storage contract should stay domain-level and method-specific.</action>
  <verify>rg -n '@callback (register_client|put_interaction|grant_consent|store_token|publish_key)|def (register_client|put_interaction|grant_consent|store_token|publish_key)' lib/lockspire/storage lib/lockspire/storage/ecto/repository.ex</verify>
  <acceptance_criteria>
    - `lib/lockspire/storage/client_store.ex` contains `@callback register_client/1`
    - `lib/lockspire/storage/interaction_store.ex` contains `@callback put_interaction/1`
    - `lib/lockspire/storage/token_store.ex` contains `@callback revoke_token_family/1`
    - `lib/lockspire/storage/ecto/repository.ex` contains `def register_client`
    - `lib/lockspire/storage/ecto/client_record.ex` contains `schema "lockspire_clients"`
  </acceptance_criteria>
  <done>The storage seam is behaviour-driven and the default Ecto implementation is concrete enough to support later protocol services.</done>
</task>

<task type="auto">
  <name>Task 3: Add the first core migration and repository contract tests</name>
  <files>priv/repo/migrations/20260422000100_create_lockspire_core_tables.exs, test/lockspire/storage/repository_test.exs</files>
  <read_first>lib/lockspire/storage/ecto/client_record.ex, lib/lockspire/storage/ecto/interaction_record.ex, lib/lockspire/storage/ecto/consent_grant_record.ex, lib/lockspire/storage/ecto/token_record.ex, lib/lockspire/storage/ecto/signing_key_record.ex, lib/lockspire/storage/ecto/repository.ex</read_first>
  <action>Create a migration named `20260422000100_create_lockspire_core_tables.exs` that defines `lockspire_clients`, `lockspire_interactions`, `lockspire_consent_grants`, `lockspire_tokens`, and `lockspire_signing_keys` with timestamps, exact-match/uniqueness indexes for `client_id`, `interaction_id`, `token_hash`, and `kid`, plus fields needed by the domain structs. In `test/lockspire/storage/repository_test.exs`, add ExUnit coverage for behaviour-level operations such as registering a client, storing an interaction, and loading active keys through `Lockspire.Storage.Ecto.Repository`. The tests should assert domain structs round-trip correctly and should not rely on raw `Repo` calls in the public API assertions.</action>
  <verify>test -f priv/repo/migrations/20260422000100_create_lockspire_core_tables.exs && rg -n 'create table\\(:lockspire_clients|create unique_index\\(:lockspire_clients|Lockspire.Storage.Ecto.Repository' priv/repo/migrations/20260422000100_create_lockspire_core_tables.exs test/lockspire/storage/repository_test.exs</verify>
  <acceptance_criteria>
    - `priv/repo/migrations/20260422000100_create_lockspire_core_tables.exs` contains `create table(:lockspire_clients)`
    - `priv/repo/migrations/20260422000100_create_lockspire_core_tables.exs` contains `create table(:lockspire_interactions)`
    - `priv/repo/migrations/20260422000100_create_lockspire_core_tables.exs` contains `create unique_index(:lockspire_clients, [:client_id])`
    - `test/lockspire/storage/repository_test.exs` contains `use ExUnit.Case`
    - `test/lockspire/storage/repository_test.exs` contains `Lockspire.Storage.Ecto.Repository`
  </acceptance_criteria>
  <done>Durable Postgres truth is defined by migration plus repository tests that protect the storage contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mix test test/lockspire/storage/repository_test.exs`
- [ ] `mix ecto.migrate`
- [ ] `mix compile`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Core aggregates, stores, and Ecto records are separated by boundary rather than collapsed into one generic persistence layer
- The repository contract is domain-specific and supports later protocol services without changing the host seam
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-host-seam/01-02-SUMMARY.md`
</output>
