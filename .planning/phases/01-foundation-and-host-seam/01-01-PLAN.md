---
phase: 01-foundation-and-host-seam
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .formatter.exs
  - config/config.exs
  - lib/lockspire.ex
  - lib/lockspire/application.ex
  - lib/lockspire/config.ex
  - lib/lockspire/host/account_resolver.ex
  - lib/lockspire/host/claims.ex
  - lib/lockspire/host/interaction_result.ex
  - test/lockspire/config_test.exs
autonomous: true
requirements:
  - INTE-01
  - INTE-02
user_setup: []
must_haves:
  truths:
    - The library boots as an embedded OTP application and exposes a narrow `Lockspire` API instead of requiring a standalone auth service.
    - Host integration is configured through explicit runtime modules, with `Lockspire.Host.AccountResolver` defining the account/login/claims seam.
    - The root namespace, supervision tree, and config helpers preserve internal boundaries between protocol, storage, generators, and web integration.
  artifacts:
    - mix.exs defines the `:lockspire` application and core dependencies for Phoenix, LiveView, Ecto SQL, PostgreSQL, Oban, and telemetry.
    - lib/lockspire.ex, lib/lockspire/application.ex, and lib/lockspire/config.ex exist with real public API and boot/config code.
    - lib/lockspire/host/account_resolver.ex and companion host seam types exist as behaviours and structs, not loose callback config.
    - test/lockspire/config_test.exs covers the config and host seam contract.
  key_links:
    - `Lockspire.Config.account_resolver!/0` returns the module configured under `config :lockspire, :account_resolver`.
    - `Lockspire.Application` supervises only library-owned services and does not assume control of host sessions or accounts.
    - The root `Lockspire` module delegates host seam lookups through `Lockspire.Config` and `Lockspire.Host.AccountResolver`.
---

<objective>
Establish the base Elixir/Phoenix library structure, runtime configuration model, and host seam contract that every later Lockspire phase builds on.

Purpose: Lock the embedded-library shape before protocol work begins, so later plans inherit a small public API, explicit host ownership, and stable internal boundaries.
Output: A bootable `:lockspire` application skeleton, configuration helpers, and behaviour modules for host account resolution and interaction handoff.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-and-host-seam/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/ARCHITECTURE.md
@lockspire-idea.md
@prompts/lockspire-host-app-integration-seam.md
@prompts/lockspire-oauth-oidc-implementation-playbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the base OTP application and dependency skeleton</name>
  <files>mix.exs, .formatter.exs, config/config.exs, lib/lockspire/application.ex</files>
  <read_first>lockspire-idea.md, .planning/research/ARCHITECTURE.md, .planning/phases/01-foundation-and-host-seam/01-CONTEXT.md</read_first>
  <action>Create the initial Mix project files for the `:lockspire` library. Define `Lockspire.MixProject` with `app: :lockspire`, `version: "0.1.0"`, `elixir: "~> 1.18"`, and dependencies for `:phoenix`, `:phoenix_live_view`, `:ecto_sql`, `:postgrex`, `:oban`, and `:opentelemetry_api`. Set `application/0` to start `Lockspire.Application` and include `:logger`, `:runtime_tools`, and `:telemetry`. Add `.formatter.exs` entries for `mix.exs`, `config/*.exs`, `lib/**/*.ex`, and `test/**/*.exs`. In `config/config.exs`, define the runtime keys `:repo`, `:account_resolver`, `:issuer`, `:mount_path`, and `:oban` under `config :lockspire`. Implement `Lockspire.Application` so it starts only library-owned services, leaving room for later storage, telemetry, and generator supervisors without assuming any host auth/session responsibility.</action>
  <verify>test -f mix.exs && test -f lib/lockspire/application.ex && rg -n 'app: :lockspire|def application do|config :lockspire' mix.exs config/config.exs lib/lockspire/application.ex</verify>
  <acceptance_criteria>
    - `mix.exs` contains `app: :lockspire`
    - `mix.exs` contains `mod: {Lockspire.Application, []}`
    - `mix.exs` lists `{:phoenix,` and `{:ecto_sql,`
    - `config/config.exs` contains `config :lockspire,`
    - `lib/lockspire/application.ex` contains `defmodule Lockspire.Application do`
  </acceptance_criteria>
  <done>The repository has a real library skeleton with the required dependencies, formatter config, runtime config keys, and OTP application module.</done>
</task>

<task type="auto">
  <name>Task 2: Define the narrow public API and host seam behaviours</name>
  <files>lib/lockspire.ex, lib/lockspire/config.ex, lib/lockspire/host/account_resolver.ex, lib/lockspire/host/claims.ex, lib/lockspire/host/interaction_result.ex</files>
  <read_first>lockspire-idea.md, prompts/lockspire-host-app-integration-seam.md, prompts/Oauth server jtbd and domain.md, .planning/phases/01-foundation-and-host-seam/01-CONTEXT.md</read_first>
  <action>Implement `lib/lockspire.ex` as a small façade that exposes `config/0`, `issuer/0`, `mount_path/0`, and `account_resolver!/0` rather than protocol-heavy entrypoints. Add `Lockspire.Config` with explicit getters for `repo!/0`, `account_resolver!/0`, `issuer!/0`, `mount_path/0`, and `oban_config/0`; failures must raise actionable errors naming the missing config key. Create `Lockspire.Host.AccountResolver` as the singular host seam behaviour with callbacks for `resolve_current_account/2`, `resolve_account/2`, `build_claims/2`, and `redirect_for_login/2`. Add companion types/structs in `Lockspire.Host.Claims` and `Lockspire.Host.InteractionResult` so the contract is typed and structured instead of free-form maps. Keep host-owned concerns out of the library API: do not add user schema modules, session helpers, macros, or generic callback registries.</action>
  <verify>rg -n 'def account_resolver!|@callback resolve_current_account|defmodule Lockspire.Host.Claims|defmodule Lockspire.Host.InteractionResult' lib/lockspire.ex lib/lockspire/config.ex lib/lockspire/host</verify>
  <acceptance_criteria>
    - `lib/lockspire.ex` contains `def account_resolver!`
    - `lib/lockspire/config.ex` contains `def repo!` and `def account_resolver!`
    - `lib/lockspire/host/account_resolver.ex` contains `@callback resolve_current_account/2`
    - `lib/lockspire/host/account_resolver.ex` contains `@callback build_claims/2`
    - `lib/lockspire/host/interaction_result.ex` contains `defstruct`
  </acceptance_criteria>
  <done>The public API is intentionally small and the host seam is represented by explicit modules, callbacks, and structs.</done>
</task>

<task type="auto">
  <name>Task 3: Add contract-focused tests for runtime config and host seam wiring</name>
  <files>test/lockspire/config_test.exs</files>
  <read_first>mix.exs, config/config.exs, lib/lockspire.ex, lib/lockspire/config.ex, lib/lockspire/host/account_resolver.ex</read_first>
  <action>Create `test/lockspire/config_test.exs` using ExUnit to lock the Phase 1 contract. Cover the happy path where `config :lockspire` provides a repo module, issuer, mount path, and account resolver. Add failure assertions showing `Lockspire.Config.repo!/0` and `Lockspire.Config.account_resolver!/0` raise clear errors when missing. Add a test module implementing `Lockspire.Host.AccountResolver` so the test suite verifies the callback surface is usable by a host app without macros. Keep tests focused on boundary and configuration behaviour rather than protocol implementation.</action>
  <verify>test -f test/lockspire/config_test.exs && rg -n 'ExUnit|Lockspire.Config.repo!|Lockspire.Config.account_resolver!|defmodule .*AccountResolver' test/lockspire/config_test.exs</verify>
  <acceptance_criteria>
    - `test/lockspire/config_test.exs` contains `use ExUnit.Case`
    - `test/lockspire/config_test.exs` contains `Lockspire.Config.repo!`
    - `test/lockspire/config_test.exs` contains `Lockspire.Config.account_resolver!`
    - `test/lockspire/config_test.exs` defines a test resolver module implementing `Lockspire.Host.AccountResolver`
  </acceptance_criteria>
  <done>The base contract is guarded by tests that will fail if the host seam or config model drifts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mix test test/lockspire/config_test.exs`
- [ ] `mix format --check-formatted`
- [ ] `mix compile`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No macros, user-schema ownership, or standalone-service assumptions are introduced
- The configured host seam is singular, explicit, and discoverable from the root `Lockspire` API
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-host-seam/01-01-SUMMARY.md`
</output>
