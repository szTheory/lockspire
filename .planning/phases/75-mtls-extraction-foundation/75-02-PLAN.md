---
phase: 75-mtls-extraction-foundation
plan: 02
type: execute
wave: 2
depends_on: [75-01]
files_modified:
  - lib/lockspire/mtls/plug.ex
  - test/lockspire/mtls/plug_test.exs
autonomous: true
requirements: [MTLS-EXT-04]
must_haves:
  truths:
    - MTLS Plug successfully puts extracted cert into conn.private[:lockspire_mtls_cert]
    - MTLS Plug halts pipeline with HTTP 400 when cert is invalid or missing
  artifacts:
    - path: lib/lockspire/mtls/plug.ex
      provides: Plug middleware for MTLS extraction
  key_links:
    - from: lib/lockspire/mtls/plug.ex
      to: lib/lockspire/mtls/extractor.ex
      via: invoking the configured extractor
      pattern: extractor.extract(conn, opts)
---

<objective>
Implement explicitly configured Plug middleware to extract Mutual TLS (mTLS) client certificates.
Purpose: Allow host applications to explicitly opt-in to mTLS extraction on a per-route basis, preventing spoofed header vulnerabilities on unconfigured routes.
Output: The `Lockspire.MTLS.Plug` module with tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-mtls-extraction-foundation/75-RESEARCH.md
@.planning/phases/75-mtls-extraction-foundation/75-PATTERNS.md
@.planning/phases/75-mtls-extraction-foundation/75-01-SUMMARY.md
</context>

<interfaces>
From lib/lockspire/mtls/extractor.ex:
```elixir
defmodule Lockspire.MTLS.Extractor do
  @callback extract(Plug.Conn.t(), keyword()) :: {:ok, binary()} | {:error, atom()}
end
```
</interfaces>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Implement MTLS Plug</name>
  <files>lib/lockspire/mtls/plug.ex, test/lockspire/mtls/plug_test.exs</files>
  <behavior>
    - Test 1: Given a valid configuration and successful extraction, sets `conn.private[:lockspire_mtls_cert]` to the extracted DER binary and continues the pipeline.
    - Test 2: Given an extraction error `{:error, reason}`, returns HTTP 400 with a JSON error body and halts the pipeline.
  </behavior>
  <action>
    Implement `Lockspire.MTLS.Plug` as a standard `Plug`.
    In `init/1`, it should accept and return options, requiring an `:extractor` key formatted as `{ExtractorModule, options}`.
    In `call/2`, invoke the configured extractor: `module.extract(conn, opts)`.
    If `{:ok, cert}` is returned, assign it via `Plug.Conn.put_private(conn, :lockspire_mtls_cert, cert)`.
    If `{:error, _}` is returned, format a JSON error response (e.g., `{"error": "invalid_request", "error_description": "Valid client certificate required"}`) and halt the request with `send_resp(400, body)`.
    Use `Lockspire.Protocol.FAPI20EnforcerPlug` error handling patterns as shown in `75-PATTERNS.md`.
  </action>
  <verify>
    <automated>mix test test/lockspire/mtls/plug_test.exs</automated>
  </verify>
  <done>Plug properly enforces mTLS extraction and saves the certificate to the connection.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network to Router | Explicit router placement enforces mTLS only where expected, ignoring spoofable headers on other routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03 | Spoofing | Plug | mitigate | Enforces that extraction happens explicitly, dropping requests entirely if extraction fails rather than leaving them in a half-authenticated state. |
</threat_model>

<verification>
mix format
mix test test/lockspire/mtls/plug_test.exs
</verification>

<success_criteria>
- Host can configure `plug Lockspire.MTLS.Plug, extractor: {Mod, opts}`.
- Successful extraction exposes `conn.private[:lockspire_mtls_cert]`.
- Failed extraction halts the plug.
- All tests for plug run and pass.
</success_criteria>

<output>
After completion, create `.planning/phases/75-mtls-extraction-foundation/75-02-SUMMARY.md`
</output>
