---
phase: 75-mtls-extraction-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/mtls/extractor.ex
  - lib/lockspire/mtls/cowboy_direct_extractor.ex
  - test/lockspire/mtls/cowboy_direct_extractor_test.exs
  - lib/lockspire/mtls/proxy_header_extractor.ex
  - test/lockspire/mtls/proxy_header_extractor_test.exs
autonomous: true
requirements: [MTLS-EXT-01, MTLS-EXT-02, MTLS-EXT-03]
must_haves:
  truths:
    - Client certificates are reliably extracted from Cowboy connections
    - Client certificates are correctly parsed from standard proxy headers
    - Proxy header extractors correctly handle URL-encoded PEM formats
    - Proxy header extractors correctly handle Envoy XFCC formats
    - Invalid proxy headers are rejected safely without crashing
  artifacts:
    - path: lib/lockspire/mtls/extractor.ex
      provides: Extractor behaviour definition
    - path: lib/lockspire/mtls/cowboy_direct_extractor.ex
      provides: Cowboy direct extraction implementation
    - path: lib/lockspire/mtls/proxy_header_extractor.ex
      provides: Proxy header extraction implementation
  key_links:
    - from: lib/lockspire/mtls/cowboy_direct_extractor.ex
      to: Plug.Conn.get_peer_data
      via: direct socket retrieval
      pattern: Plug.Conn.get_peer_data
    - from: lib/lockspire/mtls/proxy_header_extractor.ex
      to: ":public_key"
      via: PEM decoding
      pattern: :public_key.pem_decode
---

<objective>
Establish the foundation for extracting Mutual TLS (mTLS) client certificates.
Purpose: Create the `Lockspire.MTLS.Extractor` behaviour and implement the Cowboy-native and Proxy Header extraction primitives. This ensures the system can extract certificates safely without trusting implicit spoofable headers.
Output: The `Extractor` behaviour, `CowboyDirectExtractor`, and `ProxyHeaderExtractor` modules with comprehensive tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/75-mtls-extraction-foundation/75-RESEARCH.md
@.planning/phases/75-mtls-extraction-foundation/75-PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Task 0: Define Extractor Behaviour</name>
  <files>lib/lockspire/mtls/extractor.ex</files>
  <action>
    Create the `Lockspire.MTLS.Extractor` module. 
    It MUST define a single callback: `@callback extract(Plug.Conn.t(), keyword()) :: {:ok, binary()} | {:error, atom()}`.
    The binary returned MUST be the raw DER-encoded certificate.
    Use `Lockspire.Host.TokenExchangeValidator` from `75-PATTERNS.md` as an analog for the behaviour definition layout.
  </action>
  <verify>
    <automated>mix format lib/lockspire/mtls/extractor.ex</automated>
  </verify>
  <done>File exists and successfully compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement Cowboy Direct Extractor</name>
  <files>lib/lockspire/mtls/cowboy_direct_extractor.ex, test/lockspire/mtls/cowboy_direct_extractor_test.exs</files>
  <behavior>
    - Test 1: Successful extraction when `conn` has `ssl_cert` populated in `Plug.Conn.get_peer_data(conn)`.
    - Test 2: Error `{:error, :no_cert}` when `ssl_cert` is nil or missing.
  </behavior>
  <action>
    Implement `Lockspire.MTLS.CowboyDirectExtractor` adopting `Lockspire.MTLS.Extractor`.
    Use `Plug.Conn.get_peer_data(conn)[:ssl_cert]`. Since it is natively a raw DER-encoded binary, return it directly inside `{:ok, cert}`.
    If it is missing, return `{:error, :no_cert}`.
    Use `lib/lockspire/host/default_delegation_validator.ex` as an analog reference for implementing and documenting the behaviour.
  </action>
  <verify>
    <automated>mix test test/lockspire/mtls/cowboy_direct_extractor_test.exs</automated>
  </verify>
  <done>Tests pass and module conforms to Extractor behaviour.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Proxy Header Extractor</name>
  <files>lib/lockspire/mtls/proxy_header_extractor.ex, test/lockspire/mtls/proxy_header_extractor_test.exs</files>
  <behavior>
    - Test 1: Given a `format: :url_encoded_pem` option, successfully decode URI-encoded PEM and return the DER binary using `:public_key.pem_decode/1`.
    - Test 2: Given a `format: :envoy_xfcc` option, parse the `Cert="..."` parameter correctly from the string and return the DER binary.
    - Test 3: Return `{:error, :missing_header}` when the requested header is absent.
    - Test 4: Return `{:error, :invalid_format}` when the parsing or decoding fails.
  </behavior>
  <action>
    Implement `Lockspire.MTLS.ProxyHeaderExtractor` adopting `Lockspire.MTLS.Extractor`.
    Retrieve the configured header from `Plug.Conn.get_req_header/2`.
    Support the `format` option with two modes:
    1. `:url_encoded_pem`: Decode the URI string and parse the PEM.
    2. `:envoy_xfcc`: Extract the certificate from the `Cert="..."` component in the XFCC string, then decode the URI string and parse the PEM.
    Use `:public_key.pem_decode(pem_string)` to retrieve `[{:Certificate, der, :not_encrypted} | _]`.
    DO NOT hand-roll PEM parsers; use `:public_key` strictly as defined in RESEARCH.md.
    Use `lib/lockspire/host/default_delegation_validator.ex` as an analog reference for implementing and documenting the behaviour.
  </action>
  <verify>
    <automated>mix test test/lockspire/mtls/proxy_header_extractor_test.exs</automated>
  </verify>
  <done>Tests pass and the module can extract both formats safely.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Proxy to Application | Edge proxy passes potentially untrusted/spoofable HTTP headers into the system. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Spoofing | ProxyHeaderExtractor | mitigate | The extractor itself does not trust headers implicitly; it requires explicit initialization configuration to run on a specific route. |
| T-75-02 | Denial of Service | ProxyHeaderExtractor | mitigate | Relies on built-in fast OTP C-based parsers (`:public_key.pem_decode`) instead of slow Regex to mitigate ASN.1 parsing DoS vectors. |
</threat_model>

<verification>
mix format
mix test test/lockspire/mtls/
</verification>

<success_criteria>
- The `Lockspire.MTLS.Extractor` behaviour is fully defined.
- `CowboyDirectExtractor` correctly unwraps native DER strings.
- `ProxyHeaderExtractor` successfully extracts and converts PEM from `x-forwarded-client-cert` in two formats.
- All tests for extractors run and pass.
</success_criteria>

<output>
After completion, create `.planning/phases/75-mtls-extraction-foundation/75-01-SUMMARY.md`
</output>