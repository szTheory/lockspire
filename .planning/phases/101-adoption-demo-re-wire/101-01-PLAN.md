---
phase: 101-adoption-demo-re-wire
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/protect-phoenix-api-routes.md
  - examples/adoption_demo/lib/adoption_demo_web/router.ex
  - priv/templates/lockspire.install/router.ex
  - scripts/demo/adoption_smoke.py
autonomous: true
requirements: [DEMO-03]

must_haves:
  truths:
    - "The canonical pipeline block's VerifyToken line declares audience: \"https://billing.acme-ledger.test\" (absolute HTTPS URI) in all four RECIPE-01 sites"
    - "The string audience: \"billing-api\" no longer appears in any of the four canonical pipeline blocks"
    - "The prose example at docs/protect-phoenix-api-routes.md (~line 42, OUTSIDE the BEGIN/END markers) no longer shows the broken bare-string audience: \"billing-api\" — it shows the absolute URI for doc consistency"
    - "release_readiness_contract_test passes: the normalized SHA-256 of the canonical block is byte-identical across all four files"
  artifacts:
    - path: "docs/protect-phoenix-api-routes.md"
      provides: "Canonical block with absolute-URI audience (hash-locked site 1) + prose example aligned to the same URI"
      contains: "audience: \"https://billing.acme-ledger.test\""
    - path: "examples/adoption_demo/lib/adoption_demo_web/router.ex"
      provides: "Canonical block with absolute-URI audience (hash-locked site 2)"
      contains: "audience: \"https://billing.acme-ledger.test\""
    - path: "priv/templates/lockspire.install/router.ex"
      provides: "Canonical block with absolute-URI audience (hash-locked site 3)"
      contains: "audience: \"https://billing.acme-ledger.test\""
    - path: "scripts/demo/adoption_smoke.py"
      provides: "Commented canonical block with absolute-URI audience (hash-locked site 4)"
      contains: "audience: \"https://billing.acme-ledger.test\""
  key_links:
    - from: "all four canonical pipeline blocks"
      to: "release_readiness_contract_test normalized SHA-256 clause"
      via: "byte-identical audience: edit applied to every site"
      pattern: "audience: \"https://billing\\.acme-ledger\\.test\""
---

<objective>
Replace the bare, non-absolute `audience: "billing-api"` in the canonical `# BEGIN/END LOCKSPIRE_PROTECTED_PIPELINE` block with the absolute HTTPS URI `https://billing.acme-ledger.test` (D-01), applied byte-identically across all four hash-locked RECIPE-01 sites (D-03), and align the doc's prose example (outside the markers) to the same URI so the published doc never shows the broken bare-string pattern. This satisfies DEMO-03 (the demo router declares an explicit `audience:` matching the `resource=` URI used at token request) and keeps the four-file content-hash lock intact.

Purpose: The bare string `"billing-api"` is not a valid resource URI — `valid_resource_uri?` (`authorization_request.ex:617`) rejects it as `:invalid_target`, so a demo that tried to mint a token for it would never round-trip to 200. The validated resource flows verbatim into the token `aud` list, and `VerifyToken.validate_audience` does an exact `Enum.member?` against the configured `audience` with no normalization — so both the route's `audience:` and the smoke's `resource=` MUST be the identical absolute URI. This plan settles that URI in the block; Plan 02 (Wave 2) echoes it at runtime.

Output: All four canonical blocks declare `audience: "https://billing.acme-ledger.test"`; the doc's prose example shows the same URI; the contract-hash lock still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-adoption-demo-re-wire/101-CONTEXT.md
@.planning/phases/97-contract-docs-first/97-CONTEXT.md

<interfaces>
<!-- The hash-lock mechanism the edit must satisfy. From test/lockspire/release_readiness_contract_test.exs. -->
<!-- normalize/2 for :python_commented and :elixir_in_commented_heredoc strips a leading "# " prefix -->
<!-- per line, then strips uniform indent and trailing whitespace; the plain branch strips uniform -->
<!-- indent + trailing whitespace only. After normalization, all four blocks MUST hash identically. -->
<!-- Practical consequence: the VerifyToken line's text after the "# " comment prefix (in the .py and -->
<!-- install-template commented forms) and after de-indent (in the .ex/.md plain forms) must be the -->
<!-- same bytes. Changing only the audience VALUE keeps every other byte the same, so a byte-identical -->
<!-- value edit on the VerifyToken line in all four files keeps the hashes equal. -->
<!-- extract_canonical_pipeline! captures ONLY the bytes between the BEGIN/END markers (regex, lines -->
<!-- 145-150). The doc's prose example at ~line 42 sits OUTSIDE the markers, so editing it does NOT -->
<!-- affect any contract hash — it is a doc-consistency edit only. -->

The four RECIPE-01 sites and their kinds (from the contract test):
- docs/protect-phoenix-api-routes.md            (plain fenced elixir, ~line 18)
- examples/adoption_demo/lib/adoption_demo_web/router.ex  (plain elixir, ~line 25)
- priv/templates/lockspire.install/router.ex    (elixir_in_commented_heredoc, ~line 13 — "# " prefixed)
- scripts/demo/adoption_smoke.py                (python_commented, ~line 246 — "# " prefixed)

Current VerifyToken line content (identical after normalization in all four):
  plug Lockspire.Plug.VerifyToken, scopes: ["read:billing"], audience: "billing-api", enforce_audience: true

Doc prose example carrying the same bare string OUTSIDE the markers (docs/protect-phoenix-api-routes.md ~line 42):
  See the canonical pipeline above; this example pins `audience:` (e.g., `audience: "billing-api"`) to constrain the route ...
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace audience value with the absolute URI in all four hash-locked canonical blocks and align the doc prose example</name>
  <files>docs/protect-phoenix-api-routes.md, examples/adoption_demo/lib/adoption_demo_web/router.ex, priv/templates/lockspire.install/router.ex, scripts/demo/adoption_smoke.py</files>
  <read_first>
    - docs/protect-phoenix-api-routes.md (site 1 — find the VerifyToken line inside the BEGIN/END markers, ~line 18; AND the prose example outside the markers, ~line 42)
    - examples/adoption_demo/lib/adoption_demo_web/router.ex (site 2 — VerifyToken line ~line 25)
    - priv/templates/lockspire.install/router.ex (site 3 — commented "# " VerifyToken line inside the heredoc, ~line 13)
    - scripts/demo/adoption_smoke.py (site 4 — commented "# " VerifyToken line in exercise_authorization_code(), ~line 246)
    - test/lockspire/release_readiness_contract_test.exs lines 140-214 and 745-790 (the extract_canonical_pipeline!/normalize/canonical_hash! helpers and the two clauses that assert byte-identical hash + non-empty audience across the four sites — read so you understand exactly what your edit must preserve; note the BEGIN/END regex extracts inside-marker bytes only, so the ~line-42 prose is out of scope of the hash)
  </read_first>
  <action>
    Step A — the four hash-locked blocks (D-01/D-03): In each of the four files, locate the `Lockspire.Plug.VerifyToken` line inside the `# BEGIN/END LOCKSPIRE_PROTECTED_PIPELINE` markers and change ONLY the audience value from `"billing-api"` to `"https://billing.acme-ledger.test"`. Do not touch any other byte on the line: keep `scopes: ["read:billing"]` and `enforce_audience: true` exactly as-is, keep the leading `# ` comment prefix on the install-template (site 3) and Python smoke (site 4) forms, keep the plain (uncommented) form on the doc fenced block (site 1) and demo router (site 2). The edit is a single-value substitution applied identically to all four; editing fewer than four reds CI on the normalized-SHA-256 mismatch clause.

    Step B — the doc prose example (doc-consistency, NOT hash-locked): In docs/protect-phoenix-api-routes.md, the prose paragraph at ~line 42 sits OUTSIDE the BEGIN/END markers and currently reads `this example pins `audience:` (e.g., `audience: "billing-api"`) to constrain the route ...`. Update the inline example value `"billing-api"` there to `"https://billing.acme-ledger.test"` so the published doc never displays the broken bare-string audience anywhere. This line is outside the marker regex, so it does NOT change any contract hash — it exists only to keep the rendered doc internally consistent with the block above it.

    Make NO other changes in this plan — the smoke's runtime `resource=` literal is added in Plan 02 (Wave 2).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && grep -rc 'audience: "billing-api"' docs/protect-phoenix-api-routes.md examples/adoption_demo/lib/adoption_demo_web/router.ex priv/templates/lockspire.install/router.ex scripts/demo/adoption_smoke.py | grep -v ':0$' && echo "FAIL: stale billing-api audience still present (block or prose)" && exit 1 || { for f in examples/adoption_demo/lib/adoption_demo_web/router.ex priv/templates/lockspire.install/router.ex scripts/demo/adoption_smoke.py; do [ "$(grep -c 'audience: "https://billing.acme-ledger.test"' "$f")" -eq 1 ] || { echo "FAIL: $f does not contain exactly one absolute-URI audience"; exit 1; }; done; [ "$(grep -c 'audience: "https://billing.acme-ledger.test"' docs/protect-phoenix-api-routes.md)" -eq 2 ] || { echo "FAIL: docs/protect-phoenix-api-routes.md must contain exactly two absolute-URI audience occurrences (block line ~18 + prose line ~42)"; exit 1; }; mix test test/lockspire/release_readiness_contract_test.exs -o no_color; }</automated>
  </verify>
  <acceptance_criteria>
    - docs/protect-phoenix-api-routes.md contains `audience: "https://billing.acme-ledger.test"` inside the BEGIN/END markers
    - docs/protect-phoenix-api-routes.md prose example (~line 42, outside the markers) shows `audience: "https://billing.acme-ledger.test"` (no bare `"billing-api"` anywhere in the file)
    - examples/adoption_demo/lib/adoption_demo_web/router.ex contains exactly one `audience: "https://billing.acme-ledger.test"` inside the BEGIN/END markers
    - priv/templates/lockspire.install/router.ex contains exactly one `# ` + `audience: "https://billing.acme-ledger.test"` inside the BEGIN/END markers
    - scripts/demo/adoption_smoke.py contains exactly one `# ` + `audience: "https://billing.acme-ledger.test"` inside the BEGIN/END markers
    - The string `audience: "billing-api"` appears zero times across all four files (block AND doc prose)
    - `mix test test/lockspire/release_readiness_contract_test.exs` passes — specifically the clause "canonical lockspire_protected_api pipeline is byte-identical across the four RECIPE-01 sites" and the clause "canonical lockspire_protected_api pipeline declares a non-empty audience: across all four RECIPE-01 sites (D-07)"
  </acceptance_criteria>
  <done>All four canonical blocks declare the absolute-URI audience, the doc prose example matches, release_readiness_contract_test passes with byte-identical hashes, and no stale `billing-api` audience remains anywhere.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → host protected route (`/api/billing/summary`) | A bearer `at+jwt` crosses here; `VerifyToken` checks `aud` exact-match against the route's `audience:` |
| edit-time → four hash-locked source files | The audience value must propagate identically; divergence is a contract-integrity hazard caught by CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Spoofing/Elevation (token substitution) | `audience:` on `VerifyToken` in the canonical block | mitigate | Use the absolute URI `https://billing.acme-ledger.test` (D-01). An absolute, resource-specific audience prevents a token minted for one resource being accepted by another (audience-confusion). The bare `"billing-api"` both failed `valid_resource_uri?` AND was a non-specific label; the absolute URI is the structural fix. HIGH-severity bug-pattern (cross-API token reuse) is closed by this exact-match audience. |
| T-101-02 | Tampering (silent divergence of the hash-lock guard) | The four RECIPE-01 canonical blocks | mitigate | Apply the audience edit byte-identically to all four files (D-03); `release_readiness_contract_test`'s normalized-SHA-256 pairwise clause fails loudly on any divergence, so a partial/typo'd edit cannot silently ship. This guard is the integrity control for the audience change. |

No threat in this plan rises to HIGH-and-unmitigated: T-101-01 (the audience-confusion class) is the bug this plan fixes and is mitigated by the absolute-URI audience; T-101-02 is mitigated by the existing CI hash-lock.
</threat_model>

<verification>
- `mix test test/lockspire/release_readiness_contract_test.exs` passes (byte-identical hash + non-empty audience clauses).
- `grep -rc 'audience: "billing-api"' <four files>` returns 0 matches (block AND doc prose).
- `grep -c 'audience: "https://billing.acme-ledger.test"'` returns exactly 1 in the three non-doc files and exactly 2 in docs/protect-phoenix-api-routes.md (block line ~18 + prose line ~42).
</verification>

<success_criteria>
All four canonical pipeline blocks declare `audience: "https://billing.acme-ledger.test"`, the doc prose example matches, the prior `"billing-api"` value is gone everywhere, and `release_readiness_contract_test` passes — satisfying DEMO-03 and preserving the RECIPE-01 four-file hash lock.
</success_criteria>

<output>
Create `.planning/phases/101-adoption-demo-re-wire/101-01-SUMMARY.md` when done.
</output>
