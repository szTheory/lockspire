---
phase: 101-adoption-demo-re-wire
plan: 02
type: execute
wave: 2
depends_on: ["101-01"]
files_modified:
  - scripts/demo/adoption_smoke.py
autonomous: true
requirements: [DEMO-01, DEMO-02]

must_haves:
  truths:
    - "exercise_authorization_code() sends resource=https://billing.acme-ledger.test on BOTH the /authorize and /token requests"
    - "exercise_authorization_code() calls GET /api/billing/summary with Authorization: Bearer <issued access_token> and asserts HTTP 200"
    - "The existing anonymous GET /api/billing/summary -> 401 assertion is preserved"
    - "The existing /userinfo stored-opaque 200 assertion (and the alice@acme.test email check) is preserved unchanged"
    - "The CI Adoption Demo Smoke job fails loudly if the 200-with-issued-token round-trip regresses"
  artifacts:
    - path: "scripts/demo/adoption_smoke.py"
      provides: "Auth-code -> at+jwt -> /api/billing/summary -> 200 round-trip plus preserved 401 and /userinfo assertions"
      contains: "https://billing.acme-ledger.test"
  key_links:
    - from: "exercise_authorization_code() /authorize + /token requests"
      to: "minted at+jwt aud claim"
      via: "resource= param equal to the block's audience URI (D-04)"
      pattern: "resource.*https://billing\\.acme-ledger\\.test"
    - from: "exercise_authorization_code() GET /api/billing/summary"
      to: "VerifyToken audience exact-match"
      via: "Authorization: Bearer <issued access_token> asserting HTTP 200"
      pattern: "api/billing/summary"
---

<objective>
Re-wire `scripts/demo/adoption_smoke.py`'s `exercise_authorization_code()` so it requests a resource-bound `at+jwt` and proves the blessed host-API path end-to-end: send `resource=https://billing.acme-ledger.test` on both the `/authorize` and `/token` requests (D-02), then `GET /api/billing/summary` with `Authorization: Bearer <issued access_token>` asserting HTTP 200 (D-07). Keep the existing anonymous-401 assertion and the existing `/userinfo` stored-opaque assertion unchanged. This satisfies DEMO-01 (end-to-end auth-code -> at+jwt -> 200) and DEMO-02 (the smoke's new 200-with-issued-token assertion; the anonymous-401 is no longer the sole RS-protection proof).

Purpose: The current smoke only proves `401-on-anonymous` — a half-proof. After Phase 99 flipped default issuance to `:jwt` and Phase 100 proved the bound-token pipeline, the demo can and must show the full positive path an adopter copies. The runtime `resource=` literal MUST equal the URI baked into the canonical block by Plan 01 (D-04) or the round-trip reproduces the audience-mismatch 401.

Output: A smoke that exercises and asserts the full positive RS-protection round-trip, with the negative (401) and Lockspire-owned (/userinfo) assertions preserved, run by the CI Adoption Demo Smoke job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-adoption-demo-re-wire/101-CONTEXT.md
@.planning/phases/99-signer-extraction-jwt-default-issuance/99-CONTEXT.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-CONTEXT.md

<interfaces>
<!-- Existing structures in adoption_smoke.py the executor builds on. Do not re-explore the codebase. -->

exercise_authorization_code() current shape (scripts/demo/adoption_smoke.py):
  - authorize_params dict (lines ~177-187): response_type/client_id/redirect_uri/scope/state/nonce/prompt/code_challenge/code_challenge_method. scope already includes "read:billing".
  - GET /lockspire/authorize?<urlencoded authorize_params> (line ~189)
  - POST /lockspire/token with grant_type=authorization_code, client_id, redirect_uri, code, code_verifier (lines ~219-229)
  - token_json = json_body(...); token_json["access_token"] is the issued at+jwt (line ~232)
  - GET /lockspire/userinfo with Authorization: Bearer <access_token>; asserts 200 (label "userinfo accepts issued access token", line ~240) + email == "alice@acme.test" (line ~242) -- PRESERVE UNCHANGED
  - commented BEGIN/END LOCKSPIRE_PROTECTED_PIPELINE block (lines ~244-251) -- already carries the absolute-URI audience after Plan 01; DO NOT edit it here
  - anonymous_api = Browser(BASE_URL).request("GET", "/api/billing/summary"); assert_status(..., 401, "protected API rejects anonymous request") (lines ~253-254) -- PRESERVE

Helpers available: assert_status(response, expected, label); json_body(response, label); Browser(BASE_URL).request(method, target, data=None, headers=None, follow=False).

Demo controller (no change needed) billing_summary/2 returns JSON whose access_token.audience == token.claims["aud"], so an optional audience-echo assertion can check the aud list contains the URI.

Phase-99 fact (no code change needed): acme-ledger-public has no access_token_format -> server default :jwt -> typ: at+jwt. resource= on the authorize/token requests flows verbatim into the token aud list (RFC 8707), and VerifyToken does an exact Enum.member? audience match.

Pre-edit baseline (confirmed): the smoke currently contains NEITHER the string `resource` NOR a "...accepts issued at+jwt" label — so a positive grep for those signals fails on pre-edit content and passes only after the new step is added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread resource= into the auth-code flow and add the MANDATORY 200-with-issued-token assertion</name>
  <files>scripts/demo/adoption_smoke.py</files>
  <read_first>
    - scripts/demo/adoption_smoke.py (the whole exercise_authorization_code() function, ~lines 173-254, plus the assert_status/json_body/Browser helpers at the top)
    - .planning/phases/101-adoption-demo-re-wire/101-01-SUMMARY.md (confirm the block audience URI Plan 01 settled, so the runtime literal matches it byte-for-byte per D-04)
    - examples/adoption_demo/priv/repo/seeds.exs (confirm acme-ledger-public has no access_token_format so :jwt default applies -- D-06; no edit)
  </read_first>
  <action>
    In `exercise_authorization_code()`:

    (1) Define a single module-level constant `BILLING_RESOURCE = "https://billing.acme-ledger.test"` and reference it in both runtime sites below so the two literals cannot drift and visibly equal the block URI (D-04).

    (2) Add `resource: BILLING_RESOURCE` to the `authorize_params` dict so the GET /lockspire/authorize request carries the Resource Indicator (D-02).

    (3) Add the same `resource: BILLING_RESOURCE` key/value to the POST /lockspire/token request body params dict so the token request echoes it (D-02 — the token request must echo the code-request resource for the aud to bind).

    (4) MANDATORY core assertion (D-07, DEMO-01/DEMO-02): after the preserved /userinfo assertion and near the preserved anonymous-401 assertion, add a NEW step that issues `GET /api/billing/summary` on a Browser with `headers={"authorization": "Bearer " + token_json["access_token"]}` and calls `assert_status(..., 200, "protected API accepts issued at+jwt")`. Use that exact label string so the round-trip is positively sampled and distinguishable from the 401 line. This assertion is NOT optional.

    (5) OPTIONAL (Claude's discretion per CONTEXT.md:50, D-07): you MAY additionally parse the 200 body with json_body and assert its `access_token["audience"]` list contains BILLING_RESOURCE as extra audience-echo evidence. This is encouraged-but-not-required and is SEPARATE from the mandatory 200 assertion in (4); skipping it does not weaken the core deliverable.

    DO NOT modify the commented BEGIN/END block (lines ~244-251 — Plan 01 owns it), DO NOT remove or weaken the anonymous-401 assertion, and DO NOT touch the /userinfo assertion or its email check.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && python3 -m py_compile scripts/demo/adoption_smoke.py && grep -q '"https://billing.acme-ledger.test"' scripts/demo/adoption_smoke.py && grep -Eq '["'"'"']resource["'"'"']' scripts/demo/adoption_smoke.py && grep -Fq 'assert_status' scripts/demo/adoption_smoke.py && grep -Fq '"protected API accepts issued at+jwt"' scripts/demo/adoption_smoke.py && grep -Eq 'Bearer .*token_json\["access_token"\]' scripts/demo/adoption_smoke.py && grep -Fq '"/api/billing/summary"' scripts/demo/adoption_smoke.py && grep -Fq '"protected API rejects anonymous request"' scripts/demo/adoption_smoke.py && grep -Fq '"userinfo accepts issued access token"' scripts/demo/adoption_smoke.py</automated>
  </verify>
  <acceptance_criteria>
    - scripts/demo/adoption_smoke.py byte-compiles (`python3 -m py_compile` exits 0)
    - A module-level `BILLING_RESOURCE = "https://billing.acme-ledger.test"` constant exists and is referenced in BOTH the authorize_params dict and the /token request params dict (a `resource` key appears in both)
    - The runtime resource literal equals the canonical block's audience URI byte-for-byte (D-04): `https://billing.acme-ledger.test`
    - MANDATORY: exercise_authorization_code() issues `GET /api/billing/summary` with `Authorization: Bearer <token_json["access_token"]>` and calls `assert_status(..., 200, "protected API accepts issued at+jwt")` — this exact label is present (it positively distinguishes the new 200 step from the pre-existing 401 line) and the assertion is not optional
    - The existing anonymous `GET /api/billing/summary` -> `assert_status(..., 401, "protected API rejects anonymous request")` assertion is still present
    - The existing `/userinfo` -> `assert_status(..., 200, "userinfo accepts issued access token")` and the `email == "alice@acme.test"` check are present and unchanged
    - The commented BEGIN/END LOCKSPIRE_PROTECTED_PIPELINE block is unchanged from its post-Plan-01 state (still `# audience: "https://billing.acme-ledger.test"`)
    - OPTIONAL (not required to pass): an audience-echo assertion on the 200 body's `access_token["audience"]` list — present at Claude's discretion, absence does not fail the task
    - Behavior assertion (executor-run or CI): running `python3 scripts/demo/adoption_smoke.py` against the seeded demo app reaches "adoption demo smoke passed" with the new 200 step exercised; the CI `Adoption Demo Smoke` job (`.github/workflows/ci.yml`, the `adoption-demo` job invoking `python3 scripts/demo/adoption_smoke.py`) fails loudly (non-zero exit, printed assertion) if the 200-with-issued-token round-trip stops returning 200
  </acceptance_criteria>
  <done>The smoke requests a resource-bound at+jwt, asserts the /api/billing/summary 200 round-trip via the distinct "protected API accepts issued at+jwt" label, preserves the anonymous-401 and /userinfo assertions, byte-compiles, and the runtime resource literal matches the block URI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke client → host protected route | The smoke presents an issued bearer `at+jwt`; `VerifyToken` exact-matches `aud` against the route `audience:` |
| smoke runtime literal → minted token aud | The `resource=` the smoke sends determines the token `aud`; it must equal the route audience or the 200 path 401s |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-03 | Spoofing/Elevation (audience-confusion at proof time) | smoke `resource=` literal vs route `audience:` | mitigate | Send `resource=https://billing.acme-ledger.test` on both authorize and token, equal to the Plan-01 block URI (D-02/D-04). Defining one module-level constant referenced in both runtime sites prevents drift between the two runtime literals; matching the block URI prevents the audience-mismatch 401. This is the executable proof that the absolute-URI audience defeats token substitution. |
| T-101-04 | Repudiation/Denial (regression goes unnoticed) | CI Adoption Demo Smoke job | mitigate | The new `assert_status(..., 200, ...)` raises on any non-200, exiting non-zero, so CI fails loudly (DEMO-02). The preserved anonymous-401 assertion keeps the negative-path proof. No silent green. |

No HIGH-and-unmitigated threats: the audience-confusion class (T-101-03) is the very bug-pattern this proof exercises and is mitigated by the matched absolute URI; regression-blindness (T-101-04) is mitigated by the loud-failing CI assertion.
</threat_model>

<verification>
- `python3 -m py_compile scripts/demo/adoption_smoke.py` exits 0.
- `resource` param set to `https://billing.acme-ledger.test` on both the authorize and token requests; runtime literal equals the Plan-01 block URI (D-04).
- New `GET /api/billing/summary` Bearer-token step asserts 200 under the distinct label "protected API accepts issued at+jwt" (positively sampled, fails on pre-edit content); anonymous-401 and /userinfo assertions preserved.
- CI Adoption Demo Smoke job (`adoption-demo` in `.github/workflows/ci.yml`) runs the smoke and fails loudly on regression.
</verification>

<success_criteria>
Running the smoke against the seeded demo completes the auth-code -> at+jwt -> /api/billing/summary -> 200 round-trip while preserving the anonymous-401 and /userinfo stored-opaque assertions; CI fails loudly if the new round-trip regresses — satisfying DEMO-01 and DEMO-02.
</success_criteria>

<output>
Create `.planning/phases/101-adoption-demo-re-wire/101-02-SUMMARY.md` when done.
</output>
