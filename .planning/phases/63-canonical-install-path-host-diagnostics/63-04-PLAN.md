---
phase: 63-canonical-install-path-host-diagnostics
plan: 63-04
type: execute
wave: 3
depends_on:
  - 63-01
  - 63-02
  - 63-03
files_modified:
  - docs/install-and-onboard.md
  - docs/sigra-companion-host.md
  - docs/ecosystem-overview.md
  - docs/supported-surface.md
  - priv/templates/lockspire.install/fapi_smoke_e2e_test.exs
  - test/integration/install_generator_test.exs
  - test/integration/phase6_onboarding_e2e_test.exs
  - test/support/generated_host_app_web/router.ex
  - test/support/generated_host_app_web/endpoint.ex
  - test/lockspire/release_readiness_contract_test.exs
autonomous: true
requirements:
  - HOST-01
  - HOST-02
  - HOST-03
must_haves:
  truths:
    - "Docs describe one canonical generic install path, with Sigra positioned as the recommended companion only."
    - "The canonical install verification story includes `mix lockspire.verify` and `mix lockspire.upgrade` with explicit ownership boundaries."
    - "Repo-owned install proof exercises a generated-host router or endpoint rather than calling `Lockspire.Web.Router` directly for the canonical install claim."
  artifacts:
    - path: docs/install-and-onboard.md
      provides: "canonical install, verify, and upgrade contract for host maintainers"
    - path: priv/templates/lockspire.install/fapi_smoke_e2e_test.exs
      provides: "generated proof template aligned with the host-router install story"
    - path: test/integration/phase6_onboarding_e2e_test.exs
      provides: "truthful generated-host onboarding proof through host wiring"
  key_links:
    - from: docs/install-and-onboard.md
      to: docs/supported-surface.md
      via: "canonical support contract link"
      pattern: "supported-surface"
    - from: test/integration/phase6_onboarding_e2e_test.exs
      to: test/support/generated_host_app_web/router.ex
      via: "host-router proof rather than direct Lockspire router proof"
      pattern: "GeneratedHostAppWeb"
---

<objective>
Make the public install story and the repo-owned install proof tell the same truth.

Purpose: Phase 63 is only complete if maintainers can follow one generic install path, understand ownership and upgrade boundaries, run diagnostics, and see repo-owned proof that actually exercises host wiring. This plan aligns docs and tests around that canonical story.
Output: updated install/companion/support docs, a generated proof template aligned with host wiring, and an onboarding integration test that proves the host-router contract rather than only embedded-router behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-canonical-install-path-host-diagnostics/63-CONTEXT.md
@.planning/phases/63-canonical-install-path-host-diagnostics/63-RESEARCH.md
@docs/install-and-onboard.md
@docs/sigra-companion-host.md
@docs/supported-surface.md
@priv/templates/lockspire.install/fapi_smoke_e2e_test.exs
@test/integration/install_generator_test.exs
@test/integration/phase6_onboarding_e2e_test.exs
@test/support/generated_host_app_web/router.ex
@test/support/generated_host_app_web/endpoint.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align canonical docs with install, verify, and upgrade truth</name>
  <files>docs/install-and-onboard.md, docs/sigra-companion-host.md, docs/ecosystem-overview.md, docs/supported-surface.md, test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - docs/install-and-onboard.md
    - docs/sigra-companion-host.md
    - docs/ecosystem-overview.md
    - docs/supported-surface.md
    - .planning/phases/63-canonical-install-path-host-diagnostics/63-CONTEXT.md
    - .planning/phases/63-canonical-install-path-host-diagnostics/63-RESEARCH.md
  </read_first>
  <action>Update the docs so they describe the same canonical Phase 63 contract: `mix lockspire.install` is the one generic Phoenix-first entrypoint, `--sigra-host` only adjusts guidance, `mix lockspire.verify` is the post-install diagnostics step, and `mix lockspire.upgrade` applies only to Lockspire-managed scaffolding under manifest control. Make the managed-vs-host-owned distinction explicit in `docs/install-and-onboard.md` and preserve `docs/supported-surface.md` as the canonical support contract. Update release-readiness contract tests so those claims are enforced in-repo.</action>
  <acceptance_criteria>
    - `docs/install-and-onboard.md` contains `mix lockspire.verify`
    - `docs/install-and-onboard.md` contains `mix lockspire.upgrade`
    - `docs/sigra-companion-host.md` still states Lockspire must not import Sigra at compile time
    - `docs/supported-surface.md` contains `one canonical Phoenix onboarding path`
    - `test/lockspire/release_readiness_contract_test.exs` asserts at least one new Phase 63 install-truth claim
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <done>The public docs, support contract, and release-truth tests all describe the same install, verify, and upgrade story.</done>
</task>

<task type="auto">
  <name>Task 2: Move canonical install proof to the generated-host router surface</name>
  <files>priv/templates/lockspire.install/fapi_smoke_e2e_test.exs, test/integration/install_generator_test.exs, test/integration/phase6_onboarding_e2e_test.exs, test/support/generated_host_app_web/router.ex, test/support/generated_host_app_web/endpoint.ex</files>
  <read_first>
    - priv/templates/lockspire.install/fapi_smoke_e2e_test.exs
    - test/integration/install_generator_test.exs
    - test/integration/phase6_onboarding_e2e_test.exs
    - test/support/generated_host_app_web/router.ex
    - test/support/generated_host_app_web/endpoint.ex
    - .planning/phases/63-canonical-install-path-host-diagnostics/63-RESEARCH.md
  </read_first>
  <action>Refactor the generated smoke proof and the repo-owned onboarding integration proof so the canonical install claim runs through the generated-host router or endpoint surface instead of calling `Lockspire.Web.Router` directly. Preserve the existing authorization-code plus PKCE assertions, discovery, and JWKS checks, but route them through `GeneratedHostAppWeb` wiring so the proof covers the forward mount and host-owned routes as installed. Update generator tests to assert the generated smoke template points at the generated-host proof surface, not the embedded router directly.</action>
  <acceptance_criteria>
    - `priv/templates/lockspire.install/fapi_smoke_e2e_test.exs` does not contain `@endpoint Lockspire.Web.Router`
    - `test/integration/phase6_onboarding_e2e_test.exs` contains `GeneratedHostAppWeb`
    - `test/integration/phase6_onboarding_e2e_test.exs` does not use `Lockspire.Web.Router.call(` for the canonical host proof path
    - `test/integration/install_generator_test.exs` asserts the generated smoke file references the generated-host proof surface
  </acceptance_criteria>
  <verify>
    <automated>mix test test/integration/install_generator_test.exs test/integration/phase6_onboarding_e2e_test.exs</automated>
  </verify>
  <done>The repo's canonical install proof now exercises host router mounting and generated-host wiring rather than proving only the embedded router in isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs -> adopter expectations | Contradictory or overstated install claims damage support truth |
| repo proof -> claimed onboarding shape | Direct embedded-router tests cannot justify host-router installation claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-04-01 | R | install docs | mitigate | Keep one canonical install/verify/upgrade story and enforce it in release-readiness tests |
| T-63-04-02 | T | generated smoke proof | mitigate | Route the canonical proof through generated-host wiring instead of `Lockspire.Web.Router` direct calls |
| T-63-04-03 | S | Sigra positioning | mitigate | Preserve comments-only companion positioning and explicit no-compile-time-coupling wording |
</threat_model>

<verification>
Run release-readiness contract tests first, then run the install generator and onboarding integration proofs together. Completion requires docs truth and host-router proof truth to pass in the same verification lane.
</verification>

<success_criteria>
- Public docs name one canonical generic install path with explicit ownership, verify, and upgrade guidance.
- Sigra remains the recommended companion path without compile-time coupling or a second topology.
- The canonical install proof exercises generated-host router wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/63-canonical-install-path-host-diagnostics/63-04-SUMMARY.md`
</output>
