---
phase: 63-canonical-install-path-host-diagnostics
plan: 63-02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/lockspire.verify.ex
  - lib/lockspire/install/verify.ex
  - lib/lockspire/install/verify/check.ex
  - test/mix/tasks/lockspire_verify_test.exs
  - test/lockspire/install/verify_test.exs
  - test/lockspire/application_test.exs
  - test/lockspire/config_test.exs
autonomous: true
requirements:
  - HOST-02
must_haves:
  truths:
    - "`mix lockspire.verify` is the canonical post-install verification command."
    - "Diagnostics cover runtime config, seam module presence, router wiring, `/verify` host routes, and pending migrations."
    - "Existing boot-time config validation in `Lockspire.Config` and `Lockspire.Oban` remains intact."
  artifacts:
    - path: lib/mix/tasks/lockspire.verify.ex
      provides: "CLI entrypoint and user-facing diagnostics output"
    - path: lib/lockspire/install/verify.ex
      provides: "orchestration for install checks and normalized result shaping"
    - path: test/mix/tasks/lockspire_verify_test.exs
      provides: "failure-message and success-output proof for the Mix task"
  key_links:
    - from: lib/mix/tasks/lockspire.verify.ex
      to: lib/lockspire/install/verify.ex
      via: "task delegates checks to library code"
      pattern: "Lockspire\\.Install\\.Verify"
    - from: lib/lockspire/install/verify.ex
      to: lib/lockspire/config.ex
      via: "required config check reuses existing runtime source of truth"
      pattern: "Config"
---

<objective>
Add a canonical `mix lockspire.verify` diagnostics command that catches the highest-risk host integration mistakes before request-time failure.

Purpose: The install path currently relies too heavily on maintainers inferring correctness from generated files and eventual runtime failures. Phase 63 needs an explicit verification command that checks router wiring, host seam presence, migrations, and required runtime configuration using install-oriented messages.
Output: a new verify Mix task, a reusable diagnostics library module, and tests covering both actionable failure output and the success case.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-canonical-install-path-host-diagnostics/63-CONTEXT.md
@.planning/phases/63-canonical-install-path-host-diagnostics/63-RESEARCH.md
@.planning/phases/63-canonical-install-path-host-diagnostics/63-PATTERNS.md
@lib/mix/tasks/lockspire.install.ex
@lib/lockspire/config.ex
@lib/lockspire/oban.ex
@lib/lockspire/web/router.ex
@test/support/generated_host_app_web/router.ex
@test/lockspire/application_test.exs
@test/lockspire/config_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce `mix lockspire.verify` and normalized diagnostics checks</name>
  <files>lib/mix/tasks/lockspire.verify.ex, lib/lockspire/install/verify.ex, lib/lockspire/install/verify/check.ex, test/mix/tasks/lockspire_verify_test.exs, test/lockspire/install/verify_test.exs</files>
  <read_first>
    - lib/mix/tasks/lockspire.install.ex
    - lib/lockspire/config.ex
    - lib/lockspire/oban.ex
    - lib/lockspire/web/router.ex
    - test/support/generated_host_app_web/router.ex
    - .planning/phases/63-canonical-install-path-host-diagnostics/63-RESEARCH.md
  </read_first>
  <action>Create `lib/mix/tasks/lockspire.verify.ex` as a focused CLI task with `--help` and no generator side effects. Back it with `Lockspire.Install.Verify`, which should run concrete checks for: required `:lockspire` config keys, account-resolver and interaction-handler module availability, compiled host-router presence of the Lockspire forward mount, compiled host-router presence of the host-owned `/verify` routes, and pending Lockspire/Oban migrations. Use one normalized check result shape so the Mix task can print stable `OK` or `ERROR` lines with explicit fix guidance. Keep check logic in library modules, not inside the task file.</action>
  <acceptance_criteria>
    - `lib/mix/tasks/lockspire.verify.ex` contains `defmodule Mix.Tasks.Lockspire.Verify`
    - `lib/mix/tasks/lockspire.verify.ex` contains `mix lockspire.verify`
    - `lib/lockspire/install/verify.ex` contains checks for `router`, `config`, `verify routes`, and `migrations`
    - `test/mix/tasks/lockspire_verify_test.exs` asserts at least one actionable failure string for missing router wiring
    - `test/lockspire/install/verify_test.exs` covers a passing result set and a failing result set
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/lockspire_verify_test.exs test/lockspire/install/verify_test.exs</automated>
  </verify>
  <done>The repo has a post-install verification command with stable, testable diagnostics for the highest-risk host mistakes.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve fail-fast boot validation while aligning diagnostics messaging</name>
  <files>test/lockspire/application_test.exs, test/lockspire/config_test.exs, lib/lockspire/install/verify.ex</files>
  <read_first>
    - test/lockspire/application_test.exs
    - test/lockspire/config_test.exs
    - lib/lockspire/config.ex
    - lib/lockspire/oban.ex
    - lib/lockspire/install/verify.ex
  </read_first>
  <action>Keep `Lockspire.Config` and `Lockspire.Oban` as the runtime fail-fast source of truth. Do not move those errors into install-time generation. Instead, align `mix lockspire.verify` messages with the existing runtime validation language so a maintainer sees the same missing-key or invalid-subsystem story before boot. Add or adjust tests only where needed to prove the verify command complements runtime validation rather than replacing it.</action>
  <acceptance_criteria>
    - `test/lockspire/application_test.exs` still asserts clear runtime boot failures for missing repo or invalid Oban config
    - `test/lockspire/config_test.exs` still asserts required-config errors for missing mount path or issuer-related config
    - `lib/lockspire/install/verify.ex` references `Lockspire.Config` and/or `Lockspire.Oban` rather than duplicating config rules in a second source of truth
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/application_test.exs test/lockspire/config_test.exs test/mix/tasks/lockspire_verify_test.exs</automated>
  </verify>
  <done>Boot-time failures remain authoritative, and the verify command surfaces the same integration problems earlier with install-oriented remediation guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host repo wiring -> Lockspire runtime | Missing routes or seam modules can produce misleading runtime failures |
| migration state -> shipped feature claims | Repo may look installed while critical durable protocol tables are missing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-02-01 | D | router wiring | mitigate | Inspect compiled router truth and report missing forward mount or `/verify` routes directly |
| T-63-02-02 | T | runtime config | mitigate | Reuse fail-fast config sources and surface missing keys before first request |
| T-63-02-03 | R | migration state | mitigate | Check pending Lockspire and Oban migrations explicitly so the install story remains truthful |
</threat_model>

<verification>
Run the verify-task and diagnostics tests first, then rerun the focused runtime-config tests. Completion requires actionable negative-path output and one passing end-to-end verify result.
</verification>

<success_criteria>
- `mix lockspire.verify` exists and is the documented verification step after install.
- Diagnostics detect missing config, missing seams, router mount errors, `/verify` route gaps, and pending migrations.
- Runtime fail-fast behavior remains unchanged and aligned with diagnostics output.
</success_criteria>

<output>
After completion, create `.planning/phases/63-canonical-install-path-host-diagnostics/63-02-SUMMARY.md`
</output>
