---
phase: 63-canonical-install-path-host-diagnostics
plan: 63-01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/lockspire.install.ex
  - lib/lockspire/generators/install.ex
  - lib/lockspire/generators/templates.ex
  - priv/templates/lockspire.install/config.exs
  - priv/templates/lockspire.install/router.ex
  - priv/templates/lockspire.install/account_resolver.ex
  - priv/templates/lockspire.install/interaction_handler.ex
  - priv/templates/lockspire.install/fapi_smoke_e2e_test.exs
  - test/integration/install_generator_test.exs
autonomous: true
requirements:
  - HOST-01
  - HOST-03
must_haves:
  truths:
    - "`mix lockspire.install` no longer calls `Lockspire.mount_path/0` during generation."
    - "The canonical install path remains generic to Phoenix hosts, with `--sigra-host` changing guidance only."
    - "Generated artifacts are explicitly marked as `host-owned` or `Lockspire-managed`."
    - "Security-sensitive host seam stubs fail intentionally until the host implements them."
  artifacts:
    - path: lib/lockspire/generators/install.ex
      provides: "bootstrap-safe assign building plus ownership-aware render path"
    - path: lib/lockspire/generators/templates.ex
      provides: "template inventory with ownership classification for each generated artifact"
    - path: test/integration/install_generator_test.exs
      provides: "regression proof for bootstrap-safe generation, ownership headers, and comment-only Sigra variant"
  key_links:
    - from: lib/mix/tasks/lockspire.install.ex
      to: lib/lockspire/generators/install.ex
      via: "install task delegates option parsing and help text to generator contract"
      pattern: "Install\\.run"
    - from: lib/lockspire/generators/templates.ex
      to: priv/templates/lockspire.install/router.ex
      via: "managed router seam classification"
      pattern: "router\\.ex"
---

<objective>
Make the canonical install generator bootstrap-safe and explicit about file ownership without creating a second install topology.

Purpose: Phase 63 must make the embedded Phoenix install path boring and truthful. The generator should be able to create its own config seam safely, preserve the generic Phoenix path, keep Sigra as comments-only guidance, and tell maintainers which files Lockspire may later manage versus which files belong entirely to the host.
Output: a bootstrap-safe `mix lockspire.install`, ownership-aware template inventory, intentionally failing host seam stubs, and updated generator tests that prove the new contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/63-canonical-install-path-host-diagnostics/63-CONTEXT.md
@.planning/phases/63-canonical-install-path-host-diagnostics/63-RESEARCH.md
@.planning/phases/63-canonical-install-path-host-diagnostics/63-PATTERNS.md
@lib/mix/tasks/lockspire.install.ex
@lib/lockspire/generators/install.ex
@lib/lockspire/generators/templates.ex
@priv/templates/lockspire.install/account_resolver.ex
@priv/templates/lockspire.install/interaction_handler.ex
@priv/templates/lockspire.install/router.ex
@test/integration/install_generator_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make `mix lockspire.install` bootstrap-safe and keep one canonical path</name>
  <files>lib/mix/tasks/lockspire.install.ex, lib/lockspire/generators/install.ex, priv/templates/lockspire.install/interaction_handler.ex, priv/templates/lockspire.install/router.ex, test/integration/install_generator_test.exs</files>
  <read_first>
    - lib/mix/tasks/lockspire.install.ex
    - lib/lockspire/generators/install.ex
    - priv/templates/lockspire.install/interaction_handler.ex
    - priv/templates/lockspire.install/router.ex
    - .planning/phases/63-canonical-install-path-host-diagnostics/63-CONTEXT.md
    - .planning/phases/63-canonical-install-path-host-diagnostics/63-RESEARCH.md
  </read_first>
  <action>Extend `mix lockspire.install` to accept an explicit mount-path input such as `--mount-path`, default it to `"/lockspire"`, thread that value through `Lockspire.Generators.Install.build_assigns/1`, and remove the current `Lockspire.mount_path/0` dependency from generation entirely. Keep `--sigra-host` as a comments-only variant: help text, moduledoc wording, and generated comments may change, but the generated file set, paths, and compile-time dependencies must remain identical to the generic Phoenix path. Update the generated interaction and router templates to use the install-time mount path assign, not runtime config discovery.</action>
  <acceptance_criteria>
    - `lib/mix/tasks/lockspire.install.ex` contains `mount_path: :string`
    - `lib/lockspire/generators/install.ex` does not contain `Lockspire.mount_path()`
    - `lib/lockspire/generators/install.ex` contains `Keyword.get(opts, :mount_path, \"/lockspire\")`
    - `test/integration/install_generator_test.exs` asserts generation succeeds without pre-setting Lockspire runtime config
    - `test/integration/install_generator_test.exs` still covers `--sigra-host` without asserting different generated paths
  </acceptance_criteria>
  <verify>
    <automated>mix test test/integration/install_generator_test.exs</automated>
  </verify>
  <done>The install generator can render its first config/router seam without pre-existing Lockspire config, and the canonical path is still one generic Phoenix topology.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit ownership annotations and intentionally failing host seam stubs</name>
  <files>lib/lockspire/generators/templates.ex, priv/templates/lockspire.install/config.exs, priv/templates/lockspire.install/router.ex, priv/templates/lockspire.install/account_resolver.ex, priv/templates/lockspire.install/fapi_smoke_e2e_test.exs, test/integration/install_generator_test.exs</files>
  <read_first>
    - lib/lockspire/generators/templates.ex
    - priv/templates/lockspire.install/config.exs
    - priv/templates/lockspire.install/router.ex
    - priv/templates/lockspire.install/account_resolver.ex
    - priv/templates/lockspire.install/fapi_smoke_e2e_test.exs
    - test/integration/install_generator_test.exs
    - .planning/phases/63-canonical-install-path-host-diagnostics/63-CONTEXT.md
  </read_first>
  <action>Extend `Lockspire.Generators.Templates.all/0` so each template entry carries concrete ownership metadata: `ownership: :managed` for `config/lockspire.exs`, `lib/<web>/router/lockspire.ex`, and the generated smoke test, and `ownership: :host_owned` for account, interaction, consent, authorized-apps, and verification seams. Update the rendered templates to include explicit ownership headers using that classification. Harden `account_resolver.ex` so security-sensitive placeholder callbacks fail loudly with actionable TODO guidance instead of returning structurally valid fake claims or fake account lookup results. Preserve the redirect-to-login placeholder as safe bootstrap behavior. Update generator tests to assert ownership headers and intentional-failure placeholders are present.</action>
  <acceptance_criteria>
    - `lib/lockspire/generators/templates.ex` contains `ownership: :managed`
    - `lib/lockspire/generators/templates.ex` contains `ownership: :host_owned`
    - `priv/templates/lockspire.install/account_resolver.ex` contains `raise`
    - `test/integration/install_generator_test.exs` asserts rendered ownership wording for at least one managed file and one host-owned file
    - `test/integration/install_generator_test.exs` no longer accepts resolver defaults that silently return empty claims for unimplemented host logic
  </acceptance_criteria>
  <verify>
    <automated>mix test test/integration/install_generator_test.exs --seed 0</automated>
  </verify>
  <done>Generated files tell the maintainer who owns them, and unimplemented host account seams fail explicitly instead of pretending to be complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| install task -> host codebase | Lockspire writes source files into a host Phoenix app |
| generated host seams -> production auth behavior | Placeholder code must not look secure or complete when it is not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01-01 | T | install generator | mitigate | Remove runtime-config dependency from generation so first-run scaffolding cannot fail unpredictably |
| T-63-01-02 | S | generated resolver stub | mitigate | Replace permissive placeholder logic with explicit failure guidance for unimplemented host-owned seams |
| T-63-01-03 | R | generated artifact ownership | mitigate | Stamp each file with an explicit ownership class so future upgrade behavior is not ambiguous |
</threat_model>

<verification>
Run `mix test test/integration/install_generator_test.exs` after each task. The phase slice is complete only when generation works without pre-existing Lockspire config and the rendered fixture contains ownership annotations plus intentional-failure host seam placeholders.
</verification>

<success_criteria>
- `mix lockspire.install` is bootstrap-safe.
- `--sigra-host` remains comments-only and does not create a second install topology.
- Generated artifacts clearly distinguish managed scaffolding from host-owned seams.
- Unimplemented host seam callbacks no longer appear production-ready by default.
</success_criteria>

<output>
After completion, create `.planning/phases/63-canonical-install-path-host-diagnostics/63-01-SUMMARY.md`
</output>
