---
phase: 129-token-endpoint-cohesion
plan: 02
type: execute
wave: 2
depends_on: [129-01]
files_modified:
  - lib/lockspire/protocol/access_token_signer.ex
  - lib/lockspire/protocol/refresh_exchange.ex
  - lib/lockspire/protocol/id_token.ex
  - lib/lockspire/protocol/rfc8693_exchange.ex
  - test/lockspire/protocol/access_token_signer_test.exs
  - test/lockspire/protocol/refresh_exchange_test.exs
  - test/lockspire/protocol/id_token_test.exs
  - test/lockspire/protocol/rfc8693_exchange_test.exs
autonomous: true
requirements: [RUNTIME-03]
must_haves:
  truths:
    - "All access-token responses and claims still expire in 3600 seconds."
    - "All ID tokens still expire in 3600 seconds."
    - "All refresh tokens still expire in 2592000 seconds."
  artifacts:
    - path: "lib/lockspire/protocol/token_lifetime.ex"
      provides: "Only internal owner of default token durations"
  key_links:
    - from: "access, ID, refresh, and RFC8693 issuance modules"
      to: "lib/lockspire/protocol/token_lifetime.ex"
      via: "named policy function calls"
---

<objective>
Replace every duplicated token-duration literal with the shared internal policy while preserving response, claim, and persisted expiration values.

Purpose: Make security-relevant default durations auditable in one place.
Output: All issuance paths consume `TokenLifetime` and focused tests pin their wire values.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/129-token-endpoint-cohesion/129-RESEARCH.md
@.planning/phases/129-token-endpoint-cohesion/129-01-SUMMARY.md
@lib/lockspire/protocol/token_lifetime.ex
@lib/lockspire/protocol/access_token_signer.ex
@lib/lockspire/protocol/refresh_exchange.ex
@lib/lockspire/protocol/id_token.ex
@lib/lockspire/protocol/rfc8693_exchange.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify access and refresh lifetime consumers</name>
  <files>lib/lockspire/protocol/access_token_signer.ex, lib/lockspire/protocol/refresh_exchange.ex, test/lockspire/protocol/access_token_signer_test.exs, test/lockspire/protocol/refresh_exchange_test.exs</files>
  <behavior>
    - JWT access claim `exp - iat`, OAuth `expires_in`, and persisted access expiry remain 3600.
    - Rotated refresh persisted expiry remains 2592000 and rotation/reuse semantics do not change.
  </behavior>
  <action>Replace module attributes with `TokenLifetime` calls at claim, response, and persisted-token construction sites. Add focused equality assertions against public response/decoded claim/persisted timestamp behavior, keeping all rotation transactions, hash updates, audit, and reuse-family revocation unchanged.</action>
  <verify><automated>mix test test/lockspire/protocol/access_token_signer_test.exs test/lockspire/protocol/refresh_exchange_test.exs</automated></verify>
  <done>Access and refresh issuance consume one policy and retain exact current durations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unify ID-token and RFC 8693 lifetime consumers</name>
  <files>lib/lockspire/protocol/id_token.ex, lib/lockspire/protocol/rfc8693_exchange.ex, test/lockspire/protocol/id_token_test.exs, test/lockspire/protocol/rfc8693_exchange_test.exs</files>
  <behavior>
    - ID-token `exp - iat` remains 3600.
    - RFC 8693 issued token response and persisted token expiry remain 3600.
  </behavior>
  <action>Use the same named policy functions in ID-token claims and both RFC 8693 issuance branches. Pin decoded claims, response `expires_in`, and persisted timestamps; do not alter subject-token validation, audience shape, restricted claims, or token format selection.</action>
  <verify><automated>mix test test/lockspire/protocol/id_token_test.exs test/lockspire/protocol/rfc8693_exchange_test.exs</automated></verify>
  <done>Every in-scope default duration has one policy owner with wire-compatible proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Lifetime policy → signed/persisted tokens | Policy values determine bearer-token validity windows. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-129-04 | Elevation of privilege | Token expiry | high | mitigate | Assert exact claim, response, and persistence durations across every issuance path. |
| T-129-05 | Tampering | Refresh lifecycle | high | mitigate | Keep rotation/reuse transaction tests passing while replacing only duration sources. |
</threat_model>

<verification>Focused lifetime suites and `mix compile --warnings-as-errors` pass.</verification>
<success_criteria>No duplicated default duration remains in the covered issuance modules; all current expiry behavior is exact.</success_criteria>
<output>Create `.planning/phases/129-token-endpoint-cohesion/129-02-SUMMARY.md` when done.</output>

