---
phase: 36-end-to-end-proof-and-milestone-closure
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/phase36_auth_code_dpop_e2e_test.exs
autonomous: true
requirements:
  - DPoP-12
must_haves:
  truths:
    - "A browser-style authorization-code client can complete `/authorize` and consent through the host-owned interaction seam, redeem the code at `/token` with a DPoP proof, and receive a truthful `token_type: \"DPoP\"` response."
    - "The same issued DPoP-bound access token is usable on the Lockspire-owned `userinfo` surface only with `Authorization: DPoP` plus a valid proof."
    - "Missing or mismatched DPoP proof on the auth-code token exchange fails through the real HTTP seam instead of being covered only by unit tests."
    - "The browser proof stays inside the repo-native integration harness and does not introduce a second acceptance runner or demo app."
  artifacts:
    - path: test/integration/phase36_auth_code_dpop_e2e_test.exs
      provides: hosted browser/auth-code DPoP end-to-end proof from `/authorize` through `/token` and owned-surface consumption
  key_links:
    - from: test/integration/phase36_auth_code_dpop_e2e_test.exs
      to: /authorize -> /interactions/:id/complete -> /token
      via: repo-native browser-host interaction path reused from the PAR/auth-code harness
      pattern: 'build_conn\(:get, "/authorize".+build_conn\(:post, "/interactions/'
    - from: test/integration/phase36_auth_code_dpop_e2e_test.exs
      to: /token
      via: auth-code redemption with a real `dpop` request header
      pattern: 'put_req_header\("dpop",'
    - from: test/integration/phase36_auth_code_dpop_e2e_test.exs
      to: /userinfo
      via: DPoP-bound access token reused on the Lockspire-owned endpoint
      pattern: 'put_req_header\("authorization", "DPoP "'
---

<objective>
Add the browser-style authorization-code DPoP proof required to close the milestone with real hosted interaction coverage.

Purpose: Prove the shipped auth-code DPoP slice per D-01, D-02, D-04, D-08, and D-09 through the existing Phoenix/host-owned seams instead of protocol-only tests or a new harness.
Output: A dedicated repo-native integration test covering authorize, consent, callback, `/token` redemption with DPoP, owned `userinfo` consumption, and at least one negative proof path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/EPIC.md
@.planning/phases/36-end-to-end-proof-and-milestone-closure/36-CONTEXT.md
@.planning/phases/36-end-to-end-proof-and-milestone-closure/36-RESEARCH.md
@.planning/phases/36-end-to-end-proof-and-milestone-closure/36-PATTERNS.md
@.planning/phases/34-token-issuance-and-refresh-device-binding/34-CONTEXT.md
@.planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-CONTEXT.md
@test/integration/phase15_par_authorization_e2e_test.exs
@test/integration/phase3_oidc_token_lifecycle_e2e_test.exs

<interfaces>
From test/integration/phase15_par_authorization_e2e_test.exs:
```elixir
authorize_conn =
  build_conn(:get, "/authorize", %{"client_id" => client.client_id, "request_uri" => request_uri})
  |> Lockspire.Web.Router.call(Lockspire.Web.Router.init([]))

consent_complete_conn =
  build_conn(:post, "/interactions/#{interaction_id}/complete", %{"decision" => "approve"})
  |> Lockspire.Web.Router.call(Lockspire.Web.Router.init([]))
```

From test/integration/phase3_oidc_token_lifecycle_e2e_test.exs:
```elixir
userinfo_conn =
  build_conn(:get, "/userinfo")
  |> put_req_header("authorization", "Bearer " <> token_response["access_token"])
  |> Lockspire.Web.Router.call(Lockspire.Web.Router.init([]))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the hosted browser auth-code DPoP success path</name>
  <files>test/integration/phase36_auth_code_dpop_e2e_test.exs</files>
  <read_first>
    - test/integration/phase15_par_authorization_e2e_test.exs
    - test/integration/phase3_oidc_token_lifecycle_e2e_test.exs
    - .planning/phases/36-end-to-end-proof-and-milestone-closure/36-CONTEXT.md
    - .planning/phases/34-token-issuance-and-refresh-device-binding/34-CONTEXT.md
    - .planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a DPoP-mode public client completes `/authorize`, consent, callback, and `/token` redemption with PKCE and receives `token_type == "DPoP"`
    - Test 2: the returned DPoP-bound access token succeeds on `GET /userinfo` only when presented as `Authorization: DPoP <token>` with a matching DPoP proof
    - Test 3: the same flow still exercises the host-owned interaction seam through redirects and consent completion rather than seeded authorization-code storage alone
  </behavior>
  <action>Create `test/integration/phase36_auth_code_dpop_e2e_test.exs` per D-01, D-02, and D-04 by mirroring the Phase 15 browser-host interaction flow and the Phase 3 owned-endpoint assertions. Register a DPoP-mode public client, drive `/authorize -> /interactions/:id/complete -> client callback -> /token` through `Lockspire.Web.Router`, sign a real DPoP proof for the `/token` request, and assert the token response is truthful for the shipped slice: access token present, ID token present when scope warrants it, and `token_type: "DPoP"` per the durable binding rules from Phase 34. Reuse repo-native helpers and `JarTestHelpers.sign_dpop_proof/2`; do not introduce a demo app, a second acceptance harness, or seeded-code shortcuts as the primary proof path.</action>
  <acceptance_criteria>
    - `rg -n 'phase36.*DPoP|token_type.+DPoP' test/integration/phase36_auth_code_dpop_e2e_test.exs` finds the dedicated auth-code DPoP proof
    - `rg -n '"/authorize"|"/interactions/.+/complete"|"/token"' test/integration/phase36_auth_code_dpop_e2e_test.exs` confirms the real hosted interaction path is exercised
    - `rg -n 'put_req_header\\("dpop",' test/integration/phase36_auth_code_dpop_e2e_test.exs` confirms the token exchange uses a real DPoP header
    - `MIX_ENV=test mix test --include integration test/integration/phase36_auth_code_dpop_e2e_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test.setup && MIX_ENV=test mix test --include integration test/integration/phase36_auth_code_dpop_e2e_test.exs -x</automated>
  </verify>
  <done>Lockspire has one dedicated browser-style end-to-end test that proves auth-code + PKCE DPoP issuance through the host-owned interaction seam and truthful `token_type: "DPoP"` behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add auth-code DPoP negative proof coverage without widening the harness</name>
  <files>test/integration/phase36_auth_code_dpop_e2e_test.exs</files>
  <read_first>
    - test/integration/phase36_auth_code_dpop_e2e_test.exs
    - .planning/phases/36-end-to-end-proof-and-milestone-closure/36-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: redeeming the auth code without the required DPoP proof fails with the repo’s existing standards-shaped DPoP token error
    - Test 2: presenting the bound access token to `userinfo` with the wrong auth scheme or a mismatched proof fails through the owned endpoint
  </behavior>
  <action>Extend the same integration file per D-08 and D-09 with one or two high-signal negative assertions that prove the browser slice stays narrow and truthful. Cover a missing-or-invalid DPoP proof on `/token` for the DPoP-mode client and at least one owned-endpoint misuse case for the issued access token, using the existing error contracts from Phases 34 and 35 rather than inventing broader host-resource claims. Keep the negative path in the same repo-native test module so Phase 36 closes from a single browser-proof artifact instead of a new helper layer.</action>
  <acceptance_criteria>
    - `rg -n 'invalid_dpop_proof|invalid_token' test/integration/phase36_auth_code_dpop_e2e_test.exs` finds end-to-end negative-path assertions
    - `rg -n 'Authorization: DPoP|\"DPoP \"|wrong auth scheme|Bearer' test/integration/phase36_auth_code_dpop_e2e_test.exs` finds owned-surface misuse coverage
    - `MIX_ENV=test mix test --include integration test/integration/phase36_auth_code_dpop_e2e_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test.setup && MIX_ENV=test mix test --include integration test/integration/phase36_auth_code_dpop_e2e_test.exs -x</automated>
  </verify>
  <done>The browser auth-code DPoP proof covers both the successful hosted interaction path and at least one real HTTP failure path, while staying inside the narrow repo-owned support contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser client -> `/authorize` and consent seam | Untrusted browser parameters and redirect state cross into the hosted interaction path here |
| browser client -> `/token` | The DPoP proof and PKCE verifier are untrusted until protocol validation succeeds |
| issued access token -> `/userinfo` | A DPoP-bound token must not downgrade into generic bearer behavior on the owned protected-resource surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-01 | Spoofing | auth-code `/token` exchange | mitigate | Prove a DPoP-mode public client cannot redeem the code without a valid DPoP proof per D-02 and D-08 |
| T-36-02 | Elevation of Privilege | `userinfo` downgrade path | mitigate | Prove the issued DPoP-bound token works only with the DPoP auth scheme and matching proof per D-08 and D-09 |
| T-36-03 | Repudiation | milestone proof posture | mitigate | Keep the browser proof inside one repo-native integration artifact per D-01 and D-04 so public support claims map directly to executable evidence |
</threat_model>

<verification>
Run the browser DPoP proof after this plan:

`MIX_ENV=test mix test.setup && MIX_ENV=test mix test --include integration test/integration/phase36_auth_code_dpop_e2e_test.exs -x`
</verification>

<success_criteria>
- A DPoP-mode browser client completes the real hosted auth-code flow and receives a truthful DPoP token response.
- The issued access token is accepted on Lockspire-owned `userinfo` only with the DPoP auth scheme plus a valid proof.
- The test does not introduce any second acceptance harness, demo app, or broader host protected-resource claim.
</success_criteria>

<output>
After completion, create `.planning/phases/36-end-to-end-proof-and-milestone-closure/36-01-SUMMARY.md`
</output>
