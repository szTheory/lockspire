---
phase: 102-generated-host-scaffolding-telemetry-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/lockspire/release_readiness_contract_test.exs
autonomous: true
requirements: [SCAFFOLD-01, SCAFFOLD-02]
must_haves:
  truths:
    - "The release-readiness contract test fails loudly if any token-format prompt/branch is ever added to the install task or generator source."
    - "The release-readiness contract test fails loudly if the install-template canonical :lockspire_protected_api block is ever uncommented or de-synced."
    - "A maintainer running `mix test test/lockspire/release_readiness_contract_test.exs` sees the two new SCAFFOLD guard clauses pass against the already-shipped (Phases 97/101) install scaffolding."
  artifacts:
    - path: "test/lockspire/release_readiness_contract_test.exs"
      provides: "Two new SCAFFOLD regression-guard clauses + @install_task_path/@install_generator_path constants"
      contains: "@install_task_path"
  key_links:
    - from: "test/lockspire/release_readiness_contract_test.exs"
      to: "lib/mix/tasks/lockspire.install.ex + lib/lockspire/generators/install.ex"
      via: "File.read! + refute on token-format regex"
      pattern: "refute.*access_token_format"
    - from: "test/lockspire/release_readiness_contract_test.exs"
      to: "priv/templates/lockspire.install/router.ex"
      via: "raw-bytes BEGIN/END marker capture + commented-line assert"
      pattern: "BEGIN LOCKSPIRE_PROTECTED_PIPELINE"
---

<objective>
Add two regression-guard clauses to `test/lockspire/release_readiness_contract_test.exs` that fence the already-shipped install scaffolding behavior (SCAFFOLD-01/02, satisfied at the behavior level by Phases 97/101 per D-01) so it cannot silently regress.

Purpose: SCAFFOLD-01/02 add NO new generated-output behavior (D-01). The real Phase-102 SCAFFOLD work is two drift fences (D-02): a no-format-prompt refute over the install task + generator source, and an uncomment-ready assertion that the install-template canonical pipeline block stays fully commented.
Output: Two new contract-test clauses + two new path constants (`@install_task_path`, `@install_generator_path`) in the existing contract test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-CONTEXT.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-RESEARCH.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add no-format-prompt refute guard (SCAFFOLD-02, D-02 #1)</name>
  <files>test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - test/lockspire/release_readiness_contract_test.exs (the file being modified; read lines 78-100 for the existing `Path.expand(... __DIR__)` constant idiom, and the surrounding `describe`/`test` structure so the new clause matches the file's conventions)
    - lib/mix/tasks/lockspire.install.ex (the refute TARGET — confirm OptionParser strict list parses only web/scope/path/mount_path/help/sigra_host; no token-format switch)
    - lib/lockspire/generators/install.ex (the second refute TARGET — confirm no `:jwt`/`:opaque`/format logic in the generator SOURCE; it renders the template via EEx.eval_file at runtime)
    - priv/templates/lockspire.install/router.ex (must NOT be a refute target — it legitimately contains `audience:`/`enforce_audience:`)
  </read_first>
  <action>
    Add two new module constants beside the existing `@install_template_router_path` (defined ~line 88), using the same `Path.expand("../../...", __DIR__)` idiom:
    `@install_task_path Path.expand("../../lib/mix/tasks/lockspire.install.ex", __DIR__)` and
    `@install_generator_path Path.expand("../../lib/lockspire/generators/install.ex", __DIR__)`.
    Add a new test clause (D-02 #1, SCAFFOLD-02) that iterates over `[@install_task_path, @install_generator_path]`, reads each with `File.read!/1`, and `refute`s the source matches `~r/access_token_format|token[_ ]format|:jwt|:opaque/i` with a failure message naming SCAFFOLD-02 and the offending path. CRITICAL: refute is over the install TASK + GENERATOR source ONLY — never the template (the template legitimately carries `audience:`/`enforce_audience:`). The generator renders the template at runtime via EEx, so the generator source itself stays clean — this refute is safe and passes today (per RESEARCH Citation Accuracy Audit).
  </action>
  <verify>
    <automated>mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/lockspire/release_readiness_contract_test.exs` contains the literal constant `@install_task_path` and `@install_generator_path`.
    - The new clause references `@install_task_path` and `@install_generator_path` and contains `refute` with the regex alternation `access_token_format` and `:jwt` and `:opaque`.
    - `priv/templates/lockspire.install/router.ex` is NOT among the refute targets (grep the new clause: it iterates only the task + generator constants).
    - `mix test test/lockspire/release_readiness_contract_test.exs` exits 0.
    - Mutation check: temporarily inserting the string `access_token_format:` into `lib/mix/tasks/lockspire.install.ex` makes the new clause FAIL (revert after confirming).
  </acceptance_criteria>
  <done>The no-format-prompt guard exists, targets only the task + generator source, and passes against current (clean) sources.</done>
</task>

<task type="auto">
  <name>Task 2: Add uncomment-ready RAW-bytes guard (SCAFFOLD-01, D-02 #2)</name>
  <files>test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - test/lockspire/release_readiness_contract_test.exs (read lines 140-168 for `extract_canonical_pipeline!/2` def + the marker regex at ~line 146 + the `normalize/2` clause at ~line 164 that STRIPS the `# ` prefix; read ~line 745-768 for the existing byte-identity use site that must be left intact)
    - priv/templates/lockspire.install/router.ex (read lines 11-18 — the `# BEGIN/END LOCKSPIRE_PROTECTED_PIPELINE` commented canonical block inside the `lockspire_routes/0` heredoc)
  </read_first>
  <action>
    Add a new test clause (D-02 #2, SCAFFOLD-01) that reads `@install_template_router_path` with `File.read!/1` (RAW bytes — do NOT use `extract_canonical_pipeline!/2`'s output here). Capture the body between the markers using `Regex.run(~r/# BEGIN LOCKSPIRE_PROTECTED_PIPELINE\n(.*?)\n[ \t]*# END LOCKSPIRE_PROTECTED_PIPELINE/ms, raw, capture: :all_but_first)`. Split the captured body on `"\n"`, and for every non-blank line (`String.trim(line) != ""`) `assert line =~ ~r/^\s*#/` with a failure message naming SCAFFOLD-01. CRITICAL anti-pattern to avoid (Pitfall 3): `extract_canonical_pipeline!(path, :elixir_in_commented_heredoc)` runs `normalize/2` (~line 164) which strips the `# ` prefix — asserting "every line commented" against that normalized output is a tautology that always passes. The "still commented" property REQUIRES the raw `File.read!` bytes. Leave the existing byte-identity test (~line 745, the `:elixir_in_commented_heredoc` four-file hash compare) untouched — it complements, not replaces, this raw guard.
  </action>
  <verify>
    <automated>mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The new clause calls `File.read!(@install_template_router_path)` and runs `Regex.run` with the `BEGIN LOCKSPIRE_PROTECTED_PIPELINE` ... `END LOCKSPIRE_PROTECTED_PIPELINE` marker pattern, capturing `:all_but_first`.
    - The new clause does NOT pass the install-template path through `extract_canonical_pipeline!/2` for the commented-state assertion (it operates on raw bytes).
    - The new clause asserts every non-blank body line matches `~r/^\s*#/`.
    - `mix test test/lockspire/release_readiness_contract_test.exs` exits 0.
    - Mutation check: temporarily uncommenting one body line in `priv/templates/lockspire.install/router.ex` (removing its leading `# `) makes the new clause FAIL (revert after confirming).
  </acceptance_criteria>
  <done>The uncomment-ready guard reads raw bytes, asserts every canonical-block body line is commented, passes today, and fails when a line is uncommented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source files → contract test | The guards read repo source bytes at test time; no untrusted runtime input crosses here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | install task/generator silently regaining a token-format prompt | mitigate | D-02 #1 refute guard over task + generator source fails the suite if `access_token_format`/`:jwt`/`:opaque` reappears (SCAFFOLD-02 false-security regression). |
| T-102-02 | Tampering | install-template canonical block accidentally uncommented/de-synced | mitigate | D-02 #2 raw-bytes guard fails the suite if any block body line loses its `# ` prefix (SCAFFOLD-01). |
| T-102-03 | Repudiation | guard becomes a tautology (Pitfall 3) and silently proves nothing | mitigate | guard asserts against RAW `File.read!` bytes, not the prefix-stripping `extract_canonical_pipeline!/2` output; acceptance includes a mutation check. |
</threat_model>

<verification>
- `mix test test/lockspire/release_readiness_contract_test.exs` exits 0 with both new clauses present and passing.
- Mutation checks for both guards confirm they FAIL on regression (token-format string in task; uncommented template line).
</verification>

<success_criteria>
SCAFFOLD-01 and SCAFFOLD-02 are fenced: the install task/generator cannot silently regain a token-format decision, and the install-template canonical pipeline block cannot silently become uncommented, without the release-readiness contract test failing.
</success_criteria>

<output>
Create `.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-01-SUMMARY.md` when done.
</output>
