---
phase: 102-generated-host-scaffolding-telemetry-migration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/plug/verify_token.ex
  - test/lockspire/plug/verify_token_telemetry_test.exs
autonomous: true
requirements: [TELEMETRY-01]
must_haves:
  truths:
    - "An operator subscribed to [:lockspire, :rs, :token_format] receives an event with %{count: 1} and metadata %{token_format: :jwt, client_id, audience, binding_type} on every verified at+jwt through VerifyToken."
    - "The same event fires with %{token_format: :\"opaque-rejected\", client_id: nil, audience: nil, binding_type: nil} when an opaque token hits the plug."
    - "The event is emitted via direct :telemetry.execute/3 (no audit double-emit, no nil-metadata drop)."
  artifacts:
    - path: "lib/lockspire/plug/verify_token.ex"
      provides: "Two direct :telemetry.execute/3 emit sites (JWT-success + opaque-rejection)"
      contains: ":telemetry.execute"
    - path: "test/lockspire/plug/verify_token_telemetry_test.exs"
      provides: "attach_many → assert_received capture test for both emit sites + literal-atom check"
      exports: []
  key_links:
    - from: "lib/lockspire/plug/verify_token.ex do_verify_token/3 (claims in hand)"
      to: "[:lockspire, :rs, :token_format]"
      via: "direct :telemetry.execute with %{count: 1}"
      pattern: "telemetry\\.execute.*:rs, :token_format"
    - from: "lib/lockspire/plug/verify_token.ex opaque-rejection branch"
      to: "[:lockspire, :rs, :token_format]"
      via: "direct :telemetry.execute with :\"opaque-rejected\""
      pattern: "opaque-rejected"
---

<objective>
Emit a `[:lockspire, :rs, :token_format]` telemetry event at two sites inside `Lockspire.Plug.VerifyToken` (TELEMETRY-01), via direct `:telemetry.execute/3`, and prove it with a capture test.

Purpose: Operators get a per-request RS verification counter showing what token shape flows through their plug — `:jwt` on success, `:"opaque-rejected"` on the structural-opaque reject path — with `client_id`/`audience`/`binding_type` metadata, without audit-log flooding or nil-metadata loss.
Output: Two emit sites in `verify_token.ex` + new `test/lockspire/plug/verify_token_telemetry_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-CONTEXT.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-RESEARCH.md
@.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing telemetry capture test for both emit sites</name>
  <files>test/lockspire/plug/verify_token_telemetry_test.exs</files>
  <read_first>
    - test/lockspire/clients_test.exs (lines 158-182 — the repo's telemetry idiom: `:telemetry.attach_many/4` → handler `send`s to `self()` → `assert_received`; MIRROR this, but use the 4-arg tuple so measurements are assertable)
    - lib/lockspire/plug/verify_token.ex (the full module — emit sites are in `verify_token/3` opaque branch lines 105-122 and `do_verify_token/3` lines 124-136; `binding_type/1` helper ~467; `AccessToken` aliased line 15)
    - lib/lockspire/access_token.ex (the struct — fields :token, :claims, :client_id, :authorization_scheme, :binding_type, :binding_requirements, :error, binding_verified: false; NO `audience` field, so success metadata reads `claims["aud"]`)
    - test/integration/phase100_sender_constraint_e2e_test.exs (precedent for minting a real signed at+jwt via `AccessTokenSigner.issue/3` + active-key setup, to drive the JWT-success path through `VerifyToken.call/2`)
    - test/support (check for an existing signing-key / active-key fixture BEFORE adding one)
  </read_first>
  <behavior>
    - JWT-success: exercising `VerifyToken.call/2` with a valid signed `at+jwt` emits `{:telemetry_event, [:lockspire, :rs, :token_format], %{count: 1}, %{token_format: :jwt, client_id: <cid>, audience: <aud>, binding_type: <bt>}}`.
    - Opaque-rejection: exercising `VerifyToken.call/2` with an opaque (non-3-segment / non-base64url) token emits `{:telemetry_event, [:lockspire, :rs, :token_format], %{count: 1}, %{token_format: :"opaque-rejected", client_id: nil, audience: nil, binding_type: nil}}`.
    - Literal-atom contract: the opaque assertion pattern-matches the literal hyphenated atom `:"opaque-rejected"` (a `:opaque_rejected` spelling would fail the match — covers D-07 for free).
  </behavior>
  <action>
    Create `test/lockspire/plug/verify_token_telemetry_test.exs`. Add private `attach_events/1` and `detach_events/1` helpers mirroring `clients_test.exs:158-182` but with the 4-arg handler `fn event, measurements, metadata, test_pid -> send(test_pid, {:telemetry_event, event, measurements, metadata}) end` over events `[[:lockspire, :rs, :token_format]]`; use a unique `handler_id` via `System.unique_integer([:positive])` and `:telemetry.detach(handler_id)` in `on_exit` to prevent handler leaks across async tests. Write two tests: (1) mint a real signed `at+jwt` (reuse `AccessTokenSigner.issue/3` + active-key setup from the phase100 e2e test; reuse any existing `test/support` signing fixture rather than adding one), drive it through `VerifyToken.call/2` on a conn with an `Authorization: Bearer <jwt>` header, then `assert_received` the `token_format: :jwt` 4-arg tuple; (2) drive an opaque token (e.g. a random non-3-segment string) through `VerifyToken.call/2` and `assert_received` the `token_format: :"opaque-rejected"` tuple with all-`nil` metadata. Run the test FIRST and confirm it fails RED (emit sites do not exist yet). Do NOT add the emit code in this task.
  </action>
  <verify>
    <automated>mix test test/lockspire/plug/verify_token_telemetry_test.exs || true</automated>
  </verify>
  <acceptance_criteria>
    - File `test/lockspire/plug/verify_token_telemetry_test.exs` exists.
    - It uses `:telemetry.attach_many/4` with a 4-arg handler and `assert_received {:telemetry_event, [:lockspire, :rs, :token_format], %{count: 1}, ...}`.
    - It asserts the literal atom `:"opaque-rejected"` (hyphenated) in the opaque case.
    - It detaches the handler in `on_exit`.
    - Running the file FAILS (RED) at this task because the emit sites are not yet implemented.
  </acceptance_criteria>
  <done>The telemetry capture test exists, covers both sites + the literal atom, detaches handlers, and is RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit the event at both verify_token.ex sites (GREEN)</name>
  <files>lib/lockspire/plug/verify_token.ex</files>
  <read_first>
    - lib/lockspire/plug/verify_token.ex (full module — confirm SITE B is the `if opaque_shape?(token)` branch building `%AccessToken{}` at lines 114-118, and SITE A is in `do_verify_token/3` after `verify_signature_and_claims` yields `claims` ~line 127, BEFORE/independent of `apply_restrictions/2` at line 136)
    - lib/lockspire/observability.ex (lines 25-44 — the `emit/4` helper; understand WHY D-03 bypasses it: it double-emits a `[:lockspire, :audit, ...]` copy AND runs redaction that drops nil metadata. Do NOT use emit/4 here)
  </read_first>
  <action>
    Add direct `:telemetry.execute/3` calls at two sites (D-03/D-04/D-05). SITE A (JWT-success, D-05 #1): inside `do_verify_token/3`, after the `with` confirms `claims` (claims in hand) and at format-decision time — emit BEFORE/independent of `apply_restrictions/2` so the `:jwt` count is coherent with the opaque count regardless of subsequent audience/scope failure (Pitfall 4). Call `:telemetry.execute([:lockspire, :rs, :token_format], %{count: 1}, %{token_format: :jwt, client_id: Map.get(claims, "client_id"), audience: Map.get(claims, "aud"), binding_type: binding_type(claims)})`. Read `audience` from `claims["aud"]` — the `AccessToken` struct has NO `audience` field. SITE B (opaque-rejection, D-05 #2): inside the `if opaque_shape?(token)` branch (lines 105-122), emit `:telemetry.execute([:lockspire, :rs, :token_format], %{count: 1}, %{token_format: :"opaque-rejected", client_id: nil, audience: nil, binding_type: nil})` — opaque tokens carry no parseable claims so every field except the literal hyphenated atom `:"opaque-rejected"` (D-07) is `nil`. Measurement is the numeric map `%{count: 1}` at both sites (D-04). Security discipline (V7): emit EXACTLY these four metadata keys — never `token`, `claims`, `cnf`, or `jti`. You MAY extract a small private `emit_token_format/3` helper (Claude's discretion per CONTEXT) provided it calls `:telemetry.execute/3` directly and keeps the two sites' metadata shapes as specified. Do NOT route through `Observability.emit/4` (Pitfall 2 — it would drop the all-nil opaque metadata via `Redaction.sanitize_value(nil, _)` returning `:drop`, and flood the audit log).
  </action>
  <verify>
    <automated>mix test test/lockspire/plug/verify_token_telemetry_test.exs test/lockspire/plug/verify_token_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lockspire/plug/verify_token.ex` contains `:telemetry.execute([:lockspire, :rs, :token_format]` at two distinct sites (grep count >= 2, or one `emit_token_format` helper called twice).
    - The file contains the literal `:"opaque-rejected"` atom and reads `audience` from `Map.get(claims, "aud")` (or `claims["aud"]`), not a struct field.
    - The file does NOT call `Observability.emit` for this event (grep: no `Observability.emit` with `:token_format`).
    - `mix test test/lockspire/plug/verify_token_telemetry_test.exs` exits 0 (GREEN).
    - `mix test test/lockspire/plug/verify_token_test.exs` still exits 0 (no regression to existing verifier behavior).
  </acceptance_criteria>
  <done>Both emit sites use direct :telemetry.execute/3 with the specified metadata; the telemetry test is GREEN and the existing verify_token test still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host API request (Bearer/DPoP token) → VerifyToken.call/2 | Untrusted bearer/DPoP token crosses here; the plug's verification behavior is FROZEN (Phase 98). Phase 102 only observes the format decision. |
| VerifyToken → out-of-process :telemetry subscribers | Metadata leaves the BEAM-internal verifier toward operator Telemetry.Metrics reporters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-04 | Information Disclosure | telemetry metadata leaking token bytes / claims / cnf material to subscribers (V7 logging) | mitigate | Emit EXACTLY four keys (`token_format`, `client_id`, `audience`, `binding_type`); never `token`, `claims`, `cnf`, `jti`. The direct-execute path skips redaction, so the emit site IS the redaction discipline. |
| T-102-05 | Denial of Service | audit-log flooding via per-request emit | mitigate | D-03 direct `:telemetry.execute/3` bypasses `Observability.emit/4`, so no `[:lockspire, :audit, :rs, :token_format]` copy is written per protected request. |
| T-102-06 | Information Disclosure | nil opaque metadata silently dropped, hiding the reject signal | mitigate | Direct execute (no `Redaction.for_telemetry`); the all-nil opaque metadata reaches subscribers intact (test asserts `binding_type: nil` present). |
| T-102-07 | Tampering | future "optimization" routing the event back through emit/4 | mitigate | Plan + acceptance criteria forbid `Observability.emit` for this event; telemetry test asserts the all-nil opaque metadata survives. |
</threat_model>

<verification>
- `mix test test/lockspire/plug/verify_token_telemetry_test.exs` exits 0 (both sites + literal atom).
- `mix test test/lockspire/plug/verify_token_test.exs` exits 0 (no verifier-behavior regression).
- Grep confirms direct `:telemetry.execute/3` and no `Observability.emit` for this event.
</verification>

<success_criteria>
TELEMETRY-01 is satisfied: every verification attempt that reaches a format decision emits `[:lockspire, :rs, :token_format]` with `%{count: 1}` and the four-key metadata (`:jwt` on success, `:"opaque-rejected"` on the opaque-reject path), via direct `:telemetry.execute/3`, proven by the capture test.
</success_criteria>

<output>
Create `.planning/phases/102-generated-host-scaffolding-telemetry-migration/102-02-SUMMARY.md` when done.
</output>
