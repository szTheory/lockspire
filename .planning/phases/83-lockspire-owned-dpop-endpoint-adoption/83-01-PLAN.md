---
phase: 83-lockspire-owned-dpop-endpoint-adoption
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/token_endpoint_dpop.ex
  - lib/lockspire/protocol/token_exchange.ex
  - lib/lockspire/protocol/refresh_exchange.ex
  - lib/lockspire/web/controllers/token_controller.ex
  - test/lockspire/protocol/token_endpoint_dpop_test.exs
  - test/lockspire/protocol/token_exchange_test.exs
  - test/lockspire/protocol/refresh_exchange_test.exs
  - test/lockspire/web/token_controller_test.exs
autonomous: true
requirements:
  - NONCE-AS-01
  - NONCE-AS-02
  - NONCE-AS-03
must_haves:
  truths:
    - All Lockspire-owned `/token` grants that already validate through `Lockspire.Protocol.TokenEndpointDPoP` share one authorization-server nonce challenge contract.
    - Missing required DPoP proof still maps to `invalid_dpop_proof`, while present-but-missing-or-invalid nonce maps to `use_dpop_nonce`.
    - DPoP-bound refresh exchanges preserve existing MTLS and stored-binding validation while adopting the same authorization-server nonce retry contract.
    - RFC 8693 token exchange remains outside the Phase 83 nonce-support claim.
  artifacts:
    - path: lib/lockspire/protocol/token_endpoint_dpop.ex
      provides: Shared authorization-server nonce validation and typed `use_dpop_nonce` mapping for supported `/token` flows
    - path: lib/lockspire/protocol/token_exchange.ex
      provides: Uniform adoption of the token-endpoint DPoP nonce seam across authorization-code, device-code, and CIBA flows
    - path: lib/lockspire/protocol/refresh_exchange.ex
      provides: Refresh-token adoption of the authorization-server nonce seam without MTLS/binding drift
    - path: lib/lockspire/web/controllers/token_controller.ex
      provides: Exact `/token` retry wire contract with `DPoP-Nonce` exposure
  key_links:
    - from: lib/lockspire/protocol/token_exchange.ex
      to: lib/lockspire/protocol/token_endpoint_dpop.ex
      via: shared issuance-context resolution
      pattern: "TokenEndpointDPoP.resolve_context"
    - from: lib/lockspire/protocol/refresh_exchange.ex
      to: lib/lockspire/protocol/token_endpoint_dpop.ex
      via: refresh issuance-context resolution
      pattern: "resolve_refresh_context"
    - from: lib/lockspire/web/controllers/token_controller.ex
      to: lib/lockspire/protocol/token_exchange.ex
      via: thin error/header adaptation
      pattern: "maybe_put_dpop_nonce"
---

<objective>
Make the Lockspire-owned `/token` surface uniformly nonce-aware across the supported DPoP grant paths without changing pre-existing non-nonce behavior.

Purpose: Phase 83 should close the authorization-server slice by ensuring authorization-code, device-code, CIBA, and DPoP-bound refresh requests all share one nonce challenge/retry contract at the `TokenEndpointDPoP` seam.
Output: Shared `/token` nonce adoption through the existing protocol seam, plus exact controller-level proof of the public retry contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-CONTEXT.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-RESEARCH.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-PATTERNS.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-VALIDATION.md
@.planning/phases/82-shared-dpop-nonce-primitive/82-01-PLAN.md
@lib/lockspire/protocol/token_endpoint_dpop.ex
@lib/lockspire/protocol/token_exchange.ex
@lib/lockspire/protocol/refresh_exchange.ex
@lib/lockspire/web/controllers/token_controller.ex
@test/lockspire/protocol/token_endpoint_dpop_test.exs
@test/lockspire/protocol/token_exchange_test.exs
@test/lockspire/protocol/refresh_exchange_test.exs
@test/lockspire/web/token_controller_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep one authorization-server nonce seam across supported `/token` grants</name>
  <files>lib/lockspire/protocol/token_endpoint_dpop.ex, lib/lockspire/protocol/token_exchange.ex, lib/lockspire/protocol/refresh_exchange.ex, test/lockspire/protocol/token_endpoint_dpop_test.exs, test/lockspire/protocol/token_exchange_test.exs, test/lockspire/protocol/refresh_exchange_test.exs</files>
  <read_first>
    - .planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-CONTEXT.md
    - .planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-RESEARCH.md
    - lib/lockspire/protocol/token_endpoint_dpop.ex
    - lib/lockspire/protocol/token_exchange.ex
    - lib/lockspire/protocol/refresh_exchange.ex
    - test/lockspire/protocol/token_endpoint_dpop_test.exs
    - test/lockspire/protocol/token_exchange_test.exs
    - test/lockspire/protocol/refresh_exchange_test.exs
  </read_first>
  <action>
    Audit every Lockspire-owned `/token` grant path against Decision D-02 and keep the authorization-server nonce contract centralized in `Lockspire.Protocol.TokenEndpointDPoP`. Ensure authorization-code, device-code, and CIBA continue to flow through `resolve_context/2`, and ensure DPoP-bound or FAPI-required refresh requests continue to flow through `resolve_refresh_context/3` with `nonce_purpose: :authorization_server`. Preserve the existing split from Decision D-04: missing required proof still returns `invalid_dpop_proof` with `reason_code: :missing_dpop_proof`, while present proofs with missing or invalid nonce return `use_dpop_nonce` with `reason_code` `:missing_dpop_nonce` or `:invalid_dpop_nonce`. Do not route RFC 8693 token exchange through this seam and do not change its support claim in tests or comments. Expand protocol coverage so at least one authorization-code case, one device-code case, one CIBA case, and one refresh case prove retryable nonce failure and successful retry where the rest of the DPoP checks pass.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/token_endpoint_dpop_test.exs test/lockspire/protocol/token_exchange_test.exs test/lockspire/protocol/refresh_exchange_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lockspire/protocol/token_exchange.ex` still contains `TokenEndpointDPoP.resolve_context` for authorization-code, device-code, and CIBA exchanges.
    - `lib/lockspire/protocol/refresh_exchange.ex` still contains `resolve_refresh_context` for refresh issuance.
    - `test/lockspire/protocol/token_exchange_test.exs` contains nonce-failure coverage for supported `/token` grant paths beyond authorization code.
    - `test/lockspire/protocol/refresh_exchange_test.exs` proves a DPoP-bound refresh request returns `use_dpop_nonce` before retry and succeeds after retry with a valid nonce.
    - RFC 8693 tests and code paths remain outside the nonce-adoption seam.
  </acceptance_criteria>
  <done>The supported Lockspire-owned `/token` flows now share one authorization-server nonce contract at the protocol seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the exact `/token` HTTP retry contract without expanding the controller matrix</name>
  <files>lib/lockspire/web/controllers/token_controller.ex, test/lockspire/web/token_controller_test.exs</files>
  <read_first>
    - lib/lockspire/web/controllers/token_controller.ex
    - test/lockspire/web/token_controller_test.exs
    - .planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-PATTERNS.md
  </read_first>
  <action>
    Keep `Lockspire.Web.TokenController` as a thin transport adapter: it should continue to set cache headers, set status from protocol errors, emit `DPoP-Nonce` only when `error.dpop_nonce` is present, and expose `DPoP-Nonce` through `Access-Control-Expose-Headers`. Expand `test/lockspire/web/token_controller_test.exs` so the HTTP-level proof covers the exact `400` retry contract for at least one supported DPoP `/token` grant beyond the existing authorization-code case if coverage is still missing after Task 1. Assert the JSON error body contains `"error" => "use_dpop_nonce"`, the response header list contains `dpop-nonce`, and the retried request with the supplied nonce succeeds with `"token_type" => "DPoP"`. Do not duplicate the full replay or binding matrix at the controller layer.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/token_controller_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lockspire/web/controllers/token_controller.ex` still contains `maybe_put_dpop_nonce`.
    - `test/lockspire/web/token_controller_test.exs` contains an exact `/token` retry proof with `status == 400`, JSON `"error" == "use_dpop_nonce"`, and a `dpop-nonce` response header.
    - A retry using the supplied nonce succeeds and asserts `"token_type" == "DPoP"`.
    - The controller suite does not add replay, `ath`, or binding-mismatch matrix duplication that already belongs in protocol tests.
  </acceptance_criteria>
  <done>The public `/token` retry surface is proved with exact wire-level assertions and no controller-level scope creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client `/token` request -> `TokenEndpointDPoP` | Untrusted DPoP proofs, nonces, and MTLS material enter the authorization-server issuance path. |
| Protocol errors -> `TokenController` | Typed nonce failures must survive transport adaptation without collapsing into generic invalid-proof errors. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Drift | supported `/token` grants | mitigate | Keep supported DPoP grant paths on the shared `TokenEndpointDPoP` seam and add per-grant retry proof. |
| T-83-02 | Tampering | `/token` transport contract | mitigate | Assert the exact `400` + `use_dpop_nonce` + `DPoP-Nonce` controller contract and retry success path. |
</threat_model>

<verification>
Run the protocol suites after Task 1 and the controller suite after Task 2. Finish by rerunning the full `/token` phase subset so authorization-code, device-code, CIBA, and refresh nonce behavior stay aligned.
</verification>

<success_criteria>
Every supported Lockspire-owned `/token` DPoP path uses one authorization-server nonce contract, and the public retry surface is proved with exact HTTP assertions.
</success_criteria>

<output>
After completion, create `.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-01-SUMMARY.md`
</output>
