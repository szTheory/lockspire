---
phase: 83-lockspire-owned-dpop-endpoint-adoption
plan: 02
type: execute
wave: 2
depends_on:
  - 83-01
files_modified:
  - lib/lockspire/protocol/protected_resource_dpop.ex
  - lib/lockspire/protocol/userinfo.ex
  - lib/lockspire/web/controllers/userinfo_controller.ex
  - test/lockspire/protocol/protected_resource_dpop_test.exs
  - test/lockspire/web/userinfo_controller_test.exs
autonomous: true
requirements:
  - NONCE-RS-01
  - NONCE-RS-02
  - NONCE-RS-03
must_haves:
  truths:
    - `/userinfo` keeps resource-server nonce validation inside `Lockspire.Protocol.ProtectedResourceDPoP` instead of re-implementing proof rules in the controller.
    - Resource nonce failures remain `401` retry challenges with `WWW-Authenticate: DPoP ... error="use_dpop_nonce"` plus `DPoP-Nonce`.
    - Retried `/userinfo` requests succeed only when the supplied nonce is valid and replay, `ath`, binding, MTLS, and bearer checks still pass.
    - `401` vs `403` semantics remain unchanged; nonce failures do not become authorization failures.
  artifacts:
    - path: lib/lockspire/protocol/protected_resource_dpop.ex
      provides: Single protected-resource nonce rule set for `/userinfo`
    - path: lib/lockspire/protocol/userinfo.ex
      provides: Thin orchestration over protected-resource validation plus existing MTLS/bearer policy
    - path: lib/lockspire/web/controllers/userinfo_controller.ex
      provides: Exact DPoP-aware `401` challenge rendering with exposed retry headers
    - path: test/lockspire/web/userinfo_controller_test.exs
      provides: Public retry proof for `/userinfo`
  key_links:
    - from: lib/lockspire/protocol/userinfo.ex
      to: lib/lockspire/protocol/protected_resource_dpop.ex
      via: DPoP-bound access validation
      pattern: "validate_userinfo_access"
    - from: lib/lockspire/web/controllers/userinfo_controller.ex
      to: lib/lockspire/protocol/userinfo.ex
      via: thin challenge rendering
      pattern: "put_www_authenticate"
---

<objective>
Close the Lockspire-owned protected-resource nonce slice on `/userinfo` while preserving the existing protected-resource semantics.

Purpose: Phase 83 should make `/userinfo` a truthful resource-server nonce retry surface using the existing `ProtectedResourceDPoP` seam, with one exact HTTP-level proof of the challenge and retry contract.
Output: Protocol-owned resource nonce behavior plus `/userinfo` controller proof for `401` challenge shape, `DPoP-Nonce`, exposed headers, and successful retry.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-CONTEXT.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-RESEARCH.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-PATTERNS.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-VALIDATION.md
@.planning/phases/80-sender-constraining-integration/80-03-PLAN.md
@lib/lockspire/protocol/protected_resource_dpop.ex
@lib/lockspire/protocol/userinfo.ex
@lib/lockspire/web/controllers/userinfo_controller.ex
@test/lockspire/protocol/protected_resource_dpop_test.exs
@test/lockspire/web/userinfo_controller_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep `/userinfo` on one protected-resource nonce seam</name>
  <files>lib/lockspire/protocol/protected_resource_dpop.ex, lib/lockspire/protocol/userinfo.ex, test/lockspire/protocol/protected_resource_dpop_test.exs</files>
  <read_first>
    - .planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-CONTEXT.md
    - .planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-RESEARCH.md
    - lib/lockspire/protocol/protected_resource_dpop.ex
    - lib/lockspire/protocol/userinfo.ex
    - test/lockspire/protocol/protected_resource_dpop_test.exs
  </read_first>
  <action>
    Preserve `Lockspire.Protocol.ProtectedResourceDPoP` as the single resource-server nonce validator seam and keep `Lockspire.Protocol.Userinfo` as thin orchestration over it. Ensure nonce validation stays tied to `nonce_purpose: :resource_server`, that missing or invalid nonce still maps to `use_dpop_nonce`, and that valid nonces do not mask later replay, `ath`, key-binding, wrong-scheme, invalid-target, or MTLS failures. Expand `test/lockspire/protocol/protected_resource_dpop_test.exs` so it proves retry success after a challenge nonce is supplied and adds valid-nonce variants for at least replay, wrong `ath`, and wrong proof key to assert those cases still return their original `invalid_token` reason codes rather than nonce errors.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/protected_resource_dpop_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/lockspire/protocol/userinfo.ex` still contains `ProtectedResourceDPoP.validate_userinfo_access`.
    - `test/lockspire/protocol/protected_resource_dpop_test.exs` contains a retry-success path after receiving a resource-server nonce.
    - `test/lockspire/protocol/protected_resource_dpop_test.exs` proves replay, wrong `ath`, and wrong proof key remain non-nonce failures when a valid nonce is present.
    - No new generic gateway or host-plug abstractions are introduced in this plan.
  </acceptance_criteria>
  <done>`/userinfo` stays on one protected-resource nonce seam, with nonce and non-nonce failures clearly separated at the protocol layer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the exact `/userinfo` DPoP retry challenge and header exposure</name>
  <files>lib/lockspire/web/controllers/userinfo_controller.ex, test/lockspire/web/userinfo_controller_test.exs</files>
  <read_first>
    - lib/lockspire/web/controllers/userinfo_controller.ex
    - test/lockspire/web/userinfo_controller_test.exs
    - test/lockspire/plug/require_token_test.exs
    - .planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-PATTERNS.md
  </read_first>
  <action>
    Keep `Lockspire.Web.UserinfoController` as a thin challenge renderer. Expand `test/lockspire/web/userinfo_controller_test.exs` so one exact retry flow proves the first request returns `401`, `WWW-Authenticate` contains `DPoP realm="Lockspire Userinfo"` and `error="use_dpop_nonce"`, the response contains a `dpop-nonce` header, and `access-control-expose-headers` exposes both `DPoP-Nonce` and `WWW-Authenticate`. Then prove a second request using the supplied nonce succeeds with `200` and the expected userinfo claims. Do not duplicate the full protected-resource negative matrix at the controller layer; keep those checks protocol-side.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/userinfo_controller_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/lockspire/web/userinfo_controller_test.exs` contains a `/userinfo` retry flow with first-response `status == 401` and second-response `status == 200`.
    - The first response asserts `WWW-Authenticate` includes `error="use_dpop_nonce"`.
    - The first response asserts `get_resp_header(conn, "dpop-nonce")` returns a nonce value.
    - The first response asserts `access-control-expose-headers` includes both `DPoP-Nonce` and `WWW-Authenticate`.
    - The success response still returns expected userinfo claims such as `"sub"` and `"email"`.
  </acceptance_criteria>
  <done>The public `/userinfo` retry contract is locked with exact challenge and success assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client protected-resource request -> `ProtectedResourceDPoP` | Untrusted proof, `ath`, nonce, and presented access token enter the resource-server validator. |
| Protocol errors -> `UserinfoController` | Retryable nonce failures must render as DPoP challenges without changing protected-resource authorization semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-03 | Drift | protected-resource validator | mitigate | Keep `ProtectedResourceDPoP` as the only nonce rule set and add valid-nonce regressions for replay, `ath`, and binding. |
| T-83-04 | Tampering | `/userinfo` challenge rendering | mitigate | Assert exact `401` DPoP challenge shape, nonce header emission, and header exposure in the controller suite. |
</threat_model>

<verification>
Run the protected-resource protocol suite after Task 1 and the userinfo controller suite after Task 2. Finish by rerunning both together so protocol and HTTP challenge behavior remain aligned.
</verification>

<success_criteria>
`/userinfo` behaves as a truthful resource-server nonce retry surface, with protocol-owned validation and exact public challenge proof.
</success_criteria>

<output>
After completion, create `.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-02-SUMMARY.md`
</output>
