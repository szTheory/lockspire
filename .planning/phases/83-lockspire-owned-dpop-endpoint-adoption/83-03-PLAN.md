---
phase: 83-lockspire-owned-dpop-endpoint-adoption
plan: 03
type: execute
wave: 3
depends_on:
  - 83-01
  - 83-02
files_modified:
  - test/lockspire/protocol/token_endpoint_dpop_test.exs
  - test/lockspire/protocol/token_exchange_test.exs
  - test/lockspire/protocol/refresh_exchange_test.exs
  - test/lockspire/protocol/protected_resource_dpop_test.exs
  - test/lockspire/web/token_controller_test.exs
  - test/lockspire/web/userinfo_controller_test.exs
autonomous: true
requirements:
  - NONCE-AS-03
  - NONCE-RS-03
must_haves:
  truths:
    - Nonce adoption does not reclassify replay, `ath`, key-binding, wrong-scheme, MTLS, or bearer failures into `use_dpop_nonce`.
    - Authorization-server and resource-server nonce support are both proved with protocol-heavy tests and only thin controller-level wire-contract assertions.
    - The `/token` and `/userinfo` retry contracts remain truthful without overclaiming host-plug or RFC 8693 support.
  artifacts:
    - path: test/lockspire/protocol/token_exchange_test.exs
      provides: Grant-specific `/token` regression coverage with nonce support active
    - path: test/lockspire/protocol/refresh_exchange_test.exs
      provides: Refresh-path DPoP, MTLS, and binding regressions with nonce enforcement active
    - path: test/lockspire/protocol/protected_resource_dpop_test.exs
      provides: Protected-resource replay, `ath`, and binding regressions with a valid nonce present
    - path: test/lockspire/web/token_controller_test.exs
      provides: Thin `/token` wire-contract proof without negative-matrix duplication
    - path: test/lockspire/web/userinfo_controller_test.exs
      provides: Thin `/userinfo` wire-contract proof without negative-matrix duplication
  key_links:
    - from: test/lockspire/protocol/token_exchange_test.exs
      to: lib/lockspire/protocol/token_endpoint_dpop.ex
      via: supported `/token` grant regression coverage
      pattern: "use_dpop_nonce"
    - from: test/lockspire/protocol/protected_resource_dpop_test.exs
      to: lib/lockspire/protocol/protected_resource_dpop.ex
      via: valid-nonce non-regression assertions
      pattern: "invalid_dpop_ath"
---

<objective>
Lock the non-nonce regression matrix in place so Phase 83 can adopt nonce support without semantic drift.

Purpose: After the `/token` and `/userinfo` retry paths land, this plan hardens the proof boundary by asserting that replay, `ath`, binding, MTLS, and bearer semantics still behave exactly as before when nonce support is active.
Output: Protocol-heavy regression coverage plus a thin, non-duplicative controller proof boundary for the Lockspire-owned nonce surfaces.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-CONTEXT.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-RESEARCH.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-VALIDATION.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-01-PLAN.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-02-PLAN.md
@test/lockspire/protocol/token_endpoint_dpop_test.exs
@test/lockspire/protocol/token_exchange_test.exs
@test/lockspire/protocol/refresh_exchange_test.exs
@test/lockspire/protocol/protected_resource_dpop_test.exs
@test/lockspire/web/token_controller_test.exs
@test/lockspire/web/userinfo_controller_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add valid-nonce regression proof for `/token` and `/userinfo` negative paths</name>
  <files>test/lockspire/protocol/token_endpoint_dpop_test.exs, test/lockspire/protocol/token_exchange_test.exs, test/lockspire/protocol/refresh_exchange_test.exs, test/lockspire/protocol/protected_resource_dpop_test.exs</files>
  <read_first>
    - .planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-RESEARCH.md
    - test/lockspire/protocol/token_endpoint_dpop_test.exs
    - test/lockspire/protocol/token_exchange_test.exs
    - test/lockspire/protocol/refresh_exchange_test.exs
    - test/lockspire/protocol/protected_resource_dpop_test.exs
  </read_first>
  <action>
    Extend the protocol suites so Phase 83 proves nonce support does not mask existing failures. For `/token`, add cases where the proof includes a valid authorization-server nonce but still fails because of replay, invalid `iat`, DPoP binding mismatch, or MTLS/client-certificate mismatch as appropriate for the supported grant path, and assert those cases keep their original reason codes instead of returning `use_dpop_nonce`. For `/userinfo`, add the equivalent valid-resource-nonce cases for replay, wrong `ath`, wrong proof key, wrong authorization scheme, and invalid target URI. Keep these assertions at the protocol layer unless the failure is controller-specific.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/token_endpoint_dpop_test.exs test/lockspire/protocol/token_exchange_test.exs test/lockspire/protocol/refresh_exchange_test.exs test/lockspire/protocol/protected_resource_dpop_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Each touched protocol suite contains at least one valid-nonce regression case that still returns a non-nonce failure reason.
    - `/token` protocol regressions preserve their existing error codes for replay, invalid `iat`, binding mismatch, or MTLS mismatch.
    - `/userinfo` protocol regressions preserve their existing `invalid_token` reason codes for replay, `ath`, binding, wrong scheme, and invalid target URI.
    - No protocol test starts asserting host-plug or RFC 8693 nonce support.
  </acceptance_criteria>
  <done>The core negative-path matrix stays stable with nonce support active.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep the controller proof boundary thin and phase-scoped</name>
  <files>test/lockspire/web/token_controller_test.exs, test/lockspire/web/userinfo_controller_test.exs</files>
  <read_first>
    - test/lockspire/web/token_controller_test.exs
    - test/lockspire/web/userinfo_controller_test.exs
    - .planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-PATTERNS.md
  </read_first>
  <action>
    Review the controller suites after Plans 01 and 02 and keep only the HTTP-contract assertions Phase 83 actually needs: exact status code, JSON error body, `DPoP-Nonce`, exposed headers, and retry success. If any controller tests added during Phase 83 duplicate protocol-only replay, `ath`, or binding matrices, collapse them back into the protocol suites and leave one thin wire-contract proof per owned surface. Ensure `/token` controller proof remains limited to Lockspire-owned grant paths and `/userinfo` controller proof remains limited to the Lockspire-owned protected-resource surface.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/token_controller_test.exs test/lockspire/web/userinfo_controller_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/lockspire/web/token_controller_test.exs` contains retry-contract assertions but not a duplicated replay or binding matrix.
    - `test/lockspire/web/userinfo_controller_test.exs` contains retry-contract assertions but not a duplicated protocol negative matrix.
    - Both controller suites remain explicitly scoped to Lockspire-owned surfaces only.
  </acceptance_criteria>
  <done>The phase ends with protocol-heavy coverage and thin controller proofs that match the support boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Valid nonce -> downstream validation | Supplying a correct nonce must not bypass replay, `ath`, binding, MTLS, or scheme checks. |
| Protocol proof -> controller proof | HTTP-layer tests must prove only transport behavior, not silently replace protocol-level negative coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-02 | Reclassification | `/token` negative paths | mitigate | Add valid-nonce regression cases that assert original non-nonce reason codes remain intact. |
| T-83-03 | Reclassification | `/userinfo` negative paths | mitigate | Add valid-nonce regression cases for replay, `ath`, binding, and wrong-scheme outcomes. |
| T-83-04 | Scope drift | controller proof boundary | mitigate | Keep controller tests limited to exact public wire-contract assertions for owned surfaces only. |
</threat_model>

<verification>
Run the full phase-targeted protocol suite after Task 1 and the controller subset after Task 2. Finish by rerunning the full phase-targeted suite, then `mix test`, before phase verification.
</verification>

<success_criteria>
Nonce adoption on Lockspire-owned endpoints is backed by protocol-heavy non-regression proof, and the controller test boundary stays narrow and truthful.
</success_criteria>

<output>
After completion, create `.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-03-SUMMARY.md`
</output>
