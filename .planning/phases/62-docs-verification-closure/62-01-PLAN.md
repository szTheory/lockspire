---
phase: 62
plan: 01
type: execute
wave: 2
depends_on: [02]
files_modified:
  - README.md
  - docs/supported-surface.md
  - SECURITY.md
  - docs/install-and-onboard.md
  - docs/maintainer-conformance.md
  - docs/maintainer-release.md
  - docs/private-key-jwt-host-guide.md
autonomous: true
requirements:
  - DOC-01
  - V-03
must_haves:
  truths:
    - Public docs describe only the shipped `jwks_uri` + `private_key_jwt` slice.
    - `docs/supported-surface.md` remains the canonical support contract.
    - `README.md` stays summary-level and links to the canonical docs.
    - `SECURITY.md` explains guarded remote JWKS fetch boundaries, issuer-bound `aud`, replay posture, redaction expectations, and explicit exclusions.
    - Maintainer docs no longer contain the specific stale negative claims called out in Phase 62 context about unsupported `private_key_jwt` or `jwks_uri` behavior.
  artifacts:
    - path: docs/private-key-jwt-host-guide.md
      provides: Focused integrator guide for registration, audience, fetch constraints, and rotation behavior
    - path: test/lockspire/release_readiness_contract_test.exs
      provides: Final executable hierarchy and runtime-truth verification for claim-bearing doc updates
    - path: docs/supported-surface.md
      provides: Canonical support-truth contract for the shipped client-auth slice
    - path: SECURITY.md
      provides: Security-boundary wording for the shipped slice and explicit exclusions
  key_links:
    - from: README.md
      to: docs/supported-surface.md
      via: Canonical support contract link
    - from: docs/install-and-onboard.md
      to: docs/private-key-jwt-host-guide.md
      via: Focused client-auth guide link
---

<objective>
Align Lockspire's claim-bearing documentation with the shipped `jwks_uri` + `private_key_jwt` surface without broadening the embedded-library posture.

Purpose: An integrator should be able to understand the supported client-auth slice from docs alone, and maintainers should no longer carry the specific stale disclaimers identified in Phase 62 context.
Output: A focused host guide plus targeted updates to public and maintainer docs that all describe the same shipped surface truthfully.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-docs-verification-closure/62-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@README.md
@docs/supported-surface.md
@SECURITY.md
@docs/install-and-onboard.md
@docs/maintainer-conformance.md
@docs/maintainer-release.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add the focused `private_key_jwt` host guide</name>
  <files>docs/private-key-jwt-host-guide.md</files>
  <action>Create a focused integrator guide for Lockspire's shipped client-auth slice. Cover client registration shape, `jwks` xor `jwks_uri`, guarded `jwks_uri` fetch constraints, accepted signing algorithms as derived from issuer posture, issuer-identifier `aud`, bounded rotation refresh behavior, and the representative Lockspire-owned direct-client endpoints that consume the shared auth seam. Keep the guide explanatory rather than normative and do not expand into sample-app sprawl or unsupported auth methods.</action>
  <verify>
    <automated>mix docs.verify</automated>
  </verify>
  <done>The repo has one focused client-auth guide that teaches the shipped slice without widening scope, and its public claims are ready for final contract validation in Plan 62-03.</done>
</task>

<task type="auto">
  <name>Task 2: Update public support and security truth surfaces</name>
  <files>README.md, docs/supported-surface.md, SECURITY.md, docs/install-and-onboard.md</files>
  <action>Update the public docs so they all describe the same shipped client-auth surface. Keep `docs/supported-surface.md` as canonical, keep `README.md` concise and link-driven, keep `SECURITY.md` boundary-focused, and keep `docs/install-and-onboard.md` narrow by linking to the new guide. Fix only the direct truth mismatches called out in Phase 62 context for this closure slice: stale `jwks_uri` fetch exclusions, stale `private_key_jwt` support disclaimers, and any release-posture wording that would otherwise contradict the shipped direct-client auth surface. Do not broaden this into unrelated editorial cleanup.</action>
  <verify>
    <automated>mix docs.verify</automated>
  </verify>
  <done>Public docs consistently describe the shipped slice and point readers to the right canonical source, pending final hierarchy enforcement in Plan 62-03.</done>
</task>

<task type="auto">
  <name>Task 3: Remove stale maintainer-only disclaimers without turning runbooks into product contracts</name>
  <files>docs/maintainer-conformance.md, docs/maintainer-release.md</files>
  <action>Clean up only the specific stale wording in maintainer docs that Phase 62 context identifies as directly contradictory to the shipped slice, especially `private_key_jwt` and `jwks_uri` support disclaimers. Preserve the distinction between maintainer workflow truth and runtime support truth: maintainer docs should describe how to verify and release the shipped slice, but `docs/supported-surface.md` remains the canonical product contract. Do not use this task for unrelated maintainer-doc modernization.</action>
  <verify>
    <automated>mix docs.verify</automated>
  </verify>
  <done>Maintainer docs no longer contradict the product docs or runtime behavior for the Phase 62 closure slice, and they are ready for final contract validation in Plan 62-03.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Public docs -> Integrator expectations | Overclaims or stale underclaims can mislead adopters about auth guarantees |
| Maintainer runbooks -> Release posture | Workflow docs must not drift into false product truth or preserve stale disclaimers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01-01 | Tampering | Public support docs | mitigate | Keep one canonical support contract and update related docs in the same pass |
| T-62-01-02 | Information Disclosure | SECURITY.md wording | mitigate | Describe boundaries, redaction posture, and exclusions precisely without leaking internals |
| T-62-01-03 | Repudiation | Maintainer docs | mitigate | Remove stale statements that contradict repo-proven runtime behavior |
</threat_model>

<verification>
mix docs.verify
Final claim-bearing hierarchy and runtime-truth validation is completed in Plan 62-03 via `mix test test/lockspire/release_readiness_contract_test.exs`.
</verification>

<success_criteria>
- `docs/private-key-jwt-host-guide.md` explains the shipped slice clearly and narrowly.
- `README.md`, `docs/supported-surface.md`, `SECURITY.md`, and `docs/install-and-onboard.md` describe the same shipped support surface.
- Maintainer docs no longer contradict support truth for `jwks_uri` and `private_key_jwt`.
</success_criteria>

<output>
After completion, create `.planning/phases/62-docs-verification-closure/62-01-SUMMARY.md`
</output>
