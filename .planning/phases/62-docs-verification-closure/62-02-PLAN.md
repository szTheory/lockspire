---
phase: 62
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/integration/phase62_private_key_jwt_e2e_test.exs
  - test/lockspire/protocol/client_auth_test.exs
  - test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs
  - test/lockspire/jwks_fetcher_test.exs
  - test/lockspire/jwks_fetcher/target_safety_test.exs
autonomous: true
requirements:
  - V-01
  - V-02
  - DOC-01
must_haves:
  truths:
    - Lockspire proves the shipped `private_key_jwt` slice through a small representative HTTP boundary test set.
    - Inline `jwks` and guarded `jwks_uri` client authentication both work on a Lockspire-owned direct-client seam.
    - Negative-path HTTP proof stays externally generic as `invalid_client` and does not leak verifier internals.
    - Lower-level tests carry the full verifier and fetcher matrix without multiplying full E2Es per endpoint.
  artifacts:
    - path: test/integration/phase62_private_key_jwt_e2e_test.exs
      provides: Representative HTTP proof for inline `jwks`, `jwks_uri`, rotation refresh, and generic rejection behavior
    - path: test/lockspire/protocol/client_auth_test.exs
      provides: Dense claim, signature, replay, and audience matrix coverage
    - path: test/lockspire/jwks_fetcher_test.exs
      provides: Fetch safety, redirect rejection, cache TTL, forced refresh, and preserve-last-known-good behavior
  key_links:
    - from: test/integration/phase62_private_key_jwt_e2e_test.exs
      to: test/lockspire/protocol/client_auth_test.exs
      via: Boundary proof backed by shared verifier matrix
    - from: test/integration/phase62_private_key_jwt_e2e_test.exs
      to: test/lockspire/jwks_fetcher_test.exs
      via: Rotation and remote key retrieval semantics
---

<objective>
Add repo-owned verification for the shipped `private_key_jwt` surface using a small set of high-signal HTTP proofs and deeper lower-level matrix coverage where the shared auth logic lives.

Purpose: Prove positive-path auth, negative-path rejection, and `jwks_uri` rotation recovery without creating an endpoint-by-endpoint E2E matrix.
Output: One representative Phase 62 integration test plus targeted expansions to shared verifier and fetcher tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62-docs-verification-closure/62-CONTEXT.md
@.planning/REQUIREMENTS.md
@test/lockspire/protocol/client_auth_test.exs
@test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs
@test/lockspire/jwks_fetcher_test.exs
@test/lockspire/jwks_fetcher/target_safety_test.exs
@test/integration/phase29_dcr_e2e_test.exs
@test/integration/phase37_protocol_strictness_e2e_test.exs
@test/integration/phase57_rar_introspection_verification_e2e_test.exs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add one representative Phase 62 HTTP proof</name>
  <files>test/integration/phase62_private_key_jwt_e2e_test.exs</files>
  <action>Create a focused integration test anchored on a Lockspire-owned direct-client boundary, preferably `/token`. Include one positive inline `jwks` `private_key_jwt` request, one positive `jwks_uri` request that proves remote fetch plus bounded rotation refresh behavior, and one negative request that returns only the generic `invalid_client` wire contract. Reuse existing test helpers and avoid browser-driven or host-login-heavy expansion.</action>
  <verify>
    <automated>mix test test/integration/phase62_private_key_jwt_e2e_test.exs</automated>
  </verify>
  <done>The repo has a realistic boundary proof for the shipped client-auth slice without E2E sprawl.</done>
</task>

<task type="auto">
  <name>Task 2: Fill any shared verifier matrix gaps</name>
  <files>test/lockspire/protocol/client_auth_test.exs, test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs</files>
  <action>Audit the existing lower-level tests against `V-01` and `V-02`. Add or tighten cases for signature failures, `alg=none`, unsupported or symmetric algorithms, issuer/sub mismatch, wrong issuer-string `aud`, expiration/skew semantics, replay ordering, replay-store failure, and representative cross-surface consistency where the logic already lives. Keep this matrix in shared tests rather than duplicating full HTTP coverage per endpoint.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/client_auth_test.exs test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs</automated>
  </verify>
  <done>Shared verifier tests prove the negative-path and cross-surface behavior the docs claim.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten remote JWKS fetch and rotation proof</name>
  <files>test/lockspire/jwks_fetcher_test.exs, test/lockspire/jwks_fetcher/target_safety_test.exs</files>
  <action>Ensure the fetcher tests explicitly cover the guarded remote-key contract the new docs describe: redirects disabled and rejected, unsafe target rejection, bounded TTL and cache refresh behavior, key rotation recovery via one forced refresh path, and preserve-last-known-good semantics when refresh fails. Add only the missing assertions needed to make the doc claims executable.</action>
  <verify>
    <automated>mix test test/lockspire/jwks_fetcher_test.exs test/lockspire/jwks_fetcher/target_safety_test.exs</automated>
  </verify>
  <done>Remote JWKS redirect rejection, target safety, and rotation semantics are all repo-proven and aligned with the docs.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| OAuth client -> Lockspire endpoints | Untrusted direct-client requests carrying assertions |
| Lockspire -> Remote `jwks_uri` | Narrow outbound fetch boundary for client key retrieval |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-02-01 | Spoofing | `private_key_jwt` assertion auth | mitigate | Prove cryptographic validation and issuer-bound claim enforcement |
| T-62-02-02 | Information Disclosure | HTTP error responses | mitigate | Assert only generic `invalid_client` behavior at the wire boundary |
| T-62-02-03 | Denial of Service | Remote JWKS fetch path | mitigate | Prove unsafe-target rejection, bounded fetches, and cache-preserving refresh behavior |
</threat_model>

<verification>
mix test test/integration/phase62_private_key_jwt_e2e_test.exs
mix test test/lockspire/protocol/client_auth_test.exs test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs
mix test test/lockspire/jwks_fetcher_test.exs test/lockspire/jwks_fetcher/target_safety_test.exs
</verification>

<success_criteria>
- One representative HTTP proof demonstrates inline `jwks`, `jwks_uri`, and generic rejection behavior.
- Shared lower-level tests prove the verifier matrix and direct-client surface consistency claimed by docs.
- JWKS fetcher tests prove redirect rejection, guarded fetch, and rotation recovery semantics described by the shipped slice.
</success_criteria>

<output>
After completion, create `.planning/phases/62-docs-verification-closure/62-02-SUMMARY.md`
</output>
