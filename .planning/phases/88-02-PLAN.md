---
phase: 88
plan: 2
title: Add Sealed Verifier Material And Enforce Narrow `client_secret_jwt` Posture
type: execute
wave: 2
depends_on:
  - "88-01"
files_modified:
  - lib/lockspire/domain/client.ex
  - lib/lockspire/storage/ecto/client_record.ex
  - lib/lockspire/clients.ex
  - lib/lockspire/admin/clients.ex
  - lib/lockspire/protocol/registration.ex
  - lib/lockspire/protocol/registration_management.ex
  - lib/lockspire/storage/ecto/repository.ex
  - lib/lockspire/security/policy.ex
  - lib/lockspire/protocol/client_auth/client_secret_jwt.ex
  - lib/lockspire/protocol/security_profile.ex
  - priv/repo/migrations/*_add_client_secret_jwt_verifier_material_to_lockspire_clients.exs
  - test/lockspire/protocol/client_auth_test.exs
  - test/lockspire/audit/event_test.exs
  - test/lockspire/storage/ecto/client_record_test.exs
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
user_setup: []
must_haves:
  truths:
    - "Valid client_secret_jwt assertions can be verified without storing raw client secrets at rest."
    - "client_secret_jwt accepts HS256 only and is denied under FAPI-effective profiles."
    - "Replay, audience, timing, and redaction behavior matches the existing JWT auth contract."
  artifacts:
    - path: "lib/lockspire/protocol/client_auth/client_secret_jwt.ex"
      provides: "Strict symmetric JWT verifier"
      contains: "HS256"
    - path: "lib/lockspire/security/policy.ex"
      provides: "Seal and unseal helpers for verifier material"
      contains: "client_secret_jwt"
    - path: "lib/lockspire/storage/ecto/client_record.ex"
      provides: "Persisted verifier-material field"
      contains: "client_secret_jwt"
    - path: "priv/repo/migrations/*_add_client_secret_jwt_verifier_material_to_lockspire_clients.exs"
      provides: "Schema support for persisted verifier material"
  key_links:
    - from: "lib/lockspire/protocol/registration.ex"
      to: "lib/lockspire/security/policy.ex"
      via: "secret issuance persists sealed verifier material"
    - from: "lib/lockspire/admin/clients.ex"
      to: "lib/lockspire/storage/ecto/repository.ex"
      via: "secret rotation refreshes sealed verifier material"
    - from: "lib/lockspire/protocol/registration_management.ex"
      to: "lib/lockspire/storage/ecto/repository.ex"
      via: "self-service secret rotation refreshes sealed verifier material"
    - from: "lib/lockspire/protocol/client_auth/client_secret_jwt.ex"
      to: "lib/lockspire/security/policy.ex"
      via: "HS256 verifier material unwrap"
      pattern: "unseal"
---

<objective>
Make the `client_secret_jwt` success path concretely executable by persisting sealed verifier material alongside the existing secret hash, then enforce the narrow HS256-only and non-FAPI runtime posture.

Purpose: AUTH-01 cannot be achieved with hash-only storage, so this plan adds the minimum internal secret-material lifecycle needed for symmetric verification without exposing raw secrets at rest or widening public support truth.
Output: sealed verifier-material persistence on secret issuance/rotation paths, a strict symmetric verifier, and unit/audit proof for success and negative paths.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/88-shared-client-secret-jwt-runtime/88-CONTEXT.md
@.planning/phases/88-shared-client-secret-jwt-runtime/88-RESEARCH.md
@lib/lockspire/domain/client.ex
@lib/lockspire/storage/ecto/client_record.ex
@lib/lockspire/clients.ex
@lib/lockspire/admin/clients.ex
@lib/lockspire/protocol/registration.ex
@lib/lockspire/protocol/registration_management.ex
@lib/lockspire/storage/ecto/repository.ex
@lib/lockspire/security/policy.ex
@lib/lockspire/protocol/client_auth/private_key_jwt.ex
@lib/lockspire/protocol/security_profile.ex
@test/lockspire/protocol/client_auth_test.exs
@test/lockspire/storage/ecto/client_record_test.exs
@test/lockspire/audit/event_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist sealed verifier material on secret issuance and rotation paths</name>
  <files>lib/lockspire/domain/client.ex, lib/lockspire/storage/ecto/client_record.ex, lib/lockspire/clients.ex, lib/lockspire/admin/clients.ex, lib/lockspire/protocol/registration.ex, lib/lockspire/protocol/registration_management.ex, lib/lockspire/storage/ecto/repository.ex, lib/lockspire/security/policy.ex, priv/repo/migrations/*_add_client_secret_jwt_verifier_material_to_lockspire_clients.exs</files>
  <read_first>lib/lockspire/clients.ex, lib/lockspire/protocol/registration.ex, lib/lockspire/protocol/registration_management.ex, lib/lockspire/storage/ecto/repository.ex</read_first>
  <action>Add a dedicated internal field for sealed `client_secret_jwt` verifier material and the migration needed to persist it. Populate that field whenever Lockspire generates or rotates a confidential client secret, including operator rotation and self-service management rotation, so future `client_secret_jwt` clients have verifier material without changing public response shape. Reuse the existing one-time secret issuance paths so raw secrets remain transient, persist the existing hash unchanged for password-style auth, and store only encrypted or sealed verifier material for HMAC use.</action>
  <verify>mix test test/lockspire/storage/ecto/client_record_test.exs test/lockspire/admin/clients_test.exs test/lockspire/protocol/registration_test.exs test/lockspire/protocol/registration_management_test.exs</verify>
  <acceptance_criteria>
    - New or rotated confidential secrets persist both `client_secret_hash` and sealed `client_secret_jwt` verifier material.
    - The new field round-trips through Ecto persistence and both operator and self-service rotation paths refresh it.
    - No public serializer or audit payload exposes the sealed verifier-material field.
  </acceptance_criteria>
  <done>The repo has an internal verifier-material source for HS256 that preserves the existing hashed-at-rest secret posture.</done>
</task>

<task type="auto">
  <name>Task 2: Implement strict HS256-only verifier behavior with explicit FAPI denial</name>
  <files>lib/lockspire/protocol/client_auth/client_secret_jwt.ex, lib/lockspire/security/policy.ex, lib/lockspire/protocol/security_profile.ex, test/lockspire/protocol/client_auth_test.exs</files>
  <read_first>lib/lockspire/protocol/client_auth/private_key_jwt.ex, .planning/phases/88-shared-client-secret-jwt-runtime/88-PATTERNS.md</read_first>
  <action>Implement `ClientSecretJwt.verify/3` to unseal verifier material, verify `HS256` signatures only, require `iss` and `sub` equal to `client_id`, require issuer-string `aud`, enforce timing and bounded lifetime, and record replay only after verified claims. Deny `client_secret_jwt` outright for FAPI-effective profiles instead of treating symmetric algorithms as part of the existing general allowlist.</action>
  <verify>mix test test/lockspire/protocol/client_auth_test.exs</verify>
  <acceptance_criteria>
    - Valid `HS256` assertions for eligible clients succeed.
    - `RS256`, `HS384`, `HS512`, `alg=none`, and FAPI-effective profile attempts fail closed.
  </acceptance_criteria>
  <done>The symmetric verifier has a concrete success path and strict failure posture aligned with Phase 88 constraints.</done>
</task>

<task type="auto">
  <name>Task 3: Extend audit and negative-path proof without leaking secret-derived material</name>
  <files>test/lockspire/protocol/client_auth_test.exs, test/lockspire/audit/event_test.exs</files>
  <read_first>test/lockspire/audit/event_test.exs, lib/lockspire/redaction.ex</read_first>
  <action>Add unit and audit proof for audience mismatch, lifetime violations, replay, algorithm denial, and FAPI denial. Assert that telemetry and audit metadata include only stable safe fields such as `client_id`, `auth_method`, and `reason_code`, and never retain raw assertion, decoded claim, sealed verifier material, or secret-derived bytes.</action>
  <verify>mix test test/lockspire/protocol/client_auth_test.exs test/lockspire/audit/event_test.exs</verify>
  <acceptance_criteria>
    - Negative-path reason codes are stable and map to generic `invalid_client`.
    - Audit normalization strips any symmetric verifier-material details.
  </acceptance_criteria>
  <done>Negative-path and redaction proof covers the new verifier without weakening the repo’s observability posture.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `mix test test/lockspire/storage/ecto/client_record_test.exs test/lockspire/admin/clients_test.exs test/lockspire/protocol/registration_test.exs test/lockspire/protocol/registration_management_test.exs`
- [ ] `mix test test/lockspire/protocol/client_auth_test.exs test/lockspire/audit/event_test.exs`
- [ ] Valid `client_secret_jwt` success is achievable without introducing raw-secret-at-rest persistence.
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- AUTH-01 has a concrete verifier-material path that preserves current secret-handling truth.
- HS256-only and non-FAPI runtime posture is enforced in code and proof.

</success_criteria>

<output>
After completion, create `.planning/phases/88-02-SUMMARY.md`
</output>
