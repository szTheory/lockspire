---
phase: 99-signer-extraction-jwt-default-issuance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/discovery.ex
autonomous: true
requirements: [DISCOVERY-01]
must_haves:
  truths:
    - "GET /.well-known/openid-configuration returns access_token_signing_alg_values_supported == [\"RS256\",\"ES256\",\"PS256\"]"
    - "The key is published unconditionally (present even when the response would otherwise vary), matching id_token_signing_alg_values_supported's static placement"
  artifacts:
    - path: lib/lockspire/protocol/discovery.ex
      provides: "access_token_signing_alg_values_supported literal in openid_configuration/0 static block"
      contains: "access_token_signing_alg_values_supported"
  key_links:
    - from: lib/lockspire/protocol/discovery.ex
      to: "openid_configuration/0 static map"
      via: "literal [\"RS256\",\"ES256\",\"PS256\"] alongside id_token_signing_alg_values_supported"
      pattern: "access_token_signing_alg_values_supported.*RS256"
---

<objective>
Advertise `access_token_signing_alg_values_supported: ["RS256", "ES256", "PS256"]` in the OpenID Connect discovery document as a static, unconditionally-published literal — truthful because after Phase 99 every grant path can mint `at+jwt`.

Purpose: DISCOVERY-01 (ROADMAP success criterion #4). This plan is independent of the signer and storage work — the key is a fixed literal — so it runs in Wave 1 in parallel with the foundation.

Output: One added key in `discovery.ex`'s `openid_configuration/0` static map, plus a discovery test assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-signer-extraction-jwt-default-issuance/99-CONTEXT.md
@.planning/phases/99-signer-extraction-jwt-default-issuance/99-RESEARCH.md
@.planning/phases/99-signer-extraction-jwt-default-issuance/99-PATTERNS.md

<interfaces>
<!-- The exact sibling slot and the anti-pattern to avoid. Verified at the cited lines. -->

Static map slot — lib/lockspire/protocol/discovery.ex:78-95:
  def openid_configuration do ... %{
    "issuer" => issuer, ...
    "id_token_signing_alg_values_supported" => id_token_signing_alg_values_supported()  # :95, UNCONDITIONAL static key
  }

DO NOT reuse — lib/lockspire/protocol/security_profile.ex:62-64:
  allowed_signing_algorithms/1 returns ["RS256","ES256","PS256","EdDSA"] for :none and
  ["ES256","PS256"] under FAPI — neither matches the required truthful triple (Pitfall 4).

Existing discovery test shape — test/lockspire/protocol/discovery_test.exs:488-510:
  asserts config["id_token_signing_alg_values_supported"] equals an explicit list.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish access_token_signing_alg_values_supported as a static literal</name>
  <files>lib/lockspire/protocol/discovery.ex</files>
  <read_first>
    - lib/lockspire/protocol/discovery.ex (the openid_configuration/0 static map at :78-95; the id_token_signing_alg_values_supported sibling at :95)
    - 99-RESEARCH.md "Pitfall 4: DISCOVERY-01 gating does not match the actual sibling" (publish unconditionally as a static key; do NOT gate on token_endpoint)
    - test/lockspire/protocol/discovery_test.exs:488-510 (the alg-list assertion shape to mirror)
  </read_first>
  <behavior>
    - openid_configuration/0 returns a map whose "access_token_signing_alg_values_supported" key equals exactly ["RS256", "ES256", "PS256"] (order significant).
    - The key is present unconditionally (not gated on token_endpoint mounting), alongside the always-present "id_token_signing_alg_values_supported".
  </behavior>
  <action>In `lib/lockspire/protocol/discovery.ex`, add the literal key `"access_token_signing_alg_values_supported" => ["RS256", "ES256", "PS256"]` to the static map returned by `openid_configuration/0`, immediately alongside `"id_token_signing_alg_values_supported"` (:95). Use a bare literal list — do NOT derive it from `SecurityProfile.allowed_signing_algorithms/1` (which returns EdDSA for `:none` and only ["ES256","PS256"] under FAPI, per D-11 and Pitfall 4). Do NOT add any `token_endpoint`-mounted gate: the true sibling `id_token_signing_alg_values_supported` is unconditional, so this key is too (Pitfall 4 corrects D-11's "gated" phrasing).</action>
  <verify>
    <automated>mix test test/lockspire/protocol/discovery_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - A test asserting `Lockspire.Protocol.Discovery.openid_configuration()["access_token_signing_alg_values_supported"] == ["RS256", "ES256", "PS256"]` passes.
    - `grep -n "access_token_signing_alg_values_supported" lib/lockspire/protocol/discovery.ex` shows exactly one literal occurrence and NO call to `SecurityProfile.allowed_signing_algorithms`.
    - The key is present in `openid_configuration/0`'s output regardless of any conditional endpoint-mounting branch (assert presence unconditionally).
    - `mix test test/lockspire/protocol/discovery_test.exs` is green (existing id_token alg-list assertions stay green).
  </acceptance_criteria>
  <done>Discovery advertises the fixed truthful triple unconditionally; discovery tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lockspire server → public discovery client | The discovery document is unauthenticated and read by any adopter; published values must be truthful, not aspirational. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-04 | Information Disclosure (untruthful metadata) | `access_token_signing_alg_values_supported` advertised but unmintable | mitigate | Publishing is truthful only because Plans 03/04/05 make every grant path able to mint `at+jwt` by default; this plan is gated by the phase as a whole. The literal triple matches the algs the active signing key actually uses (RS256/ES256/PS256) — never `none`/`EdDSA` (avoids the `SecurityProfile.allowed_signing_algorithms` superset). |
| T-99-05 | Spoofing (alg confusion downstream) | Advertising `alg=none`-adjacent values | mitigate | The literal list deliberately excludes `none` and `EdDSA`; no client-controlled alg is implied. |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | No packages added in Phase 99. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/discovery_test.exs` green, including a new assertion for the literal triple.
- `grep` confirms no reuse of `SecurityProfile.allowed_signing_algorithms` for this key.
</verification>

<success_criteria>
- DISCOVERY-01: `/.well-known/openid-configuration` advertises `access_token_signing_alg_values_supported: ["RS256","ES256","PS256"]`, published unconditionally.
</success_criteria>

<output>
Create `.planning/phases/99-signer-extraction-jwt-default-issuance/99-02-SUMMARY.md` when done.
</output>
