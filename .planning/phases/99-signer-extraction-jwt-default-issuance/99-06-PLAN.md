---
phase: 99-signer-extraction-jwt-default-issuance
plan: 06
type: execute
wave: 2
depends_on: ["99-01"]
files_modified:
  - lib/lockspire/admin/clients.ex
  - lib/lockspire/web/live/admin/clients_live/form_component.ex
  - lib/lockspire/web/live/admin/clients_live/show.ex
  - test/lockspire/web/live/admin/clients_live/show_test.exs
autonomous: true
requirements: [FORMAT-02]
must_haves:
  truths:
    - "An operator on the admin client-detail edit form can select an access_token_format override (inherit | jwt | opaque) and saving it persists nil/:jwt/:opaque"
    - "The inherit option casts to nil; no :inherit sentinel is stored"
    - "The client-detail SHOW page renders global / override / effective access-token-format rows, with a nil override displayed as inherit"
    - "The effective value resolves per-client override -> server default -> :jwt (same precedence as the signer)"
  artifacts:
    - path: lib/lockspire/admin/clients.ex
      provides: "access_token_format in @mutable_fields + normalize_mutable_field clause (inherit/nil -> nil)"
      contains: "access_token_format"
    - path: lib/lockspire/web/live/admin/clients_live/form_component.ex
      provides: "inherit|jwt|opaque override <select> + doclink + nil-aware defaults_for"
      contains: "client[access_token_format]"
    - path: lib/lockspire/web/live/admin/clients_live/show.ex
      provides: "global/override/effective display rows"
      contains: "access_token_format"
  key_links:
    - from: lib/lockspire/web/live/admin/clients_live/form_component.ex
      to: lib/lockspire/admin/clients.ex
      via: "client[access_token_format] param normalized by normalize_mutable_field/2 then persisted via update_changeset/2"
      pattern: "access_token_format"
    - from: lib/lockspire/web/live/admin/clients_live/show.ex
      to: "ServerPolicy.access_token_format"
      via: "effective resolution reads server default for the effective row"
      pattern: "access_token_format"
---

<objective>
Add the per-client `access_token_format` override surface to the admin client-detail screen: an `inherit | jwt | opaque` `<select>` on the edit form with a tradeoff doclink, the `normalize_mutable_field/2` plumbing that maps `inherit`→`nil`, and global / override / effective display rows on the SHOW page — all mirroring the established `dpop_policy` / `security_profile` idioms per the approved UI-SPEC.

Purpose: FORMAT-02 (the only net-new frontend surface in the phase). The UI-SPEC is the binding contract for copy, option values, placement, and the Anti-Drift Checklist; the per-client storage was landed in Plan 01.

Output: edits to `admin/clients.ex`, `form_component.ex`, `show.ex`, and the show LiveView test. No new pages, routes, CSS classes, telemetry, or doctor task (those are Phase 102).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-signer-extraction-jwt-default-issuance/99-CONTEXT.md
@.planning/phases/99-signer-extraction-jwt-default-issuance/99-PATTERNS.md
@.planning/phases/99-signer-extraction-jwt-default-issuance/99-UI-SPEC.md

<interfaces>
<!-- The exact admin idioms to mirror, and the UI-SPEC locked constraints. Verified at the cited lines. -->

Mutable field + normalizer dispatch — lib/lockspire/admin/clients.ex:
  :14-33 @mutable_fields; :472-484 normalize_mutable_field/2 dispatch; :484-489 :dpop_policy clause;
  normalizer shapes to HYBRID: normalize_dpop_policy/1 (:575-590, the inherit option) +
  normalize_authorization_signing_alg/1 (:611-629, the ""/nil -> {:ok, nil} cast).

Override <select> + defaults_for — lib/lockspire/web/live/admin/clients_live/form_component.ex:
  :95-106 dpop_policy <select> (mirror; :if={@mode == :edit}; name="client[dpop_policy]");
  :406-416 defaults_for(:edit, client); :427-429 the nil-guard idiom for authorization_signed_response_alg.

SHOW display trio — lib/lockspire/web/live/admin/clients_live/show.ex:
  :169-171 security-profile global/override/effective rows (mirror exactly: <code> for global/override, <strong> for effective);
  :660-661 value_or_not_configured/1 nil-guard idiom (adapt: nil -> "inherit", not "Not configured");
  DO NOT borrow the :194-203 lockspire-admin-warning block (access_token_format is a reversible, non-destructive override — UI-SPEC Color).

Server default source for the effective row — ServerPolicy.access_token_format (default :jwt), via Repository.get_server_policy().

LOCKED by UI-SPEC: option values inherit/jwt/opaque (this order), inherit->nil cast, doclink target docs/protect-phoenix-api-routes.md.
Copy (Claude's discretion, supplied by UI-SPEC Copywriting Contract): label "Access token format override"; options
  "Inherit from server default" / "JWT (RFC 9068 at+jwt)" / "Opaque (Lockspire-stored)"; help text + anchor "Learn when to choose JWT vs opaque".
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: admin/clients.ex — @mutable_fields entry + normalize_mutable_field clause (inherit -> nil)</name>
  <files>lib/lockspire/admin/clients.ex</files>
  <read_first>
    - lib/lockspire/admin/clients.ex:14-33 (@mutable_fields), :472-489 (normalize_mutable_field dispatch + :dpop_policy clause), :575-590 (normalize_dpop_policy), :611-629 (normalize_authorization_signing_alg nil-cast)
    - 99-UI-SPEC.md "Surface 1b" + "Executor Anti-Drift Checklist" (no :inherit sentinel; nullable)
    - test/lockspire/admin/clients_test.exs (extend — override normalize + persist round-trip)
  </read_first>
  <behavior>
    - normalize_mutable_field(:access_token_format, "inherit") -> nil; (nil) -> nil; ("") -> nil.
    - normalize_mutable_field(:access_token_format, "jwt") -> :jwt; ("opaque") -> :opaque.
    - An unknown value returns the unchanged input (matching the dispatch's existing :error -> value fallback shape).
    - Updating a client through the Admin.Clients update path with %{"access_token_format" => "opaque"} persists :opaque and reading the client back returns :opaque; "inherit" persists nil.
  </behavior>
  <action>In `lib/lockspire/admin/clients.ex` add `access_token_format` to `@mutable_fields` (:14-33). Add a `normalize_mutable_field(:access_token_format, value)` clause in the :472-489 region mirroring the `:dpop_policy` dispatch (:484-489), backed by a new private `normalize_access_token_format/1` that is a HYBRID: `"inherit"`/`nil`/`""` → `{:ok, nil}` (like `normalize_authorization_signing_alg/1` at :611-629), `:jwt`/`"jwt"` → `{:ok, :jwt}`, `:opaque`/`"opaque"` → `{:ok, :opaque}` (like `normalize_dpop_policy/1` at :575-590), anything else → `:error`. Do NOT store an `:inherit` sentinel — `nil` is the inherit state (D-06). The persisted write rides the existing `update_changeset/2` path wired in Plan 01.</action>
  <verify>
    <automated>mix test test/lockspire/admin/clients_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "access_token_format" lib/lockspire/admin/clients.ex` shows it in `@mutable_fields` and in a `normalize_mutable_field` clause.
    - A test asserts: `"inherit"`/`nil` → `nil`, `"jwt"` → `:jwt`, `"opaque"` → `:opaque`; unknown → unchanged input (no crash).
    - An update round-trip through `Admin.Clients` with `%{"access_token_format" => "opaque"}` persists `:opaque`; with `"inherit"` persists `nil`.
    - No `:inherit` atom is stored anywhere (the persisted value is `nil` for inherit).
    - `mix test test/lockspire/admin/clients_test.exs` is green.
  </acceptance_criteria>
  <done>Override is a mutable field; normalize maps inherit->nil and jwt/opaque->atoms; round-trip persists; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: form_component.ex — override <select> + doclink + nil-aware defaults_for</name>
  <files>lib/lockspire/web/live/admin/clients_live/form_component.ex</files>
  <read_first>
    - lib/lockspire/web/live/admin/clients_live/form_component.ex:95-106 (dpop_policy <select>), :406-416 (defaults_for(:edit)), :427-429 (nil-guard helper idiom)
    - 99-UI-SPEC.md "Surface 1", "Surface 2", "Copywriting Contract", "Executor Anti-Drift Checklist"
  </read_first>
  <behavior>
    - In :edit mode the form shows a <select name="client[access_token_format]"> with exactly three options inherit/jwt/opaque in that order, placed directly after the dpop_policy select.
    - The currently-stored value pre-selects: stored nil -> "inherit" option selected; :jwt -> "jwt"; :opaque -> "opaque".
    - A doclink <a href="docs/protect-phoenix-api-routes.md"> appears in a lockspire-admin-help block beside the select.
  </behavior>
  <action>In `lib/lockspire/web/live/admin/clients_live/form_component.ex`, add the override `<select>` immediately after the `dpop_policy` select (:106), edit-mode gated (`:if={@mode == :edit}`), form param `client[access_token_format]`, with exactly three `<option>`s: `inherit` ("Inherit from server default"), `jwt` ("JWT (RFC 9068 at+jwt)"), `opaque` ("Opaque (Lockspire-stored)"), using the `selected={@defaults.access_token_format == "<value>"}` pattern. Add the help text + doclink inside a `lockspire-admin-help` block: a plain `<a href="docs/protect-phoenix-api-routes.md">Learn when to choose JWT vs opaque</a>` (locked target; copy per UI-SPEC). Extend `defaults_for(:edit, client)` (:406-416) with `access_token_format: format_default_for_select(client.access_token_format)` where a new nil-aware helper maps `nil → "inherit"`, `:jwt → "jwt"`, `:opaque → "opaque"` (model the nil-guard on :427-429 — UNLIKE `dpop_policy`, which stores a real atom and uses plain `Atom.to_string/1`). Do NOT introduce a new CSS class, spacing/typography/color token, warning styling, or confirmation dialog (UI-SPEC Anti-Drift Checklist).</action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/clients_live/show_test.exs && mix compile --warnings-as-errors 2>&1 | grep -qiE "warning|error" && echo CHECK-WARNINGS || true</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'client\[access_token_format\]' lib/lockspire/web/live/admin/clients_live/form_component.ex` shows the form param name.
    - `grep -cE 'value="(inherit|jwt|opaque)"' lib/lockspire/web/live/admin/clients_live/form_component.ex` returns 3 (exactly three options).
    - `grep -n "docs/protect-phoenix-api-routes.md" lib/lockspire/web/live/admin/clients_live/form_component.ex` shows the locked doclink target.
    - The `defaults_for(:edit, ...)` map includes `access_token_format` mapped through a nil-aware helper that yields `"inherit"` for `nil`.
    - The control is edit-mode gated (`:if={@mode == :edit}`) and sits directly after the `dpop_policy` select.
    - `mix compile` succeeds with no new warnings; the LiveView test suite compiles and renders the form.
  </acceptance_criteria>
  <done>Edit form shows the inherit/jwt/opaque override select + doclink; stored value pre-selects correctly; no drift; compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: show.ex — global / override / effective display rows</name>
  <files>lib/lockspire/web/live/admin/clients_live/show.ex, test/lockspire/web/live/admin/clients_live/show_test.exs</files>
  <read_first>
    - lib/lockspire/web/live/admin/clients_live/show.ex:169-171 (security-profile global/override/effective trio), :660-661 (value_or_not_configured nil-guard idiom), :194-203 (the warning block to NOT borrow)
    - 99-UI-SPEC.md "Surface 3" + "Copywriting Contract" + "Nil-rendering note"
    - test/lockspire/web/live/admin/clients_live/show_test.exs (extend — assert the three rows render)
  </read_first>
  <behavior>
    - The client-detail SHOW page renders three rows: "Global access token format:" (<code>, server default), "Client access token override:" (<code>, nil shown as inherit), "Effective access token format:" (<strong>, resolved).
    - For a client with nil override and a server default :jwt, the override row shows "inherit" and the effective row shows "jwt".
    - For a client with :opaque override, the effective row shows "opaque" regardless of server default.
  </behavior>
  <action>In `lib/lockspire/web/live/admin/clients_live/show.ex`, add three `<p>` rows alongside the security-profile trio (:169-171): a global row showing `ServerPolicy.access_token_format` in `<code>`, an override row showing `@client.access_token_format` in `<code>` (render `nil` as the word `inherit` via a helper modeled on `value_or_not_configured/1` at :660-661 but emitting `"inherit"` not `"Not configured"`), and an effective row in `<strong>` showing the resolved value. Compute the effective value with the SAME per-client → server-default → `:jwt` precedence as the signer (assign it in `mount`/`handle_params`, reading the server default via `Repository.get_server_policy()`). Do NOT borrow the `lockspire-admin-warning` block (:194-203) — this override is reversible and non-destructive (UI-SPEC Color). Use the existing copy from the UI-SPEC Copywriting Contract for the three row labels.</action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/clients_live/show_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The SHOW LiveView test renders and asserts the presence of the three labels "Global access token format", "Client access token override", "Effective access token format".
    - A test with a `nil`-override client + server default `:jwt` asserts the override row text contains `inherit` and the effective row contains `jwt`.
    - A test with an `:opaque`-override client asserts the effective row contains `opaque`.
    - `grep -cn "lockspire-admin-warning" lib/lockspire/web/live/admin/clients_live/show.ex` shows the warning class was NOT added for this surface (count unchanged from the existing security-profile usage; no new occurrence near the access-token rows).
    - `mix test test/lockspire/web/live/admin/clients_live/show_test.exs` is green.
  </acceptance_criteria>
  <done>SHOW page shows global/override/effective rows with nil-as-inherit and signer-aligned effective resolution; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator (admin form) → client override | The form param `client[access_token_format]` crosses into the persisted record; it is normalized to a known enum (or nil) before persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-19 | Tampering | A forged form value (e.g. `client[access_token_format]=admin`) reaching the record | mitigate | `normalize_mutable_field/2` maps only `inherit`/`jwt`/`opaque`/`nil`/`""`; unknown values are not coerced to a valid enum, and `update_changeset/2`'s Ecto.Enum rejects out-of-set values. |
| T-99-20 | Information Disclosure | Misleading effective-format display causing an operator to believe a client is JWT when it is opaque (or vice versa) | mitigate | The effective row uses the SAME per-client → server-default → `:jwt` precedence as the signer, so the displayed effective value matches what is actually minted. Tests assert override + effective coherence. |
| T-99-21 | Tampering (UI drift) | Storing an `:inherit` sentinel atom instead of `nil`, desyncing display from storage | mitigate | UI-SPEC Anti-Drift Checklist enforced: `inherit`→`nil`, no sentinel; grep/test confirm `nil` is stored and rendered as `inherit`. |
| T-99-SC | Tampering | npm/pip/cargo installs | accept | No packages added in Phase 99 (server-rendered LiveView; no component registry — UI-SPEC Registry Safety not applicable). |
</threat_model>

<verification>
- `mix test test/lockspire/admin/clients_test.exs test/lockspire/web/live/admin/clients_live/show_test.exs` green.
- `mix compile` clean (no new warnings).
- UI-SPEC Anti-Drift Checklist satisfied: three options inherit/jwt/opaque, `client[access_token_format]` param, inherit→nil, doclink target, three SHOW rows, no new CSS/warning/dialog.
</verification>

<success_criteria>
- FORMAT-02: operators can read and change a per-client `access_token_format` override (`:jwt | :opaque | nil`) on the admin client-detail screen, with a doclink to the tradeoff; the effective value resolves with the same precedence as the signer.
</success_criteria>

<output>
Create `.planning/phases/99-signer-extraction-jwt-default-issuance/99-06-SUMMARY.md` when done.
</output>
