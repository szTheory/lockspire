---
phase: 50-delegation-and-act-claims
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - lib/lockspire/host/default_delegation_validator.ex
  - lib/lockspire/protocol/token_exchange/delegation.ex
  - test/lockspire/host/default_delegation_validator_test.exs
  - test/lockspire/protocol/token_exchange/delegation_test.exs
autonomous: true
requirements: [TE-04]
must_haves:
  truths:
    - "A default validator automatically maps standard claims to the `act` object for out-of-the-box RFC 8693 compliance."
    - "Token exchange requests are rejected if the resulting nested `act` claims would exceed the configured depth limit."
  artifacts:
    - path: "lib/lockspire/host/default_delegation_validator.ex"
      provides: "Default mapping logic for act claims"
    - path: "lib/lockspire/protocol/token_exchange/delegation.ex"
      provides: "Depth resolution and constraint checking logic"
  key_links:
    - from: "lib/lockspire/protocol/token_exchange/delegation.ex"
      to: "lib/lockspire/host/default_delegation_validator.ex"
      via: "Delegation extraction flow"
---

<objective>
Implement RFC 8693 delegation logic by extracting actor claims into the `act` claim and enforcing `max_delegation_depth` limits during Token Exchange.

Purpose: Generate standards-compliant delegated tokens and strictly prevent nested token bloat DOS vectors at runtime.
Output: A `DefaultDelegationValidator` and delegation depth checking logic integrated into the token exchange flow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/lockspire/host/token_exchange_validator.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Default Delegation Validator</name>
  <files>lib/lockspire/host/default_delegation_validator.ex, test/lockspire/host/default_delegation_validator_test.exs</files>
  <behavior>
    - Extracts `sub` and `client_id` from the `actor_token` claims.
    - Formats them into an `act` claim object mapping.
    - Implements `Lockspire.Host.TokenExchangeValidator` behaviour.
  </behavior>
  <action>Create `Lockspire.Host.DefaultDelegationValidator` that implements `Lockspire.Host.TokenExchangeValidator`. In the `validate/1` callback, if an `actor_token` context is present, extract its `sub` and `client_id` claims and return `{:ok, %{claims: %{act: %{sub: sub, client_id: client_id}}}}`. If an `act` claim already exists in the `actor_token`, nest it properly per RFC 8693. Write corresponding tests.</action>
  <verify>
    <automated>mix test test/lockspire/host/default_delegation_validator_test.exs</automated>
  </verify>
  <done>The validator correctly structures the `act` claim for nested and non-nested delegations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Delegation Depth Enforcement</name>
  <files>lib/lockspire/protocol/token_exchange/delegation.ex, test/lockspire/protocol/token_exchange/delegation_test.exs</files>
  <behavior>
    - Resolves the max depth from Client config or falls back to ServerPolicy default.
    - Counts the layers of `act` claims in the `actor_token`.
    - Returns an error if `current_depth + 1 > resolved_depth`.
  </behavior>
  <action>Create a module `Lockspire.Protocol.TokenExchange.Delegation` to hold the depth checking logic. Implement `check_depth(actor_token_claims, client, server_policy)` which resolves max depth (`client.max_delegation_depth || policy.max_delegation_depth || 3`), recursively counts the `act` objects in `actor_token_claims`, and returns `:ok` or `{:error, "invalid_request", "max_delegation_depth_exceeded"}` if depth would be exceeded. Write tests validating the depth calculation.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/token_exchange/delegation_test.exs</automated>
  </verify>
  <done>Logic correctly calculates depth and rejects if limits are breached.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Token Exchange API | Incoming `actor_token` JWTs can be deeply nested malicious payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-02 | Denial of Service | Delegation Depth Check | mitigate | Enforce `max_delegation_depth` by counting `act` layers before minting new tokens. |
</threat_model>

<verification>
mix test test/lockspire/host/default_delegation_validator_test.exs test/lockspire/protocol/token_exchange/delegation_test.exs
</verification>

<success_criteria>
`DefaultDelegationValidator` accurately produces RFC 8693 nested `act` claims, and `Delegation` module correctly prevents excessive depths.
</success_criteria>

<output>
After completion, create `.planning/phases/50-delegation-and-act-claims/50-02-SUMMARY.md`
</output>
