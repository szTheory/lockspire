---
phase: 50-delegation-and-act-claims
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/YYYYMMDDHHMMSS_add_max_delegation_depth.exs
  - lib/lockspire/storage/ecto/server_policy_record.ex
  - lib/lockspire/storage/ecto/client_record.ex
  - lib/lockspire/domain/server_policy.ex
  - lib/lockspire/domain/client.ex
  - lib/lockspire/admin/server_policy.ex
  - lib/lockspire/admin/clients.ex
autonomous: true
requirements: [TE-04]
must_haves:
  truths:
    - "System administrators can configure a maximum delegation depth globally and per-client."
    - "A hard limit of 5 is enforced to prevent JWT bloat DOS attacks."
  artifacts:
    - path: "lib/lockspire/domain/server_policy.ex"
      provides: "max_delegation_depth field in domain struct"
    - path: "lib/lockspire/domain/client.ex"
      provides: "max_delegation_depth field in domain struct"
  key_links:
    - from: "lib/lockspire/admin/server_policy.ex"
      to: "lib/lockspire/storage/ecto/server_policy_record.ex"
      via: "changeset validation enforcing <= 5"
---

<objective>
Introduce `max_delegation_depth` configurations to ServerPolicy and Client models to prevent token bloat during RFC 8693 token exchange delegation.

Purpose: Prevent cascading infrastructure failures (e.g., HTTP header size limits) caused by unbounded nested JWT `act` claims.
Output: Database columns, schema updates, domain struct fields, and changeset validations for `max_delegation_depth`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/lockspire/domain/server_policy.ex
@lib/lockspire/domain/client.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ecto Migration for max_delegation_depth</name>
  <files>priv/repo/migrations/*_add_max_delegation_depth.exs</files>
  <behavior>
    - Adds `max_delegation_depth` (integer, default 3) to `server_policies`.
    - Adds `max_delegation_depth` (integer, nullable) to `clients`.
  </behavior>
  <action>Generate and write an Ecto migration using `mix ecto.gen.migration add_max_delegation_depth`. Modify the generated file to add `max_delegation_depth` integer column to both `server_policies` (with default: 3, null: false) and `clients` (with null: true).</action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>Migration runs successfully and columns exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Domain Structs and Ecto Schemas</name>
  <files>lib/lockspire/domain/server_policy.ex, lib/lockspire/domain/client.ex, lib/lockspire/storage/ecto/server_policy_record.ex, lib/lockspire/storage/ecto/client_record.ex</files>
  <behavior>
    - Domain structs have the new field.
    - Ecto schemas cast and map the new field.
  </behavior>
  <action>Add `max_delegation_depth: non_neg_integer()` to the typespec and struct definitions of both `Lockspire.Domain.ServerPolicy` (default 3) and `Lockspire.Domain.Client` (default nil). Update the corresponding Ecto `*_record.ex` schemas to include `field :max_delegation_depth, :integer` and add it to their internal cast lists/mappings.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Code compiles without warnings and structs have the fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Validation in Admin Contexts</name>
  <files>lib/lockspire/admin/server_policy.ex, lib/lockspire/admin/clients.ex, test/lockspire/admin/server_policy_test.exs, test/lockspire/admin/clients_test.exs</files>
  <behavior>
    - Setting max_delegation_depth > 5 returns an error.
    - Setting max_delegation_depth < 0 returns an error.
  </behavior>
  <action>Update the Ecto changesets in `Lockspire.Admin.ServerPolicy` and `Lockspire.Admin.Clients` (or their respective internal changeset functions) to cast `max_delegation_depth` and enforce a maximum value of 5 (`validate_number(:max_delegation_depth, less_than_or_equal_to: 5, greater_than_or_equal_to: 0)`). Create or update corresponding tests to verify these validations.</action>
  <verify>
    <automated>mix test test/lockspire/admin/server_policy_test.exs test/lockspire/admin/clients_test.exs</automated>
  </verify>
  <done>Tests pass, proving the hard limit of 5 cannot be bypassed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin API -> Database | Configuration values provided by administrators must be sanitized. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-01 | Denial of Service | Changeset Validation | mitigate | Enforce absolute maximum of 5 on max_delegation_depth to prevent large JWT generation that could crash HTTP proxies. |
</threat_model>

<verification>
mix ecto.migrate
mix test test/lockspire/admin/server_policy_test.exs test/lockspire/admin/clients_test.exs
</verification>

<success_criteria>
The database supports `max_delegation_depth`, and domain/schema models map it correctly with validation limiting it to a max of 5.
</success_criteria>

<output>
After completion, create `.planning/phases/50-delegation-and-act-claims/50-01-SUMMARY.md`
</output>
