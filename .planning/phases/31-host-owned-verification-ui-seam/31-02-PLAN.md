---
phase: "31"
plan: "02"
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - lib/lockspire/generators/install.ex
  - lib/lockspire/generators/templates.ex
  - priv/templates/lockspire.install/router.ex
  - priv/templates/lockspire.install/verification_controller.ex
  - priv/templates/lockspire.install/verification_html.ex
  - priv/templates/lockspire.install/verification_html/index.html.heex
  - test/integration/install_generator_test.exs
autonomous: true
requirements:
  - DEV-04
  - DEV-05
must_haves:
  truths:
    - "mix lockspire.install generates editable host-owned /verify files and routes"
    - "A prefilled verification URL renders a code entry form without auto-submit or GET side effects"
    - "The generated starter seam re-displays the code and request context before explicit approve or deny"
  artifacts:
    - path: "priv/templates/lockspire.install/verification_controller.ex"
      provides: "Host-owned verification browser seam"
      contains: "def lookup"
    - path: "priv/templates/lockspire.install/router.ex"
      provides: "Generated host-owned /verify routes"
      contains: "get \"/verify\", LockspireVerificationController, :show"
    - path: "test/integration/install_generator_test.exs"
      provides: "Generator proof for new verification seam files"
      contains: "LockspireVerificationController"
  key_links:
    - from: "lib/lockspire/generators/templates.ex"
      to: "priv/templates/lockspire.install/verification_controller.ex"
      via: "template inventory entry"
      pattern: "verification_controller.ex"
    - from: "priv/templates/lockspire.install/verification_controller.ex"
      to: "lib/lockspire/protocol/device_verification.ex"
      via: "lookup and approve/deny protocol calls"
      pattern: "Lockspire.Protocol.DeviceVerification"
    - from: "priv/templates/lockspire.install/verification_controller.ex"
      to: "lib/lockspire/host/account_resolver.ex"
      via: "signed-in actor binding before approval"
      pattern: "Lockspire.account_resolver!\\(\\)"
---

<objective>
Generate the primary supported Phase 31 host seam: editable `/verify` routes and starter Phoenix browser code in the host app, not a Lockspire-owned browser UI.

Purpose: Implement D-01 through D-09 by extending `mix lockspire.install` with a controller-first, host-owned verification seam that demonstrates secure prefill, review, approve, and deny behavior.
Output: Generator inventory updates, router template changes, new host-owned verification controller and HEEx template files, and integration tests proving generator output and overwrite safety.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-host-owned-verification-ui-seam/31-CONTEXT.md
@.planning/phases/31-host-owned-verification-ui-seam/31-RESEARCH.md
@.planning/phases/31-host-owned-verification-ui-seam/31-PATTERNS.md
@.planning/phases/31-host-owned-verification-ui-seam/31-UI-SPEC.md
@docs/install-and-onboard.md

<interfaces>
From `lib/lockspire/generators/install.ex`:
```elixir
def run(opts \\ [])

Enum.each(Templates.all(), fn template ->
  render_template(template, assigns)
end)
```

From `lib/lockspire/generators/templates.ex`:
```elixir
def all do
  [
    %{template: "consent_live.ex", output: ...},
    %{template: "authorized_apps_controller.ex", output: ...}
  ]
end
```

From `priv/templates/lockspire.install/router.ex`:
```elixir
def lockspire_routes do
  """
  scope "/", <%= @web_module %> do
    pipe_through [:browser]
  """
end
```

From `lib/lockspire/host/account_resolver.ex`:
```elixir
@callback resolve_current_account(conn_or_socket :: term(), context()) ::
            {:ok, account()} | {:redirect, redirect_result()} | {:error, term()}
@callback build_claims(account(), context()) ::
            {:ok, Lockspire.Domain.Claims.t()} | {:error, term()}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend install generation and host-owned /verify routes</name>
  <files>lib/lockspire/generators/install.ex, lib/lockspire/generators/templates.ex, priv/templates/lockspire.install/router.ex, test/integration/install_generator_test.exs</files>
  <read_first>
    - lib/lockspire/generators/install.ex
    - lib/lockspire/generators/templates.ex
    - priv/templates/lockspire.install/router.ex
    - test/integration/install_generator_test.exs
    - docs/install-and-onboard.md
    - .planning/phases/31-host-owned-verification-ui-seam/31-CONTEXT.md
    - .planning/phases/31-host-owned-verification-ui-seam/31-UI-SPEC.md
  </read_first>
  <action>
    Extend `mix lockspire.install` per D-01, D-02, D-03, D-09, and D-22.

    Update template inventory and assigns so install now generates:
    - `lib/<host_web_path>/controllers/lockspire_verification_controller.ex`
    - `lib/<host_web_path>/controllers/lockspire_verification_html.ex`
    - `lib/<host_web_path>/controllers/lockspire_verification_html/index.html.heex`

    Keep the current refusal-to-overwrite-modified-files behavior unchanged.

    Update the generated router helper so the host-owned seam is explicit:
    - `get "/verify", LockspireVerificationController, :show`
    - `post "/verify", LockspireVerificationController, :lookup`
    - `post "/verify/:handle/approve", LockspireVerificationController, :approve`
    - `post "/verify/:handle/deny", LockspireVerificationController, :deny`

    Add generated comments in the router template that:
    - mark `/verify` as host-owned per D-01 and D-05
    - require host auth/session/rate-limit wiring
    - state that `verification_uri_complete` is prefill-only
    - warn against treating GET as approval or logging raw verification query strings

    Extend `test/integration/install_generator_test.exs` to assert the new files are emitted and that reruns still refuse to overwrite modified generated host files.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/integration/install_generator_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'verification_controller.ex' lib/lockspire/generators/templates.ex` succeeds.
    - `grep -q 'get \"/verify\", LockspireVerificationController, :show' priv/templates/lockspire.install/router.ex` succeeds.
    - `grep -q 'post \"/verify\", LockspireVerificationController, :lookup' priv/templates/lockspire.install/router.ex` succeeds.
    - `grep -q 'prefill-only' priv/templates/lockspire.install/router.ex` succeeds.
    - `mix test test/integration/install_generator_test.exs` passes.
  </acceptance_criteria>
  <done>Install generation now emits a host-owned verification seam and route scaffolding without weakening overwrite safety.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the controller-first verification starter seam</name>
  <files>priv/templates/lockspire.install/verification_controller.ex, priv/templates/lockspire.install/verification_html.ex, priv/templates/lockspire.install/verification_html/index.html.heex, test/integration/install_generator_test.exs</files>
  <read_first>
    - priv/templates/lockspire.install/consent_live.ex
    - priv/templates/lockspire.install/router.ex
    - lib/lockspire/web/interaction_controller.ex
    - lib/lockspire/host/account_resolver.ex
    - .planning/phases/31-host-owned-verification-ui-seam/31-UI-SPEC.md
    - .planning/phases/31-host-owned-verification-ui-seam/31-CONTEXT.md
  </read_first>
  <behavior>
    - `show/2` reads `user_code` only as a prefill value and never performs lookup, approve, or deny on GET.
    - `lookup/2` calls the library lookup seam, renders neutral invalid-or-expired messaging, and shows a review step with `user_code`, client context, scopes, and explicit confirmation copy.
    - `approve/2` and `deny/2` resolve the signed-in actor through `Lockspire.account_resolver!()` and submit the opaque handle plus claims-derived `subject_id` to `Lockspire.Protocol.DeviceVerification`.
  </behavior>
  <action>
    Create a controller-first starter seam instead of a required generated LiveView. This is the coherent choice for Phase 31 because it preserves literal `GET /verify` and `POST /verify` host seams, keeps GET side-effect free, and matches the rate-limit contract more directly while staying inside Phoenix-native host-owned code.

    In `verification_controller.ex`, generate these actions:
    - `show/2`: render the entry form; if `params["user_code"]` exists, use it only to prefill the visible input and display a comment that GET is side-effect free.
    - `lookup/2`: normalize the posted code, call `Lockspire.Protocol.DeviceVerification.lookup_pending_device_authorization/2`, render neutral invalid-or-expired copy on `:not_found` or `:expired`, and render review state on success using the opaque `verification_handle`
    - `approve/2` and `deny/2`: resolve the current account and claims via `Lockspire.account_resolver!()`, build an actor map with `subject_id: claims.subject`, then call the separate approve or deny protocol function

    In the generated template:
    - render the code in a visible chip or bordered panel before approval
    - show client name and requested scopes before the action buttons
    - include explicit copy telling the user to confirm the code matches the requesting device
    - keep approve and deny as separate buttons in one visual group

    Do not generate `phx-mounted`, `window.onload`, hidden auto-submit inputs, JS redirects, or GET mutations. Add comments warning the host not to log raw query strings or raw user codes. Keep the starter seam host-editable and calm rather than generic library-owned UI.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/integration/install_generator_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def lookup' priv/templates/lockspire.install/verification_controller.ex` succeeds.
    - `grep -q 'Lockspire.Protocol.DeviceVerification.lookup_pending_device_authorization' priv/templates/lockspire.install/verification_controller.ex` succeeds.
    - `grep -q 'Lockspire.account_resolver!()' priv/templates/lockspire.install/verification_controller.ex` succeeds.
    - `grep -q 'confirm the code matches' priv/templates/lockspire.install/verification_html/index.html.heex` succeeds.
    - `grep -q 'Do not auto-submit' priv/templates/lockspire.install/verification_controller.ex` succeeds.
    - `mix test test/integration/install_generator_test.exs` passes.
  </acceptance_criteria>
  <done>The generator now produces an editable host-owned verification controller and template that preserve the anti-phishing and explicit-user-action rules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated host browser code -> verification protocol | Host-owned browser inputs and session state cross into library-owned lookup and mutation APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-04 | Spoofing | `priv/templates/lockspire.install/verification_controller.ex` | mitigate | Treat `verification_uri_complete` as prefill-only, keep GET side-effect free, and require a visible code confirmation plus explicit submit before lookup or approval. |
| T-31-05 | Information Disclosure | `priv/templates/lockspire.install/router.ex` and generated controller/template comments | mitigate | Warn against logging raw verification query strings or raw user codes and switch post-lookup mutations to opaque handle routes. |
| T-31-06 | Elevation of Privilege | generated approve/deny actions | mitigate | Require signed-in actor resolution through `Lockspire.account_resolver!()` and pass claims-derived `subject_id` into the library mutation API before approval. |
</threat_model>

<verification>
MIX_ENV=test mix test test/integration/install_generator_test.exs
</verification>

<success_criteria>
The primary supported Phase 31 UX path is a generated, host-owned `/verify` seam that demonstrates secure prefill, neutral lookup failures, explicit actor-bound approval, and separate deny behavior without turning Lockspire into a browser-UI product.
</success_criteria>

<output>
After completion, create `.planning/phases/31-host-owned-verification-ui-seam/31-02-SUMMARY.md`
</output>
