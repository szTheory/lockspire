---
phase: "31"
plan: "03"
type: execute
wave: 2
depends_on:
  - 31-01
files_modified:
  - docs/device-flow-host-guide.md
  - docs/install-and-onboard.md
  - docs/supported-surface.md
autonomous: true
requirements:
  - DEV-06
must_haves:
  truths:
    - "Host teams have a concrete rate-limit contract for GET /verify and POST /verify"
    - "Onboarding points to the generated verification seam and the device-flow host guide during install"
    - "Supported-surface docs describe the shipped Phase 31 slice without over-claiming Phase 32 polling/token support"
  artifacts:
    - path: "docs/device-flow-host-guide.md"
      provides: "Dedicated host guide for verification UI security, rate limiting, and anti-phishing wiring"
      contains: "## Rate limiting /verify"
    - path: "docs/install-and-onboard.md"
      provides: "Install next steps that point hosts to the verification seam and host guide"
      contains: "verification"
    - path: "docs/supported-surface.md"
      provides: "Truthful support claim for the host-owned device verification seam"
      contains: "device verification"
  key_links:
    - from: "docs/install-and-onboard.md"
      to: "docs/device-flow-host-guide.md"
      via: "host setup guidance"
      pattern: "device-flow-host-guide"
    - from: "docs/supported-surface.md"
      to: "docs/device-flow-host-guide.md"
      via: "supported-slice explanation"
      pattern: "device-flow-host-guide"
---

<objective>
Document the host-owned security contract for `/verify` so Phase 31 ships an installable seam and concrete abuse-control guidance instead of hand-wavy warnings.

Purpose: Implement D-17 through D-22 and satisfy DEV-06 with specific host-side rate-limit guidance, onboarding links, and truthful supported-surface wording.
Output: A dedicated device-flow host guide plus updated onboarding and supported-surface docs.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-host-owned-verification-ui-seam/31-CONTEXT.md
@.planning/phases/31-host-owned-verification-ui-seam/31-RESEARCH.md
@docs/install-and-onboard.md
@docs/supported-surface.md
@docs/dynamic-registration.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the dedicated device-flow host guide</name>
  <files>docs/device-flow-host-guide.md</files>
  <read_first>
    - docs/dynamic-registration.md
    - docs/install-and-onboard.md
    - .planning/phases/31-host-owned-verification-ui-seam/31-CONTEXT.md
    - .planning/phases/31-host-owned-verification-ui-seam/31-RESEARCH.md
    - .planning/phases/31-host-owned-verification-ui-seam/31-UI-SPEC.md
  </read_first>
  <action>
    Create `docs/device-flow-host-guide.md` as the canonical Phase 31 host guide per D-18 through D-22.

    Include concrete sections for:
    - host-owned responsibility framing and the generated `/verify` seam
    - anti-phishing rules: `verification_uri_complete` is prefill-only, re-display the code, require user confirmation, and never approve on GET
    - rate-limiting `GET /verify` and `POST /verify`
    - IP trust guidance for reverse proxies and load balancers
    - user-code normalization before limit keys, using the exact behavior `strip separators + uppercase`
    - a primary IP bucket, a secondary normalized-code bucket, a tighter `{normalized_user_code, ip}` failure bucket, and an optional signed-in session/account bucket
    - 429 handling with short `Retry-After`, neutral copy, and stepped or exponential backoff on repeated POST failures
    - redacted logging/audit guidance keyed by fingerprints instead of raw codes

    Include idiomatic examples for both a small custom Plug/Hammer-style approach and a PlugAttack-style router-layer approach, but keep the contract behavioral rather than package-required. Explicitly state that Lockspire does not ship a built-in runtime rate limiter for this seam.
  </action>
  <verify>
    <automated>mix docs.verify</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '## Rate limiting /verify' docs/device-flow-host-guide.md` succeeds.
    - `grep -q 'verification_uri_complete is prefill-only' docs/device-flow-host-guide.md` succeeds.
    - `grep -q 'Retry-After' docs/device-flow-host-guide.md` succeeds.
    - `grep -q 'normalized_user_code' docs/device-flow-host-guide.md` succeeds.
    - `grep -q 'Lockspire does not provide built-in rate limiting' docs/device-flow-host-guide.md` succeeds.
  </acceptance_criteria>
  <done>The project has one dedicated, concrete host guide for the Phase 31 verification seam and its required abuse controls.</done>
</task>

<task type="auto">
  <name>Task 2: Wire onboarding and supported-surface docs to the Phase 31 seam</name>
  <files>docs/install-and-onboard.md, docs/supported-surface.md</files>
  <read_first>
    - docs/install-and-onboard.md
    - docs/supported-surface.md
    - docs/device-flow-host-guide.md
    - .planning/ROADMAP.md
    - .planning/phases/31-host-owned-verification-ui-seam/31-CONTEXT.md
  </read_first>
  <action>
    Update onboarding and supported-surface docs so the new Phase 31 seam is visible at install time and described truthfully.

    In `docs/install-and-onboard.md`:
    - add the generated verification controller and template files to the host-owned file list per D-02 and D-03
    - add next steps telling the host to wire auth/session behavior, add rate limiting, and read `docs/device-flow-host-guide.md`

    In `docs/supported-surface.md`:
    - change the device-flow statement from fully out-of-scope to a narrow truthful claim that Lockspire supports a generated host-owned verification seam in Phase 31
    - keep polling/token issuance out of the support claim until Phase 32 ships

    Do not claim that Lockspire enforces rate limiting automatically, and do not imply a Lockspire-owned browser UI.
  </action>
  <verify>
    <automated>mix docs.verify</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'device-flow-host-guide' docs/install-and-onboard.md` succeeds.
    - `grep -q 'verification' docs/install-and-onboard.md` succeeds.
    - `grep -q 'host-owned device verification seam' docs/supported-surface.md` succeeds.
    - `grep -q 'polling' docs/supported-surface.md` succeeds.
    - `mix docs.verify` passes.
  </acceptance_criteria>
  <done>Hosts encounter the Phase 31 verification seam and its security contract during install and the docs stay truthful about what remains for Phase 32.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation -> host implementation | Security-sensitive host behavior is guided by docs rather than enforced by runtime helpers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-07 | Spoofing | `docs/device-flow-host-guide.md` | mitigate | Document that `verification_uri_complete` is prefill-only, require visible code confirmation, and forbid GET approval semantics. |
| T-31-08 | Denial of Service | `/verify` guidance | mitigate | Provide exact IP, normalized-code, and failure-bucket rate-limit dimensions plus short `Retry-After` handling for brute-force and spray protection. |
| T-31-09 | Information Disclosure | logging guidance | mitigate | Require redacted logging keyed by fingerprints rather than raw codes and warn against logging raw verification query strings. |
</threat_model>

<verification>
mix docs.verify
</verification>

<success_criteria>
The docs give host teams actionable `/verify` abuse-control guidance, point them to the generated verification seam during onboarding, and keep the support contract truthful about the current device-flow slice.
</success_criteria>

<output>
After completion, create `.planning/phases/31-host-owned-verification-ui-seam/31-03-SUMMARY.md`
</output>
