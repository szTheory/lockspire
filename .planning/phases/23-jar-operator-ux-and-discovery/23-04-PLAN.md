---
phase: 23-jar-operator-ux-and-discovery
plan: 04
type: execute
wave: 2
depends_on:
  - 2
files_modified:
  - lib/lockspire/domain/client.ex
  - lib/lockspire/storage/ecto/client_record.ex
  - priv/repo/migrations/20260425222000_add_jar_policy_to_clients.exs
  - lib/lockspire/admin/clients.ex
  - test/lockspire/admin/clients_test.exs
autonomous: true
requirements:
  - JAR-06
must_haves:
  truths:
    - "Each client can store a JAR override alongside the existing PAR override"
    - "Setting JAR to required fails when the client has no usable inline JWK material"
    - "Invalid JAR override values are rejected with a field-specific error"
  artifacts:
    - path: "lib/lockspire/domain/client.ex"
      provides: "client registration state with JAR override"
    - path: "lib/lockspire/storage/ecto/client_record.ex"
      provides: "Ecto mapping for client JAR override"
    - path: "lib/lockspire/admin/clients.ex"
      provides: "safe-update boundary for client JAR overrides"
    - path: "test/lockspire/admin/clients_test.exs"
      provides: "client override and validation proof"
  key_links:
    - from: "lib/lockspire/admin/clients.ex"
      to: "lib/lockspire/domain/client.ex"
      via: "safe-update fields"
      pattern: "jar_policy"
    - from: "lib/lockspire/admin/clients.ex"
      to: "test/lockspire/admin/clients_test.exs"
      via: "inline-jwks guardrail contract"
      pattern: "jar_policy_requires_inline_jwks"
---

<objective>
Persist and guard per-client JAR enforcement overrides.

Purpose: operators must be able to set client overrides safely, and the system must reject impossible required states per D-07.
Output: client rows, safe-update validation, and tests for JAR overrides.
</objective>

<execution_context>
@/Users/jon/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jon/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-jar-operator-ux-and-discovery/23-CONTEXT.md
@lib/lockspire/domain/client.ex
@lib/lockspire/storage/ecto/client_record.ex
@lib/lockspire/admin/clients.ex
@test/lockspire/admin/clients_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store client JAR overrides</name>
  <files>lib/lockspire/domain/client.ex, lib/lockspire/storage/ecto/client_record.ex, priv/repo/migrations/20260425222000_add_jar_policy_to_clients.exs</files>
  <action>Extend the client domain struct and Ecto schema so `jar_policy` is a first-class mutable field, defaulting to `:inherit` and persisting alongside the existing PAR override. Add the migration for the clients table and keep all existing client security defaults unchanged.</action>
  <verify><automated>mix test test/lockspire/admin/clients_test.exs</automated></verify>
  <done>Client rows can persist and reload `jar_policy` with the rest of the client state.</done>
</task>

<task type="auto">
  <name>Task 2: Guard the impossible required state</name>
  <files>lib/lockspire/admin/clients.ex, test/lockspire/admin/clients_test.exs</files>
  <action>Allow `jar_policy` through the safe-update boundary, then reject `required` when the client has no usable inline JWK material. Keep the validation explicit and field-scoped so the admin surface can explain why the setting is unavailable per D-07.</action>
  <verify><automated>mix test test/lockspire/admin/clients_test.exs</automated></verify>
  <done>Required JAR cannot be saved for a client that lacks inline JWK material, and invalid override values still return a field error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/admin UI → client update boundary | Untrusted form input changes durable client policy state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-07 | T | `lib/lockspire/admin/clients.ex` | mitigate | Validate `jar_policy` values and block `required` when inline JWKs are absent. |
| T-23-08 | D | `lib/lockspire/storage/ecto/client_record.ex` | mitigate | Persist the override atomically with the rest of the client row so operator edits do not partially apply. |
</threat_model>

<verification>
Run the client admin test file and confirm the new override behavior, invalid mode rejection, and required-without-JWK guardrail all pass.
</verification>

<success_criteria>
Clients can carry JAR overrides, and the admin boundary blocks impossible required states.
</success_criteria>

<output>
After completion, create `.planning/phases/23-jar-operator-ux-and-discovery/23-04-SUMMARY.md`
</output>
