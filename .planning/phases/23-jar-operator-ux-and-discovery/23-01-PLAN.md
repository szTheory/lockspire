---
phase: 23-jar-operator-ux-and-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/jar.ex
  - lib/lockspire/protocol/discovery.ex
  - test/lockspire/web/discovery_controller_test.exs
autonomous: true
requirements:
  - JAR-05
must_haves:
  truths:
    - "Discovery publishes JAR capability booleans, not policy state"
    - "The advertised signing algorithms match the supported JAR allow-list"
    - "The discovery document omits request-object encryption and generic external request_uri claims"
  artifacts:
    - path: "lib/lockspire/protocol/jar.ex"
      provides: "public JAR signing-alg accessor"
    - path: "lib/lockspire/protocol/discovery.ex"
      provides: "OIDC discovery metadata builder"
    - path: "test/lockspire/web/discovery_controller_test.exs"
      provides: "truth contract for discovery metadata"
  key_links:
    - from: "lib/lockspire/protocol/jar.ex"
      to: "lib/lockspire/protocol/discovery.ex"
      via: "signing-alg allow-list accessor"
      pattern: "request_object_signing_alg_values_supported"
    - from: "lib/lockspire/protocol/discovery.ex"
      to: "test/lockspire/web/discovery_controller_test.exs"
      via: "metadata contract test"
      pattern: "request_parameter_supported|request_uri_parameter_supported"
---

<objective>
Expose truthful JAR capability metadata in discovery without leaking enforcement policy.

Purpose: integrators need machine-readable proof of JAR capability, while discovery must stay capability-first per D-01 through D-04.
Output: discovery JSON that advertises JAR support and nothing broader.
</objective>

<execution_context>
@/Users/jon/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jon/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-jar-operator-ux-and-discovery/23-CONTEXT.md
@lib/lockspire/protocol/jar.ex
@lib/lockspire/protocol/discovery.ex
@test/lockspire/web/discovery_controller_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish JAR capability metadata</name>
  <files>lib/lockspire/protocol/jar.ex, lib/lockspire/protocol/discovery.ex</files>
  <action>Add a public accessor on `Lockspire.Protocol.Jar` for the supported signing algorithms, then have `Lockspire.Protocol.Discovery` emit `request_parameter_supported: true`, `request_uri_parameter_supported: false`, and `request_object_signing_alg_values_supported` from that accessor. Keep the surface capability-first per D-01 through D-04: do not add encryption metadata, do not add any generic external `request_uri` claim, and do not leak policy state into discovery.</action>
  <verify><automated>mix test test/lockspire/web/discovery_controller_test.exs</automated></verify>
  <done>Discovery includes the exact JAR capability fields and still omits the unsupported request-object claims.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the discovery contract</name>
  <files>test/lockspire/web/discovery_controller_test.exs</files>
  <action>Extend the discovery contract test to assert the new JAR capability keys, assert the signing-alg list matches the supported allow-list, and continue refuting every unsupported request-object field named in the phase context.</action>
  <verify><automated>mix test test/lockspire/web/discovery_controller_test.exs</automated></verify>
  <done>The test fails if discovery advertises unsupported request-object fields or drifts from the allowed signing algorithms.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/app client → discovery endpoint | Untrusted consumers read metadata and may cache or compare it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | T | `lib/lockspire/protocol/discovery.ex` | mitigate | Emit only truthful capability fields and refute unsupported request-object keys in contract tests. |
| T-23-02 | I | discovery JSON | mitigate | Never publish policy state or unsupported request-object features; keep the document capability-first per D-01. |
</threat_model>

<verification>
Run the discovery controller test and confirm the JSON contains the new JAR fields with no unsupported request-object leaks.
</verification>

<success_criteria>
JAR discovery metadata is present, exact, and truthful; unsupported request-object metadata remains absent.
</success_criteria>

<output>
After completion, create `.planning/phases/23-jar-operator-ux-and-discovery/23-01-SUMMARY.md`
</output>
