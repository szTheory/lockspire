---
phase: 23-jar-operator-ux-and-discovery
plan: 03
type: execute
wave: 2
depends_on:
  - 2
files_modified:
  - lib/lockspire/protocol/jar_policy.ex
  - lib/lockspire/admin/server_policy.ex
  - test/lockspire/protocol/jar_policy_test.exs
  - test/lockspire/admin/server_policy_test.exs
autonomous: true
requirements:
  - JAR-06
must_haves:
  truths:
    - "Operators can read and write the global JAR policy through the admin boundary"
    - "Effective JAR requirement resolves from the global default plus client override"
    - "Invalid JAR mode input is rejected with a field-specific error"
  artifacts:
    - path: "lib/lockspire/protocol/jar_policy.ex"
      provides: "effective JAR requirement resolver"
    - path: "lib/lockspire/admin/server_policy.ex"
      provides: "global JAR policy command/query boundary"
    - path: "test/lockspire/protocol/jar_policy_test.exs"
      provides: "resolver behavior proof"
    - path: "test/lockspire/admin/server_policy_test.exs"
      provides: "global JAR policy persistence proof"
  key_links:
    - from: "lib/lockspire/admin/server_policy.ex"
      to: "lib/lockspire/protocol/jar_policy.ex"
      via: "global policy query/update input"
      pattern: "jar_policy"
    - from: "lib/lockspire/protocol/jar_policy.ex"
      to: "test/lockspire/protocol/jar_policy_test.exs"
      via: "resolved-policy contract"
      pattern: "resolve_effective_policy"
---

<objective>
Add the JAR resolver and global admin commands that sit on top of the persisted default.

Purpose: the operator surface needs a canonical answer for global JAR policy and an executable resolver for runtime truth per D-05 and D-06.
Output: a new JAR policy module plus admin get/put coverage.
</objective>

<execution_context>
@/Users/jon/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jon/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-jar-operator-ux-and-discovery/23-CONTEXT.md
@lib/lockspire/protocol/par_policy.ex
@lib/lockspire/domain/server_policy.ex
@lib/lockspire/admin/server_policy.ex
@test/lockspire/admin/server_policy_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the JAR effective-policy resolver</name>
  <files>lib/lockspire/protocol/jar_policy.ex, test/lockspire/protocol/jar_policy_test.exs</files>
  <action>Create `Lockspire.Protocol.JarPolicy` with the same precedence shape as `ParPolicy`, but keyed to `jar_policy` and explicit JAR nouns from D-05/D-06. The resolver should return global policy, client policy, effective policy, and a boolean `jar_required?`, and it should accept nil/map/struct clients the same way the PAR resolver does.</action>
  <verify><automated>mix test test/lockspire/protocol/jar_policy_test.exs</automated></verify>
  <done>Runtime JAR requirement can be computed deterministically from a global policy plus client override.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the global admin boundary</name>
  <files>lib/lockspire/admin/server_policy.ex, test/lockspire/admin/server_policy_test.exs</files>
  <action>Add JAR-specific get/put functions to `Lockspire.Admin.ServerPolicy` without changing the PAR API shape. Normalize only `optional` and `required`, preserve field-specific error details for bad input, and update the test module to prove both PAR and JAR defaults round-trip independently.</action>
  <verify><automated>mix test test/lockspire/admin/server_policy_test.exs</automated></verify>
  <done>The admin boundary can read and update JAR policy while leaving PAR behavior intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/admin boundary → policy command boundary | Untrusted operator input must be normalized before persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | T | `lib/lockspire/admin/server_policy.ex` | mitigate | Normalize `jar_policy` inputs to the explicit allow-list and reject everything else with a field error. |
| T-23-06 | R | `lib/lockspire/protocol/jar_policy.ex` | mitigate | Return a structured `Resolved` record so runtime and admin truth use the same auditable shape. |
</threat_model>

<verification>
Run the resolver and server-policy tests together and confirm both the runtime shape and the persistence boundary are pinned.
</verification>

<success_criteria>
Global JAR policy can be stored, queried, and resolved without changing PAR semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/23-jar-operator-ux-and-discovery/23-03-SUMMARY.md`
</output>
