---
phase: 23-jar-operator-ux-and-discovery
plan: 06
type: execute
wave: 4
depends_on:
  - 4
  - 5
files_modified:
  - lib/lockspire/web/live/admin/clients_live/show.ex
  - lib/lockspire/web/live/admin/clients_live/form_component.ex
  - lib/lockspire/web/router.ex
  - docs/operator-admin.md
  - test/lockspire/web/live/admin/clients_live_test.exs
autonomous: true
requirements:
  - JAR-06
must_haves:
  truths:
    - "Client details expose a dedicated JAR override action"
    - "The override form explains when required is unavailable because inline JWK material is missing"
    - "Saving the override persists the client JAR policy and preserves the effective requirement display"
  artifacts:
    - path: "lib/lockspire/web/live/admin/clients_live/show.ex"
      provides: "client detail workflow for JAR override"
    - path: "lib/lockspire/web/live/admin/clients_live/form_component.ex"
      provides: "JAR override form and helper copy"
    - path: "docs/operator-admin.md"
      provides: "operator-facing route documentation"
    - path: "test/lockspire/web/live/admin/clients_live_test.exs"
      provides: "client override contract"
  key_links:
    - from: "lib/lockspire/web/live/admin/clients_live/show.ex"
      to: "lib/lockspire/admin/clients.ex"
      via: "safe client update boundary"
      pattern: "jar_policy"
    - from: "lib/lockspire/web/live/admin/clients_live/form_component.ex"
      to: "lib/lockspire/protocol/jar_policy.ex"
      via: "effective requirement copy"
      pattern: "jar_required\?"
---

<objective>
Add the client-level JAR override workflow and document it.

Purpose: per-client enforcement is the operator half of D-09 through D-13, and it must explain the inline-JWK prerequisite clearly.
Output: client override route, form, docs, and tests.
</objective>

<execution_context>
@/Users/jon/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jon/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-jar-operator-ux-and-discovery/23-CONTEXT.md
@lib/lockspire/web/live/admin/clients_live/show.ex
@lib/lockspire/web/live/admin/clients_live/form_component.ex
@lib/lockspire/admin/clients.ex
@lib/lockspire/protocol/jar_policy.ex
@docs/operator-admin.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the client JAR override workflow</name>
  <files>lib/lockspire/web/live/admin/clients_live/show.ex, lib/lockspire/web/live/admin/clients_live/form_component.ex, lib/lockspire/web/router.ex</files>
  <action>Add the `:jar_policy` action to client detail, route it at `/admin/clients/:client_id/jar-policy`, and render the form using `JarPolicy.resolve_effective_policy/2` so the page shows the global policy, the client override, and the effective requirement in the same explicit wording used elsewhere. When the client lacks inline JWKs, the UI should make `required` visibly unavailable or clearly explained instead of presenting an impossible choice per D-07.</action>
  <verify><automated>mix test test/lockspire/web/live/admin/clients_live_test.exs</automated></verify>
  <done>The client override route exists, renders the effective requirement, and submits through the existing safe-update path.</done>
</task>

<task type="auto">
  <name>Task 2: Update operator truth surfaces</name>
  <files>docs/operator-admin.md, test/lockspire/web/live/admin/clients_live_test.exs</files>
  <action>Document the new client override route in the operator guide and extend the LiveView test to prove the JAR action is reachable, the helper copy is visible, and save behavior persists the override. Keep the wording short and causal so operators can tell capability from enforcement.</action>
  <verify><automated>mix test test/lockspire/web/live/admin/clients_live_test.exs</automated></verify>
  <done>The operator guide and client LiveView contract both cover the new JAR override workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser → client detail LiveView | Form input can change durable client policy state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-11 | T | `lib/lockspire/web/live/admin/clients_live/show.ex` | mitigate | Route all client edits through the safe-update boundary and keep JAR-specific inputs scoped to the override workflow. |
| T-23-12 | I | `docs/operator-admin.md` | accept | The docs only describe shipped routes and policy semantics; they do not expose secrets or internal request-object mechanics. |
</threat_model>

<verification>
Run the client LiveView tests and confirm the new route, helper copy, and persistence behavior all pass together.
</verification>

<success_criteria>
Client overrides are editable from a dedicated route, the prerequisite is obvious, and the docs match the shipped workflow.
</success_criteria>

<output>
After completion, create `.planning/phases/23-jar-operator-ux-and-discovery/23-06-SUMMARY.md`
</output>
