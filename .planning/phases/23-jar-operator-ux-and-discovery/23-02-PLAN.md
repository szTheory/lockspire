---
phase: 23-jar-operator-ux-and-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/domain/server_policy.ex
  - lib/lockspire/storage/ecto/server_policy_record.ex
  - priv/repo/migrations/20260425221000_add_jar_policy_to_server_policies.exs
  - test/lockspire/domain/server_policy_jar_test.exs
autonomous: true
requirements:
  - JAR-06
must_haves:
  truths:
    - "The durable server policy stores a JAR default alongside the existing PAR default"
    - "A fresh read after write returns the same JAR policy value"
    - "Persisting the new JAR field does not disturb the existing PAR field"
  artifacts:
    - path: "lib/lockspire/domain/server_policy.ex"
      provides: "durable server-policy struct"
    - path: "lib/lockspire/storage/ecto/server_policy_record.ex"
      provides: "Ecto persistence mapping for server policy"
    - path: "priv/repo/migrations/20260425221000_add_jar_policy_to_server_policies.exs"
      provides: "database column for JAR policy default"
    - path: "test/lockspire/domain/server_policy_jar_test.exs"
      provides: "repository round-trip proof for JAR policy"
  key_links:
    - from: "lib/lockspire/domain/server_policy.ex"
      to: "lib/lockspire/storage/ecto/server_policy_record.ex"
      via: "struct ↔ Ecto field mapping"
      pattern: "jar_policy"
    - from: "lib/lockspire/storage/ecto/server_policy_record.ex"
      to: "test/lockspire/domain/server_policy_jar_test.exs"
      via: "persist-and-reload contract"
      pattern: "get_server_policy|put_server_policy"
---

<objective>
Persist the global JAR enforcement default in durable storage.

Purpose: JAR policy must survive restarts and behave like the existing global PAR default per D-05 and D-06.
Output: a server policy row that carries both policy defaults.
</objective>

<execution_context>
@/Users/jon/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/jon/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-jar-operator-ux-and-discovery/23-CONTEXT.md
@lib/lockspire/domain/server_policy.ex
@lib/lockspire/storage/ecto/server_policy_record.ex
@priv/repo/migrations/20260424180000_add_lockspire_server_policy_and_client_par_policy.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable JAR default storage</name>
  <files>lib/lockspire/domain/server_policy.ex, lib/lockspire/storage/ecto/server_policy_record.ex, priv/repo/migrations/20260425221000_add_jar_policy_to_server_policies.exs</files>
  <action>Extend the server-policy struct and Ecto schema so they carry `jar_policy` as an explicit `:optional | :required` field alongside the existing PAR default. Add a migration that adds the database column with a safe `optional` default, and keep the existing PAR column untouched. Follow D-05 and D-06 so the new global JAR default uses the same durable-server-policy shape as PAR.</action>
  <verify><automated>mix test test/lockspire/domain/server_policy_jar_test.exs</automated></verify>
  <done>The database can store and reload `jar_policy` without changing `par_policy`.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the new global default round-trips</name>
  <files>test/lockspire/domain/server_policy_jar_test.exs</files>
  <action>Create a focused repository test that writes a server policy with a non-default JAR mode, reads it back, and confirms the PAR mode remains unchanged. Keep the assertions narrow so the test pins persistence truth rather than UI behavior.</action>
  <verify><automated>mix test test/lockspire/domain/server_policy_jar_test.exs</automated></verify>
  <done>Round-trip proof exists for the new global JAR default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration/runtime → durable storage | Schema changes must preserve existing policy truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | T | server-policy schema/record | mitigate | Add explicit `jar_policy` mapping plus round-trip coverage so schema drift cannot silently corrupt policy defaults. |
| T-23-04 | D | migration path | mitigate | Use a non-breaking default and keep the existing PAR column intact so upgrades do not strand the server policy row. |
</threat_model>

<verification>
Run the focused repository test and confirm the new JAR default survives a write/read cycle.
</verification>

<success_criteria>
The durable server policy now carries a JAR default and preserves the existing PAR default.
</success_criteria>

<output>
After completion, create `.planning/phases/23-jar-operator-ux-and-discovery/23-02-SUMMARY.md`
</output>
