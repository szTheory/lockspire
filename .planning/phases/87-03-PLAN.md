---
phase: 87
plan: 3
title: Align Operator And Release Truth With Canonical Support Surface
type: documentation
wave: 2
depends_on:
  - 87-01
files_modified:
  - docs/operator-admin.md
  - docs/maintainer-release.md
autonomous: true
requirements:
  - PROOF-02
---

<objective>
Align operator guidance and release-readiness proof with the corrected support contract so milestone-close truth stays centralized and executable.
</objective>

<scope>
This plan is limited to targeted wording fixes in `docs/operator-admin.md` and `docs/maintainer-release.md`. It must not create a second support matrix or add new automated doc-drift tests per D-01, D-02, D-03, and the Phase 87 deferred-items boundary.
</scope>

<threat_model>
## Threats

- `T-87-05` Operator docs may continue to tell users that DCR cannot manage logout propagation metadata, contradicting shipped behavior.
- `T-87-06` Release-readiness docs may drift and allow a second support-truth source to emerge.

## Mitigations

- Replace the stale operator-only claim with wording that says admin remains a valid explicit workflow while DCR now manages the same existing logout propagation metadata for eligible self-service clients per D-02 and D-07.
- Keep one short operator explanation of the logout asymmetry and separate post-logout redirect URIs from logout propagation settings per D-08 and D-09.
- Keep maintainer wording deferential to `docs/supported-surface.md` without adding new automated doc-drift assertions, per D-01, D-02, and the deferred boundary.
</threat_model>

<tasks>
- [ ] `87-03-01` Correct the operator workflow guide without changing the product boundary.
  - Files: `docs/operator-admin.md`
  - Action: update the logout propagation workflow section so it says operators can still manage these settings in admin, but Dynamic Client Registration now manages the same existing logout propagation metadata for eligible self-service clients; preserve the short warning that back-channel is durable and front-channel is best effort only per D-07, D-08, and D-09.
  - Verify: `mix docs.verify && rg -n "self-service clients|Back-channel logout is the reliable path|Front-channel logout is best effort only|Post-logout redirect URIs|Logout propagation" docs/operator-admin.md`

- [ ] `87-03-02` Keep maintainer release truth anchored to the canonical support contract.
  - Files: `docs/maintainer-release.md`
  - Action: do a narrow release-truth pass so any DCR/logout wording continues to defer to `docs/supported-surface.md` instead of restating or narrowing the support matrix, while keeping the maintainer guide out of support-matrix detail per D-01 and D-02.
  - Verify: `mix docs.verify && rg -n "docs/supported-surface.md|canonical support contract|does not define a second public support contract|Public release claims stay anchored to `docs/supported-surface.md`" docs/maintainer-release.md`
</tasks>

<verification>
- Run `mix docs.verify`.
- Confirm `docs/operator-admin.md` says DCR manages the existing logout propagation metadata while preserving the durable back-channel vs best-effort front-channel asymmetry and the redirect-vs-propagation separation.
- Confirm `docs/maintainer-release.md` continues to defer public support truth to `docs/supported-surface.md` and does not restate a second support matrix.
</verification>

<success_criteria>
- `docs/operator-admin.md` truthfully says DCR can manage the shipped logout propagation metadata while admin remains a valid explicit operator path.
- `docs/maintainer-release.md` continues to defer to `docs/supported-surface.md` and does not create a second support matrix.
</success_criteria>
