---
phase: 34-token-issuance-and-refresh-device-binding
plan: "02"
type: execute
wave: 2
depends_on:
  - "34-01"
files_modified:
  - lib/lockspire/protocol/token_endpoint_dpop.ex
  - lib/lockspire/protocol/refresh_exchange.ex
  - lib/lockspire/storage/token_store.ex
  - lib/lockspire/storage/ecto/repository.ex
  - test/lockspire/protocol/refresh_exchange_test.exs
autonomous: true
requirements:
  - DPoP-07
must_haves:
  truths:
    - "A DPoP-bound refresh token can only rotate when the presented proof is bound to the stored refresh-token key."
    - "Refresh proof-key mismatch, missing proof, or malformed proof collapses publicly to invalid_grant while retaining private reason codes."
    - "Rotated child access and refresh tokens preserve the original family cnf.jkt binding."
  artifacts:
    - path: lib/lockspire/protocol/refresh_exchange.ex
      provides: protocol-owned refresh binding enforcement and truthful token_type selection
    - path: lib/lockspire/storage/ecto/repository.ex
      provides: atomic compare-and-write refresh rotation with binding checks
    - path: test/lockspire/protocol/refresh_exchange_test.exs
      provides: proof for successful DPoP rotation, bearer fallback, and invalid_grant collapse
  key_links:
    - from: lib/lockspire/protocol/refresh_exchange.ex
      to: lib/lockspire/protocol/token_endpoint_dpop.ex
      via: refresh-specific DPoP context resolution
      pattern: "resolve_refresh_context"
    - from: lib/lockspire/protocol/refresh_exchange.ex
      to: lib/lockspire/storage/token_store.ex
      via: atomic rotate_refresh_token contract with expected cnf
      pattern: "expected_cnf"
    - from: lib/lockspire/storage/ecto/repository.ex
      to: lib/lockspire/domain/token.ex
      via: child token persistence carrying presented refresh cnf forward
      pattern: 'cnf: expected_cnf'
---

<objective>
Enforce DPoP binding on refresh rotation atomically and preserve family binding truth without widening public errors beyond OAuth-shaped `invalid_grant`.

Purpose: Refresh rotation is the highest-risk place to lose sender-constrained truth, so the compare-and-write step must stay durable, atomic, and privately diagnosable.
Output: A refresh rotation path that validates the presented proof key against the stored refresh token, preserves `cnf.jkt` on rotated children, and keeps public failures collapsed to `invalid_grant`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-token-issuance-and-refresh-device-binding/34-CONTEXT.md
@.planning/phases/34-token-issuance-and-refresh-device-binding/34-RESEARCH.md
@.planning/phases/34-token-issuance-and-refresh-device-binding/34-VALIDATION.md
@.planning/phases/34-token-issuance-and-refresh-device-binding/34-01-PLAN.md
@lib/lockspire/protocol/token_endpoint_dpop.ex
@lib/lockspire/protocol/refresh_exchange.ex
@lib/lockspire/storage/token_store.ex
@lib/lockspire/storage/ecto/repository.ex
@test/lockspire/protocol/refresh_exchange_test.exs

<interfaces>
From lib/lockspire/storage/token_store.ex:
```elixir
@callback rotate_refresh_token(String.t(), String.t(), DateTime.t(), Token.t(), Token.t()) ::
            {:ok, %{presented_refresh_token: Token.t(), refresh_token: Token.t(), access_token: Token.t()}}
            | {:error, store_error()}
```

From lib/lockspire/protocol/refresh_exchange.ex:
```elixir
@spec exchange_refresh_token(Client.t(), map()) :: {:ok, Success.t()} | {:error, Error.t()}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the refresh rotation persistence contract with atomic cnf comparison</name>
  <files>lib/lockspire/storage/token_store.ex, lib/lockspire/storage/ecto/repository.ex</files>
  <read_first>
    - lib/lockspire/storage/token_store.ex
    - lib/lockspire/storage/ecto/repository.ex
    - lib/lockspire/domain/token.ex
    - .planning/phases/34-token-issuance-and-refresh-device-binding/34-CONTEXT.md
    - .planning/phases/34-token-issuance-and-refresh-device-binding/34-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: repository rotation succeeds when `expected_cnf` matches the presented refresh token and persists the same cnf on child access and refresh tokens
    - Test 2: repository rotation returns a typed mismatch error before family mutation when `expected_cnf` does not match
    - Test 3: bearer refresh rotation still succeeds when `expected_cnf` is nil
  </behavior>
  <action>Extend the storage seam per D-08, D-12, and D-16 by changing `TokenStore.rotate_refresh_token/5` to accept a sixth argument named `expected_cnf`, shaped exactly as `nil | %{"jkt" => binary}`. Update `Lockspire.Storage.Ecto.Repository.rotate_refresh_token/6` so the locked refresh-token row is compared against `expected_cnf` inside the same transaction that revokes the presented token and stores the child access/refresh tokens. When `expected_cnf` matches, copy that same cnf onto both child tokens before insert. When the binding mismatches, return a typed store error such as `:dpop_binding_mismatch` without revoking the token family, without rotating children, and without turning storage into an HTTP proof parser. Preserve the existing reuse-detected family revocation path exactly for true refresh-token replay.</action>
  <acceptance_criteria>
    - `rg -n '@callback rotate_refresh_token\\(.+expected_cnf' lib/lockspire/storage/token_store.ex` confirms the storage contract takes the binding expectation
    - `rg -n 'def rotate_refresh_token\\(.+expected_cnf' lib/lockspire/storage/ecto/repository.ex` confirms the repository implementation takes the new argument
    - `rg -n 'dpop_binding_mismatch' lib/lockspire/storage/ecto/repository.ex` confirms the repository has a typed mismatch outcome
    - `rg -n 'cnf: expected_cnf' lib/lockspire/storage/ecto/repository.ex` confirms child token persistence carries the binding forward
    - `MIX_ENV=test mix test test/lockspire/protocol/refresh_exchange_test.exs -x` passes once the refresh tests are updated
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test.setup && MIX_ENV=test mix test test/lockspire/protocol/refresh_exchange_test.exs -x</automated>
  </verify>
  <done>The storage contract can atomically compare the presented refresh-token binding against an expected cnf map and persist the same binding on rotated child tokens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce refresh DPoP semantics and collapse proof-key failures to invalid_grant</name>
  <files>lib/lockspire/protocol/token_endpoint_dpop.ex, lib/lockspire/protocol/refresh_exchange.ex, test/lockspire/protocol/refresh_exchange_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/token_endpoint_dpop.ex
    - lib/lockspire/protocol/refresh_exchange.ex
    - test/lockspire/protocol/refresh_exchange_test.exs
    - .planning/phases/34-token-issuance-and-refresh-device-binding/34-CONTEXT.md
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-01-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: a DPoP-bound refresh token rotates successfully with `token_type == "DPoP"` and child tokens carrying the same `cnf["jkt"]`
    - Test 2: missing, malformed, or wrong-key DPoP proofs for a DPoP-bound refresh token return `%Error{error: "invalid_grant"}` with private reason codes
    - Test 3: bearer refresh rotation still returns `"Bearer"` and existing reuse detection remains intact
  </behavior>
  <action>Extend the shared helper from Plan 34-01 with `resolve_refresh_context(%Client{} = client, %Token{} = presented_refresh_token, request)` so refresh-specific proof failures follow D-16: public `invalid_grant`, private typed reasons, no provider-specific error names. In `Lockspire.Protocol.RefreshExchange`, call that helper after `fetch_presented_refresh_token/2`, pass the returned `expected_cnf` into the repository rotate call, set child access/refresh token `cnf` from the resolved context, and return `token_type: "DPoP"` only when the rotated family is DPoP-bound. Keep bearer refresh tokens on the existing success path. Expand `test/lockspire/protocol/refresh_exchange_test.exs` to cover successful DPoP rotation, wrong-key rejection, missing-proof rejection, malformed-proof rejection, bearer refresh unchanged, and replay-family revocation unchanged.</action>
  <acceptance_criteria>
    - `rg -n 'def resolve_refresh_context\\(%Client\\{} = client, %Token\\{} = presented_refresh_token, request\\)' lib/lockspire/protocol/token_endpoint_dpop.ex` finds the refresh-specific helper
    - `rg -n 'token_type: context\\.token_type' lib/lockspire/protocol/refresh_exchange.ex` confirms truthful token type selection
    - `rg -n 'expected_cnf' lib/lockspire/protocol/refresh_exchange.ex` confirms the protocol passes the binding expectation into storage
    - `rg -n 'error == \"invalid_grant\"' test/lockspire/protocol/refresh_exchange_test.exs` finds public error-collapse proof
    - `rg -n 'token_type == \"DPoP\"' test/lockspire/protocol/refresh_exchange_test.exs` finds truthful DPoP refresh success proof
    - `MIX_ENV=test mix test test/lockspire/protocol/refresh_exchange_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test.setup && MIX_ENV=test mix test test/lockspire/protocol/refresh_exchange_test.exs -x</automated>
  </verify>
  <done>Refresh exchange preserves DPoP binding truth through rotation, rejects wrong-key or missing-proof attempts as public `invalid_grant`, and leaves bearer rotation plus replay-family revocation behavior intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> refresh exchange | Untrusted refresh token strings and DPoP headers attempt to rotate durable token-family state |
| refresh protocol -> repository transaction | The protocol passes a binding expectation that storage must compare atomically before mutating family state |
| repository transaction -> token family rows | A mismatch here must not create partially rotated or partially revoked family state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-04 | Spoofing | `Lockspire.Protocol.RefreshExchange` | mitigate | Require a valid proof whose `jkt` matches the presented refresh token’s durable `cnf` before rotating any DPoP-bound family |
| T-34-05 | Tampering | `Lockspire.Storage.Ecto.Repository.rotate_refresh_token/6` | mitigate | Compare `expected_cnf` and persist child-token cnf inside the same `FOR UPDATE` transaction so wrong-key attempts cannot mutate family state |
| T-34-06 | Information Disclosure | refresh public error contract | mitigate | Collapse proof-key mismatch, missing proof, and malformed proof to public `invalid_grant` while keeping private reason codes for telemetry and audit per D-16 |
</threat_model>

<verification>
Run the refresh-focused quick suite after this plan:

`MIX_ENV=test mix test.setup && MIX_ENV=test mix test test/lockspire/protocol/refresh_exchange_test.exs -x`
</verification>

<success_criteria>
- DPoP-bound refresh tokens rotate only when the presented proof key matches the stored `cnf.jkt`.
- Rotated child access and refresh tokens preserve the original binding.
- Missing, malformed, and wrong-key refresh proofs return public `invalid_grant` with private typed reasons.
- Bearer refresh rotation and reuse detection continue to behave exactly as before.
</success_criteria>

<output>
After completion, create `.planning/phases/34-token-issuance-and-refresh-device-binding/34-02-SUMMARY.md`
</output>
