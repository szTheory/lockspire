---
phase: 133-clean-room-saas-journey
plan: 04
type: execute
wave: 4
depends_on: [133-02, 133-03]
files_modified:
  - scripts/acceptance/clean_room_saas_journey.py
  - test/integration/phase133_clean_room_saas_journey_test.exs
autonomous: true
requirements: [E2E-02, E2E-03]
must_haves:
  truths:
    - "A browser-like HTTP client crosses from the confidential-client origin to provider login/consent and back with the dedicated bearer client, completing confidential authorization code plus S256 PKCE (D-01, D-04, D-05, D-06)."
    - "Discovery/JWKS/ID-token/userinfo validation completes inside the client process before the provider protected API is called (D-07, D-08)."
    - "The protected API accepts the intended audience/scope and returns only documented semantic token facts after independent host policy approval (D-09, D-10, D-14)."
  artifacts:
    - path: scripts/acceptance/clean_room_saas_journey.py
      provides: named browser-like HTTP journey steps and safe assertions
    - path: test/integration/phase133_clean_room_saas_journey_test.exs
      provides: requirement-labelled real-listener acceptance proof
  key_links:
    - from: scripts/acceptance/clean_room_saas_journey.py
      to: confidential client authorization/callback endpoints
      via: explicit cross-origin redirects and per-origin cookie handling
      pattern: "callback"
    - from: confidential client
      to: provider protected API
      via: validated access token after OIDC/userinfo completion
      pattern: "billing"
---

<objective>
Complete one positive clean-room SaaS login and protected-resource journey over real HTTP.

Purpose: Compose the installed provider and independent client into the milestone's central acceptance spine before adding lifecycle and adversarial cases.
Output: Named HTTP journey runner and one full positive integration test.

Decision trace: Implements D-01, D-04 through D-10, D-14, D-17, and D-18.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-clean-room-saas-journey/133-CONTEXT.md
@.planning/phases/133-clean-room-saas-journey/133-RESEARCH.md
@.planning/phases/133-clean-room-saas-journey/133-VALIDATION.md
@.planning/phases/133-clean-room-saas-journey/133-02-SUMMARY.md
@.planning/phases/133-clean-room-saas-journey/133-03-SUMMARY.md
@scripts/demo/adoption_smoke.py
@test/integration/phase3_oidc_token_lifecycle_e2e_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Cross both origins through code, PKCE, callback, and protected API</name>
  <files>scripts/acceptance/clean_room_saas_journey.py, test/integration/phase133_clean_room_saas_journey_test.exs</files>
  <behavior>
    - Runner follows client start → provider authorize → host login → host consent → client callback across two distinct origins with origin-correct cookies and a bounded redirect count.
    - Token exchange uses confidential Basic authentication and the stored verifier; callback state is already consumed in the client.
    - Client receipt proves signature/issuer/audience/expiration/nonce and matching userinfo subject were validated before protected API invocation.
    - Billing response is 200 and contains only semantic subject, scopes, audiences, expires_at, confirmation, and a host-policy result; raw claims/tokens are absent.
  </behavior>
  <action>Write the failing full-journey test first and drive only live endpoints with the client app's bearer identity; assert its selected public client id differs from the DPoP identity without exposing either secret. Reuse the adoption smoke's standard-library cookie, redirect, form, CSRF, and JSON mechanics, but maintain distinct cookie jars/origin rules and delegate all confidential/OIDC decisions to the client app. Structure output as named safe stages: readiness, discovery, authorization, callback, OIDC, userinfo, resource. Assert order through client safe status receipts rather than private process or database inspection per D-14. Any failure must pass through Plan 01 redaction.</action>
  <verify><automated>mix test --include integration test/integration/phase133_clean_room_saas_journey_test.exs --only happy_path</automated></verify>
  <done>A fresh two-app run completes the entire separate-origin login and protected API path using only public package and wire surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove ordering, one-time callback, and semantic host-policy boundaries</name>
  <files>scripts/acceptance/clean_room_saas_journey.py, test/integration/phase133_clean_room_saas_journey_test.exs</files>
  <behavior>
    - Reusing the completed callback cannot exchange again or recreate a session.
    - Client safe receipt records validation-stage completion before resource call without containing tokens, code, nonce, verifier, secret, or cookies.
    - A protocol-valid identity denied by the illustrative host tenant/product predicate receives the host-owned denial without changing Lockspire protocol behavior.
  </behavior>
  <action>Add black-box assertions that the callback is terminal and that the client exposes only safe boolean/stage evidence. Exercise one allowed and one denied host account through otherwise identical valid protocol requests to show the D-09 authorization split. Keep response assertions limited to documented status/challenge/semantic JSON; do not inspect repository rows, assigns, or private reason atoms.</action>
  <verify><automated>mix test --include integration test/integration/phase133_clean_room_saas_journey_test.exs --only happy_path --only boundary</automated></verify>
  <done>The composed path proves correct validation ordering, one-time callback behavior, and separate host product authorization without sensitive evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser-like runner ↔ two origins | Redirects, cookies, CSRF, and callback data cross origin boundaries. |
| Client session → protected provider API | Validated identity/token data becomes a resource request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-133-13 | Spoofing | cross-origin callback | critical | mitigate | Real origin/cookie redirects plus server-side state and one-time callback proof. |
| T-133-14 | Elevation of privilege | protected API | high | mitigate | Require OIDC/userinfo completion before resource call and assert protocol plus host-policy gates. |
| T-133-15 | Information disclosure | journey assertions | high | mitigate | Safe stage receipts, semantic response allowlist, and centralized redaction. |
</threat_model>

<verification>
- `mix test --include integration test/integration/phase133_clean_room_saas_journey_test.exs --only happy_path --only boundary`
</verification>

<success_criteria>
- One fresh run proves E2E-02 and E2E-03 across actual listeners and distinct origins.
- Every validation step precedes session/resource completion.
- Wire evidence is semantic, redacted, and independent of internal storage or structs.
</success_criteria>

<output>Create `.planning/phases/133-clean-room-saas-journey/133-04-SUMMARY.md` when done.</output>
