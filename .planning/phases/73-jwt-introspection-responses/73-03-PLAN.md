---
phase: 73-jwt-introspection-responses
plan: 03
type: execute
wave: 3
depends_on:
  - 73-02
files_modified:
  - docs/supported-surface.md
  - test/lockspire/release_readiness_contract_test.exs
autonomous: true
requirements:
  - INT-01
must_haves:
  truths:
    - "The canonical public support contract explicitly describes RFC 9701 JWT introspection as an `Accept`-negotiated representation of the existing introspection endpoint."
    - "Public docs show both active and inactive JWT examples without overclaiming strict mode, encryption, discovery metadata, or host MIME setup."
    - "Release-readiness tests fail if the supported-surface wording drifts ahead of the shipped Phase 73 slice."
  artifacts:
    - path: "docs/supported-surface.md"
      provides: "Canonical support-truth wording for the shipped JWT introspection surface"
    - path: "test/lockspire/release_readiness_contract_test.exs"
      provides: "Release-contract assertions pinning the new RFC 9701 wording"
  key_links:
    - from: "docs/supported-surface.md"
      to: "test/lockspire/release_readiness_contract_test.exs"
      via: "release-contract assertions for public support claims"
      pattern: "token-introspection\\+jwt|RFC 9701|active: false"
---

<objective>
Close the public support-truth slice for Phase 73 by documenting the shipped RFC 9701 behavior and pinning it in release-readiness tests.

Purpose: D-26 is a locked deliverable, and Lockspire’s public support contract must stay synchronized with the executable repo truth.
Output: Updated supported-surface wording with active/inactive JWT examples plus release-readiness assertions that prevent future overclaim.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-jwt-introspection-responses/73-CONTEXT.md
@.planning/phases/73-jwt-introspection-responses/73-RESEARCH.md
@.planning/phases/73-jwt-introspection-responses/73-02-PLAN.md
@docs/supported-surface.md
@test/lockspire/release_readiness_contract_test.exs

<interfaces>
From docs/supported-surface.md:
```markdown
This page is the canonical public support contract for what Lockspire currently supports, what it does not support, and what repo-owned proof backs those claims.
```

From test/lockspire/release_readiness_contract_test.exs:
```elixir
@supported_surface_path Path.expand("../../docs/supported-surface.md", __DIR__)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the canonical support contract per D-23 through D-26</name>
  <files>docs/supported-surface.md</files>
  <action>Update `docs/supported-surface.md` so the supported surface explicitly says Lockspire supports RFC 9701 JWT introspection responses on the existing introspection endpoint when callers send `Accept: application/token-introspection+jwt`. Include one active and one inactive JWT example or example-shaped snippet per D-26, and keep the wording narrow: representation negotiation only, no host MIME registration per D-07, no encryption or discovery-metadata claims per D-23, and no Phase 74 strictness language per D-09. Preserve the embedded-library boundary and direct-client auth posture in the wording.</action>
  <verify>
    <automated>rg -n "RFC 9701|application/token-introspection\\+jwt|active: false|strict mode|encryption" docs/supported-surface.md</automated>
  </verify>
  <done>The canonical public contract describes the shipped Phase 73 slice accurately, includes active and inactive JWT examples, and does not overclaim adjacent Message Signing features.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin Phase 73 public wording in release-readiness tests</name>
  <files>test/lockspire/release_readiness_contract_test.exs</files>
  <behavior>
    - Test 1: release-readiness asserts that the supported-surface doc names RFC 9701 and `application/token-introspection+jwt` as the explicit negotiation trigger.
    - Test 2: release-readiness asserts the doc includes both active and inactive JWT examples, including the narrow inactive `active: false` shape.
    - Test 3: release-readiness refutes overclaims such as strict-mode enforcement, encryption support, or host MIME registration requirements inside the Phase 73 wording.
  </behavior>
  <action>Extend `Lockspire.ReleaseReadinessContractTest` to pin the new supported-surface claims so repo-truth docs cannot drift ahead of the implementation. Follow the repo’s existing pattern of file-content assertions: assert the RFC 9701 wording and examples exist, and explicitly refute adjacent-scope claims that would widen Phase 73 beyond D-23 and D-26. Keep this as a support-contract test only; do not duplicate controller behavior assertions already covered in Plan 73-02.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <done>Release-readiness tests enforce the exact public support contract for JWT introspection so future wording cannot silently overclaim or underdescribe the shipped slice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs -> integrator expectations | Human-facing support docs drive adoption and must stay inside repo-proven truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-07 | Repudiation | `docs/supported-surface.md` | mitigate | Document RFC 9701 as an explicit `Accept`-negotiated introspection representation with active/inactive examples per D-24 through D-26. |
| T-73-08 | Tampering | `test/lockspire/release_readiness_contract_test.exs` | mitigate | Pin the supported-surface wording and refute strict-mode, encryption, and host-MIME overclaims so public docs cannot drift beyond the shipped slice. |
</threat_model>

<verification>
Run the release-readiness test and inspect the supported-surface wording to confirm the public contract matches the endpoint’s actual Phase 73 behavior and boundary.
</verification>

<success_criteria>
The canonical support docs describe RFC 9701 JWT introspection exactly as shipped in Phase 73, and release-readiness tests enforce that contract against future drift.
</success_criteria>

<output>
After completion, create `.planning/phases/73-jwt-introspection-responses/73-03-SUMMARY.md`
</output>
