---
phase: 71-jarm-core
plan: 02
type: execute
wave: 2
depends_on: ["71-01"]
files_modified:
  - lib/lockspire/protocol/authorization_request.ex
  - lib/lockspire/protocol/authorization_flow.ex
  - lib/lockspire/protocol/discovery.ex
autonomous: true
requirements:
  - JARM-01
  - JARM-02
must_haves:
  truths:
    - "Requests with JARM composite modes are valid"
    - "Authorization responses use JARM JWT formatting when requested"
    - "JARM metadata is exposed in discovery"
  artifacts:
    - path: "lib/lockspire/protocol/authorization_flow.ex"
      provides: "Redirect and formatting logic"
    - path: "lib/lockspire/protocol/authorization_request.ex"
      provides: "JARM mode validation"
  key_links:
    - from: "lib/lockspire/protocol/authorization_flow.ex"
      to: "lib/lockspire/protocol/jarm.ex"
      via: "Jarm.sign/2"
      pattern: "Jarm\\.sign"
---

<objective>
Integrate JARM validation into the authorization flow, format the response redirects, and advertise JARM support in Discovery metadata.

Purpose: To fulfill client requests for signed authorization responses by connecting the `Jarm` signer module to the authorization completion flow, while properly formatting the HTTP response (query, fragment, or form_post).
Output: Updated AuthorizationRequest, AuthorizationFlow, and Discovery modules supporting JARM.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-jarm-core/71-01-SUMMARY.md
</context>

<interfaces>
From lib/lockspire/protocol/jarm.ex (created in 71-01):
```elixir
defmodule Lockspire.Protocol.Jarm do
  @spec sign(map(), map()) :: {:ok, String.t()} | {:error, any()}
end
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Request Validation and Discovery</name>
  <files>lib/lockspire/protocol/discovery.ex, lib/lockspire/protocol/authorization_request.ex</files>
  <action>
    1. Update `lib/lockspire/protocol/discovery.ex` to add `jwt`, `query.jwt`, `fragment.jwt`, and `form_post.jwt` to `@response_modes_supported`. Add `authorization_signing_alg_values_supported` reflecting supported algorithms (e.g., ["RS256", "ES256"]).
    2. Update `lib/lockspire/protocol/authorization_request.ex` to include the `.jwt` variants in `@allowed_response_modes` and permit them through `validate_response_mode/1`.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/discovery_test.exs test/lockspire/protocol/authorization_request_test.exs</automated>
  </verify>
  <done>Discovery advertises JARM and requests with JARM modes are accepted.</done>
</task>

<task type="auto">
  <name>Task 2: Authorization Flow Integration</name>
  <files>lib/lockspire/protocol/authorization_flow.ex</files>
  <action>
    Update `lib/lockspire/protocol/authorization_flow.ex` to intercept response formatting when `interaction.response_mode` indicates a JARM mode (`jwt` or ending in `.jwt`).
    Instead of returning raw params, pass `code`, `state`, etc., to `Lockspire.Protocol.Jarm.sign(params, context)`.
    Implement `format_jarm_redirect(interaction, mode, jwt)` using the pattern from PATTERNS.md to format the final redirect URI (merging `response=jwt_string` into query or fragment) or return `{:form_post, uri, params}` for `form_post.jwt`.
    Ensure default delivery logic correctly assigns `query.jwt` for `code` and `fragment.jwt` for implicit types if the client only specified `response_mode=jwt`.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/controllers/authorize_controller_test.exs</automated>
  </verify>
  <done>AuthorizationFlow successfully wraps parameters in a JWS and constructs the correct redirect/form payload.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Response Formatting | Passing sensitive authorization codes and states to the client via HTTP redirects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-71-03 | Information Disclosure | AuthorizationFlow | mitigate | Ensure fragment vs query delivery mode rules are strictly followed so tokens aren't leaked in query strings for implicit flows. |
</threat_model>

<verification>
- `mix test` passes
</verification>

<success_criteria>
- End-to-end authorization requests using `response_mode=jwt` succeed
- Tokens are properly nested in query/fragment/form_post parameters
</success_criteria>

<output>
After completion, create `.planning/phases/71-jarm-core/71-02-SUMMARY.md`
</output>
