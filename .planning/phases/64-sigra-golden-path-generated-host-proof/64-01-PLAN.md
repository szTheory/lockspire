---
phase: 64-sigra-golden-path-generated-host-proof
plan: 64-01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/generated_host_app_web/controllers/session_controller.ex
  - test/support/generated_host_app_web/router.ex
  - test/support/generated_host_app_web/plugs/put_current_scope.ex
  - test/support/generated_host_app/lockspire/test_account_resolver.ex
  - test/integration/phase31_generated_host_verification_e2e_test.exs
  - test/integration/phase37_protocol_strictness_e2e_test.exs
autonomous: true
requirements:
  - SIGRA-02
must_haves:
  truths:
    - "The generated-host fixture models a narrow Sigra-shaped `current_scope` seam instead of resolving accounts directly from raw session keys."
    - "Host login flow preserves `return_to` and `interaction_id` safely across the login bounce."
    - "No new Sigra-specific topology or compile-time dependency is introduced."
  artifacts:
    - path: test/support/generated_host_app_web/plugs/put_current_scope.ex
      provides: "host-only bridge from generated-host session state to `conn.assigns.current_scope`"
    - path: test/support/generated_host_app/lockspire/test_account_resolver.ex
      provides: "generated-host resolver that reads `current_scope.user` rather than bypassing the host seam"
    - path: test/integration/phase37_protocol_strictness_e2e_test.exs
      provides: "regression proof that stricter browser flows still work through the new host seam"
  key_links:
    - from: test/support/generated_host_app_web/router.ex
      to: test/support/generated_host_app_web/plugs/put_current_scope.ex
      via: "browser pipeline assigns host scope before Lockspire routes run"
      pattern: "current_scope"
    - from: test/support/generated_host_app/lockspire/test_account_resolver.ex
      to: test/support/generated_host_app_web/controllers/session_controller.ex
      via: "login controller seeds the host state the resolver consumes"
      pattern: "interaction_id|current_scope"
---

<objective>
Make the generated-host fixture behave like a narrow Sigra-backed host seam before the onboarding proof is deepened.

Purpose: Phase 64 needs the repo-owned generated host to model the same seam the docs already promise: a host-authenticated session becomes `conn.assigns.current_scope`, Lockspire reads `current_scope.user`, and the host login bounce safely preserves the OAuth interaction resume data.
Output: a generated-host plug or equivalent bridge for `current_scope`, a resolver that consumes that scope, and regression coverage proving the new seam does not break existing browser-sensitive flows.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/64-sigra-golden-path-generated-host-proof/64-CONTEXT.md
@.planning/phases/64-sigra-golden-path-generated-host-proof/64-RESEARCH.md
@.planning/phases/64-sigra-golden-path-generated-host-proof/64-PATTERNS.md
@test/support/generated_host_app_web/controllers/session_controller.ex
@test/support/generated_host_app_web/router.ex
@test/support/generated_host_app/lockspire/test_account_resolver.ex
@test/integration/phase31_generated_host_verification_e2e_test.exs
@test/integration/phase37_protocol_strictness_e2e_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a narrow generated-host `current_scope` bridge</name>
  <files>test/support/generated_host_app_web/router.ex, test/support/generated_host_app_web/plugs/put_current_scope.ex, test/support/generated_host_app_web/controllers/session_controller.ex</files>
  <read_first>
    - test/support/generated_host_app_web/router.ex
    - test/support/generated_host_app_web/controllers/session_controller.ex
    - docs/ecosystem-overview.md
    - .planning/phases/64-sigra-golden-path-generated-host-proof/64-CONTEXT.md
  </read_first>
  <action>Create a generated-host-only plug that derives a narrow `current_scope` assign from fixture session state before the forwarded Lockspire routes execute. Keep the payload minimal, for example a nested `%{user: ...}` shape plus only the extra metadata Phase 37 still needs. Wire that plug into the generated-host browser pipeline or equivalent host path instead of teaching Lockspire about Sigra directly. Extend the host login controller so the login form and POST preserve both `return_to` and `interaction_id`, while still refusing unsafe external redirects.</action>
  <acceptance_criteria>
    - `test/support/generated_host_app_web/plugs/put_current_scope.ex` contains `current_scope`
    - `test/support/generated_host_app_web/router.ex` invokes the new plug before forwarding `/lockspire`
    - `test/support/generated_host_app_web/controllers/session_controller.ex` includes hidden or propagated `interaction_id` handling
    - `test/support/generated_host_app_web/controllers/session_controller.ex` still restricts post-login redirects to local paths
  </acceptance_criteria>
  <verify>
    <automated>mix test test/integration/phase37_protocol_strictness_e2e_test.exs</automated>
  </verify>
  <done>The generated host now assigns a Sigra-shaped `current_scope` and the login seam preserves OAuth resume data safely.</done>
</task>

<task type="auto">
  <name>Task 2: Make the generated-host resolver consume `current_scope.user`</name>
  <files>test/support/generated_host_app/lockspire/test_account_resolver.ex, test/integration/phase31_generated_host_verification_e2e_test.exs, test/integration/phase37_protocol_strictness_e2e_test.exs</files>
  <read_first>
    - test/support/generated_host_app/lockspire/test_account_resolver.ex
    - test/integration/phase31_generated_host_verification_e2e_test.exs
    - test/integration/phase37_protocol_strictness_e2e_test.exs
    - .planning/phases/64-sigra-golden-path-generated-host-proof/64-RESEARCH.md
  </read_first>
  <action>Refactor the shared generated-host resolver so `resolve_current_account/2` reads `conn.assigns.current_scope.user` and only redirects when no host scope is present. Keep `resolve_account/2` and `build_claims/2` compatible with the existing generated-host test surface, but make the account data derive from the scope-owned user payload rather than from a raw session key. Update the verification and strictness integration tests only as needed so they still cover their original behavior through the new seam.</action>
  <acceptance_criteria>
    - `test/support/generated_host_app/lockspire/test_account_resolver.ex` contains `current_scope`
    - `test/support/generated_host_app/lockspire/test_account_resolver.ex` no longer reads `current_account_id` directly inside `resolve_current_account/2`
    - `test/integration/phase31_generated_host_verification_e2e_test.exs` still proves host-owned `/verify` behavior through the resolver seam
    - `test/integration/phase37_protocol_strictness_e2e_test.exs` still proves `prompt=none` and `auth_time` behavior through the host endpoint
  </acceptance_criteria>
  <verify>
    <automated>mix test test/integration/phase31_generated_host_verification_e2e_test.exs test/integration/phase37_protocol_strictness_e2e_test.exs</automated>
  </verify>
  <done>The shared generated-host resolver now matches the documented Sigra seam and existing browser proofs remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host session -> Lockspire resolver | The proof must show Lockspire reading a host-owned scope seam rather than bypassing it |
| login redirect -> OAuth interaction resume | Resume parameters must survive the host login bounce without becoming an open redirect vector |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01-01 | T | login bounce | mitigate | Preserve `interaction_id` and `return_to` explicitly while keeping local-path-only redirect rules |
| T-64-01-02 | S | host scope seam | mitigate | Resolve accounts from `current_scope.user`, not from direct Lockspire-owned assumptions |
| T-64-01-03 | R | topology truth | mitigate | Reuse the existing generated host instead of introducing a second Sigra fixture tree |
</threat_model>

<verification>
Run the Phase 37 strictness test after the scope bridge lands, then rerun Phase 31 verification plus Phase 37 together after the shared resolver is updated.
</verification>

<success_criteria>
- The generated-host fixture exposes a narrow `current_scope` seam before Lockspire routes run.
- Login redirect preservation includes `interaction_id` and `return_to`.
- Existing generated-host verification and strictness proofs still pass through the new seam.
</success_criteria>

<output>
After completion, create `.planning/phases/64-sigra-golden-path-generated-host-proof/64-01-SUMMARY.md`
</output>
