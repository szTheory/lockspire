---
phase: 82-shared-dpop-nonce-primitive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/dpop_nonce.ex
  - lib/lockspire/protocol/dpop.ex
  - lib/lockspire/protocol/token_endpoint_dpop.ex
  - lib/lockspire/protocol/protected_resource_dpop.ex
autonomous: true
requirements:
  - NONCE-CORE-01
  - NONCE-CORE-02
  - NONCE-CORE-03
  - NONCE-CORE-04
must_haves:
  truths:
    - Lockspire owns one shared DPoP nonce primitive instead of separate token- and resource-side implementations.
    - Authorization-server and resource-server nonces are purpose-separated at validation time, so cross-surface nonce reuse fails deterministically.
    - Nonce checking composes into the existing `Lockspire.Protocol.DPoP.validate_proof/2` seam and remains opt-in per caller.
    - Missing nonce and invalid/wrong-purpose/stale nonce produce typed internal failures that downstream adapters can map without reparsing proofs.
  artifacts:
    - path: lib/lockspire/protocol/dpop_nonce.ex
      provides: Stateless nonce issue/validate helper rooted in `secret_key_base`
    - path: lib/lockspire/protocol/dpop.ex
      provides: Optional nonce enforcement in the shared DPoP proof validator
    - path: lib/lockspire/protocol/token_endpoint_dpop.ex
      provides: Authorization-server consumption of typed nonce validator failures
    - path: lib/lockspire/protocol/protected_resource_dpop.ex
      provides: Resource-server consumption of typed nonce validator failures
  key_links:
    - from: lib/lockspire/protocol/dpop.ex
      to: lib/lockspire/protocol/dpop_nonce.ex
      via: claim validation branch for `nonce`
      pattern: "nonce_purpose"
    - from: lib/lockspire/protocol/token_endpoint_dpop.ex
      to: lib/lockspire/protocol/dpop.ex
      via: typed validator failure mapping
      pattern: "missing_dpop_nonce"
    - from: lib/lockspire/protocol/protected_resource_dpop.ex
      to: lib/lockspire/protocol/dpop.ex
      via: typed validator failure mapping
      pattern: "invalid_dpop_nonce"
---

<objective>
Add the shared DPoP nonce primitive and wire nonce validation into the existing proof validator without widening Lockspire's product surface.

Purpose: Phase 82 establishes one reusable nonce core before later phases adopt it on the public HTTP surfaces. The phase should land a stateless, purpose-separated primitive plus a single validator seam that token and protected-resource adapters can consume.
Output: `DPoPNonce` issue/validate helpers, opt-in nonce checking inside `DPoP.validate_proof/2`, and typed nonce failure propagation into the existing DPoP protocol consumers.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/82-shared-dpop-nonce-primitive/82-CONTEXT.md
@.planning/phases/82-shared-dpop-nonce-primitive/82-RESEARCH.md
@.planning/phases/82-shared-dpop-nonce-primitive/82-PATTERNS.md
@.planning/phases/82-shared-dpop-nonce-primitive/82-VALIDATION.md
@.planning/phases/33-dpop-proof-validation-and-replay-state/33-01-PLAN.md
@lib/lockspire/protocol/dpop.ex
@lib/lockspire/protocol/token_endpoint_dpop.ex
@lib/lockspire/protocol/protected_resource_dpop.ex

<interfaces>
From `lib/lockspire/protocol/dpop.ex`:
```elixir
@spec validate_proof(String.t(), keyword()) :: {:ok, t()} | {:error, validate_reason()}
```

Required validation options shape after this plan:
```elixir
[
  nonce_purpose: :authorization_server | :resource_server,
  secret_key_base: String.t(),
  nonce_max_age: non_neg_integer()
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the stateless shared nonce primitive with explicit purpose separation</name>
  <files>lib/lockspire/protocol/dpop_nonce.ex</files>
  <read_first>
    - .planning/phases/82-shared-dpop-nonce-primitive/82-CONTEXT.md
    - .planning/phases/82-shared-dpop-nonce-primitive/82-RESEARCH.md
    - lib/lockspire/protocol/dpop.ex
  </read_first>
  <behavior>
    - Downstream callers can issue nonces for `:authorization_server` and `:resource_server` through one shared module.
    - Nonces are opaque, unpredictable, and self-contained.
    - Validation rejects wrong-purpose, malformed, or expired nonces without exposing signing internals.
  </behavior>
  <action>
    Add `Lockspire.Protocol.DPoPNonce` as a narrow protocol helper. Keep it stateless and rooted in `secret_key_base`, with one issue function and one validate function. Encode the nonce purpose inside the signed payload so cross-surface reuse fails deterministically. Do not introduce database state, ETS state, operator knobs, or transport-aware return values.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/dpop_nonce_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The primitive issues nonce values for both supported purposes.
    - Validation succeeds only for the matching purpose.
    - Missing or invalid nonce input can later map to typed proof-validator failures.
    - No HTTP status, header, or controller logic appears in the primitive module.
  </acceptance_criteria>
  <done>The shared nonce core exists as a reusable protocol helper with explicit purpose separation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose nonce enforcement into the shared DPoP proof validator and preserve typed downstream mapping</name>
  <files>lib/lockspire/protocol/dpop.ex, lib/lockspire/protocol/token_endpoint_dpop.ex, lib/lockspire/protocol/protected_resource_dpop.ex</files>
  <read_first>
    - lib/lockspire/protocol/dpop_nonce.ex
    - lib/lockspire/protocol/dpop.ex
    - lib/lockspire/protocol/token_endpoint_dpop.ex
    - lib/lockspire/protocol/protected_resource_dpop.ex
    - .planning/phases/82-shared-dpop-nonce-primitive/82-PATTERNS.md
  </read_first>
  <behavior>
    - `DPoP.validate_proof/2` performs nonce checking only when the caller explicitly asks for it.
    - Missing nonce returns `:missing_dpop_nonce`.
    - Wrong-purpose, malformed, or stale nonce returns `:invalid_dpop_nonce`.
    - Existing non-nonce validation behavior remains unchanged for callers that do not request nonce enforcement.
  </behavior>
  <action>
    Extend `Lockspire.Protocol.DPoP.validate_proof/2` so nonce checking becomes another optional claim-validation branch in the existing validator flow. Thread `nonce_purpose`, `secret_key_base`, and nonce age through the existing validation opts. Then update token-endpoint and protected-resource DPoP consumers to branch on the typed nonce failures first and preserve those reasons for later public `use_dpop_nonce` mapping, without collapsing them into generic `invalid_dpop_proof`/`invalid_token` too early.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/dpop_test.exs test/lockspire/protocol/token_endpoint_dpop_test.exs test/lockspire/protocol/protected_resource_dpop_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The shared validator accepts nonce enforcement options without creating a second proof-validation path.
    - Callers that omit nonce options keep their prior behavior.
    - Token and protected-resource protocol adapters preserve typed nonce failure distinctions for later HTTP challenge work.
  </acceptance_criteria>
  <done>The shared proof validator now owns nonce checking, and downstream DPoP consumers receive stable typed nonce failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client proof -> `DPoPNonce` / `DPoP` | Untrusted nonce claim material enters Lockspire's proof-validation path. |
| Host config -> `secret_key_base` | Host-provided signing root must stay the only trust input for stateless nonce issuance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Spoofing | `DPoPNonce` | mitigate | Sign opaque nonce payloads with `secret_key_base` so clients cannot mint acceptable values. |
| T-82-02 | Elevation of Privilege | shared nonce primitive | mitigate | Encode and validate nonce purpose so authorization-server and resource-server values cannot be reused across surfaces. |
| T-82-03 | Tampering | `DPoP.validate_proof/2` integration | mitigate | Return typed nonce failures from one shared validator path instead of letting endpoint adapters implement divergent nonce checks. |
</threat_model>

<verification>
Run the dedicated nonce primitive tests first, then the shared validator and protocol-adapter suites. Keep nonce enforcement opt-in and verify that non-nonce callers remain unaffected.
</verification>

<success_criteria>
Lockspire has one reusable DPoP nonce primitive and one shared validator seam for nonce-enforced proof checking, ready for later public-surface adoption.
</success_criteria>

<output>
After completion, create `.planning/phases/82-shared-dpop-nonce-primitive/82-01-SUMMARY.md`
</output>
