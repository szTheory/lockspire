---
phase: 82-shared-dpop-nonce-primitive
plan: 02
type: execute
wave: 2
depends_on:
  - 82-01
files_modified:
  - test/lockspire/protocol/dpop_nonce_test.exs
  - test/lockspire/protocol/dpop_test.exs
  - test/lockspire/protocol/token_endpoint_dpop_test.exs
  - test/lockspire/protocol/protected_resource_dpop_test.exs
autonomous: true
requirements:
  - NONCE-CORE-01
  - NONCE-CORE-02
  - NONCE-CORE-03
  - NONCE-CORE-04
must_haves:
  truths:
    - The shared nonce primitive has direct unit proof for issuance, purpose separation, and age-bound validation.
    - The shared proof validator returns typed nonce failures that later endpoint adapters can trust.
    - Token-endpoint and protected-resource protocol tests prove the nonce primitive composes into both surfaces without cross-purpose leakage.
  artifacts:
    - path: test/lockspire/protocol/dpop_nonce_test.exs
      provides: Unit proof for nonce issuance, validation, purpose separation, and expiry behavior
    - path: test/lockspire/protocol/dpop_test.exs
      provides: Shared validator proof for missing and invalid nonce failure reasons
    - path: test/lockspire/protocol/token_endpoint_dpop_test.exs
      provides: Authorization-server DPoP adapter proof for nonce-specific failures
    - path: test/lockspire/protocol/protected_resource_dpop_test.exs
      provides: Resource-server DPoP adapter proof for nonce-specific failures
  key_links:
    - from: test/lockspire/protocol/dpop_nonce_test.exs
      to: lib/lockspire/protocol/dpop_nonce.ex
      via: direct primitive contract proof
      pattern: "authorization_server"
    - from: test/lockspire/protocol/dpop_test.exs
      to: lib/lockspire/protocol/dpop.ex
      via: typed missing/invalid nonce assertions
      pattern: "missing_dpop_nonce"
    - from: test/lockspire/protocol/token_endpoint_dpop_test.exs
      to: lib/lockspire/protocol/token_endpoint_dpop.ex
      via: authorization-server nonce mapping proof
      pattern: "use_dpop_nonce"
    - from: test/lockspire/protocol/protected_resource_dpop_test.exs
      to: lib/lockspire/protocol/protected_resource_dpop.ex
      via: resource-server nonce mapping proof
      pattern: "use_dpop_nonce"
---

<objective>
Add focused proof for the shared nonce primitive and its typed validator outcomes.

Purpose: After the primitive lands, Phase 82 still needs direct proof that nonce issuance is purpose-separated and that missing/invalid nonce paths remain typed and reusable across both DPoP-consuming protocol adapters.
Output: dedicated nonce tests plus expanded validator and protocol-adapter tests that lock the primitive's purpose separation and typed failure surface in place.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/82-shared-dpop-nonce-primitive/82-CONTEXT.md
@.planning/phases/82-shared-dpop-nonce-primitive/82-RESEARCH.md
@.planning/phases/82-shared-dpop-nonce-primitive/82-VALIDATION.md
@lib/lockspire/protocol/dpop_nonce.ex
@lib/lockspire/protocol/dpop.ex
@lib/lockspire/protocol/token_endpoint_dpop.ex
@lib/lockspire/protocol/protected_resource_dpop.ex
@test/lockspire/protocol/dpop_test.exs
@test/lockspire/protocol/token_endpoint_dpop_test.exs
@test/lockspire/protocol/protected_resource_dpop_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dedicated unit proof for nonce issuance, purpose separation, and bounded validity</name>
  <files>test/lockspire/protocol/dpop_nonce_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/dpop_nonce.ex
    - .planning/phases/82-shared-dpop-nonce-primitive/82-RESEARCH.md
  </read_first>
  <behavior>
    - A nonce issued for `:authorization_server` validates only for that purpose.
    - A nonce issued for `:resource_server` validates only for that purpose.
    - Expired or malformed nonce values fail validation cleanly.
  </behavior>
  <action>
    Add a dedicated `DPoPNonce` test module instead of proving the primitive only indirectly through higher-level protocol tests. Use fixed reference time or bounded max-age inputs where necessary so tests prove purpose separation, non-empty issuance, and expiration semantics without depending on controller transport behavior.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/dpop_nonce_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The primitive has its own focused unit tests.
    - Purpose mismatch fails deterministically.
    - Expired and malformed nonce values do not pass validation.
  </acceptance_criteria>
  <done>`DPoPNonce` has direct unit proof for the shared primitive contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock typed nonce failures into the shared validator and both DPoP protocol consumers</name>
  <files>test/lockspire/protocol/dpop_test.exs, test/lockspire/protocol/token_endpoint_dpop_test.exs, test/lockspire/protocol/protected_resource_dpop_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/dpop.ex
    - lib/lockspire/protocol/token_endpoint_dpop.ex
    - lib/lockspire/protocol/protected_resource_dpop.ex
    - test/lockspire/protocol/dpop_test.exs
    - test/lockspire/protocol/token_endpoint_dpop_test.exs
    - test/lockspire/protocol/protected_resource_dpop_test.exs
  </read_first>
  <behavior>
    - The shared validator returns `:missing_dpop_nonce` when nonce enforcement is requested and the proof omits `nonce`.
    - The shared validator returns `:invalid_dpop_nonce` for wrong-purpose or otherwise invalid supplied nonce values.
    - Token-endpoint and protected-resource DPoP consumers preserve that distinction when they build their own higher-level errors.
  </behavior>
  <action>
    Expand the shared DPoP validator tests to assert both typed nonce failure reasons directly. Then extend the token-endpoint and protected-resource protocol tests so each surface proves it consumes the shared primitive with the right purpose and keeps the nonce-specific failure path distinguishable from generic invalid-proof failures. Keep the proof at the protocol layer; controller and generated-host retry proof belong to later phases.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/dpop_test.exs test/lockspire/protocol/token_endpoint_dpop_test.exs test/lockspire/protocol/protected_resource_dpop_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Shared validator tests cover both typed nonce failure atoms.
    - Token-endpoint protocol tests prove authorization-server nonce purpose usage.
    - Protected-resource protocol tests prove resource-server nonce purpose usage.
    - No Phase 82 proof depends on controller/browser-level routing.
  </acceptance_criteria>
  <done>The shared nonce primitive and typed validator outcomes are locked in by focused protocol proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test fixtures -> primitive | Proof helpers must exercise both supported purposes without implicitly normalizing away the surface distinction. |
| Shared validator -> protocol consumers | Typed nonce failures must remain stable across adapters so later public-surface work does not drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Spoofing | primitive proof coverage | mitigate | Add direct tests proving wrong-purpose nonces never validate successfully. |
| T-82-02 | Tampering | validator failure mapping | mitigate | Assert exact typed atoms in shared validator tests before adapter-level mapping. |
| T-82-03 | Drift | token/protected-resource adapters | mitigate | Expand both protocol test suites so later surface adoption cannot silently change nonce semantics. |
</threat_model>

<verification>
Run the dedicated primitive test first, then the shared validator and both protocol-adapter suites together. Finish with a full targeted phase run to ensure all nonce proof remains synchronized.
</verification>

<success_criteria>
Phase 82 has repo-native proof that the shared nonce primitive is purpose-separated, age-bounded, and exposed through typed validator failures that both DPoP protocol consumers can rely on.
</success_criteria>

<output>
After completion, create `.planning/phases/82-shared-dpop-nonce-primitive/82-02-SUMMARY.md`
</output>
