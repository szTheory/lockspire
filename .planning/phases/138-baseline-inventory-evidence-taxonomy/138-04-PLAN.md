---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "04"
type: execute
wave: 4
depends_on: ["138-03"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-01, BASE-02, TRIAGE-01, TRIAGE-02]
must_haves:
  truths:
    - "BASE-01 BASE-02 D-06 D-07: fetch, branch, tag, and worktree collection each emit a receipt, and the snapshot completeness fails closed when any requested Git domain is unavailable, partial, or malformed."
    - "BASE-01 unclassified-edge backstop: a Git condition outside the known receipt grammar becomes an explicit limitation that prevents a clean or synchronized conclusion."
    - "BASE-02 empty edge: a valid empty Git result remains distinct from malformed non-empty command output, with hermetic proof for branches, tags, and worktrees."
    - "TRIAGE-01 D-07 D-10: complete zero, null nodes, malformed JSON, malformed or missing pageInfo, premature pagination, authentication failure, and API failure each produce a distinct fail-visible receipt."
    - "TRIAGE-02 D-07 D-23: a successful zero-issue query remains descriptive evidence and cannot be produced by an authentication, API, parsing, or pagination failure."
    - "D-11 D-24: rendered GitHub fields redact credential markers case-insensitively and strip or safely encode C0/C1 controls before Markdown or delimiter rendering."
  prohibitions:
    - statement: "Incomplete or malformed evidence must not be presented as a complete, clean, synchronized, or successful-zero observation."
      verification: flagged-unverified
    - statement: "The ledger must not retain credential-like text, raw GitHub bodies or logs, or uncontrolled C0/C1 control characters."
      verification: flagged-unverified
    - statement: "Phase 138 collection must not mutate GitHub objects, local refs, worktrees, release-owned files, or product/runtime surface; the D-09 fetch remains the only permitted Git metadata mutation."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Per-domain Git receipts, aggregate fail-closed completeness, distinct GitHub failure receipts, and hardened sanitization."
      contains: "render_source_receipts"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Hermetic malformed/empty Git, GraphQL failure-class, and adversarial sanitization fixtures."
      exports: ["assert_baseline_inventory_collector!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused ExUnit entry points for corrected source-integrity behavior."
      contains: "baseline inventory collector"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: test/support/lockspire/release_proof/package_assertions.ex
      via: "fake git/gh commands exercise each source outcome through the production CLI"
      pattern: "run_.*fixture"
    - from: scripts/maintainer/baseline_inventory.sh
      to: "snapshot front matter and Source receipts"
      via: "all requested source statuses feed one aggregate completeness decision"
      pattern: "status:"
---

<objective>
Close the Git/GitHub evidence-integrity gaps without disturbing the already-working deterministic inventory rows.

Purpose: Make every requested source fail visibly and ensure hostile external text cannot corrupt or leak through the authoritative Markdown evidence.
Output: Corrected collector source plus runnable failure, empty-result, unknown-condition, and sanitization contracts for BASE-01, BASE-02, TRIAGE-01, and TRIAGE-02.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-RESEARCH.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-01-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-02-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make every Git inventory domain receipt-driven and fail-closed</name>
  <reversibility rating="costly">Source-receipt names and completeness semantics are durable evidence cited by later phases, so changing them requires coordinated collector, fixture, and ledger updates.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — current `collect_git_baseline`, `collect_git_refs`, `collect_git_tags`, `collect_git_worktrees`, `render_source_receipts`, and front-matter aggregation seams
    - test/support/lockspire/release_proof/package_assertions.ex — existing fake-Git command harness and Git-domain assertions to extend behaviorally
    - test/lockspire/release/repository_hygiene_contract_test.exs — focused ExUnit delegation pattern
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed truths 3, 5, 20, and 25 plus CR-01
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-06, D-07, D-09, D-10, D-23, and D-24
  </read_first>
  <behavior>
    - "A successful branch, tag, or worktree query with no rows emits a complete receipt and the exact successful-zero wording for that domain."
    - "A nonzero command, malformed non-empty ref row, malformed worktree stanza, or unknown Git receipt condition emits partial/unavailable with the source name, exit/status evidence, limitation, and recovery proof."
    - "The top-level status is complete only when every requested Git source is complete; fetch success cannot mask a failed sibling source."
    - "Existing equal-SHA adjacency, canonical IDs, deterministic ordering, repeat-run identity, and proposal-only fields remain green."
  </behavior>
  <action>Start with failing hermetic cases in `PackageAssertions` for valid empty output, malformed non-empty output, and nonzero execution separately across branches, tags, and worktrees, plus an unknown Git receipt condition. Refactor the three Git domain collectors to retain raw-output outcome before normalization, validate the complete row/stanza grammar, and emit individual source receipts with `complete`, `partial`, or `unavailable` per D-07. Aggregate fetch and every requested Git-domain status into front matter and conclusion copy; a malformed or unavailable child source must suppress clean/synchronized/empty conclusions while preserving all usable sibling evidence. Keep the existing `git for-each-ref`, worktree porcelain, subject-derived IDs, sorting, D-09 fetch allowlist, and proposal-only rows unchanged when inputs are valid.</action>
  <acceptance_criteria>
    - Focused tests prove valid-empty and malformed-non-empty are different outcomes for every Git domain and that a failed domain changes both its receipt and snapshot completeness.
    - An unknown Git condition renders an explicit limitation and no clean/synchronized conclusion.
    - Existing adjacency, ordering, idempotency, and non-destructive source-contract assertions remain green.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
  </verify>
  <done>Every requested Git source has an honest receipt, and no malformed or unavailable domain can masquerade as successful zero or complete evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve distinct GitHub failures and sanitize hostile rendered fields</name>
  <reversibility rating="costly">GitHub receipt reason codes and the rendered sanitization contract are durable operator evidence consumed by later phase audits.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `github_auth_receipt`, `combine_graphql_pages`, GitHub collectors/renderers, and `markdown_value`
    - test/support/lockspire/release_proof/package_assertions.ex — `run_github_fixture!`, `fake_gh_script`, and current happy/zero assertions
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed truth 12, CR-05, and the credential/control-character gap
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-07, D-10, D-11, D-16, D-23, and D-24
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md — reasoned no-external-integration declaration to preserve; this plan exercises the existing read-only GitHub CLI collector and adds no integration surface
  </read_first>
  <behavior>
    - "Each required GitHub failure class has a stable distinct receipt code and limitation: null_nodes, malformed_json, malformed_page_info, premature_pagination, auth_failed, and api_failed."
    - "Only authenticated, parsed, terminal pagination with a valid empty nodes array can render successful-zero wording."
    - "Uppercase and mixed-case TOKEN, SECRET, PASSWORD, AUTHORIZATION, ghp_, and github_pat_ markers redact; CR and all other C0/C1 controls cannot reach Markdown rows or parsing delimiters."
    - "Separate PR/issue namespaces, numeric ordering, duplicate corroboration, conservative disposition logic, and no-empty-queue semantics remain green."
  </behavior>
  <action>Add table-driven fake-`gh` scenarios that execute the production `--scope github` command for each failure class and assert distinct receipts rather than source-string presence. Preserve parse/validation reason information across `combine_graphql_pages` and its callers so authentication, transport/API exit, JSON syntax, node shape, pageInfo shape, and terminal-cursor failures cannot collapse into one status. Harden `markdown_value` and every GitHub field boundary with locale-stable case-insensitive credential detection and C0/C1 removal or visible safe encoding before delimiter parsing and Markdown escaping; include adversarial titles, refs, URLs, and receipt text with carriage return, unit separator, escape, DEL, and C1 bytes. Keep only the existing safe-field GraphQL allowlist and do not add raw bodies, comments, logs, headers, environment values, or a new external API integration.</action>
  <acceptance_criteria>
    - Every named GitHub failure fixture produces its own stable code/limitation, suppresses successful-zero and merge-ready conclusions, and leaves any valid sibling evidence explicitly partial rather than complete.
    - Mixed-case credential sentinels render `[REDACTED]`; adversarial output contains no raw sentinel or C0/C1 byte and cannot shift row columns.
    - Existing PR adjacency/order and issue namespace/empty/order tests pass unchanged, and the API coverage verify-pre command still accepts the no-integration declaration.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy</automated>
  </verify>
  <done>Every GitHub failure mode is independently diagnosable and no credential-like or control-character input can be durably rendered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git command output → source receipts | Mutable ref/worktree output must be distinguished as valid empty, valid rows, malformed, or unavailable before conclusions are rendered. |
| GitHub CLI/GraphQL output → parser → Markdown | Authenticated remote data and failure output cross parser and durable-rendering boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-13 | Tampering / Repudiation | Git completeness aggregation | high | mitigate | Per-domain validation and receipts feed one fail-closed snapshot status; malformed, unknown, and unavailable fixtures block complete/clean/empty conclusions. |
| T-138-14 | Repudiation | GitHub pagination/auth/API receipts | high | mitigate | Stable distinct failure codes retain auth, transport, parse, node, pageInfo, and terminal-pagination causes through the renderer. |
| T-138-15 | Information disclosure | GitHub field sanitization | high | mitigate | Safe-field allowlist, case-insensitive credential redaction, C0/C1 neutralization, Markdown escaping, and adversarial byte fixtures. |
| T-138-16 | Elevation of privilege | repository-maintainer collector | medium | mitigate | Preserve read-only Git/GitHub commands and the sole D-09 fetch mutation; source contracts reject ref, worktree, GitHub, release, or runtime mutation surface. |

## High-Severity Blocking Rule

Any failed or missing mitigation for T-138-13, T-138-14, or T-138-15 blocks Plan 138-04. All named failure and hostile-input fixtures must execute the production collector and pass before later gap plans may run.

## OWASP ASVS L1 Control Mapping

| ASVS family | Applicability and control intent | Named mitigation / task | Acceptance and verification evidence |
|-------------|----------------------------------|-------------------------|--------------------------------------|
| V2 Authentication | Applicable only at the existing GitHub CLI authentication boundary: an unauthenticated collector must fail visibly and must never be interpreted as an authenticated successful-zero result. Credential enrollment, password, MFA, session, and token-issuance controls are not applicable because this plan neither authenticates users nor handles credentials; `gh` owns authentication. | T-138-14; Task 2, **Preserve distinct GitHub failures and sanitize hostile rendered fields**, retains `auth_failed` separately from API, parse, and pagination failures. | Task 2 acceptance requires every named GitHub failure fixture to produce its own receipt and suppress successful-zero/merge-ready conclusions. Evidence: `mix test test/lockspire/release/repository_hygiene_contract_test.exs` plus the API-coverage verify-pre command in Task 2. |
| V4 Access Control | Applicable to least-authority collection: Git/GitHub observations remain read-only and the D-09 fetch is the sole permitted metadata mutation. Host-user authorization and object-level application access controls are not applicable because this maintainer CLI exposes no user or tenant resource. | T-138-16; Task 1 preserves the D-09 allowlist and source-contract mutation assertions, while Task 2 preserves the safe-field GraphQL allowlist and adds no mutation query. | Task 1 acceptance keeps non-destructive source-contract assertions green; Task 2 acceptance keeps the no-integration declaration valid. Evidence: the focused ExUnit command in both tasks and `node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy`. |
| V5 Validation, Sanitization and Encoding | Applicable to all untrusted Git/GitHub output before receipt parsing, delimiter use, or Markdown rendering. | T-138-13 and T-138-15; Task 1 validates full Git row/stanza grammar and distinguishes malformed from valid-empty, while Task 2 applies case-insensitive credential redaction, C0/C1 neutralization, and Markdown escaping at every GitHub field boundary. | Task 1 acceptance proves malformed-non-empty differs from valid-empty for every Git domain. Task 2 acceptance proves mixed-case sentinels redact and hostile bytes cannot shift columns. Evidence: `bash -n scripts/maintainer/baseline_inventory.sh && mix test test/lockspire/release/repository_hygiene_contract_test.exs`. |
| V7 Error Handling and Logging | Applicable to safe failure handling and durable evidence receipts: failure categories must remain distinct, sanitized, and fail closed without retaining raw bodies, logs, headers, or environment values. Application security logging/audit-event controls beyond this evidence ledger are not applicable to this maintainer-only collector. | T-138-13, T-138-14, and T-138-15; Tasks 1 and 2 emit explicit `partial`/`unavailable` receipts, stable failure codes, limitations, and recovery proof while suppressing clean/complete/successful-zero conclusions. | Task 1 acceptance covers unknown, unavailable, malformed, and valid-empty outcomes; Task 2 acceptance covers null nodes, malformed JSON/pageInfo, premature pagination, auth, and API failures. Evidence: the focused ExUnit command and Task 2 API-coverage verify-pre command. |
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: per-domain Git receipt/status state; aggregate snapshot completeness; distinct GitHub failure codes; hardened `markdown_value`/field sanitization; preserved `--scope git|github` CLI.
- `test/support/lockspire/release_proof/package_assertions.ex`: executable fake-Git empty/malformed/failure/unknown scenarios; fake-`gh` null/malformed/pageInfo/premature/auth/API scenarios; mixed-case credential and C0/C1 adversarial fixtures.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: focused source-integrity and sanitization contract entry points.
- Commands: `bash scripts/maintainer/baseline_inventory.sh --scope git --output PATH`; `bash scripts/maintainer/baseline_inventory.sh --scope github --output PATH`.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Current non-destructive local/remote Git inventory | 138-04 | COVERED | Fail-closed receipts repair false completeness. |
| REQ | BASE-01 | Exact refreshed baseline or exact divergence | 138-04 | COVERED | Preserves verified baseline and adds unknown-condition backstop. |
| REQ | BASE-02 | Every branch/tag/worktree with honest evidence | 138-04 | COVERED | Per-domain validation and receipts. |
| REQ | TRIAGE-01 | Every open PR through complete evidence | 138-04 | COVERED | Distinct pagination/auth/API failures and sanitization. |
| REQ | TRIAGE-02 | Separate issue evidence with honest zero | 138-04 | COVERED | Successful zero gated on complete pagination. |
| RESEARCH | — | Receipt-first fail-visible collection and allowlisted fields | 138-04 | COVERED | Corrects execution misses in existing implementation. |
| CONTEXT | D-06–D-11 | Provenance, completeness, stable interfaces, redaction | 138-04 | COVERED | Git/GitHub receipts and hostile fixtures. |
| CONTEXT | D-12–D-16 | Exact proposal taxonomy and conservative evidence | 138-04 | COVERED | Existing row behavior retained by regression tests. |
| CONTEXT | D-21–D-24 | Calm explicit copy and focused proof | 138-04 | COVERED | Failure copy and executable fixtures. |

<verification>
- `bash -n scripts/maintainer/baseline_inventory.sh`
- `mix test test/lockspire/release/repository_hygiene_contract_test.exs`
- `node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy`
</verification>

<success_criteria>
- Failed/malformed Git domains cannot leave a complete snapshot or successful-zero section.
- All required GitHub pagination/auth/API failure classes are distinct and executable under fixtures.
- Credential-like and C0/C1 input is safely redacted/neutralized without corrupting evidence rows.
- Already-verified adjacency, ordering, idempotency, namespace, and proposal-only behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-04-SUMMARY.md` when done.
</output>
