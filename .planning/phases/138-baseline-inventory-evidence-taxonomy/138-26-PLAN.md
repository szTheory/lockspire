---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "26"
type: execute
wave: 26
depends_on: ["138-25"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-01, BASE-02]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BASE-02 D-06 D-07 D-10 D-14 D-22 D-24: every legal tab/newline worktree path crosses a delimiter-safe structured transport and produces exactly one valid deterministic proposal-only row, or the worktree receipt becomes partial with a named limitation."
    - "BASE-01 BASE-02 D-09: an output path is resolved against the invocation working directory before repository-root Git work begins, and --replace can affect only that exact canonical target."
    - "BASE-01 D-07: git, python3, and jq are checked before fetch, source scanning, relation verification, or publication."
    - statement: "BASE-01 unclassified remains unresolved: an unavailable dependency, ambiguous caller path, malformed worktree record, or unknown baseline condition blocks collection with a named diagnostic."
      verification: backstop
  prohibitions:
    - statement: "The collector must not mutate refs, tags, worktrees, GitHub objects, release-owned files, or any output other than the exact caller-resolved target."
      verification: flagged-unverified
    - statement: "Legal path bytes must not be silently split, dropped, normalized into a different identity, or reported complete after parse failure."
      verification: flagged-unverified
    - statement: "The correction must not add a schema, sidecar, runtime module, Mix task, public API, or gitignored evidence mirror."
      verification: flagged-unverified
    - statement: "Diagnostics and rendered rows must not expose credentials, raw environment values, or uncontrolled bodies."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Delimiter-safe worktree transport, invocation-relative target resolution, and deterministic dependency preflight."
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused Phase 138 path-boundary test entry point."
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Real-repository delimiter-path, nested-caller replacement, and no-jq process fixtures."
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: test/support/lockspire/release_proof/package_assertions.ex
      via: "production CLI executions against real temporary Git repositories"
      pattern: "worktree|--output|--replace|jq"
---

<objective>
Make Git worktree rows and output publication safe for every legal caller/path shape before any later relation hardening or republication.

Purpose: Close the fail-open BASE-02 delimiter defect and the wrong-target replacement defect at the collector's first filesystem boundary, while making dependency failure immediate and deterministic.
Output: Production collector corrections and focused hermetic process regressions; no ledger replacement.

Decision coverage: D-03 repo-local Bash ownership; D-06 exact canonical paths; D-07 fail-visible completeness; D-09 fetch-only metadata mutation; D-10 structured Git interfaces; D-11 safe rendering; D-14 one structurally valid row per observation; D-15 proposal-only state; D-22 deterministic accessible output; D-24 focused executable proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-RESEARCH.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-PATTERNS.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-25-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one hostile worktree path from Git observation to one valid ledger row</name>
  <reversibility rating="reversible">The transport is private collector plumbing and can be replaced while preserving stable path-derived evidence IDs and Markdown output.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `finalize_worktree_stanza`, `collect_git_worktrees`, `sort_evidence_rows`, and `render_git_inventory`
    - test/lockspire/release/repository_hygiene_contract_test.exs — existing tagged Phase 138 delegation pattern
    - test/support/lockspire/release_proof/package_assertions.ex — temporary Git repository and production collector process helpers
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed BASE-02 truth and required real-repository fixtures
  </read_first>
  <behavior>
    - A real linked worktree whose path contains a tab yields exactly one 12-field Markdown row with the exact path-derived stable ID and a complete receipt.
    - A real linked worktree whose path contains a newline yields exactly one 12-field Markdown row with the exact path-derived stable ID and a complete receipt.
    - Any malformed structured record produces a partial worktree receipt and cannot preserve an affirmative aggregate.
    - Existing ordinary worktree ordering and IDs remain byte-stable.
  </behavior>
  <action>Add a focused test delegating to a new PackageAssertions process contract. Build actual temporary Git repositories and linked worktrees with tab and newline characters; do not simulate this boundary with hand-authored tab rows. First prove the current production CLI corrupts or miscounts the row. Then replace raw tab-delimited `finalize_worktree_stanza` transport with NUL-delimited or JSON-object transport from worktree parsing through deterministic sorting and final rendering. Preserve valid path bytes for D-20 identity, encode only at the final Markdown/YAML boundary, validate field cardinality before rendering, and mark the exact domain plus aggregate partial on decode/shape failure. Keep ordinary branch/tag behavior and D-09 non-destructive collection unchanged.</action>
  <acceptance_criteria>
    - Real tab and newline worktree paths each render as one valid 12-field proposal-only row with stable identity.
    - A parse/shape failure is visible as partial and cannot produce successful-zero or complete aggregate evidence.
    - Ordinary worktree output remains deterministic and existing Git-domain contracts pass.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_path_gap</automated>
    <fails_when>non-zero exit, ExUnit reports 0 tests, either hostile path is missing/duplicated/mis-fielded, or malformed transport retains complete status</fails_when>
  </verify>
  <done>Every legal worktree path is represented through a delimiter-safe end-to-end production path or fails closed visibly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind publication to caller cwd and preflight required tools before observation</name>
  <reversibility rating="reversible">CLI path resolution and preflight ordering are local contracts with direct process regressions.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — argument parsing, relation branch, REPO_ROOT cd, OUTPUT_DIR resolution, lock registration, and `yaml_quoted_scalar`
    - test/support/lockspire/release_proof/package_assertions.ex — publication integrity and relation runner fixtures
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md — CR-05 and WR-01 exact acceptance boundaries
  </read_first>
  <behavior>
    - Invocation from a nested directory with `--output name.md --replace` changes only nested/name.md.
    - Invocation from the same directory with `--output ../name.md --replace` changes only the caller-resolved parent/name.md.
    - A missing jq exits immediately with a named diagnostic before fake fetch, GitHub, maintained scan, relation scan, lock, or target change.
  </behavior>
  <action>Capture the physical invocation directory before any repository-root change and resolve the exact output target against it. Carry that one canonical absolute target through parent validation, owned-path registration, lock naming, temporary-file creation, identity checks, no-follow rename, and cleanup. Add nested-caller fixtures with sentinel files at both the caller-resolved and former re-rooted locations, testing `name.md` and `../name.md` under `--replace`. Keep jq as the researched structured-data dependency and move its preflight beside git/python3 ahead of both relation and collection source work. Add PATH-isolated non-GitHub collection and relation cases that assert the named immediate failure, and use fake command sentinels to prove no source observation preceded the preflight.</action>
  <acceptance_criteria>
    - Only the target resolved against invocation cwd changes; neighboring and former repository-root targets retain exact bytes.
    - Target identity, lock-first behavior, and no-follow atomic publication remain enforced on the canonical absolute path.
    - Missing mandatory dependencies are reported before external/local source work and before output mutation.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_path_gap &amp;&amp; bash -n scripts/maintainer/baseline_inventory.sh</automated>
    <fails_when>non-zero exit, ExUnit reports 0 tests, a sentinel outside the caller-resolved target changes, source sentinels show work before dependency failure, or shell syntax fails</fails_when>
  </verify>
  <done>The collector resolves exactly the caller-named target before cd and discovers mandatory dependency absence before observing or mutating state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git worktree porcelain → structured rows | Legal path bytes are untrusted fields that must retain record boundaries and identity. |
| Caller cwd/argument → publication target | Relative names must resolve once to the exact intended filesystem object. |
| Host executable set → source observation | Missing tools must fail before network, repository metadata, locks, or output writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-138-108 | Tampering | worktree row transport | high | mitigate | Use NUL/JSON structured transport, validate cardinality, and exercise real delimiter-bearing paths. |
| T-138-109 | Tampering / Elevation of privilege | relative --replace target | high | mitigate | Resolve against invocation cwd once and preserve canonical target through locking and no-follow rename. |
| T-138-110 | Denial of service / Repudiation | dependency preflight | medium | mitigate | Detect every mandatory executable before any source work and emit a named deterministic error. |
| T-138-111 | Information disclosure | path rendering | high | mitigate | Preserve identity internally and apply centralized display normalization only at final output boundaries. |

## ASVS L1 and Blocking Rule

V5 and V7 govern path validation, structured transport, and fail-visible diagnostics. Every high-severity mitigation must pass before Plan 138-27 begins.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: delimiter-safe worktree record transport; invocation-cwd target resolver; early mandatory-tool preflight.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: new `:phase138_path_gap` focused delegation.
- `test/support/lockspire/release_proof/package_assertions.ex`: new path-boundary assertion entry point plus real tab/newline worktree, nested caller, and dependency-preflight fixtures.
- `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-26-SUMMARY.md`: RED/GREEN evidence, commands, and exact gap outcomes.
- No ledger, schema, migration, package, runtime module, public API, JSON/YAML sidecar, or gitignored mirror is produced.

## Flagged assumptions from spec-less probe

| Candidate | Status | Plan treatment |
|---|---|---|
| BASE-01 unclassified | unresolved | Unknown dependency/path/baseline states produce a named blocking diagnostic. |
| BASE-02 adjacency | flagged assumption | Delimiter-bearing neighboring records retain exact boundaries and IDs. |
| BASE-02 empty | flagged assumption | Complete-zero requires successful valid structured observation. |
| BASE-02 ordering | flagged assumption | Structured records sort deterministically without using rendered text as identity. |
| BASE-02 idempotency | flagged assumption | Repeated hostile-path collection produces equal rows and preserves non-target bytes. |
| BASE-02 concurrency | flagged assumption | Canonical caller-resolved target participates in the existing lock-first transaction. |

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Current non-destructive inventory | 138-26 Tasks 1-2 | COVERED |
| REQ | BASE-01 | Fail-visible baseline/preflight and exact target | 138-26 Task 2 | COVERED |
| REQ | BASE-02 | Every worktree represented safely | 138-26 Task 1 | COVERED |
| RESEARCH | — | Structured transport validates before rendering and preserves exact identity | 138-26 Task 1 | COVERED |
| CONTEXT | D-03, D-06, D-07, D-09, D-10, D-11, D-14, D-15, D-20, D-22, D-24 | Maintainer boundary, completeness, safe paths, identity, proof | 138-26 Tasks 1-2 | COVERED |
| VERIFICATION | worktree delimiters, caller-relative output, jq warning | Current gaps 2 and 6 plus warning | 138-26 Tasks 1-2 | COVERED |

<verification>
  <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_path_gap &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; bash -n scripts/maintainer/baseline_inventory.sh</automated>
  <fails_when>non-zero exit, any ExUnit command reports 0 tests, hostile paths corrupt rows, wrong targets change, dependency failure occurs after source work, or shell syntax fails</fails_when>
</verification>

<success_criteria>
- Legal tab/newline worktree paths are structurally safe and fail closed on malformed transport.
- Relative output semantics are bound to invocation cwd and exact-target replacement remains atomic/no-follow.
- jq or its replacement runtime is validated before every consumer path.
- No schema work or API-coverage opt-out is introduced.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-26-SUMMARY.md` when done.
</output>
