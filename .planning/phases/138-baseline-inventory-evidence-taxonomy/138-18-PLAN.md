---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "18"
type: execute
wave: 18
depends_on: ["138-17"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-01, BASE-02]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BASE-01 D-06 D-07: missing, malformed, or unreadable HEAD/main/origin-main/divergence/porcelain evidence makes the Git baseline and snapshot partial; failed command stdout cannot become an identity value."
    - statement: "BASE-01 unclassified: any baseline condition outside the validated SHA, divergence, and porcelain grammar is surfaced as a flagged source limitation and blocks a clean or synchronized conclusion."
      verification: backstop
    - "BASE-02 adjacency: requested output, lock, and temporary paths are resolved as distinct exact filesystem objects; symlinks and directories cannot alias a regular-file publication target."
    - "BASE-02 empty: an absent regular-file target may be created once under lock, while an existing target requires explicit replacement authority checked under that lock."
    - "BASE-02 concurrency: exactly one absent-target writer can publish; the loser rechecks current target state before any fetch or collection."
    - "BASE-02 idempotency: a rejected target type or detected worktree drift preserves all prior target and neighboring bytes and removes only collector-owned temporary state."
  prohibitions:
    - statement: "Missing Git identities or failed status/divergence commands must not be rendered as complete, clean, or synchronized evidence."
      verification: flagged-unverified
    - statement: "Replacement authority must not permit publication into a directory, through a symlink, or onto a changed working-tree snapshot."
      verification: flagged-unverified
    - statement: "Publication hardening must not add mutation beyond the D-09 metadata fetch and the explicitly authorized regular-file ledger rename."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Validated Git-baseline identities and no-follow regular-file publication transaction."
      contains: "collect_git_baseline"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Missing-ref, malformed-divergence, directory/symlink target, double-writer, and mid-collection drift fixtures."
      exports: ["assert_baseline_inventory_publication_integrity_gaps!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused tagged publication and baseline integrity entry point."
      contains: "closes publication integrity gaps"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: canonical Markdown frontmatter
      via: "collect_git_baseline validates exact object-format SHAs and aggregate_snapshot_status consumes baseline completeness"
      pattern: "collect_git_baseline|aggregate_snapshot_status"
    - from: scripts/maintainer/baseline_inventory.sh
      to: output target
      via: "acquire_output_lock precedes current target type/replace checks and the final same-directory rename"
      pattern: "acquire_output_lock"
---

<objective>
Make Git-baseline completeness and filesystem publication one validated, fail-closed transaction.

Purpose: Prevent false-complete local evidence and prevent replacement from resolving to any object other than the exact regular ledger file.
Output: Hardened baseline capture/publication plus deterministic contracts for verification gaps 3 and 4 and the related review findings.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-13-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fold every baseline identity and command outcome into snapshot completeness</name>
  <reversibility rating="costly">Baseline receipt fields are immutable evidence consumed by the relation verifier and downstream phases.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `collect_git_baseline`, `capture_worktree_projection`, `capture_snapshot_identity`, `aggregate_snapshot_status`, and `render_front_matter`
    - test/support/lockspire/release_proof/package_assertions.ex — baseline fake-Git harness and publication transaction fixtures
    - test/lockspire/release/repository_hygiene_contract_test.exs — existing Git receipt and publication tests
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md — CR-01 missing-ref false-complete reproduction
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — stable worktree and failed-read contracts
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-06, D-07, D-09, D-10, D-23, and D-24
  </read_first>
  <behavior>
    - "Failed rev-parse output is discarded and replaced by one explicit unavailable value, never combined with ref text."
    - "Each SHA matches the repository object format and divergence/status results match their exact grammars."
    - "Any missing/malformed baseline component makes both the baseline receipt and top-level status partial."
  </behavior>
  <action>Create `:phase138_publication_gap` tests through `assert_baseline_inventory_publication_integrity_gaps!/0` for missing HEAD, missing local main, missing remote main, failed `rev-parse` that emits stdout, malformed SHA, malformed divergence, and failed porcelain. Capture each command in an explicit conditional, discard failure stdout, validate the complete object digest and numeric divergence grammar, and feed every identity/status result into the single `aggregate_snapshot_status` rendered in frontmatter. Preserve usable sibling evidence and exact limitations while suppressing clean/synchronized conclusions.</action>
  <acceptance_criteria>
    - Every missing/malformed/failed baseline fixture renders top-level and Git-baseline partial status with one safe limitation.
    - Failure stdout cannot appear in a SHA/frontmatter identity field.
    - Complete SHA/divergence/porcelain controls retain exact values and deterministic receipts.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_publication_gap</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests</fails_when>
  </verify>
  <done>Every published baseline status is derived from validated readable identity, divergence, and worktree evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Restrict replacement to the exact regular-file target under lock</name>
  <reversibility rating="costly">Target-type and no-follow rules protect the canonical ledger from path confusion and overwrite races.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — Task 1 baseline status, path resolution, `acquire_output_lock`, owned-path registry, final rename, and cleanup traps
    - test/support/lockspire/release_proof/package_assertions.ex — `assert_baseline_inventory_publication_transaction!/0`, process barriers, digest checks, and retry helpers
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md — CR-05 directory and symlink destination finding
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — concurrent writer and mid-collection working-tree drift gaps
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-01, D-02, D-07, D-09, D-10, and D-24
  </read_first>
  <behavior>
    - "An existing directory, symlink to a file, symlink to a directory, or non-regular object is rejected even with --replace."
    - "The target parent and basename are validated under the acquired target lock immediately before a no-follow same-directory atomic rename."
    - "Two absent-target writers and every mid-collection worktree drift variant preserve winner/prior bytes and scoped cleanup."
  </behavior>
  <action>Extend the tagged production-process fixture with existing-directory, symlink-to-directory, symlink-to-file, and non-regular targets, capturing target and neighboring digests before and after. Under the held lock, require an existing output to be a non-symlink regular file and an absent output to remain absent until publication; validate the parent and basename again immediately before a rename operation whose semantics cannot treat the destination as a directory. Re-run the synchronized two-writer, tracked modify/delete, untracked addition, failed status, signal cleanup, and retry controls so the tighter target contract preserves Plan 138-13 behavior.</action>
  <acceptance_criteria>
    - Every non-regular or symlink target exits nonzero, preserves target/prior/neighbor contents, and leaves no misnamed temporary ledger inside a directory.
    - Exactly one absent-target writer publishes and the loser performs no fetch/collection without replacement authority.
    - Mid-collection drift and status failure abort before rename; stable regular-file creation and explicit replacement succeed.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_publication_gap</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests</fails_when>
  </verify>
  <done>Only the exact lock-owned regular ledger path can be created or replaced, and it represents one stable working-tree snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git command output → baseline receipt | Missing refs, malformed identities, and failed commands can spoof complete local evidence. |
| Concurrent filesystem actors → output transaction | Target type, symlinks, and races can redirect or overwrite publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-72 | Spoofing / Repudiation | baseline identity capture | high | mitigate | Explicit command branches, object-format validation, grammar checks, and aggregate partial status. |
| T-138-73 | Tampering | output target type | high | mitigate | Reject symlinks/non-regular objects under lock and use exact parent/basename rename semantics. |
| T-138-74 | Tampering / Denial of service | concurrent publication | high | mitigate | Lock-first current-state authorization retained through recheck and rename. |
| T-138-75 | Repudiation | worktree snapshot | high | mitigate | Identical successful porcelain projections immediately before publication. |

## ASVS L1 and Blocking Rule

V4 least authority, V5 path/output validation, V7 failure visibility, and V12 file-resource controls apply. Any high-severity mitigation failure blocks this plan.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: validated baseline identity/status aggregation and no-follow regular-file publication.
- `test/support/lockspire/release_proof/package_assertions.ex`: `assert_baseline_inventory_publication_integrity_gaps!/0` with missing refs, malformed data, path types, writer race, and snapshot drift.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: tagged `:phase138_publication_gap` test.
- No ORM/schema path is modified; the schema-push detector has no applicable target.

<verification>
- `bash -n scripts/maintainer/baseline_inventory.sh`
- `mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_publication_gap`
</verification>

<success_criteria>
- Verification gaps 3 and 4 are closed without regressing BASE-02 concurrency or idempotency.
- Supporting review CR-01 and CR-05 are fixed with exact adversarial fixtures.
- BASE-01 unclassified and BASE-02 adjacency/empty/concurrency probe items are explicitly fail-closed.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-18-SUMMARY.md` when done.
</output>
