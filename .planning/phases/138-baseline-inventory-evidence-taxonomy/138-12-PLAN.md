---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "12"
type: execute
wave: 12
depends_on: ["138-11"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [TRIAGE-01, TRIAGE-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "TRIAGE-01 D-07 D-10 D-16: a pull request can receive merge-ready only when every modern check run and legacy status context is collected through a complete nested connection and is in an explicitly accepted terminal-success state."
    - "TRIAGE-01 D-07 D-14: pending/null, legacy failure, unrecognized non-success, nested pagination beyond 100 contexts, malformed nested pageInfo, and nested query failure remain visible and suppress affirmative disposition authority."
    - "TRIAGE-01 TRIAGE-02 D-07 D-20: duplicate GitHub node identities corroborate only when every normalized decision-bearing field agrees; contradictory copies or an impossible outer page chain make the exact namespace and aggregate partial."
    - "TRIAGE-02 D-10 D-23: issue and pull-request namespace separation, deterministic numeric ordering, and complete-zero issue wording remain unchanged when all outer and nested evidence is valid."
  prohibitions:
    - statement: "Incomplete, pending, contradictory, or unknown GitHub evidence must not produce an affirmative pull-request or issue disposition."
      verification: flagged-unverified
    - statement: "GitHub hardening must not retain raw bodies, comments, check logs, tokens, credentials, environment values, or uncontrolled response payloads."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Complete nested PR check/status-context collection, strict terminal-state evaluation, outer page-chain validation, and contradiction-aware duplicate handling."
      contains: "summarize_pr_checks"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Executable pending, legacy-status, nested-pagination, unknown-conclusion, contradictory-duplicate, and impossible-page-chain fixtures."
      exports: ["assert_baseline_inventory_github_check_completeness!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused end-to-end GitHub check-completeness and duplicate-consistency entry point."
      contains: "requires complete GitHub check evidence"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: GitHub GraphQL
      via: "the pull-request query follows every statusCheckRollup context page and validates outer and nested pageInfo before normalization"
      pattern: "statusCheckRollup|contexts"
    - from: test/support/lockspire/release_proof/package_assertions.ex
      to: scripts/maintainer/baseline_inventory.sh
      via: "fake gh page streams execute the production --scope github path and assert the rendered aggregate verdict"
      pattern: "assert_baseline_inventory_github_check_completeness!"
---

<objective>
Close the GitHub evidence-integrity path from paginated check/status inputs through final proposal rendering.

Purpose: Prevent incomplete, pending, legacy-failing, or contradictory remote evidence from being normalized into a complete aggregate or `merge-ready` proposal.
Output: A production-quality end-to-end GitHub completeness slice plus exhaustive duplicate and pagination regressions for TRIAGE-01 and TRIAGE-02.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-RESEARCH.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-11-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove pending and nested PR checks fail closed end to end</name>
  <reversibility rating="costly">The normalized PR check receipt and merge-ready gate are durable proposal evidence consumed by later maintenance phases.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — GraphQL query near `collect_github_collection`, normalized PR rows, `summarize_pr_checks`, `propose_pr_disposition`, and aggregate disposition-authority gate
    - test/support/lockspire/release_proof/package_assertions.ex — fake `gh` page stream, GitHub fixture runner, and existing aggregate fail-closed assertions
    - test/lockspire/release/repository_hygiene_contract_test.exs — focused delegation pattern
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed truth for pending/null and nested check completeness plus CR-01 reproductions
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-06, D-07, D-10, D-11, D-13 through D-16, and D-24
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md — reasoned no-product-integration declaration to preserve
  </read_first>
  <behavior>
    - "A PR with one successful CheckRun and one null/pending CheckRun renders non-actionable evidence and cannot render merge-ready."
    - "A legacy StatusContext failure, ACTION_REQUIRED, STARTUP_FAILURE, unrecognized non-success conclusion, or missing conclusion produces failed/unknown check evidence and cannot render merge-ready."
    - "A statusCheckRollup containing more than 100 contexts is complete only after every nested page is collected and terminal pageInfo plus totalCount agree with the normalized count."
    - "Nested API, parse, count, cursor, or pageInfo failure produces a stable PR-specific limitation, makes the PR namespace and GitHub aggregate partial, and preserves safe sibling observations as defer-only evidence."
  </behavior>
  <action>Begin with `PackageAssertions.assert_baseline_inventory_github_check_completeness!/0` and a focused ExUnit entry point that reproduce SUCCESS-plus-pending through the production `--scope github` command. Extend the GraphQL shape and fakeable query seam to collect both `CheckRun` and `StatusContext` records with their type-specific state/conclusion, `totalCount`, and nested `pageInfo`; follow every nested cursor rather than relying on the first 100 contexts. Normalize each context into an allowlisted kind/name/state/conclusion tuple while preserving null/pending and unknown values. Refactor `summarize_pr_checks` so `passed` requires a complete nested connection and every context in an explicit accepted terminal-success set; known terminal failures yield `failed`, while pending, missing, unrecognized, malformed, or incomplete evidence yields `unknown`. Feed that result through the existing pre-render row validator and aggregate disposition-authority gate so D-16 permits merge-ready only for fully proven evidence. Keep bodies, comments, raw check logs, headers, credentials, environment values, and tokens outside the query, buffers, diagnostics, fingerprints, and ledger per D-11.</action>
  <acceptance_criteria>
    - Pending/null, legacy failure, ACTION_REQUIRED, STARTUP_FAILURE, unknown conclusion, and mixed successful/nonterminal fixtures each execute the production collector and contain no merge-ready proposal.
    - A fixture with more than 100 contexts traverses every nested page and is complete only when totalCount, cursor progression, and terminal pageInfo agree; missing or malformed nested evidence yields an exact partial receipt and nonzero/partial verdict as designed.
    - A fully complete terminal-success fixture retains the existing safe PR row, deterministic ordering, and proposal-only execution state.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
  </verify>
  <done>One open PR flows from complete nested GraphQL evidence to a justified proposal, while every pending, legacy-failing, unknown, or incomplete check path fails closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject contradictory duplicates and impossible page chains before rendering</name>
  <reversibility rating="costly">Duplicate corroboration and page-chain validity determine whether remote observations can support later maintainer action.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — Task 1 normalized context data, `combine_graphql_pages`, outer pagination validation, duplicate normalization, and PR/issue validators
    - test/support/lockspire/release_proof/package_assertions.ex — Task 1 fixture matrix and current duplicate corroboration assertions
    - test/lockspire/release/repository_hygiene_contract_test.exs — GitHub completeness entry point created by Task 1
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — WR-02 contradictory duplicate and intermediate pageInfo reproduction
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-07, D-10, D-14, D-16, D-20, D-23, and D-24
  </read_first>
  <behavior>
    - "Two copies of one PR node ID corroborate only when all normalized immutable and decision-bearing fields, including check evidence, are identical."
    - "A repeated PR ID with a different head SHA, review decision, update time, mergeability, check tuple, or nested-completeness fact makes the PR namespace and aggregate partial and suppresses affirmative proposals."
    - "Every nonterminal outer page has hasNextPage=true and a usable advancing endCursor; only the final page has hasNextPage=false."
    - "Identical duplicates retain one deterministic row plus corroboration evidence, while distinct PR/issue identities and namespace ordering remain unchanged."
  </behavior>
  <action>Validate the ordered outer page chain before flattening any nodes: require each intermediate page to advertise a next page and usable cursor, reject premature terminal or repeated/non-advancing cursors, and require exactly one terminal page. Replace `unique_by(.id)` collapse with grouping by immutable node ID after normalization. Compare every allowlisted identity, state, head/base SHA, review, mergeability, updated-at, and normalized check-completeness field within a group; only byte-equivalent normalized copies count as corroboration. Route any contradiction to a stable namespace-specific receipt and aggregate partial status before rendering; preserve safe sibling observations as defer-only evidence. Add reversed-page and duplicate-order fixtures to prove deterministic output, and retain separate PR/issue identities even when numeric IDs or titles match.</action>
  <acceptance_criteria>
    - Duplicate-ID fixtures differing in head SHA, check result, review decision, or update time each produce an exact contradiction receipt, aggregate partial status, and no affirmative disposition.
    - Premature-terminal, missing-cursor, repeated-cursor, non-advancing-cursor, and post-terminal-page fixtures fail before node rendering with stable pagination limitations.
    - Identical duplicate, complete-zero issue, namespace collision, numeric ordering, redaction, and aggregate fail-closed fixtures remain green.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy</automated>
  </verify>
  <done>Duplicate nodes corroborate only on exact agreement and every outer/nested page chain proves completeness before remote evidence can become actionable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub GraphQL outer and nested connections → normalized namespace result | Remote pages, context variants, cursor metadata, and duplicates are untrusted completeness inputs. |
| Normalized PR/issue result → proposal renderer | Only complete, internally consistent evidence may acquire disposition authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-47 | Spoofing / Repudiation | nested statusCheckRollup collection | high | mitigate | Enumerate both check variants through complete nested pagination and retain pending/unknown states with exact receipts. |
| T-138-48 | Tampering | `summarize_pr_checks` and disposition gate | high | mitigate | Require complete explicit terminal-success evidence; every other state is failed/unknown and non-actionable. |
| T-138-49 | Tampering / Repudiation | duplicate and outer page normalization | high | mitigate | Validate page-chain progression and require exact normalized agreement before duplicate corroboration. |
| T-138-50 | Information disclosure | GraphQL buffers and diagnostics | high | mitigate | Preserve the D-11 field allowlist, sanitizer, private collector-owned buffers, and cleanup; fixtures assert no uncontrolled payload is rendered. |

## OWASP ASVS L1 Control Mapping

| ASVS family | Applicability and control intent | Named mitigation / task | Acceptance and verification evidence |
|-------------|----------------------------------|-------------------------|--------------------------------------|
| V2 Authentication | GitHub evidence uses the existing authenticated CLI boundary; an auth failure remains unavailable. | T-138-47; Task 1 preserves authenticated read-only collection and exact failure receipts. | Existing auth-failure fixture plus the focused production-CLI matrix. |
| V4 Access Control | Remote evidence is proposal-only and incomplete evidence receives no action authority. | T-138-48; both tasks gate dispositions on complete aggregate evidence. | Pending, nested-failure, contradiction, and page-chain fixtures contain no affirmative proposal. |
| V5 Validation, Sanitization and Encoding | Remote contexts, pageInfo, cursors, and duplicate fields are untrusted. | T-138-47 through T-138-50; both tasks validate before rendering and preserve field allowlists. | Focused ExUnit assertions cover every named variant and payload boundary. |
| V7 Error Handling and Logging | Every incomplete or contradictory condition must remain distinguishable and fail closed. | T-138-47 through T-138-49; stable nested, page-chain, and contradiction receipts. | Production collector exits/statuses and rendered limitations are asserted per fixture. |

## High-Severity Blocking Rule

Any failed or missing mitigation for T-138-47, T-138-48, T-138-49, or T-138-50 blocks Plan 138-12. A pending, incomplete, contradictory, or unsanitized GitHub observation that renders as complete or affirmative is a blocking ASVS L1 failure.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: nested PR check/status-context pagination and normalization; explicit terminal-success evaluation; outer page-chain validator; contradiction-aware duplicate grouping; preserved `--scope github` CLI.
- `test/support/lockspire/release_proof/package_assertions.ex`: exported `assert_baseline_inventory_github_check_completeness!/0`; pending/null, legacy status, unknown conclusion, >100 nested contexts, malformed nested pageInfo, contradictory duplicate, and impossible outer-chain fixtures.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: `baseline inventory collector requires complete GitHub check evidence` focused test.
- No package, runtime, schema, Mix task, mutation query, or new product API surface is created; `COVERAGE.md` remains the reasoned no-external-product-integration declaration.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Current non-destructive evidence inventory | 138-12 | COVERED | GitHub completeness is proven before proposal rendering. |
| REQ | TRIAGE-01 | Every PR has complete current evidence | 138-12 | COVERED | Nested checks, pending states, duplicates, and page chains fail closed. |
| REQ | TRIAGE-02 | Every issue has honest complete/partial evidence | 138-12 | COVERED | Shared outer page-chain and duplicate validation preserves issue semantics. |
| RESEARCH | — | Cursor-paginated allowlisted GitHub collection | 138-12 | COVERED | Extends the researched pattern to nested and contradictory evidence. |
| CONTEXT | D-06–D-07 | Provenance and explicit completeness | 138-12 | COVERED | Nested/outer receipts expose every non-complete state. |
| CONTEXT | D-10–D-11 | Structured pagination and safe fields | 138-12 | COVERED | Complete page chains plus the existing safe-field boundary. |
| CONTEXT | D-13–D-16 | Exact proposals and terminal proof | 138-12 | COVERED | Merge-ready requires complete terminal-success evidence. |
| CONTEXT | D-20, D-23–D-24 | Honest deduplication, calm copy, executable proof | 138-12 | COVERED | Exact-agreement grouping and adversarial production fixtures. |

<verification>
- `bash -n scripts/maintainer/baseline_inventory.sh`
- `mix test test/lockspire/release/repository_hygiene_contract_test.exs`
- `mix test test/lockspire/release_readiness_contract_test.exs`
- `node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy`
</verification>

<success_criteria>
- Pending/null, legacy failure, unknown conclusion, and nested-pagination gaps cannot yield complete check evidence or merge-ready.
- Contradictory duplicate identities and impossible outer page chains make the exact namespace and aggregate partial before rendering.
- Complete PR/issue fixtures retain deterministic ordering, namespace separation, safe fields, honest complete-zero wording, and proposal-only authority.
- Verification gaps 2 and 10 from `138-VERIFICATION.md` are closed with exact adversarial regressions.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-12-SUMMARY.md` when done.
</output>
