---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "33"
type: execute
wave: 33
depends_on: ["138-32"]
files_modified:
  - tools/gsd-capabilities/lockspire-phase-finalizer/capability.json
  - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs
  - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs
  - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
  - .gsd-capabilities.json
autonomous: true
gap_closure: true
requirements: [BASE-01, BASE-02, TRIAGE-01, TRIAGE-02, LOOSE-01]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BASE-01 BASE-02 TRIAGE-01 TRIAGE-02 LOOSE-01 D-06 D-07 D-08 D-24: one tracked project capability activates a halt-capable pre-verifier publication and a halt-capable post-transition validation gate through stock execute-phase."
    - "The lexically ordered capability id lockspire-phase-finalizer places its execute:post step after code-review; pre-verify consumes a current REVIEW.md only after validating its bounded phase-local contract."
    - "The execute:post command is exactly `lockspire-finalize pre-verify`; the execute:complete:post command is exactly `lockspire-finalize post-transition`; both use onError halt and the latter is bound to GSD's host transaction."
    - "The synchronous router validates raw mode, exact subcommand, exact appended `--phase 138`, and project root before invoking Bash with cwd, shell:false, stdio:inherit, and an explicit bounded timeout."
    - "Timeout, signal, spawn error, child nonzero, malformed argv, and wrong phase are distinct nonzero outcomes; none can be interpreted as successful host routing."
    - "The installed execute-phase host unconditionally re-renders execute:post immediately before dispatch, so the capability installed by Plan 33 in this same execute invocation is discovered; a pre-wave or pre-install cached envelope is rejected as stale."
    - "Failure leaves pending host state, `$gsd-progress` blocks later routing, and `$gsd-execute-phase 138 --gaps-only --interactive` retries pending dispatch before plan filters without rerunning completed work."
    - "D-01 through D-24 remain unchanged: lifecycle automation affects timing and proof only, not ledger ownership, taxonomy, maintained-source coverage, safety, accessibility, or proposal-only scope."
  prohibitions:
    - statement: "The capability must not add any host hook, contribution, gate, skill, agent, public Mix task, runtime module, or lifecycle point beyond the two declared steps and one command family."
      verification: flagged-unverified
    - statement: "The router must not build a shell command string, interpolate manifest data, accept another phase/mode, omit its timeout, or use child stdout as routing authority."
      verification: flagged-unverified
    - statement: "The post-transition hook must not republish the ledger or clear the host receipt itself."
      verification: flagged-unverified
  artifacts:
    - path: tools/gsd-capabilities/lockspire-phase-finalizer/capability.json
      provides: "Tracked two-step feature capability ordered after code-review at execute:post."
      contains: "lockspire-finalize post-transition"
    - path: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs
      provides: "Exact two-mode synchronous router with bounded no-shell child execution."
      contains: "spawnSync"
    - path: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs
      provides: "Router tests for exact dispatch plus timeout, signal, spawn-error, and nonzero propagation."
      contains: "ETIMEDOUT"
    - path: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
      provides: "Installed-runtime tests for hook order, host receipt persistence, same-phase recovery, and later-phase blocking."
      contains: "post-completion-finalizer"
    - path: .gsd-capabilities.json
      provides: "Tracked project-scope activation ledger for the consented local bundle."
      contains: "lockspire-phase-finalizer"
  key_links:
    - from: tools/gsd-capabilities/lockspire-phase-finalizer/capability.json
      to: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs
      via: "both safe ref.command strings route through the single confined command family"
      pattern: "pre-verify|post-transition|routeLockspireFinalizeCommand"
    - from: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs
      to: scripts/maintainer/finalize_phase_138_inventory.sh
      via: "fixed Bash argv, verified cwd, shell:false, inherited stdio, and bounded timeout"
      pattern: "spawnSync|finalize_phase_138_inventory.sh"
    - from: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
      to: .gsd-capabilities.json
      via: "installed overlay and ordered hook envelopes are exercised from project scope"
      pattern: "execute:post|execute:complete:post"
---

<objective>
Activate the two Phase 138 writer-boundary commands so one stock execute-phase run publishes truthful evidence before verification and validates it after transition, with durable automatic recovery.

Purpose: Make the corrected Plan 32 lifecycle binding real through a tracked, consented GSD 1.13 project capability rather than a manual finalizer command.
Output: A two-hook manifest, one confined two-mode router, executable unit/lifecycle tests, and an active tracked project installation.

Decision coverage: D-01 and D-02 retain canonical Markdown authority; D-03 and D-04 retain repo-local maintainer ownership; D-05 through D-11 preserve deterministic completeness, provenance, non-destructive collection, and redaction; D-12 through D-20 preserve taxonomy and exact maintained identities; D-21 through D-24 preserve one calm public command and executable proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/config.json
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-32-SUMMARY.md
@scripts/maintainer/finalize_phase_138_inventory.sh
@/Users/jon/projects/gsd-core/docs/reference/capability-manifest.md
@/Users/jon/projects/gsd-core/docs/reference/gsd-capability-command.md
@/Users/jon/.codex/gsd-core/bin/lib/post-completion-finalizer-state.cjs
@/Users/jon/.codex/gsd-core/references/loop-hook-dispatch.md
@/Users/jon/.codex/gsd-core/workflows/execute-phase.md
@/Users/jon/.codex/gsd-core/workflows/progress.md
@/Users/jon/.codex/gsd-core/workflows/transition.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Route both lifecycle boundaries through one bounded command family</name>
  <reversibility rating="reversible">The project capability can be removed through GSD's supported lifecycle and changes no Lockspire runtime or stored product data.</reversibility>
  <files>tools/gsd-capabilities/lockspire-phase-finalizer/capability.json, tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs, tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs</files>
  <read_first>
    - /Users/jon/projects/gsd-core/docs/reference/capability-manifest.md — read the feature envelope, commands, steps, loop points, engine/runtime compatibility, path confinement, and executable-consent schema
    - /Users/jon/.codex/gsd-core/references/loop-hook-dispatch.md — read registry ordering, command allowlist, appended phase/raw behavior, and onError halt semantics
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md — read execute:post code-review ordering and command-step dispatch before normal verification
    - /Users/jon/.codex/gsd-core/workflows/transition.md — read host receipt seal, post-completion command dispatch, pending failure, and receipt completion order
    - /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs — read overlay dispatch, global raw parsing, synchronous router signature, and outer exit projection
    - scripts/maintainer/finalize_phase_138_inventory.sh — preserve Plan 32's exact two-mode argv and nonzero diagnostic contract
  </read_first>
  <behavior>
    - Manifest validation yields one feature capability, one command family, an unconditional execute:post pre-verify step, and an unconditional execute:complete:post post-transition step.
    - At execute:post, active step order is code-review then lockspire-phase-finalizer whenever code-review is active; valid phase-local REVIEW.md is the only review content the pre-verify command may consume.
    - Exact host argv for either mode is `[lockspire-finalize, MODE, --phase, 138]` with raw=true; the child argv is `[scripts/maintainer/finalize_phase_138_inventory.sh, MODE, --phase, 138]`.
    - The child runs once with verified cwd, shell:false, stdio:inherit, timeout 900000, and killSignal SIGTERM.
    - Timeout maps to 124, signal to 125, spawn error to 126, missing status to 127, and an ordinary nonzero child status is preserved; every rejection spawns nothing.
  </behavior>
  <action>Create the tracked bundle under `tools/gsd-capabilities/lockspire-phase-finalizer/`. The manifest uses id `lockspire-phase-finalizer` so lexical registry order follows `code-review`, title `Lockspire Phase Finalizer`, description `Publishes Phase 138 evidence before verification and validates it after transition bookkeeping.`, role feature, version 1.0.0, tier standard, requires `[]`, engines.gsd `>=1.13.0`, runtimeCompat supported `["*"]` and unsupported `[]`, skills/agents/hooks/config/contributions/gates empty, and one `lockspire-finalize` command entry naming the confined CommonJS module/router. Declare two step entries: execute:post uses ref.command `lockspire-finalize pre-verify`, consumes `SUMMARY.md` and `REVIEW.md`, produces the canonical inventory relation, and has onError halt; execute:complete:post uses ref.command `lockspire-finalize post-transition`, consumes that relation and the host transaction receipt, produces terminal authorized bookkeeping, and has onError halt. Do not add `when`: pre-verify still runs when code review is disabled or unavailable, while Plan 32 conditionally consumes only a valid review artifact. Implement synchronous `routeLockspireFinalizeCommand({args,cwd,raw,error})` with an injectable spawn seam. Require raw=true, exact four-element argv, MODE in the two-value set, numeric phase 138, and a real Lockspire root containing the finalizer and Phase 138 directory. Call `spawnSync("bash", ["scripts/maintainer/finalize_phase_138_inventory.sh", MODE, "--phase", "138"], {cwd, shell:false, stdio:"inherit", timeout:900000, killSignal:"SIGTERM"})`. Emit bounded distinct diagnostics and set the specified exit codes for timeout/signal/spawn/missing-status outcomes; preserve ordinary child nonzero and return cleanly only on status zero. Add Node built-in tests for both positive modes, all argv/raw/cwd rejections, fixed invocation options, one spawn, timeout, signal, spawn error, missing status, and multiple nonzero statuses.</action>
  <acceptance_criteria>
    - GSD 1.13 accepts the manifest and renders exactly the two intended halt-capable step hooks.
    - Lexical ordering places pre-verify after code-review, while pre-verify remains active independently of review success.
    - Router tests distinguish every failure class and prove no shell interpolation or uncontrolled output routing exists.
  </acceptance_criteria>
  <verify>
    <automated>node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs</automated>
    <fails_when>Node exits nonzero or runs zero tests, either valid mode fails to spawn exactly once, hook argv/options differ, invalid input spawns, or timeout/signal/spawn/nonzero outcomes collapse to success</fails_when>
  </verify>
  <done>The tracked capability deterministically routes both lifecycle boundaries through one bounded, fail-closed command family.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install the capability and prove durable host recovery end to end</name>
  <reversibility rating="reversible">The project overlay and local executable consent can be removed through the supported capability command.</reversibility>
  <files>tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs, .gsd-capabilities.json</files>
  <read_first>
    - tools/gsd-capabilities/lockspire-phase-finalizer/capability.json — confirm exact id/version/commands/two-step declarations before consent
    - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs — confirm the executable surface matches Task 1's tested source
    - /Users/jon/projects/gsd-core/docs/reference/gsd-capability-command.md — read local-source project install, explicit consent, ledger, list, and activation behavior
    - /Users/jon/.codex/gsd-core/bin/lib/post-completion-finalizer-state.cjs — exercise begin/seal/verify-hooks/status/complete in temporary repositories and preserve its 30-second Git subprocess limit
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md — assert recovery runs before plan filters and exits without plan/verifier/phase.complete reruns
    - /Users/jon/.codex/gsd-core/workflows/progress.md — assert unfinished state blocks ordinary and later-phase routes
    - /Users/jon/.codex/gsd-core/workflows/transition.md — assert pending survives halt and complete removes state only after every post-transition step succeeds
  </read_first>
  <behavior>
    - Supported project installation records explicit executable consent, tracks `.gsd-capabilities.json`, and leaves only an ignored runtime copy under `.gsd`.
    - Hook rendering shows code-review before pre-verify at execute:post and exactly one validation-only post-transition step at execute:complete:post.
    - A pre-install envelope lacks the Lockspire hook, while the mandatory fresh execute:post render after installation discovers it in the same invocation before normal verification; any host contract that reuses the stale envelope fails the test.
    - A normal fixture publishes once, presents real currentness to the verifier boundary, seals host state, validates post-transition, and completes without another ledger commit.
    - Forced post-transition nonzero/timeout/signal leaves status pending; progress blocks later-phase routing, and same-phase execute recovery verifies the unchanged hook hash and redispatches only post-transition.
    - Recovery success clears pending state without rerunning plans, pre-verify, normal verification, phase.complete, transition evolution, or ledger publication; forged receipt/writer/hooks fail closed.
  </behavior>
  <action>Add a Node integration test that resolves the installed GSD launcher/state helper from the current runtime, constructs isolated real Git repositories, and drives the actual schemaVersion 1 begin/seal/verify-hooks/status/complete commands with bounded subprocess timeouts. Combine those fixtures with injected router/finalizer counters and the installed execute-phase/progress/transition contracts to prove the normal two-hook sequence, truthful pre-verifier currentness from Plan 32, one ledger commit total, receipt before/after identity binding, pending retention for child nonzero/timeout/signal, later-phase prevention, same-phase priority retry with identical ordered hook hash, no earlier lifecycle rerun, successful host-owned completion, and rejection of malformed phase/status/mode, forged receipt identities, changed transition bytes, writer/transformation attestation drift, and reordered/replaced hooks. Add a same-invocation discovery regression: capture a pre-install execute:post envelope as the stale control, install the capability, render execute:post again, require the fresh envelope to contain the ordered pre-verify hook, and prove the stale envelope cannot satisfy dispatch. Pin the installed host prerequisite by asserting execute-phase has no pre-wave execute:post snapshot and unconditionally assigns `EXECUTE_POST_HOOKS_JSON=$(gsd_run loop render-hooks execute:post --raw)` in code_review_gate immediately before step dispatch; fail if `${EXECUTE_POST_HOOKS_JSON:-...}` or any equivalent stale-cache reuse remains. This is prerequisite evidence only—do not edit or copy GSD runtime sources into Lockspire. Run `gsd_run capability install ./tools/gsd-capabilities/lockspire-phase-finalizer --scope project --yes --raw`; retain the executable disclosure, do not hand-edit ignored `.gsd` mirrors, and track only the generated root ledger. Require `capability list` active version 1.0.0 and render both points: execute:post must place `code-review` before the exact pre-verify halt step, and execute:complete:post must contain exactly the post-transition halt step. Leave the capability installed for the normal verifier and transition that follow plan execution.</action>
  <acceptance_criteria>
    - Installation uses the supported project lifecycle, the tracked source remains authoritative, both hooks are active, and a fresh same-invocation execute:post render discovers the newly installed pre-verify step before the stock verifier.
    - The stale pre-install envelope is demonstrably insufficient, and installed execute-phase dispatch cannot reuse it or snapshot execute:post before Plan 33 runs.
    - Executable integration proof covers normal completion, every required failure class, durable pending state, same-phase no-rerun recovery, later-phase prevention, and no second publication.
    - The only recovery/operator command is the original Phase 138 gaps-only execute command.
  </acceptance_criteria>
  <verify>
    <automated>gsd_run capability install ./tools/gsd-capabilities/lockspire-phase-finalizer --scope project --yes --raw &amp;&amp; node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; gsd_run capability list --scope project --json | jq -e '.[] | select(.id == "lockspire-phase-finalizer" and .version == "1.0.0" and .status == "active")' &gt;/dev/null &amp;&amp; gsd_run loop render-hooks execute:post --raw | jq -e '([.activeHooks[] | select(.kind == "step") | .capId] | index("code-review") as $c | index("lockspire-phase-finalizer") as $l | ($c != null and $l != null and $c &lt; $l)) and ([.activeHooks[] | select(.capId == "lockspire-phase-finalizer" and .kind == "step" and .ref.command == "lockspire-finalize pre-verify" and .onError == "halt")] | length == 1)' &gt;/dev/null &amp;&amp; gsd_run loop render-hooks execute:complete:post --raw | jq -e '[.activeHooks[] | select(.capId == "lockspire-phase-finalizer" and .kind == "step" and .ref.command == "lockspire-finalize post-transition" and .onError == "halt")] | length == 1' &gt;/dev/null</automated>
    <fails_when>installation or JSON parsing fails, Node exits nonzero or runs zero tests, capability/version/status is wrong, fresh same-invocation rendering does not discover pre-verify, stale-envelope reuse remains possible, hook order or command differs, pending failure can route later, same-phase retry reruns prior stages, forged provenance passes, or publication count differs from one</fails_when>
  </verify>
  <done>The consented project capability is active and its durable two-hook lifecycle is proven recoverable through the same public execute command.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tracked source -> project overlay | GSD validates, discloses, consents, confines, installs, and records the executable surface. |
| Hook envelope -> router argv | Manifest and host-appended values are untrusted until exact mode/phase/raw validation. |
| Router -> Bash finalizer | One fixed argv array crosses with verified cwd, shell:false, inherited stdio, and a deadline. |
| Host receipt -> recovery dispatch | Same-phase recovery requires the exact sealed ordered hook hash before redispatch. |
| Child exit -> phase routing | Distinct nonzero outcomes preserve pending state and block ordinary/later-phase routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-138-139 | Spoofing | capability and hook identity | high | mitigate | Tracked source, explicit consent, active version proof, lexical order check, exact commands, and sealed hook hash. |
| T-138-140 | Tampering | router argv and child invocation | critical | mitigate | Exact closed argv, real project root, constant script path, shell:false, and fixed spawn options. |
| T-138-141 | Repudiation | installation and recovery | high | mitigate | Tracked project ledger, executable disclosure, actual host state commands, and lifecycle counter assertions. |
| T-138-142 | Information disclosure | child/router diagnostics | high | mitigate | Inherited stdio under Plan 31 redaction plus bounded class-only router errors. |
| T-138-143 | Denial of service | timeout, signal, and pending retry | medium | mitigate | Explicit 900-second outer deadline, 30-second host Git helpers, distinct nonzero statuses, and same-phase recovery. |
| T-138-144 | Elevation of privilege | next-phase routing | critical | mitigate | Both hooks halt on error; pending state blocks progress and later execute commands until exact recovery succeeds. |
</threat_model>

## Artifacts this phase produces

- `tools/gsd-capabilities/lockspire-phase-finalizer/capability.json`: tracked two-step capability ordered after code-review.
- `tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs`: tracked exact two-mode router with bounded no-shell execution.
- `tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs`: router rejection and distinct failure-class proof.
- `tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs`: installed host receipt, pending block, same-phase recovery, and no-rerun proof.
- `.gsd-capabilities.json`: tracked project-scope installation ledger.
- No ignored `.gsd` file is source authority; no manual finalizer, pause/resume ceremony, provisional verifier, second publication, or public Lockspire surface is produced.

## Spec-less probe no-silent-drop audit

| Candidate | Explicit disposition |
|---|---|
| BASE-01 unclassified | Missing/inactive pre-verify or post-transition hook halts before false completion. |
| BASE-02 adjacency/empty/ordering/idempotency/concurrency | Exact two-hook order, one publication, timeout, pending retry, and no-second-commit cases are tested. |
| TRIAGE-01 adjacency/empty/ordering/concurrency | Plan 32 runs complete authenticated PR evidence twice before verification. |
| TRIAGE-02 adjacency/empty/ordering | Plan 32 preserves terminal issue pagination and explicit zero semantics. |
| LOOSE-01 unclassified | REVIEW.md and maintained sources are consumed only through explicit validity/completeness rules. |

No-silent-drop equality: 14 candidates = 14 explicit behaviors/backstops.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | One current inventory through one recoverable stock command | 138-30 through 138-33 | COVERED |
| REQ | BASE-01, BASE-02, TRIAGE-01, TRIAGE-02, LOOSE-01 | Pre-verifier publication and post-transition terminal proof | 138-32, 138-33 | COVERED |
| RESEARCH | Repo-local executable boundary, deterministic invocation, no package install | 138-33 | COVERED |
| CONTEXT | D-01 through D-24 | Canonical ownership, provenance, taxonomy, safety, UX, and proof | 138-30 through 138-33 | COVERED |
| VERIFICATION | Truthful currentness before normal verifier | execute:post pre-verify hook | 138-33 Task 1 | COVERED |
| GSD 1.13 | Fresh execute:post discovery plus host post-completion transaction/recovery | same-invocation rerender and receipt lifecycle tests | 138-33 Task 2 | COVERED |
| CONTEXT | Deferred sidecar/database/UI/runtime expansion | none | EXCLUDED |

<verification>
  <automated>node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; jq -e '.entries["lockspire-phase-finalizer"]' .gsd-capabilities.json &gt;/dev/null &amp;&amp; gsd_run capability list --scope project --json | jq -e '.[] | select(.id == "lockspire-phase-finalizer" and .version == "1.0.0" and .status == "active")' &gt;/dev/null &amp;&amp; gsd_run loop render-hooks execute:post --raw | jq -e '([.activeHooks[] | select(.kind == "step") | .capId] | index("code-review") as $c | index("lockspire-phase-finalizer") as $l | ($c != null and $l != null and $c &lt; $l)) and ([.activeHooks[] | select(.capId == "lockspire-phase-finalizer" and .ref.command == "lockspire-finalize pre-verify" and .onError == "halt")] | length == 1)' &gt;/dev/null &amp;&amp; gsd_run loop render-hooks execute:complete:post --raw | jq -e '[.activeHooks[] | select(.capId == "lockspire-phase-finalizer" and .ref.command == "lockspire-finalize post-transition" and .onError == "halt")] | length == 1' &gt;/dev/null</automated>
  <fails_when>any command exits nonzero, Node runs zero tests, tracked activation is absent, fresh same-invocation discovery fails or stale-envelope reuse is accepted, hook order/commands differ, lifecycle recovery reruns prior stages, later routing is not blocked, or post-transition can republish</fails_when>
</verification>

<success_criteria>
- The project capability is tracked and active before the execute:post and normal-verifier boundaries.
- Installed execute-phase freshly re-renders execute:post after Plan 33 installation in the same invocation and discovers pre-verify; stale pre-wave envelopes cannot dispatch.
- Code review precedes the halt-capable pre-verify publication, which makes the canonical ledger genuinely current.
- Post-transition validation consumes the host receipt, never republishes, and leaves pending state on every failure.
- Nonzero, timeout, and signal failures are distinct; the same Phase 138 command retries only pending finalization and blocks Phase 139 meanwhile.
- One `$gsd-execute-phase 138 --gaps-only --interactive` invocation either completes both hooks or halts with durable automatic recovery through that same command.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-33-SUMMARY.md`. Leave the project capability installed and active. Return only `$gsd-execute-phase 138 --gaps-only --interactive` as the next operator command; the same command is also the pending-state recovery path.
</output>
