---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "35"
type: execute
wave: 35
depends_on: ["138-34"]
files_modified:
  - test/support/lockspire/release_proof/package_assertions.ex
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - scripts/maintainer/baseline_inventory.sh
autonomous: true
gap_closure: true
gap_ids: [G-138-97]
requirements: [BASE-01, BASE-02, TRIAGE-01, TRIAGE-02, LOOSE-01]
estimate:
  tokens: 13000
  raw_tokens: 13000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "G-138-97: both named Phase 138 lifecycle contracts pass with a real host-sealed pending receipt and exact matching writer descriptors."
    - "A genuine workflow-byte drift or forged writer descriptor still returns refresh_required; a failed post-transition attempt retains the host-owned pending receipt byte-for-byte and a valid retry succeeds."
    - "The Phase 138 finalizer remains a pre-verify publisher and post-transition read-only validator; the historical canonical ledger and Phase 139 acceptance failures are untouched."
  artifacts:
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "One coherent GSD runtime source for receipt begin/seal, installed workflow fixture, and relation validation."
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused tagged selectors for the two repaired Phase 138 lifecycle contracts."
  key_links:
    - from: test/support/lockspire/release_proof/package_assertions.ex
      to: tools/gsd-capabilities/lockspire-phase-finalizer/post-completion-finalizer-state.cjs
      via: "The receipt helper's resolveCore sees the same GSD_TOOLS path whose workflow bytes are installed into the fixture repository."
    - from: test/support/lockspire/release_proof/package_assertions.ex
      to: scripts/maintainer/baseline_inventory.sh
      via: "The post-transition validator checks the sealed workflow descriptor against the exact fixture runtime and rejects tampered descriptors."
---

<objective>
Close G-138-97 by making the two Phase 138 lifecycle fixtures seal and validate receipts against one exact GSD runtime.

Purpose: Restore executable proof of receipt-bound currentness and host-owned recovery without widening the finalizer's authority.
Output: Passing focused lifecycle contracts and retained negative writer-drift proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-32-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-33-SUMMARY.md
@test/lockspire/release/repository_hygiene_contract_test.exs
@test/support/lockspire/release_proof/package_assertions.ex
@tools/gsd-capabilities/lockspire-phase-finalizer/post-completion-finalizer-state.cjs
@scripts/maintainer/baseline_inventory.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Seal and validate one genuine Phase 138 host receipt with one runtime</name>
  <files>test/support/lockspire/release_proof/package_assertions.ex, test/lockspire/release/repository_hygiene_contract_test.exs</files>
  <behavior>
    - The post-transition relation contract's valid receipt succeeds after begin, transition write, seal, and fixture writer installation.
    - The finalizer recovery contract reaches its forged transformation probe, preserves pending bytes on failure, and succeeds after restoring the original receipt.
    - Both tests fail when the sealed workflow descriptor differs from the runtime workflow bytes used by relation validation.
  </behavior>
  <action>For G-138-97, trace `gsd_tools_path!/0`, `install_receipt_writer_fixture!/2`, the two Phase 138 fixture begin/seal calls, and the helper's `resolveCore` precedence. Capture the single resolved `gsd_tools` absolute path before `begin`; pass it explicitly as `GSD_TOOLS` to every `begin` and `seal` subprocess as well as relation/finalizer subprocesses, and copy workflow files from that same runtime into the fixture repository. Use the host helper to generate writer and transformation attestations; never hand-edit receipt fields or loosen descriptor equality to manufacture a positive case. Pin the fixture's exact descriptor identity before post-transition validation so an ambient shell variable cannot silently change authority. Keep the existing focused test names and tags; make their valid branch and recovery branch both observable. This preserves D-01, D-02, D-06, D-08, D-09, D-15, and D-24.</action>
  <verify><automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_posttransition_relation_gap --only phase138_finalizer_recovery_gap</automated><fails_when>The focused Phase 138 lifecycle mix test command exits with a nonzero status.</fails_when></verify>
  <done>Both named Phase 138 tests pass and exercise a receipt emitted by the host helper from the same exact runtime the validator reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep writer-drift rejection and pending recovery fail closed</name>
  <files>test/support/lockspire/release_proof/package_assertions.ex, test/lockspire/release/repository_hygiene_contract_test.exs, scripts/maintainer/baseline_inventory.sh</files>
  <behavior>
    - A one-byte workflow change after seal returns refresh_required while the original host receipt and repository HEAD remain unchanged.
    - A forged descriptor, changed transformation digest, missing pending receipt, or changed allowedPaths cannot produce receipt_authorized.
    - Restoration of the exact original bytes permits the validation-only retry without a new ledger commit.
  </behavior>
  <action>Extend the two existing Phase 138 fixtures with a narrow workflow-drift negative case using the copied fixture workflow and exact before/after byte checks. Assert pending receipt contents and HEAD before/after every failed call and again after retry; keep diagnostic redaction assertions. If the coherent-runtime positive case still fails, isolate a production validator discrepancy by comparing each host descriptor field to the candidate path and fix only `verify_post_transition_relation`'s path resolution in `scripts/maintainer/baseline_inventory.sh`, keeping exact path/mode/size/SHA-256 comparison and the host receipt as authority. Do not update the immutable ledger, host helper, Phase 139 acceptance fixture, or any release/product surface. Per D-07 and D-11, ambiguous or unsafe evidence remains refresh_required with redacted diagnostics.</action>
  <verify><automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_posttransition_relation_gap --only phase138_finalizer_recovery_gap</automated><fails_when>The focused Phase 138 lifecycle mix test command exits with a nonzero status.</fails_when></verify>
  <done>Both contracts pass with genuine receipt evidence; adversarial drift still fails, preserves pending state, and allows an exact validation-only retry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Host-sealed Git-common-dir receipt to repository validator | The receipt attests writer files and before/after state; fixture setup must preserve the same trust relationship as the live host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-138-35-01 | Tampering | Writer workflow descriptor | high | mitigate | Seal through the host helper, compare exact path/mode/size/SHA-256, and retain a post-seal workflow-drift negative contract. |
| T-138-35-02 | Repudiation | Failed finalizer recovery | medium | mitigate | Assert unchanged pending receipt bytes and HEAD after failure and success only after exact receipt restoration. |
| T-138-35-03 | Information disclosure | Receipt diagnostics | medium | mitigate | Keep existing credential-redaction assertions on failed post-transition output. |
</threat_model>

<verification>
Run only the two Phase 138 selectors above and record executed test count and zero failures in 138-35-SUMMARY.md. Keep the three Phase 139 lifecycle failures assigned to Phase 139; do not use a broad suite result as evidence for G-138-97.
</verification>

<success_criteria>
G-138-97 closes only when both named contracts pass with real host-generated receipt fields and the forged/changed-workflow cases still reject authority.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-35-SUMMARY.md` with command, count, result, and exact changed files.
</output>
