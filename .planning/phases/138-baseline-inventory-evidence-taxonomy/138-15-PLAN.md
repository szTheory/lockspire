---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "15"
type: execute
wave: 15
depends_on: ["138-14"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-01, BASE-02, LOOSE-01]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BASE-02 D-06 D-08 D-10: `--verify-snapshot-relation` recollects the same normalized local branch, tag, and worktree topology represented by the immutable ledger and requires its fingerprint to match."
    - "Adding, deleting, or moving a local branch/tag/worktree after publication, or failing any required topology/status query, emits a sanitized refresh_required row and exits nonzero."
    - "BASE-01 D-07: a failed relation-time `git status --porcelain=v2 --branch` command is unavailable evidence, never an empty clean projection or authorized bookkeeping."
    - "LOOSE-01 D-08 D-16 D-17 D-20: each recognized summary, clean review, passed verification, phase-completion, and transition class uses a positive old-to-new document validator that rejects deletions, empty files, type changes, wrong phase, missing required structure, and unrelated benign text."
    - "Committed and staged/unstaged lifecycle bookkeeping share the same positive semantic validators; unknown or ambiguous content remains disclosed and refresh_required."
  prohibitions:
    - statement: "Post-snapshot authorization must not ignore local branch, tag, worktree, status, source-receipt, or maintained-follow-up drift."
      verification: flagged-unverified
    - statement: "A commit subject, allowed path set, or absence of forbidden keywords must not authorize destructive or semantically unrelated bookkeeping."
      verification: flagged-unverified
    - statement: "The snapshot relation verifier must not mutate refs, worktrees, GitHub objects, maintained records, release-owned files, or the canonical ledger."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Complete Git topology/status receipt comparison and positive lifecycle content validators for committed and working-tree bookkeeping."
      contains: "verify_snapshot_relation"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Local branch/tag/worktree drift, failed-status, deletion, empty-file, wrong-phase, and arbitrary-content classifier histories."
      exports: ["assert_baseline_inventory_snapshot_relation_fail_closed!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused full relation fail-closed and positive lifecycle validation entry point."
      contains: "fails closed on topology and destructive bookkeeping drift"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: "git_receipt_fingerprint in the immutable ledger"
      via: "relation-time normalized branch/tag/worktree recollection uses the same projection and hashing contract as publication"
      pattern: "git_receipt_fingerprint"
    - from: scripts/maintainer/baseline_inventory.sh
      to: ".planning lifecycle documents"
      via: "class-specific parent/current blob parsers prove required frontmatter, headings, values, and monotonic transition semantics"
      pattern: "classify_lifecycle_commit|verify_worktree_projection"
---

<objective>
Make the immutable-snapshot relation complete for local Git topology and positively validate every authorized bookkeeping transition.

Purpose: Ensure currentness fails closed on unreadable or changed local evidence and on destructive or semantically unrelated lifecycle edits that happen to use familiar subjects and paths.
Output: Relation-time Git topology/status fingerprints, positive class validators, and hostile committed/working-tree histories for BASE-01, BASE-02, and LOOSE-01.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-RESEARCH.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-10-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-14-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind relation currentness to local topology and readable status</name>
  <reversibility rating="costly">The currentness relation is the trust contract through which later phases consume the immutable ledger.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — publication-time Git fingerprint construction, `verify_external_snapshot_receipts`, `verify_worktree_projection`, `verify_snapshot_relation`, Git-domain collectors, and read-only classifier output
    - test/support/lockspire/release_proof/package_assertions.ex — temporary snapshot histories, relation runner, current source-receipt fixtures, and linked-worktree helpers
    - test/lockspire/release/repository_hygiene_contract_test.exs — snapshot-currentness and semantic-drift entry points
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed topology-currentness and failed-status truths plus CR-04/CR-06 reproductions
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-06 through D-10, D-14, D-15, and D-24
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-14-SUMMARY.md — quoted-frontmatter and structured rendering interfaces the relation reader must consume
  </read_first>
  <behavior>
    - "An unchanged branch/tag/worktree set reproduces the ledger's normalized Git receipt fingerprint and permits continued lifecycle classification."
    - "Adding, deleting, or moving a local branch, annotated/lightweight tag, or linked worktree yields a domain-specific mismatch row and refresh_required."
    - "Failure or malformed output from branch, tag, worktree, hash, or relation-time status commands yields unavailable evidence and a nonzero relation result."
    - "A clean readable worktree remains authorized only when every topology, origin-main, GitHub, ledger-blob, ancestry, and maintained projection check also succeeds."
  </behavior>
  <action>Start with `PackageAssertions.assert_baseline_inventory_snapshot_relation_fail_closed!/0` histories that accept a stable snapshot, then mutate one local branch, tag, and linked worktree at a time and reproduce the current false authorization. Extract one read-only Git topology projection used by both publication and relation verification: normalize branch, tag, and worktree identities/order/receipt status with the same stable-ID and hashing contract, excluding no real topology. Update `verify_external_snapshot_receipts` or its Git-specific peer to recollect that projection without fetch or mutation and compare it with the ledger's parsed `git_receipt_fingerprint`; a mismatch or any non-complete domain produces a sanitized domain row, `refresh_required`, and nonzero exit. Refactor `verify_worktree_projection` to capture `git status --porcelain=v2 --branch` in an explicit conditional and emit `working_tree|unavailable|refresh_required|...` on command failure before any empty-state branch. Add failed/malformed branch, tag, worktree, stable-ID, and status-query fixtures and assert the verifier remains read-only by comparing refs, worktrees, index, worktree, GitHub fixture state, and ledger bytes before/after.</action>
  <acceptance_criteria>
    - Add/delete/move fixtures for local branches, tags, and linked worktrees each return nonzero refresh_required with the exact changed domain; the stable control remains authorized.
    - Forced failures for status and every topology query cannot return authorized_bookkeeping or an empty clean row and contain a sanitized unavailable diagnostic.
    - Repeated stable relation runs are deterministic and leave refs, worktrees, index, working tree, external fixture state, and ledger bytes unchanged.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
  </verify>
  <done>The relation verifier authorizes currentness only when local branch, tag, worktree, and status evidence is complete and identical to the immutable ledger.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace keyword denial with positive lifecycle transition validators</name>
  <reversibility rating="costly">Lifecycle authorization defines which post-snapshot repository writes can coexist with current evidence without recollection.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — Task 1 relation flow, `added_projection_is_safe`, `classify_lifecycle_commit`, phase-summary/review/verification/completion/transition checks, and `verify_worktree_projection`
    - test/support/lockspire/release_proof/package_assertions.ex — Task 1 relation matrix and existing authorized/hostile lifecycle history builders
    - test/lockspire/release/repository_hygiene_contract_test.exs — relation fail-closed entry point created by Task 1
    - /Users/jon/.codex/gsd-core/workflows/execute-plan.md — executor task/SUMMARY commit shape and required summary frontmatter
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md — review, verification, completion, and tracking order
    - /Users/jon/.codex/gsd-core/workflows/transition.md — PROJECT/STATE transition semantics
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — destructive bookkeeping truth and CR-05 reproductions
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-08, D-12 through D-20, D-23, and D-24
  </read_first>
  <behavior>
    - "Each authorized lifecycle class requires its complete mandatory path set, regular-file status, non-empty parseable content, expected phase identity, required frontmatter/headings, and valid old-to-new state transition."
    - "Deletion, rename, type change, empty file, wrong phase, missing required structure, arbitrary benign replacement, mixed change, or unexpected sibling path yields unknown/refresh_required."
    - "A gaps_found verification becomes superseded only through the exact mapped complete summaries and a new passed verification with zero gaps, zero behavior-unverified items, and no human-needed state."
    - "Staged/unstaged transition bookkeeping is validated against HEAD with the same positive transition parser as committed transition bookkeeping."
  </behavior>
  <action>Replace `added_projection_is_safe` authorization with named positive validators for phase summary, clean/skipped review, passed verification, phase completion, and transition. For each class, inspect `git diff-tree --raw` status and parent/current blobs: require exact mandatory paths, regular non-empty files, exact subject/author/committer contract, parsed expected phase/plan identity, required frontmatter keys and semantic headings, and class-specific monotonic old-to-new values. Reject deletions, type changes, renames, empty/missing files, wrong phase/plan, unchanged or regressed state, arbitrary prose, unexpected paths, and any new/changed pending operational projection. Keep exact supersession proof for the prior gaps report and require the passed replacement's zero-gap contract. Reuse the same validators for index/worktree transition candidates by materializing or reading the HEAD-to-working projection without mutating it. Extend every authorized control with paired deletion, empty-file, wrong-phase, missing-heading, arbitrary-benign-content, and mixed-path near misses; include the exact phase-completion deletion of `.planning/STATE.md` and transition replacement reproductions from CR-05.</action>
  <acceptance_criteria>
    - The phase-completion commit deleting, emptying, renaming, or replacing STATE with benign unrelated text fails refresh_required despite an exact authorized-looking subject.
    - Summary, review, verification, completion, committed transition, and uncommitted transition controls pass only with their exact positive structure and old-to-new semantics; every paired near miss fails.
    - Superseded gaps, pending markers, proposal rows, todo-close, mixed/source changes, author mismatch, merge ancestry, external receipts, topology, and dirty-worktree safeguards remain green.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <done>Only positively proven, non-destructive Phase 138 lifecycle transitions are authorized after the snapshot; familiar subjects or paths alone grant nothing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutable local topology/status → relation verdict | Branches, tags, worktrees, command results, and dirty state can diverge independently of HEAD history. |
| Post-snapshot commits and working-tree edits → lifecycle classifier | Subjects, paths, statuses, blobs, and semantic transitions are untrusted authorization inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-59 | Tampering / Repudiation | relation-time Git topology | high | mitigate | Recollect the same normalized domains, compare the immutable fingerprint, and fail on mismatch or incomplete query. |
| T-138-60 | Repudiation | relation-time status | high | mitigate | Explicitly preserve status command failure as unavailable/refresh_required with nonzero exit. |
| T-138-61 | Spoofing / Tampering | lifecycle class authorization | high | mitigate | Require exact identity, raw change status, mandatory path set, parseable old/new blobs, and positive semantic transition. |
| T-138-62 | Elevation of privilege | committed and uncommitted bookkeeping | high | mitigate | Reuse class validators for both states and reject deletions, type changes, arbitrary content, mixed paths, and pending projection drift. |

## OWASP ASVS L1 Control Mapping

| ASVS family | Applicability and control intent | Named mitigation / task | Acceptance and verification evidence |
|-------------|----------------------------------|-------------------------|--------------------------------------|
| V4 Access Control | Post-snapshot authorization is a least-authority decision over exact evidence, not path or author familiarity. | T-138-61 and T-138-62; Task 2 uses positive class contracts. | Authorized controls and deletion/empty/wrong-phase/arbitrary paired near misses. |
| V5 Validation, Sanitization and Encoding | Git topology, status, diff metadata, frontmatter, and blobs are untrusted. | T-138-59 through T-138-62; both tasks validate normalized data and quote/sanitize disclosures. | Topology/failure fixtures and class-specific parent/current assertions. |
| V7 Error Handling and Logging | Any unreadable, unknown, destructive, or ambiguous state must be disclosed and require refresh. | T-138-59 through T-138-62; stable failure rows and nonzero classifier verdicts. | Focused production relation matrix asserts every exact failure category. |

## High-Severity Blocking Rule

Any failed or missing mitigation for T-138-59, T-138-60, T-138-61, or T-138-62 blocks Plan 138-15. Ignored topology, swallowed status failure, deletion authorization, or path/keyword-only approval is a blocking ASVS L1 failure.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: shared publication/relation Git topology projection; complete `git_receipt_fingerprint` recheck; explicit relation-time status failure row; positive phase-summary/review/verification/completion/transition validators; shared committed and working-tree semantic validation.
- `test/support/lockspire/release_proof/package_assertions.ex`: exported `assert_baseline_inventory_snapshot_relation_fail_closed!/0`; branch/tag/worktree add/delete/move; query/status failure; deletion, empty-file, type-change, wrong-phase, missing-structure, arbitrary-content, and mixed-path histories.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: `baseline snapshot relation fails closed on topology and destructive bookkeeping drift` focused test.
- Existing `--verify-snapshot-relation LEDGER` remains the only CLI surface; no mutation, hook, sidecar, or custom finalizer is added.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Current evidence relation at later HEAD | 138-15 | COVERED | Local topology/status and semantic lifecycle drift are fail-closed. |
| REQ | BASE-01 | Readable exact local state | 138-15 | COVERED | Explicit status failure and currentness checks. |
| REQ | BASE-02 | Every branch/tag/worktree remains current | 138-15 | COVERED | Relation compares the same normalized topology fingerprint. |
| REQ | LOOSE-01 | Maintained lifecycle changes remain accounted for | 138-15 | COVERED | Positive validators and exact gaps supersession. |
| RESEARCH | — | Receipt-first bounded currentness | 138-15 | COVERED | Immutable snapshot plus complete live relation comparison. |
| CONTEXT | D-06–D-10 | Provenance, completeness, revalidation, stable Git interfaces | 138-15 | COVERED | Read-only topology/status projection and fingerprints. |
| CONTEXT | D-12–D-20 | Terminal proof, proposal authority, maintained history | 138-15 | COVERED | Positive old-to-new lifecycle validators and supersession. |
| CONTEXT | D-23–D-24 | Calm failure copy and adversarial proof | 138-15 | COVERED | Exact refresh rows and hostile histories. |

<verification>
- `bash -n scripts/maintainer/baseline_inventory.sh`
- `mix test test/lockspire/release/repository_hygiene_contract_test.exs`
- `mix test test/lockspire/release_readiness_contract_test.exs`
- `mix ci`
- A before/after repository-state assertion proves `--verify-snapshot-relation` does not mutate refs, worktrees, index, files, GitHub fixture state, or ledger bytes.
</verification>

<success_criteria>
- Local branch, tag, and worktree drift or query failure always returns refresh_required.
- Relation-time status failure cannot become a clean authorized projection.
- Every lifecycle authorization is positive and rejects deletion, empty/type-changed/wrong-phase/arbitrary/mixed near misses in both committed and working-tree forms.
- Verification gaps 5, 6, and 7 from `138-VERIFICATION.md` are closed with exact adversarial regressions.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-15-SUMMARY.md` when done.
</output>
