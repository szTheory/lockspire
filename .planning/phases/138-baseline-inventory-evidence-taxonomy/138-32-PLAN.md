---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "32"
type: execute
wave: 32
depends_on: ["138-31"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - scripts/maintainer/finalize_phase_138_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
  - .planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md
autonomous: true
gap_closure: true
requirements: [BASE-01, BASE-02, TRIAGE-01, TRIAGE-02, LOOSE-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BASE-01 BASE-02 TRIAGE-01 TRIAGE-02 LOOSE-01 D-01 D-02 D-06 D-07 D-08 D-24: execute:post publishes one deterministic ledger-only commit before the ordinary verifier, so that verifier observes and can truthfully certify a current canonical Markdown inventory."
    - "The finalizer has exactly two modes: pre-verify performs the only publication; post-transition performs validation only and cannot create or amend a ledger commit."
    - "Post-transition validates GSD's pending Git-common-dir receipt, including phase, point, complete observations, ordered hook envelope/hash, host-emitted `writer` attestation, host-emitted `transformation` attestation/digest, and current bytes before authorizing bookkeeping."
    - "The relation admits only the exact verification and phase.complete commit chain after the pre-verify ledger commit plus the exact transition-written PROJECT.md, STATE.md, ROADMAP.md, and REQUIREMENTS.md state sealed under protocol gsd-transition-v1 and its exact allowedPaths."
    - "A failed post-transition validation leaves host pending state untouched; the same public execute-phase command can retry without another publication or rerunning plans, verification, phase.complete, or transition evolution."
    - "D-03 D-04 D-09 D-11 D-15 D-16: both modes remain repo-local, non-destructive, redaction-safe maintainer tooling and never broaden Phase 138's proposal-only authority."
  prohibitions:
    - statement: "Post-transition must not collect, replace, stage, commit, or amend the canonical ledger or any transition-owned planning file."
      verification: flagged-unverified
    - statement: "A forged, malformed, stale, mismatched, missing, or non-pending host receipt, writer attestation, transformation digest, or allowedPaths set must not authorize bookkeeping."
      verification: flagged-unverified
    - statement: "The pre-verify path must not treat an absent or malformed review report as valid consumed review evidence."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Current-at-publication and receipt-bound authorized-bookkeeping relation verification."
      contains: "authorized_bookkeeping"
    - path: scripts/maintainer/finalize_phase_138_inventory.sh
      provides: "Distinct pre-verify publication and post-transition validation-only commands."
      contains: "post-transition"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Real-repository two-boundary, receipt-forgery, idempotence, and recovery contract fixtures."
      contains: "assert_phase_138_finalizer_contract!"
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused Phase 138 pre-verify and post-transition finalizer test selectors."
      contains: "phase138_finalizer_gap"
    - path: .planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md
      provides: "The canonical ledger made current by the single pre-verify publication."
      contains: "snapshot_relation"
  key_links:
    - from: scripts/maintainer/finalize_phase_138_inventory.sh
      to: scripts/maintainer/baseline_inventory.sh
      via: "pre-verify invokes double collection and current relation proof; post-transition invokes receipt-bound relation proof only"
      pattern: "pre-verify|post-transition|--verify-snapshot-relation"
    - from: scripts/maintainer/finalize_phase_138_inventory.sh
      to: .planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md
      via: "one ledger-only pre-verifier commit followed by validation-only post-transition reuse"
      pattern: "baseline-inventory-2026-08-28.md"
    - from: test/support/lockspire/release_proof/package_assertions.ex
      to: scripts/maintainer/finalize_phase_138_inventory.sh
      via: "real Git repositories exercise both lifecycle modes and adversarial host receipts"
      pattern: "pre-verify|post-transition|post-completion-finalizer.json"
---

<objective>
Publish truthful current evidence before normal verification, then prove that same evidence remains authoritative across exact host-owned completion and transition bookkeeping.

Purpose: Remove the verifier/currentness cycle without inventing provisional verification semantics, while using GSD 1.13's durable post-completion transaction as the sole recovery authority.
Output: A two-mode finalizer, relation support for both writer boundaries, and adversarial executable proof.

Decision coverage: D-01 and D-02 preserve one authoritative Markdown ledger; D-03 and D-04 keep mechanics in maintainer scripts; D-05 through D-11 preserve bounded deterministic provenance, source completeness, fetch safety, and redaction; D-12 through D-20 preserve exact proposal-only taxonomy and maintained-source identity; D-21 through D-24 preserve a calm one-command terminal experience and focused proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/config.json
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-29-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md
@/Users/jon/.codex/gsd-core/bin/lib/post-completion-finalizer-state.cjs
@/Users/jon/.codex/gsd-core/workflows/execute-phase.md
@/Users/jon/.codex/gsd-core/workflows/progress.md
@/Users/jon/.codex/gsd-core/workflows/transition.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one ledger stays current across both lifecycle boundaries</name>
  <reversibility rating="reversible">The relation extension is private evidence logic with hermetic controls; immutable prior ledgers remain in Git history.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — read snapshot identity, current relation, lifecycle closeout, working-tree validation, review/maintained discovery, and redaction functions before editing
    - test/support/lockspire/release_proof/package_assertions.ex — read existing real-repository relation and closeout fixtures, command helpers, and Phase 138 selector conventions before editing
    - test/lockspire/release/repository_hygiene_contract_test.exs — read focused Phase 138 delegation and tag conventions before editing
    - /Users/jon/.codex/gsd-core/bin/lib/post-completion-finalizer-state.cjs — read schemaVersion 1, pending state, complete before/after observations, ordered hook normalization/hash, host-emitted writer/transformation structures and digest serialization, exact allowedPaths, state path, and 30-second Git helper timeouts
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md — read execute:post order, normal verifier, phase.complete bookkeeping, and priority-first pending recovery
    - /Users/jon/.codex/gsd-core/workflows/transition.md — read receipt begin/seal/complete and exact transition writer order
  </read_first>
  <behavior>
    - RED: the existing relation cannot be genuinely current before verification and also authorize the later host-sealed transition state without republishing.
    - GREEN: a ledger-only child created at execute:post is current at its actual HEAD, before the ordinary verifier runs.
    - GREEN: after passed verification and phase.complete commits, post-transition relation succeeds only for the exact pending host receipt, host-emitted writer/transformation attestations, and current bytes it seals.
    - Same-cardinality closeout forgery, unexpected commits/paths, receipt forgery, tampered writer descriptor, writer drift, transformation digest mismatch, changed/reordered allowedPaths, hook-order/hash drift, extra dirt, and changed bytes all remain non-authoritative.
  </behavior>
  <action>Extend the production relation with two explicit boundaries. At the pre-verify boundary, accept one ledger-only child whose candidate was built from two complete independent observations of the same source state; verify the new HEAD, exact one-parent/one-path commit, candidate identity, refs, source receipts, review provenance, and clean or explicitly recorded worktree state before returning current. Treat `138-REVIEW.md` as consumed evidence only when it is a bounded regular non-symlink at the deterministic phase path, its frontmatter phase/status/schema is valid for the active code-review predecessor, and its identity is included in both observations; when absent or invalid, never parse its body or claim it was consumed, and emit the existing explicit unavailable/partial source semantics per D-07. At the post-transition boundary, read the host receipt only from the absolute Git-common-dir `gsd-lifecycle/post-completion-finalizer.json`; require schemaVersion 1, status pending, phase 138, point execute:complete:post, an exact ordered normalized hook envelope and SHA-256, and exact current equality with every `after` identity and porcelainSha256 field. Treat `writer` as host-emitted begin-time attestation: require protocol `gsd-transition-v1`, exact allowedPaths for PROJECT.md/STATE.md/ROADMAP.md/REQUIREMENTS.md, and exact path/mode/size/SHA-256 descriptors for transition.md, execute-phase.md, and post-completion-finalizer-state.cjs; re-observe those files to reject writer drift, but never infer writer authority from live hashes when the attestation is missing. Require `transformation` as host-emitted seal-time attestation with the same protocol/allowedPaths and recompute its deterministic SHA-256 over protocol, writer, and the complete before/after observations using the helper's exact serialization contract. Prove each `before` identity from the receipt's HEAD, validate the bounded commit chain from the pre-verify publication through passed `138-VERIFICATION.md` and repository-owned phase.complete bookkeeping, and allow current worktree differences only for the regular allowedPaths whose before/after identities and semantic lifecycle transitions match the attestation. Preserve the Plan 30 exact closeout set and Plan 31 redaction matrix. Add real-repository positive and adversarial tests for truthful pre-verifier currentness, exact post-transition success, all receipt fields, same-cardinality substitutions, unexpected commits/paths, staged or untracked state, symlinks, hook reorder/hash mismatch, tampered writer descriptors, changed writer bytes, transformation digest mismatch, allowedPaths removal/addition/reorder, and diagnostic redaction.</action>
  <acceptance_criteria>
    - The ordinary verifier can run after pre-verify and observe a genuinely current canonical relation at the actual ledger-publication HEAD.
    - The same ledger reaches authorized_bookkeeping after transition only when exact repository-owned commits and the host-sealed before/after state agree.
    - No provisional/readiness verification state, second ledger publication, or broader dirty-worktree authority is introduced.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_preverify_relation_gap --only phase138_posttransition_relation_gap --only phase138_closeout_gap --only phase138_redaction_gap</automated>
    <fails_when>shell syntax fails, ExUnit exits nonzero or runs zero tests, pre-verifier currentness is not real, any forged writer/transformation/digest/allowedPaths evidence is authorized, a second ledger commit is required, or raw review/credential/receipt data reaches output</fails_when>
  </verify>
  <done>One canonical ledger is provably current before verification and remains relation-authorized through only the exact host-receipted transition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pre-verify publication and post-transition validation-only modes</name>
  <reversibility rating="costly">The publication commit becomes downstream evidence authority, so later correction is additive rather than destructive.</reversibility>
  <files>scripts/maintainer/finalize_phase_138_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex, .planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — invoke only Task 1's production collection and relation interfaces
    - test/support/lockspire/release_proof/package_assertions.ex — reuse real Git repositories and subprocess helpers rather than duplicating production classifiers
    - test/lockspire/release/repository_hygiene_contract_test.exs — add focused pre-verify, post-transition, and recovery assertions under the existing contract suite
    - /Users/jon/.codex/gsd-core/bin/lib/post-completion-finalizer-state.cjs — consume the host receipt as read-only authority and preserve pending state for host-owned completion/retry
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md — pin same-phase preparing/pending recovery before plan filters and the no-rerun exit contract
    - /Users/jon/.codex/gsd-core/workflows/progress.md — pin ordinary and later-phase routing blockage while host state exists
  </read_first>
  <behavior>
    - `pre-verify --phase 138` performs two complete agreeing collections and creates exactly one ledger-only publication whose production relation is current before returning zero.
    - Repeating pre-verify against an already-current ledger is a verified no-op and creates no second commit.
    - `post-transition --phase 138` reads pending host state and validates its host-emitted writer/transformation attestations plus the existing ledger relation; it never recollects, republishes, stages, commits, amends, or removes host state.
    - Post-transition failure returns nonzero with the host receipt still pending; a same-phase retry can succeed without rerunning plans, the verifier, phase.complete, or transition evolution.
    - A later phase, wrong phase, missing/malformed receipt, hook mismatch, tampered writer/transformation/allowedPaths, unexplained bookkeeping, timeout-derived incomplete evidence, or source/writer drift returns nonzero.
  </behavior>
  <action>Replace the single late-publication design with strict `pre-verify` and `post-transition` subcommands plus exact `--phase 138` parsing. `pre-verify` runs after all gap-plan summaries and code review: assert primary checkout and expected phase state, conditionally validate the current review artifact as defined in Task 1, create two separately allocated complete candidates, normalize only declared non-semantic collection timestamps, require semantic equality, atomically replace the canonical path, and commit exactly that path with a fixed subject. Immediately run the production relation at the new HEAD and return zero only for truthful currentness; on retry, revalidate the already-current relation and return without a commit. `post-transition` must be validation-only: open the mode-0600 regular host state through the resolved Git common directory without following symlinks, validate the complete pending receipt, active hook identity, exact host-emitted `writer`, and exact host-emitted `transformation` protocol/allowedPaths/digest, prove the bounded verifier/phase.complete chain since the recorded pre-verify publication, re-observe writer files and sealed transition bytes/status, and require production authorized_bookkeeping. Never replace host attestation with post-hoc inference from installed hashes, create a private receipt, consume/remove the host receipt, or repair lifecycle files; GSD owns begin/seal/complete and leaves pending state on nonzero. Extend hermetic tests to run the normal two-hook path and prove one publication, truthful verifier input, post-transition success, idempotent pre-verify, no post-transition publication, tampered writer descriptor/current writer, transformation digest, and allowedPaths rejection, pending preservation after failure, same-phase successful retry, and installed workflow contracts that recovery precedes plan filtering while progress blocks later-phase routing.</action>
  <acceptance_criteria>
    - Normal execution creates exactly one ledger-only commit before verification and performs validation only after transition.
    - Every post-transition failure preserves the pending host transaction for the documented same-phase execute command.
    - Recovery tests prove no plan, verifier, phase.complete, transition evolution, or second ledger publication occurs on pending redispatch.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/finalize_phase_138_inventory.sh &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_finalizer_gap --only phase138_finalizer_recovery_gap --only phase138_preverify_relation_gap --only phase138_posttransition_relation_gap</automated>
    <fails_when>shell syntax fails, ExUnit exits nonzero or runs zero tests, pre-verify publishes more than once, post-transition changes Git state, pending state is lost on failure, retry reruns an earlier lifecycle stage, later-phase routing is not blocked, or any hostile receipt returns success</fails_when>
  </verify>
  <done>The two-mode finalizer publishes once before truthful verification and makes post-completion validation safely recoverable through the host transaction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| execute:post state -> pre-verify publication | Two independent complete observations must agree before one ledger-only commit. |
| Code-review output -> maintained evidence | Review bytes are consumed only after bounded path, file, schema, status, and identity validation. |
| Git-common-dir receipt -> post-transition authority | Host state is untrusted until schema, mode, phase, hooks, hashes, identities, and current bytes all agree. |
| Completion commits -> authorized bookkeeping | Only deterministic verifier and phase.complete changes between publication and receipt-before HEAD are accepted. |
| Finalizer result -> host routing | Nonzero leaves pending state and blocks next-phase routing; zero requires the production relation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-138-133 | Spoofing | host writer/transformation attestation | critical | mitigate | Validate Git-common-dir location, regular mode-0600 state, schema/phase/point/status, gsd-transition-v1 writer descriptors, exact allowedPaths, transformation digest, hook envelope/hash, and current identities. |
| T-138-134 | Tampering | publication and completion chain | critical | mitigate | Double collection, one-parent/one-path commit, exact commit/path semantics, and current byte re-observation. |
| T-138-135 | Repudiation | verifier/phase.complete provenance | high | mitigate | Bind passed report, fixed repository-owned closeout transforms, commit ancestry, and host receipt before HEAD. |
| T-138-136 | Information disclosure | review, receipt, and diagnostics | critical | mitigate | Validate before reading, hash exact bytes, and emit only bounded classifications through the shared redaction policy. |
| T-138-137 | Denial of service | failed post-transition finalizer | medium | mitigate | Preserve host pending state and use same-phase priority recovery; helper Git subprocesses remain bounded at 30 seconds. |
| T-138-138 | Elevation of privilege | post-transition success | critical | mitigate | Validation-only mode returns zero solely for receipt-bound authorized_bookkeeping and cannot publish or mutate host state. |
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: two-boundary relation proof grounded in the pre-verify publication and host-sealed post-transition state.
- `scripts/maintainer/finalize_phase_138_inventory.sh`: strict `pre-verify` publication and `post-transition` validation-only commands.
- `test/support/lockspire/release_proof/package_assertions.ex`: normal, adversarial, pending, same-phase retry, and later-phase block fixtures.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: focused finalizer and recovery selectors.
- `.planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md`: one canonical inventory made current before the verifier and never republished after transition.
- No private receipt, provisional verifier status, manual checkpoint, public Lockspire API, ignored runtime mirror authority, destructive disposition, or second operator command is produced.

## Spec-less probe no-silent-drop audit

| Candidate | Explicit disposition |
|---|---|
| BASE-01 unclassified | Unsupported publication or host-receipt state remains refresh_required. |
| BASE-02 adjacency/empty/ordering/idempotency/concurrency | Exact topology, double collection, one publication, validation-only retry, and no-second-commit behavior are tested. |
| TRIAGE-01 adjacency/empty/ordering/concurrency | Existing authenticated pagination/OID contracts run in both pre-verify observations. |
| TRIAGE-02 adjacency/empty/ordering | Existing issue namespace, terminal zero, and deterministic ordering contracts run in both observations. |
| LOOSE-01 unclassified | Review and maintained records are consumed only with explicit complete/partial/unavailable provenance. |

No-silent-drop equality: 14 candidates = 14 explicit behaviors/backstops.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | One current non-destructive inventory visible to the stock verifier and retained through transition | 138-32 | COVERED |
| REQ | BASE-01, BASE-02, TRIAGE-01, TRIAGE-02, LOOSE-01 | Complete current evidence plus exact lifecycle proof | 138-32 Tasks 1-2 | COVERED |
| RESEARCH | Bash ownership, double observation, immutable Markdown, explicit receipts | 138-32 | COVERED |
| CONTEXT | D-01 through D-24 | Ownership, provenance, taxonomy, redaction, UX, and proof | 138-30 through 138-33 | COVERED |
| VERIFICATION | Verifier/currentness cycle and late-publication gap | execute:post publication plus execute:complete:post validation | 138-32 Tasks 1-2 | COVERED |
| GSD 1.13 | Host transaction, pending recovery, and later-phase block | receipt-bound validation and contract tests | 138-32 Task 2 | COVERED |
| CONTEXT | Deferred sidecar/database/UI/runtime expansion | none | EXCLUDED |

<verification>
  <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; bash -n scripts/maintainer/finalize_phase_138_inventory.sh &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_finalizer_gap --only phase138_finalizer_recovery_gap --only phase138_preverify_relation_gap --only phase138_posttransition_relation_gap --only phase138_closeout_gap --only phase138_redaction_gap</automated>
  <fails_when>any command exits nonzero, ExUnit runs zero tests, the verifier cannot observe genuine currentness, publication count differs from one, post-transition mutates Git or host state, pending recovery reruns prior lifecycle work, later-phase routing proceeds, or hostile provenance is authorized</fails_when>
</verification>

<success_criteria>
- The execute:post path creates the phase's only new ledger commit and proves currentness before the ordinary verifier.
- Passed verification and phase.complete are validated as deterministic bounded bookkeeping rather than bypassed by a provisional status.
- The execute:complete:post path reads the host's pending receipt and proves authorized bookkeeping without recollection or publication.
- Failure leaves durable pending state; the same Phase 138 execute command retries only the finalizer and blocks all later-phase routing meanwhile.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-32-SUMMARY.md`. Plan 33 installs both hooks; the only operator command remains `$gsd-execute-phase 138 --gaps-only --interactive`.
</output>
