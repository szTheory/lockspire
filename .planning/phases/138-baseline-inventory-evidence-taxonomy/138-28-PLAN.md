---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "28"
type: execute
wave: 28
depends_on: ["138-27"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-02, TRIAGE-01, TRIAGE-02, LOOSE-01]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BASE-02 TRIAGE-01 TRIAGE-02 LOOSE-01 D-11 D-24: AWS access keys, Slack tokens, JWTs, PEM private-key markers, bearer credentials, and high-confidence opaque credential shapes are conservatively redacted before any Markdown, YAML, receipt, limitation, URL, or diagnostic rendering."
    - "Every untrusted rendered-field domain—branch, tag, worktree, PR, issue, maintained record, source receipt, and limitation—uses one centralized credential predicate and emits only [REDACTED] or a safe non-reversible reference for matched values."
    - "Benign identifiers and prose remain useful; invalid UTF-8, Markdown metacharacters, deterministic ordering, stable IDs, and proposal-only dispositions retain their prior contracts."
    - statement: "LOOSE-01 unclassified remains unresolved: unfamiliar or ambiguous high-entropy credential-like content is treated conservatively and a source that cannot be rendered safely becomes partial."
      verification: backstop
  prohibitions:
    - statement: "Durable ledgers, test diagnostics, and summaries must not contain raw credentials, private-key material, uncontrolled bodies, logs, or environment values."
      verification: flagged-unverified
    - statement: "Redaction must not alter canonical internal identity before stable evidence IDs and deduplication are computed."
      verification: flagged-unverified
    - statement: "Safe-field allowlisting must not be broadened to compensate for incomplete credential detection."
      verification: flagged-unverified
    - statement: "This plan must not add runtime/package API, schema, sidecar, destructive action authority, or GitHub coverage opt-out."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "One centralized conservative credential detector used by every final rendering boundary."
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused credential-boundary contract entry point."
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Credential-shape matrix across every untrusted rendered-field domain plus benign controls."
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: test/support/lockspire/release_proof/package_assertions.ex
      via: "production collector output scanned for raw fixture secrets and required redaction markers"
      pattern: "redacted_display_value|markdown_value|yaml_quoted_scalar"
---

<objective>
Close the durable evidence redaction boundary across every untrusted field shape and source domain.

Purpose: Prevent common and opaque credential forms from entering canonical evidence while preserving exact internal identity and useful safe metadata.
Output: Centralized detector, complete cross-domain adversarial fixtures, and full regression proof; no ledger replacement yet.

Decision coverage: D-05 compact safe rendering; D-06 provenance; D-07 redaction failure is not completeness; D-11 strong redaction; D-14 stable identity/row fields; D-20 identity before display; D-22 accessible deterministic Markdown; D-23 calm limitation copy; D-24 focused proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-RESEARCH.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-26-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-27-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Redact one credential shape through every collection domain</name>
  <reversibility rating="reversible">The detector is a private display-boundary policy whose tests pin compatibility and false-positive controls.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `redacted_display_value`, Markdown/YAML normalizers, Git/GitHub/maintained renderers, receipts, and limitations
    - test/support/lockspire/release_proof/package_assertions.ex — hostile branch/tag/worktree, GitHub, maintained, and encoding fixtures
    - test/lockspire/release/repository_hygiene_contract_test.exs — focused delegation/tag conventions
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md — CR-04 exact missing shapes and sinks
  </read_first>
  <behavior>
    - AWS, Slack, JWT, PEM, bearer, and opaque credential fixtures never appear raw in output.
    - Each shape is exercised in branch, tag, worktree, PR title/URL-safe field, issue field, maintained subject/rationale/reference, receipt, and limitation sinks where that domain accepts untrusted text.
    - Stable IDs continue to derive from exact canonical subjects before display redaction, so two distinct secrets may render alike without deduplicating incorrectly.
  </behavior>
  <action>Create a named credential matrix with syntactically valid representative values for AWS access-key IDs, Slack token families, three-segment JWTs, PEM private-key boundaries, bearer authorization values, existing GitHub tokens, and opaque base64/base64url-like values of at least 32 characters containing upper-case, lower-case, and numeric characters. Inject each class through real production collector fixture paths for all applicable untrusted rendering domains and assert raw values are absent from both stdout and ledger bytes. Then centralize detection in one function called after internal stable-ID/deduplication work but before every Markdown/YAML/receipt/limitation/diagnostic sink. Exempt only separately validated public formats covered by Task 2 controls. For a matched value render `[REDACTED]` or a safe non-reversible reference only when the row requires correlation. If a value cannot be rendered safely, downgrade the exact source rather than publishing it. Preserve D-10 safe-field allowlists instead of relying on the detector to sanitize broader input.</action>
  <acceptance_criteria>
    - Every credential class is absent raw from every applicable sink and produces the expected safe marker/reference.
    - Stable IDs remain distinct for distinct canonical subjects even when display values collapse to one marker.
    - Unsafe rendering cannot coexist with complete source status.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_redaction_gap</automated>
    <fails_when>non-zero exit, ExUnit reports 0 tests, any raw matrix value appears, any applicable domain bypasses the detector, or distinct internal identities collapse</fails_when>
  </verify>
  <done>One production rendering policy prevents the complete credential matrix from crossing any durable or diagnostic boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound conservative detection with benign controls and full source regressions</name>
  <reversibility rating="reversible">False-positive thresholds are test-pinned and can be adjusted without changing stored schema or public APIs.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — completed Task 1 detector and all callers
    - test/support/lockspire/release_proof/package_assertions.ex — existing Unicode, Markdown, ordering, duplicate, archive, and proposal-only assertions
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md — safe-field allowlisting and redaction capability
  </read_first>
  <behavior>
    - Ordinary SHAs, evidence IDs, paths, release names, URLs, and prose do not trigger opaque-secret redaction solely because they are long.
    - Invalid UTF-8 remains percent-escaped, Markdown/YAML remain valid, and existing credential keywords remain redacted.
    - Full Git, GitHub, maintained, publication, and relation contract suites remain green.
  </behavior>
  <action>Add boundary controls around the opaque-secret heuristic: ordinary 40/64 Git object IDs, expected evidence IDs, UUID-like/public identifiers, version strings, known public URLs, and natural prose must retain useful display. Pin length/alphabet/context rules tightly enough to be conservative without hiding all long identifiers, and document the chosen private function contract in source comments without embedding fixture secret literals in production. Rerun the complete repository-hygiene contract plus API coverage and inspect generated fixture ledgers for every raw secret sentinel. Keep GitHub full coverage, exact source completeness, Unicode/Markdown/YAML encoding, deterministic ordering, and proposal-only taxonomy unchanged.</action>
  <acceptance_criteria>
    - Benign controls remain visible and credential controls remain redacted.
    - Complete focused and full contracts pass without skipped/zero-test results.
    - API coverage remains 8/8 and no sidecar/schema/public surface is added.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy &amp;&amp; bash -n scripts/maintainer/baseline_inventory.sh</automated>
    <fails_when>non-zero exit, ExUnit reports 0 tests, benign public identifiers are redacted, raw secret sentinels appear, API coverage reports block=true, or shell syntax fails</fails_when>
  </verify>
  <done>Conservative redaction closes credential leaks without erasing safe evidence or weakening any collection domain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Untrusted source text → internal canonical identity | Exact source bytes are needed for IDs but cannot be emitted blindly. |
| Canonical identity → Markdown/YAML/diagnostics | Every public/durable sink requires centralized credential detection and encoding. |
| Test fixture output → assertions/logs | Failure diagnostics must avoid echoing raw secret values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-138-116 | Information disclosure | rendered source fields | critical | mitigate | Centralize shape-aware credential detection before every sink and scan outputs for raw sentinels. |
| T-138-117 | Tampering | redaction/dedup ordering | high | mitigate | Compute stable identity from canonical input before display redaction and test distinct collapsed displays. |
| T-138-118 | Denial of service | opaque-secret false positives | medium | mitigate | Pin public identifier/prose controls and bounded context-sensitive heuristic rules. |
| T-138-119 | Repudiation | redacted source completeness | high | mitigate | Downgrade sources that cannot be safely rendered and retain named limitations/recheck proof. |

## ASVS L1 and Blocking Rule

V5 and V7 apply to untrusted field validation, output encoding, redaction, and fail-visible source status. The critical disclosure boundary blocks republication until all domains pass.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: centralized credential-shape predicate and complete rendering-boundary routing.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: new `:phase138_redaction_gap` delegation.
- `test/support/lockspire/release_proof/package_assertions.ex`: credential matrix, all-domain fixtures, identity-collapse assertion, and benign false-positive controls.
- `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-28-SUMMARY.md`: exact classes/sinks tested and full gate results without raw secret values.
- No ledger, schema, sidecar, dependency, public/runtime API, or mutation authority is produced.

## Flagged assumptions from spec-less probe

| Candidate | Status | Plan treatment |
|---|---|---|
| BASE-02 adjacency | flagged assumption | Adjacent hostile/benign rows retain separate IDs after display redaction. |
| BASE-02 ordering | flagged assumption | Sorting uses canonical structured identity, not collapsed display text. |
| BASE-02 idempotency | flagged assumption | Repeated collection produces identical safe output. |
| TRIAGE-01 adjacency | flagged assumption | PR fields remain bound to the correct exact object after redaction. |
| TRIAGE-01 ordering | flagged assumption | PR numeric ordering survives display collapse. |
| TRIAGE-02 adjacency | flagged assumption | Issue namespace/identity remains distinct after redaction. |
| TRIAGE-02 ordering | flagged assumption | Issue numeric ordering survives display collapse. |
| LOOSE-01 unclassified | unresolved | Unsafe unknown credential-like content becomes redacted or partial, never silently emitted. |

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Safe current inventory | 138-28 Tasks 1-2 | COVERED |
| REQ | BASE-02 | Safe ref/worktree rows | 138-28 Task 1 | COVERED |
| REQ | TRIAGE-01 | Safe PR fields | 138-28 Task 1 | COVERED |
| REQ | TRIAGE-02 | Safe issue fields | 138-28 Task 1 | COVERED |
| REQ | LOOSE-01 | Safe maintained records | 138-28 Task 1 | COVERED |
| RESEARCH | redaction and safe-field allowlist | Central detector without broadened inputs | 138-28 Tasks 1-2 | COVERED |
| CONTEXT | D-05, D-06, D-07, D-10, D-11, D-14, D-20, D-22, D-23, D-24 | Safe useful deterministic evidence | 138-28 Tasks 1-2 | COVERED |
| VERIFICATION | missing credential shapes/domains | Current gap 5 | 138-28 Tasks 1-2 | COVERED |

<verification>
  <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_redaction_gap &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy &amp;&amp; bash -n scripts/maintainer/baseline_inventory.sh</automated>
  <fails_when>non-zero exit, any ExUnit command reports 0 tests, raw credential sentinels appear, benign controls are hidden, API coverage reports block=true, or shell syntax fails</fails_when>
</verification>

<success_criteria>
- Every named credential class and untrusted rendered domain is covered by executable production-CLI fixtures.
- Internal identity remains exact; redaction occurs only at rendering boundaries.
- Benign evidence remains useful and every prior encoding/completeness contract stays green.
- No schema work or hidden API opt-out is introduced.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-28-SUMMARY.md` when done.
</output>
