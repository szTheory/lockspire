---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "13"
type: execute
wave: 13
depends_on: ["138-12"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-01, BASE-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BASE-02 D-02 D-07 D-24 concurrency: the target lock is acquired before overwrite authorization, fetch, snapshot capture, or collection, and remains owned through atomic rename and cleanup."
    - "Two absent-target writers that begin concurrently cannot both publish; after the winner renames and releases, the loser re-evaluates existence under lock and refuses without --replace."
    - "BASE-01 D-06 D-07: the exact porcelain-v2 working-tree snapshot is captured after lock acquisition and re-observed immediately before rename, excluding only named collector-owned paths."
    - "Tracked modification, tracked deletion, untracked addition, status-query failure, or any other non-owned worktree drift aborts publication nonzero, preserves prior target bytes, and leaves a source-specific limitation."
  prohibitions:
    - statement: "Overwrite authorization must not be decided outside the target lock or reused after another writer has published."
      verification: flagged-unverified
    - statement: "A changed or unreadable working tree must not be represented by an earlier clean receipt or published as one stable snapshot."
      verification: flagged-unverified
    - statement: "Publication hardening must not add repository mutation beyond the D-09 metadata fetch and the explicitly requested canonical output replacement."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Lock-first publication transaction and fail-closed worktree identity recheck."
      contains: "verify_snapshot_identity_unchanged"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Synchronized absent-target double-writer and modify/delete/untracked/status-failure publication fixtures."
      exports: ["assert_baseline_inventory_publication_transaction!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused lock-first and worktree-stability production-process entry point."
      contains: "publishes under one lock-stable working-tree snapshot"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: "${OUTPUT}.lock and atomic rename"
      via: "path resolution, lock acquisition, overwrite decision, capture, recheck, and rename form one exclusive transaction"
      pattern: "LOCK_DIR|verify_snapshot_identity_unchanged"
    - from: test/support/lockspire/release_proof/package_assertions.ex
      to: scripts/maintainer/baseline_inventory.sh
      via: "explicit writer barriers and stateful fake Git drive both races through production processes"
      pattern: "assert_baseline_inventory_publication_transaction!"
---

<objective>
Make canonical publication one exclusive, worktree-stable transaction.

Purpose: Prevent a second absent-target writer from overwriting the winner and prevent any ledger from carrying a stale clean-worktree claim after local state changes or becomes unreadable.
Output: Lock-first output authorization, immediate pre-rename worktree revalidation, and deterministic process-level regressions for BASE-01 and BASE-02.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-RESEARCH.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-10-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-12-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Acquire the output lock before deciding publication authority</name>
  <reversibility rating="costly">The target-lock order protects the canonical ledger and every future replacement invocation.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — output path resolution, pre-lock existence check, fetch/snapshot ordering, `LOCK_DIR`, collector-owned path registration, traps, and final rename
    - test/support/lockspire/release_proof/package_assertions.ex — live writer harness, process-group control, readiness files, same-target assertions, and cleanup helpers
    - test/lockspire/release/repository_hygiene_contract_test.exs — output-lifecycle and live-writer focused tests
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed concurrent-publication truth and exact two-absent-target reproduction
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-01, D-02, D-07, D-09, D-10, and D-24
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-10-SUMMARY.md — existing signal, owned-artifact, and atomic-rename behavior to preserve
  </read_first>
  <behavior>
    - "Two writers targeting one absent path can both reach a pre-lock barrier, but exactly one acquires publication authority and creates the target."
    - "After writer A publishes and releases, writer B acquires the lock, observes the now-existing target, and exits nonzero without --replace; writer A's bytes remain unchanged."
    - "With --replace, one lock owner performs existence authorization, fetch, snapshot capture, collection, recheck, and rename before releasing ownership."
    - "Signals and errors remove only collector-owned artifacts; existing output and unrelated neighboring files remain intact and a clean retry succeeds."
  </behavior>
  <action>Add `PackageAssertions.assert_baseline_inventory_publication_transaction!/0` with an explicit two-process barrier that places both production collectors after argument/path validation but before lock acquisition while the target is absent. Refactor the main flow so it resolves and validates the output parent without creating or replacing the target, acquires the per-target directory lock, installs ownership-aware cleanup, and only then evaluates target existence and `--replace` authorization. Hold that lock across D-09 fetch, `capture_snapshot_identity`, every source query, immediate snapshot recheck, same-directory rename, and final cleanup. When a waiting writer acquires the released lock, it must rerun the existence decision against current filesystem state and refuse absent `--replace`; it may not reuse a pre-lock observation or snapshot. Preserve INT/TERM/HUP behavior, different-target independence, lock-owner diagnostics, and cleanup limited to exact collector-owned paths.</action>
  <acceptance_criteria>
    - The synchronized absent-target fixture proves exactly one writer exits zero, the loser refuses after acquiring/rechecking under lock, and the winner's target digest is unchanged.
    - Source-command counters prove no losing writer performs fetch or evidence collection after discovering the new target without replacement authority.
    - Existing pre-rename/pagination contention, INT/TERM/HUP, prior-target preservation, retry, and different-target fixtures remain green.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
  </verify>
  <done>Overwrite authority, snapshot capture, and publication occur under one lock, so two absent-target writers cannot both publish.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Revalidate exact working-tree state immediately before rename</name>
  <reversibility rating="costly">Working-tree provenance is part of the immutable evidence snapshot and is consumed by later exact-SHA decisions.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — Task 1 lock-held flow, `INITIAL_PORCELAIN`, `capture_snapshot_identity`, `verify_snapshot_identity_unchanged`, owned-path exclusions, and publication abort diagnostics
    - test/support/lockspire/release_proof/package_assertions.ex — Task 1 live barriers and current ref/source drift fixtures
    - test/lockspire/release/repository_hygiene_contract_test.exs — publication transaction entry point created by Task 1
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed stable-working-tree truth and tracked-deletion reproduction
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-06, D-07, D-09, D-10, D-11, and D-24
  </read_first>
  <behavior>
    - "The same explicit `git status --porcelain=v2 --branch` command succeeds at capture and immediately before rename, and its normalized non-owned output is byte-equivalent."
    - "A tracked modification, tracked deletion, or untracked addition introduced after capture aborts publication, names working-tree drift safely, and preserves prior target bytes."
    - "A capture-time or recheck-time status exit greater than zero is unavailable evidence, exits nonzero, and cannot be treated as an empty or clean porcelain result."
    - "Exact collector output, lock, render, and page-buffer paths are excluded structurally without hiding any other file in the same directory."
  </behavior>
  <action>Replace the capture-time status fallback with an explicit success/failure branch that records unavailable state and stops complete publication when Git cannot provide porcelain-v2 evidence. Add one helper that runs the exact status command, validates success, normalizes ordering without losing status codes, and filters only the exact lock/output/temp/page paths already registered as collector-owned. Store its captured projection in `INITIAL_PORCELAIN`; call the same helper inside `verify_snapshot_identity_unchanged` as the final check before rename and require exact equality. Extend the synchronized publication fixture so state changes occur after initial capture but before recheck for a modified tracked file, deleted tracked file, and new untracked file, plus separate capture and recheck command failures. Each failure must emit a sanitized source-specific limitation, preserve the prior target byte-for-byte, remove owned artifacts, and pass after the repository stabilizes.</action>
  <acceptance_criteria>
    - Modify, delete, and untracked fixtures each abort nonzero after capture, identify worktree drift, preserve the previous target, clean owned artifacts, and succeed on a stable retry.
    - Capture-time and recheck-time status failures each render/emit unavailable evidence and cannot publish a complete or clean snapshot.
    - A stable fixture containing only collector-owned paths produces the same normalized worktree evidence and publishes successfully; a similarly named unrelated path remains visible and blocks equality.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <done>Every published ledger derives from one readable, unchanged working-tree projection captured and rechecked under the publication lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent local processes → canonical output path | Competing collectors may observe different target states unless existence and publication are one locked transaction. |
| Mutable working tree → immutable ledger | Local modifications and command failures can invalidate provenance between capture and rename. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-51 | Tampering / Denial of service | output existence and target lock | high | mitigate | Acquire first, decide overwrite under lock, retain ownership through rename, and prove the absent-target race with synchronized processes. |
| T-138-52 | Repudiation / Tampering | working-tree snapshot | high | mitigate | Explicitly capture and immediately recheck identical porcelain-v2 projections under lock. |
| T-138-53 | Repudiation | Git status failure | high | mitigate | Preserve command failure as unavailable evidence and abort rather than collapsing it to empty output. |
| T-138-54 | Elevation of privilege | collector-owned exclusions | medium | mitigate | Match only registered exact paths; near-name fixtures ensure unrelated changes cannot be hidden. |

## OWASP ASVS L1 Control Mapping

| ASVS family | Applicability and control intent | Named mitigation / task | Acceptance and verification evidence |
|-------------|----------------------------------|-------------------------|--------------------------------------|
| V4 Access Control | Only the current lock owner may authorize and replace the output. | T-138-51; Task 1 encloses existence, capture, collection, and rename in one ownership interval. | Two-writer and signal fixtures prove exclusive publication and scoped cleanup. |
| V5 Validation, Sanitization and Encoding | Filesystem paths and porcelain records are untrusted inputs. | T-138-52 and T-138-54; Task 2 validates command success and exact owned-path exclusions. | Modify/delete/untracked and near-name fixtures compare normalized projections. |
| V7 Error Handling and Logging | Race loss, local drift, and unreadable state must fail visibly while preserving prior evidence. | T-138-51 through T-138-53; both tasks emit bounded diagnostics and nonzero exits. | Production-process tests assert prior bytes, diagnostics, cleanup, and retry. |

## High-Severity Blocking Rule

Any failed or missing mitigation for T-138-51, T-138-52, or T-138-53 blocks Plan 138-13. A second unauthorized publish, stale working-tree receipt, or swallowed status failure is a blocking ASVS L1 integrity failure.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: lock-first main flow; under-lock overwrite authorization; exact owned-path registry; explicit porcelain-v2 capture; immediate worktree projection recheck; preserved collector CLI.
- `test/support/lockspire/release_proof/package_assertions.ex`: exported `assert_baseline_inventory_publication_transaction!/0`; synchronized absent-target writers; modification, deletion, untracked, capture-failure, recheck-failure, near-name, cleanup, and retry fixtures.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: `baseline inventory collector publishes under one lock-stable working-tree snapshot` focused test.
- No new durable artifact, public API, package/runtime surface, schema, or cleanup authority is created.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | One current non-destructive inventory | 138-13 | COVERED | Publication is exclusive and bound to stable local state. |
| REQ | BASE-01 | Exact clean/divergent local state | 138-13 | COVERED | Porcelain capture/recheck and explicit failures. |
| REQ | BASE-02 | Safe repeatable inventory publication | 138-13 | COVERED | Lock-first overwrite decision and two-writer proof. |
| RESEARCH | — | Receipt-first atomic artifact | 138-13 | COVERED | Lock, capture, recheck, and rename are one transaction. |
| CONTEXT | D-01–D-02 | Canonical Markdown ownership | 138-13 | COVERED | Only requested output replacement is durable. |
| CONTEXT | D-06–D-07 | Provenance and fail-visible completeness | 138-13 | COVERED | Exact worktree projection and unavailable status. |
| CONTEXT | D-09–D-10 | Safe fetch and script interfaces | 138-13 | COVERED | D-09 fetch remains the sole Git metadata mutation; porcelain-v2 is retained. |
| CONTEXT | D-11, D-24 | Sanitized diagnostics and focused proof | 138-13 | COVERED | Adversarial process fixtures exercise production paths. |

<verification>
- `bash -n scripts/maintainer/baseline_inventory.sh`
- `mix test test/lockspire/release/repository_hygiene_contract_test.exs`
- `mix test test/lockspire/release_readiness_contract_test.exs`
- Source diff confirms the only Git metadata mutation remains `git fetch --prune --tags "$REMOTE"` and output writes remain lock/temp/rename scoped.
</verification>

<success_criteria>
- Two absent-target writers cannot both publish and the loser cannot reuse a pre-lock overwrite decision.
- Every published ledger carries a readable working-tree projection that remains identical through the final pre-rename recheck.
- Modification, deletion, untracked-file, and status-failure regressions preserve prior evidence and fail visibly.
- Verification gaps 3 and 4 from `138-VERIFICATION.md` are closed with deterministic production-process tests.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-13-SUMMARY.md` when done.
</output>
