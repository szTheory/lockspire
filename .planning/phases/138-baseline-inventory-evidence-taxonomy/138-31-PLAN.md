---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "31"
type: execute
wave: 31
depends_on: ["138-30"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-02, TRIAGE-01, TRIAGE-02, LOOSE-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "BASE-02 TRIAGE-01 TRIAGE-02 LOOSE-01 D-11 D-24: every 32+ character mixed-case alphanumeric opaque component with a digit is conservatively redacted even when it contains five or more consecutive lowercase characters."
    - "Only anchored validated safe-format allowlists bypass opaque redaction; a generic lowercase run, readable prefix, URL prefix, path prefix, receipt label, or diagnostic context never proves safety."
    - "The lowercase-run credential shape is absent raw across Git branch/tag/worktree fields, GitHub PR/issue fields, maintained rows, YAML, URL/path, receipt, limitation, and diagnostic sinks while canonical identity and deduplication remain exact."
    - "Exact Git object widths, stable evidence IDs, UUIDs, semantic/release versions, approved Lockspire release labels, ordinary prose, and safe public URLs remain visible through narrow anchored controls."
    - statement: "LOOSE-01 unclassified remains unresolved: an unfamiliar opaque component that is not an exact validated safe format is redacted, and an unrenderable source becomes partial."
      verification: backstop
  prohibitions:
    - statement: "Collection and publication must not authorize merge, close, delete, prune, rewrite, cleanup, or release actions."
      verification: flagged-unverified
    - statement: "Republication must not amend, delete, or rewrite any prior immutable ledger or historical evidence."
      verification: flagged-unverified
    - statement: "The ledger and diagnostics must not expose credentials, uncontrolled bodies, raw logs, or environment values."
      verification: flagged-unverified
    - statement: "Partial, unavailable, malformed, stale, ambiguous, forged, or caller-injected evidence must not be presented as complete or authoritative."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Conservative credential_like_value policy with narrow safe-format allowlists."
      contains: "credential_like_value"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Lowercase-run opaque credential matrix across every durable and diagnostic sink plus benign controls."
      contains: "assert_baseline_inventory_credential_redaction!"
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused phase138_redaction_gap behavioral entry point."
      contains: "phase138_redaction_gap"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: every Markdown/YAML/receipt/limitation/diagnostic renderer
      via: "redacted_display_value calls credential_like_value after identity and before encoding"
      pattern: "credential_like_value|redacted_display_value"
    - from: test/lockspire/release/repository_hygiene_contract_test.exs
      to: test/support/lockspire/release_proof/package_assertions.ex
      via: "phase138_redaction_gap delegation"
      pattern: "assert_baseline_inventory_credential_redaction!"
---

<objective>
Close the lowercase-run opaque credential bypass at every rendered evidence boundary without sacrificing stable identity or known public identifiers.

Purpose: Ensure a plausible OAuth/client credential cannot be durably published merely because it contains a word-like lowercase run.
Output: A closed safe-format allowlist, conservative opaque detector, and cross-domain adversarial fixtures.

Decision coverage: D-05: keeps output compact; D-06: and D-07: keep receipts truthful; D-11: requires redaction; D-14: and D-20: preserve exact identity before display; D-22: and D-24: require deterministic accessible executable proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-28-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-30-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Redact one lowercase-run opaque branch through the production collector</name>
  <reversibility rating="reversible">The private detector and test fixture can be reverted without changing stored schema or public API.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — read credential_like_value, redacted_display_value, Markdown/YAML normalization, and Git row rendering before editing
    - test/support/lockspire/release_proof/package_assertions.ex — read assert_baseline_inventory_credential_redaction!, credential-redaction Git fixture construction, raw-output diagnostics, and benign_values before editing
    - test/lockspire/release/repository_hygiene_contract_test.exs — read the phase138_redaction_gap entry point before editing
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — read the exact raw branch reproduction and required sink coverage
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-28-SUMMARY.md — read identity-before-display and false-positive control decisions that must remain intact
  </read_first>
  <behavior>
    - RED: a real branch named `AbCdEfGhIjKlMnOpQrStUvWxYz012345abcdef` is present as an exact canonical subject internally but never appears raw in durable output or failure diagnostics.
    - GREEN: the branch row retains its deterministic GIT-BR evidence ID and emits [REDACTED] at the display boundary.
    - Exact 40/64 hexadecimal Git object IDs remain visible; the lowercase run alone never exempts an opaque candidate.
  </behavior>
  <action>Add the verifier's exact lowercase-run opaque value as a real Git branch fixture and prove the test fails against the current production collector. Remove the generic `[a-z]{5}` exemption from `credential_like_value`. Add the private `safe_public_identifier_value` predicate as one closed, anchored allowlist for the already-supported benign identifiers rather than a heuristic readability escape; keep exact 40/64 hexadecimal object IDs, stable evidence IDs, UUIDs, semantic/release versions, explicitly structured Lockspire release labels, and safe public URL structure only when each component separately passes the credential policy. Preserve D-20 canonical identity/deduplication before D-11 display redaction. Run the unchanged production CLI path, not a duplicate test-only detector.</action>
  <acceptance_criteria>
    - The exact lowercase-run branch value is absent raw from ledger bytes and captured diagnostics.
    - Its deterministic evidence row remains present with [REDACTED], proposal-only state, and unchanged identity semantics.
    - Every anchored benign control stays visible and generic lowercase runs confer no exemption.
  </acceptance_criteria>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_redaction_gap</automated>
    <fails_when>the command exits nonzero, reports 0 tests, the lowercase-run credential appears raw in output/diagnostics, its evidence row disappears, or an anchored benign control is redacted</fails_when>
  </verify>
  <done>The reproduced lowercase-run opaque branch is redacted by the production collector without changing canonical evidence identity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the opaque credential vector across every sink and safe control</name>
  <reversibility rating="reversible">This extends repository-local fixtures and a private renderer policy only.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — read the Task 1 credential and safe-format predicates plus every call to redacted_display_value before editing
    - test/support/lockspire/release_proof/package_assertions.ex — read all Git/GitHub/maintained credential fixture builders, assert_no_raw_credential!, receipt/limitation/diagnostic assertions, and benign controls before editing
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md — read the unchanged 8/8 external API coverage decision
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — read the required Git, GitHub, maintained, YAML, URL/path, receipt, limitation, and diagnostic sink list
  </read_first>
  <behavior>
    - The lowercase-run opaque value is redacted from branch, tag, worktree, pull-request, issue, and maintained-record display fields.
    - The same value is redacted when embedded in YAML scalars, URL/path components, receipt values, limitation text, and error diagnostics.
    - Named provider/JWT/PEM/bearer/keyword credential detection remains independent and green.
    - Safe controls are exact positive allowlist cases; one-character near misses that acquire mixed-case/digit opaque shape are redacted.
  </behavior>
  <action>Extend `assert_baseline_inventory_credential_redaction!/0` and its production CLI fixtures so the exact lowercase-run opaque vector traverses every required source and rendering sink: Git branch/tag/worktree, GitHub PR/issue, maintained subject/reference/rationale, canonical YAML, URL/path component, source receipt, limitation, and captured diagnostic. Assert raw absence by byte search and require [REDACTED] or a safe non-reversible reference while stable IDs remain distinct. Add near-miss tests around each safe-format allowlist so only the anchored form bypasses redaction. Re-run all earlier named credential vectors and the full repository-hygiene contract. Preserve the existing GraphQL queries and 8/8 INTEGRATE coverage with no new external API capability.</action>
  <acceptance_criteria>
    - All named sinks redact the lowercase-run opaque vector and no diagnostic failure includes it raw.
    - AWS, Slack, JWT, PEM, bearer, GitHub, and keyword families remain covered independently.
    - Exact safe identifiers remain useful, while safe-format near misses fail closed.
    - Full repository-hygiene behavior remains green and proposal-only.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
    <fails_when>shell syntax fails, the command exits nonzero, reports 0 tests, any required sink emits the raw opaque vector, a known credential family regresses, a safe-format near miss remains visible, or stable IDs collapse</fails_when>
  </verify>
  <done>Every durable and diagnostic boundary conservatively redacts lowercase-run opaque credentials with narrow, behaviorally pinned safe exceptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Git/GitHub/maintained input → canonical identity | Raw subjects remain exact only inside trusted transient identity processing. |
| Canonical identity → renderer/diagnostic | Every uncontrolled field crosses one conservative credential policy before durable or diagnostic output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-138-129 | Information disclosure | credential_like_value lowercase exemption | critical | mitigate | Remove heuristic exemption and accept only anchored validated safe formats. |
| T-138-130 | Spoofing | readable prefix / URL / path wrapper | high | mitigate | Scan every component independently; wrapper text never proves an opaque component safe. |
| T-138-131 | Tampering | identity after redaction | high | mitigate | Preserve exact stable-ID and deduplication work before rendering [REDACTED]. |
| T-138-132 | Repudiation | diagnostic/limitation sink | high | mitigate | Run the same predicate before receipt, limitation, and error output and assert raw byte absence. |

## ASVS L1 and Blocking Rule

V4, V5, and V7 apply. Critical/high mitigations block until production-path fixtures pass.
</threat_model>

## Artifacts this phase produces

- Modified private symbol `credential_like_value` and new private helper `safe_public_identifier_value` in `scripts/maintainer/baseline_inventory.sh`.
- Expanded lowercase-run opaque vectors and safe-format near misses in existing `assert_baseline_inventory_credential_redaction!/0` in `test/support/lockspire/release_proof/package_assertions.ex`.
- Existing `phase138_redaction_gap` test selector remains the entry point in `test/lockspire/release/repository_hygiene_contract_test.exs`.
- No new source path, dependency, schema, sidecar, runtime/public API, external API capability, destructive operation, or executed disposition is produced.

## Spec-less probe no-silent-drop audit

| Candidate | Explicit disposition |
|---|---|
| BASE-01 unclassified | Preserved fail-closed by Plan 30 and rechecked in Plan 32; this plan does not alter baseline authority. |
| BASE-02 adjacency | Exact identity precedes redaction, so adjacent rows cannot collapse. |
| BASE-02 empty | Redacted rows remain counted; complete-zero cannot be inferred from hidden display text. |
| BASE-02 ordering | Sorting remains on canonical identity before rendering. |
| BASE-02 idempotency | The same canonical opaque value yields the same stable ID and rendered marker. |
| BASE-02 concurrency | Existing lock/source-stability checks are unchanged and rechecked in Plan 32. |
| TRIAGE-01 adjacency | Separate normalized PR records retain node identity when display fields redact. |
| TRIAGE-01 empty | Redaction cannot turn a nonempty authenticated PR page into complete-zero. |
| TRIAGE-01 ordering | Numeric PR ordering precedes final encoding. |
| TRIAGE-01 concurrency | Existing outer/nested OID equality is unchanged and rechecked in Plan 32. |
| TRIAGE-02 adjacency | Issue namespace and duplicate agreement remain exact before display. |
| TRIAGE-02 empty | Only authenticated terminal pagination can establish complete-zero. |
| TRIAGE-02 ordering | Numeric issue ordering precedes final encoding. |
| LOOSE-01 unclassified | Unresolved backstop: non-allowlisted opaque text is redacted and unsafe rendering makes the source partial. |

No-silent-drop equality: 14 candidates = 12 explicit preservation/behavior checks + 2 fail-closed cross-plan/backstop treatments.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Safe current non-destructive inventory | 138-31, 138-32 | COVERED |
| REQ | BASE-02 | Safe branch/tag/worktree evidence | 138-31 | COVERED |
| REQ | TRIAGE-01 | Safe PR evidence | 138-31 | COVERED |
| REQ | TRIAGE-02 | Safe issue evidence | 138-31 | COVERED |
| REQ | LOOSE-01 | Safe maintained evidence | 138-31 | COVERED |
| REQ | BASE-01 | Current baseline relation | 138-30, 138-32 | COVERED |
| RESEARCH | Allowlisted records, final-boundary redaction, Bash/ExUnit ownership | 138-31 | COVERED |
| CONTEXT | D-01 through D-24 | Preserved; directly exercises D-05, D-06, D-07, D-11, D-14, D-20, D-22, D-24 | COVERED |
| VERIFICATION | Lowercase-run opaque credential leak | 138-31 Tasks 1-2 | COVERED |
| CONTEXT | Deferred sidecar/database/UI/runtime expansion | none | EXCLUDED |

<verification>
  <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.4.1 mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy</automated>
  <fails_when>shell syntax fails, the command exits nonzero, reports 0 tests, API coverage reports block=true or fewer than 8 integrated capabilities, any raw lowercase-run credential reaches output, or a safe-format near miss bypasses redaction</fails_when>
</verification>

<success_criteria>
- The exact reproduced opaque credential and all equivalent sink placements are redacted.
- Benign public identifiers remain visible only through narrow anchored allowlists.
- Canonical identity, deterministic ordering, complete-zero semantics, proposal-only scope, and 8/8 GitHub coverage remain intact.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-31-SUMMARY.md` when done.
</output>
