---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "14"
type: execute
wave: 14
depends_on: ["138-13"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-02, LOOSE-01]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "LOOSE-01 D-11 D-14 D-20: hostile maintained subjects and evidence paths never cross a delimiter-separated semantic row boundary; every lifecycle, disposition, evidence, confidence, recheck, authority, and execution field remains attached to its stable REC ID."
    - "LOOSE-01 D-07 D-17 D-23: tracked-marker search exit 0 means matched evidence, exit 1 with an empty valid buffer means complete-zero, and exits greater than 1 mean unavailable/partial."
    - "BASE-02 D-01 D-02 D-06 D-22: canonical frontmatter is valid YAML and the complete artifact is valid UTF-8 for repository identities and paths containing YAML indicators, leading/trailing whitespace, tabs, and multibyte Unicode."
    - "Invalid filename byte sequences are represented by one deterministic reversible display encoding without changing exact internal Git path bytes or canonical identity derivation."
  prohibitions:
    - statement: "Untrusted path or prose bytes must not shift semantic evidence columns, create a new record, or change an allowed lifecycle or disposition."
      verification: flagged-unverified
    - statement: "A successful maintained-marker search with no matches must not be reported as a command failure or partial evidence."
      verification: flagged-unverified
    - statement: "Sanitization must not truncate UTF-8 by raw byte ranges, emit unquoted YAML strings, expose secrets, execute path content, or alter source records."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Structured maintained-row transport, exact marker exit semantics, Unicode-safe display normalization, YAML quoted-scalar encoding, and separate Markdown cell encoding."
      contains: "render_front_matter"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Per-ID semantic tab-path assertions, marker-free complete-zero fixture, parsed YAML frontmatter, Unicode and invalid-byte render checks."
      exports: ["assert_baseline_inventory_maintained_encoding!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused structured maintained encoding and canonical artifact validity entry point."
      contains: "preserves maintained semantics and valid canonical encoding"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: "REC JSON records and Markdown rows"
      via: "jq-built structured records retain field identity until the final per-field Markdown encoder"
      pattern: "REC|jq -c"
    - from: scripts/maintainer/baseline_inventory.sh
      to: "YAML frontmatter"
      via: "Unicode-safe display normalization feeds a JSON-compatible quoted scalar encoder accepted by YAML"
      pattern: "yaml_.*scalar|render_front_matter"
---

<objective>
Make maintained evidence and the canonical artifact structurally safe across semantic fields, marker-free repositories, YAML, and Unicode.

Purpose: Preserve exact evidence meaning for hostile valid Git paths while ensuring a successful zero-marker scan and a parseable UTF-8 Markdown ledger.
Output: Structured REC transport, correct selector exit taxonomy, separate YAML/Markdown encoders, and adversarial artifact parsing for BASE-02 and LOOSE-01.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-RESEARCH.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-PATTERNS.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-09-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-13-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace tab-delimited REC transport with structured records</name>
  <reversibility rating="costly">REC field semantics and stable identity are a cross-phase citation contract even though the internal transport remains private.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — maintained marker/family row assembly, `summarize_archive_container`, `deduplicate_record_refs`, `render_maintained_inventory`, and every `IFS`/tab-delimited row parser
    - test/support/lockspire/release_proof/package_assertions.ex — hostile maintained repository builder, tabbed filename fixture, stable-ID assertions, and rendered-row helpers
    - test/lockspire/release/repository_hygiene_contract_test.exs — maintained fail-closed focused entry point
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed hostile-tab semantic truth and CR-07 reproduction
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-05, D-10 through D-15, D-17 through D-20, D-22, and D-24
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-09-SUMMARY.md — exact-path versus sanitized-display and stable REC identity decisions to preserve
  </read_first>
  <behavior>
    - "A path containing a tab, newline, colon, pipe, backtick, brackets, shell metacharacters, and multibyte text produces exactly one REC identity."
    - "For the tab-path fixture, the row for that exact REC ID retains kind, lifecycle, maintained disposition, evidence reference, rationale, confidence, recheck, authority, and executed=no at their named fields."
    - "Cross-family duplicates and supersession aggregate every reference and rationale by stable REC ID without first-row-wins loss."
    - "No hostile input creates a heading, extra table row, temp path, command side effect, or shifted semantic field."
  </behavior>
  <action>Add failing per-ID assertions for every D-14 semantic field of the existing tabbed-path fixture, then replace delimiter-separated maintained row strings with JSON Lines in collector-owned buffers. Construct each record with `jq -cn --arg` using named keys for ID, kind, exact canonical identity, sanitized display subject, lifecycle, disposition, evidence references, rationale, confidence, recheck, authority, and executed state. Perform deduplication and supersession aggregation by stable REC ID over structured objects; do not serialize untrusted values through tabs, newlines, colons, or shell word splitting. Render one normalized object at a time and apply Markdown encoding separately to each displayed field. Keep exact path bytes for quoted Git lookup and stable identity input, while display/evidence values remain sanitized. Extend reversed discovery, duplicate, archive, unclassified, and hostile-path fixtures to assert named field equality rather than merely absence of literal separators.</action>
  <acceptance_criteria>
    - The tabbed-path fixture asserts every semantic field by REC ID and retains the exact allowed maintained taxonomy and executed=no state.
    - No maintained row pipeline uses an untrusted delimiter-separated shell record; structured parsing failure downgrades the exact family/aggregate and cannot render a partial shifted row.
    - Cross-family, reverse-order, supersession, archive-summary, unclassified, stable-ID, and no-side-effect fixtures remain green.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
  </verify>
  <done>Every hostile maintained path remains one stable structured record whose proposal semantics cannot be shifted by its bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Treat a zero-match marker search as complete-zero evidence</name>
  <reversibility rating="reversible">The selector exit mapping is isolated behind the maintained-family receipt contract.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — Task 1 structured marker rows, `git grep -z -nE` invocation, selector output buffer, per-family receipts, and maintained aggregate status
    - test/support/lockspire/release_proof/package_assertions.ex — maintained fixture repository builder and selector-status assertions after Task 1
    - test/lockspire/release/repository_hygiene_contract_test.exs — structured maintained entry point created by Task 1
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed marker-free complete-zero truth and WR-01 reproduction
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-07, D-17, D-19, D-23, and D-24
  </read_first>
  <behavior>
    - "A valid repository with no tracked TODO/FIXME matches yields marker selector exit 1, an empty buffer, a complete receipt, zero rows, and explicit successful-zero wording."
    - "Exit 0 requires at least one valid structured marker record; malformed non-empty output becomes partial."
    - "Exit greater than 1 remains unavailable with exact safe exit evidence and makes maintained/snapshot status partial."
    - "Other D-17 families remain independently complete/partial and cannot be masked by the marker outcome."
  </behavior>
  <action>Add a marker-free tracked repository fixture and drive the production `--scope maintained` path without a fake successful marker. Capture `git grep -z -nE` output and exit explicitly without `set -e` or conditional-call suppression. Map exit 0 to matched evidence only after structured record validation; map exit 1 plus an empty buffer to a complete tracked-marker receipt with count zero and the established calm successful-zero copy; map statuses greater than 1 or an impossible exit/buffer combination to unavailable/partial with exact sanitized limitation. Keep D-17 path bounds and D-19 exclusions unchanged, and assert that marker complete-zero does not turn another failed family into a complete aggregate.</action>
  <acceptance_criteria>
    - The marker-free fixture produces complete status, count zero, no REC marker row, and explicit successful-zero wording without a failure limitation.
    - Exit 2/42 and malformed exit/output combinations remain unavailable/partial and preserve safe sibling evidence without aggregate complete status.
    - Marker matches, family complete-zero, ambiguity, selector failure, archive, exclusion, and proposal taxonomy fixtures remain green.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
  </verify>
  <done>A successful search with no maintained markers is honest complete-zero evidence, distinct from every execution or parsing failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit parseable YAML and valid UTF-8 without byte truncation</name>
  <reversibility rating="costly">The canonical Markdown frontmatter is a durable cross-phase evidence interface and must remain parseable by later tooling.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `markdown_value`, control-byte filtering, `render_front_matter`, repository identity/path capture, receipt fingerprints, and downstream frontmatter readers
    - test/support/lockspire/release_proof/package_assertions.ex — Task 1/2 hostile repository builder, canonical output readers, and temporary complete-ledger fixture
    - test/lockspire/release/repository_hygiene_contract_test.exs — maintained encoding entry point
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed YAML/UTF-8 truth and WR-03 reproduction using `: #€`
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-01, D-02, D-05, D-06, D-11, D-14, D-21 through D-24
  </read_first>
  <behavior>
    - "Repository identities containing `: #`, YAML indicators, quotes, tabs, and leading/trailing whitespace round-trip through parsed frontmatter as one scalar."
    - "Representative Unicode including café and euro-sign text remains valid UTF-8 and is not damaged by removal of raw continuation-byte ranges."
    - "Invalid path byte sequences receive one documented deterministic escaped display form while exact internal bytes still drive Git lookup and stable identity."
    - "Markdown cells continue to escape table/control syntax, and YAML frontmatter uses a distinct quoted-scalar encoder."
  </behavior>
  <action>Split display normalization, Markdown-cell encoding, and YAML-scalar encoding into explicit functions. Normalize valid UTF-8 as Unicode, remove or visibly escape Unicode control code points rather than raw byte ranges, and deterministically percent-escape invalid input bytes for display without altering the exact internal path used by Git or identity hashing. Emit every dynamic frontmatter string through a JSON-compatible quoted-scalar encoder using the existing `jq` dependency; YAML accepts these quoted scalars and no dynamic value remains bare. Update frontmatter readers/classifier fixtures to consume the quoted values without weakening exact SHA checks. Extend the hostile repository fixture with `: #€`, leading/trailing whitespace, quotes, indicators, and invalid filename bytes. Assert `String.valid?/1` for the complete artifact, parse the extracted frontmatter with the repository's existing GSD/YAML-capable parser command, verify round-tripped scalar values, and retain Markdown table column counts and redaction assertions.</action>
  <acceptance_criteria>
    - The generated complete ledger is valid UTF-8 and its frontmatter parser accepts every hostile repository identity/path case with the expected scalar values.
    - Euro-sign and other representative multibyte characters remain intact; invalid filename bytes use the deterministic display escape and do not corrupt the artifact.
    - Dynamic frontmatter values are quoted, Markdown rows remain column-stable, secrets/control characters remain absent, and the production snapshot-relation reader still resolves exact anchor/fingerprint values.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs query frontmatter.validate .planning/phases/138-baseline-inventory-evidence-taxonomy/138-14-PLAN.md --schema plan-gap-closure</automated>
  </verify>
  <done>The canonical ledger is valid UTF-8 with parseable quoted YAML frontmatter and field-safe Markdown for every hostile tested repository path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exact Git path bytes → structured maintained records | Valid hostile filenames must preserve identity and semantic fields without delimiter interpretation. |
| Structured evidence → YAML/Markdown UTF-8 artifact | Untrusted text crosses format and Unicode encoding boundaries before durable publication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-55 | Tampering / Elevation | maintained semantic transport | high | mitigate | Replace delimiter strings with named JSON records and assert every semantic field for hostile IDs. |
| T-138-56 | Repudiation | tracked-marker exit taxonomy | medium | mitigate | Distinguish exits 0, 1, and greater than 1 with explicit buffer validation and receipts. |
| T-138-57 | Tampering / Information disclosure | YAML and Markdown encoders | high | mitigate | Separate encoders, quoted scalars, per-field escaping, redaction, and parsed artifact tests. |
| T-138-58 | Denial of service / Tampering | UTF-8 normalization | high | mitigate | Preserve valid Unicode, escape invalid bytes deterministically, and reject malformed final artifacts. |

## OWASP ASVS L1 Control Mapping

| ASVS family | Applicability and control intent | Named mitigation / task | Acceptance and verification evidence |
|-------------|----------------------------------|-------------------------|--------------------------------------|
| V4 Access Control | Maintained discovery remains bounded and source records remain read-only. | T-138-55 and T-138-56; Tasks 1 and 2 preserve D-17/D-19 boundaries. | Existing manifest, exclusion, and no-source-write fixtures remain green. |
| V5 Validation, Sanitization and Encoding | Paths, prose, Unicode, JSON, YAML, and Markdown are untrusted at each boundary. | T-138-55, T-138-57, and T-138-58; Tasks 1 and 3 use named records and format-specific encoders. | Per-ID semantic assertions, UTF-8 validation, YAML parse/round-trip, and Markdown column checks. |
| V7 Error Handling and Logging | Selector, structured parse, UTF-8, or YAML failure must be visible and block complete publication. | T-138-56 through T-138-58; Tasks 2 and 3 provide exact fail-visible outcomes. | Marker exit matrix and hostile artifact parsing run through focused ExUnit. |

## High-Severity Blocking Rule

Any failed or missing mitigation for T-138-55, T-138-57, or T-138-58 blocks Plan 138-14. Shifted REC semantics, invalid UTF-8, invalid YAML, raw secret/control leakage, or silent byte loss is a blocking ASVS L1 failure.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: JSON Lines maintained-row buffer; named-field REC aggregation/rendering; exact `git grep` exit mapping; Unicode-safe display normalization; `yaml_quoted_scalar`; separate Markdown cell encoder; preserved exact-byte Git identity seam.
- `test/support/lockspire/release_proof/package_assertions.ex`: exported `assert_baseline_inventory_maintained_encoding!/0`; per-ID tab semantics; marker-free complete-zero; exit matrix; YAML-indicator, euro-sign, multibyte, invalid-byte, parser, round-trip, and Markdown-column fixtures.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: `baseline inventory collector preserves maintained semantics and valid canonical encoding` focused test.
- No canonical sidecar, package dependency, runtime module, schema, public API, or source-record mutation is created.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | One auditable canonical inventory | 138-14 | COVERED | Semantic rows and artifact syntax remain parseable under hostile input. |
| REQ | BASE-02 | Safe repeatable canonical inventory | 138-14 | COVERED | Valid YAML/UTF-8 output and deterministic display encoding. |
| REQ | LOOSE-01 | Every maintained follow-up with intact taxonomy | 138-14 | COVERED | Structured REC fields and honest complete-zero markers. |
| RESEARCH | — | Allowlisted records and bounded maintained manifest | 138-14 | COVERED | Structured internal JSON remains temporary; Markdown remains canonical. |
| CONTEXT | D-01–D-02 | Canonical Markdown without sidecar | 138-14 | COVERED | JSON Lines is collector-owned temporary transport only. |
| CONTEXT | D-05–D-07 | Compact structured fields and complete receipts | 138-14 | COVERED | Named records, zero-match semantics, and quoted frontmatter. |
| CONTEXT | D-10–D-15 | Stable interfaces, redaction, exact taxonomy | 138-14 | COVERED | Field-safe transport and distinct encoders preserve row meaning. |
| CONTEXT | D-17–D-20 | Bounded sources and lossless aggregation | 138-14 | COVERED | Existing selectors/summaries/exclusions plus structured dedup. |
| CONTEXT | D-21–D-24 | Accessible Markdown and adversarial proof | 138-14 | COVERED | UTF-8/YAML parsing and semantic column assertions. |

<verification>
- `bash -n scripts/maintainer/baseline_inventory.sh`
- `mix test test/lockspire/release/repository_hygiene_contract_test.exs`
- `mix test test/lockspire/release_readiness_contract_test.exs`
- `mix ci`
- Source diff confirms temporary structured buffers remain collector-owned and no canonical sidecar or source-record mutation was added.
</verification>

<success_criteria>
- A tabbed hostile path cannot shift any maintained semantic field and is asserted by exact REC ID.
- A marker-free repository yields complete-zero while actual selector failures remain unavailable/partial.
- YAML frontmatter parses and the complete ledger is valid UTF-8 for hostile YAML indicators, Unicode, and invalid path bytes.
- Verification gaps 8, 9, and 11 from `138-VERIFICATION.md` are closed with exact adversarial regressions.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-14-SUMMARY.md` when done.
</output>
