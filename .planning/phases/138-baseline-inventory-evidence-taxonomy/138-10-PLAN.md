---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "10"
type: execute
wave: 10
depends_on: ["138-09"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-01, BASE-02, TRIAGE-01, TRIAGE-02, LOOSE-01]
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "BASE-02 TRIAGE-01 D-07 D-24: live same-target writers and handled interruption are proven against production collector processes, including a writer held during GitHub pagination."
    - "D-06 D-09: after the allowed fetch, the collector captures evidence_base_sha, UTC collection start, HEAD/main/remote-main, initial porcelain, declared source scopes, and source-receipt fingerprints before dependent rendering and rechecks the snapshot immediately before atomic publication."
    - "A published ledger is an immutable snapshot: its ledger-only commit has exactly one parent, that parent equals evidence_base_sha, and the ledger blob is unchanged after that commit."
    - "Later HEAD movement is classified from the immutable ledger commit through a linear first-parent chain; each accepted commit is disclosed with SHA, author, subject, files, lifecycle class, and semantic verdict."
    - "Only recognized Phase 138 summary, clean review, passed verification, phase-completion, and transition bookkeeping may be authorized, and only when exact message, author/committer, path shape, and content rules prove that no new or changed pending follow-up marker or collected disposition was introduced."
    - "A prior gaps_found verification is removed from the operational-follow-up projection only when every listed gap maps to an executed gap-plan SUMMARY; without that exact supersession proof it remains pending and later verification drift requires refresh."
    - "Source/script/plan changes, ref movement outside the authorized chain, source-receipt mismatch, mixed commits, unknown messages/authors/shapes, pending markers, non-passed verification, review findings, and ambiguous diffs fail closed with refresh_required."
    - "All 14 spec-less edge candidates retain an explicit tested or backstop disposition across Plans 138-01 through 138-10."
  prohibitions:
    - statement: "No path-only allowlist may authorize post-snapshot drift."
      verification: flagged-unverified
    - statement: "The currentness verdict must not claim that HEAD stopped changing or treat all GSD-authored commits as harmless."
      verification: flagged-unverified
    - statement: "The classifier must not mutate Git refs, GitHub objects, maintained records, release-owned files, or the canonical ledger."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Live-writer safety, pre-source snapshot capture, pre-publish receipt recheck, and read-only post-snapshot drift classification."
      contains: "verify_snapshot_relation"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Process-level concurrency, ref/source drift, immutable relation, lifecycle authorization, and hostile near-miss fixtures."
      exports: ["assert_baseline_inventory_output_lifecycle!/0", "assert_baseline_inventory_snapshot_currentness!/0", "assert_baseline_inventory_post_snapshot_drift!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused entry points for live publication, snapshot, and post-snapshot classifier contracts."
      contains: "fails closed on semantically relevant post-snapshot drift"
  key_links:
    - from: test/support/lockspire/release_proof/package_assertions.ex
      to: scripts/maintainer/baseline_inventory.sh
      via: "temporary Git histories and synchronized OS processes execute the production collection and --verify-snapshot-relation paths"
      pattern: "assert_baseline_inventory_post_snapshot_drift!"
    - from: scripts/maintainer/baseline_inventory.sh
      to: .planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md
      via: "the ledger records the immutable anchor and receipt digests consumed by the read-only classifier"
      pattern: "evidence_base_sha"
---

<objective>
Make collection ref-stable and define the executable currentness contract for an immutable Phase 138 evidence snapshot.

Purpose: Close both behavior-unverified concurrency edges and replace the impossible final-write rule with a bounded, fail-closed relation that normal execute-phase tasks and the normal verifier can evaluate.
Output: Production live-writer tests, pre-publication snapshot/receipt rechecks, and a deterministic post-snapshot lifecycle classifier for all five Phase 138 requirements.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REPO-HYGIENE-CHECKLIST.md
@.planning/RELEASE-TRAIN.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-RESEARCH.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-09-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove live same-target interruption and held-pagination contention</name>
  <reversibility rating="costly">The target lock and atomic publication behavior protect the canonical evidence artifact relied on by all later milestone phases.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — target-lock acquisition, collector-owned temporary files, signal traps, GitHub pagination, cleanup, and atomic rename
    - test/support/lockspire/release_proof/package_assertions.ex — `assert_baseline_inventory_output_lifecycle!/0`, command fixtures, process helpers, and artifact cleanup assertions
    - test/lockspire/release/repository_hygiene_contract_test.exs — focused delegation entry points
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — both behavior-unverified items and exact expected outcomes
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-02, D-07, D-10, D-15, and D-24
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-06-SUMMARY.md — prior lifecycle claim whose process-level coverage is incomplete
  </read_first>
  <behavior>
    - "A first production collector signals that it is live and held before rename; a second same-target collector refuses while prior target bytes stay unchanged."
    - "INT, TERM, and HUP remove only the live collector's owned artifacts, preserve prior bytes, and permit a complete retry."
    - "A GitHub writer held during pagination blocks same-target GitHub and Git-scope writers; refused-writer sentinels never reach the target."
    - "Different target paths remain independent."
  </behavior>
  <action>Replace the pre-created-lock simulation with a deterministic live-process harness using `Port.open` or an equivalent OS-process API and explicit file/pipe readiness signals. Seed a complete target, start the production collector, wait until fake Git or GitHub announces a pre-rename hold, then launch a competitor. Assert lock refusal and byte-identical prior output. Exercise INT, TERM, and HUP as table-driven live cases, await exit, prove that only collector-owned lock/render/page artifacts were removed, and retry successfully. Add the distinct pagination hold with same-target GitHub and Git competitors plus different-target independence. Use bounded readiness timeouts, not timing sleeps. Modify signal/ownership cleanup only when the live fixture exposes a production defect, preserving same-directory atomic rename and D-09's sole metadata mutation.</action>
  <acceptance_criteria>
    - Focused tests start and signal actual collector processes for all three handled signals.
    - Same-target competitors refuse during both pre-rename and GitHub-pagination holds; prior bytes, cleanup, retry, and no-interleaving assertions pass.
    - Explicit synchronization has bounded failure diagnostics and different targets complete independently.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
  </verify>
  <done>The two previously behavior-unverified concurrency edges are executable and green against live production collector processes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture and recheck the complete evidence snapshot before publication</name>
  <reversibility rating="costly">Snapshot provenance and receipt fingerprints are durable downstream evidence fields used to decide whether refresh is mandatory.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — current main flow, `collect_git_baseline`, front matter, `INITIAL_PORCELAIN`, source receipts, and final rename
    - test/support/lockspire/release_proof/package_assertions.ex — Task 1 synchronization and stateful fake Git/GitHub dispatch
    - test/lockspire/release/repository_hygiene_contract_test.exs — focused lifecycle entry points
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — late provenance capture and stale-ledger root cause
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-06 through D-11 and D-24
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-06-SUMMARY.md — evidence-base parent relation and collector-owned-artifact exclusion to preserve
  </read_first>
  <behavior>
    - "The allowed fetch finishes first; one capture step then records the UTC start, full HEAD/main/remote-main SHAs, evidence base, initial porcelain, repository identity, and declared source scopes before dependent source rendering."
    - "The ledger records normalized receipt fingerprints for Git, both GitHub namespaces, and maintained-source discovery, without retaining uncontrolled payloads."
    - "A tracked Phase 138 gaps report is classified as superseded lifecycle evidence only when all of its gap-plan mappings have matching complete summaries; unresolved or ambiguous reports remain in the pending projection."
    - "Immediately before rename, immutable refs and source receipt fingerprints are re-observed; any changed ref, GitHub page/row receipt, maintained marker projection, or repository identity aborts publication."
    - "A drift abort is nonzero, leaves the prior target byte-identical, cleans owned artifacts, and permits stable retry."
  </behavior>
  <action>Add `PackageAssertions.assert_baseline_inventory_snapshot_currentness!/0` with stateful fake Git and GitHub inputs. Refactor startup so `git fetch --prune --tags "$REMOTE"` runs first and a single `capture_snapshot_identity` records `SNAPSHOT_STARTED_AT`, `EVIDENCE_BASE_SHA`, HEAD/main/remote-main, initial porcelain, repository identity, and declared source scopes before Git/GitHub/maintained rendering. Normalize each source receipt into an allowlisted identity/count/status/page-terminal projection and hash it with the existing Git hashing primitive; never hash or retain raw bodies, logs, credentials, or uncontrolled prose. Extend the maintained projection with an exact supersession rule for the existing Phase 138 gaps report: parse its declared gap-to-plan mappings and treat it as non-pending lifecycle evidence only when every mapped gap plan has a tracked SUMMARY whose front matter is complete; otherwise retain it as pending/ambiguous. Immediately before `mv -f`, run `verify_snapshot_identity_unchanged`: re-read refs without another fetch, recheck the GitHub receipt projection through the fakeable query seam, and recompute the maintained marker/path projection from the captured Git tree. On any mismatch, emit a sanitized source-specific diagnostic, exit nonzero, preserve the prior target, and clean owned artifacts. Cover HEAD, main, remote-main, repository identity, external receipt, maintained-marker drift, valid verification supersession, missing-summary supersession failure, and stable retry. Render the exact base SHA, collection timestamp, receipt fingerprints, and bounded-as-of wording required by D-06/D-08.</action>
  <acceptance_criteria>
    - Command-order proof places fetch and snapshot capture before dependent queries and the identity/receipt recheck immediately before atomic rename.
    - Every ref and receipt drift fixture aborts publication, preserves prior bytes, cleans owned artifacts, and passes after stabilization.
    - Stable fixtures retain complete receipts, full SHAs, initial porcelain without collector artifacts, safe source receipt digests, and no raw external payload.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
  </verify>
  <done>Every published ledger identifies one source-stable evidence snapshot rechecked immediately before atomic publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Classify post-snapshot lifecycle drift by commit shape and semantic content</name>
  <reversibility rating="costly">The accepted-drift vocabulary is a maintainer trust contract; later phases rely on its fail-closed refresh verdict.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — Task 2 snapshot fields, source projection, CLI parser, sanitizers, and read-only Git seams
    - test/support/lockspire/release_proof/package_assertions.ex — temporary repository builders and Task 2 source-drift fixtures
    - test/lockspire/release/repository_hygiene_contract_test.exs — currentness entry point from Task 2
    - /Users/jon/.codex/gsd-core/workflows/execute-plan.md — task commit, SUMMARY, and execution-tracking commit shapes
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md — code-review, verifier, `phase.complete`, todo-close, and transition ordering
    - /Users/jon/.codex/gsd-core/workflows/transition.md — post-completion PROJECT/STATE mutation semantics
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-06 through D-08, D-11, D-15, D-17 through D-20, and D-24
  </read_first>
  <behavior>
    - "`--verify-snapshot-relation LEDGER` resolves the immutable ledger commit, proves it changes only that ledger, proves its sole parent equals evidence_base_sha, and proves the ledger blob is unchanged at the requested HEAD."
    - "The verifier walks one linear first-parent chain from ledger commit to HEAD and emits one sanitized row per later commit with SHA, matched lifecycle class, author, subject, paths, and semantic verdict."
    - "Recognized summary, clean review, passed verification, phase-completion, and transition fixtures pass only with exact message/author/path/content shapes and an unchanged pending-follow-up projection."
    - "A passed verification may replace the exact superseded Phase 138 gap report without changing a collected operational disposition; any new, retained, or remapped gap remains refresh_required."
    - "Uncommitted transition bookkeeping is disclosed as a working-tree row and is authorized only when its PROJECT/STATE projection is mechanical and introduces no pending follow-up."
    - "Allowed path with a new pending marker, allowed message with a source change, mixed commit, unknown commit, author/committer mismatch, merge/ref movement, changed ledger blob, and external receipt drift each return refresh_required."
  </behavior>
  <action>Add a mutually exclusive read-only CLI mode `--verify-snapshot-relation LEDGER` with optional test-only command overrides through the existing fixture environment. Resolve the ledger commit with Git, require one parent equal to the ledger's single `evidence_base_sha`, require that commit's diff to contain exactly the canonical ledger path, require the current ledger blob to equal the committed blob, and require HEAD/main to descend through a merge-free first-parent chain. Bind recognized later commits to the ledger commit's author and committer identities. Implement explicit lifecycle classes for: the Phase 138 plan-summary commit; a clean/skipped Phase 138 review; a passed Phase 138 verification with zero gaps, zero behavior-unverified items, and no human-needed status; the exact `docs(phase-138): complete phase execution` tracking commit; and transition bookkeeping limited to PROJECT/STATE progress updates. A passed verification may replace only the Task 2-proven superseded gap report; any added, retained, or remapped gap changes the operational projection and requires refresh. Each class must validate the exact subject grammar, exact path set, expected front-matter/status transition, and a normalized diff projection that rejects added or changed pending todos, findings, blockers, concerns, handoffs, checkpoints, TODO/FIXME markers, active requirements, or proposal dispositions. Also inspect staged and unstaged state: accept and disclose only the exact transition PROJECT/STATE projection with no pending additions; every other dirty-worktree shape requires refresh. Treat todo-close commits as refresh-required because they move maintained follow-up records. Recheck Task 2's external source receipts with read-only `git ls-remote` and authenticated/fake GitHub queries. Emit `authorized_bookkeeping` only for proven rows and `refresh_required` for every unknown or ambiguous condition; do not authorize from paths alone and do not mutate repository or ledger state.</action>
  <acceptance_criteria>
    - Synthetic histories cover authorized summary, clean review, superseded-report-to-passed verification, phase completion, committed transition, and uncommitted transition bookkeeping and assert the complete disclosed commit/working-tree table.
    - Hostile near-misses cover an allowed path with a new pending marker, an allowed message plus source change, mixed paths, unknown subject, unknown author/committer, merge/ref movement, changed ledger, and external receipt drift; every case fails closed.
    - The production classifier is read-only, sanitized, deterministic across repeated runs, and exits nonzero whenever refresh is mandatory.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <done>The repository can distinguish an exact immutable snapshot plus narrowly authorized bookkeeping from any semantically relevant or ambiguous drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Concurrent/signaled processes → canonical target | Live writers and interruption compete for one durable evidence path. |
| Mutable Git/GitHub/maintained sources → snapshot publication | Source identity can change during collection and must be rechecked before rename. |
| Post-snapshot Git history → currentness verdict | Commit metadata, diffs, authors, and external receipts are untrusted classifier inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-37 | Tampering / Denial of service | live target lock and signal cleanup | high | mitigate | Deterministic live holds, same-target refusal, INT/TERM/HUP, unchanged prior bytes, owned cleanup, retry, and different-target fixtures. |
| T-138-38 | Repudiation / Tampering | captured snapshot and receipt fingerprints | high | mitigate | Fetch first, capture exact identity and receipt projections, recheck immediately before rename, and abort on any mismatch. |
| T-138-39 | Spoofing / Repudiation | lifecycle commit classifier | high | mitigate | Bind to ledger author/committer, exact subject grammar, exact path shape, merge-free ancestry, semantic diff projection, and disclosed per-commit verdicts. |
| T-138-40 | Tampering / Information disclosure | post-snapshot content and external receipts | high | mitigate | Fail closed on source/plan/mixed/marker/receipt drift and sanitize all classifier output. |
| T-138-41 | Elevation of privilege | proposal handoff | medium | mitigate | Preserve executed=no and revalidation authority; classifier and collection perform no disposition action. |

## OWASP ASVS L1 Control Mapping

| ASVS family | Applicability and control intent | Named mitigation / task | Acceptance and verification evidence |
|-------------|----------------------------------|-------------------------|--------------------------------------|
| V2 Authentication | Live GitHub receipt comparison uses the existing authenticated CLI boundary. | T-138-38 and T-138-40; Tasks 2 and 3 fail closed on auth or receipt mismatch. | Stateful external-receipt fixtures and the focused currentness command. |
| V4 Access Control | Only the lock owner publishes; all evidence and verification remain proposal-only/read-only. | T-138-37 and T-138-41; Tasks 1 and 3. | Live contention/signal tests and source-policy regression proof. |
| V5 Validation, Sanitization and Encoding | Refs, receipts, commit metadata, diffs, paths, and remote fields are untrusted. | T-138-38 through T-138-40; Tasks 2 and 3 validate normalized projections and sanitize diagnostics. | Ref/receipt drift plus hostile commit-shape fixtures. |
| V7 Error Handling and Logging | Any uncertain collection or history condition must preserve prior evidence and demand refresh. | T-138-37 through T-138-40; all tasks return nonzero with source-specific safe diagnostics. | Live interruption, retry, ambiguous-diff, unknown-commit, and external-drift tests. |

## High-Severity Blocking Rule

Any failed or missing mitigation for T-138-37, T-138-38, T-138-39, or T-138-40 blocks Plan 138-10. Path-only authorization, an undisclosed post-snapshot commit, a changed source receipt, or a published drifted snapshot is forbidden at ASVS L1.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: live signal-safe publication; `capture_snapshot_identity`; source receipt fingerprints; `verify_snapshot_identity_unchanged`; read-only `--verify-snapshot-relation LEDGER`; semantic lifecycle classifiers and refresh verdicts.
- `test/support/lockspire/release_proof/package_assertions.ex`: live process holds/signals, ref and receipt drift fixtures, temporary lifecycle histories, authorized bookkeeping cases, and all hostile near-misses.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: focused live-writer, snapshot-currentness, and post-snapshot semantic-drift entry points.

<verification>
- `bash -n scripts/maintainer/baseline_inventory.sh`
- `mix test test/lockspire/release/repository_hygiene_contract_test.exs`
- `mix test test/lockspire/release_readiness_contract_test.exs`
- `node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy`
</verification>

<success_criteria>
- Both behavior-unverified concurrency items and the stale-snapshot root cause have executable proof.
- The snapshot is source-stable at publication and currentness is an explicit immutable-anchor-plus-classified-drift relation, never an unchanged-HEAD claim.
- Every accepted lifecycle commit is proven by message, identity, path, and semantic content; every hostile near-miss requires refresh.
- Plans 138-07 through 138-09 remain intact, all five requirements and D-01 through D-24 remain covered, and no deferred subsystem or mutation enters scope.
- The 14 spec-less edge candidates and descriptor-less prohibitions remain explicit with no silent drop.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-10-SUMMARY.md` when done. Plan 138-11 then publishes and verifies the canonical ledger from the committed Plan 138-10 implementation.
</output>
