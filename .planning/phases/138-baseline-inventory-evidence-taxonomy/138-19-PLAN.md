---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "19"
type: execute
wave: 19
depends_on: ["138-18"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-01, BASE-02, LOOSE-01]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "BASE-01 D-07 D-08: relation-time status, branch, tag, worktree, origin, GitHub, and maintained receipt reads are complete and matching before currentness can be authorized."
    - statement: "BASE-01 unclassified: any unreadable or unrecognized relation input is disclosed as refresh_required with a source-specific flagged limitation."
      verification: backstop
    - "BASE-02 D-06 D-08: local branch, tag, and worktree topology is recollected through the same normalized projection and fingerprint used by the immutable ledger; any drift exits nonzero."
    - "BASE-02 ordering and idempotency: stable topology and repeated relation runs produce the same ordered rows/verdict without mutating repository state."
    - "LOOSE-01 D-14 D-16 D-20: a snapshot ledger must satisfy the complete canonical schema and every authorized lifecycle class must positively validate every changed companion document."
  prohibitions:
    - statement: "A minimally specified, partial, wrong-repository, or forged ledger must not become snapshot authority."
      verification: flagged-unverified
    - statement: "An allowed-looking subject or path set must not authorize deletion, type change, arbitrary companion content, or semantic regression."
      verification: flagged-unverified
    - statement: "The relation verifier must not mutate refs, worktrees, the index, GitHub objects, maintained records, release-owned files, or ledger bytes."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Strict ledger schema/receipt validation, topology-aware currentness, and complete positive lifecycle transition validators."
      contains: "resolve_snapshot_ledger_commit"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Forged-ledger, topology/status failure, and companion-document mutation histories."
      exports: ["assert_baseline_inventory_relation_integrity_gaps!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused tagged relation-integrity entry point."
      contains: "closes snapshot relation integrity gaps"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: canonical ledger frontmatter and receipts
      via: "resolve_snapshot_ledger_commit validates schema before verify_external_snapshot_receipts can authorize currentness"
      pattern: "resolve_snapshot_ledger_commit|verify_external_snapshot_receipts"
    - from: scripts/maintainer/baseline_inventory.sh
      to: post-snapshot lifecycle commits
      via: "classify_lifecycle_commit delegates every allowed path to a class-specific old-to-new validator"
      pattern: "classify_lifecycle_commit|validate_.*commit"
---

<objective>
Make the snapshot relation authoritative only for a complete canonical ledger and positively benign lifecycle transitions.

Purpose: Fail closed on forged authority, local topology/status drift, and destructive companion-document rewrites.
Output: Strict ledger resolution, complete currentness replay, and full lifecycle transition contracts for verification gaps 5, 6, and 7.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-15-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-16-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Require a complete canonical ledger schema before resolving authority</name>
  <reversibility rating="costly">Ledger schema validation controls the root of trust for all later currentness decisions.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `front_matter_value_from_blob`, `resolve_snapshot_ledger_commit`, `verify_external_snapshot_receipts`, and fingerprint parsing
    - test/support/lockspire/release_proof/package_assertions.ex — `write_snapshot_ledger!/4`, `assert_baseline_inventory_snapshot_commit_resolution!/0`, and relation runner
    - test/lockspire/release/repository_hygiene_contract_test.exs — current snapshot-resolution tests
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md — CR-03 incomplete/forged ledger authority finding
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — immutable relation and source-fingerprint expectations
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-01, D-02, D-06 through D-08, D-14, and D-24
  </read_first>
  <behavior>
    - "Exactly one closed YAML frontmatter block declares phase, scope, complete status, repository identity, collection bounds, valid SHAs, and fingerprints for every declared source."
    - "not_applicable is accepted only for a source excluded by the declared scope; a full canonical ledger requires Git, GitHub, and maintained fingerprints."
    - "Missing/duplicate frontmatter, partial status, invalid SHA/fingerprint, wrong repository, or missing maintained fingerprint returns refresh_required."
  </behavior>
  <action>Add the tagged `:phase138_relation_gap` entry point and replace the minimal `write_snapshot_ledger!` control with a representative complete ledger fixture. Introduce a strict schema validator called before snapshot commit resolution succeeds: parse one bounded frontmatter block, validate canonical phase/scope/status/repository fields, object-format SHAs, collection window, and all declared source fingerprints, and reject implicit `not_applicable`. Extend fixtures for each missing field, duplicate/unterminated frontmatter, partial status, wrong repository, invalid digest, excluded versus required sources, and missing maintained fingerprint. Recollect and compare maintained evidence whenever its scope is declared.</action>
  <acceptance_criteria>
    - Only the complete canonical control resolves and reaches later relation classification.
    - Every forged/minimal/partial/wrong-identity variant exits nonzero refresh_required before authorization.
    - Git, GitHub, and maintained fingerprint comparisons are all required for the full-scope ledger.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_relation_gap</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests</fails_when>
  </verify>
  <done>A ledger becomes relation authority only after its complete canonical schema and declared source receipts are validated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replay topology and status as mandatory read-only currentness evidence</name>
  <reversibility rating="costly">The replay defines when later phases may continue citing an immutable evidence snapshot.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — Task 1 schema gate, `normalize_relation_git_domain`, `verify_git_snapshot_receipt`, `verify_worktree_projection`, and `verify_snapshot_relation`
    - test/support/lockspire/release_proof/package_assertions.ex — complete ledger control, local topology histories, Git failure shims, and before/after state snapshots
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — topology drift and failed-status truths
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-06 through D-10 and D-24
  </read_first>
  <behavior>
    - "Adding, deleting, or moving any local branch, tag, or linked worktree changes the exact domain fingerprint and requires refresh."
    - "A status/topology command failure or malformed output is unavailable evidence and cannot flow through an empty-output branch."
    - "Stable repeated verification is deterministic and leaves refs, worktrees, index, files, external fixtures, and ledger unchanged."
  </behavior>
  <action>Using Task 1's complete ledger fixture, ensure publication and relation verification share one normalized branch/tag/worktree projection and object-format validation. Capture `git status --porcelain=v2 --branch` and every topology command with explicit success/failure state before parsing. Compare domain and aggregate fingerprints, emit a sanitized source-specific row for mismatch/unavailable/malformed evidence, and return nonzero refresh_required. Exercise add/delete/move topology, status exit 42, each topology failure, malformed output, stable repeat, and before/after read-only assertions.</action>
  <acceptance_criteria>
    - Every topology mutation or source failure yields the exact domain row and nonzero refresh_required.
    - A failed status command never becomes a clean worktree row.
    - Stable repeated controls are byte-identical and demonstrably read-only.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_relation_gap</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests</fails_when>
  </verify>
  <done>Immutable evidence is current only while every local topology and worktree-status observation is readable and unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Positively validate every companion document in lifecycle commits</name>
  <reversibility rating="costly">Lifecycle authorization is a security boundary over post-snapshot repository writes.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `classify_lifecycle_commit`, all `validate_*_commit` helpers, raw path-status checks, and working-tree transition validator
    - test/support/lockspire/release_proof/package_assertions.ex — authorized lifecycle histories and hostile deletion/arbitrary-content variants
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md — CR-04 unvalidated companion-document finding
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — destructive phase-completion authorization reproduction
    - /Users/jon/.codex/gsd-core/workflows/execute-plan.md — SUMMARY artifact contract
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md — review, verification, and phase-completion document transitions
  </read_first>
  <behavior>
    - "Every allowed companion path has a class-specific parent-to-child validator or is removed from that class's allowed path set."
    - "Deletion, rename, type change, arbitrary nonempty content, wrong phase, missing structure, or regressive value in any companion makes the commit unknown."
    - "Committed and staged/unstaged transition candidates use the same positive semantic contract."
  </behavior>
  <action>For summary, review, verification, completion, and transition classes, enumerate every currently allowed path and either validate its raw write status, old/new identity, required structure, and monotonic semantic transition or narrow the class so that path is unauthorized. Do not let `all_commit_paths_are_regular_writes` substitute for content proof. Pair each authorized control with arbitrary, wrong-phase, malformed-frontmatter, deletion, rename/type-change, and semantic-regression variants for PLAN, STATE, ROADMAP, REQUIREMENTS, REVIEW, VERIFICATION, and SUMMARY as applicable. Reuse the same validators for index/worktree projections and retain exact gaps-report supersession rules.</action>
  <acceptance_criteria>
    - Arbitrary content in any formerly pass-through companion path yields unknown/refresh_required despite an authorized-looking subject.
    - Every authorized lifecycle control validates all changed files positively and every hostile paired variant fails.
    - Exact supersession, proposal rows, external receipts, topology, and dirty-worktree safeguards remain green.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_relation_gap</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests</fails_when>
  </verify>
  <done>Only fully validated, non-destructive lifecycle changes remain authorized after the snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ledger bytes → snapshot authority | Hand-written or partial frontmatter can forge the root of currentness. |
| Live Git state → relation verdict | Topology and status can drift or become unreadable independently of commit history. |
| Lifecycle commits/working edits → authorization | Familiar subjects and paths can conceal destructive or unrelated companion changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-76 | Spoofing | ledger schema resolution | high | mitigate | Validate the complete canonical schema and every declared fingerprint before resolution. |
| T-138-77 | Tampering / Repudiation | topology and status replay | high | mitigate | Shared normalized projections, explicit failures, exact fingerprints, and read-only assertions. |
| T-138-78 | Spoofing / Tampering | lifecycle classification | high | mitigate | Positive old-to-new validation for every changed companion document. |
| T-138-79 | Elevation of privilege | committed/uncommitted authorization | high | mitigate | Reject all unvalidated paths, statuses, identities, structures, and semantic regressions. |

## ASVS L1 and Blocking Rule

V4 least authority, V5 validation, and V7 fail-visible error handling apply. Any high-severity mitigation failure blocks this plan; no partial authority is accepted.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: strict ledger schema validator; complete external/local receipt replay; positive lifecycle companion validators.
- `test/support/lockspire/release_proof/package_assertions.ex`: `assert_baseline_inventory_relation_integrity_gaps!/0` with forged ledgers, topology/status failures, and companion mutation matrices.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: tagged `:phase138_relation_gap` test.
- No schema/ORM path is in scope and no schema push is required.

<verification>
- `bash -n scripts/maintainer/baseline_inventory.sh`
- `mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_relation_gap`
- `mix test test/lockspire/release_readiness_contract_test.exs`
</verification>

<success_criteria>
- Verification gaps 5, 6, and 7 are closed by complete schema/currentness/transition proof.
- Supporting review CR-03 and CR-04 have exact fail-first hostile histories.
- BASE-01 unclassified plus BASE-02 ordering/idempotency probe items remain explicit and fail closed.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-19-SUMMARY.md` when done.
</output>
