---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "22"
type: execute
wave: 22
depends_on: ["138-21"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-02, TRIAGE-01, LOOSE-01]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "TRIAGE-01 D-06 D-07 D-10 D-14 D-16: each nested PR check result carries exactly one commit OID equal to the outer row's headRefOid; missing, ambiguous, malformed, or unequal OIDs make that PR and aggregate evidence partial with no affirmative disposition authority."
    - "TRIAGE-01 concurrency: a process-level fixture that changes the PR head between outer and nested queries proves the production collector fails closed instead of joining different observations."
    - "TRIAGE-01 adjacency/empty/ordering: exact duplicate agreement, complete-zero behavior, and deterministic numeric row order remain unchanged while temporal binding is enforced."
    - "LOOSE-01 D-17 D-18 D-20: archive expansion is classified from the complete tracked blob, while only the rendered display excerpt is bounded; an actionable marker after more than 400 benign characters produces an individual REC row."
    - statement: "LOOSE-01 unclassified: any archive whose full-document state cannot be read or classified remains an explicit partial or ambiguous receipt rather than a summarized complete record."
      verification: backstop
    - "BASE-02 D-06 D-07 D-10: branch, tag, and worktree object IDs are accepted only when valid_snapshot_sha matches SNAPSHOT_OBJECT_FORMAT exactly; intermediate and opposite-format lengths make the affected domain partial."
    - "BASE-02 adjacency/empty/ordering/idempotency/concurrency: format rejection preserves valid sibling rows, honest complete-zero behavior, deterministic ordering and IDs, repeatable output, and the existing lock-stable snapshot contract."
    - "D-03 D-04 D-09 D-15 D-24: corrections stay in the tracked repo-local collector and existing ExUnit contract surface, perform no disposition, and add no runtime, Mix-task, schema, sidecar, or hygiene-checker ownership."
  prohibitions:
    - statement: "PR evidence must not combine outer metadata from one commit with nested checks from another commit or grant authority when commit identity is absent or ambiguous."
      verification: flagged-unverified
    - statement: "Display truncation must not control full-document maintained-record classification or hide late actionable evidence."
      verification: flagged-unverified
    - statement: "Malformed or wrong-format Git object IDs must not be rendered as complete evidence, and one malformed row must not erase valid siblings."
      verification: flagged-unverified
    - statement: "Gap closure must not execute proposal dispositions, mutate GitHub objects or refs, prune local tags, or broaden Lockspire product/runtime surface."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Temporally bound PR checks, full-blob archive classification, and repository-format-bound Git object validation."
      contains: "collect_pr_check_evidence"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Process-level push-between-queries, late-marker, and adversarial object-format fixtures."
      exports: ["assert_baseline_inventory_current_verification_gaps!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused tagged entry point for the current Phase 138 verification gaps."
      contains: "closes current Phase 138 verification gaps"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: "normalized PR rows and disposition authority"
      via: "combine_pr_check_pages returns one nested commit_oid which collect_pr_check_evidence compares exactly with outer headRefOid before attaching checks"
      pattern: "commit_oid|headRefOid|nested_head"
    - from: scripts/maintainer/baseline_inventory.sh
      to: "maintained REC rows"
      via: "requires_archive_expansion receives the full collector-owned blob; fragment remains display-only"
      pattern: "requires_archive_expansion"
    - from: scripts/maintainer/baseline_inventory.sh
      to: "Git branch, tag, and worktree receipts"
      via: "all observed object names pass through valid_snapshot_sha after SNAPSHOT_OBJECT_FORMAT capture"
      pattern: "valid_snapshot_sha"
---

<objective>
Close the three current evidence-integrity defects and the object-format warning through production-path adversarial contracts.

Purpose: Ensure PR, maintained-record, and Git-domain evidence cannot look complete when observations are temporally incoherent, display-truncated, or impossible for the repository format.
Output: A hardened tracked collector plus focused ExUnit process regressions, without changing the canonical ledger yet.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-RESEARCH.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-PATTERNS.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-17-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-20-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-21-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind one PR row end-to-end to one exact head commit</name>
  <reversibility rating="costly">The normalized nested-check receipt is shared by PR completeness, rendering, and disposition authority, so undo requires coordinated collector and fixture changes.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `combine_graphql_pages`, `combine_pr_check_pages`, `collect_pr_check_evidence`, `validate_github_rows`, and PR disposition rendering
    - test/support/lockspire/release_proof/package_assertions.ex — existing GitHub fixtures, fake `gh` process, and `assert_baseline_inventory_github_integrity_gaps!/0`
    - test/lockspire/release/repository_hygiene_contract_test.exs — existing `:phase138_github_gap` entry point and timeout conventions
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed temporal-coherence truth and required process regression
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-06, D-07, D-10, D-14 through D-16, D-23, and D-24
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md — existing nested check-run/status-context capability decision
  </read_first>
  <behavior>
    - "A one-PR outer query and nested query returning the same full commit OID produce one coherent validated PR row end-to-end."
    - "A push simulated between the outer and nested process calls returns a different nested commit OID, makes nested_complete false, and suppresses merge-ready authority."
    - "Missing, null, multiple, malformed, or page-varying nested commit OIDs fail closed with a specific sanitized limitation."
  </behavior>
  <action>Start red by adding a tagged `:phase138_current_gap` test delegating to `assert_baseline_inventory_current_verification_gaps!/0`, with a fake authenticated `gh` process that records outer capture and then returns a different latest commit OID for the nested call. Extend the nested GraphQL query to request the latest commit's `oid` alongside its status-check contexts. Make `combine_pr_check_pages` validate exactly one non-null latest commit, require the same full OID on every pagination response, and emit `commit_oid` only with complete contexts. Pass each outer `headRefOid` into `collect_pr_check_evidence`, compare exact case-normalized hex identity before attaching checks, and record a precise fail-closed nested limitation for missing, ambiguous, malformed, page-changing, or unequal identity. Ensure `validate_github_rows`, aggregate completeness, `summarize_pr_checks`, and `propose_pr_disposition` cannot grant `merge-ready` on any identity failure. Preserve the safe-field allowlist, top-level GraphQL error handling, cursor/count invariants, exact duplicate corroboration, deterministic order, and proposal-only D-15 boundary. Keep the tracked `.planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md` unchanged because commit identity is a consistency field within its already-integrated nested check-run/status-context pagination capability, not a new GitHub operation.</action>
  <acceptance_criteria>
    - Matching outer and nested full OIDs retain complete PR evidence and existing disposition behavior.
    - A real process-call sequence that changes the nested OID after outer capture makes the PR namespace and aggregate partial and never renders merge-ready.
    - Missing, ambiguous, malformed, or page-inconsistent nested identity produces no affirmative disposition and no unsafe diagnostic content.
    - Existing pagination, duplicate, pending-check, complete-zero, redaction, and numeric ordering cases remain green.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_current_gap</automated>
    <fails_when>non-zero exit, the ExUnit summary reports 0 tests, the mismatch fixture renders merge-ready, or a nested receipt lacks one commit_oid on the matching path</fails_when>
  </verify>
  <done>One production PR path is proven end-to-end to join outer metadata and nested checks only at the exact same head commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify archive expansion from the complete tracked document</name>
  <reversibility rating="reversible">The expansion predicate remains internal to the maintained-family collector and can be changed with its focused fixtures.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `requires_archive_expansion`, archive-family loop, `MAINTAINED_RECORD_OUTPUT`, fragment construction, classification, and REC rendering
    - test/support/lockspire/release_proof/package_assertions.ex — maintained archive fixtures and `assert_baseline_inventory_maintained_integrity_gaps!/0`
    - test/lockspire/release/repository_hygiene_contract_test.exs — `:phase138_maintained_gap` and new current-gap delegation point
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — late actionable-marker failure
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-05, D-07, D-17 through D-20, D-22 through D-24
  </read_first>
  <behavior>
    - "An archived tracked document with more than 400 benign characters before a later actionable marker expands into an individual stable REC row."
    - "The rendered excerpt remains bounded and safely encoded independently of the full-document expansion decision."
    - "A genuinely fulfilled archive remains summarized; a full-blob read/classification failure makes the family and aggregate partial."
  </behavior>
  <action>Start with a late-marker fixture whose first 400-plus characters are benign and whose later full blob contains one recognized actionable, unresolved, contradictory, ambiguous, or fix-now marker. Pass the collector-owned complete blob path to `requires_archive_expansion` or an equivalent full-document classifier; construct the bounded fragment separately and only after expansion authority is known. Preserve D-18 archive summaries for genuinely fulfilled records, D-19 exclusion-before-count behavior, exact path identity, stable REC deduplication, redaction, valid UTF-8, and final field encoding. Add negative controls proving a late marker expands, a benign archive stays summarized, and an unreadable/malformed authoritative blob is visibly partial rather than complete-zero or archive-summary evidence.</action>
  <acceptance_criteria>
    - The late-marker archive yields exactly one individual REC row with its stable path-derived identity.
    - Display text stays bounded and safely encoded while classification observes the complete document.
    - Benign archives remain summarized and unreadable or ambiguous archives fail visibly without action authority.
    - Existing exclusion counts, reverse ordering, deduplication, hostile paths, and archive complete-zero behavior remain green.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_current_gap</automated>
    <fails_when>non-zero exit, the ExUnit summary reports 0 tests, the late-marker fixture is absent from REC rows, or the rendered excerpt exceeds its documented bound</fails_when>
  </verify>
  <done>Archive expansion uses authoritative full-document evidence while display remains independently bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce the repository object format across every Git domain</name>
  <reversibility rating="reversible">Object validation centralizes on the existing private `valid_snapshot_sha` helper with no external contract change.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `capture_snapshot_identity`, `valid_snapshot_sha`, `collect_git_refs`, `collect_git_tags`, `finalize_worktree_stanza`, and `collect_git_worktrees`
    - test/support/lockspire/release_proof/package_assertions.ex — fake Git `--show-object-format`, branch/tag/worktree output, malformed-domain, and sibling-retention scenarios
    - test/lockspire/release/repository_hygiene_contract_test.exs — Git domain receipt and new current-gap entry points
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — impossible 41–63-character OID warning
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-06, D-07, D-09, D-10, D-14, and D-24
  </read_first>
  <behavior>
    - "A SHA-1 fixture accepts exactly 40 hexadecimal characters and rejects 41, 63, and 64 characters in branch, tag, and worktree domains."
    - "A SHA-256 fixture accepts exactly 64 hexadecimal characters and rejects 40, 41, and 63 characters in all three domains."
    - "Malformed rows make only their domain and aggregate partial while valid siblings remain visible as defer-only evidence."
  </behavior>
  <action>Replace every branch, tag, and worktree range-length object check with the existing `valid_snapshot_sha` authority after `SNAPSHOT_OBJECT_FORMAT` is captured. Apply it both when parsing worktree `HEAD` lines and when finalizing stanzas so no intermediate path can reaccept an invalid value. Add adversarial SHA-1 and SHA-256 fake-Git scenarios for intermediate length, opposite valid-format length, non-hex, empty, mixed valid/invalid siblings, blank stanza closure, stanza rollover, and final flush. Require partial/unavailable receipts, suppression of only malformed rows, preservation of valid sibling IDs/order, and absence of successful-zero wording on malformed output.</action>
  <acceptance_criteria>
    - Branch, tag, and worktree rows accept only the exact length and hexadecimal shape selected by SNAPSHOT_OBJECT_FORMAT.
    - Intermediate and opposite-format lengths are rejected in SHA-1 and SHA-256 fixtures across all three domains.
    - Valid siblings remain visible with stable IDs while the affected domain and aggregate are partial and non-authoritative.
    - Existing baseline refs, empty-domain behavior, lock/currentness checks, and non-destructive command policy remain green.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_current_gap &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
    <fails_when>non-zero exit, either ExUnit invocation reports 0 tests, any 41–63-character object ID renders as complete, or an opposite-format ID is accepted</fails_when>
  </verify>
  <done>Every Git-domain object identity is validated against the captured repository format and malformed evidence fails closed without erasing safe siblings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Outer PR query → nested check query | Remote state can change between authenticated read-only observations. |
| Tracked archive blob → expansion decision and excerpt | Full-document evidence is authoritative; display truncation is not. |
| Git command output → evidence rows | Ref and worktree object IDs are untrusted until bound to the repository object format. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-88 | Spoofing / Repudiation | PR outer/nested join | high | mitigate | Return one nested commit OID, require exact equality with outer headRefOid, and process-test a push between calls. |
| T-138-89 | Tampering / Repudiation | archive expansion | high | mitigate | Classify from the complete tracked blob, bound only display, and test a late actionable marker. |
| T-138-90 | Spoofing / Tampering | Git domain object IDs | high | mitigate | Route every object through valid_snapshot_sha bound to SNAPSHOT_OBJECT_FORMAT and cover both formats adversarially. |
| T-138-91 | Information disclosure | failure diagnostics and Markdown | high | mitigate | Retain safe-field allowlists, sanitized limitations, structured records, redaction, and format-specific encoding. |
| T-138-92 | Elevation of privilege | proposal disposition authority | high | mitigate | Any temporal, archive, or object-integrity ambiguity makes the affected source and aggregate partial with defer-only evidence. |

## ASVS L1 and Blocking Rule

V2 applies to the existing authenticated read-only GitHub boundary; V4, V5, and V7 govern bounded authority, validation/encoding, and fail-visible errors. Any failed high-severity mitigation blocks completion of this plan and canonical recollection.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: exact outer/nested PR-head binding, full-document archive expansion authority, and object-format-bound Git-domain validation.
- `test/support/lockspire/release_proof/package_assertions.ex`: `assert_baseline_inventory_current_verification_gaps!/0` plus matching/mismatching PR heads, late archive markers, and SHA-1/SHA-256 adversarial fixtures.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: tagged `:phase138_current_gap` production-path test.
- `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-22-SUMMARY.md`: executor evidence for all three failed integrity truths and the object-format warning.
- `.planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md` remains unchanged because the nested commit OID strengthens the already-integrated nested check-run/status-context pagination capability rather than adding a GitHub operation.
- No canonical ledger replacement, sidecar, schema/ORM file, runtime module, dependency, public API, GitHub mutation, or disposition action is produced in this plan.

## Current verification gaps mapped

| Gap | Closing task |
|-----|--------------|
| Nested PR checks are not bound to outer headRefOid | Task 1 |
| Archive expansion is decided from a 400-character display excerpt | Task 2 |
| Git domains accept impossible 41–63-character object IDs | Task 3 |
| Canonical ledger is stale at HEAD | Deferred by dependency to 138-23 after these fixes |

## Spec-less probe no-silent-drop audit

| Candidate | Explicit disposition in this plan |
|-----------|-----------------------------------|
| BASE-01 unclassified | Not implementation-relevant here; retained for 138-23 live baseline/currentness audit. |
| BASE-02 adjacency | Mixed valid/invalid Git-domain siblings retain safe rows and partial authority. |
| BASE-02 empty | Empty valid domains remain explicit complete-zero; malformed domains cannot masquerade as empty. |
| BASE-02 ordering | Valid sibling rows keep deterministic ordering after malformed rows are suppressed. |
| BASE-02 idempotency | Repeated SHA-1/SHA-256 fixture runs preserve identical rows and receipts. |
| BASE-02 concurrency | Existing lock-stable snapshot contract is rerun and preserved. |
| TRIAGE-01 adjacency | Outer and nested observations join only on exact PR identity plus head OID. |
| TRIAGE-01 empty | Existing authenticated complete-zero PR contract remains green. |
| TRIAGE-01 ordering | Existing deterministic numeric PR ordering remains green. |
| TRIAGE-01 concurrency | A push between outer and nested process calls fails closed. |
| TRIAGE-02 adjacency | Not changed here; exact issue duplicate agreement is rerun by the full focused suite and live audit in 138-23. |
| TRIAGE-02 empty | Not changed here; authenticated issue complete-zero is rerun by the full focused suite and live audit in 138-23. |
| TRIAGE-02 ordering | Not changed here; deterministic numeric issue ordering is rerun by the full focused suite and live audit in 138-23. |
| LOOSE-01 unclassified | Full-blob unreadable or ambiguous archive evidence becomes an explicit partial/ambiguous receipt. |

No-silent-drop equality: 14 surfaced candidates = 14 explicit dispositions across this implementation plan and its dependent final-currentness plan; none is omitted or dismissed.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan / Task | Status |
|--------|----|----------------------|-------------|--------|
| GOAL | — | One current non-destructive Git/GitHub/maintained evidence inventory | 138-22 then 138-23 | COVERED |
| REQ | BASE-01 | Exact local/remote state or limitation | 138-23 | COVERED |
| REQ | BASE-02 | Exact-format refs/worktrees and proposal-only rows | 138-22 Task 3, 138-23 | COVERED |
| REQ | TRIAGE-01 | Temporally coherent complete PR evidence | 138-22 Task 1, 138-23 | COVERED |
| REQ | TRIAGE-02 | Complete issue evidence and honest zero | preserved suite plus 138-23 live audit | COVERED |
| REQ | LOOSE-01 | Full maintained follow-up inventory | 138-22 Task 2, 138-23 | COVERED |
| RESEARCH | receipt-first/fail-visible | Validate before render and preserve explicit limitations | 138-22 Tasks 1-3 | COVERED |
| RESEARCH | Bash plus ExUnit boundary | No package, schema, runtime, or public surface additions | 138-22 Tasks 1-3 | COVERED |
| CONTEXT | D-01–D-05 | Canonical Markdown and repo-maintainer ownership | 138-22 boundary, 138-23 publication | COVERED |
| CONTEXT | D-06–D-11 | Provenance, completeness, safe commands, and redaction | 138-22 Tasks 1-3, 138-23 | COVERED |
| CONTEXT | D-12–D-16 | Exact taxonomy, evidence fields, proposal-only authority | 138-22 Task 1, 138-23 | COVERED |
| CONTEXT | D-17–D-20 | Maintained families, full-document archive decisions, exclusions, deduplication | 138-22 Task 2, 138-23 | COVERED |
| CONTEXT | D-21–D-24 | Calm accessible output and focused executable proof | 138-22 Tasks 1-3, 138-23 | COVERED |
| CONTEXT | Deferred ideas | Sidecar/database/UI/CI subsystem and product/runtime surface | none | EXCLUDED |
| VERIFICATION | stale canonical ledger | immutable recollection after fixes | 138-23 | COVERED |
| VERIFICATION | outer/nested PR head mismatch | exact OID binding plus inter-query push fixture | 138-22 Task 1 | COVERED |
| VERIFICATION | late archive marker | full-blob classification plus >400-character fixture | 138-22 Task 2 | COVERED |
| VERIFICATION | 41–63-character Git OIDs | SNAPSHOT_OBJECT_FORMAT validation plus adversarial fixtures | 138-22 Task 3 | COVERED |

All current goal, requirement, research, locked-context, verification, and review inputs are covered. Deferred product/runtime work and later-phase mutations remain explicit exclusions.

<verification>
- Run shell syntax and the tagged current-gap test after each behavior slice.
- Run the complete repository hygiene contract after all three changes to catch regression across shared fixtures.
- Require failures to remain source-specific, proposal-only, sanitized, and non-destructive.
</verification>

<success_criteria>
- Nested checks can never acquire authority without an exact outer-head match, including a push between queries.
- Archive classification observes the complete blob while only display is bounded.
- Every Git-domain OID is exact for the captured SHA-1 or SHA-256 repository format.
- The full focused suite passes with no existing completeness, redaction, ordering, identity, or publication regression.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-22-SUMMARY.md` when done.
</output>
