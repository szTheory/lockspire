---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "20"
type: execute
wave: 20
depends_on: ["138-19"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
autonomous: true
gap_closure: true
requirements: [BASE-02, LOOSE-01]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "LOOSE-01 D-11 D-14 D-20: hostile maintained paths remain named structured records through aggregation, so no path byte can shift lifecycle, disposition, evidence, confidence, recheck, authority, or execution fields."
    - "LOOSE-01 D-07 D-17 D-23: tracked-marker exit 1 plus an empty buffer is complete-zero; exit 0 requires valid records; every other or inconsistent state is unavailable/partial."
    - statement: "LOOSE-01 unclassified: an unmatched tracked follow-up remains an explicit flagged ambiguous receipt with canonical identity and recheck need rather than a guessed disposition."
      verification: backstop
    - "BASE-02 D-18 D-19 empty and adjacency: all-excluded archive matches produce complete-zero and mixed matches count/expand only included paths from one filtered set."
    - "BASE-02 ordering and idempotency: REC rows, archive summaries, YAML scalars, and Markdown cells are deterministic and valid across tabs, indicators, Unicode, and invalid path bytes."
  prohibitions:
    - statement: "Untrusted path or prose bytes must not shift semantic columns, create records/headings, expose secrets, or execute as shell content."
      verification: flagged-unverified
    - statement: "Excluded maintained paths must not inflate archive counts or create evidence rows."
      verification: flagged-unverified
    - statement: "Repository tests must not depend on a personal absolute Codex or GSD installation path."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Structured hostile-path transport, exact marker exits, pre-count archive filtering, and portable YAML/UTF-8 rendering."
      contains: "collect_maintained_records"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Semantic-field, marker-zero, archive-exclusion, YAML/UTF-8, and portable parser assertions."
      exports: ["assert_baseline_inventory_maintained_integrity_gaps!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused tagged maintained-integrity entry point."
      contains: "closes maintained evidence integrity gaps"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: REC rows and archive summaries
      via: "included NUL-delimited paths become named JSON records before count, aggregation, and final field encoding"
      pattern: "maintained_record_json|deduplicate_record_refs"
    - from: test/support/lockspire/release_proof/package_assertions.ex
      to: canonical ledger frontmatter
      via: "repository-portable parsing verifies YAML and UTF-8 without a developer-home dependency"
      pattern: "assert_baseline_inventory_maintained_integrity_gaps!"
---

<objective>
Close maintained-record integrity gaps from exact Git path bytes through valid, portable Markdown evidence.

Purpose: Preserve every semantic field, honest zero/count receipt, and canonical encoding without incidental paths or developer-local test dependencies.
Output: Hardened maintained collection/rendering and focused contracts for verification gaps 8, 9, and 11.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-14-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove hostile maintained paths retain every named semantic field</name>
  <reversibility rating="costly">REC identity and field semantics are cross-phase citation contracts.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `maintained_record_json`, `deduplicate_record_refs`, `render_maintained_inventory`, exact-path lookup, and field encoders
    - test/support/lockspire/release_proof/package_assertions.ex — hostile maintained repository fixture and `assert_baseline_inventory_maintained_encoding!/0`
    - test/lockspire/release/repository_hygiene_contract_test.exs — existing maintained encoding test
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — tab-delimited semantic corruption and hostile YAML/UTF-8 truths
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-05, D-10 through D-15, D-20, D-22, and D-24
  </read_first>
  <behavior>
    - "One hostile exact Git path yields one stable REC ID and all named D-14 fields remain attached to that ID."
    - "Cross-family duplicates and supersession aggregate by REC ID without losing references or rationale."
    - "Structured parse/encoding failure makes the exact family and aggregate partial without rendering a shifted row."
  </behavior>
  <action>Add the tagged `:phase138_maintained_gap` entry point and per-ID assertions for kind, canonical/display subject, lifecycle, disposition, evidence references, rationale, confidence, recheck, authority, and executed state using tab, newline, colon, pipe, backtick, brackets, shell metacharacters, multibyte Unicode, and invalid-byte paths. Audit every maintained buffer so untrusted values remain named JSON records until final per-field Markdown/YAML encoding; eliminate any remaining shell delimiter parsing. Preserve exact path bytes for Git lookup/REC hashing, aggregate references by stable ID, and downgrade malformed structured data before rendering.</action>
  <acceptance_criteria>
    - Every hostile fixture produces exactly one stable record with all expected named fields and no side effect, shifted field, injected row, or heading.
    - Reverse-order duplicates and supersession retain every evidence reference/rationale deterministically.
    - Malformed internal structured data is partial and cannot produce a row.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_maintained_gap</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests</fails_when>
  </verify>
  <done>Every maintained subject crosses the collector as one stable named record whose semantics cannot be shifted by path bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive marker and archive receipts from one included-path set</name>
  <reversibility rating="reversible">Selector and archive accounting are isolated behind maintained-family receipts.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `collect_maintained_family_paths`, `is_excluded_maintained_path`, `is_archive_family`, `summarize_archive_container`, and marker `git grep` handling
    - test/support/lockspire/release_proof/package_assertions.ex — Task 1 fixture plus marker/selector/archive helpers
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md — WR-02 archive counts before exclusions
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — marker-free complete-zero truth
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-07, D-17 through D-20, D-23, and D-24
  </read_first>
  <behavior>
    - "Marker exit 1 plus empty buffer is complete-zero; exit 0 requires valid records; exit greater than 1 or inconsistent output is unavailable/partial."
    - "Archive exclusions are applied before count, summary ID, complete-zero, or expansion decisions."
    - "All-excluded and mixed archive fixtures use the same filtered path set for every receipt and row."
  </behavior>
  <action>Capture the marker selector's status and NUL buffer independently, validate the exact exit/buffer matrix, and retain calm successful-zero copy only for exit 1 with empty output. For archive families, filter NUL-delimited candidate paths through D-19 exclusions into a collector-owned included-path buffer first; compute count, complete-zero, summary identity, and expansion exclusively from that buffer. Add marker-free, exit 0/1/2/42 inconsistency, all-excluded, mixed included/excluded, reverse-order, and gate-owned excluded-path fixtures without reading or rewriting excluded trees.</action>
  <acceptance_criteria>
    - Marker-free repositories produce a complete count-zero receipt and no REC marker row; all non-success variants remain partial/unavailable.
    - All-excluded archives produce complete-zero, while mixed archives count and expand only included paths.
    - Other family failures remain independent and cannot be masked by a successful marker/archive result.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_maintained_gap</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests</fails_when>
  </verify>
  <done>Marker and archive receipts are exact, exclusion-aware observations rather than inferred counts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make YAML and UTF-8 proof repository-portable</name>
  <reversibility rating="costly">Canonical frontmatter and display encoding are durable interfaces consumed by later phases.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `normalize_utf8_bytes`, `yaml_quoted_scalar`, `markdown_value`, and `render_front_matter`
    - test/support/lockspire/release_proof/package_assertions.ex — current `read_frontmatter_with_gsd!` absolute invocation and hostile parser fixtures
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md — WR-03 personal absolute test path finding
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — invalid YAML/UTF-8 reproduction
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-01, D-02, D-06, D-11, D-21 through D-24
  </read_first>
  <behavior>
    - "Dynamic YAML strings round-trip as one scalar and the complete artifact is valid UTF-8."
    - "Valid Unicode remains intact; invalid filename bytes use deterministic reversible display encoding without changing identity input."
    - "The focused contract runs on CI and any developer account without a hard-coded home path."
  </behavior>
  <action>Remove the personal absolute GSD executable from `PackageAssertions`. Parse frontmatter through repository-owned test logic or an explicitly configured tool resolved from the active environment with a clear prerequisite failure; keep the test hermetic and CI-safe. Exercise YAML indicators, leading/trailing whitespace, quotes, tabs, euro/café text, and invalid bytes through the production renderer. Keep YAML scalar encoding distinct from Markdown cell encoding, assert `String.valid?/1`, round-trip every dynamic scalar, and verify the snapshot-relation reader consumes the same values.</action>
  <acceptance_criteria>
    - The repository contains no developer-home path in the Phase 138 contract helper.
    - Hostile dynamic values produce valid UTF-8, parseable YAML, stable Markdown columns, and exact round-trips.
    - Relation parsing, redaction, control-byte removal, and invalid-byte display controls remain green.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_maintained_gap</automated>
    <fails_when>non-zero exit, or the summary reports 0 tests</fails_when>
  </verify>
  <done>Canonical encoding is valid and its proof runs from repository context rather than one maintainer's machine layout.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Exact Git path bytes → maintained records | Hostile bytes can corrupt semantics before stable identity and classification. |
| Candidate paths → archive/marker receipts | Excluded or no-match paths can falsify counts and completeness. |
| Structured records → YAML/Markdown artifact | Encoding and developer-local tooling can make canonical evidence unreadable or unverifiable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-80 | Tampering / Elevation | REC transport | high | mitigate | Named JSON records and per-ID semantic assertions through final encoding. |
| T-138-81 | Repudiation | marker/archive receipts | high | mitigate | Exact exit/buffer taxonomy and exclusion-before-count from one filtered set. |
| T-138-82 | Tampering / Information disclosure | YAML/Markdown/UTF-8 | high | mitigate | Format-specific encoders, parser round-trips, valid-Unicode preservation, and redaction. |
| T-138-83 | Denial of service | test portability | medium | mitigate | Repository-owned or explicitly resolved parser with clear prerequisites and no personal path. |

## ASVS L1 and Blocking Rule

V4 bounded read authority, V5 validation/encoding, and V7 fail-visible errors apply. Any high-severity mitigation failure blocks this plan.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: fully structured maintained transport; exact marker exit handling; exclusion-first archive accounting; valid YAML/UTF-8/Markdown rendering.
- `test/support/lockspire/release_proof/package_assertions.ex`: `assert_baseline_inventory_maintained_integrity_gaps!/0` and portable parser/hostile path fixtures.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: tagged `:phase138_maintained_gap` test.
- No canonical sidecar, schema/ORM path, runtime module, dependency, or public API is introduced.

<verification>
- `bash -n scripts/maintainer/baseline_inventory.sh`
- `mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_maintained_gap`
- `mix format --check-formatted test/lockspire/release/repository_hygiene_contract_test.exs test/support/lockspire/release_proof/package_assertions.ex`
</verification>

<success_criteria>
- Verification gaps 8, 9, and 11 are closed with semantic, marker-zero, and parser/Unicode proofs.
- Supporting review WR-02 and WR-03 have exact regressions.
- LOOSE-01 unclassified plus BASE-02 empty/adjacency/ordering/idempotency probes remain explicit and fail closed.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-20-SUMMARY.md` when done.
</output>
