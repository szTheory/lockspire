---
phase: 138-baseline-inventory-evidence-taxonomy
plan: "17"
type: execute
wave: 17
depends_on: ["138-16"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
  - .planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md
autonomous: true
gap_closure: true
requirements: [TRIAGE-01, TRIAGE-02]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "TRIAGE-01 D-07 D-10 D-16: every outer GraphQL page and every PR status-check page is error-free, terminally complete, count-consistent, and free of pending or unknown conclusions before a PR can receive merge-ready."
    - "TRIAGE-01 adjacency: duplicate PR identities corroborate only when all normalized decision-bearing fields agree; contradictory copies make the PR namespace and aggregate partial."
    - "TRIAGE-01 empty: authenticated terminal zero is distinct from partial data, GraphQL errors, malformed page data, and unavailable collection."
    - "TRIAGE-01 ordering: safe PR rows retain deterministic numeric ordering after invalid or contradictory groups are suppressed."
    - "TRIAGE-01 concurrency: nested and outer GitHub pages are collected while the target lock is held and cannot interleave with another writer."
    - "TRIAGE-02 adjacency: duplicate issue identities require exact normalized agreement and never merge with the PR namespace."
    - "TRIAGE-02 empty: zero open issues is reported only for an authenticated, error-free, terminal collection and remains evidence rather than a health target."
    - "TRIAGE-02 ordering: safe issue rows retain deterministic numeric ordering while contradictory groups remain visibly partial."
  prohibitions:
    - statement: "Incomplete, pending, contradictory, or error-bearing GitHub evidence must not acquire affirmative disposition authority."
      verification: flagged-unverified
    - statement: "The GitHub collector must not retain raw bodies, comments, logs, tokens, credentials, environment values, or uncontrolled response payloads."
      verification: flagged-unverified
    - statement: "The inventory must not turn an empty PR or issue queue into the definition of repository health."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Error-aware outer/nested GraphQL validation and exact-agreement duplicate handling for both namespaces."
      contains: "combine_graphql_pages"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Production-CLI GraphQL error, pending check, contradictory PR, and contradictory issue fixtures."
      exports: ["assert_baseline_inventory_github_integrity_gaps!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused tagged GitHub gap-closure entry point."
      contains: "closes GitHub integrity gaps"
    - path: .planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md
      provides: "The single explicit full-by-default GitHub GraphQL/CLI capability matrix."
      contains: "INTEGRATE"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: GitHub GraphQL page streams
      via: "combine_graphql_pages and combine_pr_check_pages reject top-level errors before normalized rows reach render_github_collection"
      pattern: "errors|contradiction_count"
    - from: test/support/lockspire/release_proof/package_assertions.ex
      to: scripts/maintainer/baseline_inventory.sh
      via: "fake gh page streams execute the production --scope github path"
      pattern: "assert_baseline_inventory_github_integrity_gaps!"
---

<objective>
Close the remaining GitHub completeness and contradiction gaps at the exact parser-to-disposition boundary.

Purpose: Ensure every PR and issue proposal is derived from complete, internally consistent, error-free remote evidence.
Output: Hardened GitHub page normalization and focused adversarial contracts for verification gaps 2 and 10 plus the supporting review findings.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-12-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/138-16-SUMMARY.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Drive one error-bearing pending PR through the complete fail-closed path</name>
  <reversibility rating="costly">GitHub completeness and merge-ready semantics are durable evidence contracts consumed by later maintenance phases.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex, .planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — `combine_graphql_pages`, `combine_pr_check_pages`, `collect_pr_check_evidence`, `summarize_pr_checks`, and `propose_pr_disposition`
    - test/support/lockspire/release_proof/package_assertions.ex — existing fake-`gh` page stream and `assert_baseline_inventory_github_check_completeness!/0`
    - test/lockspire/release/repository_hygiene_contract_test.exs — existing GitHub focused tests and tag style
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — failed PR completeness truth and pending/null reproduction
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md — WR-01 GraphQL partial-data-plus-errors finding
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-07, D-10, D-11, D-14, D-16, and D-24
  </read_first>
  <behavior>
    - "An outer or nested response with a nonempty top-level errors array makes the exact collection partial even when data is structurally usable."
    - "A SUCCESS check beside a pending/null context remains unknown and cannot yield merge-ready."
    - "Safe sibling rows remain visible only as defer evidence when aggregate completeness is partial."
  </behavior>
  <action>Add a tagged failing ExUnit path `:phase138_github_gap` delegating to `assert_baseline_inventory_github_integrity_gaps!/0`, beginning with a production `--scope github` fixture whose PR has one successful check, one pending/null context, and a nonempty GraphQL `errors` array. Validate top-level errors before accepting `.data` in both outer and nested combiners, retain a stable sanitized limitation, and feed the partial state through `summarize_pr_checks`, row validation, aggregate authority, and rendering. Preserve the D-11 safe-field allowlist and the complete terminal-success control from Plan 138-12; no mutation query or new API surface is introduced.</action>
  <acceptance_criteria>
    - Outer and nested partial-data-plus-errors fixtures are partial, name the affected namespace/PR safely, and contain no merge-ready proposal.
    - SUCCESS-plus-pending/null and unknown conclusions remain non-actionable even when every other field is valid.
    - A complete terminal-success control remains complete, deterministically ordered, and proposal-only.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_github_gap &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy</automated>
    <fails_when>non-zero exit, the test summary reports 0 tests, or API coverage reports block=true</fails_when>
  </verify>
  <done>One PR traverses the real query, parser, summarizer, authority, and renderer path, and any error/pending evidence fails closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply exact duplicate agreement to issues and preserve namespace truth</name>
  <reversibility rating="costly">Duplicate agreement controls whether a remote identity can support a later maintainer action.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — Task 1 error-aware page combiners, `collect_github_collection`, `validate_github_rows`, and `render_github_collection`
    - test/support/lockspire/release_proof/package_assertions.ex — Task 1 fixture plus existing PR contradiction and complete-zero issue controls
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-REVIEW.md — CR-02 contradictory issue omission finding
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-VERIFICATION.md — contradictory duplicate truth and namespace expectations
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/138-CONTEXT.md — D-07, D-13, D-14, D-16, D-20, and D-23
  </read_first>
  <behavior>
    - "Two issue copies with one immutable or decision-bearing mismatch make issues and the aggregate partial."
    - "Contradictory identities are disclosed without an actionable row; exact duplicates retain one deterministic corroborated row."
    - "A complete-zero issue result remains valid only when no contradiction or GraphQL error occurred."
  </behavior>
  <action>Generalize contradiction handling so every GitHub collection checks `contradiction_count` before setting rows validated or complete. Compare the normalized allowlisted issue identity/state/title/update/URL fields, retain safely corroborated siblings under aggregate partial status, and emit one stable sanitized contradiction receipt without inventing a disposition for the contradictory identity. Extend the tagged fixture with differing issue titles/states/update times, reversed duplicate order, exact duplicate controls, PR/issue numeric collisions, zero issues, and stable numeric ordering. Update the existing `COVERAGE.md` in place—never create a second coverage artifact—to replace its no-product-integration declaration with a full-by-default matrix for the GitHub CLI/GraphQL surface used here: authentication/repository identity, outer PR pagination, nested check/status pagination, issue pagination, top-level error handling, normalized duplicate agreement, safe-field/redaction handling, and aggregate disposition gating. Mark each exercised capability `INTEGRATE`; any genuine opt-out requires a concrete reason.</action>
  <acceptance_criteria>
    - Every contradictory-issue variant makes the issue namespace and aggregate partial and suppresses close/retain/defer authority for that identity.
    - Exact duplicate issues corroborate to one stable row independent of page order.
    - Separate namespaces, complete-zero wording, redaction, and deterministic ordering remain green.
    - The existing `COVERAGE.md` is the sole coverage artifact, enumerates every GitHub capability above, and passes `api-coverage.verify-pre` with no unexplained opt-out.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_github_gap &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy</automated>
    <fails_when>non-zero exit, the test summary reports 0 tests, or API coverage reports block=true</fails_when>
  </verify>
  <done>PR and issue duplicates share one exact-agreement rule, and contradictory remote observations cannot disappear into a complete receipt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub GraphQL responses → normalized rows | Remote pages, errors, cursors, pending contexts, and duplicates are untrusted completeness inputs. |
| Normalized namespaces → proposal renderer | Only complete and internally consistent evidence may acquire disposition authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-68 | Repudiation | outer/nested GraphQL parsing | high | mitigate | Reject nonempty errors arrays and incomplete/pending contexts before completeness is assigned. |
| T-138-69 | Tampering | duplicate normalization | high | mitigate | Require exact normalized agreement for PR and issue identities and disclose contradictions as partial. |
| T-138-70 | Information disclosure | diagnostics and Markdown | high | mitigate | Preserve safe-field allowlists, sanitized limitations, private buffers, and adversarial redaction checks. |
| T-138-71 | Elevation of privilege | proposal authority | high | mitigate | Gate every affirmative disposition on complete namespace and aggregate evidence. |

## ASVS L1 and Blocking Rule

V4 least authority, V5 validation/encoding, and V7 fail-visible error handling apply. Any failed mitigation at severity high blocks this plan; partial, pending, contradictory, or unsanitized evidence cannot satisfy either task.
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: error-aware `combine_graphql_pages`/`combine_pr_check_pages`, collection-neutral contradiction handling, and preserved `--scope github` surface.
- `test/support/lockspire/release_proof/package_assertions.ex`: `assert_baseline_inventory_github_integrity_gaps!/0` with partial-data errors, pending checks, contradictory issues, exact duplicates, namespace collisions, zero, and ordering fixtures.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: tagged `:phase138_github_gap` production-path test.
- `COVERAGE.md` remains the single existing API-coverage artifact and becomes the explicit full-by-default GitHub CLI/GraphQL matrix; this plan adds no Lockspire product API surface.

<verification>
- `bash -n scripts/maintainer/baseline_inventory.sh`
- `mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_github_gap`
- `node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/138-baseline-inventory-evidence-taxonomy`
</verification>

<success_criteria>
- Verification gaps 2 and 10 are closed by error-aware, pending-aware, exact-agreement behavior for both GitHub namespaces.
- Supporting review CR-02 and WR-01 have exact production-path regressions.
- TRIAGE-01/02 adjacency, empty, ordering, and concurrency probe items remain explicit and green.
</success_criteria>

<output>
Create `.planning/phases/138-baseline-inventory-evidence-taxonomy/138-17-SUMMARY.md` when done.
</output>
