---
phase: 74-fapi-2-0-message-signing-strict-mode
plan: 04
type: execute
wave: 3
depends_on:
  - 74-02
files_modified:
  - lib/lockspire/protocol/introspection.ex
  - lib/lockspire/web/controllers/introspection_controller.ex
  - test/lockspire/protocol/introspection_test.exs
  - test/lockspire/web/introspection_controller_test.exs
autonomous: true
requirements:
  - ENF-01
---

<objective>
Require JWT introspection success responses for strict-profile callers.

Purpose: Strict message-signing mode is incomplete unless `/introspect` rejects success-path downgrade to JSON for callers under the new tier.
Output: A protocol-owned strict caller-entitlement rule plus strict-mode negotiation enforcement in the introspection controller.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-fapi-2-0-message-signing-strict-mode/74-CONTEXT.md
@.planning/phases/74-fapi-2-0-message-signing-strict-mode/74-RESEARCH.md
@.planning/phases/73-jwt-introspection-responses/73-CONTEXT.md
@.planning/phases/74-fapi-2-0-message-signing-strict-mode/74-02-PLAN.md
@lib/lockspire/protocol/introspection.ex
@lib/lockspire/web/controllers/introspection_controller.ex
@test/lockspire/protocol/introspection_test.exs
@test/lockspire/web/introspection_controller_test.exs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define and expose the strict caller-entitlement rule in the protocol layer per D-18 through D-21</name>
  <files>lib/lockspire/protocol/introspection.ex, test/lockspire/protocol/introspection_test.exs</files>
  <behavior>
    - Test 1: an authenticated caller whose own effective profile resolves to `:fapi_2_0_message_signing` is marked entitled for strict introspection handling.
    - Test 2: an authenticated caller whose effective profile is `:none` or `:fapi_2_0_security` is explicitly non-entitled for strict introspection handling, even though the protocol still authenticates it successfully.
    - Test 3: the protocol-owned success or error shape gives the controller enough information to distinguish entitled strict callers from authenticated-but-non-entitled callers without re-authenticating or inferring policy ad hoc.
    - Test 4: inactive payload semantics stay unchanged.
  </behavior>
  <action>Implement the smallest truthful entitlement rule in `Lockspire.Protocol.Introspection`: strict message-signing introspection is reserved for authenticated callers whose own effective security profile resolves to `:fapi_2_0_message_signing`. Authenticated callers with weaker effective profiles remain non-entitled for strict-mode handling and must not be treated as implicitly eligible just because they are confidential clients. Keep this rule protocol-owned, not controller-owned. The controller should receive explicit entitlement truth from `Introspection` so it can enforce strict-mode negotiation without guessing caller policy or inventing a broader resource-server model.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/protocol/introspection_test.exs</automated>
  </verify>
  <done>The protocol layer explicitly distinguishes entitled strict callers from authenticated but non-entitled callers and preserves the existing payload truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject JSON success downgrade for strict callers per D-15 through D-17</name>
  <files>lib/lockspire/web/controllers/introspection_controller.ex, test/lockspire/web/introspection_controller_test.exs</files>
  <behavior>
    - Test 1: missing, wildcard-only, malformed, or JSON-only `Accept` headers do not yield successful JSON responses for strict callers.
    - Test 2: strict callers explicitly negotiating `application/token-introspection+jwt` receive the existing Phase 73 JWT success contract.
    - Test 3: error responses remain JSON, including signer failure paths.
    - Test 4: non-strict callers preserve the Phase 73 optional baseline.
  </behavior>
  <action>Make `IntrospectionController` caller-entitlement-aware: strict callers must positively negotiate `application/token-introspection+jwt` to receive any successful response, while all downgrade and error cases remain standard JSON OAuth errors.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/web/introspection_controller_test.exs</automated>
  </verify>
  <done>Strict callers can only receive successful introspection responses via explicit JWT negotiation, while all downgrade and error paths remain JSON-shaped.</done>
</task>
</tasks>

<verification>
Run the introspection protocol and controller suites, then confirm strict callers cannot receive successful JSON introspection responses without explicit JWT negotiation.
</verification>

<success_criteria>
Under `:fapi_2_0_message_signing`, successful `/introspect` responses require explicit JWT negotiation for strict callers while non-strict callers keep the Phase 73 baseline.
</success_criteria>
