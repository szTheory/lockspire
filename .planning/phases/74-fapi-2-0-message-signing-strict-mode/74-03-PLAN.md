---
phase: 74-fapi-2-0-message-signing-strict-mode
plan: 03
type: execute
wave: 3
depends_on:
  - 74-02
files_modified:
  - lib/lockspire/protocol/authorization_request.ex
  - test/lockspire/protocol/authorization_request_test.exs
autonomous: true
requirements:
  - ENF-01
---

<objective>
Enforce explicit JARM at `/authorize` when strict message-signing mode is effective.

Purpose: Phase 74 must reject non-JARM authorization requests at the protocol seam that already owns redirect-safe validation.
Output: Strict response-mode validation for direct and PAR-backed authorization requests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-fapi-2-0-message-signing-strict-mode/74-CONTEXT.md
@.planning/phases/74-fapi-2-0-message-signing-strict-mode/74-RESEARCH.md
@.planning/phases/74-fapi-2-0-message-signing-strict-mode/74-02-PLAN.md
@lib/lockspire/protocol/authorization_request.ex
@test/lockspire/protocol/authorization_request_test.exs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Reject omitted and raw non-JWT response modes under strict mode per D-08 through D-12</name>
  <files>lib/lockspire/protocol/authorization_request.ex, test/lockspire/protocol/authorization_request_test.exs</files>
  <behavior>
    - Test 1: omitted `response_mode` fails under effective strict message-signing.
    - Test 2: `query`, `fragment`, and `form_post` fail under effective strict message-signing.
    - Test 3: `jwt`, `query.jwt`, `fragment.jwt`, and `form_post.jwt` remain accepted.
    - Test 4: the same rule applies to PAR-backed validation.
  </behavior>
  <action>Add the strict JARM gate in `AuthorizationRequest` after effective-profile resolution and response-mode parsing, not in the Phase 41 plug. Reject invalid strict-mode requests explicitly; do not silently upgrade them.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/protocol/authorization_request_test.exs</automated>
  </verify>
  <done>Strict message-signing requests cannot proceed through `/authorize` without an explicit JWT response mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve fail-closed encrypted-JARM semantics and baseline compatibility per D-13 and D-14</name>
  <files>lib/lockspire/protocol/authorization_request.ex, test/lockspire/protocol/authorization_request_test.exs</files>
  <behavior>
    - Test 1: strict mode does not require JARM encryption.
    - Test 2: already-selected encrypted JARM paths do not downgrade to signed-only or raw behavior.
    - Test 3: non-strict profiles preserve the current compatibility baseline.
  </behavior>
  <action>Keep encryption opt-in and per-client, but ensure strict mode never creates a downgrade path when encrypted JARM has already been requested or configured for the authorization response.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/protocol/authorization_request_test.exs</automated>
  </verify>
  <done>Strict mode preserves optional encryption and fail-closed behavior without creating any downgrade path from encrypted JARM to weaker responses.</done>
</task>
</tasks>

<verification>
Run the authorization-request suite and confirm strict mode requires explicit JARM while baseline profiles remain unchanged.
</verification>

<success_criteria>
Under `:fapi_2_0_message_signing`, `/authorize` rejects non-JARM requests explicitly and preserves fail-closed encrypted-JARM behavior.
</success_criteria>
