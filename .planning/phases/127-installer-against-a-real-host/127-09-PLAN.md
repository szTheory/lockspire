---
phase: 127-installer-against-a-real-host
plan: 09
type: execute
wave: 6
depends_on: [127-02, 127-03, 127-04, 127-05, 127-06, 127-07, 127-08]
files_modified:
  - scripts/maintainer/adopter_path_walk.sh
  - .planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md
  - .planning/phases/127-installer-against-a-real-host/COVERAGE.md
autonomous: false
requirements: [INSTALL-01, INSTALL-02, INSTALL-03]

must_haves:
  truths:
    - "Every one of the twelve installer-attributed defects Phase 127 owns carries an explicit disposition in the ledger — fixed, or deferred with a stated reason — and none is silent (ROADMAP success criterion 4)"
    - "The six harness workarounds this phase genuinely closes are removed from the walk script, and each removal is reconciled with its ledger entry in the same commit, in both directions"
    - "The ledger reconciliation gate passes after every ledger edit: no harness marker lacks a claiming entry, and no entry claims a marker that does not exist"
    - "No retirement note re-creates the phantom claim it means to close — every retired entry keeps all defect-ID tokens off the first line of its workaround field, because the reconciliation parser captures only that first line"
    - "The walk harness's router-body extractor works against the macro-shaped template, and the three router walk steps the contract test requires all still exist"
    - "The walk harness still substitutes its own issuer, a freshly generated secret, and its own proof scope, even though the config template now emits the scope vocabulary key that previously guarded that whole block"
    - "The ADOPT-D18 workaround survives the ADOPT-D03 removal it was nested inside, narrowed to supplying only the `on_mount:` value the router template deliberately omits (D-11) — the harness wraps nothing of its own, so the generated host router carries exactly one `live_session` around the consent route"
    - "Seven workaround markers remain in the walk script and two in the flow driver, as the harness and ledger contract tests require"
    - "No ledger entry quotes a raw access token, ID token, authorization code, session cookie, or the seeded walk password"
    - "A real `mix adopter.walk` run confirms the six removals, and its PASS and FAIL delta is recorded — no marker is removed on reasoning alone"
  artifacts:
    - path: ".planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md"
      provides: "Explicit disposition for all twelve owned defects"
      contains: "ADOPT-D01"
    - path: "scripts/maintainer/adopter_path_walk.sh"
      provides: "Walk harness with the six closed workarounds removed and the router extractor repaired"
      contains: "LOCKSPIRE_WALK_WORKAROUND"
    - path: ".planning/phases/127-installer-against-a-real-host/COVERAGE.md"
      provides: "Reasoned API-coverage declaration for the phase scope"
      contains: "No external API integration"
  key_links:
    - from: "scripts/maintainer/adopter_path_walk.sh"
      to: ".planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md"
      via: "two-way set equality between workaround markers and ledger workaround claims"
      pattern: "LOCKSPIRE_WALK_WORKAROUND"
    - from: "scripts/maintainer/adopter_path_walk.sh"
      to: "priv/templates/lockspire.install/router.ex"
      via: "the body extractor parses the generated helper produced from this template"
      pattern: "extract_lockspire_routes_body"
    - from: "scripts/maintainer/adopter_path_walk.sh"
      to: "priv/templates/lockspire.install/router.ex"
      via: "the ADOPT-D18 workaround supplies only the on_mount: value the template's live_session omits (127-05 / D-11); a second wrapper would nest and fail to compile, and deleting the marker with its enclosing ADOPT-D03 block would strand the ledger entry"
      pattern: "live_session :lockspire_consent"
---

<objective>
Discharge ROADMAP success criterion 4: every installer-attributed defect in the Phase 126 ledger is
either fixed here or explicitly deferred with a stated reason recorded in the ledger. Silence on a
defect fails the phase.

Purpose: the ledger is not documentation, it is a contract. `defect_ledger_contract_test.exs`
enforces two-way set equality between the workaround markers in the harness and the workaround
claims in the ledger, and it fails in both directions. Six markers become obsolete once this
phase's fixes land, but removing them is a change to what the walk actually executes — and the
walk's own router-body extractor is coupled to the exact defective template shape this phase
replaced. None of that is validated by anything in `mix ci`, which is why this plan ends with a
real walk run rather than a claim.

Output: six of thirteen markers removed and seven kept, their entries reconciled, the extractor
repaired, the config workaround's guard re-scoped, the ADOPT-D18 workaround narrowed to the one
value the new router template omits, twelve dispositions recorded, three future candidates logged, a
reasoned coverage declaration written, and one real `mix adopter.walk` run whose delta is recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/127-installer-against-a-real-host/127-CONTEXT.md
@.planning/phases/127-installer-against-a-real-host/127-RESEARCH.md
@.planning/phases/127-installer-against-a-real-host/127-PATTERNS.md
@.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md
@.planning/phases/126-adopter-path-walk-defect-ledger/COVERAGE.md
@AGENTS.md
</context>

## Artifacts this phase produces

See the full phase-level inventory in `127-01-PLAN.md` → "Artifacts this phase produces". This plan
creates `.planning/phases/127-installer-against-a-real-host/COVERAGE.md`. It removes six workaround
markers from the walk harness and edits twelve ledger entries; it creates no code symbol.

<tasks>

<task type="auto">
  <name>Task 1: Retire the six closed workarounds and repair the three harness couplings this phase broke</name>
  <precondition>Plans 127-02 through 127-08 are merged, so the six defects whose workarounds are being removed are actually fixed in the library and templates.</precondition>
  <files>scripts/maintainer/adopter_path_walk.sh, .planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md</files>
  <read_first>
    - `scripts/maintainer/adopter_path_walk.sh` — the six blocks to remove at lines 529, 582, 782, 790, 930, and 1307 and the workaround code each guards; `:268-282` the router-body extractor coupled to the old heredoc delimiter; `:648-670` the config workaround, whose whole substitution block sits behind a guard that tests for the scope-vocabulary key; `:789-815`, where the ADOPT-D18 marker sits **inside** the ADOPT-D03 block being removed and must be re-scoped rather than deleted with it; and the **seven** blocks that stay, at lines 653, 808, 1017, 1038, 1102, 1224, and 1355. The file carries **thirteen** markers today — verify with `grep -c 'LOCKSPIRE_WALK_WORKAROUND' scripts/maintainer/adopter_path_walk.sh` before touching anything, because six of the acceptance criteria below depend on that arithmetic
    - `scripts/maintainer/adopter_path_flow.py` — `:149` and `:246`, the two markers (ADOPT-D10, ADOPT-D14) that live outside the walk script. The reconciliation test harvests markers from **every** file under the maintainer scripts directory, so these two count toward the ledger contract but toward none of this plan's walk-script greps
    - `test/lockspire/maintainer/adopter_walk_contract_test.exs` — `:237-252`, which requires at least one marker to remain in the walk script, and `:254-260`, which requires the three router walk steps to keep existing
    - `test/lockspire/maintainer/defect_ledger_contract_test.exs` — `:57-70` the entry heading and field format the parser requires, `:86-99` how markers are harvested from every non-directory file under the maintainer scripts directory, `:103-112` how workaround claims are extracted, and `:187-211` the two-way set equality assertions
    - `.planning/phases/127-installer-against-a-real-host/127-PATTERNS.md` — the ledger and marker reconciliation section, in particular the parsing trap: the workaround field regex has no multiline modifier, so only the field's first line contributes defect-ID tokens
    - `.planning/phases/127-installer-against-a-real-host/127-RESEARCH.md` — Pitfalls 2, 3, 8, 9, and 11, and the defect-set table's marker-removable column
    - `priv/templates/lockspire.install/router.ex` — the macro-shaped template the extractor must now parse
    - `priv/templates/lockspire.install/config.exs` — the template that now emits the key the config workaround's guard tests for
  </read_first>
  <action>
Remove the six workaround blocks whose defects this phase actually closed: the dependency-unlock
workaround, the HEEx patch workaround, the stand-in operator pipeline, the browser-pipe rewiring of
the interaction and consent routes, the resolver login-path patch, and the client-registration
detour that bypasses the documented Mix task. Remove the marker comment and the code it guards
together — a marker left behind with its workaround deleted, or a workaround left behind with its
marker deleted, are both wrong.

Do **not** remove the **seven** that stay in the walk script. Count them explicitly, because the
arithmetic is where this task goes wrong: the script carries **thirteen** markers today, six leave,
**seven** remain — ADOPT-D04 (config shape, re-scoped below), ADOPT-D05 **twice** (application-tree
wiring, two separate sites), ADOPT-D06 (key lifecycle), ADOPT-D07 (migrations path), ADOPT-D18 (the
consent `on_mount:` value, re-scoped below), and ADOPT-D19 (the protected-route claims access). Two
*further* markers — ADOPT-D10 and ADOPT-D14 — live in `scripts/maintainer/adopter_path_flow.py`,
not in the walk script; the reconciliation test counts them because it harvests every file under
the maintainer scripts directory, but no grep in this plan's acceptance criteria does. Do not
conflate the two sets.

If a post-edit count comes out at six rather than seven, the correct response is **never** to
delete a seventh marker. Every one of the seven is claimed by a live ledger entry, so deleting one
fails the reconciliation gate's ledger-to-marker direction with a set-equality mismatch. A count of
six means a marker was removed that should have stayed — restore it.

Those seven correspond to defects this phase deliberately does not close: the installer never
injects into the host's `mix.exs` or `application.ex`, the key lifecycle is host action, the
migration redesign is deferred, and ADOPT-D18 and ADOPT-D19 are Phase 128's. The harness contract
test also requires at least one marker to remain in the walk script; with these staying, that
holds, but it is a tripwire worth knowing before anyone optimizes marker removal further.

Repair the router-body extractor. It locates the body by matching lines that are exactly four
spaces followed by a bare triple-quote — the heredoc delimiter the template no longer has. Against
the macro-shaped template it matches nothing, so the walk step that pastes the body would paste an
empty string and then fail for a reason that has nothing to do with the defect it tests. Rewrite it
to extract the macro's quoted body from the new shape. Prefer a structural extraction over another
delimiter match if one is available; a delimiter match on the quote block's opening and its matching
end is acceptable, but it must be resilient to the block's internal indentation. Replace the whole
function body, including its delimiter-counting state variable — an acceptance criterion greps for
that variable's absence, because leaving it in place is the signature of a half-repair. The three
router walk steps must all keep existing — the harness contract test asserts each by name — so
repair the extractor, do not delete the step it feeds.

Prove the repaired extractor rather than reasoning about it. The function is self-contained and
takes a file path, so it can be exercised without running the walk: lift the function's source out
of the script with `awk` and run it against the committed runtime fixture that 127-05 regenerates,
`test/support/generated_host_app_web/router/lockspire.ex`. The exact command is in the acceptance
criteria. This is a genuine fence, not a formality — run against the macro-shaped fixture the
current extractor emits nothing at all, so the criterion fails on an unrepaired extractor and
passes only on a working one.

Re-scope the ADOPT-D18 workaround; do **not** let it leave with ADOPT-D03. The D18 marker sits
*inside* the ADOPT-D03 block being removed, and its workaround wraps the consent route in a
`live_session` of the harness's own making inside the generated host router. Plan 127-05 (D-11) now
emits that block from the template with the `on_mount:` value deliberately absent — the *block* is
template-shaped and belongs to this phase, the `phx.gen.auth`-specific *value* is guide-shaped and
belongs to Phase 128. After the ADOPT-D03 removal there are two wrong outcomes and one right one.

Wrong: delete the D18 marker along with its enclosing block. That strands the ADOPT-D18 ledger
entry claiming a marker no harness file backs, and the reconciliation gate fails in the
ledger-to-marker direction.

Wrong: keep injecting a wrapper of the harness's own. The template already wrapped that route, so a
second wrapper nests one inside the other — a hard compile error in the generated host router, and
`step-03b-router-wire` fails for a reason unrelated to any defect it tests.

Right: narrow the workaround so it supplies **only** the value the template omits. Patch the
generated `lib/host_app_web/router/lockspire.ex` in place — adding the `on_mount:` option to the
block the template emitted — rather than inserting anything into the host router. Keep the
ADOPT-D18 marker on the narrowed workaround and rewrite its comment to describe what it now does
and why the value is still the harness's to supply. Editing the generated file is the ownership
model this whole phase is built on, and it is exactly what an adopter following Phase 128's guide
will do. This is the same shape as the config-guard re-scoping below: in both cases a template
gained the structure the harness used to supply, and in both cases the harness must narrow to the
residual value rather than vanish.

Re-scope the config workaround. Its issuer substitution, its freshly generated secret, and its own
proof-scope addition all sit behind a single guard that tests whether the scope-vocabulary key is
absent. Now that the template emits that key, the guard becomes false and the whole block is
skipped — including the issuer substitution the walk still genuinely needs, because the template's
issuer is a placeholder host name that no running walk can use. Split the substitutions so each one
is guarded by whether that specific value still needs replacing, rather than by the presence of one
unrelated key. The secret substitution must also account for the template now emitting a
`secret_key_base` placeholder line: replace the placeholder rather than appending a second key.
Keep the marker for this entry — the walk-specific value substitution is not a defect; no adopter
requests the scope this harness invented for its own proof.

Reconcile the ledger in the same commit, and mind the parser. The workaround field regex has no
multiline modifier, so only the **first line** of a workaround field contributes defect-ID tokens
to the reconciliation set. A retirement note that names the defect's own ID on that first line
re-creates the exact phantom claim it is trying to close, and the test then fails saying a ledger
entry claims a marker no harness file backs. Write each retired entry's workaround field so its
first line contains **no** defect-ID token at all — state that no workaround remains and that the
disposition note below explains the retirement, then put any cross-references on continuation lines.
Equally, do not add a defect-ID token to any other entry's first workaround line; one entry
currently references two sibling IDs safely only because those tokens sit on a continuation line,
and reflowing that paragraph would break the suite.

Update ADOPT-D18's workaround description in the same commit as its re-scoping, because the current
text describes a workaround that will no longer exist: it says the consent route is wrapped in its
own `live_session` in the generated host router, and after the re-scoping the harness only supplies
the `on_mount:` value into the block the template emits. Keep `ADOPT-D18` on that field's **first**
line — the marker still exists, so this entry's claim is correct and must survive the two-way
check. Only the description changes. Its owning phase stays 128.

Run the ledger reconciliation test after **each** ledger edit, not once at the end. Treat these
edits as code.
  </action>
  <verify>
    <automated>mix test test/lockspire/maintainer/</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lockspire/maintainer/` exits 0
    - `grep -c 'LOCKSPIRE_WALK_WORKAROUND' scripts/maintainer/adopter_path_walk.sh` returns **7** (thirteen today, minus the six removed)
    - `grep -Ec 'LOCKSPIRE_WALK_WORKAROUND: ADOPT-D(02|03|08|09|15|16)' scripts/maintainer/adopter_path_walk.sh` returns 0
    - `grep -Ec 'LOCKSPIRE_WALK_WORKAROUND: ADOPT-D(04|05|06|07|18|19)' scripts/maintainer/adopter_path_walk.sh` returns **7** (D04, D05 twice, D06, D07, D18, D19 — this already returns 7 today and is a preservation fence, so a drop to 6 means a marker that should have stayed was removed)
    - `grep -c 'LOCKSPIRE_WALK_WORKAROUND' scripts/maintainer/adopter_path_flow.py` returns 2 (ADOPT-D10 and ADOPT-D14 are untouched by this plan)
    - `grep -c 'step-03b-router-call' scripts/maintainer/adopter_path_walk.sh` returns 1 or more, and the same for `step-03b-router-paste` and `step-03b-router-wire`
    - The router-body extractor no longer scans for the old heredoc delimiter: `awk '/^extract_lockspire_routes_body\(\) \{/,/^\}$/' scripts/maintainer/adopter_path_walk.sh | grep -c '"""'` returns 0 (returns 1 today) and `grep -c 'marker_count' scripts/maintainer/adopter_path_walk.sh` returns 0 (returns 2 today)
    - The repaired extractor actually extracts from the macro-shaped helper — lift the function out of the script and run it against the fixture 127-05 regenerates: `bash -c "$(awk '/^extract_lockspire_routes_body\(\) \{/,/^\}$/' scripts/maintainer/adopter_path_walk.sh); extract_lockspire_routes_body test/support/generated_host_app_web/router/lockspire.ex" | grep -c 'Lockspire.Web.AdminRouter'` returns 1 or more. The current extractor returns 0 against a macro-shaped helper, so this fails on an unrepaired extractor
    - ADOPT-D18 survives the ADOPT-D03 removal: `grep -c 'LOCKSPIRE_WALK_WORKAROUND: ADOPT-D18' scripts/maintainer/adopter_path_walk.sh` returns 1
    - ADOPT-D18 still supplies the value the template omits: `grep -c 'mount_current_scope' scripts/maintainer/adopter_path_walk.sh` returns 1 or more
    - ADOPT-D18 is no longer riding inside the removed ADOPT-D02/D03 wired-body insertion: `grep -c 'forward "/", Lockspire.Web.AdminRouter' scripts/maintainer/adopter_path_walk.sh` returns 0 (returns 1 today)
    - `bash -n scripts/maintainer/adopter_path_walk.sh` exits 0
    - For each of the six retired entries, the **first** line of its workaround field carries no defect-ID token. Extract that line per ID rather than with a range expression — an `awk` range whose end pattern also matches its start line collapses to the heading and silently measures nothing: `for id in D02 D03 D08 D09 D15 D16; do awk -v pat="^### ADOPT-$id\$" '$0 ~ pat {f=1; next} f && /^- \*\*Workaround:\*\*/ {print; exit}' .planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md; done | grep -Ec 'ADOPT-D[0-9]+'` returns 0. It returns 6 today, so it is live
    - Non-vacuity guard on the line above — the same loop with `| grep -c '^- \*\*Workaround:\*\*'` returns 6, proving all six workaround lines were actually reached rather than the extraction matching nothing
    - `mix test test/lockspire/maintainer/defect_ledger_contract_test.exs` exits 0
    - `mix test test/lockspire/maintainer/adopter_walk_contract_test.exs` exits 0
  </acceptance_criteria>
  <done>Six workarounds are gone from the harness and seven remain, with their ledger entries reconciled in the same commit; the router extractor parses the new template shape; the config workaround still performs the substitutions the walk needs; the ADOPT-D18 workaround is narrowed to the `on_mount:` value rather than deleted or duplicated; and the reconciliation gate passes in both directions.</done>
</task>

<task type="auto">
  <name>Task 2: Record a disposition for all twelve owned defects and log the deferrals</name>
  <files>.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md, .planning/phases/127-installer-against-a-real-host/COVERAGE.md</files>
  <read_first>
    - `.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md` — all twelve owned entries and the six-field format each carries
    - `test/lockspire/maintainer/defect_ledger_contract_test.exs` — `:24-32` the allowed source tokens and the owning-phase vocabulary, `:126-141` the all-six-fields-non-blank invariant, and `:217-223` the two content prohibitions on seeded passwords and bearer-shaped strings
    - `.planning/REQUIREMENTS.md` — the Out of Scope table, which forbids widening the supported-surface contract for this milestone and is the stated reason behind two of the deferrals
    - `.planning/phases/127-installer-against-a-real-host/127-CONTEXT.md` — the two deferral decisions and the deferred-ideas list
    - `.planning/phases/127-installer-against-a-real-host/127-RESEARCH.md` — the defect-set table with each defect's real root cause and marker disposition, and Pitfall 8 on the residual config workaround
    - `.planning/phases/126-adopter-path-walk-defect-ledger/COVERAGE.md` — the house style for a phase coverage declaration
    - `.planning/phases/127-installer-against-a-real-host/127-PATTERNS.md` — the ledger parsing constraints, again, because every edit here is subject to them
  </read_first>
  <action>
Record an explicit disposition for each of the twelve defects this phase owns. Nothing may be
silent — an entry that is neither fixed nor deferred with a stated reason fails the phase's fourth
success criterion. For each, state which it is and where the evidence lives.

Fixed in this phase: the router injection defect, the stand-in pipeline defect, the unpiped forward
defect (its installer half; the guide half remains Phase 128's), the config shape defect (its
template half), the client-registration repo access defect, the resolver login path defect, the
dependency range defect, and the HEEx compile defect. For each, name the file changed and the test
that now fences it, so a reader can verify the claim without re-deriving it.

Fixed in part, with the remainder deferred and the reason stated:
- The application-tree defect. The installer half is fixed — the printed next steps now name the
  wiring — and the guide half is Phase 128's. The harness marker stays because the harness must
  still perform host wiring the installer deliberately does not inject; zero injection is the fence.
- The key-lifecycle defect. Same shape: the printed next steps now name all three stages, the guide
  half is Phase 128's, and the harness must still make the calls.
- The migration-path defect. The wrong remediation strings are fixed at all three in-scope sites.
  The redesign — a dedicated migrate task, or a host migration generator writing a migration against
  versioned modules — is deferred, and the reason is that both require new public surface, which
  this milestone's Out of Scope table forbids. Record it as a future candidate visibly enough that
  Phase 128 does not silently inherit an unsolvable documentation problem. Adopters still need a
  switch; that is the accepted cost, and it should be written as such.
- The login-resume defect. Deferred to Phase 128 with the reason stated: the generated login
  function that ignores a posted return parameter is host scaffolding the installer cannot and must
  not patch.
- The residual config workaround. Record that the template half is fixed and that what remains in
  the harness is walk-specific value substitution — a real issuer, a freshly generated secret, and
  a scope this harness invented for its own proof — which is not an adopter-facing defect at all.

Add a future-candidates note covering the three deferred designs: installer injection into the
host's router, config, or `mix.exs`; the migration redesign in both its forms; and session-backed
interaction resume. Also record the manifest version-field change made in 127-01 — it is not a
numbered defect, but it is a behavior change to a generated artifact found by exactly the method
this phase exists to apply, and it belongs in the record.

Every edit is subject to the parser constraints from Task 1: keep defect-ID tokens off the first
line of any workaround field unless a matching marker genuinely exists, keep all six fields
non-blank, keep source tokens within the allowed vocabulary, and keep the owning-phase field naming
one of the recognized phases. Quote no raw access token, ID token, authorization code, session
cookie, or seeded password — only error classes, step IDs, and file-and-line citations, exactly as
the ledger's own preamble promises.

Write `.planning/phases/127-installer-against-a-real-host/COVERAGE.md`. The API-coverage detector
returned no detection over the current phase scope, but it re-runs at seal time over a scope that
includes these plan bodies. Pre-empt a seal-time block with a reasoned one-line declaration in the
form used by the sibling Phase 126 file: `No external API integration: <reason>`. The reason is
that Phase 127 integrates no third-party API or SDK — the only external service touched anywhere in
the phase is the Hex registry, for a version-range resolution, which is a build-time dependency
operation rather than a runtime integration. Do not fabricate matrix rows for capabilities that do
not exist; Phase 126's file authored a matrix because that phase genuinely exercised Lockspire's own
protocol surface end to end, and this phase does not.
  </action>
  <verify>
    <automated>mix test test/lockspire/maintainer/defect_ledger_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lockspire/maintainer/defect_ledger_contract_test.exs` exits 0
    - Each of `ADOPT-D01`, `ADOPT-D02`, `ADOPT-D03`, `ADOPT-D04`, `ADOPT-D05`, `ADOPT-D06`, `ADOPT-D07`, `ADOPT-D08`, `ADOPT-D09`, `ADOPT-D14`, `ADOPT-D15`, `ADOPT-D16` appears as a heading in the ledger and carries a Phase 127 disposition sentence
    - The ledger contains a future-candidates section naming installer injection, the migration redesign, and session-backed interaction resume
    - `grep -Ec 'eyJ[A-Za-z0-9_-]{10,}' .planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md` returns 0 (no bearer-shaped string)
    - `.planning/phases/127-installer-against-a-real-host/COVERAGE.md` exists and contains `No external API integration`
    - `mix test test/lockspire/maintainer/` exits 0
    - `mix docs.verify` exits 0
  </acceptance_criteria>
  <done>All twelve owned defects carry an explicit, reader-verifiable disposition, three deferrals state their reasons, and the phase's coverage declaration is written in the house style.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the adopter walk once and record the delta</name>
  <files>.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md</files>
  <read_first>
    - `scripts/maintainer/adopter_path_walk.sh` — the edited harness, in particular the six steps whose workarounds were removed and the config-import step whose guard was re-scoped
    - `.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md` — the front matter's recorded Phase 126 baseline of 19 PASS and 12 FAIL, and the dispositions written in Task 2
    - `.planning/phases/127-installer-against-a-real-host/127-RESEARCH.md` — Pitfall 11 and Open Question 1, which frame this as a real fork rather than a formality
  </read_first>
  <action>
Run `mix adopter.walk` once locally, read the harness's own report, and record the PASS and FAIL
delta into the ledger's disposition notes. This is a human-verified step because removing a
workaround changes what the walk executes, and only a real run against a real generated host, a
real database, and a real booted server can show that the previously-worked-around step now stands
on its own. Do not re-add a removed marker if a step regresses — report the regression instead, so
the ledger claim gets corrected rather than the workaround restored.
  </action>
  <verify>
    <human-check>`mix adopter.walk` completes and its report shows the six previously-worked-around steps passing unaided, with the PASS and FAIL delta against the 19/12 baseline recorded in the ledger</human-check>
  </verify>
  <acceptance_criteria>
    - `mix adopter.walk` runs to completion and produces its report
    - The dependency-add, install, router-paste, router-wire, resolver, and client-registration steps each report PASS with no workaround applied
    - The config-import step reports PASS after the guard re-scoping
    - `step-03b-router-wire` compiles — a nested `live_session` around the consent route would be a hard compile error here, so a PASS is the evidence that the ADOPT-D18 re-scoping supplies the `on_mount:` value into the template's block rather than adding a wrapper of its own
    - The consent page in `step-06b-flow` renders for the logged-in walk user rather than the "sign in is required" rejection — the evidence that the narrowed ADOPT-D18 workaround still lands its value
    - The PASS and FAIL counts are compared against the recorded Phase 126 baseline of 19 PASS and 12 FAIL, and the delta is written into the ledger
    - `git status --porcelain` shows only the ledger edit recording the delta (the walk's own tree is gitignored)
    - `mix test test/lockspire/maintainer/` still exits 0 after the delta is recorded
  </acceptance_criteria>
  <done>One real walk run has confirmed each of the six retired workarounds, and the PASS and FAIL delta is recorded in the ledger.</done>
  <what-built>
Plans 127-02 through 127-08 fixed the installer, its templates, the dependency range, and the
re-run semantics. Task 1 of this plan removed six of the harness's thirteen workarounds and kept
seven, repaired the walk's router-body extractor for the macro-shaped template, and re-scoped two
workarounds the new templates partially absorbed: the config guard, and ADOPT-D18, which was nested
inside the removed ADOPT-D03 block and now supplies only the `on_mount:` value the router template
deliberately omits. Task 2 recorded a disposition for all twelve owned defects.

Nothing in `mix ci` re-runs the walk — that is deliberate, because a per-run `mix phx.new` stays
out of every automated lane and automating the walk is Phase 130's concern. So the six marker
removals are, until this checkpoint, changes to what the walk executes that no execution has
validated. This is the only step that turns them from reasoning into evidence.
  </what-built>
  <how-to-verify>
Run the walk once, locally, against a real generated host:

1. Confirm the prerequisites the walk needs: `pg_isready` reports the local PostgreSQL is accepting
   connections, and the pinned `phx_new 1.8.9` archive is installed.
2. Run `mix adopter.walk` and let it complete. Expect it to take a few minutes on a warm machine —
   it generates a host, fetches dependencies, compiles, migrates, boots a server, and drives a real
   authorization-code plus PKCE flow.
3. Read the harness's own report. For each of the six steps whose workaround was removed —
   the dependency add, the install, the router paste, the router wire, the resolver, and the client
   registration — confirm the step now PASSes on its own, with no workaround applied.
4. Confirm the two **re-scoped** workarounds, which are the changes most likely to have gone wrong
   because in both cases a template gained structure the harness used to supply:
   - the config-import step still PASSes — the template now emits the scope-vocabulary key the old
     guard tested for, so a mis-split leaves the placeholder issuer in place and the host cannot boot;
   - `step-03b-router-wire` compiles and the consent page in `step-06b-flow` renders for the
     logged-in walk user. A nested `live_session` around the consent route fails compilation at
     router-wire; a dropped `on_mount:` value renders the "sign in is required" rejection at the
     consent page even though the session cookie is valid. Either symptom means the ADOPT-D18
     re-scoping is wrong — and neither is fixed by restoring the old wrapper, which the template
     now duplicates.
5. Compare the PASS and FAIL counts against the Phase 126 baseline recorded in the ledger front
   matter (19 PASS, 12 FAIL) and note the delta.
6. Record the resulting delta in the ledger's disposition notes: which previously-worked-around steps
   now pass unaided, and any step that regressed.
7. If any of the six regresses, do NOT re-add the marker silently. Report which step failed and what
   it failed on — a regression here means one of plans 127-02 through 127-08 did not actually close
   the defect its ledger entry now claims is fixed, and the claim must be corrected rather than the
   workaround restored.

The walk writes into `tmp/adopter-walk/`, which is gitignored, so a completed run should leave
`git status --porcelain` clean apart from the ledger edit recording the delta.
  </how-to-verify>
  <resume-signal>Type "approved" with the PASS and FAIL counts, or describe which steps regressed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| walk harness → generated host and local database | the harness generates an app, runs migrations, boots a server, and drives a real authorization flow with real credentials |
| walk output → committed ledger | observations from a run carrying real tokens, codes, cookies, and a seeded password are transcribed into a committed file |
| harness markers ↔ ledger claims | a machine-enforced two-way contract; either side moving alone breaks the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-127-33 | Information Disclosure | ledger entries transcribed from a real walk run | high | mitigate | only error classes, step IDs, and file-and-line citations are recorded — never a raw access token, ID token, authorization code, session cookie, or the seeded walk password. The existing contract test gates seeded passwords and bearer-shaped strings, and an acceptance criterion adds a bearer-shape grep over the whole file |
| T-127-34 | Repudiation | a marker removed on reasoning alone | high | mitigate | the blocking checkpoint requires one real walk run and records the PASS and FAIL delta per retired workaround. A regression corrects the ledger claim rather than silently restoring the workaround |
| T-127-35 | Tampering | the config workaround's re-scoped guard | medium | mitigate | the substitutions are split so each is guarded by whether its own value still needs replacing; the checkpoint explicitly re-verifies the config-import step, because this is the change most likely to fail silently |
| T-127-36 | Repudiation | a retirement note that re-creates the claim it closes | medium | mitigate | the reconciliation parser reads only the first line of a workaround field, so every retired entry keeps defect-ID tokens off that line; an acceptance criterion extracts each of the six retired entries' first workaround line per ID (never with an `awk` range whose end pattern matches its own start line, which collapses to the heading and measures nothing) and asserts zero defect-ID tokens across them, with a companion non-vacuity guard asserting all six lines were reached |
| T-127-38 | Tampering | the ADOPT-D18 workaround, nested inside the removed ADOPT-D03 block | high | mitigate | 127-05 emits the consent `live_session` from the template with `on_mount:` omitted (D-11), so the D18 workaround must narrow to that value rather than leave with its enclosing block. Deleting it strands the ledger entry; keeping the harness's own wrapper nests a second `live_session` and breaks the generated host router's compile. Acceptance criteria assert the marker survives, the value is still supplied, and the removed wired-body insertion is gone; the blocking checkpoint re-verifies both `step-03b-router-wire` and the consent page render |
| T-127-37 | Information Disclosure | walk artifacts under the temporary directory | medium | accept | the walk's working tree is gitignored and contains a generated secret and a seeded password by design; it is not committed, and bringing it under an artifact allowlist is a Phase 130 candidate carried over from Phase 126 |
| T-127-SC | Tampering | package-manager installs | high | accept | this plan installs no packages; the walk itself resolves a generated host's own dependencies inside a gitignored temporary tree, and RESEARCH's Package Legitimacy Audit records zero new packages for the library phase-wide |
</threat_model>

<verification>
- `mix test test/lockspire/maintainer/` — both maintainer contract tests, run after every ledger edit
- `bash -n scripts/maintainer/adopter_path_walk.sh` — the edited harness parses
- `mix ci` — the full lane green with every plan's changes in place
- `mix adopter.walk` — one real run, delta recorded (blocking checkpoint)
</verification>

<success_criteria>
- All twelve owned defects carry an explicit disposition; none is silent
- Six of the walk script's thirteen workarounds are removed and seven remain, all reconciled with their ledger entries in the same commit, passing the gate in both directions
- No retirement note re-creates a phantom workaround claim, and the fence proving it is non-vacuous
- The router extractor, the config workaround, and the ADOPT-D18 workaround all work against the new template shapes — the first repaired, the latter two narrowed
- Three deferrals state their reasons and are logged as future candidates
- One real walk run confirms the removals and both re-scopings, and its PASS and FAIL delta is recorded
</success_criteria>

<output>
Create `.planning/phases/127-installer-against-a-real-host/127-09-SUMMARY.md` when done
</output>
