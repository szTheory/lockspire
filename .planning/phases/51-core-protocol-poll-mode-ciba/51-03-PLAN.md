---
phase: 51-core-protocol-poll-mode-ciba
plan: 03
type: execute
wave: 3
depends_on: ["51-02"]
files_modified:
  - lib/lockspire/protocol/token_exchange.ex
  - lib/lockspire/storage/ecto/repository.ex
  - test/integration/phase51_ciba_poll_mode_e2e_test.exs
autonomous: true
requirements: [CIBA-02, CIBA-03]
must_haves:
  truths:
    - "Token endpoint accepts grant_type=urn:openid:params:grant-type:ciba"
    - "Clients receive authorization_pending while consent is unresolved"
    - "Slow_down error is returned and polling interval increased if client polls too fast"
    - "Successful token issuance once consent is granted"
  artifacts:
    - path: "lib/lockspire/protocol/token_exchange.ex"
      provides: "CIBA grant exchange logic"
    - path: "lib/lockspire/storage/ecto/repository.ex"
      provides: "CIBA poll interval enforcement"
  key_links:
    - from: "lib/lockspire/protocol/token_exchange.ex"
      to: "lib/lockspire/storage/ecto/repository.ex"
      via: "record_ciba_poll/3 call"
---

<objective>
Implement the CIBA token grant type and the state machine logic for polling, including pending states and "slow down" enforcement.

Purpose: Allow Consumption Devices to retrieve tokens after user authorization, while preventing endpoint abuse via strict polling interval management.
Output: Integrated token exchange logic and durable poll tracking.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PATTERNS.md
@.planning/research/RESEARCH.md
@.planning/phases/51-core-protocol-poll-mode-ciba/51-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate CIBA Grant in Token Exchange</name>
  <files>lib/lockspire/protocol/token_exchange.ex</files>
  <action>
    - Add `urn:openid:params:grant-type:ciba` to the `exchange/1` dispatch in `Lockspire.Protocol.TokenExchange`.
    - Implement `exchange_ciba/1` following the pattern in PATTERNS.md.
    - Implement `redeem_ciba_authorization/4` to handle the actual token issuance when the state is `approved`.
    - Ensure DPoP context is resolved if applicable (using `TokenEndpointDPoP.resolve_context/2`).
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/token_exchange_test.exs</automated>
  </verify>
  <done>Token endpoint accepts and processes CIBA grant requests.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CIBA Polling and Interval Logic</name>
  <files>lib/lockspire/storage/ecto/repository.ex</files>
  <action>
    - Implement `record_ciba_poll/3` in `Repository`.
    - Follow the `evaluate_device_poll` logic to handle CIBA states:
      - `pending`: Check `next_poll_allowed_at`. If `now < next_poll_allowed_at`, return `slow_down` and increment `effective_poll_interval_seconds` by 5. Otherwise, update `next_poll_allowed_at` to `now + interval` and return `authorization_pending`.
      - `approved`: Return success for token issuance.
      - `denied`, `expired`, `consumed`: Return relevant OAuth errors.
    - Ensure `auth_req_id_hash` is used for lookups.
  </action>
  <verify>
    <automated>mix test test/lockspire/storage/ecto/repository_test.exs</automated>
  </verify>
  <done>Repository correctly enforces CIBA polling constraints and returns appropriate states.</done>
</task>

<task type="auto">
  <name>Task 3: Final E2E Verification of CIBA Poll Mode</name>
  <files>test/integration/phase51_ciba_poll_mode_e2e_test.exs</files>
  <action>
    - Complete the integration test to simulate:
      1. Initiation via `/bc-authorize`.
      2. Polling before interval expires (`slow_down`).
      3. Polling while pending (`authorization_pending`).
      4. (Simulated) Manual database update to `approved` status.
      5. Polling after approval (successful token delivery).
      6. Polling after consumption (`invalid_grant`).
  </action>
  <verify>
    <automated>mix test test/integration/phase51_ciba_poll_mode_e2e_test.exs</automated>
  </verify>
  <done>CIBA Poll Mode is fully functional and verified end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Token Endpoint | Untrusted polling request. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-05 | Denial of Service | `/token` (CIBA) | mitigate | Enforce `slow_down` with interval incrementing to deter aggressive polling. |
| T-51-06 | Information Disclosure | `auth_req_id` | mitigate | Use constant-time comparison (if possible) or secure hashing for `auth_req_id` lookups. |
</threat_model>

<verification>
Execute the full integration test suite.
</verification>

<success_criteria>
- Polling respects the `interval` and `slow_down` logic.
- Tokens are only issued for `approved` authorizations.
- Authorizations are `consumed` after successful exchange.
</success_criteria>

<output>
After completion, create `.planning/phases/51-core-protocol-poll-mode-ciba/51-03-SUMMARY.md`
</output>
