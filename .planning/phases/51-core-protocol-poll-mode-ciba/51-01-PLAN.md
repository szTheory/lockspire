---
phase: 51-core-protocol-poll-mode-ciba
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/domain/ciba_authorization.ex
  - lib/lockspire/storage/ecto/ciba_authorization_record.ex
  - lib/lockspire/storage/ciba_authorization_store.ex
  - lib/lockspire/storage/ecto/repository.ex
  - priv/repo/migrations/20260505191900_create_lockspire_ciba_authorizations.exs
  - test/integration/phase51_ciba_poll_mode_e2e_test.exs
autonomous: true
requirements: [CIBA-01, CIBA-02, CIBA-03]
must_haves:
  truths:
    - "Database schema exists to track CIBA authorizations and poll intervals"
    - "Domain model supports CIBA issuance and state management"
    - "Store behaviour and Repo implementation are ready for CIBA operations"
  artifacts:
    - path: "lib/lockspire/domain/ciba_authorization.ex"
      provides: "CIBA domain model"
    - path: "lib/lockspire/storage/ecto/ciba_authorization_record.ex"
      provides: "Ecto schema for CIBA"
    - path: "lib/lockspire/storage/ciba_authorization_store.ex"
      provides: "Store behaviour"
  key_links:
    - from: "lib/lockspire/storage/ecto/repository.ex"
      to: "lib/lockspire/storage/ecto/ciba_authorization_record.ex"
      via: "Ecto queries"
---

<objective>
Establish the durable storage foundation and domain models for CIBA authorizations. This includes the database schema, Ecto record mapping, domain entity, and the storage contract implementation in the Repository.

Purpose: CIBA requires persistent state to track asynchronous user consent and enforce polling intervals across system restarts.
Output: Migration, domain/storage files, and an integration test scaffold.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/PATTERNS.md
@.planning/research/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define CIBA Database Schema and Domain Model</name>
  <files>
    priv/repo/migrations/20260505191900_create_lockspire_ciba_authorizations.exs,
    lib/lockspire/domain/ciba_authorization.ex,
    lib/lockspire/storage/ecto/ciba_authorization_record.ex
  </files>
  <action>
    - Create a migration for `lockspire_ciba_authorizations` table. Fields: `auth_req_id_hash` (unique index), `client_id`, `scopes` (array), `status` (enum: pending, approved, denied, consumed, expired), `subject_id`, `approved_at`, `denied_at`, `consumed_at`, `expired_at`, `effective_poll_interval_seconds`, `next_poll_allowed_at`, `expires_at`, `binding_message`.
    - Implement `Lockspire.Domain.CibaAuthorization` mirroring the snippet in PATTERNS.md. Use `Policy.hash_token/1` for `auth_req_id_hash`.
    - Implement `Lockspire.Storage.Ecto.CibaAuthorizationRecord` with Ecto schema and changesets, following the `DeviceAuthorizationRecord` pattern.
  </action>
  <verify>
    <automated>mix ecto.migrate && mix test test/lockspire/domain/ciba_authorization_test.exs</automated>
  </verify>
  <done>Migration applied and domain model unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Implement CIBA Storage Store and Repository Integration</name>
  <files>
    lib/lockspire/storage/ciba_authorization_store.ex,
    lib/lockspire/storage/ecto/repository.ex
  </files>
  <action>
    - Define `Lockspire.Storage.CibaAuthorizationStore` behaviour with callbacks: `put_ciba_authorization/1`, `fetch_ciba_authorization_by_auth_req_id_hash/1`, `record_ciba_poll/3`, `transition_ciba_authorization/3`.
    - Implement the behaviour in `Lockspire.Storage.Ecto.Repository`. Follow the implementation patterns of `DeviceAuthorizationStore` in the same file.
  </action>
  <verify>
    <automated>mix test test/lockspire/storage/ecto/repository_test.exs</automated>
  </verify>
  <done>Repository implements CIBA storage contract correctly.</done>
</task>

<task type="auto">
  <name>Task 3: Create Integration Test Scaffold</name>
  <files>test/integration/phase51_ciba_poll_mode_e2e_test.exs</files>
  <action>
    Create a new integration test file that will eventually cover the full CIBA Poll Mode flow. For now, it should test that a CIBA authorization can be persisted and retrieved via the storage layer.
  </action>
  <verify>
    <automated>mix test test/integration/phase51_ciba_poll_mode_e2e_test.exs</automated>
  </verify>
  <done>Integration test scaffold exists and passes initial persistence checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB → Application | Authorization secrets (auth_req_id) must not be stored in plain text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Information Disclosure | `lockspire_ciba_authorizations` | mitigate | Use `Policy.hash_token/1` to store only the hash of `auth_req_id`. |
| T-51-02 | Denial of Service | `/token` (polling) | mitigate | Durably track `next_poll_allowed_at` to enforce `slow_down` penalties. |
</threat_model>

<verification>
Run `mix test` to ensure storage and domain logic are sound.
</verification>

<success_criteria>
- Migration is successfully applied to the database.
- CIBA authorization records can be created, retrieved, and updated.
- Polling state (intervals) is correctly tracked in the Ecto record.
</success_criteria>

<output>
After completion, create `.planning/phases/51-core-protocol-poll-mode-ciba/51-01-SUMMARY.md`
</output>
