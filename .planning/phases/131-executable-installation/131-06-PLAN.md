---
phase: 131-executable-installation
plan: 06
type: execute
wave: 4
depends_on:
  - 131-01
  - 131-03
  - 131-04
  - 131-05
files_modified:
  - lib/lockspire/install/verify.ex
  - test/lockspire/install/verify_test.exs
  - test/mix/tasks/lockspire_verify_test.exs
  - docs/install-and-onboard.md
autonomous: true
requirements:
  - INST-04
must_haves:
  truths:
    - "mix lockspire.verify reports repo, resolver, issuer, mount path, logout path, Oban, seam-module, router/order, and host-path migration defects independently with exact remediation (D-13)."
    - "Verification inspects the compiled generated route table and host migration path; one missing seam does not hide other detectable failures (D-04, D-13)."
    - "Onboarding commands describe plain host mix ecto.migrate, actual generated consent/routes/tests, and a separate FAPI opt-in command only after those behaviors are executable."
  artifacts:
    - path: "lib/lockspire/install/verify.ex"
      provides: "Aggregate fail-closed install verification service"
    - path: "test/lockspire/install/verify_test.exs"
      provides: "Independent config/router/migration diagnostic coverage"
    - path: "test/mix/tasks/lockspire_verify_test.exs"
      provides: "Exact actionable CLI output and exit behavior"
    - path: "docs/install-and-onboard.md"
      provides: "Truthful packaged-host installation sequence"
  key_links:
    - from: "lib/lockspire/install/verify.ex"
      to: "Phoenix.Router.routes/1"
      via: "compiled host route and order verification"
      pattern: "Phoenix.Router.routes"
    - from: "lib/lockspire/install/verify.ex"
      to: "priv/repo/migrations"
      via: "host project migration discovery and packaged inventory comparison"
      pattern: "priv/repo/migrations"
    - from: "docs/install-and-onboard.md"
      to: "lib/mix/tasks/lockspire.verify.ex"
      via: "documented verification command and remediation loop"
      pattern: "mix lockspire.verify"
---

<objective>
Close the executable-installation loop with exhaustive diagnostics and documentation that matches the generated package behavior.

Purpose: Let an adopter diagnose every missing host seam in one run and follow only commands proven by the generated-host suite.
Output: Independent verification checks, CLI proof, corrected onboarding, and the phase-wide quality gate.

Decision trace: Completes D-04, D-09, D-13, D-15, and D-16 at the supported diagnostics/documentation boundary.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-executable-installation/131-CONTEXT.md
@.planning/phases/131-executable-installation/131-RESEARCH.md
@.planning/phases/131-executable-installation/131-PATTERNS.md
@.planning/phases/131-executable-installation/131-VALIDATION.md
@.planning/phases/131-executable-installation/131-01-SUMMARY.md
@.planning/phases/131-executable-installation/131-03-SUMMARY.md
@.planning/phases/131-executable-installation/131-04-SUMMARY.md
@.planning/phases/131-executable-installation/131-05-SUMMARY.md
@lib/lockspire/install/verify.ex
@docs/install-and-onboard.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Report every required host seam and ordered route independently</name>
  <files>lib/lockspire/install/verify.ex, test/lockspire/install/verify_test.exs, test/mix/tasks/lockspire_verify_test.exs</files>
  <behavior>
    - Missing repo, account_resolver, issuer, mount_path, logout_path, or invalid Oban runtime each produce a separate failed check naming config/lockspire.exs and the exact key.
    - Missing resolver/interaction-handler modules are reported even when another config item is absent.
    - Router verification requires host verification/authorized-app/consent routes, guarded admin before public forwarding, and the actual configured mount path.
    - Migration verification checks the host priv/repo/migrations delivery path and applied database state, with plain mix ecto.migrate remediation.
    - The Mix task presents all checks and exits unsuccessfully when any check fails.
  </behavior>
  <action>Refactor Verify.run/1 so required config values are captured independently without a bang accessor aborting the aggregate. Emit one stable Check per repo, account_resolver, issuer, mount_path, logout_path, and Oban contract, each with exact config/lockspire.exs remediation per D-13; then continue all safely inspectable seam, router, and migration checks. Extend router_check to require generated consent plus host verification/authorized-app routes and assert guarded admin precedes public forwarding through Phoenix.Router.routes/1 per D-04. Inspect migrations from the host project_root/priv/repo/migrations delivered by Plans 02/04, compare the packaged inventory/identity, and run Ecto status against the host path so plain mix ecto.migrate is truthful per D-09. Keep errors redaction-safe and fail closed. Update unit and Mix-task tests for multiple simultaneous failures, exact fixes, missing operator guard/order/consent/public routes, migration delivery gaps, and all-green aggregate.</action>
  <verify>
    <automated>mix test test/lockspire/install/verify_test.exs test/mix/tasks/lockspire_verify_test.exs</automated>
  </verify>
  <done>One verification run exposes every independent defect with an executable fix, accepts only the compiled ordered generated route/migration contract, and fails whenever any required seam is unsafe or absent.</done>
</task>

<task type="auto">
  <name>Task 2: Publish only the now-executable onboarding path</name>
  <files>docs/install-and-onboard.md</files>
  <action>Update onboarding after the code/tests are green: document packaged install, host-owned router import/operator pipeline and macro call, generated consent/account/login/logout seams, plain mix ecto.migrate from host priv/repo/migrations, mix lockspire.verify remediation, and the default generated test command. Explain that --with-fapi-smoke and its command/profile are explicit opt-in evidence per D-15. Remove the adoption-demo dependency-path migration workaround and any implication that source strings or alternate hand-written routes are supported proof. Preserve the embedded-library boundary and identify Phase 132 resource-server material and Phase 133 separate-origin proof only by existing links, without implementing or promising them here. Run documentation verification and the full Phase 131 gates.</action>
  <verify>
    <automated>mix docs.verify &amp;&amp; mix test test/integration/install_generator_test.exs test/integration/install_upgrade_test.exs test/lockspire/install/verify_test.exs test/mix/tasks/lockspire_verify_test.exs</automated>
  </verify>
  <done>The guide's commands match executable generated behavior, distinguish default/FAPI proof, and pass documentation plus focused installation gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host runtime/config/router/database → verification report | Diagnostics inspect adopter-controlled modules and state without exposing secrets. |
| Documentation → adopter deployment | Published commands determine route protection, migrations, and security-profile expectations. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-131-22 | Elevation of privilege | router verification | high | mitigate | Require compiled admin-before-public ordering and all host/consent routes; fail on missing/shadowed guard. |
| T-131-23 | Tampering | config diagnostics | high | mitigate | Check every required key independently and reject invalid values rather than silently substituting defaults. |
| T-131-24 | Information disclosure | verify output | high | mitigate | Emit module names, paths, key names, and safe status only; never inspect/log tokens, client secrets, claims, or raw interaction data. |
| T-131-25 | Denial of service | migration diagnosis | medium | mitigate | Distinguish missing host files, pending migrations, and unreachable repo with exact bounded remediation. |
| T-131-26 | Repudiation | onboarding truth | medium | mitigate | Gate documentation against compiled generated artifacts and focused install/upgrade/verify suites. |

## ASVS L1 Control-to-Automated-Test Evidence

| ASVS L1 control | Threats | Required automated evidence |
|---|---|---|
| V4 Access Control — verification fails closed when privileged route ordering/guarding is absent | T-131-22 | `mix test test/lockspire/install/verify_test.exs test/mix/tasks/lockspire_verify_test.exs` must reject missing, shadowed, or unguarded admin routes from the compiled route table. |
| V14 Configuration — required security configuration is explicit and independently diagnosed | T-131-23 | `mix test test/lockspire/install/verify_test.exs test/mix/tasks/lockspire_verify_test.exs` must produce one failure per missing/invalid required key without early abort or fallback. |
| V7 Error Handling and Logging — diagnostic output excludes secrets and sensitive authorization state | T-131-24 | `mix test test/lockspire/install/verify_test.exs test/mix/tasks/lockspire_verify_test.exs` must seed sensitive values and assert reports contain only safe key/module/path/remediation data. |
</threat_model>

<verification>
- `mix test test/lockspire/install/verify_test.exs test/mix/tasks/lockspire_verify_test.exs`
- `mix docs.verify`
- `mix test.fast && mix test.integration && mix qa && mix docs.verify`
</verification>

<success_criteria>
- Verification aggregates every required seam and actionable remediation.
- Route and migration checks inspect actual host integration surfaces.
- Onboarding uses only executable packaged-library commands.
- Full fast/integration/QA/docs phase gate is green without weakening security defaults.
- Plan completion is blocked unless every high-severity threat above has green named automated evidence; any unresolved high-severity threat prohibits creating 131-06-SUMMARY.md or declaring Phase 131 complete.
</success_criteria>

<output>
Create `.planning/phases/131-executable-installation/131-06-SUMMARY.md` when done.
</output>
