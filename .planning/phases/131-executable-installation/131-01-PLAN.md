---
phase: 131-executable-installation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/lockspire.install/router.ex
  - priv/templates/lockspire.install/config.exs
  - priv/templates/lockspire.install/account_resolver.ex
  - test/support/generated_host_app_web/router/lockspire.ex
  - test/support/generated_host_app_web/router.ex
  - test/integration/install_generator_test.exs
autonomous: true
requirements:
  - INST-01
  - INST-06
must_haves:
  truths:
    - "A rendered router helper emits executable Phoenix route AST, and the host route table places host browser and consent routes before guarded admin and public forwards (D-01, D-02, D-04)."
    - "The generated admin forward cannot compile without the host-owned :require_operator pipeline and appears before the public forward (D-03)."
    - "Generated configuration includes an explicit host-owned logout path and the rendered account resolver compiles with the real Claims fields (D-13, D-14)."
  artifacts:
    - path: "priv/templates/lockspire.install/router.ex"
      provides: "Imported defmacro lockspire_routes/0 with ordered Phoenix DSL"
      contains: "defmacro lockspire_routes"
    - path: "priv/templates/lockspire.install/account_resolver.ex"
      provides: "Truthful public Claims example"
      contains: "userinfo:"
    - path: "test/integration/install_generator_test.exs"
      provides: "Rendered-module compilation and Phoenix.Router.routes/1 proof"
  key_links:
    - from: "test/support/generated_host_app_web/router.ex"
      to: "test/support/generated_host_app_web/router/lockspire.ex"
      via: "import plus lockspire_routes() invocation"
      pattern: "import GeneratedHostAppWeb.Router.Lockspire"
    - from: "priv/templates/lockspire.install/router.ex"
      to: "Lockspire.Web.AdminRouter"
      via: "host-authenticated forward registered before the public router"
      pattern: "Lockspire.Web.AdminRouter"
---

<objective>
Make the generated host integration executable at Phoenix compile time and make its smallest config/claims examples truthful.

Purpose: Establish the phase tracer through the actual rendered router artifact, caller router, and compiled route table; remove the defects that currently let generation appear correct while producing unusable host code.
Output: An imported route macro, a generated-host router that consumes it, explicit logout configuration, a valid Claims example, and behavioral generator tests.

Decision trace: CONTEXT locked bullets are numbered top-to-bottom for these plans. This plan implements D-01 through D-04, D-13, D-14, and the generated-output portion of D-16.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-executable-installation/131-CONTEXT.md
@.planning/phases/131-executable-installation/131-RESEARCH.md
@.planning/phases/131-executable-installation/131-PATTERNS.md
@.planning/phases/131-executable-installation/131-VALIDATION.md
@lib/lockspire/web/router.ex
@priv/templates/lockspire.install/router.ex
@test/support/generated_host_app_web/router.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Compile the rendered route macro through the generated host</name>
  <files>priv/templates/lockspire.install/router.ex, test/support/generated_host_app_web/router/lockspire.ex, test/support/generated_host_app_web/router.ex, test/integration/install_generator_test.exs</files>
  <behavior>
    - Rendered helper defines an imported macro and registers concrete host verification, authorized-app, and consent routes.
    - Host router supplies its own :require_operator pipeline; guarded admin forwarding precedes the general Lockspire public forward.
    - Phoenix.Router.routes/1 reports the generated routes and their ordering from the compiled host router.
    - Removing or renaming the host operator pipeline makes the fixture compilation fail actionably rather than weakening the guard.
  </behavior>
  <action>Start with route-table assertions in install_generator_test.exs, then replace the string-returning helper with defmacro lockspire_routes/0 that returns quoted Phoenix router DSL per D-01. Emit fully qualified generated modules and the caller's :browser and :require_operator pipelines; order host verification/authorized-app routes and generated consent first, guarded Lockspire.Web.AdminRouter second, and Lockspire.Web.Router last per D-02. Do not generate operator authentication or a fallback pipeline: the fixture alone defines a realistic :require_operator plug pipeline per D-03. Import the rendered fixture module and call lockspire_routes() from GeneratedHostAppWeb.Router, keeping its protected API routes separate because Phase 132 owns that contract. Replace source-string and independently hand-written route proof with Phoenix.Router.routes/1 assertions over the actual compiled router per D-04 and D-16. Keep the committed rendered fixture byte-for-byte aligned with generator output.</action>
  <verify>
    <automated>mix test test/integration/install_generator_test.exs</automated>
  </verify>
  <done>The generated helper compiles as caller route AST; the compiled route table proves the exact host/consent/admin/public ordering and the admin path retains host-owned authorization.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile truthful generated config and claims seams</name>
  <files>priv/templates/lockspire.install/config.exs, priv/templates/lockspire.install/account_resolver.ex, test/integration/install_generator_test.exs</files>
  <behavior>
    - Generated config declares logout_path alongside repo, resolver, issuer, mount path, and prefixes.
    - The rendered resolver compiles against %Lockspire.Host.Claims{} using subject, id_token, and userinfo.
    - Host account and claim policy remain editable host-owned seams.
  </behavior>
  <action>Add the explicit host-editable logout_path to config/lockspire.exs per D-13 without adding a Lockspire-owned logout route. Replace the nonexistent Claims.claims example with subject, id_token, and userinfo per D-14 while preserving the resolver's raised implementation guidance and narrow host seam. Extend the generator test to evaluate the rendered config safely and Code.compile_string/2 the actual rendered resolver under its generated module name; assertions over template text alone do not satisfy D-16. Ensure repeated generator test runs purge any temporary compiled module cleanly.</action>
  <verify>
    <automated>mix test test/integration/install_generator_test.exs</automated>
  </verify>
  <done>The generated config exposes logout_path and the unmodified rendered resolver compiles using only real public Claims fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Host router → Lockspire public/admin routers | Host-controlled pipeline ordering determines whether operator-only routes are protected. |
| Generated config/resolver → Lockspire runtime | Host values and claims cross into protocol behavior through documented seams. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-131-01 | Elevation of privilege | generated router macro | high | mitigate | Require the caller-owned :require_operator pipeline, place the admin forward before the public forward, and assert compiled route indices. |
| T-131-02 | Tampering | generated router proof | high | mitigate | Compile and inspect the rendered macro output; remove hand-written substitute routes from the fixture. |
| T-131-03 | Information disclosure | generated claims example | medium | mitigate | Keep the example narrow and host-owned; expose only subject/id_token/userinfo maps and no secrets or raw protocol state. |
| T-131-04 | Denial of service | missing logout config | medium | mitigate | Generate an explicit host logout path; detailed fail-closed verification is completed in Plan 06. |

## ASVS L1 Control-to-Automated-Test Evidence

| ASVS L1 control | Threats | Required automated evidence |
|---|---|---|
| V4 Access Control — privileged routes require an explicit authorization layer | T-131-01 | `mix test test/integration/install_generator_test.exs` must prove the compiled route table places `:require_operator`-guarded admin forwarding before the public forward and that omitting the pipeline fails compilation. |
| V5 Validation and Sanitization — generated integration input is compiled and structurally validated | T-131-02 | `mix test test/integration/install_generator_test.exs` must render the router template, compile it unchanged in the generated-host router, and assert `Phoenix.Router.routes/1`; source-only or substitute-router evidence fails the control. |
</threat_model>

<verification>
- `mix test test/integration/install_generator_test.exs`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- The actual rendered router macro is imported and invoked by the generated-host router.
- `Phoenix.Router.routes/1` proves all required routes and order.
- The host owns and must define operator authentication.
- Rendered config and resolver compile with logout_path and real Claims fields.
- Plan completion is blocked unless every high-severity threat above has green named automated evidence; any unresolved high-severity threat prohibits creating 131-01-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/131-executable-installation/131-01-SUMMARY.md` when done.
</output>
