---
phase: 131-executable-installation
plan: 04
type: execute
wave: 2
depends_on:
  - 131-02
files_modified:
  - lib/lockspire/install/migrations.ex
  - lib/lockspire/install/operation_plan.ex
  - lib/lockspire/install/manifest.ex
  - lib/lockspire/generators/install.ex
  - lib/mix/tasks/lockspire.upgrade.ex
  - test/integration/install_upgrade_test.exs
autonomous: true
requirements:
  - INST-03
must_haves:
  truths:
    - "Fresh install and upgrade build one immutable operation plan and copy packaged migrations into host priv/repo/migrations only after migration identity/content, every managed destination, and manifest eligibility all succeed (D-09, D-10, D-12)."
    - "Old manifests remain readable, new manifests audit the complete migration inventory, and neither manifest state nor upgrade may authorize overwriting host files (D-10, D-11)."
    - "Upgrade adds only newly shipped migrations; repeat and dry-run behavior remain deterministic and non-mutating (D-11, D-12)."
    - "Public install and upgrade orchestration can be tested against a bounded test-only package migration root, and managed-file or migration collisions leave both artifact classes plus the manifest unchanged."
  artifacts:
    - path: "lib/lockspire/install/manifest.ex"
      provides: "Backward-compatible optional migration inventory"
      contains: "migrations"
    - path: "lib/lockspire/generators/install.ex"
      provides: "Install orchestration for migration preflight/apply/manifest"
    - path: "lib/lockspire/install/operation_plan.ex"
      provides: "Immutable all-artifact install/upgrade operation plan"
    - path: "lib/mix/tasks/lockspire.upgrade.ex"
      provides: "Upgrade orchestration sharing the migration service"
    - path: "test/integration/install_upgrade_test.exs"
      provides: "Fresh/repeat/upgrade/collision orchestration proof"
  key_links:
    - from: "lib/lockspire/generators/install.ex"
      to: "lib/lockspire/install/migrations.ex"
      via: "one OperationPlan validates migration and managed-file operations before either class is applied"
      pattern: "OperationPlan"
    - from: "lib/mix/tasks/lockspire.upgrade.ex"
      to: "lib/lockspire/install/manifest.ex"
      via: "refresh manifest only after approved migration and managed-file operations"
      pattern: "Manifest.build"
---

<objective>
Wire safe migration delivery into both supported generator lifecycles and persist its audit inventory compatibly.

Purpose: Turn the isolated preflight/apply primitive into the documented install and additive upgrade behavior while retaining managed-scaffolding drift protections.
Output: Backward-compatible manifest data, install/upgrade orchestration, dry-run behavior, and lifecycle integration tests.

Decision trace: Completes D-09 through D-12 across install, repeat install, upgrade, and collision behavior.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/131-executable-installation/131-CONTEXT.md
@.planning/phases/131-executable-installation/131-RESEARCH.md
@.planning/phases/131-executable-installation/131-PATTERNS.md
@.planning/phases/131-executable-installation/131-VALIDATION.md
@.planning/phases/131-executable-installation/131-02-SUMMARY.md
@lib/lockspire/install/manifest.ex
@lib/lockspire/generators/install.ex
@lib/mix/tasks/lockspire.upgrade.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the install manifest with an old-compatible migration audit</name>
  <files>lib/lockspire/install/manifest.ex, test/integration/install_upgrade_test.exs</files>
  <behavior>
    - New manifests contain a deterministically ordered migrations array with version, name, host-relative path, and checksum.
    - Existing manifests without migrations load and remain valid upgrade inputs.
    - Manifest contents never suppress disk preflight or authorize overwrites.
  </behavior>
  <action>Add an optional migration inventory input to Manifest.build while preserving the existing generator, version, inputs, and managed_files JSON shapes for v1.x compatibility. Serialize the complete inventory returned by Lockspire.Install.Migrations using stable string keys/order and Manifest.checksum/1 conventions. Add tests loading a legacy manifest with no migrations, writing/reloading a current manifest, and proving inventory metadata is audit-only: the migration service still derives safety from package and host files per D-10 and D-11.</action>
  <verify>
    <automated>mix test test/integration/install_upgrade_test.exs</automated>
  </verify>
  <done>Legacy manifests upgrade without migration metadata and current manifests record the complete deterministic migration inventory without changing overwrite authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute one immutable all-artifact plan through public install and upgrade</name>
  <files>lib/lockspire/install/migrations.ex, lib/lockspire/install/operation_plan.ex, lib/lockspire/generators/install.ex, lib/mix/tasks/lockspire.upgrade.ex, test/integration/install_upgrade_test.exs</files>
  <behavior>
    - Fresh install constructs one immutable operation plan containing migration copies/unchanged entries, every managed-file create/unchanged entry, and the resulting manifest; it performs no mkdir, copy, write, or manifest update until the whole plan validates.
    - Repeat install is byte-identical and reports migrations unchanged.
    - Upgrade adds only newly present packaged migrations and refreshes the manifest afterward.
    - Any managed-file collision or migration identity/content collision aborts before migration copies, managed scaffold writes, directory creation, or manifest mutation.
    - Upgrade dry-run reports planned migration/scaffold changes without writing any file.
    - Lifecycle tests inject a controlled package migration root through a test-only process-scoped seam while invoking Lockspire.Generators.Install.run/1 and Mix.Tasks.Lockspire.Upgrade.run/1 themselves.
  </behavior>
  <action>Create an opaque, immutable Lockspire.Install.OperationPlan struct built by pure preflight functions and accepted by a single apply/1 boundary. Both Install.run/1 and the internal branch reached by Mix.Tasks.Lockspire.Upgrade.run/1 must first render all selected templates in memory; inventory every packaged/host migration; read and classify every managed-file destination (including fresh-install host-owned and managed collisions); validate legacy/current manifest eligibility; and construct the final manifest payload. No File.mkdir_p, File.cp, File.write, Manifest.write, or status mutation may occur until that entire combined plan is {:ok, operation_plan}. Apply the approved operations in deterministic order, preserve byte-identical destinations, and write the manifest last. Keep current REFUSE/fix diagnostics and name package/host collision paths without file contents. Preserve dry-run by reporting from the same immutable plan and never calling apply/1.

Add `Lockspire.Install.Migrations.with_test_source_root/2` as an `@doc false` process-scoped injection that raises unless `Mix.env() == :test`, restores the prior process value in an `after` block, and is never exposed through either Mix task's OptionParser switches. The default source remains `Application.app_dir(:lockspire, "priv/repo/migrations")`. In install_upgrade_test.exs create controlled temporary package migration inventories inside that boundary, but call Lockspire.Generators.Install.run/1 and Mix.Tasks.Lockspire.Upgrade.run/1 exactly as adopters do. Add a fresh-install managed-destination collision with all migrations initially missing and assert the complete pre/post tree snapshot is identical: no migration directory/files, no earlier managed/host-owned outputs, and no manifest. Add a fresh-install migration version/name/content collision with all generated destinations initially missing and assert the same zero mutation across managed files, migrations, directories, and manifest. Retain repeat, additive upgrade, old manifest, late-collision, and dry-run coverage per D-09 through D-12. Tests that call Migrations.plan/apply without the public lifecycle entry points do not satisfy orchestration coverage.</action>
  <verify>
    <automated>mix test test/lockspire/install/migrations_test.exs test/integration/install_upgrade_test.exs</automated>
  </verify>
  <done>Both public lifecycle commands use one fully validated immutable operation plan; fresh managed and migration collision tests prove byte-for-byte zero mutation across migrations, generated files, directories, and manifest; the bounded fixture root executes only in tests and production defaults to packaged migrations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Install/upgrade CLI → host project tree | A library command may create managed and migration files in an adopter repository. |
| Legacy manifest → current upgrade | Untrusted/stale metadata must not weaken disk-derived collision checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-131-14 | Tampering | install/upgrade orchestration | high | mitigate | Complete migration and managed-file preflight before applying any write; use an immutable approved plan. |
| T-131-15 | Elevation of privilege | manifest authority | high | mitigate | Treat manifest migration data as audit metadata only and always rescan package/host files. |
| T-131-16 | Denial of service | partial upgrade | high | mitigate | Abort the entire operation on any late collision and test that prior candidates remain absent. |
| T-131-17 | Repudiation | migration delivery | medium | mitigate | Persist source checksums/identities and emit deterministic COPY/UNCHANGED/REFUSE diagnostics. |

## ASVS L1 Control-to-Automated-Test Evidence

| ASVS L1 control | Threats | Required automated evidence |
|---|---|---|
| V5 Validation and Sanitization — all external filesystem state is validated before mutation | T-131-14, T-131-16 | `mix test test/lockspire/install/migrations_test.exs test/integration/install_upgrade_test.exs` must exercise public install/upgrade orchestration and prove managed-file and migration collisions leave both artifact classes, directories, and manifest byte-for-byte unchanged. |
| V4 Access Control — stale metadata cannot grant overwrite authority | T-131-15 | `mix test test/integration/install_upgrade_test.exs` must load legacy/current manifests, alter host files independently of metadata, and prove disk-derived preflight refuses overwrite. |
</threat_model>

<verification>
- `mix test test/lockspire/install/migrations_test.exs test/integration/install_upgrade_test.exs`
- `mix compile --warnings-as-errors`
</verification>

<success_criteria>
- Install and upgrade both use one safe migration service.
- Collisions cannot cause partial mutation.
- Old manifests remain compatible and new manifests audit migration identities/checksums.
- Dry-run remains fully non-mutating.
- Plan completion is blocked unless every high-severity threat above has green named automated evidence; any unresolved high-severity threat prohibits creating 131-04-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/131-executable-installation/131-04-SUMMARY.md` when done.
</output>
