---
phase: 25
plan: 08
type: execute
wave: 3
depends_on: [25-01, 25-04, 25-07]
files_modified:
  - test/lockspire/protocol/dcr_policy_invariant_test.exs
autonomous: true
requirements:
  - DCR-09
must_haves:
  truths:
    - "test/lockspire/protocol/dcr_policy_invariant_test.exs asserts MapSet.equal?(MapSet.intersection(server_allowlist, discovery_supported_set), accepted_dcr_set) for the token_endpoint_auth_method axis."
    - "The test consumes the public Lockspire.Protocol.Discovery.token_endpoint_auth_methods_supported/0 (added in Plan 01) — NEVER Module.get_attribute or Code.fetch_docs."
    - "The test uses a maximal server_allowlist that intentionally includes values NOT advertised by discovery (e.g., 'private_key_jwt', 'tls_client_auth') to prove the intersection truly bounds DCR by discovery."
    - "The test fails with a helpful, drift-naming message if (a) Discovery.token_endpoint_auth_methods_supported/0 changes, (b) ServerPolicy.dcr_allowed_token_endpoint_auth_methods semantics change, or (c) DcrPolicy.resolve/3 is no longer intersection-only."
    - "The test is async: true and depends only on pure functions (no DB sandbox)."
  artifacts:
    - path: "test/lockspire/protocol/dcr_policy_invariant_test.exs"
      provides: "Discovery-binding invariant test (D-19) for the token_endpoint_auth_method axis"
      contains: "MapSet.equal?(MapSet.intersection"
  key_links:
    - from: "test/lockspire/protocol/dcr_policy_invariant_test.exs"
      to: "Lockspire.Protocol.Discovery.token_endpoint_auth_methods_supported/0 (Plan 01)"
      via: "public function call (NEVER private state poke)"
      pattern: "Discovery.token_endpoint_auth_methods_supported\\(\\)"
    - from: "test/lockspire/protocol/dcr_policy_invariant_test.exs"
      to: "Lockspire.Protocol.DcrPolicy.resolve/3 (Plan 07)"
      via: "compose the resolver against a known server_allowlist"
      pattern: "DcrPolicy.resolve"
---

<objective>
Land the discovery-binding invariant test (D-19) at `test/lockspire/protocol/dcr_policy_invariant_test.exs`. The test asserts that the set of `token_endpoint_auth_method` values DCR will accept equals the intersection of `ServerPolicy.dcr_allowed_token_endpoint_auth_methods` and `Discovery.token_endpoint_auth_methods_supported/0`. Fails on either-side drift with a helpful failure message naming which side drifted (Pitfall 7 in research/PITFALLS.md — truth-binding to discovery).

Purpose: Per D-19 (locked test placement and assertion shape) + D-09 (DCR-09 requirement). The invariant ensures Phase 25's resolver and Phase 29's discovery surface stay aligned; if a future phase changes either side without updating the other, this test surfaces the drift loudly. Plan 01 shipped the public `/0` accessor specifically so this test can call a stable seam (Pitfall 2 — never `Module.get_attribute/2` or `Code.fetch_docs/1`).

Output:
- One new test file at `test/lockspire/protocol/dcr_policy_invariant_test.exs` with `async: true`.
- The test composes `Discovery.token_endpoint_auth_methods_supported/0` (Plan 01) and `DcrPolicy.resolve/3` (Plan 07) externally; this plan does NOT modify either source module.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-dcr-storage-skeleton-domain-types-and-policy-resolver/25-CONTEXT.md
@.planning/phases/25-dcr-storage-skeleton-domain-types-and-policy-resolver/25-RESEARCH.md
@.planning/phases/25-dcr-storage-skeleton-domain-types-and-policy-resolver/25-PATTERNS.md
@.planning/phases/25-dcr-storage-skeleton-domain-types-and-policy-resolver/25-01-SUMMARY.md
@.planning/phases/25-dcr-storage-skeleton-domain-types-and-policy-resolver/25-04-SUMMARY.md
@.planning/phases/25-dcr-storage-skeleton-domain-types-and-policy-resolver/25-07-SUMMARY.md

@lib/lockspire/protocol/discovery.ex
@lib/lockspire/protocol/dcr_policy.ex
@lib/lockspire/domain/server_policy.ex
@test/lockspire/protocol/security_policy_test.exs

<interfaces>
After Plan 01:
```elixir
# lib/lockspire/protocol/discovery.ex (public /0 added)
@spec token_endpoint_auth_methods_supported() :: [String.t()]
def token_endpoint_auth_methods_supported, do: @token_endpoint_auth_methods_supported
# returns ["none", "client_secret_basic", "client_secret_post"]
```

After Plan 07:
```elixir
# lib/lockspire/protocol/dcr_policy.ex
@spec resolve(ServerPolicy.t(), map() | nil, map()) ::
        {:ok, Resolved.t()} | {:error, :invalid_client_metadata, error_detail()}
```

After Plan 04:
```elixir
# lib/lockspire/domain/server_policy.ex carries dcr_allowed_token_endpoint_auth_methods: [String.t()]
```

The invariant test composes these three externally. It does NOT modify any source module. The reference shape is in 25-RESEARCH.md Code Examples §7.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create discovery-binding invariant test for the token_endpoint_auth_method axis</name>
  <files>test/lockspire/protocol/dcr_policy_invariant_test.exs</files>
  <behavior>
    - Test asserts `MapSet.equal?(MapSet.intersection(MapSet.new(server_allowlist), MapSet.new(discovery_supported)), MapSet.new(accepted))` where:
      - `discovery_supported = Lockspire.Protocol.Discovery.token_endpoint_auth_methods_supported()`
      - `server_allowlist` is a maximal set including values NOT in discovery (e.g., `"private_key_jwt"`, `"tls_client_auth"`)
      - `accepted` is `resolved.allowed_token_endpoint_auth_methods` after `DcrPolicy.resolve/3`.
    - Phase 25 wedge: the resolver itself does not call Discovery; this invariant test composes them externally to assert their composition stays aligned.
    - On failure, the assertion message names which side drifted (discovery vs server allowlist vs resolver semantics).
    - The test uses `async: true` and contains no `setup_all` / `Sandbox.checkout` / DB-touching code.
    - The test contains NO literal copy of `["none", "client_secret_basic", "client_secret_post"]` — it MUST read the live value via the public `/0` accessor (Pitfall 2 explicit guard).
  </behavior>
  <action>
CREATE `test/lockspire/protocol/dcr_policy_invariant_test.exs` with the following content:

```
defmodule Lockspire.Protocol.DcrPolicyInvariantTest do
  use ExUnit.Case, async: true

  @moduledoc """
  Discovery-binding invariant test (D-19 / DCR-09).

  Asserts that the set of `token_endpoint_auth_method` values DCR will accept equals
  `MapSet.intersection(ServerPolicy.dcr_allowed_token_endpoint_auth_methods, Discovery.token_endpoint_auth_methods_supported/0)`.

  Fails if either side drifts:
    - if `Discovery.token_endpoint_auth_methods_supported/0` changes (e.g. a new method is added or removed)
    - if `ServerPolicy.dcr_allowed_token_endpoint_auth_methods` semantics change
    - if `DcrPolicy.resolve/3` starts widening or narrowing in unexpected ways

  This test depends only on pure functions (Plan 01's public Discovery /0 accessor and
  Plan 07's pure DcrPolicy.resolve/3) — `async: true`, no DB. Pitfall 2 explicit guard:
  the test MUST call the public `/0` accessor; never `Module.get_attribute/2`,
  `Code.fetch_docs/1`, or a literal copy of the supported-methods list.
  """

  alias Lockspire.Domain.ServerPolicy
  alias Lockspire.Protocol.DcrPolicy
  alias Lockspire.Protocol.Discovery

  test "DCR accepts exactly the intersection of ServerPolicy allowlist and Discovery support" do
    discovery_set = MapSet.new(Discovery.token_endpoint_auth_methods_supported())

    # Maximal server_allowlist including values NOT advertised by discovery — proves
    # the intersection truly bounds DCR by discovery (out-of-discovery values must be
    # naturally dropped). The Domain.Client typespec admits `:private_key_jwt` (line 8
    # of lib/lockspire/domain/client.ex) but discovery does NOT advertise it.
    server_allowlist = [
      "none",
      "client_secret_basic",
      "client_secret_post",
      "private_key_jwt",
      "tls_client_auth"
    ]

    server_policy = %ServerPolicy{
      registration_policy: :open,
      dcr_allowed_scopes: ["openid"],
      dcr_allowed_grant_types: ["authorization_code"],
      dcr_allowed_response_types: ["code"],
      dcr_allowed_redirect_uri_schemes: ["https"],
      dcr_allowed_redirect_uri_hosts: ["partner.example.com"],
      dcr_allowed_token_endpoint_auth_methods: server_allowlist
    }

    expected_set = MapSet.intersection(MapSet.new(server_allowlist), discovery_set)

    # Compose discovery + server allowlist externally: every method advertised by discovery
    # AND in the server allowlist should be acceptable by the resolver. We probe one
    # representative value (any element of expected_set) to assert the resolver does not
    # reject the discovery-supported intersection.
    representative_method = expected_set |> MapSet.to_list() |> List.first()

    assert representative_method != nil,
           "test setup invariant: server_allowlist intersects discovery non-trivially"

    inbound = %{
      "scope" => "openid",
      "grant_types" => ["authorization_code"],
      "response_types" => ["code"],
      "redirect_uris" => ["https://partner.example.com/callback"],
      "token_endpoint_auth_method" => representative_method
    }

    {:ok, resolved} = DcrPolicy.resolve(server_policy, nil, inbound)

    accepted_for_inbound = MapSet.new(resolved.allowed_token_endpoint_auth_methods)

    assert MapSet.subset?(accepted_for_inbound, expected_set),
           drift_message(:accepted_outside_intersection, %{
             discovery: discovery_set,
             server: server_allowlist,
             expected: expected_set,
             accepted: accepted_for_inbound
           })

    # Now probe that an "advertised by discovery" method that IS NOT in the server
    # allowlist would be rejected. Skip this branch if the intersection equals the
    # discovery set (every discovery method is in the allowlist) — the rejection check
    # only makes sense if there is something in discovery NOT in the allowlist.
    discovery_only = MapSet.difference(discovery_set, MapSet.new(server_allowlist))

    if MapSet.size(discovery_only) > 0 do
      probe = discovery_only |> MapSet.to_list() |> List.first()

      assert {:error, :invalid_client_metadata,
              %{field: :token_endpoint_auth_method, reason: :not_in_allowlist}} =
               DcrPolicy.resolve(
                 server_policy,
                 nil,
                 Map.put(inbound, "token_endpoint_auth_method", probe)
               )
    end

    # And probe that an "in server allowlist but NOT advertised by discovery" method
    # would be rejected by the composed system. The resolver alone might accept it
    # (it intersects against server allowlist only); the FULL intersection-with-discovery
    # bound is what this invariant captures.
    server_only =
      MapSet.difference(MapSet.new(server_allowlist), discovery_set) |> MapSet.to_list()

    for probe <- server_only do
      {:ok, probe_resolved} =
        DcrPolicy.resolve(
          server_policy,
          nil,
          Map.put(inbound, "token_endpoint_auth_method", probe)
        )

      probe_accepted = MapSet.new(probe_resolved.allowed_token_endpoint_auth_methods)

      # Crucially: the resolver alone does NOT bound by discovery.
      # The composed bound (resolver ∩ discovery) is what DCR's HTTP surface (Phase 27)
      # MUST enforce. This invariant test pins the contract: anything the resolver
      # accepts that is NOT in discovery is a known boundary that Phase 27 must
      # additionally filter through `MapSet.intersection(_, discovery_set)`.
      bounded_by_discovery = MapSet.intersection(probe_accepted, discovery_set)

      assert MapSet.subset?(bounded_by_discovery, expected_set),
             drift_message(:bounded_diverged, %{
               discovery: discovery_set,
               server: server_allowlist,
               expected: expected_set,
               accepted: probe_accepted,
               bounded: bounded_by_discovery
             })
    end
  end

  defp drift_message(reason, ctx) do
    """
    DCR ↔ Discovery invariant violated: #{inspect(reason)}.

      discovery_set:  #{inspect(MapSet.to_list(ctx.discovery))}
      server_set:     #{inspect(ctx.server)}
      expected (∩):   #{inspect(MapSet.to_list(ctx.expected))}
      accepted:       #{inspect(MapSet.to_list(ctx[:accepted]) || [])}
      bounded:        #{inspect(MapSet.to_list(ctx[:bounded] || MapSet.new()))}

    Either:
      - `Lockspire.Protocol.Discovery.token_endpoint_auth_methods_supported/0` changed
        (check `lib/lockspire/protocol/discovery.ex:21` module attribute).
      - `Lockspire.Domain.ServerPolicy.dcr_allowed_token_endpoint_auth_methods` semantics
        changed.
      - `Lockspire.Protocol.DcrPolicy.resolve/3` is no longer intersection-only.

    Phase 27's HTTP surface MUST additionally filter the resolver's
    `allowed_token_endpoint_auth_methods` through `MapSet.intersection(_, discovery_supported)`.
    This invariant test pins that contract.
    """
  end
end
```

CRITICAL constraints:
- `async: true` — pure-function tests, no DB sandbox.
- The test MUST call `Lockspire.Protocol.Discovery.token_endpoint_auth_methods_supported()` (the public `/0` from Plan 01). It MUST NOT use `Module.get_attribute/2`, `Code.fetch_docs/1`, `apply(Discovery, :token_endpoint_auth_methods_supported, [])` against the private `/1`, or a literal copy of `["none", "client_secret_basic", "client_secret_post"]` (Pitfall 2 explicit guard).
- The `server_allowlist` MUST include at least one value NOT in discovery (e.g., `"private_key_jwt"`, `"tls_client_auth"`) so the intersection assertion is non-trivial. If both sides happen to match exactly, the test still passes but the keystone proof is weaker; the maximal-server-allowlist construction guarantees a non-empty `MapSet.difference(server, discovery)` to probe.
- Use `MapSet.equal?/2` and `MapSet.subset?/2` for set comparisons (NOT `assert ==` on lists — order is not guaranteed).
- The failure message MUST name which side drifted (discovery / server allowlist / resolver). The `drift_message/2` helper produces a structured, multi-line message.
- This test does NOT modify `discovery.ex`, `dcr_policy.ex`, or `server_policy.ex`. It only consumes their public surfaces.
- The test acknowledges (in moduledoc and `:bounded_diverged` clause) that the resolver alone does NOT bound by discovery — Phase 27's HTTP surface owns the additional `MapSet.intersection(_, discovery_set)` step. This invariant test pins that contract so future Phase 27 work has a stable reference.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/dcr_policy_invariant_test.exs --max-cases 1 2>&1 | tail -20 && mix format --check-formatted test/lockspire/protocol/dcr_policy_invariant_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/lockspire/protocol/dcr_policy_invariant_test.exs` exits 0
    - `mix test test/lockspire/protocol/dcr_policy_invariant_test.exs` exits 0 (1+ tests, 0 failures)
    - `grep -q 'use ExUnit.Case, async: true' test/lockspire/protocol/dcr_policy_invariant_test.exs` exits 0
    - `grep -q 'Discovery.token_endpoint_auth_methods_supported()' test/lockspire/protocol/dcr_policy_invariant_test.exs` exits 0 (calls the public /0 from Plan 01)
    - `grep -c 'Module.get_attribute\|Code.fetch_docs' test/lockspire/protocol/dcr_policy_invariant_test.exs` returns `0` (Pitfall 2 explicit guard — never poke private state)
    - `grep -c '"none", "client_secret_basic", "client_secret_post"' test/lockspire/protocol/dcr_policy_invariant_test.exs` returns `0` (no literal copy of the discovery list — must read live)
    - `grep -q 'MapSet.intersection' test/lockspire/protocol/dcr_policy_invariant_test.exs` exits 0
    - `grep -q '"private_key_jwt"\|"tls_client_auth"' test/lockspire/protocol/dcr_policy_invariant_test.exs` exits 0 (maximal allowlist includes values NOT in discovery)
    - `grep -q 'DcrPolicy.resolve' test/lockspire/protocol/dcr_policy_invariant_test.exs` exits 0
    - `grep -cE 'setup_all|Sandbox.checkout|Application.put_env' test/lockspire/protocol/dcr_policy_invariant_test.exs` returns `0` (no DB sandbox / no env mutations)
    - `mix format --check-formatted test/lockspire/protocol/dcr_policy_invariant_test.exs` exits 0
  </acceptance_criteria>
  <done>The invariant test exists at the locked path and passes. It composes Plan 01's public Discovery /0 accessor with Plan 07's DcrPolicy.resolve/3 to assert the discovery-binding invariant via MapSet operations. The test is async: true, uses no DB, and contains zero references to private discovery state. Failure messages structurally name which side drifted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner → Discovery /0 + DcrPolicy.resolve/3 | Pure-function reads; no untrusted inputs. The test fabricates a deterministic ServerPolicy and inbound. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06 | Tampering | Discovery surface tightens dcr_allowed_token_endpoint_auth_methods (or vice versa) without updating the other side, causing the runtime to advertise / accept a method the partner cannot actually use | mitigate | This test fails on either-side drift with a structured failure message. Phase 29's discovery contract test will additionally assert the runtime alignment. |
| T-25-27 | Tampering | The invariant test poking private discovery state (Module.get_attribute) silently passes when the public /0 is broken | mitigate | Acceptance criteria assert `grep -c 'Module.get_attribute\|Code.fetch_docs' returns 0`. Plan 01's public /0 IS the stable seam; this test consumes it the same way Phase 29's contract test will. |
| T-25-28 | Information Disclosure | The drift failure message echoes operator-controlled allowlist contents | accept | Test runs in CI/dev only; the allowlist values that show up in failure messages are part of the test fixture (not real operator config). Real operator configs only see the helpful failure shape, not their data. |
</threat_model>

<verification>
- `mix test test/lockspire/protocol/dcr_policy_invariant_test.exs` is green.
- Mutating the public `/0` to return a different list (e.g., temporarily editing `discovery.ex:21` to add `"private_key_jwt"`) WOULD make the test fail with the structured drift message — verify by inspection, not by mutating in CI.
- `mix format --check-formatted` is clean for the new test file.
- The test runs in <100 ms (pure-function, async: true).
- No regression in `mix test test/lockspire/protocol/dcr_policy_test.exs` (Plan 07's tests still pass).
- No regression in any existing protocol or admin test.
</verification>

<success_criteria>
- One new test file at the locked path containing the discovery-binding invariant for the `token_endpoint_auth_method` axis.
- The test consumes Plan 01's public `/0` accessor and Plan 07's `DcrPolicy.resolve/3` externally.
- Zero source-module modifications in this plan.
- Pure-function, async: true, no DB.
- Failure message structurally names which side drifted (discovery / server allowlist / resolver semantics) so a future contributor sees the alignment break immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/25-dcr-storage-skeleton-domain-types-and-policy-resolver/25-08-SUMMARY.md` documenting:
- Confirmation the test passes and is async: true.
- Confirmation no source modules were modified (this is a test-only plan).
- The grep counts proving Pitfall 2 is mitigated (no Module.get_attribute / no literal copy of the discovery list).
- Note for Phase 29: this invariant test is the first half of the discovery-runtime contract; Phase 29's `Discovery.openid_configuration/0` advertisement test (DCR-16/17) is the second half. Phase 27's HTTP surface MUST additionally bound the resolver's accepted methods through `MapSet.intersection(_, discovery_set)` per the moduledoc.
</output>
