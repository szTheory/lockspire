---
phase: 72-jarm-encryption-and-metadata
plan: 02
type: execute
wave: 2
depends_on:
  - 72-01
files_modified:
  - lib/lockspire/protocol/jarm.ex
  - lib/lockspire/protocol/jarm/client_key_resolver.ex
  - lib/lockspire/protocol/authorization_flow.ex
  - test/lockspire/protocol/jarm_test.exs
  - test/lockspire/protocol/authorization_flow_test.exs
  - test/lockspire/web/authorize_controller_test.exs
  - test/lockspire/jwks_fetcher_test.exs
autonomous: true
requirements:
  - JARM-03
must_haves:
  truths:
    - "When a client requests encrypted JARM, Lockspire returns a nested JWT: signed first, encrypted second."
    - "Client encryption keys resolve from inline `jwks` or guarded remote `jwks_uri` with at most one refresh attempt."
    - "If encrypted JARM cannot be safely produced, the authorization flow fails closed instead of downgrading to signed-only or raw parameters."
  artifacts:
    - path: "lib/lockspire/protocol/jarm.ex"
      provides: "Single encode boundary for signed-only and nested encrypted JARM"
    - path: "lib/lockspire/protocol/jarm/client_key_resolver.ex"
      provides: "Explicit client recipient-key resolution using inline or guarded remote JWKS"
    - path: "lib/lockspire/protocol/authorization_flow.ex"
      provides: "Fail-closed redirect formatting for encrypted JARM"
  key_links:
    - from: "lib/lockspire/protocol/authorization_flow.ex"
      to: "lib/lockspire/protocol/jarm.ex"
      via: "authorization response encoding"
      pattern: "Jarm\\.(encode|sign)"
    - from: "lib/lockspire/protocol/jarm/client_key_resolver.ex"
      to: "lib/lockspire/jwks_fetcher.ex"
      via: "get_keys/2 then one refresh_keys/2 retry"
      pattern: "refresh_keys"
---

<objective>
Produce encrypted JARM as nested JWTs and resolve client recipient keys through the existing guarded JWKS seam.

Purpose: Turn the Phase 71 signer into the actual confidential authorization-response path required by JARM-03.
Output: A single JARM encode boundary, guarded recipient-key resolution, and fail-closed authorization-flow wiring.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-jarm-encryption-and-metadata/72-CONTEXT.md
@.planning/phases/72-jarm-encryption-and-metadata/72-RESEARCH.md
@.planning/phases/40-jwe-support-for-request-objects/40-CONTEXT.md
@.planning/phases/60-guarded-remote-jwks-resolution/60-CONTEXT.md
@.planning/phases/61-shared-private-key-jwt-verification/61-CONTEXT.md
@.planning/phases/71-jarm-core/71-01-SUMMARY.md
@.planning/phases/71-jarm-core/71-02-SUMMARY.md
@lib/lockspire/protocol/jarm.ex
@lib/lockspire/protocol/authorization_flow.ex
@lib/lockspire/jwks_fetcher.ex
@test/lockspire/protocol/request_object_test.exs
@test/lockspire/jwks_fetcher_test.exs

<interfaces>
From lib/lockspire/protocol/jarm.ex:
```elixir
@spec sign(map(), map()) :: {:ok, String.t()} | {:error, signing_error()}
```

From lib/lockspire/protocol/authorization_flow.ex:
```elixir
defp build_response_redirect(%Interaction{} = interaction, params, opts)
defp sign_jarm_response(interaction, params, opts)
defp format_jarm_redirect(interaction, mode, jwt)
```

From lib/lockspire/jwks_fetcher.ex:
```elixir
@spec get_keys(String.t(), keyword()) :: result()
@spec refresh_keys(String.t(), keyword()) :: result()
```

From test/lockspire/protocol/request_object_test.exs:
```elixir
jws = JOSE.JWT.sign(sig_jwk, %{"alg" => "ES256"}, claims)
{_, jws_compact} = JOSE.JWS.compact(jws)
jwe = JOSE.JWE.block_encrypt(enc_jwk, jws_compact, %{"alg" => "RSA-OAEP", "enc" => "A256GCM"})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build explicit client recipient-key resolution per D-02 through D-07</name>
  <files>lib/lockspire/protocol/jarm/client_key_resolver.ex, test/lockspire/protocol/jarm_test.exs, test/lockspire/jwks_fetcher_test.exs</files>
  <behavior>
    - Test 1: inline `jwks` picks a compatible public key, preferring `use=enc` and matching `kid` when present.
    - Test 2: `jwks_uri` resolution uses the guarded fetcher, retries only once via `refresh_keys/2` on mismatch or unavailable cached keys, and never loops.
    - Test 3: unsupported key shape or unsupported `alg`/`enc` returns a stable internal failure reason instead of a fallback key.
  </behavior>
  <action>Create a dedicated resolver for JARM recipient keys rather than hiding lookup inside `AuthorizationFlow`. Reuse `Lockspire.JwksFetcher` per D-04, preserve inline `jwks` xor guarded remote `jwks_uri` semantics from Plan 72-01, and implement explicit compatibility checks per D-05 for `RSA-OAEP-256` and `ECDH-ES`. Permit one bounded refresh attempt per D-07, but never broaden this into a retry loop or a generic remote-ingestion subsystem.</action>
  <verify>
    <automated>mix test test/lockspire/jwks_fetcher_test.exs test/lockspire/protocol/jarm_test.exs</automated>
  </verify>
  <done>JARM recipient-key resolution is explicit, guarded, bounded, and returns stable fail-closed reasons for unusable client keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert JARM signing into nested encode and fail-closed redirect behavior per D-06, D-08, D-18, and D-19</name>
  <files>lib/lockspire/protocol/jarm.ex, lib/lockspire/protocol/authorization_flow.ex, test/lockspire/protocol/jarm_test.exs, test/lockspire/protocol/authorization_flow_test.exs, test/lockspire/web/authorize_controller_test.exs</files>
  <behavior>
    - Test 1: `Jarm.encode/2` returns a signed compact JWS when no encryption metadata is present and a compact JWE wrapping that JWS when encryption metadata is present.
    - Test 2: encryption always occurs after signing, never as raw-claims JWE, and only for the allowed `alg`/`enc` pairs from D-15 and D-16.
    - Test 3: authorization approval and denial flows surface an AS-side failure when encrypted JARM cannot be produced, and they do not fall back to signed-only or raw redirect params.
    - Test 4: controller-level authorization endpoint behavior proves the browser-visible fail-closed path and confirms no redirect downgrade occurs when encrypted JARM generation fails.
  </behavior>
  <action>Refactor `Lockspire.Protocol.Jarm` into one encode boundary that always signs first and encrypts second when the client has explicit response-encryption metadata. Keep the JOSE call order aligned with Phase 40’s nested JWT precedent, preserve existing signing-key lookup for the issuer, and wire `AuthorizationFlow` to call the new encode helper instead of open-coding JARM behavior. If encryption was requested but no safe compatible client key can be used, return a stable error path that keeps external behavior non-leaky per D-08 and D-09 and never downgrades per D-06. Extend both protocol-level and controller-level coverage so the externally visible browser path proves the same fail-closed behavior as the internal protocol path.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/protocol/jarm_test.exs test/lockspire/protocol/authorization_flow_test.exs test/lockspire/web/authorize_controller_test.exs</automated>
  </verify>
  <done>Authorization responses become nested JWTs for configured clients, and encrypted-JARM failures stay fail-closed all the way through the authorization flow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authorization response claims -> JOSE encoder | Security-sensitive response data crosses into cryptographic packaging |
| Lockspire -> remote `jwks_uri` | Outbound network trust boundary for client recipient keys |
| protocol result -> browser redirect behavior | Errors must not silently weaken confidentiality guarantees |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-03 | Information disclosure | `jarm.ex` | mitigate | Enforce nested JWT only and block any downgrade from encrypted to signed-only or raw parameters per D-06 and D-18. |
| T-72-04 | Denial of service | `client_key_resolver.ex` / `jwks_fetcher.ex` | mitigate | Limit remote recovery to one guarded refresh and reuse existing low-timeout fetcher behavior per D-04 and D-07. |
| T-72-05 | Tampering | client recipient-key selection | mitigate | Require algorithm/key-shape compatibility and explicit `kid`/`use=enc` preference per D-05. |
</threat_model>

<verification>
Run the focused JARM, authorization-flow, authorize-controller, and JWKS fetcher tests. Confirm the encoded output is nested JWE for encrypted clients and that failure cases return explicit errors rather than redirect downgrades at either the protocol or browser boundary.
</verification>

<success_criteria>
Configured clients receive encrypted nested JARM responses via inline or guarded remote recipient keys, with no silent downgrade path and no new host-owned network responsibilities.
</success_criteria>

<output>
After completion, create `.planning/phases/72-jarm-encryption-and-metadata/72-02-SUMMARY.md`
</output>
