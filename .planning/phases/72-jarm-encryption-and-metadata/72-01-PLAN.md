---
phase: 72-jarm-encryption-and-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/TIMESTAMP_add_authorization_response_encryption_fields_to_lockspire_clients.exs
  - lib/lockspire/domain/client.ex
  - lib/lockspire/storage/ecto/client_record.ex
  - lib/lockspire/protocol/registration.ex
  - lib/lockspire/protocol/registration_management.ex
  - test/lockspire/storage/ecto/client_record_test.exs
  - test/lockspire/protocol/registration_test.exs
  - test/lockspire/protocol/registration_management_test.exs
autonomous: true
requirements:
  - JARM-03
must_haves:
  truths:
    - "Clients can persist `authorization_encrypted_response_alg` and `authorization_encrypted_response_enc` alongside existing JARM signing metadata."
    - "DCR intake and RFC 7592 updates reject incoherent encrypted-JARM metadata instead of accepting partial or weakening configurations."
    - "The existing `jwks` xor `jwks_uri` contract remains intact for clients that enable encrypted JARM."
  artifacts:
    - path: "lib/lockspire/domain/client.ex"
      provides: "Durable client fields for authorization-response encryption metadata"
    - path: "lib/lockspire/storage/ecto/client_record.ex"
      provides: "Ecto schema and changesets for persisted encryption metadata"
    - path: "lib/lockspire/protocol/registration.ex"
      provides: "DCR validation and persistence for encrypted-JARM metadata"
    - path: "lib/lockspire/protocol/registration_management.ex"
      provides: "RFC 7592 update path for encrypted-JARM metadata"
  key_links:
    - from: "lib/lockspire/protocol/registration.ex"
      to: "lib/lockspire/domain/client.ex"
      via: "persist_client/4 metadata mapping"
      pattern: "authorization_encrypted_response_(alg|enc)"
    - from: "lib/lockspire/protocol/registration_management.ex"
      to: "lib/lockspire/storage/ecto/client_record.ex"
      via: "apply_metadata_to_client -> changeset persistence"
      pattern: "authorization_encrypted_response_(alg|enc)"
---

<objective>
Persist the client metadata that enables encrypted JARM and make DCR/RFC 7592 tell the truth about when it is valid.

Purpose: Establish the durable contract Phase 72 needs before any runtime encryption logic exists.
Output: Migration, domain/Ecto support, and registration-management validation for encrypted-JARM metadata.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-jarm-encryption-and-metadata/72-CONTEXT.md
@.planning/phases/72-jarm-encryption-and-metadata/72-RESEARCH.md
@.planning/phases/72-jarm-encryption-and-metadata/PATTERNS.md
@.planning/phases/72-jarm-encryption-and-metadata/ADVISOR.md
@.planning/phases/59-registration-policy-metadata-truth/59-CONTEXT.md
@.planning/phases/71-jarm-core/71-CONTEXT.md
@lib/lockspire/domain/client.ex
@lib/lockspire/storage/ecto/client_record.ex
@lib/lockspire/protocol/registration.ex
@lib/lockspire/protocol/registration_management.ex

<interfaces>
From lib/lockspire/domain/client.ex:
```elixir
@type t :: %__MODULE__{
  authorization_signed_response_alg: signing_alg() | nil,
  jwks: map() | nil,
  jwks_uri: String.t() | nil,
  metadata: map()
}
```

From lib/lockspire/protocol/registration.ex:
```elixir
def validate_intake_metadata(metadata, %Resolved{} = _resolved, server_policy)
defp validate_jwks(metadata)
defp persist_client(metadata, %Resolved{} = resolved, iat_record, credentials, source)
```

From lib/lockspire/protocol/registration_management.ex:
```elixir
def update(String.t(), update_request())
defp apply_metadata_to_client(%Client{} = client, metadata)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add durable encrypted-JARM client metadata per D-02, D-03, D-14, D-16, and D-18</name>
  <files>priv/repo/migrations/TIMESTAMP_add_authorization_response_encryption_fields_to_lockspire_clients.exs, lib/lockspire/domain/client.ex, lib/lockspire/storage/ecto/client_record.ex, test/lockspire/storage/ecto/client_record_test.exs</files>
  <behavior>
    - Test 1: a `ClientRecord` round-trip preserves `authorization_encrypted_response_alg` and `authorization_encrypted_response_enc`.
    - Test 2: the changeset accepts only the narrow response-encryption allow-list (`RSA-OAEP-256`, `ECDH-ES`, `A256GCM`, `A128GCM`) and rejects unsupported values such as CBC modes.
    - Test 3: persisted clients without encryption metadata remain valid and unchanged.
  </behavior>
  <action>Create the client-table migration and extend `Lockspire.Domain.Client` plus `Lockspire.Storage.Ecto.ClientRecord` so encrypted-JARM metadata is first-class durable state rather than hidden extension metadata. Keep the JWE surface intentionally narrow per D-14 through D-17, and model encryption as an addition to the existing JARM signing contract, not a replacement for it. Update `changeset/2`, `update_changeset/2`, and `to_domain/1` so both create and update paths see the same fields.</action>
  <verify>
    <automated>mix test test/lockspire/storage/ecto/client_record_test.exs</automated>
  </verify>
  <done>The client schema, domain struct, and persistence layer all expose the encrypted-JARM fields and reject out-of-scope `alg`/`enc` values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce DCR and RFC 7592 metadata truth per D-02, D-03, D-05, D-06, D-08, D-18, and D-21</name>
  <files>lib/lockspire/protocol/registration.ex, lib/lockspire/protocol/registration_management.ex, test/lockspire/protocol/registration_test.exs, test/lockspire/protocol/registration_management_test.exs</files>
  <behavior>
    - Test 1: registration accepts encrypted JARM only when signing metadata is also present and either inline `jwks` or guarded `jwks_uri` is configured.
    - Test 2: registration and management updates reject `jwks` plus `jwks_uri`, encryption metadata without compatible signing metadata, and partial `alg`/`enc` submissions.
    - Test 3: accepted metadata is persisted onto the client domain object returned from registration and update flows.
  </behavior>
  <action>Extend the registration and management validators so encrypted JARM is explicit and unsurprising: preserve `jwks` xor `jwks_uri` per D-03, reuse the existing Lockspire-owned metadata-validation path rather than introducing a new policy plane per D-19 and D-20, and fail with `invalid_client_metadata` when encryption metadata is partial, unsupported, or missing the mandatory signing metadata per D-18. Thread the accepted values through `persist_client/4` and `apply_metadata_to_client/2` so Phase 72 runtime code can rely on durable fields instead of ad hoc maps.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/registration_test.exs test/lockspire/protocol/registration_management_test.exs</automated>
  </verify>
  <done>DCR and RFC 7592 update flows persist encrypted-JARM metadata only for coherent configurations and preserve the repo's existing `jwks` xor `jwks_uri` rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client metadata -> DCR/RFC 7592 validators | Untrusted registration payload crosses into durable client state |
| persisted client row -> protocol runtime | Invalid stored crypto metadata would weaken later authorization-response handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-01 | Tampering | `registration.ex` / `registration_management.ex` | mitigate | Reject partial or unsupported encryption metadata and preserve `jwks` xor `jwks_uri` per D-03 and D-18. |
| T-72-02 | Information disclosure | `client_record.ex` / migration | mitigate | Keep encryption capability limited to explicit durable fields and avoid silently inferring weaker runtime behavior from missing metadata. |
</threat_model>

<verification>
Run the focused DCR and persistence tests, then confirm the new client fields are present in the migration, domain struct, and Ecto conversion path.
</verification>

<success_criteria>
Client registrations and RFC 7592 updates can truthfully persist encrypted-JARM metadata, reject incoherent configurations, and leave the embedded-library boundary unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/72-jarm-encryption-and-metadata/72-01-SUMMARY.md`
</output>
