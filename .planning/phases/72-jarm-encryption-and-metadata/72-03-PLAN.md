---
phase: 72-jarm-encryption-and-metadata
plan: 03
type: execute
wave: 3
depends_on:
  - 72-01
  - 72-02
files_modified:
  - lib/lockspire/protocol/discovery.ex
  - lib/lockspire/protocol/discovery/authorization_response_capabilities.ex
  - test/lockspire/protocol/discovery_test.exs
  - test/lockspire/web/discovery_controller_test.exs
autonomous: true
requirements:
  - JARM-03
must_haves:
  truths:
    - "Discovery publishes one coherent authorization-response capability story for signing and encryption."
    - "JARM encryption metadata is advertised only when the mounted authorization surface and issuer crypto posture can actually support it."
    - "Discovery metadata does not depend on transient client registrations, remote JWKS reachability, or momentary fetch health."
  artifacts:
    - path: "lib/lockspire/protocol/discovery/authorization_response_capabilities.ex"
      provides: "Shared capability source for authorization response modes and algorithm metadata"
    - path: "lib/lockspire/protocol/discovery.ex"
      provides: "Truthful publication of signing and encryption metadata"
    - path: "test/lockspire/protocol/discovery_test.exs"
      provides: "Verification that mounted-surface truth and allow-lists stay coupled"
  key_links:
    - from: "lib/lockspire/protocol/discovery.ex"
      to: "lib/lockspire/protocol/discovery/authorization_response_capabilities.ex"
      via: "shared runtime capability helper"
      pattern: "authorization_response"
---

<objective>
Publish truthful JARM signing and encryption discovery metadata from one shared authorization-response capability source.

Purpose: Keep runtime behavior and advertised metadata coupled so Lockspire does not overclaim support or split JARM truth across multiple helpers.
Output: Shared discovery capability helper and verification coverage for mounted, truthful JARM metadata.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/72-jarm-encryption-and-metadata/72-CONTEXT.md
@.planning/phases/72-jarm-encryption-and-metadata/72-RESEARCH.md
@.planning/phases/59-registration-policy-metadata-truth/59-CONTEXT.md
@.planning/phases/71-jarm-core/71-02-SUMMARY.md
@lib/lockspire/protocol/discovery.ex
@test/lockspire/protocol/discovery_test.exs

<interfaces>
From lib/lockspire/protocol/discovery.ex:
```elixir
@spec openid_configuration() :: map()
defp mounted_endpoint_metadata()
defp authorization_signing_alg_values_supported()
```

From test/lockspire/protocol/discovery_test.exs:
```elixir
assert config["token_endpoint_auth_signing_alg_values_supported"] == [...]
refute Map.has_key?(config, "revocation_endpoint_auth_methods_supported")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract one shared authorization-response capability source per D-10 through D-13</name>
  <files>lib/lockspire/protocol/discovery/authorization_response_capabilities.ex, lib/lockspire/protocol/discovery.ex, test/lockspire/protocol/discovery_test.exs, test/lockspire/web/discovery_controller_test.exs</files>
  <behavior>
    - Test 1: signing and encryption metadata are both derived from the same helper when the authorization endpoint is mounted.
    - Test 2: the helper publishes the narrow encryption allow-list (`RSA-OAEP-256`, `ECDH-ES`, `A256GCM`, `A128GCM`) and the effective signing algorithms from the current issuer security profile.
    - Test 3: no JARM signing or encryption metadata is published when the mounted authorization-response surface cannot truthfully support it.
    - Test 4: the published `/.well-known/openid-configuration` endpoint exposes the same signing and encryption metadata contract as the protocol helper.
  </behavior>
  <action>Create a small shared capability helper for authorization responses and have `Discovery` consume it for `response_modes_supported`, `authorization_signing_alg_values_supported`, `authorization_encryption_alg_values_supported`, and `authorization_encryption_enc_values_supported`. Keep the capability source runtime-truthful per D-10 through D-13: derive from the mounted authorization surface plus effective issuer crypto posture, not from client rows, `jwks_uri` availability, or fetch health. Verify both the protocol helper and the published discovery endpoint so the repo proves there is no helper-to-controller drift.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs</automated>
  </verify>
  <done>Discovery reads all JARM authorization-response metadata from one shared capability source and only advertises the narrow, truthful algorithm surface.</done>
</task>

<task type="auto">
  <name>Task 2: Close verification gaps around truthful metadata publication</name>
  <files>test/lockspire/protocol/discovery_test.exs, test/lockspire/web/discovery_controller_test.exs</files>
  <action>Expand the discovery test matrix so Phase 72 proves the coupled publication rules: mounted authorization surface present vs absent, default vs FAPI signing posture, and publication of encryption metadata without any dependency on transient registered clients. Cover both the helper and the HTTP endpoint contract. Keep the assertions recommendation-heavy and explicit; the point is to pin truth, not to create feature-marketing snapshots.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs</automated>
  </verify>
  <done>The test suite proves that JARM response signing and encryption metadata stay coupled, truthful, and stable across runtime posture changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime capability -> discovery document | Metadata drives relying-party behavior and must match real server capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-72-06 | Spoofing | `discovery.ex` metadata publication | mitigate | Publish only mounted, effective authorization-response capability rather than compile-time feature marketing per D-10 through D-13. |
| T-72-07 | Information disclosure | discovery encryption metadata | accept | Advertising stable issuer-wide supported algorithms is intentional and does not reveal per-client or transient operational state per D-11 and D-12. |
</threat_model>

<verification>
Run the discovery protocol and controller tests and inspect the resulting metadata shape for mounted vs unmounted authorization surfaces and default vs FAPI signing posture.
</verification>

<success_criteria>
`/.well-known/openid-configuration` publishes JARM signing and encryption metadata from one shared, truthful capability source with the exact narrow algorithm surface planned for Phase 72.
</success_criteria>

<output>
After completion, create `.planning/phases/72-jarm-encryption-and-metadata/72-03-SUMMARY.md`
</output>
