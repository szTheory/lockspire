---
phase: "32"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/domain/device_authorization.ex
  - lib/lockspire/storage/device_authorization_store.ex
  - lib/lockspire/storage/ecto/device_authorization_record.ex
  - priv/repo/migrations/20260428130000_extend_lockspire_device_authorizations_polling_state.exs
  - lib/lockspire/storage/ecto/repository.ex
  - test/lockspire/storage/ecto/repository_device_authorization_test.exs
autonomous: true
requirements:
  - DEV-09
must_haves:
  truths:
    - "Polling interval enforcement is durable and node-safe rather than process-local"
    - "A too-early poll atomically widens the next allowed interval and returns enough internal state to map to slow_down"
    - "An approved device authorization can only be consumed once under row lock"
  artifacts:
    - path: "lib/lockspire/storage/ecto/device_authorization_record.ex"
      provides: "Durable polling-state fields for device authorizations"
      contains: "next_poll_allowed_at"
    - path: "priv/repo/migrations/20260428130000_extend_lockspire_device_authorizations_polling_state.exs"
      provides: "Schema extension for durable poll interval and consume semantics"
      contains: "alter table(:lockspire_device_authorizations)"
    - path: "lib/lockspire/storage/ecto/repository.ex"
      provides: "Row-locked poll evaluation and consume callbacks"
      contains: "def consume_device_authorization"
    - path: "test/lockspire/storage/ecto/repository_device_authorization_test.exs"
      provides: "Executable proof for too-early polling, sticky interval growth, and single-winner consume"
      contains: "slow_down"
  key_links:
    - from: "lib/lockspire/storage/device_authorization_store.ex"
      to: "lib/lockspire/storage/ecto/repository.ex"
      via: "device-code fetch and poll/consume callbacks"
      pattern: "fetch_device_authorization_by_device_code_hash"
    - from: "lib/lockspire/storage/ecto/repository.ex"
      to: "lib/lockspire/storage/ecto/device_authorization_record.ex"
      via: "FOR UPDATE poll evaluation and consume mapping"
      pattern: "lock\\(\"FOR UPDATE\"\\)"
---

<objective>
Add the durable polling-state and single-winner repository foundation for RFC 8628 backpressure and token redemption.

Purpose: Phase 32 cannot implement `authorization_pending`, `slow_down`, or safe success redemption unless poll timing truth and consume semantics live in Postgres/Ecto, not in controller memory.
Output: extended device-authorization state, new store/repository callbacks, and executable repository proof for early-poll backpressure and atomic consume.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-polling-token-issuance/32-CONTEXT.md
@.planning/phases/32-polling-token-issuance/32-RESEARCH.md
@.planning/phases/32-polling-token-issuance/32-PATTERNS.md
@.planning/phases/31-host-owned-verification-ui-seam/31-VERIFICATION.md
@.planning/phases/31-host-owned-verification-ui-seam/31-04-SUMMARY.md

<interfaces>
From `lib/lockspire/domain/device_authorization.ex`:
```elixir
@type status :: :pending | :approved | :denied | :consumed | :expired
defstruct [:verification_handle, :status, :subject_id, :approved_at, :denied_at, :consumed_at, :expires_at]
```

From `lib/lockspire/storage/device_authorization_store.ex`:
```elixir
@callback fetch_device_authorization_by_user_code_hash(String.t()) ::
            {:ok, DeviceAuthorization.t() | nil} | {:error, term()}

@callback transition_device_authorization(String.t(), [DeviceAuthorization.status()], map()) ::
            {:ok, DeviceAuthorization.t()} | {:error, term()}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend durable polling-state fields and storage callbacks</name>
  <files>lib/lockspire/domain/device_authorization.ex, lib/lockspire/storage/device_authorization_store.ex, lib/lockspire/storage/ecto/device_authorization_record.ex, priv/repo/migrations/20260428130000_extend_lockspire_device_authorizations_polling_state.exs</files>
  <read_first>
    - lib/lockspire/domain/device_authorization.ex
    - lib/lockspire/storage/device_authorization_store.ex
    - lib/lockspire/storage/ecto/device_authorization_record.ex
    - priv/repo/migrations/20260428090000_extend_lockspire_device_authorizations_verification_state.exs
    - .planning/phases/32-polling-token-issuance/32-CONTEXT.md
    - .planning/phases/32-polling-token-issuance/32-RESEARCH.md
    - .planning/phases/32-polling-token-issuance/32-PATTERNS.md
  </read_first>
  <action>
    Extend `Lockspire.Domain.DeviceAuthorization` and the Ecto schema with durable poll-state needed by D-04 through D-09 and D-21 through D-23.

    Add explicit fields for:
    - `effective_poll_interval_seconds` with a default of `5`
    - `next_poll_allowed_at`

    Initialize these at issuance time so newly created device authorizations start with a 5-second poll interval and a first allowed poll timestamp derived from issuance time. Keep the exact computation in the domain or protocol helper explicit and deterministic.

    Add the additive migration `20260428130000_extend_lockspire_device_authorizations_polling_state.exs` to add both columns, backfill `effective_poll_interval_seconds` to `5` for existing rows, and index `next_poll_allowed_at` if helpful for operational introspection. Do not remove or weaken any Phase 31 fields.

    Widen `Lockspire.Storage.DeviceAuthorizationStore` with device-poll-specific callbacks:
    - `fetch_device_authorization_by_device_code_hash/1`
    - `record_device_poll/3` where the repository can return a typed outcome such as `:pending`, `:slow_down`, `:expired`, `:denied`, `:consumed`, or `:invalid_grant`
    - `consume_device_authorization/3` for the atomic winning redemption path from `:approved` to `:consumed`

    Keep these callbacks protocol-facing and storage-owned. Do not put OAuth public error strings into the store behaviour; keep it on typed atoms and timestamps.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/storage/ecto/repository_device_authorization_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'effective_poll_interval_seconds' lib/lockspire/domain/device_authorization.ex` succeeds.
    - `grep -q 'next_poll_allowed_at' lib/lockspire/domain/device_authorization.ex` succeeds.
    - `grep -q 'field(:effective_poll_interval_seconds' lib/lockspire/storage/ecto/device_authorization_record.ex` succeeds.
    - `grep -q 'field(:next_poll_allowed_at' lib/lockspire/storage/ecto/device_authorization_record.ex` succeeds.
    - `grep -q 'fetch_device_authorization_by_device_code_hash' lib/lockspire/storage/device_authorization_store.ex` succeeds.
    - `grep -q 'record_device_poll' lib/lockspire/storage/device_authorization_store.ex` succeeds.
    - `grep -q 'consume_device_authorization' lib/lockspire/storage/device_authorization_store.ex` succeeds.
    - `grep -q 'add :effective_poll_interval_seconds, :integer' priv/repo/migrations/20260428130000_extend_lockspire_device_authorizations_polling_state.exs` succeeds.
    - `grep -q 'add :next_poll_allowed_at, :utc_datetime_usec' priv/repo/migrations/20260428130000_extend_lockspire_device_authorizations_polling_state.exs` succeeds.
  </acceptance_criteria>
  <done>Device authorizations now have durable poll-state fields and a storage contract that can support RFC 8628 polling semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement row-locked poll evaluation and single-winner consume</name>
  <files>lib/lockspire/storage/ecto/repository.ex, test/lockspire/storage/ecto/repository_device_authorization_test.exs</files>
  <read_first>
    - lib/lockspire/storage/ecto/repository.ex
    - test/lockspire/storage/ecto/repository_device_authorization_test.exs
    - lib/lockspire/storage/ecto/device_authorization_record.ex
    - .planning/phases/32-polling-token-issuance/32-PATTERNS.md
    - .planning/phases/32-polling-token-issuance/32-VALIDATION.md
  </read_first>
  <behavior>
    - A poll before `next_poll_allowed_at` returns a typed slow-down outcome and durably increments `effective_poll_interval_seconds` by exactly `5`.
    - A compliant poll against `:pending` returns a typed pending outcome without widening the interval.
    - A poll against `:approved` remains redeemable only through the consume callback.
    - A consume attempt can win only once and moves the record from `:approved` to `:consumed` with `consumed_at` set.
  </behavior>
  <action>
    Implement the new callbacks in `Lockspire.Storage.Ecto.Repository` using `transact(fn -> ... lock("FOR UPDATE") ... end)` in the same style already used for interactions and Phase 31 device verification.

    Add `fetch_device_authorization_by_device_code_hash/1`.

    Add `record_device_poll/3` that:
    - loads the device authorization by hashed `device_code`
    - locks the row with `FOR UPDATE`
    - rejects unknown rows with a typed nil/not-found outcome
    - rejects client mismatches with a typed mismatch outcome
    - returns a typed expired/denied/consumed outcome for terminal rows
    - when status is `:pending` and `now < next_poll_allowed_at`, increments `effective_poll_interval_seconds` by `5`, advances `next_poll_allowed_at`, updates `updated_at`, and returns a typed slow-down outcome containing the new interval
    - when status is `:pending` and `now >= next_poll_allowed_at`, returns a typed pending outcome without widening the interval
    - when status is `:approved`, returns a typed approved-ready outcome without consuming

    Add `consume_device_authorization/3` that:
    - locks the approved row by `verification_handle` or record id
    - only accepts `:approved`
    - writes `status: :consumed` and `consumed_at: now`
    - returns `:invalid_state` on repeat or stale consume attempts

    Extend `test/lockspire/storage/ecto/repository_device_authorization_test.exs` with concrete cases for early polling, sticky interval growth, compliant pending polling, client mismatch, terminal state classification, and single-winner consume/replay behavior.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/storage/ecto/repository_device_authorization_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def fetch_device_authorization_by_device_code_hash' lib/lockspire/storage/ecto/repository.ex` succeeds.
    - `grep -q 'def record_device_poll' lib/lockspire/storage/ecto/repository.ex` succeeds.
    - `grep -q 'def consume_device_authorization' lib/lockspire/storage/ecto/repository.ex` succeeds.
    - `grep -q 'lock(\"FOR UPDATE\")' lib/lockspire/storage/ecto/repository.ex` succeeds.
    - `grep -q 'slow_down' test/lockspire/storage/ecto/repository_device_authorization_test.exs` succeeds.
    - `grep -q 'effective_poll_interval_seconds' test/lockspire/storage/ecto/repository_device_authorization_test.exs` succeeds.
    - `grep -q 'consumed_at' test/lockspire/storage/ecto/repository_device_authorization_test.exs` succeeds.
    - `mix test test/lockspire/storage/ecto/repository_device_authorization_test.exs` passes.
  </acceptance_criteria>
  <done>Repository polling semantics are now durable, sticky, and replay-safe for Phase 32 token exchange.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Token protocol -> repository | Polling and consume operations cross into durable state that must resist concurrency and replay. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01 | Denial of Service | `record_device_poll/3` | mitigate | Persist `effective_poll_interval_seconds` and `next_poll_allowed_at`, then widen the next window by exactly 5 seconds on each early poll under row lock. |
| T-32-02 | Tampering | `consume_device_authorization/3` | mitigate | Require expected-state `:approved` and lock the row `FOR UPDATE` so only one poll can win redemption. |
| T-32-03 | Spoofing | device-code lookup path | mitigate | Keep lookup keyed by hashed `device_code` plus client-binding validation rather than raw unscoped lookups. |
</threat_model>

<verification>
MIX_ENV=test mix test test/lockspire/storage/ecto/repository_device_authorization_test.exs
</verification>

<success_criteria>
Lockspire has durable Postgres-backed truth for device polling intervals and single-winner consume, so later protocol work can map repository outcomes into RFC 8628 token responses safely.
</success_criteria>

<output>
After completion, create `.planning/phases/32-polling-token-issuance/32-01-SUMMARY.md`
</output>
