---
phase: "32"
plan: "02"
type: execute
wave: 2
depends_on:
  - 32-01
files_modified:
  - lib/lockspire/protocol/token_exchange.ex
  - test/lockspire/protocol/token_exchange_test.exs
autonomous: true
requirements:
  - DEV-07
  - DEV-08
  - DEV-09
must_haves:
  truths:
    - "The existing token endpoint accepts the device grant type through TokenExchange"
    - "Pending, slow_down, denied, expired, mismatch, replay, and success cases map to RFC-shaped token outcomes"
    - "Approved openid device requests can reuse normal token success logic, including optional id_token"
  artifacts:
    - path: "lib/lockspire/protocol/token_exchange.ex"
      provides: "Device grant branch wired into shared token issuance"
      contains: "urn:ietf:params:oauth:grant-type:device_code"
    - path: "test/lockspire/protocol/token_exchange_test.exs"
      provides: "Executable protocol proof for pending, slow_down, terminal mappings, and success redemption"
      contains: "authorization_pending"
  key_links:
    - from: "lib/lockspire/protocol/token_exchange.ex"
      to: "lib/lockspire/storage/device_authorization_store.ex"
      via: "device poll evaluation and consume callbacks"
      pattern: "record_device_poll"
    - from: "lib/lockspire/protocol/token_exchange.ex"
      to: "lib/lockspire/web/controllers/token_json.ex"
      via: "shared success and error response contract"
      pattern: "TokenJSON"
---

<objective>
Add the device-code grant branch inside `Lockspire.Protocol.TokenExchange` and map durable repository outcomes into standard OAuth/device-flow token responses.

Purpose: Lockspire should treat device flow as another first-class route into the existing token issuance pipeline, not as a special controller or a second response family.
Output: device-grant routing in `TokenExchange`, shared issuance reuse, and protocol tests for pending, slow_down, terminal errors, success, replay, refresh-token policy, and optional `id_token`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-polling-token-issuance/32-CONTEXT.md
@.planning/phases/32-polling-token-issuance/32-RESEARCH.md
@.planning/phases/32-polling-token-issuance/32-PATTERNS.md
@.planning/phases/32-polling-token-issuance/32-01-PLAN.md
@.planning/phases/31-host-owned-verification-ui-seam/31-04-SUMMARY.md

<interfaces>
From `lib/lockspire/protocol/token_exchange.ex`:
```elixir
@spec exchange(map()) :: result()
def exchange(request) when is_map(request)
```

From `lib/lockspire/web/controllers/token_json.ex`:
```elixir
def access_token_response(%Success{} = success) :: map()
def error_response(%Error{} = error) :: map()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route the device grant into TokenExchange and preserve shared issuance</name>
  <files>lib/lockspire/protocol/token_exchange.ex, test/lockspire/protocol/token_exchange_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/token_exchange.ex
    - test/lockspire/protocol/token_exchange_test.exs
    - lib/lockspire/storage/device_authorization_store.ex
    - .planning/phases/32-polling-token-issuance/32-RESEARCH.md
    - .planning/phases/32-polling-token-issuance/32-PATTERNS.md
    - .planning/phases/32-polling-token-issuance/32-VALIDATION.md
  </read_first>
  <behavior>
    - `exchange/1` recognizes `grant_type=urn:ietf:params:oauth:grant-type:device_code`.
    - Client authentication reuses the current token-endpoint path and keeps `invalid_client` behavior unchanged.
    - Successful device-grant redemption returns the same `Success` struct used by auth-code and refresh-token exchanges.
  </behavior>
  <action>
    Extend `Lockspire.Protocol.TokenExchange.exchange/1` and related helpers to support the exact device grant string `urn:ietf:params:oauth:grant-type:device_code`.

    Keep the controller surface unchanged; all new branching belongs inside `TokenExchange`.

    Add a device-specific validation/redeem path that:
    - authenticates the client through the same `authenticate_client/3` path already used for other token grants
    - reads `device_code` from params and hashes it before repository lookup
    - calls the new repository/store poll-evaluation callback from Plan 32-01
    - maps typed outcomes into OAuth/device-flow public errors exactly as locked in Phase 32 context:
      - pending -> `authorization_pending`
      - too early -> `slow_down`
      - denied -> `access_denied`
      - expired -> `expired_token`
      - mismatch / consumed / unknown / replay -> `invalid_grant`

    Reuse the existing token persistence, refresh-token policy, token formatter, observability, and optional `id_token` issuance paths rather than cloning them. If the current auth-code implementation needs a small shared helper extraction for success assembly or persistence, do that extraction in place and keep the public contract unchanged.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/protocol/token_exchange_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'urn:ietf:params:oauth:grant-type:device_code' lib/lockspire/protocol/token_exchange.ex` succeeds.
    - `grep -q 'authorization_pending' test/lockspire/protocol/token_exchange_test.exs` succeeds.
    - `grep -q 'slow_down' test/lockspire/protocol/token_exchange_test.exs` succeeds.
    - `grep -q 'expired_token' test/lockspire/protocol/token_exchange_test.exs` succeeds.
    - `grep -q 'access_denied' test/lockspire/protocol/token_exchange_test.exs` succeeds.
    - `mix test test/lockspire/protocol/token_exchange_test.exs` passes.
  </acceptance_criteria>
  <done>TokenExchange now recognizes the device grant and routes it through shared Lockspire token issuance machinery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove terminal mappings, replay safety, refresh policy, and optional id_token behavior</name>
  <files>lib/lockspire/protocol/token_exchange.ex, test/lockspire/protocol/token_exchange_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/token_exchange.ex
    - test/lockspire/protocol/token_exchange_test.exs
    - .planning/phases/31-host-owned-verification-ui-seam/31-04-SUMMARY.md
    - .planning/phases/32-polling-token-issuance/32-CONTEXT.md
  </read_first>
  <behavior>
    - A successful device grant can issue `refresh_token` only when existing policy allows it.
    - A successful approved `openid` request can include `id_token` through the same shared issuance rules as auth-code flow.
    - A stale repeat poll after winning redemption becomes `invalid_grant` rather than a second success.
  </behavior>
  <action>
    Extend `test/lockspire/protocol/token_exchange_test.exs` with concrete repository-backed device-flow cases covering:
    - approved pending authorization becomes a successful token response exactly once
    - a second poll after success returns `invalid_grant`
    - approved requests with `openid` can receive `id_token`
    - refresh-token issuance remains governed by existing client/scope policy and does not become implicit just because the grant is device flow
    - device authorizations approved for one client cannot be redeemed by another client

    In `TokenExchange`, preserve private reason codes for telemetry/audit even when the public error collapses to `invalid_grant`. Keep the device-flow path aligned with the current error struct and emission pattern.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/protocol/token_exchange_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'invalid_grant' test/lockspire/protocol/token_exchange_test.exs` succeeds.
    - `grep -q 'id_token' test/lockspire/protocol/token_exchange_test.exs` succeeds.
    - `grep -q 'refresh_token' test/lockspire/protocol/token_exchange_test.exs` succeeds.
    - `grep -q 'reason_code' lib/lockspire/protocol/token_exchange.ex` succeeds.
    - `mix test test/lockspire/protocol/token_exchange_test.exs` passes.
  </acceptance_criteria>
  <done>The device grant path now has executable proof for all major RFC-shaped outcomes, replay safety, and OIDC-consistent success behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/token` protocol entry -> repository | Public polling input is translated into durable state reads and consume attempts. |
| Shared issuance path -> token persistence/signing | Successful device redemptions enter the same durable token and signing-key machinery as other grants. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-04 | Information Disclosure | public device-flow error mapping | mitigate | Collapse unknown, mismatch, consumed, and replay states to `invalid_grant` publicly while retaining private reason codes. |
| T-32-05 | Tampering | device success redemption | mitigate | Redeem only through the single-winner consume path from Plan 32-01, then reuse shared issuance logic once. |
| T-32-06 | Elevation of Privilege | device flow OIDC handling | mitigate | Preserve existing subject/claims/signing-key checks so approved device requests cannot bypass normal `id_token` issuance rules. |
</threat_model>

<verification>
MIX_ENV=test mix test test/lockspire/protocol/token_exchange_test.exs
</verification>

<success_criteria>
Device polling behaves like a first-class token grant inside Lockspire's existing token pipeline, with RFC-shaped continuation/terminal errors and normal OAuth/OIDC success semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/32-polling-token-issuance/32-02-SUMMARY.md`
</output>
