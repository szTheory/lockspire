---
phase: "32"
plan: "03"
type: execute
wave: 3
depends_on:
  - 32-01
  - 32-02
files_modified:
  - lib/lockspire/web/controllers/token_controller.ex
  - lib/lockspire/web/controllers/token_json.ex
  - test/lockspire/web/token_controller_test.exs
  - lib/lockspire/protocol/discovery.ex
  - test/lockspire/protocol/discovery_test.exs
  - test/lockspire/web/discovery_controller_test.exs
  - docs/device-flow-host-guide.md
  - docs/install-and-onboard.md
  - docs/supported-surface.md
  - test/lockspire/release_readiness_contract_test.exs
  - test/integration/phase32_device_flow_token_exchange_e2e_test.exs
autonomous: true
requirements:
  - DEV-07
  - DEV-08
must_haves:
  truths:
    - "The HTTP token endpoint returns RFC-shaped device-flow responses with the existing cache-header posture"
    - "Discovery truth advertises the shipped device-flow token support and does not over-claim beyond repo reality"
    - "Docs and E2E proof teach polling, slow_down backoff, and successful token redemption coherently"
  artifacts:
    - path: "test/lockspire/web/token_controller_test.exs"
      provides: "HTTP proof for device grant token responses"
      contains: "urn:ietf:params:oauth:grant-type:device_code"
    - path: "lib/lockspire/protocol/discovery.ex"
      provides: "Truthful grant metadata including device flow"
      contains: "device_code"
    - path: "test/integration/phase32_device_flow_token_exchange_e2e_test.exs"
      provides: "End-to-end proof from approval to token success and replay failure"
      contains: "/token"
  key_links:
    - from: "lib/lockspire/web/controllers/token_controller.ex"
      to: "lib/lockspire/protocol/token_exchange.ex"
      via: "thin controller wiring for device grant"
      pattern: "TokenExchange.exchange"
    - from: "lib/lockspire/protocol/discovery.ex"
      to: "test/lockspire/web/discovery_controller_test.exs"
      via: "truthful HTTP discovery publication"
      pattern: "grant_types_supported"
    - from: "docs/install-and-onboard.md"
      to: "docs/device-flow-host-guide.md"
      via: "host onboarding guidance for polling semantics"
      pattern: "device-flow-host-guide"
---

<objective>
Finish the public device-flow support contract at the HTTP, discovery, docs, and end-to-end proof layers.

Purpose: once protocol and storage support exist, Lockspire still needs truthful outward behavior: stable `/token` responses, discovery metadata, documentation, and executable end-to-end proof.
Output: HTTP and discovery tests, docs updates, release-readiness contract proof, and an end-to-end device-flow token exchange test.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-polling-token-issuance/32-CONTEXT.md
@.planning/phases/32-polling-token-issuance/32-RESEARCH.md
@.planning/phases/32-polling-token-issuance/32-PATTERNS.md
@.planning/phases/32-polling-token-issuance/32-02-PLAN.md
@docs/device-flow-host-guide.md
@docs/install-and-onboard.md
@docs/supported-surface.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the HTTP token-endpoint contract for device polling</name>
  <files>lib/lockspire/web/controllers/token_controller.ex, lib/lockspire/web/controllers/token_json.ex, test/lockspire/web/token_controller_test.exs</files>
  <read_first>
    - lib/lockspire/web/controllers/token_controller.ex
    - lib/lockspire/web/controllers/token_json.ex
    - test/lockspire/web/token_controller_test.exs
    - lib/lockspire/protocol/token_exchange.ex
    - .planning/phases/32-polling-token-issuance/32-VALIDATION.md
  </read_first>
  <behavior>
    - `POST /token` accepts the device grant and returns 200 success, `authorization_pending`, `slow_down`, `access_denied`, `expired_token`, or `invalid_grant` with the current cache-header posture.
    - `invalid_client` remains the only 401 case with the normal auth challenge behavior.
    - If Lockspire includes `interval` on `slow_down`, it is emitted consistently through `TokenJSON` rather than controller-only logic.
  </behavior>
  <action>
    Extend `test/lockspire/web/token_controller_test.exs` with repository-backed HTTP cases for device polling:
    - pending compliant poll returns status 400 with `error=authorization_pending`
    - too-early poll returns status 400 with `error=slow_down`
    - approved poll returns 200 with the standard token JSON shape
    - denied and expired polls return `access_denied` and `expired_token`
    - stale repeat poll after success returns `invalid_grant`

    Keep `TokenController` thin. Only touch controller wiring if the device grant path needs additional store injection. If the chosen implementation emits `interval` on `slow_down`, add that field in `TokenJSON.error_response/1` behind an explicit optional field on the `Error` struct so the behavior stays centralized and testable.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/web/token_controller_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'urn:ietf:params:oauth:grant-type:device_code' test/lockspire/web/token_controller_test.exs` succeeds.
    - `grep -q 'authorization_pending' test/lockspire/web/token_controller_test.exs` succeeds.
    - `grep -q 'slow_down' test/lockspire/web/token_controller_test.exs` succeeds.
    - `grep -q 'expired_token' test/lockspire/web/token_controller_test.exs` succeeds.
    - `mix test test/lockspire/web/token_controller_test.exs` passes.
  </acceptance_criteria>
  <done>The public `/token` HTTP contract now has executable proof for device-flow pending, backpressure, success, and terminal cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update discovery truth and support-contract docs</name>
  <files>lib/lockspire/protocol/discovery.ex, test/lockspire/protocol/discovery_test.exs, test/lockspire/web/discovery_controller_test.exs, docs/device-flow-host-guide.md, docs/install-and-onboard.md, docs/supported-surface.md, test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/discovery.ex
    - test/lockspire/protocol/discovery_test.exs
    - test/lockspire/web/discovery_controller_test.exs
    - docs/device-flow-host-guide.md
    - docs/install-and-onboard.md
    - docs/supported-surface.md
    - test/lockspire/release_readiness_contract_test.exs
    - .planning/phases/32-polling-token-issuance/32-CONTEXT.md
  </read_first>
  <behavior>
    - discovery metadata truthfully includes the shipped device-flow grant support and any now-supported device authorization endpoint metadata
    - onboarding and supported-surface docs describe device polling semantics without over-claiming extra product surface
    - release-readiness contract tests pin the updated docs truth
  </behavior>
  <action>
    Update `Lockspire.Protocol.Discovery` and its tests so `grant_types_supported` includes `urn:ietf:params:oauth:grant-type:device_code` once the token endpoint actually supports it. Also decide whether `device_authorization_endpoint` should now be published, since `/device/code` already exists in the repo; if you publish it, add matching protocol and HTTP discovery assertions.

    Update docs so the Phase 32 slice is truthful and calm:
    - `docs/device-flow-host-guide.md`: explain the happy path, the base 5-second poll interval, `slow_down` backoff, and when clients must stop polling on terminal errors
    - `docs/install-and-onboard.md`: point host teams to the shipped polling/token behavior and how it fits with the existing host-owned `/verify` seam
    - `docs/supported-surface.md`: widen support claims to include the shipped device-flow token path, but do not claim broader CIAM or hosted-auth features

    Update `test/lockspire/release_readiness_contract_test.exs` to pin the new doc truth.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'urn:ietf:params:oauth:grant-type:device_code' lib/lockspire/protocol/discovery.ex` succeeds.
    - `grep -q 'urn:ietf:params:oauth:grant-type:device_code' test/lockspire/protocol/discovery_test.exs` succeeds.
    - `grep -q 'urn:ietf:params:oauth:grant-type:device_code' test/lockspire/web/discovery_controller_test.exs` succeeds.
    - `grep -q 'slow_down' docs/device-flow-host-guide.md` succeeds.
    - `grep -q 'device-flow-host-guide' docs/install-and-onboard.md` succeeds.
    - `grep -q 'device flow' docs/supported-surface.md` succeeds.
    - `mix test test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs test/lockspire/release_readiness_contract_test.exs` passes.
  </acceptance_criteria>
  <done>Discovery metadata and docs now truthfully describe the shipped Phase 32 device-flow polling/token support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add end-to-end proof from host approval to token success and replay failure</name>
  <files>test/integration/phase32_device_flow_token_exchange_e2e_test.exs</files>
  <read_first>
    - test/integration/phase31_generated_host_verification_e2e_test.exs
    - test/lockspire/web/token_controller_test.exs
    - .planning/phases/32-polling-token-issuance/32-RESEARCH.md
  </read_first>
  <behavior>
    - A generated/host-approved device authorization can later be redeemed through `/token`
    - The same approved authorization cannot be redeemed successfully twice
  </behavior>
  <action>
    Create `test/integration/phase32_device_flow_token_exchange_e2e_test.exs` or extend the existing Phase 31 E2E pattern into a new focused Phase 32 file.

    The E2E test must:
    - create a device authorization
    - exercise the host-owned approval seam or equivalent approved-state setup
    - poll `/token` successfully once
    - assert the returned JSON is the canonical Lockspire token response shape
    - poll `/token` a second time with the same device code and assert `invalid_grant`

    Keep the test repo-backed and deterministic; do not substitute mocks for the consume path.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/integration/phase32_device_flow_token_exchange_e2e_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/integration/phase32_device_flow_token_exchange_e2e_test.exs` succeeds.
    - `grep -q '/token' test/integration/phase32_device_flow_token_exchange_e2e_test.exs` succeeds.
    - `grep -q 'invalid_grant' test/integration/phase32_device_flow_token_exchange_e2e_test.exs` succeeds.
    - `mix test test/integration/phase32_device_flow_token_exchange_e2e_test.exs` passes.
  </acceptance_criteria>
  <done>The shipped Phase 32 slice has end-to-end proof from approved device authorization to single-winner token redemption.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP `/token` surface -> token protocol | Public grant requests must not leak more state than intended. |
| Discovery/docs -> external integrators | Metadata and docs must match actual repo behavior to avoid unsupported-client drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-07 | Information Disclosure | HTTP/device-flow error responses | mitigate | Prove the public surface stays RFC-tight and does not leak richer internal state. |
| T-32-08 | Repudiation | docs/discovery truth | mitigate | Pin metadata and support claims with tests so public claims cannot silently drift from behavior. |
| T-32-09 | Tampering | end-to-end redemption path | mitigate | Add E2E proof that a second poll after success fails with `invalid_grant`. |
</threat_model>

<verification>
MIX_ENV=test mix test test/lockspire/web/token_controller_test.exs test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs test/lockspire/release_readiness_contract_test.exs test/integration/phase32_device_flow_token_exchange_e2e_test.exs
</verification>

<success_criteria>
Lockspire truthfully ships and proves the complete Phase 32 slice: HTTP token responses, discovery metadata, docs, and end-to-end device-flow redemption behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/32-polling-token-issuance/32-03-SUMMARY.md`
</output>
