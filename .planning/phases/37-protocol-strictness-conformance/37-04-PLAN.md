---
phase: 37-protocol-strictness-conformance
plan: 4
type: execute
wave: 3
depends_on:
  - 37-01
  - 37-02
  - 37-03
files_modified:
  - test/integration/phase37_protocol_strictness_e2e_test.exs
  - scripts/conformance/run_phase37_suite.sh
  - scripts/conformance/phase37-plan.json
  - docs/maintainer-conformance.md
  - docs/supported-surface.md
  - test/lockspire/release_readiness_contract_test.exs
  - mix.exs
  - .github/workflows/oidf-conformance.yml
autonomous: true
requirements:
  - CONF-04
must_haves:
  truths:
    - "The repo has one deterministic command path for Phase 37 conformance proof."
    - "Generated-host integration proof covers the strictness surface before the external suite runs."
    - "A repo-native Phase 37 OIDF run completes successfully and saves proof artifacts under `.artifacts/conformance/phase37`."
    - "The full OIDF lane is available on demand or schedule without becoming required on every PR."
    - "Supported-surface docs, mix aliases, and workflow wiring are mechanically checked so proof claims cannot drift ahead of executable evidence."
  artifacts:
    - path: test/integration/phase37_protocol_strictness_e2e_test.exs
      provides: "Repo-native strictness proof for prompt=none, max_age, auth_time, and exact redirect behavior."
    - path: scripts/conformance/run_phase37_suite.sh
      provides: "Docker-first local harness entrypoint with artifact export."
    - path: .artifacts/conformance/phase37
      provides: "Saved proof artifacts from a successful Phase 37 repo-native OIDF suite run."
    - path: .github/workflows/oidf-conformance.yml
      provides: "Maintainer-triggered or scheduled hosted-lane workflow outside normal PR CI."
    - path: test/lockspire/release_readiness_contract_test.exs
      provides: "Executable proof contract that keeps docs, mix aliases, and workflow wiring aligned with the Phase 37 conformance posture."
  key_links:
    - from: mix.exs
      to: scripts/conformance/run_phase37_suite.sh
      via: mix alias
      pattern: "conformance\\.phase37"
    - from: docs/supported-surface.md
      to: scripts/conformance/phase37-plan.json
      via: documented proof scope
      pattern: "prompt=none|max_age|auth_time"
    - from: test/lockspire/release_readiness_contract_test.exs
      to: docs/supported-surface.md
      via: release-readiness contract assertions
      pattern: "phase37|conformance"
---

<objective>
Add the repo-native and hosted conformance lanes that Phase 37 needs to prove its strictness claims without turning every pull request into a flaky browser-suite run.

Purpose: Implement D-13 through D-21 and satisfy CONF-04 with truthful scope, reproducible artifacts, and maintainable DX.
Output: A generated-host E2E proof, a Docker-first OIDF harness entrypoint, a required green repo-native OIDF run with saved artifacts, and a separate maintainer workflow for the fuller hosted lane.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-protocol-strictness-conformance/37-CONTEXT.md
@.planning/phases/37-protocol-strictness-conformance/37-RESEARCH.md
@.planning/phases/37-protocol-strictness-conformance/37-VALIDATION.md
@mix.exs
@.github/workflows/ci.yml
@docs/supported-surface.md
@test/lockspire/release_readiness_contract_test.exs
@test/integration/phase3_oidc_token_lifecycle_e2e_test.exs
@test/support/generated_host_app/lockspire/test_account_resolver.ex

<interfaces>
From mix.exs:
```elixir
defp aliases do
  [
    "test.fast": ["test.setup", "test"],
    "test.integration": ["test.setup", "test --only integration"],
    "test.phase3": [...],
    "ci": [...]
  ]
end
```

From test/support/generated_host_app/lockspire/test_account_resolver.ex:
```elixir
def resolve_current_account(%Plug.Conn{} = conn, context)
def redirect_for_login(_conn_or_socket, context)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repo-native strictness integration proof</name>
  <read_first>
.planning/phases/37-protocol-strictness-conformance/37-CONTEXT.md
.planning/phases/37-protocol-strictness-conformance/37-VALIDATION.md
test/integration/phase3_oidc_token_lifecycle_e2e_test.exs
test/support/generated_host_app/lockspire/test_account_resolver.ex
docs/supported-surface.md
  </read_first>
  <files>test/integration/phase37_protocol_strictness_e2e_test.exs, docs/supported-surface.md</files>
  <behavior>
    - Test 1: exact redirect_uri mismatches still fail on the browser-safe surface.
    - Test 2: prompt=none with no visible host session returns OIDC `login_required` without host login redirection.
    - Test 3: prompt=none plus stale auth_time under max_age returns `login_required`.
    - Test 4: successful OIDC code exchange after a fresh auth event emits integer auth_time in the ID token when max_age or explicit demand was present.
  </behavior>
  <action>Create `test/integration/phase37_protocol_strictness_e2e_test.exs` as the repo-native proof lane required by D-13, D-14, and D-18. Reuse the generated-host resolver and the Phase 3 integration style, but make the fixture intentionally narrow: deterministic client registration, exact redirect URIs, session-backed login visibility, and explicit auth_time setup for max_age cases. Update `docs/supported-surface.md` so the public support statement names only the Phase 37 strictness slice that this integration test and the OIDF harness actually prove, because the release-readiness contract in Task 3 will lock those claims to executable proof.</action>
  <acceptance_criteria>
    - `test -f test/integration/phase37_protocol_strictness_e2e_test.exs` succeeds.
    - `rg -n 'prompt=none|max_age|auth_time|login_required' test/integration/phase37_protocol_strictness_e2e_test.exs` shows the strictness proof cases.
    - `rg -n 'prompt=none|max_age|auth_time' docs/supported-surface.md` documents only the proved slice.
  </acceptance_criteria>
  <verify>
    <automated>mix test --include integration test/integration/phase37_protocol_strictness_e2e_test.exs</automated>
  </verify>
  <done>The repo contains a deterministic generated-host E2E test that proves the public strictness claims before any external conformance suite run.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the two-lane OIDF conformance harness</name>
  <read_first>
.planning/phases/37-protocol-strictness-conformance/37-CONTEXT.md
.planning/phases/37-protocol-strictness-conformance/37-RESEARCH.md
mix.exs
.github/workflows/ci.yml
docs/supported-surface.md
test/lockspire/release_readiness_contract_test.exs
  </read_first>
  <files>scripts/conformance/run_phase37_suite.sh, scripts/conformance/phase37-plan.json, docs/maintainer-conformance.md, docs/supported-surface.md, mix.exs, .github/workflows/oidf-conformance.yml, test/lockspire/release_readiness_contract_test.exs</files>
  <behavior>
    - Test 1: `mix conformance.phase37` runs the Phase 37 generated-host integration proof before it starts the external OIDF suite.
    - Test 2: the repo-native lane is exposed through a non-default-PR workflow entrypoint.
    - Test 3: the release-readiness contract locks docs, alias wiring, workflow triggers, and artifact path references to executable proof.
  </behavior>
  <action>Implement D-15, D-16, D-17, and D-21 by creating `scripts/conformance/run_phase37_suite.sh` as the one obvious Docker-first local entrypoint, plus `scripts/conformance/phase37-plan.json` with the exact OP modules relevant to prompt=none, max_age, auth_time, redirect validation, and related strict authorization behavior. Add a `mix conformance.phase37` alias in `mix.exs` that first runs `mix test --include integration test/integration/phase37_protocol_strictness_e2e_test.exs`, then runs the script, boots the deterministic fixture, executes the actual OIDF suite lane, and exports artifacts into a stable `.artifacts/conformance/phase37` directory. Document prerequisites, cookie limitations, required env vars, ordering, and artifact locations in `docs/maintainer-conformance.md`. Create `.github/workflows/oidf-conformance.yml` as a manual-or-scheduled workflow that contains two non-default-PR jobs: the primary repo-native Docker-first lane that runs `MIX_ENV=test mix conformance.phase37`, and the separate hosted or staging lane for broader maintainer verification. Extend `test/lockspire/release_readiness_contract_test.exs` so docs, alias wiring, artifact path references, and workflow triggers are mechanically checked together. This task is about wiring and contract enforcement only; the actual green repo-native conformance run happens in the next task.</action>
  <acceptance_criteria>
    - `test -f scripts/conformance/run_phase37_suite.sh` and `test -f scripts/conformance/phase37-plan.json` both succeed.
    - `rg -n 'conformance\\.phase37|phase37_protocol_strictness_e2e_test' mix.exs` finds the new mix alias and the explicit pre-suite integration proof.
    - `rg -n 'workflow_dispatch|schedule|conformance\\.phase37' .github/workflows/oidf-conformance.yml` confirms the workflow exposes the repo-native lane through a non-default-PR automation entrypoint.
    - `rg -n 'phase37_protocol_strictness_e2e_test|third-party cookie|browser cookie|artifact|LOCKSPIRE_|OIDF_' docs/maintainer-conformance.md` documents the ordering, operational caveats, required inputs, and outputs.
    - `rg -n 'conformance\\.phase37|oidf-conformance|phase37_protocol_strictness_e2e_test|maintainer-conformance' test/lockspire/release_readiness_contract_test.exs` shows contract coverage for the new lane.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/conformance/run_phase37_suite.sh && python3 -m json.tool scripts/conformance/phase37-plan.json >/dev/null && MIX_ENV=test mix run -e 'aliases = Mix.Project.config()[:aliases]; unless Keyword.has_key?(aliases, :"conformance.phase37"), do: raise("missing conformance.phase37 alias")' && MIX_ENV=test mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <done>The repo exposes one deterministic local conformance command path, wires the generated-host Phase 37 E2E proof ahead of the external suite, and publishes a non-default-PR workflow plus contract test that keep the primary Docker-first lane distinct from the broader hosted maintainer lane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Run the repo-native OIDF lane and capture proof artifacts</name>
  <read_first>
.planning/phases/37-protocol-strictness-conformance/37-VALIDATION.md
scripts/conformance/run_phase37_suite.sh
scripts/conformance/phase37-plan.json
docs/maintainer-conformance.md
mix.exs
.github/workflows/oidf-conformance.yml
  </read_first>
  <files>scripts/conformance/run_phase37_suite.sh, scripts/conformance/phase37-plan.json, docs/maintainer-conformance.md, .artifacts/conformance/phase37</files>
  <behavior>
    - Test 1: the deterministic local command `MIX_ENV=test mix conformance.phase37` completes successfully against the generated-host fixture.
    - Test 2: the run exports proof artifacts into `.artifacts/conformance/phase37` exactly as documented.
    - Test 3: this task acts as the final phase gate after the faster preflight and contract checks from Task 2.
  </behavior>
  <action>Close CONF-04 by running the actual repo-native OIDF lane after Tasks 1 and 2 are complete. This is the final phase gate, not the primary fast-feedback loop. Use the checked-in `mix conformance.phase37` alias as the single command path, require it to execute the Phase 37 E2E proof and OIDF suite successfully, and save the resulting proof bundle under `.artifacts/conformance/phase37` in the shape documented by `docs/maintainer-conformance.md`. This task is execution-only: it may update the harness script, plan JSON, or maintainer docs only if needed to make the already-planned lane run green and export artifacts exactly as documented.</action>
  <acceptance_criteria>
    - `test -d .artifacts/conformance/phase37` succeeds after the task completes.
    - `find .artifacts/conformance/phase37 -type f | head` returns at least one saved artifact file from the suite run.
    - `rg -n '\\.artifacts/conformance/phase37' docs/maintainer-conformance.md scripts/conformance/run_phase37_suite.sh` shows the artifact path is consistent between docs and harness.
    - The task summary records that `MIX_ENV=test mix conformance.phase37` exited successfully.
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix conformance.phase37</automated>
  </verify>
  <done>Phase 37 has an actual successful repo-native OIDF suite run, and its proof artifacts are saved in the documented location instead of being left implicit as future follow-up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo test harness -> external OIDF suite | Local Lockspire fixtures must match the suite’s expected callback and browser behavior |
| maintainer workflow -> hosted conformance lane | External networked execution should not become implicit contributor CI |
| public docs -> actual proof artifacts | Support claims must not outrun what the repo can re-run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-11 | Repudiation | conformance artifacts | mitigate | Export harness artifacts to a stable path and document the exact command that generated them. |
| T-37-12 | Denial of Service | pull-request CI | mitigate | Keep the full OIDF suite in a separate manual/scheduled workflow instead of the default PR pipeline. |
| T-37-13 | Information Disclosure | docs and support claims | mitigate | Update `docs/supported-surface.md` only for behaviors backed by the new integration proof and checked-in harness. |
| T-37-14 | Tampering | docs / workflow proof contract | mitigate | Add `test/lockspire/release_readiness_contract_test.exs` assertions tying docs, alias wiring, and workflow triggers to the real Phase 37 conformance lane. |
</threat_model>

<verification>
- `mix test --include integration test/integration/phase37_protocol_strictness_e2e_test.exs`
- Phase-level CONF-04 gate: `MIX_ENV=test mix conformance.phase37`
- `MIX_ENV=test mix test test/lockspire/release_readiness_contract_test.exs`
</verification>

<success_criteria>
- The repo has a deterministic Phase 37 conformance command path.
- The repo-native Phase 37 OIDF lane runs successfully and saves artifacts under `.artifacts/conformance/phase37`.
- The hosted OIDF lane is wired separately from normal PR CI.
- Supported-surface docs describe only the strictness slice the repo can actually prove.
- Proof-facing docs, alias wiring, and workflow triggers are mechanically checked by the release-readiness contract test.
</success_criteria>

<output>
After completion, create `.planning/phases/37-protocol-strictness-conformance/37-04-SUMMARY.md`
</output>
