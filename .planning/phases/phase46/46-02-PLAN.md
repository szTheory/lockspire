---
phase: 46
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [lib/lockspire.ex, lib/lockspire/admin.ex, lib/lockspire/config.ex]
autonomous: true
requirements: [STAB-02]
must_haves:
  truths:
    - "Lockspire, Lockspire.Admin, and Lockspire.Config have full @doc coverage"
    - "ExDoc generated documentation has 0 warnings"
    - "Hidden internal types do not cause missing reference warnings"
  artifacts:
    - path: "lib/lockspire.ex"
      provides: "Public API documentation"
      contains: "@doc"
    - path: "lib/lockspire/admin.ex"
      provides: "Admin API documentation"
      contains: "@doc"
    - path: "lib/lockspire/config.ex"
      provides: "Config API documentation"
      contains: "@doc"
---

<objective>
Finalize public documentation by adding `@doc` blocks to all public functions in core facades and resolving ExDoc compilation warnings related to hidden types.

Purpose: Provide comprehensive, warning-free API documentation for the 1.0 release.
Output: Fully documented public facade modules and a clean `mix docs` run.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/lockspire.ex
@lib/lockspire/admin.ex
@lib/lockspire/config.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Lockspire and Config modules</name>
  <files>lib/lockspire.ex, lib/lockspire/config.ex</files>
  <action>Add comprehensive `@doc` blocks to all public functions lacking them. For `lib/lockspire.ex`, ensure `config/0`, `issuer/0`, `logout_path/0`, and `account_resolver!/0` are documented. For `lib/lockspire/config.ex`, ensure helpers like `repo!/0`, `issuer!/0`, and `oban_config/0` are fully documented.</action>
  <verify>
    <automated>mix docs</automated>
  </verify>
  <done>All public functions in Lockspire and Lockspire.Config have @doc annotations</done>
</task>

<task type="auto">
  <name>Task 2: Document Admin module</name>
  <files>lib/lockspire/admin.ex</files>
  <action>Add comprehensive `@doc` blocks directly above all `defdelegate` definitions in `lib/lockspire/admin.ex` so they are picked up by ExDoc. Explain what each admin operation does.</action>
  <verify>
    <automated>mix docs</automated>
  </verify>
  <done>Lockspire.Admin has @doc annotations for all delegated functions</done>
</task>

<task type="auto">
  <name>Task 3: Resolve ExDoc hidden type warnings</name>
  <files>lib/lockspire.ex, lib/lockspire/admin.ex</files>
  <action>Run `mix docs`. Identify any warnings related to referencing hidden `t()` types (e.g., `Lockspire.Protocol.DpopPolicy.Resolved.t()`). Change the public `@spec` definitions to refer to less specific public types (like `struct()` or `map()`) to stop referencing hidden `t()` types. Run until `mix docs --warnings-as-errors` passes cleanly. Modify the internal protocol modules if needed.</action>
  <verify>
    <automated>mix docs --warnings-as-errors</automated>
  </verify>
  <done>ExDoc compiles with zero warnings</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | No trust boundary crossed, purely structural code metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02 | Information Disclosure | @moduledoc | mitigate | Ensure no internal secrets or sensitive security implementations are leaked in the public docs. Keep docs focused on interface contracts. |
</threat_model>

<verification>
- `mix docs --warnings-as-errors` executes cleanly.
- `lib/lockspire/admin.ex` has `@doc` annotations above all `defdelegate` lines.
</verification>

<success_criteria>
Public API modules are comprehensively documented, and ExDoc builds successfully without warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/phase46/46-02-SUMMARY.md`
</output>
