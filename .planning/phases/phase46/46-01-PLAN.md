---
phase: 46
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [mix.exs, .github/workflows/ci.yml]
autonomous: true
requirements: [STAB-05]
must_haves:
  truths:
    - "mix deps.audit checks for CVE vulnerabilities using mix_audit"
    - "sobelow statically analyzes the codebase for security issues"
    - "CI pipeline runs both security audits during PR checks"
  artifacts:
    - path: "mix.exs"
      provides: "audit dependencies and task aliases"
      contains: ":mix_audit"
    - path: ".github/workflows/ci.yml"
      provides: "CI job execution"
      contains: "mix sobelow"
---

<objective>
Integrate and configure security auditing tooling (`mix_audit` and `sobelow`) into the project and CI pipeline, and fix any immediate vulnerabilities found.

Purpose: Guarantee 1.0 readiness by ensuring formal security and dependency checks run automatically.
Output: Configured security tools with a clean audit slate.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@mix.exs
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add and configure security dependencies</name>
  <files>mix.exs</files>
  <action>Add `{:mix_audit, "~> 2.1", only: [:dev, :test], runtime: false}` and `{:sobelow, "~> 0.13", only: [:dev, :test], runtime: false}` to the dependencies list in `mix.exs`. Update the `deps.audit` alias to use `mix_audit` so it actually checks for CVEs (e.g., `"deps.audit": ["deps.audit"]`). Add a new alias or update the `qa` alias to run `sobelow --config` or `sobelow` directly.</action>
  <verify>
    <automated>mix deps.get && mix deps.audit && mix sobelow</automated>
  </verify>
  <done>mix.exs includes the new dependencies and aliases, and they run cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Fix vulnerabilities and update CI workflow</name>
  <files>.github/workflows/ci.yml</files>
  <action>Update `.github/workflows/ci.yml` to ensure that `mix deps.audit` and `mix sobelow` are run as part of the "Fast Checks" or a dedicated security job. If the local run of `mix deps.audit` or `mix sobelow` identified any issues in the codebase, fix them in their respective files.</action>
  <verify>
    <automated>grep sobelow .github/workflows/ci.yml</automated>
  </verify>
  <done>CI pipeline includes the security tools and any existing vulnerabilities are patched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tooling | Security analysis tools scan project dependencies and code structure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Tampering | mix.exs | mitigate | Use locked versions for security tools, ensuring CI reproduces same checks. |
</threat_model>

<verification>
- `mix deps.audit` successfully checks for CVEs without error.
- `mix sobelow` passes cleanly.
- `ci.yml` contains commands for both tools.
</verification>

<success_criteria>
Security tooling is integrated, CI is gated on security checks, and the project is 1.0 ready from an automated security perspective.
</success_criteria>

<output>
After completion, create `.planning/phases/phase46/46-01-SUMMARY.md`
</output>
