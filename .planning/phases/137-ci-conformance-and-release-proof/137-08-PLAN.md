---
phase: 137-ci-conformance-and-release-proof
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/acceptance/clean_room/package_input.py
  - scripts/acceptance/clean_room_saas_journey.py
  - scripts/acceptance/run_clean_room_saas_journey.sh
  - test/lockspire/clean_room_release_source_contract_test.exs
autonomous: true
requirements: [REL-01]
must_haves:
  truths:
    - "The existing provider/client journey can consume the exact local Hex tar or an exact public Hex version."
    - "One selected package source is prepared once and copied into both isolated child roles with provenance verification."
    - "Install, migration, verification, boot, discovery, authorization-code plus PKCE, OIDC validation, userinfo, and protected-resource HTTP behavior run from that source."
  artifacts:
    - path: "scripts/acceptance/clean_room/package_input.py"
      provides: "Mutually exclusive checkout-build, local-tar, and exact-Hex source preparation"
    - path: "scripts/acceptance/clean_room_saas_journey.py"
      provides: "Single-source clean-room HTTP journey"
    - path: "test/lockspire/clean_room_release_source_contract_test.exs"
      provides: "Provenance, selection, and no-live-checkout contracts"
  key_links:
    - from: "CLI package source arguments"
      to: "provider and confidential-client vendor/lock roots"
      via: "one audited unpacked package tree copied below each child root"
---

<objective>
Generalize Phase 133's real SaaS journey at its package-input seam for pre- and post-publish proof.

Purpose: Reuse the strongest existing adoption evidence instead of creating a compile-only release harness.
Output: Exact local-tar and exact-public-version source modes with shared provenance.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/133-clean-room-saas-journey/133-01-SUMMARY.md
@.planning/phases/133-clean-room-saas-journey/133-02-SUMMARY.md
@.planning/phases/137-ci-conformance-and-release-proof/137-RESEARCH.md
@scripts/acceptance/clean_room/package_input.py
@scripts/acceptance/clean_room_saas_journey.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run the happy HTTP journey from an exact local Hex tar</name>
  <files>scripts/acceptance/clean_room/package_input.py, scripts/acceptance/clean_room_saas_journey.py, scripts/acceptance/run_clean_room_saas_journey.sh, test/lockspire/clean_room_release_source_contract_test.exs</files>
  <behavior>
    - `--package-tar` requires a regular in-scope `lockspire-X.Y.Z.tar`, validates its checksum/version, and unpacks it through supported Hex tooling.
    - The package is prepared once, inventoried, and copied to both child roles; neither resolves to the live checkout.
    - `--only happy_path` completes the existing separate-origin discovery/authorization/OIDC/userinfo/resource journey from that tar.
  </behavior>
  <action>Write argument/provenance contract cases first. Introduce a package-source value object or equivalent explicit functions in `package_input.py`; keep the existing checkout-build default for ordinary Phase 133 use. Validate the supplied tar before child creation and use supported Hex package inspection/unpack commands confirmed from local help rather than hand-parsing tar internals. Thread one prepared package root through provider and client preparation so the journey never rebuilds Lockspire twice.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/clean_room_release_source_contract_test.exs &amp;&amp; mix package.build &amp;&amp; bash scripts/acceptance/run_clean_room_saas_journey.sh --package-tar lockspire-$(sed -nE 's/^[[:space:]]*version:[[:space:]]*"([^"]+)".*/\1/p' mix.exs | head -1).tar --only happy_path</automated>
  </verify>
  <done>The exact built tar drives both isolated roles through the real minimal SaaS HTTP journey.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Resolve and prove one exact public Hex version</name>
  <files>scripts/acceptance/clean_room/package_input.py, scripts/acceptance/clean_room_saas_journey.py, scripts/acceptance/run_clean_room_saas_journey.sh, test/lockspire/clean_room_release_source_contract_test.exs</files>
  <behavior>
    - `--hex-version X.Y.Z` is mutually exclusive with local tar and checkout build and accepts only an exact semantic version.
    - Resolution is constrained to that exact public package, records fetched checksum/version, and rejects substitutions or mutable ranges.
    - The same install/migrate/verify/boot/minimal HTTP journey runs with no local checkout dependency.
  </behavior>
  <action>Add an exact-version source using the locked Hex client command confirmed by local help, fetching into a private run root and validating package name/version before inventory/copy. Keep child manifests and locks immutable; adapt their Lockspire source only inside ephemeral children in a deterministic way, then prove the resolved dependency path is below each child. Add fixture-mode tests for version drift, range input, symlink/path escape, and source substitution without requiring a live publish.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/clean_room_release_source_contract_test.exs &amp;&amp; python3 scripts/acceptance/clean_room/package_input.py --help &amp;&amp; python3 scripts/acceptance/clean_room_saas_journey.py --help</automated>
  </verify>
  <done>The harness has a fail-closed exact-public-version source ready for the post-publish workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local/Hex package -> child apps | Package bytes become executable dependency code. |
| Live checkout -> clean room | Acceptance proof must not resolve back to repository source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-137-29 | Tampering | package source | high | mitigate | Exact path/version, checksum, inventory, and resolved-dependency provenance checks. |
| T-137-30 | Elevation of privilege | package archive | high | mitigate | Supported Hex unpack, reject symlinks/path escape/test support before execution. |
| T-137-31 | Repudiation | clean-room source | high | mitigate | Record source kind/version/checksum once and verify both child dependency paths. |
| T-137-32 | Information disclosure | journey evidence | high | mitigate | Preserve Phase 133 sentinel registration, redaction scan, and bounded receipts. |
</threat_model>

<verification>
Source/provenance contracts pass and the actual local tar completes the existing happy HTTP journey; public-version live proof is reserved for post-publish execution.
</verification>

<success_criteria>
One mature clean-room harness proves both sides of REL-01 at the package boundary rather than only compiling a generated app.
</success_criteria>

<output>
Create `.planning/phases/137-ci-conformance-and-release-proof/137-08-SUMMARY.md` when done.
</output>
