---
phase: 137-ci-conformance-and-release-proof
plan: 03
type: execute
wave: 2
depends_on: [137-02]
files_modified:
  - test/lockspire/coverage/protocol_behavior_test.exs
  - test/lockspire/coverage/integration_surface_behavior_test.exs
  - test/lockspire/coverage/storage_operator_behavior_test.exs
autonomous: true
requirements: [CI-02]
must_haves:
  truths:
    - "The complete fast-plus-integration Mix partition reaches at least 84 percent line coverage from the measured 78.03 percent baseline."
    - "Coverage rises through observable success and failure behavior, not source-text assertions, production exclusions, or duplicate execution."
    - "Security-sensitive protocol, storage, and host/operator seams gain regression value while public v1.x behavior stays unchanged."
  artifacts:
    - path: "test/lockspire/coverage/protocol_behavior_test.exs"
      provides: "Protocol success/failure coverage selected from native uncovered-line evidence"
    - path: "test/lockspire/coverage/integration_surface_behavior_test.exs"
      provides: "HTTP and host integration edge behavior"
    - path: "test/lockspire/coverage/storage_operator_behavior_test.exs"
      provides: "Storage failure and redaction-safe operator behavior"
  key_links:
    - from: "the three behavior suites"
      to: "scripts/ci/aggregate_coverage.sh"
      via: "ordinary fast/integration partitions export the newly exercised production lines"
---

<objective>
Close the real coverage gap with behavior-rich tests until the authoritative aggregate is at least 84 percent.

Purpose: Make the new threshold sustainable and valuable rather than gaming its denominator.
Output: Focused regression tests chosen from the native aggregate's uncovered production lines.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-ci-conformance-and-release-proof/137-02-SUMMARY.md
@.planning/phases/137-ci-conformance-and-release-proof/137-RESEARCH.md
@AGENTS.md
@test/support
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Raise protocol coverage through externally observable edge behavior</name>
  <files>test/lockspire/coverage/protocol_behavior_test.exs</files>
  <behavior>
    - Tests select high-uncovered protocol paths from the fresh aggregate report and assert return values or wire-visible errors.
    - Cases include both valid and malformed/boundary inputs while preserving S256, exact redirects, single-use codes, refresh-family revocation, and no `alg=none`.
    - No test reads production source text, asserts private implementation names, or calls functions only to mark lines executed.
  </behavior>
  <action>Generate a fresh native report with Plan 02, rank uncovered `lib/lockspire/protocol/**` modules by executable lines, and add cohesive tests for the highest-value behaviors. Reuse existing case templates/fixtures. Keep tests capability-oriented and assert semantic outputs, errors, telemetry, or durable state. Do not modify production files, coverage exclusions, or test filters.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/coverage/protocol_behavior_test.exs</automated>
  </verify>
  <done>Previously uncovered protocol outcomes have direct behavioral regression proof and the aggregate percentage increases measurably.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raise HTTP and embedded-host seam coverage through request behavior</name>
  <files>test/lockspire/coverage/integration_surface_behavior_test.exs</files>
  <behavior>
    - Tests exercise under-covered Plug/controller/resource-server paths through requests and assert status, headers, and bounded bodies.
    - Negative cases prove redirection, client authentication, sender constraint, audience, or scope failures remain fail closed.
    - Host account/product authorization remains distinct from Lockspire protocol validation.
  </behavior>
  <action>Use the updated aggregate report to select uncovered `lib/lockspire/web/**`, Plug, and resource-server lines with real request-level value. Add cases through existing routers/connections rather than direct private calls. Preserve the public/admin router boundary and strong redaction. Re-run focused tests before measuring the aggregate.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/coverage/integration_surface_behavior_test.exs</automated>
  </verify>
  <done>Important HTTP and host-boundary outcomes are covered by request assertions and no host-owned policy migrates into Lockspire.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close the remaining gap with storage and operator behavior</name>
  <files>test/lockspire/coverage/storage_operator_behavior_test.exs</files>
  <behavior>
    - Tests cover under-exercised durable failure/empty/conflict behavior through repository or admin service contracts.
    - Assertions prove secrets remain redacted and mutation failures remain atomic.
    - The final once-per-partition aggregate is >=84.00 with no ignored production modules.
  </behavior>
  <action>After Tasks 1-2, regenerate coverage and target only the remaining high-value uncovered storage/admin service behavior needed to cross the floor with margin. Favor failure, empty, concurrency, and lifecycle outcomes over getters. Run the actual fast and integration export commands once each into fresh directories, aggregate them, and record the measured result in the summary. Never duplicate a partition to inflate counts.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/coverage/storage_operator_behavior_test.exs &amp;&amp; bash scripts/ci/run_test_matrix.sh --fast .artifacts/coverage/fast-timing.json .artifacts/coverage/fast "$(git rev-parse HEAD)" &amp;&amp; bash scripts/ci/run_test_matrix.sh --integration .artifacts/coverage/integration-timing.json .artifacts/coverage/integration "$(git rev-parse HEAD)" &amp;&amp; bash scripts/ci/aggregate_coverage.sh --expected-sha "$(git rev-parse HEAD)" --input .artifacts/coverage --output .artifacts/coverage/aggregate</automated>
  </verify>
  <done>The native complete Mix partition reports at least 84.00 percent from one fast and one integration execution, with behavior tests as the only denominator change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tests -> security claims | Coverage quantity must not replace behavioral correctness. |
| Fixtures -> protocol/storage runtime | Crafted inputs exercise privileged and secret-adjacent branches. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-137-09 | Repudiation | coverage percentage | high | mitigate | Record native report and prohibit exclusions, source assertions, and duplicated partitions. |
| T-137-10 | Tampering | security behavior tests | high | mitigate | Assert observable fail-closed results and preserved security defaults. |
| T-137-11 | Information disclosure | operator/HTTP fixtures | high | mitigate | Use sentinel secrets and assert rendered/logged outputs remain redacted. |
| T-137-12 | Denial of service | expanded test suite | low | accept | Focus on high-value uncovered branches and retain timing receipts for regression visibility. |
</threat_model>

<verification>
All three focused suites pass, no production or coverage configuration changes occur, and the real Plan 02 aggregate records >=84.00 percent.
</verification>

<success_criteria>
CI-02's numeric floor is achieved honestly and the added tests make security and lifecycle behavior easier to trust.
</success_criteria>

<output>
Create `.planning/phases/137-ci-conformance-and-release-proof/137-03-SUMMARY.md` when done.
</output>
