---
phase: 137-ci-conformance-and-release-proof
plan: 07
type: execute
wave: 3
depends_on: [137-06]
files_modified:
  - .github/workflows/oidf-conformance.yml
  - lib/mix/tasks/lockspire.oidf_conformance.ex
  - docs/maintainer-conformance.md
  - test/lockspire/conformance_workflow_contract_test.exs
  - test/mix/tasks/lockspire/oidf_conformance_test.exs
autonomous: true
requirements: [CONF-01, CONF-02]
must_haves:
  truths:
    - "The default-branch workflow actually schedules repo-native OIDC/FAPI evidence and remains manually runnable."
    - "Only validated redacted evidence is uploaded with bounded retention and least privilege."
    - "Docs and Mix diagnostics call the lane supplemental and make no certification or release-gate claim."
  artifacts:
    - path: ".github/workflows/oidf-conformance.yml"
      provides: "Scheduled supplemental conformance orchestration"
    - path: "docs/maintainer-conformance.md"
      provides: "Reproducible local/scheduled operator contract"
    - path: "lib/mix/tasks/lockspire.oidf_conformance.ex"
      provides: "Local prerequisite and immutable-input diagnostics"
  key_links:
    - from: ".github/workflows/oidf-conformance.yml"
      to: "Plan 06 suite runners and safe evidence directories"
      via: "scheduled/manual jobs and explicit artifact paths"
---

<objective>
Expose immutable conformance proof as a scheduled, supplemental, redaction-safe maintainer lane.

Purpose: Complete CONF-02 without turning external suite reliability into a certification or release claim.
Output: Scheduled workflow, truthful task diagnostics/docs, and workflow contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-ci-conformance-and-release-proof/137-06-SUMMARY.md
@.github/workflows/oidf-conformance.yml
@docs/maintainer-conformance.md
@lib/mix/tasks/lockspire.oidf_conformance.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Schedule repo-native profiles and retain only safe evidence</name>
  <files>.github/workflows/oidf-conformance.yml, test/lockspire/conformance_workflow_contract_test.exs</files>
  <behavior>
    - Workflow has a real cron schedule plus dispatch and uses top-level contents:read.
    - Scheduled jobs call both immutable runners and upload only their validated evidence directories using pinned actions and bounded retention.
    - Raw config/log/work directories, secrets, and hosted URLs are absent from artifact paths and summaries.
  </behavior>
  <action>Recheck official GitHub schedule/artifact syntax, then add a non-release-gating schedule to the existing workflow. Keep hosted-maintainer credentials optional and isolated; make repo-native profiles the scheduled evidence spine. Point `if: always()` upload steps only at Plan 06 safe directories after validation. Add concurrency/timeouts and preserve pinned action SHAs, least privilege, and failure visibility.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/conformance_workflow_contract_test.exs &amp;&amp; bash scripts/ci/lint_workflows.sh</automated>
  </verify>
  <done>Default-branch schedule and dispatch both produce bounded redacted repo-native evidence without widening permissions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align maintainer diagnostics and claims with immutable supplemental truth</name>
  <files>lib/mix/tasks/lockspire.oidf_conformance.ex, docs/maintainer-conformance.md, test/mix/tasks/lockspire/oidf_conformance_test.exs, test/lockspire/conformance_workflow_contract_test.exs</files>
  <behavior>
    - Mix preflight identifies missing Docker/Compose/Python/curl/jq/lock inputs before a run and points to exact commands.
    - Docs list pinned suite identity, evidence fields, schedule/manual use, and infrastructure-vs-protocol failure classification.
    - Docs explicitly retain supplemental/non-certification/non-release-gate scope.
  </behavior>
  <action>Update the task to validate the checked-in lock and current prerequisites without printing secrets. Rewrite maintainer guidance around the shared runners, exact pinned identities, safe artifact contents, and classified failures. Retain the existing hosted lane as optional input and state clearly that scheduled evidence is supplemental reliability history, not formal certification.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/mix/tasks/lockspire/oidf_conformance_test.exs test/lockspire/conformance_workflow_contract_test.exs &amp;&amp; MIX_ENV=test mix lockspire.oidf_conformance --check &amp;&amp; MIX_ENV=dev mix docs.verify</automated>
  </verify>
  <done>Maintainers can reproduce, diagnose, and interpret the lane without source-diving or overstating its assurance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Scheduled event -> external infrastructure | Unattended runs consume Docker/network inputs. |
| GitHub secrets -> hosted lane | Optional external credentials must not enter artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-137-25 | Information disclosure | workflow uploads | high | mitigate | Upload only Plan 06 validated evidence paths; contract forbidden raw paths. |
| T-137-26 | Elevation of privilege | scheduled workflow | high | mitigate | Least privileges, pinned actions, bounded inputs, no pull-request secret use. |
| T-137-27 | Repudiation | certification claim | medium | mitigate | Docs/workflow name evidence supplemental and non-certifying. |
| T-137-28 | Denial of service | scheduled external suite | low | accept | Timeouts/concurrency limit cost; lane remains supplemental. |
</threat_model>

<verification>
Actionlint/shellcheck, task tests, workflow contract, task preflight, and strict docs all pass.
</verification>

<success_criteria>
Conformance evidence runs on schedule from immutable inputs, retains no raw secrets, and remains truthfully supplemental.
</success_criteria>

<output>
Create `.planning/phases/137-ci-conformance-and-release-proof/137-07-SUMMARY.md` when done.
</output>
