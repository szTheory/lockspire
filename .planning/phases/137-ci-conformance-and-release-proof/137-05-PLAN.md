---
phase: 137-ci-conformance-and-release-proof
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/conformance/oidf-suite-lock.json
  - scripts/conformance/verify_oidf_inputs.py
  - scripts/conformance/prepare_oidf_suite.sh
  - test/lockspire/conformance_immutable_inputs_contract_test.exs
autonomous: true
requirements: [CONF-01]
must_haves:
  truths:
    - "Every OIDF source file, archive, and container has an immutable checked-in identity."
    - "Preparation fails before execution on a mutable reference, checksum mismatch, digest mismatch, or unexpected compose image."
    - "No `latest`, `master`, tag-only image, or source-build fallback participates in evidence."
  artifacts:
    - path: "scripts/conformance/oidf-suite-lock.json"
      provides: "Versioned OIDF source/checksum/OCI digest lock"
    - path: "scripts/conformance/verify_oidf_inputs.py"
      provides: "Strict lock and fetched-input validator"
    - path: "scripts/conformance/prepare_oidf_suite.sh"
      provides: "Private temporary immutable suite preparation"
  key_links:
    - from: "oidf-suite-lock.json"
      to: "prepare_oidf_suite.sh"
      via: "validated commit URLs, SHA-256 checksums, and digest-qualified image overrides"
---

<objective>
Create an immutable OIDF suite input boundary before either conformance profile runs.

Purpose: Eliminate the mutable upstream/fallback paths that currently make CONF-01 irreproducible.
Output: Auditable lock data, validator, preparation helper, and hostile-input contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-ci-conformance-and-release-proof/137-RESEARCH.md
@scripts/conformance/run_phase37_suite.sh
@scripts/conformance/run_fapi2_suite.sh
@scripts/conformance/phase37-plan.json
@scripts/conformance/fapi2-plan.json
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Validate the complete immutable suite lock</name>
  <files>scripts/conformance/oidf-suite-lock.json, scripts/conformance/verify_oidf_inputs.py, test/lockspire/conformance_immutable_inputs_contract_test.exs</files>
  <behavior>
    - Lock schema requires tag `release-v5.1.43`, commit `16ad152b1b2c0baacd3d2519128340d95deb2b8c`, archive/helper SHA-256 values, and all three OCI digests.
    - Mutable URL segments/references, non-64-hex checksums, missing inputs, duplicate keys, and digest changes fail validation.
    - The validator accepts only the expected bounded lock schema and emits no secrets.
  </behavior>
  <action>Write hostile lock fixtures in the ExUnit contract, then create strict standard-library Python validation. Pin archive SHA-256 `c11be330b5a731bf3bca312f7b44d6bfe3981b448d5f58ec3dc2e48f3f509c8e`, all helper checksums from research, server digest `sha256:f6b1bb02bc746dd216506f773bc19997b113072fd22dec515f7e3357e218d950`, nginx digest `sha256:6a13e1c9ae2d19f2d0fea0a371905d580d61b960e0da84c01d5006729a5f1426`, and mongo digest `sha256:b415b12f638e2685d06c58ab7fb5943577c50fadec6d9340ef67d21aeac72070`. Use full-commit raw/archive URLs only.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/conformance_immutable_inputs_contract_test.exs &amp;&amp; python3 scripts/conformance/verify_oidf_inputs.py --lock scripts/conformance/oidf-suite-lock.json --validate-only</automated>
  </verify>
  <done>The repository contains one complete machine-validated identity for every external conformance input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prepare checksum-verified files and digest-qualified compose</name>
  <files>scripts/conformance/prepare_oidf_suite.sh, test/lockspire/conformance_immutable_inputs_contract_test.exs</files>
  <behavior>
    - Downloads land in a private temporary directory and each hash is checked before use.
    - The normalized compose references only the three locked digest-qualified images.
    - Network, hash, parse, image, or tool failure stops preparation with a classified error and no fallback.
  </behavior>
  <action>Create a shell entry point that calls the validator, downloads only locked URLs, verifies every byte, and produces a normalized compose/environment using exact image digests. Parse and reject unexpected image references from the fetched compose. Make the output directory explicit and ownership-safe. Remove no existing runner behavior yet; this plan establishes the reusable verified seam Plan 06 will consume.</action>
  <verify>
    <automated>bash -n scripts/conformance/prepare_oidf_suite.sh &amp;&amp; MIX_ENV=test mix test test/lockspire/conformance_immutable_inputs_contract_test.exs</automated>
  </verify>
  <done>Suite preparation cannot reach Docker or Python execution until all immutable source and image identities are proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitLab/registry -> local runner | External bytes and image manifests cross into maintainer execution. |
| Checked-in lock -> downloaded suite | Repository policy binds external content to reviewed identities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-137-17 | Tampering | upstream source/download | high | mitigate | Full commit URLs plus per-file/archive SHA-256 before use. |
| T-137-18 | Tampering | container images | high | mitigate | Digest-qualified server, nginx, and Mongo images; reject compose drift. |
| T-137-19 | Elevation of privilege | fetched helper scripts | high | mitigate | Verify bytes first and execute only known locked helper paths. |
| T-137-20 | Denial of service | upstream outage | low | accept | Fail distinctly as infrastructure failure; never substitute mutable inputs. |
</threat_model>

<verification>
Hostile fixtures fail, the canonical lock validates, shell syntax passes, and no mutable reference exists in executable conformance scripts added by this plan.
</verification>

<success_criteria>
Maintainers have a reproducible, fail-closed input preparation seam for both OIDC and FAPI evidence.
</success_criteria>

<output>
Create `.planning/phases/137-ci-conformance-and-release-proof/137-05-SUMMARY.md` when done.
</output>
