---
phase: 137-ci-conformance-and-release-proof
plan: 06
type: execute
wave: 2
depends_on: [137-05]
files_modified:
  - scripts/conformance/run_phase37_suite.sh
  - scripts/conformance/run_fapi2_suite.sh
  - scripts/conformance/build_redacted_evidence.py
  - test/lockspire/conformance_redacted_evidence_contract_test.exs
autonomous: true
requirements: [CONF-01, CONF-02]
must_haves:
  truths:
    - "Both repo-native suite profiles consume the same verified immutable preparation seam."
    - "Only a bounded redacted evidence tree survives a run, including failures."
    - "Provider configs, credentials, tokens, cookies, authorization codes, hosted URLs, and raw logs never enter retained evidence."
  artifacts:
    - path: "scripts/conformance/build_redacted_evidence.py"
      provides: "Allowlisted conformance receipt and artifact-tree safety validation"
    - path: "scripts/conformance/run_phase37_suite.sh"
      provides: "Pinned OIDC profile execution"
    - path: "scripts/conformance/run_fapi2_suite.sh"
      provides: "Pinned FAPI profile execution"
  key_links:
    - from: "both suite runners"
      to: "scripts/conformance/prepare_oidf_suite.sh"
      via: "single immutable bootstrap invocation"
    - from: "suite working directory"
      to: "retained evidence directory"
      via: "allowlisted manifest builder, never recursive raw copy"
---

<objective>
Run both conformance profiles from immutable inputs and retain only safe evidence.

Purpose: Complete the executable and redaction halves of CONF-01/02 before scheduling.
Output: Deduplicated runners, structured redacted receipts, and leak/bypass tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/137-ci-conformance-and-release-proof/137-05-SUMMARY.md
@scripts/acceptance/clean_room/redaction.py
@scripts/conformance/run_phase37_suite.sh
@scripts/conformance/run_fapi2_suite.sh
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run one OIDC path through immutable bootstrap to safe receipt</name>
  <files>scripts/conformance/run_phase37_suite.sh, scripts/conformance/build_redacted_evidence.py, test/lockspire/conformance_redacted_evidence_contract_test.exs</files>
  <behavior>
    - Phase 37 runner has no mutable default, master download, source-build fallback, or unverified compose path.
    - A successful or failed fixture run emits a schema-versioned receipt with suite identity, hashes/digests, runtime/tool versions, timestamps, status, and bounded result fields.
    - Sentinel credentials, headers, URLs, raw config, and raw logs make evidence validation fail.
  </behavior>
  <action>Write evidence leak tests first. Refactor the Phase 37 runner to require Plan 05 preparation and write all raw execution material under an ephemeral work directory. Implement the standard-library evidence builder as an allowlist, not a scrub-everything copy: serialize only suite/plan identity, immutable hashes, tool versions, timestamps, exit classification, and bounded result names/statuses. Reuse Phase 133 sentinel/redaction concepts without importing child secrets.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/conformance_redacted_evidence_contract_test.exs &amp;&amp; bash -n scripts/conformance/run_phase37_suite.sh</automated>
  </verify>
  <done>The OIDC runner reaches a retained manifest only through verified inputs and an allowlisted evidence boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put FAPI evidence on the identical immutable/redacted path</name>
  <files>scripts/conformance/run_fapi2_suite.sh, scripts/conformance/build_redacted_evidence.py, test/lockspire/conformance_redacted_evidence_contract_test.exs</files>
  <behavior>
    - FAPI runner uses the same lock, preparation, and evidence schema as Phase 37 with only plan/profile data varying.
    - Runner failures still yield a safe classified receipt without exposing raw diagnostics.
    - Contracts reject divergence back to mutable downloads, raw artifact copies, or profile-specific secret fields.
  </behavior>
  <action>Refactor the FAPI runner onto the established helper interfaces and delete duplicated fetch/fallback logic. Keep profile plans and result classification distinct while sharing input and evidence policy. Add cross-runner contract assertions proving both invoke the same helpers and neither copies provider configuration or raw logs into the retained tree.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/conformance_redacted_evidence_contract_test.exs test/mix/tasks/lockspire/oidf_conformance_test.exs &amp;&amp; bash -n scripts/conformance/run_fapi2_suite.sh</automated>
  </verify>
  <done>OIDC and FAPI runs differ only in plan/profile intent; immutable inputs and redacted evidence policy cannot drift independently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Suite container -> repository evidence | Protocol runner emits secret-adjacent raw data. |
| Failure diagnostics -> retained artifact | Failure paths are most likely to leak credentials or bodies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-137-21 | Information disclosure | conformance artifact | high | mitigate | Allowlisted manifest schema and sentinel leak rejection before retention. |
| T-137-22 | Tampering | profile runner bootstrap | high | mitigate | Both scripts require the same verified Plan 05 output and remove fallback. |
| T-137-23 | Repudiation | failed suite result | medium | mitigate | Record bounded exit classification, suite identity, and timestamps on every outcome. |
| T-137-24 | Elevation of privilege | raw suite output | high | mitigate | Keep raw work ephemeral and never upload or execute retained evidence. |
</threat_model>

<verification>
Focused leak fixtures and both shell syntax checks pass; source contracts find no mutable input or raw-copy path.
</verification>

<success_criteria>
Both conformance profiles are reproducible and produce safe, reviewable evidence on pass and failure.
</success_criteria>

<output>
Create `.planning/phases/137-ci-conformance-and-release-proof/137-06-SUMMARY.md` when done.
</output>
