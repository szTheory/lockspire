---
phase: "40"
plan: "02"
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - lib/lockspire/protocol/jar.ex
  - lib/lockspire/protocol/request_object.ex
  - test/lockspire/protocol/jar_test.exs
  - test/lockspire/protocol/request_object_test.exs
autonomous: true
requirements:
  - AUTHZ-02

must_haves:
  truths:
    - "Clients can submit JAR request objects that are encrypted (JWE) and then signed (JWS)."
    - "The server successfully decrypts nested JWE request objects before validating the signature."
    - "Unencrypted (signed-only) request objects still function correctly without failure."
  artifacts:
    - path: "lib/lockspire/protocol/jar.ex"
      provides: "JWE decryption function"
      min_lines: 30
    - path: "lib/lockspire/protocol/request_object.ex"
      provides: "Pre-verification decryption pipeline step"
      min_lines: 30
  key_links:
    - from: "lib/lockspire/protocol/request_object.ex"
      to: "lib/lockspire/storage/key_store.ex"
      via: "Calls KeyStore.list_decryption_keys/0"
    - from: "lib/lockspire/protocol/request_object.ex"
      to: "lib/lockspire/protocol/jar.ex"
      via: "Calls Jar.decrypt/2"
---

<objective>
Implement nested JWT (Sign-then-Encrypt) decryption in the Request Object pipeline so clients can send encrypted JARs.

Purpose: To securely handle encrypted request objects (JWE) by decrypting them with the authorization server's registered `:enc` keys to extract the underlying signed JWS, fulfilling RFC 9101 requirements.
Output: An updated `Protocol.Jar` that can decrypt JWEs, integrated into `Protocol.RequestObject`, with extensive test coverage.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/lockspire/protocol/jar.ex
@lib/lockspire/protocol/request_object.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build JWE decryption in Protocol.Jar</name>
  <files>lib/lockspire/protocol/jar.ex</files>
  <action>Add a `decrypt(jwt, decryption_keys)` function to `Lockspire.Protocol.Jar`. Use `String.split(jwt, ".")` to detect if it's a 5-part JWE. If not, immediately return `{:ok, jwt}`. If it is, iterate through `decryption_keys`. For each key, parse its `private_jwk_encrypted` JSON into a `JOSE.JWK` and attempt `JOSE.JWK.block_decrypt(jwt, jwk)` (integrating the JWT Decoding Pattern from PATTERNS.md for parsing JWE structures). On success, return `{:ok, plain_text}` (which is the inner JWS string). If all keys fail, return `{:error, :decryption_failed}` (following the Error Handling pattern in PATTERNS.md).</action>
  <verify>
    <automated>mix test --compile</automated>
  </verify>
  <done>The `decrypt/2` function is defined and correctly delegates to `JOSE.JWK.block_decrypt` for JWEs.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate decryption in RequestObject</name>
  <files>lib/lockspire/protocol/request_object.ex</files>
  <action>Modify `Lockspire.Protocol.RequestObject.consume/3` to add a decryption step before `decode_and_verify`. Call `Lockspire.Storage.KeyStore.list_decryption_keys()` to fetch keys. Pass the JWT and keys to `Jar.decrypt/2`. If decryption fails, return a `browser_error` with `invalid_request_object` indicating decryption failure. If it succeeds, pass the unencrypted JWS string forward into `decode_and_verify(jws_string, client)` (ensuring compatibility with the Strict Verification Pattern mapped in PATTERNS.md).</action>
  <verify>
    <automated>mix test --compile</automated>
  </verify>
  <done>The consume pipeline correctly decrypts JWE strings into JWS strings and handles decryption errors safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Protocol tests for nested JWTs</name>
  <files>test/lockspire/protocol/jar_test.exs, test/lockspire/protocol/request_object_test.exs</files>
  <behavior>
    - Test 1 (Jar): `decrypt/2` ignores 3-part JWS strings and returns them unaltered.
    - Test 2 (Jar): `decrypt/2` correctly decrypts a 5-part JWE with a matching private key.
    - Test 3 (Jar): `decrypt/2` returns `{:error, :decryption_failed}` if no provided keys can decrypt the JWE.
    - Test 4 (RequestObject): `consume/3` handles a nested JWE request object end-to-end when a valid `:enc` key is present.
  </behavior>
  <action>Update tests to verify the nested JWT decryption capabilities. Use `test_jwe.exs` output or `JOSE.JWE` directly in test setup to generate valid JWE strings wrapping JWS payloads.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/jar_test.exs test/lockspire/protocol/request_object_test.exs</automated>
  </verify>
  <done>All decryption boundary cases and success paths are verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> AS | Encrypted request objects from clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-02 | Information Disclosure | `Protocol.RequestObject` | mitigate | Ensure decryption errors are generic `invalid_request_object` to avoid oracle attacks disclosing which keys exist. |
| T-40-03 | Denial of Service | `Protocol.Jar` | mitigate | Decryption must gracefully handle malformed JWE headers without raising unhandled exceptions (handled by JOSE library, but ensure `try/rescue` is covering it if necessary). |
</threat_model>

<verification>
Verify `mix test` runs smoothly and `Protocol.RequestObject` correctly digests nested request objects.
</verification>

<success_criteria>
- JWE request objects are correctly decrypted by `Protocol.Jar`.
- `Protocol.RequestObject` fetches `enc` keys and attempts decryption before signature verification.
- Unencrypted JAR objects remain natively supported.
- Decryption errors result in standard OAuth error returns, not 500 crashes.
</success_criteria>

<output>
After completion, create `.planning/phases/40-jwe/40-02-SUMMARY.md`
</output>