---
phase: "40"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/storage/ecto/signing_key_record.ex
  - lib/lockspire/domain/signing_key.ex
  - lib/lockspire/storage/key_store.ex
  - lib/lockspire/storage/ecto/repository.ex
  - lib/lockspire/admin/keys.ex
  - lib/lockspire/web/live/admin/keys_live/index.ex
  - lib/lockspire/web/live/admin/keys_live/show.ex
  - test/lockspire/storage/repository_test.exs
  - test/lockspire/protocol/jwks_test.exs
  - test/lockspire/admin/keys_test.exs
autonomous: true
requirements:
  - AUTHZ-01

must_haves:
  truths:
    - "Operators can generate, manage, and advertise `enc` keys in the server's JWKS."
    - "The authorization server can safely isolate signature keys from encryption keys."
  artifacts:
    - path: "lib/lockspire/storage/ecto/repository.ex"
      provides: "Decryption key querying and strict signature key fetching"
      min_lines: 30
    - path: "lib/lockspire/admin/keys.ex"
      provides: "Admin API to generate and list keys with different uses"
      min_lines: 30
  key_links:
    - from: "lib/lockspire/storage/ecto/repository.ex"
      to: "lib/lockspire/storage/ecto/signing_key_record.ex"
      via: "Database query filtering by key.use == :enc"
---

<objective>
Expand the domain and storage layers to support `enc` (encryption) keys natively, and ensure they are published in the JWKS endpoint without being used for token signatures. Add operator controls to generate and manage these keys.

Purpose: To allow the server to securely receive and decrypt Request Objects while maintaining strict separation of duties between signing and encryption key pairs, and allow operators to manage them.
Output: Updated domain model, Ecto schema, KeyStore interface, Repository implementation, Admin Key context, UI updates, and test coverage verifying correct key usage isolation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/lockspire/domain/signing_key.ex
@lib/lockspire/storage/ecto/signing_key_record.ex
@lib/lockspire/storage/key_store.ex
@lib/lockspire/storage/ecto/repository.ex
@lib/lockspire/admin/keys.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `enc` to domain and Ecto schema</name>
  <files>lib/lockspire/domain/signing_key.ex, lib/lockspire/storage/ecto/signing_key_record.ex</files>
  <action>Update `Lockspire.Domain.SigningKey` to allow `:enc` in the `use_type` spec (per the Key Use Type Pattern in PATTERNS.md). Update `Lockspire.Storage.Ecto.SigningKeyRecord`'s `:use` field definition to use `Ecto.Enum, values: [:sig, :enc]`. No database migration is needed as the underlying column is a text type.</action>
  <verify>
    <automated>mix test --compile</automated>
  </verify>
  <done>The `:use` enum successfully accepts `:enc` alongside `:sig` without compilation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Expand KeyStore interface and Repository queries</name>
  <files>lib/lockspire/storage/key_store.ex, lib/lockspire/storage/ecto/repository.ex</files>
  <action>Add a `list_decryption_keys/0` callback to `KeyStore` that returns `{:ok, [SigningKey.t()]}` (extending the Callback Pattern in PATTERNS.md). Implement it in `Repository` to query `SigningKeyRecord` where `key.use == :enc` and `key.status in [:active, :retiring]`. Update `fetch_active_signing_key/0` in `Repository` to explicitly filter `where([key], key.use == :sig)` so that encryption keys are never accidentally returned for JWT signing.</action>
  <verify>
    <automated>mix test --compile</automated>
  </verify>
  <done>Repository cleanly implements the new query and correctly scopes the existing signing key query.</done>
</task>

<task type="auto">
  <name>Task 3: Implement key generation and UI updates in Admin context</name>
  <files>lib/lockspire/admin/keys.ex, lib/lockspire/web/live/admin/keys_live/index.ex, lib/lockspire/web/live/admin/keys_live/show.ex</files>
  <action>Update `lib/lockspire/admin/keys.ex` to implement a `generate_key(use \\ :sig)` function. This function should generate a new `JOSE.JWK` (using `JOSE.JWK.generate_key({:rsa, 2048})` or `{:ec, "P-256"}`), map it to a new `SigningKey` struct with the provided `use`, and persist it via `Repository.publish_key/1` or an appropriate insertion method. As noted in PATTERNS.md, adjust `list_keys` / views to also manage `enc` lifecycle states by ensuring `to_view` and `to_detail_view` expose the `use` field. Update `KeysLive.Index` and `KeysLive.Show` templates to display the `use` (:sig vs :enc) in the UI, and add a button/form to invoke `generate_key(use: :enc)` and `generate_key(use: :sig)` to allow explicit operator management of both types.</action>
  <verify>
    <automated>mix test --compile</automated>
  </verify>
  <done>Admin operators have a functional UI and backend API to generate and manage encryption keys independently of signing keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Test coverage for key isolation and JWKS publishing</name>
  <files>test/lockspire/storage/repository_test.exs, test/lockspire/protocol/jwks_test.exs, test/lockspire/admin/keys_test.exs</files>
  <behavior>
    - Test 1 (Repository): `list_decryption_keys/0` returns only `:enc` keys with status `:active` or `:retiring`.
    - Test 2 (Repository): `fetch_active_signing_key/0` ignores any active `:enc` keys and only returns active `:sig` keys.
    - Test 3 (JWKS): `public_jwk_set/1` includes public JWKs for published `:enc` keys.
    - Test 4 (Admin): `generate_key/1` successfully creates `:enc` keys and they appear in `list_keys/1` with proper states.
  </behavior>
  <action>Write unit tests verifying the Repository query behaviors, the Admin generator function, and confirm that `enc` keys naturally flow through the existing JWKS pipeline.</action>
  <verify>
    <automated>mix test test/lockspire/storage/repository_test.exs test/lockspire/protocol/jwks_test.exs test/lockspire/admin/keys_test.exs</automated>
  </verify>
  <done>All tests pass, proving that `enc` and `sig` keys are functionally isolated, manageable via Admin, and published together.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal / Server | Server isolates token signing from decryption keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Elevation of Privilege | `Repository.fetch_active_signing_key/0` | mitigate | Explicitly restrict key query to `use == :sig` to prevent using decryption keys for signing server tokens. |
</threat_model>

<verification>
Ensure tests confirm isolated querying of `:sig` vs `:enc` keys, and the UI displays both distinctly.
</verification>

<success_criteria>
- Domain and Ecto types accept `enc`.
- `KeyStore.list_decryption_keys/0` is implemented and functional.
- `Repository.fetch_active_signing_key/0` is hardened to only fetch signature keys.
- Admin LiveViews can generate and display `enc` keys.
- Tests prove the JWKS endpoint includes the `enc` keys without modification to `Jwks.ex` itself.
</success_criteria>

<output>
After completion, create `.planning/phases/40-jwe/40-01-SUMMARY.md`
</output>