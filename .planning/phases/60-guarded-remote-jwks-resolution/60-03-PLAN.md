---
phase: 60-guarded-remote-jwks-resolution
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - lib/lockspire/jwks_fetcher.ex
  - test/lockspire/jwks_fetcher_test.exs
autonomous: true
requirements:
  - JWKS-03
must_haves:
  truths:
    - "Successful remote JWKS fetches are cached with explicit TTL semantics rather than ad hoc permanence."
    - "The fetcher exposes one bounded forced-refresh path so ordinary client key rotation can recover without re-registration."
    - "Failed refresh attempts have explicit cache semantics and do not silently widen trust."
  artifacts:
    - path: lib/lockspire/jwks_fetcher.ex
      provides: Explicit TTL and forced-refresh behavior for remote client keys.
    - path: test/lockspire/jwks_fetcher_test.exs
      provides: Rotation and refresh regression coverage pinned at the fetcher seam.
  key_links:
    - from: lib/lockspire/jwks_fetcher.ex
      to: lib/lockspire/protocol/client_auth.ex
      via: future key-miss or signature-mismatch refresh recovery in Phase 61
      pattern: refresh|get_keys
    - from: lib/lockspire/jwks_fetcher.ex
      to: lib/lockspire/application.ex
      via: current supervised cache entry lifecycle
      pattern: ttl|cache
---

<objective>
Add explicit cache TTL, refresh, and rotation-recovery behavior so remote client key rotation can succeed without widening trust or requiring re-registration.

Purpose: Satisfy `JWKS-03` by making the fetcher cache contract explicit and by exposing one bounded forced-refresh path for later verifier use.
Output: One plan covering TTL semantics, forced-refresh behavior, refresh-failure handling, and fetcher rotation tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-guarded-remote-jwks-resolution/60-CONTEXT.md
@.planning/phases/60-guarded-remote-jwks-resolution/60-RESEARCH.md
@.planning/phases/60-guarded-remote-jwks-resolution/60-PATTERNS.md
@lib/lockspire/jwks_fetcher.ex
@lib/lockspire/protocol/client_auth.ex
@test/lockspire/jwks_fetcher_test.exs

<interfaces>
Current success path in `lib/lockspire/jwks_fetcher.ex`:
```elixir
{:commit, jwk_set, ttl: @default_ttl}
```

Phase 60 should preserve the narrow "return keys or a stable error" contract while adding one explicit cache-bypass or refresh capability for future verifier use.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make TTL and forced-refresh behavior explicit</name>
  <read_first>
    - lib/lockspire/jwks_fetcher.ex
    - test/lockspire/jwks_fetcher_test.exs
    - .planning/phases/60-guarded-remote-jwks-resolution/60-RESEARCH.md
  </read_first>
  <files>lib/lockspire/jwks_fetcher.ex, test/lockspire/jwks_fetcher_test.exs</files>
  <behavior>
    - Successful remote JWKS fetches still cache with an explicit TTL.
    - Callers have one bounded forced-refresh path that bypasses stale cache and replaces it only on successful refetch.
    - Refresh-failure semantics are explicit and regression-tested.
  </behavior>
  <action>Extend `Lockspire.JwksFetcher` with one explicit forced-refresh path suitable for future use when signature verification encounters a missing key or stale remote set. The implementation may be a dedicated function or an opt-in parameter, but the public contract must stay narrow and obvious. Add focused tests proving: a normal cached read does not refetch unnecessarily; a forced refresh can replace cached key material after a simulated rotation; and the chosen behavior on failed refresh is explicit and pinned by tests. Do not add background refresh, polling, or proactive cache warmers.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/jwks_fetcher_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "ttl|refresh|force|cache" lib/lockspire/jwks_fetcher.ex test/lockspire/jwks_fetcher_test.exs` shows explicit cache and refresh semantics.
    - The fetcher test command passes.
    - The implementation exposes exactly one narrow refresh path rather than a generalized refresh subsystem.
  </acceptance_criteria>
  <done>The fetcher now has explicit TTL and rotation-recovery semantics that later verifier code can safely consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cached JWKS -> future client-assertion verification | Stale key material must not block ordinary rotation forever, but refresh behavior must remain bounded and explicit. |
| refresh failure -> cache state | Upstream errors during refresh must not create ambiguous trust or silently discard last-known-good state without a chosen contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-05 | T | cache refresh path | mitigate | Add one explicit forced-refresh contract with regression tests for success and failure semantics. |
</threat_model>

<verification>
Run the fetcher unit tests with warnings-as-errors and confirm cached, forced-refresh, and rotation cases all have deterministic coverage.
</verification>

<success_criteria>
Remote client keys can be cached predictably and refreshed in one bounded path when ordinary key rotation requires it.
</success_criteria>

<output>
After completion, create `.planning/phases/60-guarded-remote-jwks-resolution/60-03-SUMMARY.md`
</output>
