---
phase: 60-guarded-remote-jwks-resolution
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - lib/lockspire/jwks_fetcher.ex
  - lib/lockspire/jwks_fetcher/target_safety.ex
  - test/lockspire/jwks_fetcher_test.exs
  - test/lockspire/jwks_fetcher/target_safety_test.exs
autonomous: true
requirements:
  - JWKS-01
  - JWKS-02
must_haves:
  truths:
    - "Remote JWKS retrieval resolves only publicly routable targets and rejects loopback, link-local, private, and otherwise unsafe destinations before trust is widened."
    - "Oversized JWKS payloads fail closed under an explicit response-size boundary."
    - "Target-safety policy is testable independently of the HTTP client implementation."
  artifacts:
    - path: lib/lockspire/jwks_fetcher/target_safety.ex
      provides: Narrow destination-classification and unsafe-target rejection logic.
    - path: lib/lockspire/jwks_fetcher.ex
      provides: Integration of target-safety checks and payload-size enforcement into the fetch path.
    - path: test/lockspire/jwks_fetcher/target_safety_test.exs
      provides: Deterministic unit proof for unsafe-address rejection.
  key_links:
    - from: lib/lockspire/jwks_fetcher.ex
      to: lib/lockspire/jwks_fetcher/target_safety.ex
      via: pre-request destination safety gate
      pattern: target|resolve|unsafe
    - from: test/lockspire/jwks_fetcher_test.exs
      to: test/lockspire/jwks_fetcher/target_safety_test.exs
      via: end-to-end fetch behavior backed by pure safety classification tests
      pattern: unsafe|oversize|payload
---

<objective>
Enforce network-target safety and response-size boundaries so `jwks_uri` cannot be abused as an SSRF wedge or oversized parser input.

Purpose: Satisfy the response-size boundary in `JWKS-01` and the destination-safety rules in `JWKS-02` by adding pre-request screening and explicit body-size caps to the existing fetcher path.
Output: One plan covering target-safety logic, fetcher integration, oversized-payload rejection, and deterministic unit tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-guarded-remote-jwks-resolution/60-CONTEXT.md
@.planning/phases/60-guarded-remote-jwks-resolution/60-RESEARCH.md
@.planning/phases/60-guarded-remote-jwks-resolution/60-PATTERNS.md
@lib/lockspire/jwks_fetcher.ex
@test/lockspire/jwks_fetcher_test.exs

<interfaces>
From `lib/lockspire/jwks_fetcher.ex`:
```elixir
case Req.get(uri, req_opts) do
  {:ok, %Req.Response{status: 200, body: body}} when is_map(body) ->
    ...
  {:ok, %Req.Response{status: 200, body: body}} when is_binary(body) ->
    ...
```

Phase 60 may add a helper module and resolver injection, but should keep the public fetcher API narrow and focused on `jwks_uri`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a narrow target-safety classifier for resolved destinations</name>
  <read_first>
    - lib/lockspire/jwks_fetcher.ex
    - .planning/phases/60-guarded-remote-jwks-resolution/60-CONTEXT.md
    - .planning/phases/60-guarded-remote-jwks-resolution/60-PATTERNS.md
  </read_first>
  <files>lib/lockspire/jwks_fetcher/target_safety.ex, test/lockspire/jwks_fetcher/target_safety_test.exs</files>
  <behavior>
    - The safety layer can classify resolved IP targets as public-routable or unsafe.
    - Loopback, link-local, private-network, and equivalent unsafe destinations are rejected with explicit reasons.
    - Resolver behavior is injectable or otherwise deterministic in unit tests.
  </behavior>
  <action>Create a small helper module responsible only for destination safety classification and resolution handling. Keep it decoupled from `Req` so it can be tested directly with representative IPv4 and IPv6 cases. Add unit tests covering clearly unsafe classes and at least one accepted public-routable case. Do not widen the helper into a configurable allowlist system or a generic network-security framework.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/jwks_fetcher/target_safety_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/lockspire/jwks_fetcher/target_safety_test.exs` exists and covers both accepted and rejected destination classes.
    - The target-safety test command passes.
    - The helper remains fetcher-focused and does not introduce host-managed network policy knobs.
  </acceptance_criteria>
  <done>The repo has a narrow, deterministic destination-safety seam ready to be enforced by the fetcher.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce destination safety and payload caps in the fetcher</name>
  <read_first>
    - lib/lockspire/jwks_fetcher.ex
    - lib/lockspire/jwks_fetcher/target_safety.ex
    - test/lockspire/jwks_fetcher_test.exs
    - test/lockspire/jwks_fetcher/target_safety_test.exs
  </read_first>
  <files>lib/lockspire/jwks_fetcher.ex, test/lockspire/jwks_fetcher_test.exs</files>
  <behavior>
    - Unsafe resolved targets are rejected before the fetch path treats the JWKS as retrievable.
    - Oversized payloads fail closed under an explicit maximum body size.
    - Existing success and parse-failure behavior remain intact for safe, bounded responses.
  </behavior>
  <action>Integrate the target-safety helper into `Lockspire.JwksFetcher` so the fetch path performs destination screening before or as part of issuing the request, and add an explicit maximum-body-size boundary for successful responses. Extend `test/lockspire/jwks_fetcher_test.exs` with cases for unsafe-target rejection and oversize-body rejection using the same fast transport-test style already in the file. Keep the public API narrow and avoid adding host-owned override knobs for unsafe destinations.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/jwks_fetcher_test.exs test/lockspire/jwks_fetcher/target_safety_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "unsafe|private|loopback|payload|size" lib/lockspire/jwks_fetcher.ex test/lockspire/jwks_fetcher_test.exs test/lockspire/jwks_fetcher/target_safety_test.exs` shows the new guardrails and tests.
    - The combined fetcher + target-safety test command passes.
    - The implementation rejects unsafe-address and oversized-payload cases without relying on live network access.
  </acceptance_criteria>
  <done>`jwks_uri` resolution now rejects unsafe destinations and oversized payloads before the remote key path can widen trust.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DNS/host resolution -> outbound request | A seemingly harmless hostname must not route Lockspire into private or local network space. |
| upstream response body -> JOSE parser | Unbounded payloads must not become a memory or parser abuse vector on the auth path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03 | I | target resolution | mitigate | Resolve and classify destinations before trusting the remote JWKS path. |
| T-60-04 | D | response parsing | mitigate | Enforce a hard payload boundary and fail closed on oversize responses. |
</threat_model>

<verification>
Run both the fetcher and target-safety unit tests with warnings-as-errors and confirm unsafe-target and oversize cases are pinned.
</verification>

<success_criteria>
Unsafe `jwks_uri` targets and oversized responses are rejected as a built-in fetcher policy rather than left to caller discretion.
</success_criteria>

<output>
After completion, create `.planning/phases/60-guarded-remote-jwks-resolution/60-02-SUMMARY.md`
</output>
