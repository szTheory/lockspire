---
phase: 60-guarded-remote-jwks-resolution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/jwks_fetcher.ex
  - test/lockspire/jwks_fetcher_test.exs
autonomous: true
requirements:
  - JWKS-01
must_haves:
  truths:
    - "Remote JWKS retrieval is a narrow `https`-only client-auth seam with redirects disabled, retries disabled, and intentionally low timeout budgets."
    - "Fetcher failures normalize into stable internal reasons that later protocol handlers can map to `invalid_client` without leaking transport details."
    - "Phase 60 hardens the existing fetcher; it does not widen into a generic outbound-ingestion subsystem."
  artifacts:
    - path: lib/lockspire/jwks_fetcher.ex
      provides: The guarded request policy and normalized fetch failure model.
    - path: test/lockspire/jwks_fetcher_test.exs
      provides: Contract tests for strict request policy and fail-closed behavior.
  key_links:
    - from: lib/lockspire/jwks_fetcher.ex
      to: lib/lockspire/application.ex
      via: supervised cache seam that Phase 60 hardens in place
      pattern: lockspire_jwks_cache|Cachex
    - from: lib/lockspire/jwks_fetcher.ex
      to: lib/lockspire/protocol/client_auth.ex
      via: future `private_key_jwt` verifier dependency on guarded key retrieval
      pattern: get_keys|private_key_jwt
---

<objective>
Harden `Lockspire.JwksFetcher` request policy and failure behavior so the remote JWKS path becomes an explicit security boundary instead of a convenience helper.

Purpose: Satisfy `JWKS-01` by making the current fetcher strict, fail-closed, and later consumable by OAuth client-auth code without exposing raw transport semantics.
Output: One focused plan covering strict request defaults, `https`-only runtime enforcement, normalized failure reasons, and fetcher unit tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/60-guarded-remote-jwks-resolution/60-CONTEXT.md
@.planning/phases/60-guarded-remote-jwks-resolution/60-RESEARCH.md
@.planning/phases/60-guarded-remote-jwks-resolution/60-PATTERNS.md
@lib/lockspire/jwks_fetcher.ex
@lib/lockspire/application.ex
@lib/lockspire/protocol/client_auth.ex
@test/lockspire/jwks_fetcher_test.exs

<interfaces>
From `lib/lockspire/jwks_fetcher.ex`:
```elixir
@spec get_keys(String.t(), keyword()) :: {:ok, JOSE.JWK.t()} | {:error, term()}
def get_keys(uri, opts \\ []) when is_binary(uri) and is_list(opts)

defp fetch_from_network(uri, opts) do
  req_opts =
    Keyword.merge(
      [
        retry: false,
        receive_timeout: 5000
      ],
      opts
    )
```

From `lib/lockspire/application.ex`:
```elixir
children = [
  {Lockspire.Oban, Lockspire.Oban.runtime_config!()},
  Cachex.child_spec(name: :lockspire_jwks_cache)
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the remote fetch path intentionally strict</name>
  <read_first>
    - lib/lockspire/jwks_fetcher.ex
    - test/lockspire/jwks_fetcher_test.exs
    - .planning/phases/60-guarded-remote-jwks-resolution/60-RESEARCH.md
  </read_first>
  <files>lib/lockspire/jwks_fetcher.ex, test/lockspire/jwks_fetcher_test.exs</files>
  <behavior>
    - Non-`https` `jwks_uri` values are rejected at runtime before any request is attempted.
    - Redirect following is disabled, retries are disabled, and timeout defaults stay intentionally low.
    - HTTP, transport, timeout, redirect, and format failures surface through one stable fetcher-owned error contract.
  </behavior>
  <action>Refactor `Lockspire.JwksFetcher` so the public fetch path validates the URI scheme, applies strict `Req` defaults, and normalizes failure reasons instead of returning ad hoc transport structs or raw `Req` shapes everywhere. Keep the public contract narrow: callers still ask for keys and receive either a parsed JWK set or a fetcher-owned error. Extend `test/lockspire/jwks_fetcher_test.exs` to pin the new strict defaults and fail-closed behavior, including explicit rejection of non-`https` URIs and redirect responses. Do not add target-safety resolution logic or cache-refresh semantics in this plan.</action>
  <verify>
    <automated>MIX_ENV=test mix test --warnings-as-errors test/lockspire/jwks_fetcher_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "https|redirect|retry|timeout|invalid_jwks|http_error" lib/lockspire/jwks_fetcher.ex test/lockspire/jwks_fetcher_test.exs` shows strict runtime policy and regression coverage.
    - The fetcher test command passes.
    - No code path widens into generic retries, redirect following, or non-`https` fallback behavior.
  </acceptance_criteria>
  <done>The remote JWKS path is explicitly strict and fail-closed even before target-safety logic is added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `jwks_uri` input -> outbound request | Untrusted client metadata must not trigger permissive network behavior on the auth path. |
| HTTP/transport failures -> protocol callers | Low-level failures must not leak confusing or unsafe semantics into later OAuth client-auth behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-01 | S | `jwks_fetcher.ex` URI/runtime policy | mitigate | Enforce `https`-only runtime behavior and reject redirects/retries that could widen trust. |
| T-60-02 | I | fetcher error surface | mitigate | Normalize failures into stable internal reasons so later phases can map them truthfully to `invalid_client`. |
</threat_model>

<verification>
Run the fetcher unit tests with warnings-as-errors and confirm strict runtime policy is pinned before target-safety and rotation work land.
</verification>

<success_criteria>
`Lockspire.JwksFetcher` behaves like a guarded remote-key boundary with strict request defaults and stable failure semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/60-guarded-remote-jwks-resolution/60-01-SUMMARY.md`
</output>
