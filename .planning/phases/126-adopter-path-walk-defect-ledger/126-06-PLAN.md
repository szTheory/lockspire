---
phase: 126-adopter-path-walk-defect-ledger
plan: 06
type: execute
wave: 5
depends_on: ["126-05"]
files_modified:
  - test/lockspire/maintainer/defect_ledger_contract_test.exs
  - .planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md
  - scripts/maintainer/adopter_path_walk.sh
  - scripts/maintainer/adopter_path_flow.py
autonomous: false
requirements: [ADOPT-01, ADOPT-03]

must_haves:
  truths:
    - "A committed defect ledger exists at `.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md`, is non-empty, and every entry carries an ID plus the six fields D-37 names: walk step ID, symptom, underlying error, source, owning phase, and workaround (phase success criterion 4)."
    - "Every `# LOCKSPIRE_WALK_WORKAROUND: ADOPT-Dnn` marker in `scripts/maintainer/` has a matching ledger entry, and every ledger entry citing a workaround has a matching marker — set equality in both directions, enforced by a contract test (D-40, D-41, phase success criterion 5)."
    - "Every defect is attributed to a real source — installer, generated scaffolding, guide, reference demo, library, or environment — and to the phase that will fix it, because those two fields are Phases 127-129's scoping input (D-37)."
    - "The ledger records the guide sections the walk deliberately did not walk, and the residual nondeterminism the harness does not control, so a reader can distinguish a decision from an omission (D-15)."
    - "The ledger contains no raw access token, ID token, authorization code, session cookie, or the seeded password literal `walk-adopter-password-2026` — only error classes, step IDs, and file citations."
    - "The capturing run passes no flags: the evidence tree survives unconditionally under D-20, and the walk-started server is torn down so the port is free for the checkpoint's `--from-step` resume."
  prohibitions:
    - statement: "MUST NOT record raw credentials, tokens, authorization codes, or session material in the committed ledger — the evidence artifact must never become the leak."
      category: privacy
    - statement: "MUST NOT record a defect the walk did not actually observe, and MUST NOT omit one it did — a ledger padded from source reading or thinned by inconvenience is worse than no ledger, because downstream phases scope their work from it."
      category: transparency
    - statement: "MUST NOT fix a defect it records — the evidence stays separable from the repair; the only changes permitted here are to the harness itself."
      category: transparency
  artifacts:
    - path: ".planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md"
      provides: "The committed defect ledger: every break the walk surfaced, attributed to source and owning phase"
      contains: "ADOPT-D"
      min_lines: 60
    - path: "test/lockspire/maintainer/defect_ledger_contract_test.exs"
      provides: "Criterion 4 (non-empty, fully-fielded entries) and criterion 5 (marker↔ledger set equality) enforcement"
      contains: "LOCKSPIRE_WALK_WORKAROUND"
  key_links:
    - from: "scripts/maintainer/adopter_path_walk.sh"
      to: ".planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md"
      via: "workaround marker IDs grepped from the harness must equal the workaround IDs parsed from the ledger"
      pattern: "LOCKSPIRE_WALK_WORKAROUND: (ADOPT-D\\d+)"
    - from: ".planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md"
      to: "Phases 127, 128, 129"
      via: "the source and owning-phase fields are those phases' scoping input; Phase 127's success criterion 4 reads this file directly"
      pattern: "owning phase"
---

<objective>
Run the walk, capture what it found, and turn it into the phase's actual deliverable: a committed,
fully attributed defect ledger whose workaround claims are mechanically reconciled against the harness
source.

Purpose: phase success criteria 4 and 5 live here. A red walk with a complete ledger is a passing
phase; an empty or thin ledger is not, and a workaround left silently in the harness is the specific
failure mode criterion 5 exists to make impossible.

Output: `126-DEFECT-LEDGER.md`, `test/lockspire/maintainer/defect_ledger_contract_test.exs`, and any
marker corrections the reconciliation forces in the harness source.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/126-adopter-path-walk-defect-ledger/126-CONTEXT.md
@.planning/phases/126-adopter-path-walk-defect-ledger/126-RESEARCH.md
@.planning/phases/126-adopter-path-walk-defect-ledger/126-PATTERNS.md
@.planning/phases/126-adopter-path-walk-defect-ledger/126-05-SUMMARY.md
</context>

<inversion_warning>
`mix adopter.walk` is expected to exit non-zero in this plan, and that is a passing phase. No task
here gates on its exit code, on it reaching a final step, or on the ledger being empty. The gates are
that the ledger is non-empty, fully fielded, and reconciled.
</inversion_warning>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Ledger contract test — completeness and marker reconciliation</name>
  <files>test/lockspire/maintainer/defect_ledger_contract_test.exs</files>
  <read_first>
    - `test/lockspire/adoption_demo_docker_contract_test.exs` — the `File.read!` + `assert source =~` contract-test shape and `@repo_root Path.expand(...)` constants
    - `test/lockspire/release_readiness_contract_test.exs` — the `async: true` per-file `Path.expand` variant and its parsed-row assertions over a markdown document
    - `.planning/milestones/v1.9-MILESTONE-AUDIT.md` — the precedent for a committed, front-mattered evidence document under `.planning/` with a parsed table body
    - `.planning/phases/126-adopter-path-walk-defect-ledger/126-VALIDATION.md` — the criterion 4 and criterion 5 rows, and the prohibited gates this test must not contain
    - `.planning/phases/126-adopter-path-walk-defect-ledger/126-CONTEXT.md` — D-37's field list and D-40's marker convention
  </read_first>
  <behavior>
    `test/lockspire/maintainer/defect_ledger_contract_test.exs` (written before the ledger exists, red first):
    - Test: the ledger file exists and parses into at least one entry — an empty ledger fails
    - Test: every entry carries an ID matching the `ADOPT-D` prefix plus all six D-37 fields — walk
      step ID, symptom, underlying error, source, owning phase, workaround — with no field left blank
    - Test: every entry's source is one of the six allowed values: installer, generated scaffolding,
      guide, reference demo, library, environment
    - Test: every entry's owning phase is one of 127, 128, 129, 130, or an explicit future marker
    - Test: the set of `ADOPT-Dnn` IDs grepped from `# LOCKSPIRE_WALK_WORKAROUND:` markers across
      `scripts/maintainer/` equals the set of workaround IDs parsed from the ledger — asserted in both
      directions, so neither an unrecorded marker nor a phantom ledger claim passes
    - Test: the ledger body contains no bearer-token, authorization-code, cookie, or password material —
      assert the absence of the exact seeded password literal `walk-adopter-password-2026` (fixed by
      plan 126-01 and exported there as `LOCKSPIRE_WALK_PASSWORD`) and of a `Bearer ` prefix followed
      by a token-shaped string
  </behavior>
  <action>
Create `test/lockspire/maintainer/defect_ledger_contract_test.exs` as
`Lockspire.Maintainer.DefectLedgerContractTest`, `async: true`, following
`release_readiness_contract_test.exs`'s per-file `Path.expand` constant style — the module sits one
directory deeper than the existing analogs, so resolve paths accordingly.

Parse the ledger into entries with `Regex.scan` over whichever layout the ledger uses (a markdown
table row or a per-defect section — the layout is at the author's discretion under CONTEXT, so parse
defensively and fail with a clear message when no entry is found). Assert the behaviors listed above.

The marker reconciliation is the criterion-5 gate and must be a genuine set equality, not a subset
check in one direction: build one `MapSet` from `Regex.scan(~r/LOCKSPIRE_WALK_WORKAROUND: (ADOPT-D\d+)/, ...)`
across every file under `scripts/maintainer/`, build a second `MapSet` from the workaround field of
every parsed ledger entry, and assert both `MapSet.difference/2` results are empty with messages that
name the offending IDs. A marker with no ledger row is a workaround left silently in the harness; a
ledger row claiming a workaround that no marker backs is a false claim about the harness's state. Both
must fail.

Write the test first and confirm it is red against the absent ledger before Task 2 authors it.

This test must not contain any assertion that `mix adopter.walk` exits 0, that the walk reached its
final step, or that the ledger is empty — `126-VALIDATION.md` lists all three as prohibited gates
because each would fail the phase for the right reason.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && ! mix test test/lockspire/maintainer/defect_ledger_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The test file compiles and runs — `mix compile --warnings-as-errors` exits 0
    - `mix test test/lockspire/maintainer/defect_ledger_contract_test.exs` **fails** at the end of this task, with a message naming the missing ledger rather than an exception from the parser itself. This task's gate asserts the red state deliberately: gating on green here would let a stub ledger satisfy it, which is the exact self-certification this phase exists to eliminate. The green assertion belongs to Task 2
    - `grep -Fq 'LOCKSPIRE_WALK_WORKAROUND' test/lockspire/maintainer/defect_ledger_contract_test.exs`
    - `grep -Fq 'MapSet.difference' test/lockspire/maintainer/defect_ledger_contract_test.exs`
    - The test fails when a marker is added to `scripts/maintainer/adopter_path_walk.sh` without a matching ledger entry
    - The test fails when a ledger entry claims a workaround ID that no marker in `scripts/maintainer/` carries
    - The test fails when any ledger entry is missing one of the six D-37 fields
    - `! grep -Fq 'adopter.walk' test/lockspire/maintainer/defect_ledger_contract_test.exs` — no gate on the walk command's outcome
  </acceptance_criteria>
  <done>The ledger contract test exists, is red against the absent ledger for the right reason, and enforces entry completeness, allowed source and owning-phase values, secret absence, and two-way marker reconciliation.</done>
</task>

<task type="auto">
  <name>Task 2: Run the walk and author the committed defect ledger</name>
  <precondition>PostgreSQL is reachable with the walk credentials, network access is available for dependency and asset downloads, and `mix adopter.walk --preflight-only` completes without a PREREQUISITE exit.</precondition>
  <reversibility rating="costly">The ledger's ID scheme, field set, and location are read directly by Phases 127, 128 and 129 — Phase 127's success criterion 4 names this file — so renumbering or relocating it later means a coordinated edit across three downstream phases.</reversibility>
  <files>.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md, scripts/maintainer/adopter_path_walk.sh, scripts/maintainer/adopter_path_flow.py</files>
  <read_first>
    - `.planning/milestones/v1.9-MILESTONE-AUDIT.md` — the committed-evidence document precedent: YAML front matter carrying the verdict and counts, then a parsed table body
    - `.planning/phases/126-adopter-path-walk-defect-ledger/126-CONTEXT.md` — D-36 through D-49: the ledger location, the seven-item entry shape, why not `docs/` and why not JSON, and the seeded known-defect list the walk must confirm empirically
    - `.planning/phases/126-adopter-path-walk-defect-ledger/126-RESEARCH.md` — Pitfalls 3 through 8 and Open Questions 1 and 2, which name the defects the walk is expected to surface and the file:line evidence for each
    - `scripts/maintainer/adopter_path_walk.sh` and `scripts/maintainer/adopter_path_flow.py` — the authoritative list of markers actually present in the harness
    - `test/lockspire/maintainer/defect_ledger_contract_test.exs` — the parser the ledger's layout must satisfy
  </read_first>
  <action>
Run `mix adopter.walk` end to end, with no flags, and capture its full report. Expect a non-zero exit —
that is the deliverable evidence, not a failure.

Do **not** pass `--keep`. The evidence trail ADOPT-03 requires — `tmp/adopter-walk/host_app`,
`server.log`, and the step markers — survives unconditionally under D-20 and needs no flag. `--keep`'s
only effect is to leave the booted host server running (plan 126-01), which would violate plan 126-05's
"terminates only what it started" prohibition, trip T-126-25, and leave the walk port bound so the
`--from-step` resume check in Task 3 exits as a prerequisite failure instead of resuming. If a
maintainer later wants to poke the app by hand, that is what `--keep` is for, as a separate deliberate
invocation. If an
early failure blocks the walk from reaching a later step even with the marked workarounds already in
the harness, add the smallest additional local workaround needed to keep walking, mark it with a new
`# LOCKSPIRE_WALK_WORKAROUND: ADOPT-Dnn` comment, and give it a ledger entry — never leave one
unmarked.

Author `.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md`. Not `docs/` — that
directory is the hexdocs extras list, so a ledger there would publish Lockspire's own defect list to
adopters mid-repair and may fail the docs gate on an unlisted extra (D-38). Not JSON — the consumers
are the Phase 127-129 planners reading prose (D-39). Follow the milestone-audit shape: YAML front
matter carrying the run's verdict, the PASS and FAIL counts, the walk date, and the resolved Elixir,
OTP, PostgreSQL and `phx_new` versions; then a prose body.

Every entry carries an ID in the `ADOPT-Dnn` shape plus the six fields D-37 fixes: the walk step ID
that surfaced it, the symptom a maintainer sees, the underlying error, the source (one of installer,
generated scaffolding, guide, reference demo, library, environment), the owning phase (127, 128, 129,
130, or future), and the workaround (either none, or the marker ID present in the harness). Table or
per-section layout is at your discretion, provided every field is present per entry and the contract
test's parser accepts it.

The following defects are visible in source before the walk runs and are seeded here so a thin ledger
is recognizable as a harness failure rather than as good news. Each must be confirmed empirically by
the run — record what the walk actually observed, not what the source predicted, and drop or amend any
that did not reproduce, noting why:

- `ADOPT-D01` — `step-03b-router-call`: the generated router helper returns a String rather than a
  quoted macro, so following the guide's "call it" instruction compiles cleanly and defines zero
  routes. Source: installer. Owning phase: 127.
- `ADOPT-D02` — `step-03b-router-paste`: the helper's string body pipes through an operator pipeline
  that does not exist in a generated router, so pasting it prevents the host from compiling. Source:
  installer. Owning phase: 127.
- `ADOPT-D03` — `step-03b-router-wire`: the helper's forward sits in a pipeline-less scope, so the
  session-dependent interaction routes and the consent LiveView get no session fetch and no forgery
  protection; the reference demo works around this and the template never tells the adopter to.
  Source: installer and guide. Owning phase: 127 and 128.
- `ADOPT-D04` — `step-03a-config-import`: the generated config emits a placeholder issuer and omits
  the known-scopes, signing-algorithm, secret-key-base and Oban keys the reference demo proves are
  required. Source: installer. Owning phase: 127.
- `ADOPT-D05` — `step-03d-app-tree`: nothing documents the host's `included_applications` entry or the
  three supervision children, so Lockspire's application starts before the host repo with live default
  queues. Source: installer and guide. Owning phase: 127 and 128.
- `ADOPT-D06` — `step-06a-client`: no documented step creates an active signing key, so JWKS and token
  issuance have nothing to sign with after a stock install. Source: installer and guide. Owning phase:
  127 and 128. Record here whether one call or two were required (RESEARCH Open Question 1).
- `ADOPT-D07` — `step-04-migrate` / `step-05-verify`: the guide's documented migrate command runs none
  of Lockspire's migrations and exits zero; the release-safe form exists internally but adopters are
  given nothing. Record the concrete pending count the verifier reported. Source: installer. Owning
  phase: 127.
- `ADOPT-D08` — `step-06a-client`: the documented client-creation task declares a Mix requirement that
  replaces the default application start, so it never reaches a running repo. Source: library. Owning
  phase: 127.
- `ADOPT-D09` — `step-03c-resolver`: the resolver template hardcodes a login path that no generated
  auth host has. Source: installer. Owning phase: 127.
- `ADOPT-D10` — `step-06b-flow`: the shipped consent LiveView's decision forms emit no CSRF token, so
  the consent POST is rejected once the host adds the pipeline the LiveView itself needs. Source:
  library. Owning phase: 128 or 129. Record which fallback the driver had to use.
- `ADOPT-D11` — `step-03c-resolver`: the resolver seam raises by design with no worked example, no
  subject-reference format contract, and no claim-map shape anywhere in the guide. Source: guide.
  Owning phase: 128.
- `ADOPT-D12` — `step-06b-flow`: the reference demo shadows the consent route with its own host
  controller, so the shipped consent LiveView is never exercised by the repo's own demo. Source:
  reference demo. Owning phase: 129.
- `ADOPT-D14` — `step-06b-flow`: the generated login ignores a return-path query parameter because the
  redirect target is read from a session key only a GET through the authentication plug writes, so the
  interaction handoff cannot resume through it. Source: installer templates and guide. Owning phase:
  127 or 128.

Add two further entries that are not code defects but must not be silently absent:

- `ADOPT-D13` — residual nondeterminism: the harness does not pin Elixir or OTP and does not snapshot
  the generated app's lockfile, so a future run may resolve different versions. Source: environment.
  Owning phase: 130 or future. Workaround: none (D-15).
- A "not walked" section recording that guide §7 (upgrade) and §8 (device verification seam) were
  checked and deliberately not walked, with the reason for each, so a reader can tell a decision from
  an omission.

Add any defect the run surfaced that is not on this list. Record error classes, step IDs, and
`file:line` citations only — never a raw access token, ID token, authorization code, session cookie,
or the seeded password. Anything sensitive stays in the gitignored workdir.

Finally, reconcile: run the ledger contract test and fix mismatches by adding the missing ledger entry
or removing the phantom claim — never by deleting a marker from a workaround that is still in the
harness.
  </action>
  <verify>
    <automated>mix test test/lockspire/maintainer/ && test -s .planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lockspire/maintainer/` exits 0, including the reconciliation test
    - `.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md` exists, is non-empty, and parses into at least ten entries
    - Every entry carries an `ADOPT-D` ID, a walk step ID, a symptom, an underlying error, a source, an owning phase, and a workaround field
    - `grep -c 'ADOPT-D' .planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md` returns at least 10
    - `grep -Fq 'not walked' .planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md`
    - `! grep -Fq 'walk-adopter-password-2026' .planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md` — the exact literal plan 126-01 seeds as `LOCKSPIRE_WALK_PASSWORD`
    - `! grep -Eq 'Bearer [A-Za-z0-9._-]{20,}' .planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md`
    - The set of marker IDs in `scripts/maintainer/` equals the set of workaround IDs in the ledger, in both directions
    - `tmp/adopter-walk/` still contains `host_app/`, `server.log` and `.walk/steps/` after the run — the evidence tree was preserved with no flag passed
    - After the run, no walk-started `mix phx.server` process remains alive and the configured walk port is free, so a subsequent run's preflight passes rather than exiting as a prerequisite failure
    - Behavior: `mix adopter.walk` produced a report whose `Summary:` line and per-step attribution are quoted in the ledger's front matter and body
    - `git diff --exit-code -- lib/ priv/ docs/ examples/` reports no change — no defect was fixed in this phase
  </acceptance_criteria>
  <done>The walk has been run end to end, its report is captured, and a committed, fully attributed, secret-free defect ledger exists whose workaround claims reconcile exactly with the markers present in the harness.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Maintainer confirms the red walk and the ledger</name>
  <files>.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md</files>
  <read_first>
    - `.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md`
    - `.planning/ROADMAP.md` Phase 126 success criteria 1 through 5
  </read_first>
  <what-built>
    `mix adopter.walk` — a single maintainer command that generates a stock Phoenix 1.8.9 host app with
    Ecto, HTML, LiveView and `phx.gen.auth`, walks `docs/install-and-onboard.md` §1 through §6, boots
    the app, drives an authorization-code + PKCE flow, and asserts the issued access token at both
    `<mount>/userinfo` and a host-owned protected API route — ending in one attributed report and one
    pass/fail verdict. Alongside it, a committed defect ledger recording every break the walk surfaced,
    each attributed to its real source and to the phase that will fix it, with every harness workaround
    mechanically reconciled against the ledger.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md`. Confirm the
       front matter records a RED verdict with concrete PASS and FAIL counts, and that the entry count
       is not thin — a walk that surfaced only one or two defects means the harness stopped early, not
       that the adopter path is healthy.
    2. Spot-check three entries against their cited `file:line`. Confirm each symptom is something the
       walk observed rather than something read out of source, and that the source and owning-phase
       fields would let a Phase 127 or 128 planner scope work from them without re-investigating.
    3. Confirm the ledger contains no raw token, authorization code, cookie, or password.
    4. Run `mix test test/lockspire/maintainer/`. All contract tests should pass. This is the gate that
       proves no workaround was left silently in the harness.
    5. Re-run the walk with `mix adopter.walk --from-step 04` and confirm it resumes from
       `step-04-migrate` without regenerating the host app — this is ADOPT-03's resume claim and the
       acceptance path for roadmap criterion 2. Do not add `--keep`: the previous run already left the
       evidence tree intact and left the port free, and passing `--keep` would leave a server bound to
       that port so the next preflight rejects it.
    6. Confirm `mix ci` is still green.

    Expected outcome: `mix adopter.walk` exits non-zero with an attributed step failure. That is a
    passing phase. An exit code of 0, or an empty or near-empty ledger, is the failure to report here.
  </how-to-verify>
  <acceptance_criteria>
    - The maintainer confirms the ledger is complete enough to scope Phases 127, 128 and 129 from
    - The maintainer confirms no entry quotes secret material
    - `mix test test/lockspire/maintainer/` passes
    - `mix ci` passes
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe which entries are thin, misattributed, or missing</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols and paths created by this plan (new — exclude from drift verification):

- `.planning/phases/126-adopter-path-walk-defect-ledger/126-DEFECT-LEDGER.md` (new file, with YAML
  front-matter keys: verdict, pass count, fail count, walk date, resolved Elixir/OTP/PostgreSQL/`phx_new`
  versions)
- `test/lockspire/maintainer/defect_ledger_contract_test.exs` →
  `Lockspire.Maintainer.DefectLedgerContractTest` (new module)
- Ledger defect IDs: `ADOPT-D01` through `ADOPT-D14`, plus any the run surfaced beyond the seed list
- Ledger field vocabulary: source values `installer`, `generated scaffolding`, `guide`,
  `reference demo`, `library`, `environment`; owning-phase values `127`, `128`, `129`, `130`, `future`
- Ledger section: the "not walked" accounting for `docs/install-and-onboard.md` §7 and §8
</artifacts_this_phase_produces>

<flagged_assumptions>
- The seeded defect list is a floor, not a specification. Every entry must be confirmed empirically;
  any that does not reproduce is amended or dropped with a stated reason, and anything the run
  surfaced beyond the list is added. A ledger that matches the seed list exactly and adds nothing is
  itself a signal worth questioning.
- RESEARCH Open Question 3 (whether `tmp/adopter-walk/` should join the repo-hygiene artifact
  allowlist) is deliberately not acted on here — `scripts/maintainer/repo_hygiene_check.sh` must not
  be edited in this phase. Record it as a Phase 130 candidate in the ledger instead.
- Declined researcher recommendation, recorded deliberately: the researcher argued the migration fix
  should be sequenced ahead of or alongside the harness, since a harness built before that fix proves
  a path no adopter can walk. The roadmap locks "do not fix defects in this phase" and success
  criterion 5 exists precisely for this case, so the tension was considered and the workaround-plus-
  attribution route was chosen.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gitignored workdir → committed ledger | the only boundary across which secret material could escape into git |
| harness source → ledger claims | the marker set is the mechanical claim the ledger reconciles against |
| ledger → Phases 127, 128, 129 | attribution errors here become misdirected work downstream |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-126-31 | Information Disclosure | `126-DEFECT-LEDGER.md` | high | mitigate | record error classes, step IDs and `file:line` citations only; the contract test asserts the seeded password literal and bearer-token-shaped strings are absent; `server.log` is never quoted |
| T-126-32 | Repudiation | a workaround left silently in the harness | high | mitigate | two-way `MapSet` equality between harness markers and ledger workaround IDs, enforced by `defect_ledger_contract_test.exs` (D-40, D-41) |
| T-126-33 | Tampering | ledger location | medium | mitigate | `.planning/` rather than `docs/`, so the defect list is git-tracked but never published to adopters through the hexdocs extras list and never trips the docs gate |
| T-126-34 | Spoofing | a defect recorded but not observed | high | mitigate | every seeded entry must be confirmed empirically against the captured report; entries that did not reproduce are amended or dropped with a stated reason |
| T-126-35 | Repudiation | evidence destroyed before it is read | high | mitigate | the evidence tree survives unconditionally under D-20 with no flag required; an acceptance criterion asserts `tmp/adopter-walk/host_app/`, `server.log` and `.walk/steps/` survive the run |
| T-126-37 | Denial of Service | a walk-started server surviving the run and holding the walk port | medium | mitigate | the run passes no `--keep`, so the default teardown path applies; an acceptance criterion asserts the port is free afterwards, which is what lets the checkpoint's `--from-step` resume actually resume instead of failing preflight |
| T-126-36 | Tampering | a defect fixed instead of recorded | high | mitigate | `git diff --exit-code -- lib/ priv/ docs/ examples/` must report no change; only harness files may be modified in this phase |
</threat_model>

<verification>
- `mix test test/lockspire/maintainer/` exits 0 — all three contract test modules
- The ledger exists, is non-empty, and every entry carries all six D-37 fields plus its ID
- Marker↔ledger set equality holds in both directions
- No secret material appears in the ledger
- `tmp/adopter-walk/` survives the run with the generated host, server log and step markers intact, with no flag passed
- No walk-started server survives the run and the walk port is free, so `mix adopter.walk --from-step 04` resumes rather than exiting as a prerequisite failure
- `git diff --exit-code -- lib/ priv/ docs/ examples/` reports no change
- `mix ci` still passes
- No gate asserts `mix adopter.walk` exits 0, that it reached its final step, or that the ledger is empty
</verification>

<success_criteria>
- The walk has been run end to end and its report captured
- A committed defect ledger records every break the walk surfaced, attributed to source and owning phase
- Every harness workaround is a ledger entry, and every ledger workaround claim is a real marker
- Guide sections deliberately not walked, and residual nondeterminism, are explicitly accounted for
- No defect was fixed in this phase
- The maintainer has confirmed the red walk and the ledger at the checkpoint
</success_criteria>

<output>
Create `.planning/phases/126-adopter-path-walk-defect-ledger/126-06-SUMMARY.md` when done
</output>
