---
phase: 84-host-plug-pipeline-docs-and-milestone-closure
plan: 03
type: execute
wave: 2
depends_on:
  - 84-01
  - 84-02
files_modified:
  - test/integration/phase81_generated_host_route_protection_e2e_test.exs
autonomous: true
requirements:
  - NONCE-RS-02
  - NONCE-TRUTH-03
must_haves:
  truths:
    - "A generated host Phoenix API route protected by the shipped plug pipeline challenges a nonce-less DPoP proof with 401 plus DPoP-Nonce."
    - "Retrying the same generated host route with a fresh DPoP proof that includes the issued nonce succeeds."
    - "Repo-native tests prove `/token`, `/userinfo`, and generated-host protected-route nonce behavior together."
  artifacts:
    - path: test/integration/phase81_generated_host_route_protection_e2e_test.exs
      provides: generated-host end-to-end nonce retry proof on the canonical host route pipeline
  key_links:
    - from: test/integration/phase81_generated_host_route_protection_e2e_test.exs
      to: test/support/generated_host_app_web/router.ex
      via: `:lockspire_protected_api` pipeline
      pattern: "Lockspire.Plug.EnforceSenderConstraints"
    - from: test/integration/phase81_generated_host_route_protection_e2e_test.exs
      to: lib/lockspire/plug/require_token.ex
      via: observed HTTP challenge and retry contract
      pattern: "error=\"use_dpop_nonce\""
---

<objective>
Close the milestone with generated-host proof that the documented protected-route pipeline really supports nonce-backed DPoP retries.

Purpose: Implement D-13 through D-15 by proving the host seam end to end without duplicating the full protocol negative matrix.
Output: One generated-host E2E test that exercises nonce challenge and retry on the canonical pipeline, plus a verification command that runs the full repo-native nonce proof set.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-host-plug-pipeline-docs-and-milestone-closure/84-CONTEXT.md
@.planning/phases/84-host-plug-pipeline-docs-and-milestone-closure/84-RESEARCH.md
@test/support/generated_host_app_web/router.ex
@test/integration/phase81_generated_host_route_protection_e2e_test.exs
@test/lockspire/web/token_controller_test.exs
@test/lockspire/web/userinfo_controller_test.exs

<interfaces>
From test/support/generated_host_app_web/router.ex:
```elixir
pipeline :lockspire_protected_api do
  plug(Lockspire.Plug.VerifyToken, scopes: ["read:billing"], audience: "billing-api")
  plug(Lockspire.Plug.EnforceSenderConstraints,
    dpop_replay_store: GeneratedHostAppWeb.ProtectedApiReplayStore
  )
  plug(Lockspire.Plug.RequireToken)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the generated-host nonce challenge and retry path end to end</name>
  <files>test/integration/phase81_generated_host_route_protection_e2e_test.exs</files>
  <read_first>test/integration/phase81_generated_host_route_protection_e2e_test.exs, test/support/generated_host_app_web/router.ex, .planning/phases/84-host-plug-pipeline-docs-and-milestone-closure/84-CONTEXT.md, .planning/phases/84-host-plug-pipeline-docs-and-milestone-closure/84-RESEARCH.md</read_first>
  <behavior>
    - The canonical host pipeline first returns `401` with a DPoP challenge when a DPoP-bound token is presented without proof.
    - A nonce-less DPoP proof then returns `401`, `error="use_dpop_nonce"`, and a non-empty `DPoP-Nonce` header.
    - A second request with a fresh proof containing that nonce returns `200` on the same protected route.
  </behavior>
  <action>Extend `test/integration/phase81_generated_host_route_protection_e2e_test.exs` to make the nonce retry contract explicit on the generated-host billing route described in D-13 and D-14. Reuse the existing `:lockspire_protected_api` pipeline and billing route, assert the first proof-less DPoP request stays an `invalid_token` challenge, assert the nonce-less proof returns `error=\"use_dpop_nonce\"` with `DPoP-Nonce`, then assert a fresh proof containing the issued nonce succeeds with `200`. Do not add a broad replay / `ath` / binding matrix here per D-15.</action>
  <acceptance_criteria>`test/integration/phase81_generated_host_route_protection_e2e_test.exs` contains `error=\"use_dpop_nonce\"`; `test/integration/phase81_generated_host_route_protection_e2e_test.exs` contains `get_resp_header(challenge_conn, \"dpop-nonce\")`; `test/integration/phase81_generated_host_route_protection_e2e_test.exs` contains `success_conn.status == 200`; `test/integration/phase81_generated_host_route_protection_e2e_test.exs` still references `Lockspire.Plug.VerifyToken`, `Lockspire.Plug.EnforceSenderConstraints`, and `Lockspire.Plug.RequireToken` via the generated-host router context.</acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/web/token_controller_test.exs test/lockspire/web/userinfo_controller_test.exs test/integration/phase81_generated_host_route_protection_e2e_test.exs</automated>
  </verify>
  <done>The repo-native proof set now includes `/token`, `/userinfo`, and generated-host host-route nonce retry coverage, with the host seam closed end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated test client -> host Phoenix API route | The end-to-end proof exercises untrusted token and DPoP input through the real generated-host route pipeline |
| generated-host router -> Lockspire plugs | The test validates the exact `VerifyToken -> EnforceSenderConstraints -> RequireToken` ordering contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-07 | Repudiation | `test/integration/phase81_generated_host_route_protection_e2e_test.exs` | mitigate | Assert the exact nonce challenge and retry behavior on the generated-host route so the milestone claim remains backed by executable proof. |
| T-84-08 | Denial of Service | generated-host protected-route retry flow | accept | The plan proves only one nonce retry path; exhaustive replay and proof-flood coverage remains in lower-level protocol tests and is intentionally not duplicated at E2E level per D-15. |
| T-84-09 | Elevation of Privilege | generated-host route pipeline ordering | mitigate | Keep the E2E test mounted on the canonical `VerifyToken -> EnforceSenderConstraints -> RequireToken` pipeline so regressions in plug order break the milestone-closing proof. |
</threat_model>

<verification>
Run `MIX_ENV=test mix test test/lockspire/web/token_controller_test.exs test/lockspire/web/userinfo_controller_test.exs test/integration/phase81_generated_host_route_protection_e2e_test.exs`.
</verification>

<success_criteria>
The generated-host protected-route proof shows a nonce challenge and successful retry on the documented pipeline, and the full repo-native nonce proof set remains green for `/token`, `/userinfo`, and the host route.
</success_criteria>

<output>
After completion, create `.planning/phases/84-host-plug-pipeline-docs-and-milestone-closure/84-03-SUMMARY.md`
</output>
