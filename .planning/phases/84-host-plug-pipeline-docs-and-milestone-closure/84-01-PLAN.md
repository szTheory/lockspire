---
phase: 84-host-plug-pipeline-docs-and-milestone-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/web/protected_resource_challenge.ex
  - lib/lockspire/plug/enforce_sender_constraints.ex
  - lib/lockspire/plug/require_token.ex
  - lib/lockspire/web/controllers/userinfo_controller.ex
  - test/lockspire/plug/enforce_sender_constraints_test.exs
  - test/lockspire/plug/require_token_test.exs
autonomous: true
requirements:
  - NONCE-RS-01
  - NONCE-RS-03
must_haves:
  truths:
    - "Host Phoenix API routes protected by the shipped plug pipeline return 401 DPoP nonce challenges when a DPoP proof is present but lacks a valid resource-server nonce."
    - "The retriable nonce failure stays in the authentication bucket and does not turn into 403 insufficient_scope or generic bearer invalid_token."
    - "Userinfo and host-route protected-resource adapters emit the same DPoP nonce transport shape."
  artifacts:
    - path: lib/lockspire/web/protected_resource_challenge.ex
      provides: shared protected-resource challenge rendering helpers
    - path: lib/lockspire/plug/enforce_sender_constraints.ex
      provides: host-route sender-constraint request assembly with explicit secret_key_base handoff
    - path: lib/lockspire/plug/require_token.ex
      provides: strict host-route DPoP-aware HTTP boundary
    - path: lib/lockspire/web/controllers/userinfo_controller.ex
      provides: Lockspire-owned protected-resource adapter using the shared transport helper
  key_links:
    - from: lib/lockspire/plug/enforce_sender_constraints.ex
      to: lib/lockspire/protocol/protected_resource_dpop.ex
      via: ProtectedResourceDPoP.validate_access/2 request opts
      pattern: "secret_key_base: conn.secret_key_base"
    - from: lib/lockspire/plug/require_token.ex
      to: lib/lockspire/web/protected_resource_challenge.ex
      via: shared DPoP challenge helper
      pattern: "ProtectedResourceChallenge"
    - from: lib/lockspire/web/controllers/userinfo_controller.ex
      to: lib/lockspire/web/protected_resource_challenge.ex
      via: shared DPoP challenge helper
      pattern: "ProtectedResourceChallenge"
---

<objective>
Align the host Phoenix protected-route plug adapters with the shipped resource-server nonce contract.

Purpose: Implement D-01 through D-12 without widening the product surface or duplicating DPoP validation logic.
Output: One transport-only protected-resource challenge helper, explicit secret-key handoff in the host plug path, and focused plug tests proving unchanged 401/403 semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/84-host-plug-pipeline-docs-and-milestone-closure/84-CONTEXT.md
@.planning/phases/84-host-plug-pipeline-docs-and-milestone-closure/84-RESEARCH.md
@.planning/phases/83-lockspire-owned-dpop-endpoint-adoption/83-02-SUMMARY.md
@lib/lockspire/protocol/protected_resource_dpop.ex
@lib/lockspire/plug/enforce_sender_constraints.ex
@lib/lockspire/plug/require_token.ex
@lib/lockspire/web/controllers/userinfo_controller.ex
@test/lockspire/plug/enforce_sender_constraints_test.exs
@test/lockspire/plug/require_token_test.exs

<interfaces>
From lib/lockspire/protocol/protected_resource_dpop.ex:
```elixir
@spec validate_access(map(), map()) :: {:ok, DPoP.t()} | {:error, Error.t()}
```

From lib/lockspire/plug/require_token.ex:
```elixir
case conn.assigns[:access_token] do
  %AccessToken{error: nil, claims: claims} when not is_nil(claims) -> conn
  %AccessToken{error: error} when is_map(error) -> handle_structured_error(conn, error)
end
```

From lib/lockspire/web/controllers/userinfo_controller.ex:
```elixir
{:error, %Error{} = error} ->
  conn
  |> put_cache_headers()
  |> put_dpop_nonce(error)
  |> put_www_authenticate(error)
  |> put_status(error.status)
  |> json(UserinfoJSON.error_response(error))
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract shared protected-resource challenge transport helpers</name>
  <files>lib/lockspire/web/protected_resource_challenge.ex, lib/lockspire/plug/require_token.ex, lib/lockspire/web/controllers/userinfo_controller.ex</files>
  <read_first>lib/lockspire/web/controllers/userinfo_controller.ex, lib/lockspire/plug/require_token.ex, lib/lockspire/protocol/protected_resource_dpop.ex, .planning/phases/84-host-plug-pipeline-docs-and-milestone-closure/84-CONTEXT.md</read_first>
  <action>Create `Lockspire.Web.ProtectedResourceChallenge` as a transport-only helper per D-08, D-10, and D-11. Move the duplicated `DPoP-Nonce` header exposure and `WWW-Authenticate` DPoP formatting out of `Lockspire.Web.UserinfoController` and `Lockspire.Plug.RequireToken` into that helper, while keeping validation in `Lockspire.Protocol.ProtectedResourceDPoP`. Preserve `Lockspire.Plug.RequireToken` as the single strict host-route HTTP boundary per D-02 and D-09, keep the `401` DPoP nonce challenge body as `%{error: "use_dpop_nonce", error_description: "Resource server requires nonce in DPoP proof"}`, and keep `403 insufficient_scope` behavior untouched.</action>
  <acceptance_criteria>`lib/lockspire/web/protected_resource_challenge.ex` defines a `Lockspire.Web.ProtectedResourceChallenge` module; `lib/lockspire/plug/require_token.ex` contains `ProtectedResourceChallenge`; `lib/lockspire/web/controllers/userinfo_controller.ex` contains `ProtectedResourceChallenge`; `lib/lockspire/plug/require_token.ex` still contains `handle_insufficient_scope`; `lib/lockspire/web/controllers/userinfo_controller.ex` no longer defines local `put_dpop_nonce` and `expose_header` helpers.</acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/plug/require_token_test.exs test/lockspire/web/userinfo_controller_test.exs</automated>
  </verify>
  <done>`/userinfo` and host-route DPoP nonce responses share one transport helper, while the strict/soft adapter boundary remains unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pass endpoint secret material through the host plug path and pin unchanged semantics</name>
  <files>lib/lockspire/plug/enforce_sender_constraints.ex, test/lockspire/plug/enforce_sender_constraints_test.exs, test/lockspire/plug/require_token_test.exs</files>
  <read_first>lib/lockspire/plug/enforce_sender_constraints.ex, test/lockspire/plug/enforce_sender_constraints_test.exs, test/lockspire/plug/require_token_test.exs, lib/lockspire/protocol/protected_resource_dpop.ex, .planning/phases/84-host-plug-pipeline-docs-and-milestone-closure/84-CONTEXT.md</read_first>
  <behavior>
    - Missing resource-server nonce on a DPoP-bound host-route request records `reason_code: :missing_dpop_nonce` plus a non-empty `dpop_nonce`.
    - `RequireToken` renders nonce retries as `401` DPoP challenges with `DPoP-Nonce` and `Access-Control-Expose-Headers: DPoP-Nonce, WWW-Authenticate`.
    - Audience mismatches remain `401 invalid_token` and insufficient-scope failures remain `403 insufficient_scope`.
  </behavior>
  <action>Update `Lockspire.Plug.EnforceSenderConstraints` so the request passed to `ProtectedResourceDPoP.validate_access/2` includes `secret_key_base: conn.secret_key_base` per D-12, without moving any nonce issuance logic out of `ProtectedResourceDPoP`. Expand `test/lockspire/plug/enforce_sender_constraints_test.exs` and `test/lockspire/plug/require_token_test.exs` to assert typed nonce propagation, exact header exposure, and unchanged bearer / MTLS / `401` vs `403` semantics per D-05, D-06, and D-07.</action>
  <acceptance_criteria>`lib/lockspire/plug/enforce_sender_constraints.ex` contains `secret_key_base: conn.secret_key_base`; `test/lockspire/plug/enforce_sender_constraints_test.exs` contains `:missing_dpop_nonce`; `test/lockspire/plug/require_token_test.exs` contains `DPoP-Nonce, WWW-Authenticate`; `test/lockspire/plug/require_token_test.exs` still contains `insufficient_scope` and `status == 403` assertions.</acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/plug/enforce_sender_constraints_test.exs test/lockspire/plug/require_token_test.exs test/lockspire/web/userinfo_controller_test.exs</automated>
  </verify>
  <done>The host plug path can issue resource-server nonces with explicit endpoint secret material, and the focused plug proofs pin the unchanged challenge taxonomy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> host Phoenix protected route | Untrusted access token and DPoP proof enter the host-owned API route pipeline |
| soft plugs -> strict plug boundary | Structured sender-constraint failures cross from validation plugs into `RequireToken` response rendering |
| protocol -> HTTP adapters | Typed protected-resource errors are translated into HTTP headers and JSON bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Tampering | `lib/lockspire/plug/enforce_sender_constraints.ex` | mitigate | Pass `secret_key_base: conn.secret_key_base` into `ProtectedResourceDPoP.validate_access/2` so nonce issuance and validation use the explicit endpoint secret per D-12. |
| T-84-02 | Information Disclosure | `lib/lockspire/web/protected_resource_challenge.ex` | mitigate | Keep the helper transport-only and emit only `WWW-Authenticate`, `DPoP-Nonce`, and `Access-Control-Expose-Headers` without leaking token internals or validation details. |
| T-84-03 | Elevation of Privilege | `lib/lockspire/plug/require_token.ex` | mitigate | Preserve `403 insufficient_scope` and bearer-vs-DPoP challenge branching so nonce retries do not bypass route restriction or sender-constraint enforcement. |
</threat_model>

<verification>
Run `MIX_ENV=test mix test test/lockspire/plug/enforce_sender_constraints_test.exs test/lockspire/plug/require_token_test.exs test/lockspire/web/userinfo_controller_test.exs`.
</verification>

<success_criteria>
`EnforceSenderConstraints` remains a soft validator, `RequireToken` remains the strict host-route boundary, `/userinfo` and host routes share one protected-resource challenge helper, and the focused plug tests prove nonce-aware `401` handling without changing other protected-route semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/84-host-plug-pipeline-docs-and-milestone-closure/84-01-SUMMARY.md`
</output>
