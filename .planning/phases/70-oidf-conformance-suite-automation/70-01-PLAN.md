---
phase: 70-oidf-conformance-suite-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/conformance/run_fapi2_suite.sh
  - .github/workflows/oidf-conformance.yml
  - docs/maintainer-conformance.md
autonomous: true
requirements:
  - CONF-04
must_haves:
  truths:
    - Developer can execute FAPI 2.0 OIDF conformance suite locally via a single script.
    - The CI pipeline executes the FAPI 2.0 conformance suite automatically on push/PR.
    - Conformance artifacts (logs, exports) are saved for debugging.
  artifacts:
    - path: "scripts/conformance/run_fapi2_suite.sh"
      provides: "FAPI 2.0 suite execution"
      min_lines: 100
    - path: ".github/workflows/oidf-conformance.yml"
      provides: "CI job for FAPI 2.0"
      contains: "repo-native-fapi2"
  key_links:
    - from: ".github/workflows/oidf-conformance.yml"
      to: "scripts/conformance/run_fapi2_suite.sh"
      via: "CI step"
      pattern: "run: bash scripts/conformance/run_fapi2_suite.sh"
    - from: "scripts/conformance/run_fapi2_suite.sh"
      to: "scripts/conformance/fapi2-plan.json"
      via: "file read"
      pattern: "fapi2-plan.json"
---

<objective>
Integrate the official OpenID Foundation Conformance Suite into the automated CI pipeline for FAPI 2.0 execution.

Purpose: Automate proof of Lockspire's FAPI 2.0 compliance and close the historically deferred CONF-04 requirement without manual maintainer intervention.
Output: A new runner script, updated CI workflow, and updated maintainer docs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/conformance/run_phase37_suite.sh
@.github/workflows/oidf-conformance.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FAPI 2.0 Conformance Suite Runner Script</name>
  <files>scripts/conformance/run_fapi2_suite.sh</files>
  <action>
    Create a new script named `scripts/conformance/run_fapi2_suite.sh`.
    Duplicate the logic of `scripts/conformance/run_phase37_suite.sh` but tailor it for FAPI 2.0:
    1. Update variables to point to FAPI 2.0 paths:
       - `PLAN_PATH="${ROOT_DIR}/scripts/conformance/fapi2-plan.json"`
       - `ARTIFACT_DIR="${LOCKSPIRE_FAPI2_ARTIFACT_DIR:-${ROOT_DIR}/.artifacts/conformance/fapi2}"`
       - Update other Phase 37 specific variable names to `FAPI2`.
    2. Inside `start_local_fixture()`, ensure Lockspire starts with the FAPI 2.0 security profile enforced (e.g., configuring `Application.put_env(:lockspire, Lockspire.Admin, security_profile: :fapi_2_0_security)` or whatever equivalent is used).
    3. Make sure the script has execute permissions (`chmod +x`).
  </action>
  <verify>
    <automated>test -x scripts/conformance/run_fapi2_suite.sh && grep -q "fapi2-plan.json" scripts/conformance/run_fapi2_suite.sh</automated>
  </verify>
  <done>Executable script exists that sets up the local fixture for FAPI 2.0 and processes `fapi2-plan.json`.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate FAPI 2.0 Runner into CI</name>
  <files>.github/workflows/oidf-conformance.yml</files>
  <action>
    Modify `.github/workflows/oidf-conformance.yml` to include a new job for the FAPI 2.0 suite.
    Add a job named `repo-native-fapi2` based on the existing `repo-native-phase37` structure.
    It must:
    - Checkout code and set up Elixir/Erlang.
    - Run the postgres service and configure DB environment variables.
    - Run `bash scripts/conformance/run_fapi2_suite.sh`.
    - Upload artifacts from `.artifacts/conformance/fapi2` with the name `fapi2-conformance` (use `if: always()`).
  </action>
  <verify>
    <automated>grep -q "repo-native-fapi2" .github/workflows/oidf-conformance.yml && grep -q "scripts/conformance/run_fapi2_suite.sh" .github/workflows/oidf-conformance.yml</automated>
  </verify>
  <done>CI workflow includes an automated test run for FAPI 2.0.</done>
</task>

<task type="auto">
  <name>Task 3: Update Maintainer Documentation</name>
  <files>docs/maintainer-conformance.md</files>
  <action>
    Update `docs/maintainer-conformance.md` to reflect the new automated runner.
    Document the FAPI 2.0 script in the Local Testing Lane or equivalent section.
    Provide instructions on how maintainers can run `scripts/conformance/run_fapi2_suite.sh` locally.
  </action>
  <verify>
    <automated>grep -q "run_fapi2_suite.sh" docs/maintainer-conformance.md</automated>
  </verify>
  <done>Documentation explicitly covers the new FAPI 2.0 script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI -> OIDF Docker Suite | The CI runner launches Docker containers that issue HTTP requests against the local test fixture. The suite is a trusted official container. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Information Disclosure | `run_fapi2_suite.sh` logs | accept | Logs do not contain PII or production secrets; they contain ephemeral test credentials only. |
| T-70-02 | Elevation of Privilege | Local Test Fixture | accept | The fixture only runs locally and in ephemeral CI environments on dummy data. |
</threat_model>

<verification>
Check that the FAPI 2.0 script exists, is executable, and is integrated into the CI workflow correctly. Check that documentation exists.
</verification>

<success_criteria>
`run_fapi2_suite.sh` exists and is executed by `.github/workflows/oidf-conformance.yml`. The test setup aligns with `CONF-04`.
</success_criteria>

<output>
After completion, create `.planning/phases/70-oidf-conformance-suite-automation/70-01-SUMMARY.md`
</output>