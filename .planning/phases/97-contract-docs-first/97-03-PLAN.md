---
phase: 97-contract-docs-first
plan: 03
type: execute
wave: 2
depends_on:
  - 97-01
files_modified:
  - docs/supported-surface.md
  - docs/saas-adoption-recipe.md
autonomous: true
requirements:
  - DOCS-02
  - RECIPE-01
tags:
  - phase-97
  - docs-02
  - supported-surface
  - saas-recipe-cross-link
  - recipe-01-deference
user_setup: []

must_haves:
  truths:
    - "An adopter reading `docs/supported-surface.md` finds a new `## Explicit non-goals for host-API route protection` subsection sitting between the existing `## Explicitly out of scope` list (ends at L138) and the existing `## Trust posture` section (starts at L139 today)."
    - "The new subsection carries four bullets — one per D-09 rejected pattern — each pairing the non-goal phrase with a brief rejection-rationale clause sourced from `.planning/REQUIREMENTS.md:103-110`."
    - "`docs/saas-adoption-recipe.md:50` no longer restates the three plug names `Lockspire.Plug.VerifyToken`, `Lockspire.Plug.EnforceSenderConstraints`, `Lockspire.Plug.RequireToken` in concatenated form; it carries a cross-link to `docs/protect-phoenix-api-routes.md` instead (D-11)."
    - "Every Phase 92 substring in `assert_advanced_setup_support_contract!/1` (the 14 substrings across the two `assert_includes_all` calls) survives intact in `docs/supported-surface.md`."
    - "After this plan plus Plan 02, `mix test test/lockspire/release_readiness_contract_test.exs:642` exits 0."
  artifacts:
    - path: "docs/supported-surface.md"
      provides: "New `## Explicit non-goals for host-API route protection` subsection per D-09; remains the canonical public support contract per Phase 92 D-01"
      contains: "## Explicit non-goals for host-API route protection"
    - path: "docs/saas-adoption-recipe.md"
      provides: "Cross-link replacement for the previous plug-name restatement at L50 per D-11; closes the silent fifth-restatement drift class"
      contains: "protect-phoenix-api-routes.md"
  key_links:
    - from: "docs/supported-surface.md (new subsection)"
      to: ".planning/REQUIREMENTS.md:103-110"
      via: "rejection-rationale phrases sourced from the v1.27 Out of Scope table"
      pattern: "no introspection-at-the-RS as the host-API seam.*no auto-detection of token shape.*no dual-verifier dispatcher.*no RAR enforcement at the RS plug"
    - from: "docs/saas-adoption-recipe.md:50"
      to: "docs/protect-phoenix-api-routes.md (canonical contract page)"
      via: "markdown link"
      pattern: "protect-phoenix-api-routes\\.md"
---

<objective>
Land the two adjacent-doc edits per D-09 (DOCS-02 subsection in `docs/supported-surface.md`) and D-11 (saas-adoption-recipe.md cross-link replacement). Both edits are additive/replacement at single insertion points; neither touches the canonical pipeline block.

Purpose: DOCS-02 makes the v1.27 non-goals explicit in the canonical public support contract per Phase 92 D-01. D-11 closes the silent fifth-restatement drift class by replacing the saas-adoption-recipe L50 plug-name list with a cross-link to the canonical doc page (now authoritative after Plan 02).

Output: Two modified doc files. After this plan plus Plan 02, the release-readiness contract test turns GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/97-contract-docs-first/97-CONTEXT.md
@.planning/phases/97-contract-docs-first/97-RESEARCH.md
@.planning/phases/97-contract-docs-first/97-PATTERNS.md
@.planning/phases/97-contract-docs-first/97-VALIDATION.md
@.planning/phases/97-contract-docs-first/97-01-SUMMARY.md
@.planning/phases/92-advanced-setup-support-truth/92-CONTEXT.md

<interfaces>
<!-- DOCS-02 four non-goal patterns + paired rejection rationales (verbatim source: 97-CONTEXT.md D-09, derived from REQUIREMENTS.md:103-110) -->

The four non-goal bullets (each is one line in the markdown source; each contains a non-goal phrase + an em-dash + a rejection-rationale clause):

```
- no introspection-at-the-RS as the host-API seam — recreates gateway/CIAM productization the canon explicitly rejects
- no auto-detection of token shape — documented ecosystem footgun (Ory oathkeeper #257 class)
- no dual-verifier dispatcher — hides operator-visible complexity inside the library
- no RAR enforcement at the RS plug — RAR claims surface via `conn.assigns.access_token` for host-owned enforcement
```

Each bullet contains TWO substrings that Plan 01's `assert_advanced_setup_support_contract!/1` extension asserts: the non-goal phrase before the em-dash, and a fragment of the rejection rationale after the em-dash. Both substrings must appear verbatim in the bullet (the helper does case-sensitive `String.contains?/2` per `assert_includes_all/2`).

Insertion point in `docs/supported-surface.md` (verified by direct file read 2026-05-27):
- L113: `## Explicitly out of scope` heading (PRESERVE)
- L117-137: out-of-scope bullets (PRESERVE — Phase 92 substrings live in lines 120, 123, 129)
- L138: blank line (PRESERVE)
- L139: `## Trust posture` heading (PRESERVE)

Insert the new `## Explicit non-goals for host-API route protection` subsection BETWEEN the out-of-scope list's final bullet (L137) and the `## Trust posture` heading (L139). Surrounding blank lines per standard markdown rendering: one blank line above the new `##` heading, one blank line below the final new bullet, then `## Trust posture`.

Current `docs/saas-adoption-recipe.md:50` line (verified):
`- If exposing API routes, protect one host route with backtick-Lockspire.Plug.VerifyToken-backtick, backtick-Lockspire.Plug.EnforceSenderConstraints-backtick, and backtick-Lockspire.Plug.RequireToken-backtick.`

Replacement line (D-11):
`- If exposing API routes, follow the canonical pipeline in [backtick-docs/protect-phoenix-api-routes.md-backtick](protect-phoenix-api-routes.md).`

Phase 92 substrings in `assert_advanced_setup_support_contract!/1` that MUST survive (verified at test/support/advanced_setup_support_truth.ex:4-29; appending after L138 does not touch them):

```
First assert_includes_all/2 call (L5-20):
- "Bounded reactive remote-backtick-jwks_uri-backtick rollover support"
- "forces one refresh when verification indicates stale or unknown key material"
- "preserves the last known good cache entry when refresh fails"
- "fails the current authentication attempt closed"
- "Mutual TLS for confidential-client authentication and certificate-bound access tokens through exactly two shipped extractor patterns"
- "Lockspire.MTLS.Extractor.CowboyDirect"
- "Lockspire.MTLS.Extractor.ProxyHeader"
- "host app or deployment owns TLS termination"
- "Host Phoenix API route protection with the canonical shipped pipeline"
- "Lockspire.Plug.VerifyToken"
- "Lockspire.Plug.EnforceSenderConstraints"
- "Lockspire.Plug.RequireToken"
- "RP-initiated logout plus logout propagation from the protocol-owned backtick-/end_session/complete-backtick seam"
- "durable back-channel enqueueing with Oban and Req, plus front-channel iframe cleanup as best effort browser choreography only"

Second assert_includes_all/2 call (L22-28):
- "Generic API gateway, service-mesh, or third-party issuer protected-resource middleware remains out of scope"
- "broader resource-server integration beyond Lockspire-owned backtick-/token-backtick, Lockspire-owned protected resources, and the shipped Phoenix plug pipeline"
- "Arbitrary custom backtick-Lockspire.MTLS.Extractor-backtick implementations are not first-class peers"
- "Dynamic Client Registration does not add a new logout runtime; it only manages the existing logout propagation metadata"
- "proves front-channel logout success remotely"
```
(Note: `backtick-X-backtick` in the listing above represents a literal backtick character `\`` wrapping `X` — the substring lists in the actual helper use real backticks; this plan's interface block escapes them to avoid YAML/markdown collision.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert the DOCS-02 non-goals subsection into docs/supported-surface.md after the existing out-of-scope list</name>
  <files>docs/supported-surface.md</files>
  <read_first>
    - docs/supported-surface.md (the file being modified — read the L100-145 range carefully; structure verified at the boundary between out-of-scope list and trust-posture section)
    - .planning/phases/97-contract-docs-first/97-PATTERNS.md (the `docs/supported-surface.md` row — exact bullet wording, exact insertion point)
    - .planning/phases/97-contract-docs-first/97-CONTEXT.md decision D-09 (verbatim source of the four bullets and rejection-rationale phrasing)
    - .planning/REQUIREMENTS.md lines 103-110 (the Out of Scope table that is the ultimate source of the rejection-rationale clauses)
    - .planning/phases/92-advanced-setup-support-truth/92-CONTEXT.md D-01 (canonical-authority hierarchy — Phase 97 places DOCS-02 INSIDE supported-surface.md, NOT in a parallel claim elsewhere)
    - test/support/advanced_setup_support_truth.ex (post-Plan-01 state — the 8 new D-09 substrings in `assert_advanced_setup_support_contract!/1`'s second `assert_includes_all/2` call are what this task must satisfy)
  </read_first>
  <behavior>
    - After this edit, `docs/supported-surface.md` carries a new H2 subsection `## Explicit non-goals for host-API route protection` immediately AFTER the existing `## Explicitly out of scope` list and BEFORE `## Trust posture`.
    - The new subsection contains exactly four bullets, each combining one of the four D-09 non-goal phrases with its rejection-rationale clause (em-dash separator).
    - Every D-09 substring asserted by Plan 01's helper extension is now present verbatim in this file.
    - Every Phase 92 substring listed in the existing out-of-scope list (lines 120, 123, 129 and the broader first-list substrings) is preserved (the insertion is strictly AFTER the existing list).
    - `## Trust posture` heading and its body are unchanged.
  </behavior>
  <action>
    Edit `docs/supported-surface.md` in place. Make exactly one insertion: a new H2 subsection between the existing out-of-scope list (ends at L138) and the existing `## Trust posture` heading (currently L139).

    Insertion content (planner's discretion on exact bullet wording per CONTEXT.md Claude's Discretion bullet 2, BUT each bullet MUST contain the verbatim non-goal phrase AND a verbatim rejection-rationale fragment from D-09):

    Line A: blank line
    Line B: H2 heading reading exactly `## Explicit non-goals for host-API route protection`
    Line C: blank line
    Line D: a one-line lead paragraph (recommended verbatim: `These are explicitly out of scope for the Lockspire-owned host-API protected-route seam under v1.27.`) — this paragraph is for human readers and is not asserted by any test; planner may choose a different one-line lead, but the four bullets that follow are load-bearing.
    Line E: blank line
    Line F (bullet 1): `- no introspection-at-the-RS as the host-API seam — recreates gateway/CIAM productization the canon explicitly rejects`
    Line G (bullet 2): `- no auto-detection of token shape — documented ecosystem footgun (Ory oathkeeper #257 class)`
    Line H (bullet 3): `- no dual-verifier dispatcher — hides operator-visible complexity inside the library`
    Line I (bullet 4): `- no RAR enforcement at the RS plug — RAR claims surface via backtick-conn.assigns.access_token-backtick for host-owned enforcement` (where `backtick-...-backtick` denotes a real Markdown inline-code span wrapping `conn.assigns.access_token` — the substring `\`conn.assigns.access_token\`` must appear verbatim in this bullet so Plan 01's helper assertion matches)
    Line J: blank line
    Line K: `## Trust posture` (the EXISTING heading, unchanged — this line was at L139 in the pre-edit state)

    Important — punctuation specifics:
    - The em-dash separator is the long em-dash character `—` (U+2014). Verified that the existing supported-surface.md uses the long em-dash in similar prose (e.g., L137 ends with `— Lockspire does not treat ...`).
    - The inline-code spans around `conn.assigns.access_token` MUST use real backtick characters (Markdown inline-code syntax). The helper assertion at Plan 01 checks the literal substring including the surrounding backticks.

    Do NOT modify any existing line in this file. Do NOT touch the `## Explicitly out of scope` list above the insertion point (Phase 92 substrings live there). Do NOT modify `## Trust posture` below. Do NOT add new content elsewhere in the file.

    No fenced code blocks in this action — the bullets are inline-prose markdown above; the executor reads them directly.
  </action>
  <verify>
    <automated>grep -c "## Explicit non-goals for host-API route protection" docs/supported-surface.md ; grep -c "no introspection-at-the-RS as the host-API seam" docs/supported-surface.md ; grep -c "recreates gateway/CIAM productization the canon explicitly rejects" docs/supported-surface.md ; grep -c "no auto-detection of token shape" docs/supported-surface.md ; grep -c "documented ecosystem footgun" docs/supported-surface.md ; grep -c "no dual-verifier dispatcher" docs/supported-surface.md ; grep -c "hides operator-visible complexity inside the library" docs/supported-surface.md ; grep -c "no RAR enforcement at the RS plug" docs/supported-surface.md ; grep -c "Generic API gateway, service-mesh, or third-party issuer protected-resource middleware remains out of scope" docs/supported-surface.md ; grep -c "proves front-channel logout success remotely" docs/supported-surface.md ; grep -c "## Trust posture" docs/supported-surface.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Explicit non-goals for host-API route protection" docs/supported-surface.md` returns exactly `1`.
    - `grep -c "no introspection-at-the-RS as the host-API seam" docs/supported-surface.md` returns exactly `1`.
    - `grep -c "recreates gateway/CIAM productization the canon explicitly rejects" docs/supported-surface.md` returns exactly `1`.
    - `grep -c "no auto-detection of token shape" docs/supported-surface.md` returns exactly `1`.
    - `grep -c "documented ecosystem footgun" docs/supported-surface.md` returns exactly `1`.
    - `grep -c "no dual-verifier dispatcher" docs/supported-surface.md` returns exactly `1`.
    - `grep -c "hides operator-visible complexity inside the library" docs/supported-surface.md` returns exactly `1`.
    - `grep -c "no RAR enforcement at the RS plug" docs/supported-surface.md` returns exactly `1`.
    - `grep -c "RAR claims surface via .conn.assigns.access_token. for host-owned enforcement" docs/supported-surface.md` returns exactly `1`.
    - `grep -c "Generic API gateway, service-mesh, or third-party issuer protected-resource middleware remains out of scope" docs/supported-surface.md` returns exactly `1` (Phase 92 substring preserved).
    - `grep -c "proves front-channel logout success remotely" docs/supported-surface.md` returns exactly `1` (Phase 92 substring preserved).
    - `grep -c "## Trust posture" docs/supported-surface.md` returns exactly `1` (heading not duplicated).
  </acceptance_criteria>
  <done>
    `docs/supported-surface.md` carries the new four-bullet `## Explicit non-goals for host-API route protection` subsection between the out-of-scope list and the trust-posture section; every Phase 92 substring survives.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace the plug-name restatement at docs/saas-adoption-recipe.md:50 with a cross-link to the canonical doc</name>
  <files>docs/saas-adoption-recipe.md</files>
  <read_first>
    - docs/saas-adoption-recipe.md (the file being modified — read the L40-55 range to confirm the L50 bullet wording; verified 2026-05-27 to read: `- If exposing API routes, protect one host route with backtick-Lockspire.Plug.VerifyToken-backtick, backtick-Lockspire.Plug.EnforceSenderConstraints-backtick, and backtick-Lockspire.Plug.RequireToken-backtick.`)
    - .planning/phases/97-contract-docs-first/97-PATTERNS.md (the `docs/saas-adoption-recipe.md` row — Pattern F "Cross-Link-Replaces-Restatement")
    - .planning/phases/97-contract-docs-first/97-CONTEXT.md decision D-11 (closes the silent fifth-restatement drift class)
    - docs/protect-phoenix-api-routes.md (the cross-link target, now post-Plan-02 authoritative — confirm the relative link `protect-phoenix-api-routes.md` resolves)
  </read_first>
  <behavior>
    - After this edit, `docs/saas-adoption-recipe.md` no longer contains the concatenated three-plug-name restatement at L50.
    - The replacement bullet is a one-line cross-link to `docs/protect-phoenix-api-routes.md` per D-11; the cross-link uses standard Markdown link syntax `[text](relative-path)` and resolves to the sibling doc.
    - No other line in the file is modified. The total file length decreases or stays the same (one line out, one line in).
    - `git grep -nE 'Lockspire\.Plug\.VerifyToken.*Lockspire\.Plug\.EnforceSenderConstraints.*Lockspire\.Plug\.RequireToken' docs/saas-adoption-recipe.md` returns zero matches.
    - The cross-link's `protect-phoenix-api-routes.md` filename appears at least once in the file (as the link target).
  </behavior>
  <action>
    Edit `docs/saas-adoption-recipe.md` in place. Make exactly one line-replacement at L50.

    Find the existing L50 bullet (verified content): `- If exposing API routes, protect one host route with backtick-Lockspire.Plug.VerifyToken-backtick, backtick-Lockspire.Plug.EnforceSenderConstraints-backtick, and backtick-Lockspire.Plug.RequireToken-backtick.` (where `backtick-X-backtick` denotes a real Markdown inline-code span wrapping `X`).

    Replace it with (planner's discretion on exact wording per CONTEXT.md Claude's Discretion; load-bearing requirement is "zero three-plug-name restatement at this position; cross-link to docs/protect-phoenix-api-routes.md present"; recommended verbatim): `- If exposing API routes, follow the canonical pipeline in [backtick-docs/protect-phoenix-api-routes.md-backtick](protect-phoenix-api-routes.md).` (where `backtick-X-backtick` denotes a real Markdown inline-code span wrapping `docs/protect-phoenix-api-routes.md`).

    Important — Markdown specifics:
    - The link text uses an inline-code span (backticks) to match the existing doc style for cross-links (verified at L62 of `docs/protect-phoenix-api-routes.md` where similar cross-links use this form).
    - The link target is the bare filename `protect-phoenix-api-routes.md` (relative path; both docs sit in the same `docs/` directory).
    - The trailing period at the end of the bullet sentence is preserved.

    Do NOT touch any other line in the file. Do NOT add a second cross-link elsewhere in the bullet list. Do NOT remove the surrounding bullets at L42-49 and L52+.

    No fenced code blocks in this action.
  </action>
  <verify>
    <automated>grep -n "If exposing API routes" docs/saas-adoption-recipe.md ; grep -c "protect-phoenix-api-routes.md" docs/saas-adoption-recipe.md ; grep -cE 'Lockspire\.Plug\.VerifyToken.*Lockspire\.Plug\.EnforceSenderConstraints.*Lockspire\.Plug\.RequireToken' docs/saas-adoption-recipe.md ; mix test test/lockspire/release_readiness_contract_test.exs:642 2>&amp;1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "If exposing API routes" docs/saas-adoption-recipe.md` returns exactly one line, and that line contains the substring `protect-phoenix-api-routes.md` and does NOT contain any of the three plug names.
    - `grep -c "protect-phoenix-api-routes.md" docs/saas-adoption-recipe.md` returns at least `1`.
    - `grep -cE 'Lockspire\.Plug\.VerifyToken.*Lockspire\.Plug\.EnforceSenderConstraints.*Lockspire\.Plug\.RequireToken' docs/saas-adoption-recipe.md` returns exactly `0`.
    - After this task plus Task 1 plus Plan 02, `mix test test/lockspire/release_readiness_contract_test.exs:642` exits 0. (If Plan 02 has NOT yet landed, the failure cited substring must be one of the D-06/D-07/Phase-92 substrings in `docs/protect-phoenix-api-routes.md`, never a D-09 substring in `docs/supported-surface.md` and never a substring in `docs/saas-adoption-recipe.md`.)
  </acceptance_criteria>
  <done>
    `docs/saas-adoption-recipe.md` carries a cross-link to `docs/protect-phoenix-api-routes.md` in place of the previous three-plug-name restatement; the silent fifth-restatement drift class is closed.
  </done>
</task>

</tasks>

<threat_model>
Phase 97 does not introduce new attack surface (per 97-RESEARCH.md `## Security Domain`). This plan edits only markdown doc files — no runtime path, no input handling, no crypto, no auth boundary. Phase 98 (V5 Input Validation for the hardened plug) and Phase 99 (V6 Cryptography for the extracted `Protocol.AccessTokenSigner`) carry the security work for v1.27.
</threat_model>

<verification>
- All grep counts in both tasks pass.
- After Plan 02 also lands, `mix test test/lockspire/release_readiness_contract_test.exs:642` exits 0.
- No Phase 92 substring lost in `docs/supported-surface.md` (insertion strictly AFTER existing list).
</verification>

<success_criteria>
- Plan 03 is COMPLETE when:
  1. `docs/supported-surface.md` carries a new `## Explicit non-goals for host-API route protection` H2 subsection with four bullets pairing D-09 non-goal phrases with rejection-rationale clauses.
  2. `docs/saas-adoption-recipe.md:50` carries a cross-link to `docs/protect-phoenix-api-routes.md` in place of the previous three-plug-name restatement.
  3. After Plan 02 also lands (parallel in Wave 2), `mix test test/lockspire/release_readiness_contract_test.exs:642` exits 0 with no failures.
  4. Every Phase 92 substring asserted by `assert_advanced_setup_support_contract!/1` survives intact in `docs/supported-surface.md`.
</success_criteria>

<output>
Create `.planning/phases/97-contract-docs-first/97-03-SUMMARY.md` when done. The summary MUST record: (a) the exact line numbers (post-edit) where the new `## Explicit non-goals for host-API route protection` subsection lives; (b) the exact bullet wording chosen for each of the four bullets (verbatim, including punctuation); (c) the exact replacement wording for `docs/saas-adoption-recipe.md:50`; (d) confirmation that all `grep -c` invariants from Task 1 and Task 2 passed; (e) the green/red status of `mix test test/lockspire/release_readiness_contract_test.exs:642` after this plan landed (noting that GREEN requires Plan 02 to have also landed).
</output>
