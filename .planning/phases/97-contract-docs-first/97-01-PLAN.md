---
phase: 97-contract-docs-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/support/advanced_setup_support_truth.ex
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
tags:
  - phase-97
  - wave-0
  - test-support
  - phase-92-extension
user_setup: []

must_haves:
  truths:
    - "Running `mix test test/lockspire/release_readiness_contract_test.exs:642` after this plan but BEFORE Plans 02/03 RED-fails with a clear `expected content to include` message naming a D-06/D-07/D-09 substring"
    - "Every existing Phase 92 substring in `assert_protected_routes_guide!/1` (8 entries) and `assert_advanced_setup_support_contract!/1` (14 entries across two lists) is preserved verbatim"
    - "Helper invocation surface stays unchanged: `assert_protected_routes_guide!/1` and `assert_advanced_setup_support_contract!/1` still take `content :: binary` and return `:ok` or raise"
  artifacts:
    - path: "test/support/advanced_setup_support_truth.ex"
      provides: "Extended `assert_protected_routes_guide!/1` (D-06 contract sentence parts + D-07 caveat parts) and `assert_advanced_setup_support_contract!/1` (four D-09 non-goal patterns + four rejection-rationale clauses)"
      contains: "Lockspire issues RFC 9068 `at+jwt` access tokens by default."
  key_links:
    - from: "test/support/advanced_setup_support_truth.ex"
      to: "test/lockspire/release_readiness_contract_test.exs:642 (the existing `advanced-setup support contract stays pinned semantically across canonical and derived docs` test)"
      via: "import alias at release_readiness_contract_test.exs:4-16"
      pattern: "assert_protected_routes_guide!\\|assert_advanced_setup_support_contract!"
---

<objective>
Extend the Phase 92 substring-contract helpers in `test/support/advanced_setup_support_truth.ex` with the new Phase 97 contract requirements (D-06 contract sentence parts, D-07 forward-reference caveat parts, four D-09 non-goal patterns plus rejection-rationale clauses) before any doc edits land. This establishes the failing-test RED state that Plans 02 and 03 will turn GREEN by editing the docs.

Purpose: Make the new doc-contract requirements machine-enforceable via the existing Phase 92 helper-extension idiom. After this plan, `mix test test/lockspire/release_readiness_contract_test.exs:642` MUST fail with a clear `expected content to include ...` message naming a Phase 97 substring that is not yet present in the docs. That failure is the success signal for this plan.

Output: One modified support module, no other files touched. Phase 92's existing substrings remain (extension, not invalidation, per Phase 92 D-05 and 97-RESEARCH Wave 0 Gaps note).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/97-contract-docs-first/97-CONTEXT.md
@.planning/phases/97-contract-docs-first/97-RESEARCH.md
@.planning/phases/97-contract-docs-first/97-PATTERNS.md
@.planning/phases/97-contract-docs-first/97-VALIDATION.md
@.planning/phases/92-advanced-setup-support-truth/92-CONTEXT.md

<interfaces>
<!-- Existing helper shape. Phase 97 EXTENDS the substring lists; do NOT alter the function signature, the inner `assert_includes_all/2` body, or the failure-message format. -->

From test/support/advanced_setup_support_truth.ex:

```elixir
def assert_advanced_setup_support_contract!(content) do
  assert_includes_all(content, [
    # 14 existing Phase 92 substrings across two `assert_includes_all` calls
    # (5 in second call at L22-28; 9 in first call at L5-20)
  ])
end

def assert_protected_routes_guide!(content) do
  assert_includes_all(content, [
    # 8 existing Phase 92 substrings at L70-79
  ])
end

defp assert_includes_all(content, snippets) do
  Enum.each(snippets, fn snippet ->
    unless String.contains?(content, snippet) do
      raise "expected content to include #{inspect(snippet)}"
    end
  end)
end
```

The 8 substrings currently inside `assert_protected_routes_guide!/1` (must be preserved verbatim):

```
"For the public support contract around this surface, see [`docs/supported-surface.md`](supported-surface.md)."
"Lockspire.Plug.VerifyToken"
"Lockspire.Plug.EnforceSenderConstraints"
"Lockspire.Plug.RequireToken"
"no-op for unconstrained bearer tokens"
"error=\"use_dpop_nonce\""
"business authorization"
"tenant checks"
```

The 5 substrings currently inside the SECOND `assert_includes_all` call in `assert_advanced_setup_support_contract!/1` (must be preserved verbatim — these are the out-of-scope assertions; Phase 97 ADDS four new non-goal patterns AFTER these in the same list):

```
"Generic API gateway, service-mesh, or third-party issuer protected-resource middleware remains out of scope"
"broader resource-server integration beyond Lockspire-owned `/token`, Lockspire-owned protected resources, and the shipped Phoenix plug pipeline"
"Arbitrary custom `Lockspire.MTLS.Extractor` implementations are not first-class peers"
"Dynamic Client Registration does not add a new logout runtime; it only manages the existing logout propagation metadata"
"proves front-channel logout success remotely"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend Phase 92 helpers with Phase 97 contract substrings (D-06, D-07, D-09)</name>
  <files>test/support/advanced_setup_support_truth.ex</files>
  <read_first>
    - test/support/advanced_setup_support_truth.ex (the file being modified — current state is the Phase 92 baseline; Phase 97 extends, does not replace)
    - .planning/phases/97-contract-docs-first/97-PATTERNS.md (Pattern D "Substring-Contract Helper Extension" section explains the extend-not-invalidate idiom; the file's "test/support/advanced_setup_support_truth.ex" row near line 376-440 enumerates the exact substrings to add)
    - .planning/phases/97-contract-docs-first/97-CONTEXT.md decisions D-06, D-07, D-09 (verbatim source for the new substrings)
    - .planning/phases/92-advanced-setup-support-truth/92-CONTEXT.md D-05 (states that Phase 92 helpers may be extended but must not be invalidated)
    - .planning/REQUIREMENTS.md lines 103-110 (the rejection-rationale phrases for the four D-09 non-goal bullets)
  </read_first>
  <behavior>
    - Before edit: `mix test test/lockspire/release_readiness_contract_test.exs:642` runs GREEN (Phase 92 baseline passes against current docs).
    - After edit: `mix test test/lockspire/release_readiness_contract_test.exs:642` RED-fails with `(RuntimeError) expected content to include "Lockspire issues RFC 9068 \`at+jwt\` access tokens by default."` (or one of the other newly-added D-06/D-07/D-09 substrings — whichever the helper checks first that is currently absent from the doc).
    - Failure message uses `inspect/1` of the snippet so the exact missing string is visible to the operator.
    - `assert_protected_routes_guide!/1` continues to take a single binary content arg and either returns `:ok` (silently) or raises a `RuntimeError`.
    - `assert_advanced_setup_support_contract!/1` continues to take a single binary content arg and either returns `:ok` or raises a `RuntimeError`.
    - Every Phase 92 substring listed in `<interfaces>` above survives the edit verbatim and in the same list.
  </behavior>
  <action>
    Edit `test/support/advanced_setup_support_truth.ex` in place. Make two changes only:

    Change A — extend `assert_protected_routes_guide!/1` (currently at L69-80) by appending six new substrings AFTER the existing eight substrings inside the single `assert_includes_all/2` call. Add a one-line `# Phase 97 extensions (D-06, D-07)` comment separator between the Phase 92 substrings and the new ones for readability. The six new substrings, added verbatim and in this order:

    - `"Lockspire issues RFC 9068 \`at+jwt\` access tokens by default."` (D-06 sentence 1)
    - `"\`Lockspire.Plug.VerifyToken\` accepts JWT bearer tokens for host Phoenix API routes."` (D-06 sentence 2)
    - `"Lockspire-owned \`/userinfo\` and \`/introspect\` use stored opaque tokens; those are not interchangeable."` (D-06 sentence 3)
    - `"To opt a client back to opaque, see the admin Client Detail page."` (D-06 sentence 4)
    - `"the runtime narrowing and the default-issuance flip land in v1.27"` (D-07 caveat midphrase — partial-match avoids brittleness against the exact comma/period positions Plan 02 chooses)
    - `"opaque tokens may still be silently accepted on these routes"` (D-07 caveat tail)

    Change B — extend the SECOND `assert_includes_all/2` call inside `assert_advanced_setup_support_contract!/1` (currently at L22-28, the out-of-scope list) by appending eight new substrings AFTER the existing five substrings. Add a one-line `# Phase 97 extensions (D-09 four non-goal patterns)` comment separator. The eight new substrings, added verbatim and in this order (each non-goal pattern is paired with its rejection-rationale clause per `.planning/REQUIREMENTS.md:103-110`):

    - `"no introspection-at-the-RS as the host-API seam"`
    - `"recreates gateway/CIAM productization the canon explicitly rejects"`
    - `"no auto-detection of token shape"`
    - `"documented ecosystem footgun"`
    - `"no dual-verifier dispatcher"`
    - `"hides operator-visible complexity inside the library"`
    - `"no RAR enforcement at the RS plug"`
    - `"RAR claims surface via \`conn.assigns.access_token\` for host-owned enforcement"`

    Do NOT modify any other helper function in the file (`assert_install_and_onboard_guide!/1`, `assert_private_key_jwt_host_guide!/1`, `assert_mtls_host_guide!/1`, `assert_operator_admin_guide!/1`, `assert_dynamic_registration_guide!/1`, `assert_maintainer_release_deference!/1`, `assert_security_policy_deference!/1`, `refute_broadened_security_non_claims!/1`). Do NOT modify `assert_includes_all/2` or `refute_includes_any/2` private helpers. Do NOT add new public functions.

    No fenced code blocks in this action — the diff is mechanical: append substrings to two existing list literals.
  </action>
  <verify>
    <automated>mix compile --force --warnings-as-errors 2>&amp;1 | grep -v '^Compiling\|warnings\|generated\|^$' | head -20 ; mix test test/lockspire/release_readiness_contract_test.exs:642 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --force --warnings-as-errors` exits 0 (no new warnings introduced by the helper extension).
    - `mix test test/lockspire/release_readiness_contract_test.exs:642` exits with a non-zero status AND the failure output contains the substring `expected content to include` (proving the new D-06/D-07/D-09 substrings are now asserted against the current docs that do not yet carry them — the planned RED state).
    - `grep -c "Lockspire issues RFC 9068" test/support/advanced_setup_support_truth.ex` returns exactly `1` (the new D-06 sentence-1 substring is present in the helper module exactly once).
    - `grep -c "no introspection-at-the-RS as the host-API seam" test/support/advanced_setup_support_truth.ex` returns exactly `1`.
    - `grep -c "no-op for unconstrained bearer tokens" test/support/advanced_setup_support_truth.ex` returns exactly `1` (Phase 92 substring preserved).
    - `grep -c "proves front-channel logout success remotely" test/support/advanced_setup_support_truth.ex` returns exactly `1` (Phase 92 out-of-scope substring preserved).
    - Total line count delta is positive (file grew); no Phase 92 substring was removed.
  </acceptance_criteria>
  <done>
    The helper module compiles cleanly, the `advanced-setup support contract stays pinned semantically` test now RED-fails citing a Phase 97 substring, and every Phase 92 substring survives verbatim.
  </done>
</task>

</tasks>

<threat_model>
Phase 97 does not introduce new attack surface (per `.planning/phases/97-contract-docs-first/97-RESEARCH.md` `## Security Domain`). This plan edits only a test-support module — no runtime path, no input handling, no crypto, no auth boundary. Phase 98 (V5 Input Validation for the hardened plug) and Phase 99 (V6 Cryptography for the extracted `Protocol.AccessTokenSigner`) carry the security work for v1.27.
</threat_model>

<verification>
- `mix compile --force --warnings-as-errors` is clean.
- `mix test test/lockspire/release_readiness_contract_test.exs:642` RED-fails with a Phase 97 substring named in the `expected content to include` message.
- Phase 92 substrings preserved (verified by grep counts on representative entries).
</verification>

<success_criteria>
- Plan 01 is COMPLETE when:
  1. `test/support/advanced_setup_support_truth.ex` carries the six new D-06/D-07 substrings inside `assert_protected_routes_guide!/1` and the eight new D-09 substrings inside `assert_advanced_setup_support_contract!/1`.
  2. The file still compiles with `--warnings-as-errors`.
  3. The `advanced-setup support contract stays pinned semantically` test RED-fails with a Phase 97 substring named in the failure message (this is the desired state — Plans 02/03 will turn it green).
  4. Plans 02 and 03 can now proceed; their definition of "done" includes turning this RED state back to GREEN.
</success_criteria>

<output>
Create `.planning/phases/97-contract-docs-first/97-01-SUMMARY.md` when done. The summary MUST record: (a) the exact six D-06/D-07 substrings added; (b) the exact eight D-09 substrings added; (c) the test-line number that now RED-fails; (d) the first substring named in the failure message (so Plans 02/03 know which one to satisfy first).
</output>
