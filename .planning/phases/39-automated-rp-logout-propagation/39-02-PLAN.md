---
phase: 39-automated-rp-logout-propagation
plan: "02"
type: execute
wave: 1
depends_on:
  - "39-01"
files_modified:
  - lib/lockspire/domain/client.ex
  - lib/lockspire/storage/ecto/client_record.ex
  - lib/lockspire/clients.ex
  - lib/lockspire/admin/clients.ex
  - lib/lockspire/protocol/registration.ex
  - test/lockspire/admin/clients_test.exs
  - test/lockspire/protocol/registration_test.exs
autonomous: true
requirements:
  - SLO-03
  - SLO-04
must_haves:
  truths:
    - "Client logout propagation fields are first-class typed fields, not metadata."
    - "Operator validation enforces strict URI and session-required rules offline."
    - "DCR rejects all four logout fields as unsupported in this slice."
  artifacts:
    - path: "lib/lockspire/domain/client.ex"
      provides: "typed back/front-channel client fields"
      contains: "backchannel_logout_uri"
    - path: "lib/lockspire/protocol/registration.ex"
      provides: "unsupported-in-slice rejection for Phase 39 DCR fields"
      contains: "unsupported_in_slice"
---

<objective>
Add the operator-managed client metadata surface for Phase 39 and reject unsupported self-service intake paths.

Purpose: execution-safe logout propagation starts with truthful durable client configuration.
Output: typed client fields, strict validation, and DCR rejection.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-CONTEXT.md
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-RESEARCH.md
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend client/domain records with typed logout fields</name>
  <files>lib/lockspire/domain/client.ex, lib/lockspire/storage/ecto/client_record.ex</files>
  <action>
Add `backchannel_logout_uri`, `backchannel_logout_session_required`, `frontchannel_logout_uri`, and `frontchannel_logout_session_required` as first-class fields with truthful defaults. Keep URI presence as the only opt-in.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/admin/clients_test.exs -x</automated>
  </verify>
  <done>Client/domain records round-trip all four new fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce operator validation for logout metadata</name>
  <files>lib/lockspire/clients.ex, lib/lockspire/admin/clients.ex, test/lockspire/admin/clients_test.exs</files>
  <action>
Implement strict offline validation for absolute URIs, fragments, `*_session_required` coupling, same-origin front-channel URI rules, and normalized/deduped values.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/admin/clients_test.exs -x</automated>
  </verify>
  <done>Operators can save valid logout fields and receive field-specific errors for invalid combinations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reject Phase 39 logout fields from DCR intake</name>
  <files>lib/lockspire/protocol/registration.ex, test/lockspire/protocol/registration_test.exs</files>
  <action>
Reject the four Phase 39 logout metadata fields as `invalid_client_metadata` / `unsupported_in_slice`, matching existing unsupported-field behavior.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/protocol/registration_test.exs -x</automated>
  </verify>
  <done>DCR cannot silently widen the supported logout surface.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/lockspire/admin/clients_test.exs test/lockspire/protocol/registration_test.exs -x`
</verification>

<success_criteria>
- Logout propagation fields are first-class operator-managed client state.
- DCR support remains truthful and intentionally narrow.
</success_criteria>

<output>
After completion, create `.planning/phases/39-automated-rp-logout-propagation/39-02-SUMMARY.md`
</output>
