---
phase: 39-automated-rp-logout-propagation
plan: "05"
type: execute
wave: 4
depends_on:
  - "39-01"
  - "39-03"
  - "39-04"
files_modified:
  - lib/lockspire/protocol/logout_propagation.ex
  - lib/lockspire/storage/ecto/repository.ex
  - lib/lockspire/web/controllers/end_session_controller.ex
  - test/lockspire/protocol/logout_propagation_test.exs
  - test/lockspire/web/end_session_controller_test.exs
autonomous: true
requirements:
  - SLO-03
must_haves:
  truths:
    - "`/end_session/complete` is the authoritative propagation fork point."
    - "Back-channel jobs are enqueued transactionally with durable event/delivery persistence."
    - "Duplicate completion hits cannot strand or duplicate back-channel deliveries."
  artifacts:
    - path: "lib/lockspire/protocol/logout_propagation.ex"
      provides: "transactional completion orchestration"
      contains: "complete"
---

<objective>
Make `/end_session/complete` the execution-safe completion seam by moving persistence, revocation, and transactional enqueue into one protocol-owned orchestration path.

Purpose: this closes the checker's stranded-delivery gap and makes completion idempotent and controller-thin.
Output: `LogoutPropagation.complete/1`, transactional event/delivery persistence with job enqueue, and thin controller integration.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-CONTEXT.md
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-RESEARCH.md
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build transactional logout completion orchestration</name>
  <files>lib/lockspire/protocol/logout_propagation.ex, lib/lockspire/storage/ecto/repository.ex, test/lockspire/protocol/logout_propagation_test.exs</files>
  <action>
Implement `LogoutPropagation.complete/1` so it snapshots targets from token history before revocation, inserts the logout event and delivery rows, enqueues back-channel jobs transactionally with the persistence work, revokes the `sid` only after the snapshot/persistence contract is secured, and returns typed front-channel data for the final UX plan.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/protocol/logout_propagation_test.exs -x</automated>
  </verify>
  <done>Completion persistence and enqueue happen in one execution-safe orchestration path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep the controller thin and idempotent</name>
  <files>lib/lockspire/web/controllers/end_session_controller.ex, test/lockspire/web/end_session_controller_test.exs</files>
  <action>
Update `EndSessionController.complete/2` to delegate to `LogoutPropagation` instead of revoking inline. Add coverage for invalid handoff token fallback, duplicate completion hits, and success paths that continue without waiting on RP HTTP.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/web/end_session_controller_test.exs -x</automated>
  </verify>
  <done>The controller no longer owns revocation or enqueue logic and duplicate completion paths stay safe.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/lockspire/protocol/logout_propagation_test.exs test/lockspire/web/end_session_controller_test.exs -x`
</verification>

<success_criteria>
- No persisted delivery rows can be stranded by a post-commit/pre-enqueue crash.
- `/end_session/complete` remains authoritative and thin.
</success_criteria>

<output>
After completion, create `.planning/phases/39-automated-rp-logout-propagation/39-05-SUMMARY.md`
</output>
