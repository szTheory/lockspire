---
phase: 39-automated-rp-logout-propagation
plan: "04"
type: execute
wave: 3
depends_on:
  - "39-01"
  - "39-03"
files_modified:
  - mix.exs
  - config/config.exs
  - lib/lockspire/application.ex
  - lib/lockspire/oban.ex
  - lib/lockspire/protocol/logout_token.ex
  - lib/lockspire/workers/backchannel_logout_delivery_worker.ex
  - lib/lockspire/observability.ex
  - lib/lockspire/audit/event.ex
  - lib/lockspire/redaction.ex
  - test/lockspire/application_test.exs
  - test/lockspire/protocol/logout_token_test.exs
  - test/lockspire/workers/backchannel_logout_delivery_worker_test.exs
autonomous: true
requirements:
  - SLO-03
must_haves:
  truths:
    - "Req is the outbound HTTP client for back-channel logout delivery."
    - "Lockspire starts a named Oban instance and fails fast when required runtime config is missing or invalid."
    - "Worker execution uses snapshotted delivery rows, not live client re-resolution."
    - "Telemetry/audit capture requested, enqueued, attempted, succeeded, and failed/discarded stages without leaking raw logout tokens."
  artifacts:
    - path: "lib/lockspire/oban.ex"
      provides: "named Oban facade for Lockspire-owned queues"
      contains: "use Oban"
    - path: "lib/lockspire/workers/backchannel_logout_delivery_worker.ex"
      provides: "unique, bounded, snapshot-authoritative worker"
      contains: "logout_delivery_id"
---

<objective>
Implement the reliable back-channel delivery runtime: dependency wiring, fail-fast startup, logout-token signing, worker execution, and full lifecycle instrumentation.

Purpose: this plan delivers the durable async engine for SLO-03 without yet changing the completion endpoint orchestration.
Output: Req dependency, named Oban runtime, snapshot-authoritative worker, logout token signer, and lifecycle telemetry/audit/redaction coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-CONTEXT.md
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-RESEARCH.md
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Req and named Lockspire Oban startup wiring</name>
  <files>mix.exs, config/config.exs, lib/lockspire/application.ex, lib/lockspire/oban.ex, test/lockspire/application_test.exs</files>
  <action>
Add `Req`, introduce `Lockspire.Oban`, and start it from `Lockspire.Application` using the existing `config :lockspire, oban: []` seam. Missing or invalid runtime config must fail fast with a clear startup error.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/application_test.exs -x</automated>
  </verify>
  <done>Startup wiring is explicit, named, and fail-fast.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build logout-token signing and snapshot-authoritative worker execution</name>
  <files>lib/lockspire/protocol/logout_token.ex, lib/lockspire/workers/backchannel_logout_delivery_worker.ex, test/lockspire/protocol/logout_token_test.exs, test/lockspire/workers/backchannel_logout_delivery_worker_test.exs</files>
  <action>
Implement JOSE-backed logout-token signing and a worker that reads the snapshotted `logout_delivery` row, posts `logout_token=<jwt>` via Req, and classifies retryable vs terminal outcomes. Do not re-resolve the live client URI as authoritative dispatch input.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/protocol/logout_token_test.exs test/lockspire/workers/backchannel_logout_delivery_worker_test.exs -x</automated>
  </verify>
  <done>Back-channel delivery executes from durable snapshot state and has bounded retries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add full logout lifecycle telemetry, audit, and redaction coverage</name>
  <files>lib/lockspire/observability.ex, lib/lockspire/audit/event.ex, lib/lockspire/redaction.ex, test/lockspire/workers/backchannel_logout_delivery_worker_test.exs</files>
  <action>
Add explicit lifecycle stages for `logout requested`, `delivery enqueued`, `delivery attempted`, `delivery succeeded`, and `delivery failed/discarded`. Extend redaction so raw logout tokens, raw response bodies, and similar sensitive payloads never leave the worker/protocol boundary.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/workers/backchannel_logout_delivery_worker_test.exs -x</automated>
  </verify>
  <done>Lifecycle instrumentation is complete and redaction-safe.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/lockspire/application_test.exs test/lockspire/protocol/logout_token_test.exs test/lockspire/workers/backchannel_logout_delivery_worker_test.exs -x`
</verification>

<success_criteria>
- Back-channel runtime is fail-fast, durable, and snapshot-authoritative.
- Lifecycle instrumentation covers all locked D-21 stages.
</success_criteria>

<output>
After completion, create `.planning/phases/39-automated-rp-logout-propagation/39-04-SUMMARY.md`
</output>
