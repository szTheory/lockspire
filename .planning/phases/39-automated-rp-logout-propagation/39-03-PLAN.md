---
phase: 39-automated-rp-logout-propagation
plan: "03"
type: execute
wave: 2
depends_on:
  - "39-01"
  - "39-02"
files_modified:
  - lib/lockspire/domain/logout_event.ex
  - lib/lockspire/domain/logout_delivery.ex
  - lib/lockspire/storage/logout_store.ex
  - lib/lockspire/storage/ecto/logout_event_record.ex
  - lib/lockspire/storage/ecto/logout_delivery_record.ex
  - lib/lockspire/storage/ecto/repository.ex
  - priv/repo/migrations/20260429110000_add_logout_propagation_fields_to_lockspire_clients.exs
  - priv/repo/migrations/20260429110010_create_lockspire_logout_events.exs
  - priv/repo/migrations/20260429110020_create_lockspire_logout_deliveries.exs
  - test/lockspire/storage/ecto/repository_logout_propagation_test.exs
autonomous: true
requirements:
  - SLO-03
  - SLO-04
must_haves:
  truths:
    - "Logout events and deliveries are durable protocol truth, not queue-only state."
    - "Delivery rows snapshot target URI and session-required state so historical dispatch does not depend on live client mutation."
    - "Repository helpers can snapshot targets from token history before revocation."
  artifacts:
    - path: "lib/lockspire/storage/logout_store.ex"
      provides: "durable logout persistence contract"
      contains: "@callback"
    - path: "lib/lockspire/storage/ecto/logout_delivery_record.ex"
      provides: "snapshotted per-client per-channel delivery state"
      contains: "target_uri"
---

<objective>
Build the durable storage foundation for Phase 39, including snapshot-authoritative delivery rows and pre-revocation target selection.

Purpose: later worker and controller plans must consume durable snapshot state rather than mutable live client config.
Output: migrations, domain/store contracts, repository snapshot/persistence helpers, and repository tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-CONTEXT.md
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-RESEARCH.md
@/Users/jon/projects/lockspire/.planning/phases/39-automated-rp-logout-propagation/39-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add logout event and delivery domain/store contracts</name>
  <files>lib/lockspire/domain/logout_event.ex, lib/lockspire/domain/logout_delivery.ex, lib/lockspire/storage/logout_store.ex</files>
  <action>
Create typed domain structs and a `LogoutStore` behaviour. Model explicit delivery lifecycle states and include snapshot-owned fields such as `target_uri`, `session_required`, `client_id`, and `channel`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/storage/ecto/repository_logout_propagation_test.exs -x</automated>
  </verify>
  <done>Logout propagation has explicit domain/storage contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add migrations and Ecto records for durable logout rows</name>
  <files>priv/repo/migrations/20260429110000_add_logout_propagation_fields_to_lockspire_clients.exs, priv/repo/migrations/20260429110010_create_lockspire_logout_events.exs, priv/repo/migrations/20260429110020_create_lockspire_logout_deliveries.exs, lib/lockspire/storage/ecto/logout_event_record.ex, lib/lockspire/storage/ecto/logout_delivery_record.ex</files>
  <action>
Add the Phase 39 migrations and Ecto record modules. Persist only redaction-safe metadata on event/delivery rows; do not store raw logout tokens, raw response bodies, or unredacted sensitive query strings.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/storage/ecto/repository_logout_propagation_test.exs -x</automated>
  </verify>
  <done>Durable rows exist for one event and many deliveries with truthful defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement repository snapshot and persistence helpers</name>
  <files>lib/lockspire/storage/ecto/repository.ex, test/lockspire/storage/ecto/repository_logout_propagation_test.exs</files>
  <action>
Implement `LogoutStore` callbacks that resolve distinct target clients from durable token history before revocation and insert one event plus many snapshot-authoritative deliveries for later transactional enqueue.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/lockspire && MIX_ENV=test mix test test/lockspire/storage/ecto/repository_logout_propagation_test.exs -x</automated>
  </verify>
  <done>Repository tests prove snapshot-before-revocation and durable delivery-row creation.</done>
</task>

</tasks>

<verification>
- `MIX_ENV=test mix test test/lockspire/storage/ecto/repository_logout_propagation_test.exs -x`
</verification>

<success_criteria>
- The repo has durable logout event/delivery truth.
- Snapshot rows are authoritative for later delivery execution.
- Target selection happens before revocation removes active-session evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/39-automated-rp-logout-propagation/39-03-SUMMARY.md`
</output>
