---
phase: "121-route-scorecards-judgment-contract"
plan: "02"
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - "test/support/lockspire/web/admin_proof/route_scorecards.ex"
  - "test/lockspire/web/live/admin/design_system_contract_test.exs"
autonomous: true
requirements: [IA-01, IA-02, IA-03]
user_setup: []
must_haves:
  truths:
    - "IA-01 D-02: tests derive expected route truth from Phoenix.Router.routes(Lockspire.Web.AdminRouter) plus exactly one workflow exception."
    - "IA-01 D-03 D-04: tests fail on missing fields, missing routes, extra routes, or silently invalid follow-up routes."
    - "IA-02 D-05 D-06 D-08: deterministic ExUnit guardrails fail on missing earned-place, unsupported-action, generic-copy, secret-evidence, and component-fit rubric fields."
    - "IA-03 D-07 D-14 D-15 D-16 D-17 D-23: tests keep lab/browser/judge/theming/storybook evidence out of public or package support surface."
    - "D-18 D-19 D-20 D-21 D-22: route tests preserve host-mounted admin clarity, host-owned auth seam, operator-need framing, accessibility/focus/theme checks, and component-pattern vocabulary."
  prohibitions:
    - "No production runtime module, route, schema, migration, package dependency, browser config, screenshot report, or public theming/component API is added."
    - "No parallel HTML assertion vocabulary replaces `Lockspire.Web.AdminProof.HtmlAssertions`; scorecard parsing stays test-support only."
  artifacts:
    - path: "test/support/lockspire/web/admin_proof/route_scorecards.ex"
      provides: "Test-only scorecard parser and route expectation helper."
      exports:
        - "workflow_exceptions/0"
        - "required_fields/0"
        - "allowed_evidence_classes/0"
        - "support_promise/0"
        - "expected_routes/0"
        - "parse!/1"
    - path: "test/lockspire/web/live/admin/design_system_contract_test.exs"
      provides: "Phase 121 deterministic source/markdown/support-boundary guardrails."
      contains: "phase 121 route scorecards"
  key_links:
    - from: "test/support/lockspire/web/admin_proof/route_scorecards.ex"
      to: "lib/lockspire/web/admin_router.ex"
      via: "Phoenix.Router.routes(Lockspire.Web.AdminRouter)"
      pattern: "Phoenix.Router.routes"
    - from: "test/lockspire/web/live/admin/design_system_contract_test.exs"
      to: ".planning/phases/121-route-scorecards-judgment-contract/121-ROUTE-SCORECARDS.md"
      via: "parser helper reads and validates the scorecard artifact"
      pattern: "121-ROUTE-SCORECARDS"
---

<objective>
Add deterministic ExUnit guardrails for the Phase 121 scorecard artifact and support-boundary contract.

Purpose: Prevent markdown-only judgment drift by making route coverage, required fields, follow-up routes, evidence classes, support promises, and forbidden support-surface creep fail in CI.
Output: Test-only scorecard parser helper plus Phase 121 contract tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-route-scorecards-judgment-contract/121-CONTEXT.md
@.planning/phases/121-route-scorecards-judgment-contract/121-RESEARCH.md
@.planning/phases/121-route-scorecards-judgment-contract/121-VALIDATION.md
@.planning/phases/121-route-scorecards-judgment-contract/121-ROUTE-SCORECARDS.md
@lib/lockspire/web/admin_router.ex
@test/lockspire/web/live/admin/design_system_contract_test.exs
@test/support/lockspire/web/admin_proof/html_assertions.ex
@test/lockspire/web/live/admin/design_system_component_stress_test.exs
@docs/operator-admin.md
@docs/supported-surface.md
</context>

<artifacts_this_phase_produces>
New files:
- `.planning/phases/121-route-scorecards-judgment-contract/121-ROUTE-SCORECARDS.md`
- `test/support/lockspire/web/admin_proof/route_scorecards.ex`

New module/functions:
- `Lockspire.Web.AdminProof.RouteScorecards`
- `RouteScorecards.workflow_exceptions/0`
- `RouteScorecards.required_fields/0`
- `RouteScorecards.allowed_evidence_classes/0`
- `RouteScorecards.support_promise/0`
- `RouteScorecards.expected_routes/0`
- `RouteScorecards.parse!/1`

New test functions in `Lockspire.Web.Live.Admin.DesignSystemContractTest`:
- `test "phase 121 route scorecards cover AdminRouter route truth"`
- `test "phase 121 route scorecards enforce required judgment fields"`
- `test "phase 121 route scorecard follow-up routes stay inside known route truth"`
- `test "phase 121 scorecards preserve support boundary and deny public surface creep"`
- `test "phase 121 operate scorecards preserve read-only support truth"`

Modified docs:
- `docs/operator-admin.md` gains a maintainer-facing page scorecard workflow section.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add test-only scorecard parser helper</name>
  <files>test/support/lockspire/web/admin_proof/route_scorecards.ex</files>
  <read_first>
    - `test/support/lockspire/web/admin_proof/route_scorecards.ex` if it exists; create it if absent.
    - `.planning/phases/121-route-scorecards-judgment-contract/121-ROUTE-SCORECARDS.md`
    - `.planning/phases/121-route-scorecards-judgment-contract/121-CONTEXT.md`
    - `.planning/phases/121-route-scorecards-judgment-contract/121-RESEARCH.md`
    - `lib/lockspire/web/admin_router.ex`
    - `test/support/lockspire/web/admin_proof/html_assertions.ex`
    - `test/lockspire/web/live/admin/design_system_contract_test.exs`
  </read_first>
  <behavior>
    - `expected_routes/0` returns 29 sorted routes: 28 router-derived `/admin...` routes plus `/admin/clients/:client_id/edit?workflow=logout-propagation`.
    - `workflow_exceptions/0` returns exactly one string.
    - `required_fields/0` returns the exact D-03 field labels used by the markdown artifact.
    - `allowed_evidence_classes/0` returns `internal_lab`, `rendered_guardrail`, `manual_browser_note`, and `none`.
    - `support_promise/0` returns the exact D-16 boundary sentence.
    - `parse!/1` returns `%{route => %{field => value}}` from headings formatted as `### Scorecard: \`ROUTE\`` and bullet fields formatted as `- **Field:** value`; it raises a clear `ArgumentError` if a block has no Route field or a duplicate route.
  </behavior>
  <action>Create `Lockspire.Web.AdminProof.RouteScorecards` under `test/support` per D-02 through D-06 and D-14 through D-17. Keep it `@moduledoc false` and test-only. Implement the public functions named in this plan; keep parsing strict to the Plan 01 artifact format. Use `Phoenix.Router.routes(Lockspire.Web.AdminRouter)` for route truth, mapping `/` to `/admin` and prefixing every other path with `/admin`. Do not parse `AdminRouter` with ad hoc route regex. Do not add production files, package dependencies, browser tooling, or a public API. Keep existing rendered HTML proof vocabulary in `Lockspire.Web.AdminProof.HtmlAssertions`; this helper owns markdown scorecard parsing only.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1</automated>
    <automated>mix format test/support/lockspire/web/admin_proof/route_scorecards.ex --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - `test/support/lockspire/web/admin_proof/route_scorecards.ex` defines `defmodule Lockspire.Web.AdminProof.RouteScorecards`.
    - The module exports `workflow_exceptions/0`, `required_fields/0`, `allowed_evidence_classes/0`, `support_promise/0`, `expected_routes/0`, and `parse!/1`.
    - `expected_routes/0` uses `Phoenix.Router.routes(Lockspire.Web.AdminRouter)`.
    - `workflow_exceptions/0` contains exactly `/admin/clients/:client_id/edit?workflow=logout-propagation`.
    - No files under `lib/`, `mix.exs`, package manager manifests, or browser config files are modified by this task.
  </acceptance_criteria>
  <done>The test suite has a focused, reusable, test-only parser/route helper for deterministic scorecard guardrails.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Phase 121 scorecard contract tests</name>
  <files>test/lockspire/web/live/admin/design_system_contract_test.exs</files>
  <read_first>
    - `test/lockspire/web/live/admin/design_system_contract_test.exs`
    - `test/support/lockspire/web/admin_proof/route_scorecards.ex`
    - `test/support/lockspire/web/admin_proof/html_assertions.ex`
    - `.planning/phases/121-route-scorecards-judgment-contract/121-ROUTE-SCORECARDS.md`
    - `.planning/phases/121-route-scorecards-judgment-contract/121-CONTEXT.md`
    - `.planning/phases/121-route-scorecards-judgment-contract/121-RESEARCH.md`
    - `docs/operator-admin.md`
    - `docs/supported-surface.md`
    - `mix.exs`
    - `lib/lockspire/web/admin_router.ex`
  </read_first>
  <behavior>
    - Missing scorecard blocks fail.
    - Extra scorecard blocks fail.
    - Missing required fields fail.
    - Empty, `TBD`, `TODO`, or non-final sentinel field values fail.
    - A second `?workflow=` route fails.
    - Follow-up routes must be known scorecard routes, the single workflow exception, or explicitly marked as external, documentation-only, or absent.
    - The `## Judgment Rubric` section must include the `Page`, `Section`, `Action`, and `Component Group` scopes, and each scope must ask exactly `redundant?`, `least-surprising?`, `user-flow-oriented?`, `visually intentional?`, and `on-brand?`.
    - Generic CTA text in scorecard `Primary action` values fails.
    - Generic CTA text in representative rendered HTML fails through `Lockspire.Web.AdminProof.HtmlAssertions.assert_no_generic_cta_text/1` where rendered HTML is available in existing contract or component-stress coverage.
    - Forbidden secret or plaintext evidence in scorecards, docs, source proof, or package boundary files fails.
    - Unearned `Earned-place check` or `Component/group fit` values such as `decorative`, `placeholder`, `nice-to-have`, or `later` fail.
    - Public-surface boundary text drift fails.
    - Operate scorecards cannot imply unsupported queue mutation controls.
  </behavior>
  <action>Extend `Lockspire.Web.Live.Admin.DesignSystemContractTest` with a Phase 121 section that imports or aliases `Lockspire.Web.AdminProof.RouteScorecards`. Add `@phase_121_scorecards_path` pointing at `.planning/phases/121-route-scorecards-judgment-contract/121-ROUTE-SCORECARDS.md`. Add the five test names listed in this plan's artifacts section. The route-truth test must compare `Map.keys(RouteScorecards.parse!(markdown)) |> Enum.sort()` to `RouteScorecards.expected_routes()`. The required-fields test must iterate over all parsed scorecards and all `RouteScorecards.required_fields()`, reject blank and non-final sentinel values, check Route equals the block key wrapped in backticks or plain text, check Journey is one of the four locked journey labels, check Evidence class is allowed, check Public support promise equals `RouteScorecards.support_promise()`, assert the `## Judgment Rubric` section contains exactly the four IA-02 scopes and five required questions, reject generic CTA text in every `Primary action` value, and reject unearned values in `Earned-place check` or `Component/group fit` including `decorative`, `placeholder`, `nice-to-have`, and `later`. Add explicit source/text denylist assertions for forbidden secret and plaintext evidence across the scorecard artifact, `docs/operator-admin.md`, `docs/supported-surface.md`, `mix.exs`, and relevant admin proof/source files, using the existing redaction-denylist style in this test module. Where a representative rendered HTML fragment is already available in this file or `design_system_component_stress_test.exs`, call `Lockspire.Web.AdminProof.HtmlAssertions.assert_no_generic_cta_text/1` instead of duplicating rendered HTML parsing. The follow-up test must accept known scorecard routes, the single workflow exception, and explicit external/documentation-only/absent markers. The support-boundary test must scan the scorecard artifact, `docs/operator-admin.md`, `docs/supported-surface.md`, `mix.exs`, and `AdminRouter` for D-17 public-surface creep using the existing Phase 120 boundary style. The Operate test must read the three Operate route blocks and assert their Unsupported action check preserves read-only support truth while rejecting unbacked queue-control language. Keep the tests deterministic and do not add package-manager, browser, screenshot, or runtime route support.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1</automated>
    <automated>MIX_ENV=test mix test test/lockspire/web/live/admin/design_system_contract_test.exs test/lockspire/web/live/admin/design_system_component_stress_test.exs --max-failures 1</automated>
    <automated>mix format test/support/lockspire/web/admin_proof/route_scorecards.ex test/lockspire/web/live/admin/design_system_contract_test.exs --check-formatted</automated>
  </verify>
  <acceptance_criteria>
    - The focused contract test command passes.
    - The combined contract plus component stress command passes.
    - A missing scorecard route, an extra route, a missing required field, a second query-workflow route, or a wrong support promise would fail at least one Phase 121 test.
    - A missing rubric scope or missing rubric question for Page, Section, Action, or Component Group would fail a Phase 121 test.
    - Generic CTA text in a scorecard `Primary action` value or representative rendered HTML would fail a Phase 121 test.
    - Forbidden secret/plaintext evidence or unearned `Earned-place check` / `Component/group fit` values such as decorative, placeholder, nice-to-have, or later would fail a Phase 121 test.
    - The tests derive expected route truth from `Phoenix.Router.routes(Lockspire.Web.AdminRouter)`, not screenshot filenames or host mount prefixes.
    - The tests do not require browser automation, Node packages, new runtime routes, or public theming/design-system APIs.
  </acceptance_criteria>
  <done>IA-01, IA-02, and IA-03 are backed by deterministic ExUnit guardrails instead of markdown review alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Scorecard markdown to ExUnit parser | Malformed markdown can pass human review but fail to enforce route and field truth. |
| AdminRouter source to route inventory | Route drift must fail tests without using screenshot filenames or host-specific mount prefixes. |
| Internal proof to public support contract | Test/lab/browser wording can accidentally become supported public surface. |
| Operate scorecards to operator action model | Scorecards can imply controls that the domain does not back. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-121-06 | Tampering | `RouteScorecards.expected_routes/0` | mitigate | Use `Phoenix.Router.routes(Lockspire.Web.AdminRouter)` plus one explicit workflow exception. |
| T-121-07 | Repudiation | required judgment fields | mitigate | ExUnit tests reject missing and non-final sentinel field values. |
| T-121-08 | Information Disclosure | scorecard and docs evidence | mitigate | Source tests scan for forbidden secret-evidence concepts and require redaction/security fields. |
| T-121-09 | Elevation of Privilege | public lab/theming/browser support | mitigate | Source tests scan router/docs/package content and scorecards for D-17 creep. |
| T-121-10 | Tampering | Operate queue scorecards | mitigate | Dedicated tests reject unbacked queue-control language for interactions, device authorizations, and logouts. |
| T-121-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task exists; package legitimacy audit says none. |
</threat_model>

<verification>
Plan-level checks:
- `MIX_ENV=test mix test test/lockspire/web/live/admin/design_system_contract_test.exs --max-failures 1`
- `MIX_ENV=test mix test test/lockspire/web/live/admin/design_system_contract_test.exs test/lockspire/web/live/admin/design_system_component_stress_test.exs --max-failures 1`
- `mix format test/support/lockspire/web/admin_proof/route_scorecards.ex test/lockspire/web/live/admin/design_system_contract_test.exs --check-formatted`
</verification>

<success_criteria>
The Phase 121 route scorecard artifact is parsed by deterministic ExUnit tests, route truth is source-derived from `AdminRouter`, every required judgment/support-boundary field is enforced, follow-up routes are bounded, and public support-surface creep fails before later page polish begins.
</success_criteria>

<output>
Create `.planning/phases/121-route-scorecards-judgment-contract/121-02-SUMMARY.md` when done.
</output>
