---
phase: 49-host-policy-behaviour
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/host/token_exchange_context.ex
  - lib/lockspire/host/token_exchange_validator.ex
  - lib/lockspire/host/default_deny_token_exchange_validator.ex
  - lib/lockspire/config.ex
autonomous: true
requirements:
  - TE-03

must_haves:
  truths:
    - Developers can configure a `Lockspire.Host.TokenExchangeValidator` behaviour module.
    - Token exchange requests are denied by default.
  artifacts:
    - path: lib/lockspire/host/token_exchange_context.ex
      provides: Context data carrier for the exchange flow.
    - path: lib/lockspire/host/token_exchange_validator.ex
      provides: The validator behaviour callback specification.
    - path: lib/lockspire/host/default_deny_token_exchange_validator.ex
      provides: Default fallback implementation rejecting exchanges.
    - path: lib/lockspire/config.ex
      provides: Config accessor for the validator.
  key_links:
    - from: lib/lockspire/config.ex
      to: lib/lockspire/host/default_deny_token_exchange_validator.ex
      via: Default value fallback
      pattern: token_exchange_validator
---

<objective>
Introduce the Host behaviour module for Token Exchange and the context struct, providing domain-specific decision points.

Purpose: Bridges Lockspire's purely protocol-level exchange validation to the host app's business logic, failing securely by default.
Output: The `TokenExchangeValidator` behaviour, `TokenExchangeContext` struct, and a safe default-deny configuration accessor.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-host-policy-behaviour/49-RESEARCH.md
@.planning/phases/49-host-policy-behaviour/49-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the Token Exchange Context Struct</name>
  <files>lib/lockspire/host/token_exchange_context.ex, test/lockspire/host/token_exchange_context_test.exs</files>
  <behavior>
    - Ensure it is a struct mapping the requested exchange context.
    - Struct keys: `client_id` (String.t), `subject_token` (map), `actor_token` (map or nil), `requested_scopes` (list of strings).
    - Tests verify the struct can be initialized with required keys.
  </behavior>
  <action>Create `%Lockspire.Host.TokenExchangeContext{}` (per Locked Decisions D-XX). It acts as a data carrier passed to host integration callbacks (similar to `Lockspire.Host.Context`). Use `@enforce_keys` for `client_id`, `subject_token`, `requested_scopes`.</action>
  <verify>
    <automated>mix test test/lockspire/host/token_exchange_context_test.exs</automated>
  </verify>
  <done>Context struct defined and enforces its required keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Validator Behaviour and Default-Deny Implementation</name>
  <files>lib/lockspire/host/token_exchange_validator.ex, lib/lockspire/host/default_deny_token_exchange_validator.ex, test/lockspire/host/default_deny_token_exchange_validator_test.exs</files>
  <behavior>
    - The behavior specifies the `validate/1` callback returning `:ok | {:ok, %{claims: map()}} | {:error, term()}`.
    - Default deny implementation always returns `{:error, :unsupported_grant_type}` or `:access_denied` (with internal reason logged). We'll return `{:error, :exchange_not_configured}`.
  </behavior>
  <action>Create `Lockspire.Host.TokenExchangeValidator` defining the behaviour callback `validate(Lockspire.Host.TokenExchangeContext.t())`. Create `Lockspire.Host.DefaultDenyTokenExchangeValidator` that implements it by simply returning `{:error, :not_configured}` (per User Constraints). This guarantees we fail securely instead of implicitly allowing token exchanges without host configuration.

Follow analog `lib/lockspire/host/account_resolver.ex` from PATTERNS.md:
```elixir
defmodule Lockspire.Host.AccountResolver do
  @callback resolve_current_account(conn_or_socket :: connection(), context()) ::
              {:ok, account()} | {:redirect, InteractionResult.t()}
```
</action>
  <verify>
    <automated>mix test test/lockspire/host/default_deny_token_exchange_validator_test.exs</automated>
  </verify>
  <done>Behaviour callback defined and default implementation safely returns an error tuple.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update Config with Validator Accessor</name>
  <files>lib/lockspire/config.ex, test/lockspire/config_test.exs</files>
  <behavior>
    - `Config.token_exchange_validator()` returns the configured module or falls back to `Lockspire.Host.DefaultDenyTokenExchangeValidator`.
    - Fails securely (does not implicitly allow).
  </behavior>
  <action>Add `token_exchange_validator/0` to `Lockspire.Config`. Read from `Application.get_env(:lockspire, :token_exchange_validator)` and return `Lockspire.Host.DefaultDenyTokenExchangeValidator` if nil. Add related specs and ExUnit test ensuring the fallback works properly.

Follow analog `lib/lockspire/config.ex` from PATTERNS.md:
```elixir
  @doc """
  Returns the configured account resolver module, or raises if missing.
  """
  @spec account_resolver!() :: module()
  def account_resolver! do
    fetch_required!(:account_resolver)
  end
```
*Note: For `token_exchange_validator`, it shouldn't use `fetch_required!` but rather provide a fallback to a default strict-deny implementation using `Application.get_env(@app, :token_exchange_validator, Lockspire.Host.DefaultDenyTokenExchangeValidator)`.*
</action>
  <verify>
    <automated>mix test test/lockspire/config_test.exs</automated>
  </verify>
  <done>Config correctly exposes the validator or default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host Config -> Internal logic | The absence of config must default to an untrusted state (deny all). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01-01 | Elevation of Privilege | Config default | mitigate | Default implementation must strictly deny all exchanges. |
</threat_model>

<verification>
mix test runs without error and confirms context and default implementations work.
</verification>

<success_criteria>
The behaviour exists, the context struct is usable, and missing configuration fails closed.
</success_criteria>

<output>
After completion, create `.planning/phases/49-host-policy-behaviour/49-01-SUMMARY.md`
</output>