---
phase: 44-s01-ecto-replay-protection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/registration.ex
  - test/lockspire/protocol/registration_test.exs
autonomous: true
requirements:
  - REQ-02
must_haves:
  truths:
    - Clients with private_key_jwt are rejected if both jwks and jwks_uri are missing
    - Clients with private_key_jwt are rejected if both jwks and jwks_uri are present
  artifacts:
    - path: lib/lockspire/protocol/registration.ex
      provides: Registration DCR validation logic
  key_links:
    - from: lib/lockspire/protocol/registration.ex
      to: DCR metadata
      via: "validate_jwks/1 checking token_endpoint_auth_method"
---

<objective>
Dynamic Client Registration (DCR) validation for `jwks` and `jwks_uri` coherence.

Purpose: Fail early when DCR configurations lack a consistent public key specification.
Output: Registration validation module update with strict coherence checks for `private_key_jwt`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/44-s01-ecto-replay-protection/44-S01-STRATEGY.md
@.planning/phases/44-s01-ecto-replay-protection/RESEARCH.md
@.planning/phases/44-s01-ecto-replay-protection/44-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update DCR Validation in Registration.ex</name>
  <files>
    - lib/lockspire/protocol/registration.ex
    - test/lockspire/protocol/registration_test.exs
  </files>
  <behavior>
    - DCR rejects `private_key_jwt` missing both `jwks` & `jwks_uri`
    - DCR rejects `private_key_jwt` with both `jwks` & `jwks_uri` present
    - Existing jwks_uri `:unsupported_in_slice` restriction is removed
  </behavior>
  <action>
    In `Lockspire.Protocol.Registration.validate_jwks/1` (or equivalent metadata validator block):
    Remove the `:unsupported_in_slice` block for `jwks_uri`.
    Add logic to ensure that if `token_endpoint_auth_method` == `private_key_jwt`, the metadata must have exactly one of `jwks` or `jwks_uri`.
    If both are present, return an error specifying mutual exclusion. If neither is present, return an error specifying missing keys.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/registration_test.exs</automated>
  </verify>
  <done>Invalid metadata states for private key JWT authentication are properly rejected at intake.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DCR intake | External clients register configurations against the IDP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02-01 | Spoofing | Registration Validation | mitigate | Strictly check that cryptographic material source is well-defined (exactly one of jwks or jwks_uri) |
</threat_model>

<verification>
mix test test/lockspire/protocol/registration_test.exs
</verification>

<success_criteria>
`private_key_jwt` authentication method mandates exact coherence between jwks and jwks_uri.
</success_criteria>

<output>
After completion, create `.planning/phases/44-s01-ecto-replay-protection/44-02-PLAN-SUMMARY.md`
</output>