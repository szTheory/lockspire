---
phase: 44-s01-ecto-replay-protection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/repo/migrations/*_create_lockspire_used_jtis.exs
  - lib/lockspire/domain/used_jti.ex
  - lib/lockspire/storage/used_jti_store.ex
  - lib/lockspire/storage/ecto/used_jti_record.ex
  - lib/lockspire/storage/ecto/repository.ex
  - test/lockspire/storage/ecto/repository_used_jti_test.exs
  - lib/lockspire/workers/pruner.ex
  - test/lockspire/workers/pruner_test.exs
autonomous: true
requirements:
  - REQ-03
must_haves:
  truths:
    - Pruner automatically removes expired used JTIs
    - Used JTIs are durably stored in database
    - Storage module implements JTI tracking behavior
  artifacts:
    - path: priv/repo/migrations/*_create_lockspire_used_jtis.exs
      provides: Database table creation
    - path: lib/lockspire/domain/used_jti.ex
      provides: Domain struct
    - path: lib/lockspire/storage/ecto/used_jti_record.ex
      provides: Ecto schema mapping
    - path: lib/lockspire/storage/used_jti_store.ex
      provides: JTI store behavior
  key_links:
    - from: lib/lockspire/workers/pruner.ex
      to: lib/lockspire/storage/ecto/used_jti_record.ex
      via: "@schemas attribute list"
---

<objective>
Database Migration, Ecto Schema, Domain Struct, Storage behavior, and Pruner integration for tracking used JTIs.

Purpose: Set up the persistence layer and periodic cleanup for Replay JTIs.
Output: DB migration, domain definitions, storage implementations, and Pruner hook.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/44-s01-ecto-replay-protection/44-S01-STRATEGY.md
@.planning/phases/44-s01-ecto-replay-protection/RESEARCH.md
@.planning/phases/44-s01-ecto-replay-protection/44-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create UsedJti Domain, Schema, Migration, and Store behaviour</name>
  <files>
    - priv/repo/migrations/*_create_lockspire_used_jtis.exs
    - lib/lockspire/domain/used_jti.ex
    - lib/lockspire/storage/used_jti_store.ex
    - lib/lockspire/storage/ecto/used_jti_record.ex
  </files>
  <behavior>
    - UsedJti Domain defines fields for client_id, jti, and expires_at.
    - UsedJtiRecord schema defines lockspire_used_jtis.
  </behavior>
  <action>
    Use `mix ecto.gen.migration create_lockspire_used_jtis` to create a migration.
    The migration creates `lockspire_used_jtis` table with `client_id`, `jti`, and `expires_at` (all non-null). Add `timestamps(type: :utc_datetime_usec)`.
    Add a unique index `unique_index(:lockspire_used_jtis, [:client_id, :jti])`.
    Add an index `index(:lockspire_used_jtis, [:expires_at])`.
    Create `Lockspire.Domain.UsedJti` struct containing `:id, :client_id, :jti, :expires_at`.
    Create `Lockspire.Storage.UsedJtiStore` defining a `@callback record_used_jti(Lockspire.Domain.UsedJti.t()) :: {:ok, :accepted | :replay} | {:error, term()}`.
    Create `Lockspire.Storage.Ecto.UsedJtiRecord` for the table. Ensure a standard auto-incrementing ID is used. Apply the unique constraint in the changeset.
  </action>
  <verify>
    <automated>mix ecto.migrate</automated>
  </verify>
  <done>Database migrated, struct and behaviour created, schemas map to tables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement UsedJti storage in Ecto Repository</name>
  <files>
    - lib/lockspire/storage/ecto/repository.ex
    - test/lockspire/storage/ecto/repository_used_jti_test.exs
  </files>
  <behavior>
    - record_used_jti/1 inserts the record if unique.
    - record_used_jti/1 returns {:ok, :replay} if JTI/client_id already exists.
  </behavior>
  <action>
    Implement `Lockspire.Storage.UsedJtiStore` behavior in `Lockspire.Storage.Ecto.Repository`.
    Add `record_used_jti/1` function that attempts an insert, catching unique constraint errors to return `{:ok, :replay}`.
  </action>
  <verify>
    <automated>mix test test/lockspire/storage/ecto/repository_used_jti_test.exs</automated>
  </verify>
  <done>UsedJTIs can be successfully recorded and duplicate violations handled cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Integrate UsedJtiRecord into Pruner</name>
  <files>
    - lib/lockspire/workers/pruner.ex
    - test/lockspire/workers/pruner_test.exs
  </files>
  <behavior>
    - UsedJtiRecord is included in Pruner schemas
    - Expired UsedJti records are swept out of the database
  </behavior>
  <action>
    Add `Lockspire.Storage.Ecto.UsedJtiRecord` to the `@schemas` list in `Lockspire.Workers.Pruner`.
    Add tests asserting `UsedJtiRecord`s are pruned correctly based on their `expires_at` column.
  </action>
  <verify>
    <automated>mix test test/lockspire/workers/pruner_test.exs</automated>
  </verify>
  <done>Worker correctly sweeps the new schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB Storage | Protects against unbounded growth with pruner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01-01 | Tampering | Ecto Repository | mitigate | Ensure strict unique constraint prevents duplicate jtise for a single client_id |
| T-44-01-02 | Denial of Service | UsedJti Storage | mitigate | Pruner background worker prevents boundless growth by sweeping `expires_at` |
</threat_model>

<verification>
mix test test/lockspire/storage/ecto/repository_used_jti_test.exs
mix test test/lockspire/workers/pruner_test.exs
</verification>

<success_criteria>
Pruner successfully deletes expired UsedJti rows and duplicates are caught via unique constraint.
</success_criteria>

<output>
After completion, create `.planning/phases/44-s01-ecto-replay-protection/44-01-PLAN-SUMMARY.md`
</output>