---
phase: 61-shared-private-key-jwt-verification
plan: 61-01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/client_auth.ex
  - lib/lockspire/protocol/client_auth/private_key_jwt.ex
  - test/lockspire/protocol/client_auth_test.exs
autonomous: true
requirements:
  - PKJWT-01
must_haves:
  truths:
    - "Direct-client auth only uses unverified JWT payload data to tentatively locate a candidate client."
    - "Inline `jwks` and Phase 60 `jwks_uri` resolution feed one shared `private_key_jwt` verifier pipeline."
    - "A cryptographically invalid `client_assertion` cannot authenticate even if its payload shape looks correct."
  artifacts:
    - path: lib/lockspire/protocol/client_auth.ex
      provides: "Shared direct-client auth entrypoint delegating `private_key_jwt` to explicit verification stages"
    - path: lib/lockspire/protocol/client_auth/private_key_jwt.ex
      provides: "Key resolution and signature-verification pipeline for client assertions"
    - path: test/lockspire/protocol/client_auth_test.exs
      provides: "Cryptographic regression proof replacing payload-only acceptance tests"
  key_links:
    - from: lib/lockspire/protocol/client_auth.ex
      to: lib/lockspire/protocol/client_auth/private_key_jwt.ex
      via: "shared stage pipeline call from `authenticate/3`"
      pattern: "PrivateKeyJwt\\."
    - from: lib/lockspire/protocol/client_auth/private_key_jwt.ex
      to: lib/lockspire/jwks_fetcher.ex
      via: "registered key resolution for `jwks_uri` clients"
      pattern: "JwksFetcher\\.(get_keys|refresh_keys)"
---

<objective>
Establish one shared `private_key_jwt` verifier architecture behind `Lockspire.Protocol.ClientAuth`.

Purpose: Implement the Phase 61 architectural split required by D-04 through D-07 so signature trust happens in one place, not as endpoint-local branching.
Output: A staged verifier pipeline that keeps tentative client lookup in `ClientAuth`, moves key resolution and signature work into a dedicated helper, and replaces payload-shape-only tests with cryptographic proofs.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/SUMMARY.md
@.planning/phases/61-shared-private-key-jwt-verification/61-CONTEXT.md
@.planning/phases/61-shared-private-key-jwt-verification/61-PATTERNS.md
@lib/lockspire/protocol/client_auth.ex
@lib/lockspire/jwks_fetcher.ex
@lib/lockspire/protocol/jar.ex
@lib/lockspire/protocol/security_profile.ex
@test/lockspire/protocol/client_auth_test.exs

<interfaces>
From `lib/lockspire/protocol/client_auth.ex`:

```elixir
@spec authenticate(map(), String.t() | nil, keyword()) :: {:ok, Client.t()} | {:error, Error.t()}
def authenticate(params, authorization, opts)
```

Current tentative client lookup pattern to preserve per D-05:

```elixir
case peek_jwt_client_id(a) do
  {:ok, client_id} -> {:ok, :private_key_jwt, client_id, a}
  :error -> {:error, invalid_client("Malformed client_assertion", :invalid_client_assertion)}
end
```

From `lib/lockspire/protocol/jar.ex`:

```elixir
@spec verify_signature(String.t(), Client.t(), [String.t()]) :: {:ok, t()} | {:error, atom()}
@spec validate_claims(t(), keyword()) :: :ok | {:error, atom()}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the staged `private_key_jwt` verifier seam</name>
  <files>lib/lockspire/protocol/client_auth.ex, lib/lockspire/protocol/client_auth/private_key_jwt.ex, test/lockspire/protocol/client_auth_test.exs</files>
  <behavior>
    - Test 1: malformed or unsigned assertions can still be peeked for tentative lookup, but they fail before authentication succeeds.
    - Test 2: a client with inline `jwks` authenticates only when signature verification succeeds against a registered public key.
    - Test 3: a client with `jwks_uri` resolves keys through the Phase 60 fetcher contract before signature verification.
  </behavior>
  <action>Refactor `ClientAuth.authenticate/3` per D-04 and D-23 so the public seam keeps request-shape parsing, tentative lookup, and registered-method checks, then delegates `:private_key_jwt` to a dedicated verifier module. In the new helper, implement explicit stages for key-source selection (`jwks` vs `jwks_uri` per D-07), JOSE signature verification using existing `Jar` patterns, and stable internal error mapping. Preserve unverified payload peeking only for candidate client lookup per D-05; remove any acceptance logic that depends on decoded-but-unverified payload claims.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/client_auth_test.exs</automated>
  </verify>
  <done>`ClientAuth` delegates `private_key_jwt` to a dedicated staged verifier, inline and remote key material flow through one shared path, and payload-only acceptance paths are gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace payload-shape tests with cryptographic verifier proof</name>
  <files>test/lockspire/protocol/client_auth_test.exs</files>
  <behavior>
    - Test 1: a properly signed assertion authenticates the registered client.
    - Test 2: a structurally valid but attacker-signed assertion fails with `invalid_client`.
    - Test 3: remote-key clients use the same verifier contract as inline-key clients.
  </behavior>
  <action>Rewrite the Phase 61 `ClientAuth` tests around real JOSE-signed fixtures rather than manual Base64 payload assembly. Use repo test helpers or local JOSE fixtures to prove the shared verifier accepts only registered key material, returns stable `reason_code` atoms on signature failure, and never authenticates on payload shape alone. Keep public failure outputs generic per D-18 even when the internal reason is more specific.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/client_auth_test.exs --seed 0</automated>
  </verify>
  <done>The core verifier is covered by cryptographic tests for both `jwks` and `jwks_uri`, and no remaining Phase 61 proof depends on fake signatures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client assertion -> `ClientAuth` | Untrusted JWT material crosses into Lockspire auth logic |
| `ClientAuth` -> `JwksFetcher` | Signature verification depends on remote key material already bounded by Phase 60 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | S | `ClientAuth` tentative lookup | mitigate | Keep decoded payload use limited to candidate lookup only; no acceptance logic before signature verification per D-05/D-06 |
| T-61-02 | T | `private_key_jwt` verifier | mitigate | Reuse JOSE strict verification with registered keys and stable algorithm gating rather than trusting payload shape |
| T-61-03 | I | auth error surface | accept | Public response remains generic `invalid_client`; detailed reasons stay internal until Plan 61-04 wires telemetry/audit |
</threat_model>

<verification>
Run the `ClientAuth` test file alone first to prove the staged verifier contract, then rerun it after later plans when claim and replay checks are tightened further.
</verification>

<success_criteria>
- `ClientAuth` is split into explicit stages matching D-04.
- `private_key_jwt` signature verification is shared for both inline `jwks` and `jwks_uri`.
- No decoded-but-unverified payload claim can independently authenticate a client.
</success_criteria>

<output>
After completion, create `.planning/phases/61-shared-private-key-jwt-verification/61-01-SUMMARY.md`
</output>
