---
phase: 61-shared-private-key-jwt-verification
plan: 61-03
type: execute
wave: 3
depends_on:
  - 61-01
  - 61-02
files_modified:
  - lib/lockspire/protocol/discovery.ex
  - lib/lockspire/protocol/introspection.ex
  - lib/lockspire/web/ciba_authorization_json.ex
  - test/lockspire/protocol/discovery_test.exs
  - test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs
  - test/lockspire/protocol/backchannel_authentication_test.exs
  - test/lockspire/web/ciba_authorization_json_test.exs
  - test/lockspire/web/discovery_controller_test.exs
autonomous: true
requirements:
  - PKJWT-06
must_haves:
  truths:
    - "Token-adjacent direct-client surfaces that reuse `ClientAuth` accept verified `private_key_jwt` consistently."
    - "Introspection no longer narrows successful `ClientAuth` results to secret-only callers."
    - "Discovery publishes `private_key_jwt` and its signing algorithms wherever runtime support is truly shared."
    - "CIBA public error JSON no longer leaks internal `reason_code` values for shared client-auth failures."
  artifacts:
    - path: lib/lockspire/protocol/introspection.ex
      provides: "Runtime truth aligned with shared `ClientAuth` capability"
    - path: lib/lockspire/protocol/discovery.ex
      provides: "Metadata truth aligned with shared runtime capability"
    - path: lib/lockspire/web/ciba_authorization_json.ex
      provides: "Backchannel auth error renderer aligned with generic public client-auth failures"
    - path: test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs
      provides: "Representative endpoint proof across shared direct-client surfaces"
  key_links:
    - from: lib/lockspire/protocol/introspection.ex
      to: lib/lockspire/protocol/client_auth.ex
      via: "post-auth caller acceptance"
      pattern: "validate_confidential_caller"
    - from: lib/lockspire/protocol/discovery.ex
      to: lib/lockspire/protocol/client_auth.ex
      via: "published shared auth methods"
      pattern: "supported_auth_method_names"
    - from: lib/lockspire/web/ciba_authorization_json.ex
      to: lib/lockspire/protocol/backchannel_authentication.ex
      via: "public error rendering for CIBA client-auth failures"
      pattern: "reason_code|error_description"
---

<objective>
Wire the verified shared capability across direct-client endpoints and remove runtime/metadata drift.

Purpose: Deliver D-23 through D-27 and PKJWT-06 by letting the corrected `ClientAuth` behavior flow through existing thin endpoint adapters, while cleaning up the known runtime, metadata, and public-error drift points.
Output: Shared endpoint regression coverage, introspection runtime cleanup, discovery metadata alignment for `private_key_jwt`, and CIBA JSON cleanup for generic public client-auth failures.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/61-shared-private-key-jwt-verification/61-CONTEXT.md
@.planning/phases/61-shared-private-key-jwt-verification/61-PATTERNS.md
@lib/lockspire/protocol/client_auth.ex
@lib/lockspire/protocol/discovery.ex
@lib/lockspire/protocol/introspection.ex
@lib/lockspire/protocol/revocation.ex
@lib/lockspire/protocol/pushed_authorization_request.ex
@lib/lockspire/protocol/device_authorization.ex
@lib/lockspire/protocol/token_exchange.ex
@lib/lockspire/protocol/backchannel_authentication.ex
@lib/lockspire/web/ciba_authorization_json.ex
@test/lockspire/protocol/discovery_test.exs

<interfaces>
Thin endpoint adapter pattern already shared across direct-client surfaces:

```elixir
case ClientAuth.authenticate(params, authorization, client_auth_options(request)) do
  {:ok, %Client{} = client} -> {:ok, client}
  {:error, %ClientAuth.Error{} = error} -> ...
end
```

Current drift cleanup targets:

```elixir
defp validate_confidential_caller(%Client{token_endpoint_auth_method: method})
    when method in [:client_secret_basic, :client_secret_post] do
  {:ok, true}
end
```

```elixir
defp published_direct_client_auth_methods do
  ClientAuth.supported_auth_method_names()
  |> Enum.reject(&(&1 == "private_key_jwt"))
end
```

```elixir
def error_response(%Error{} = error) do
  %{
    error: error.error,
    error_description: error.error_description,
    reason_code: error.reason_code
  }
  |> Map.reject(fn {_k, v} -> is_nil(v) end)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove direct-client runtime and metadata drift</name>
  <files>lib/lockspire/protocol/introspection.ex, lib/lockspire/protocol/discovery.ex, test/lockspire/protocol/discovery_test.exs, test/lockspire/web/discovery_controller_test.exs</files>
  <behavior>
    - Test 1: introspection accepts confidential callers authenticated by verified `private_key_jwt`.
    - Test 2: discovery publishes `private_key_jwt` on token, revocation, and introspection metadata once runtime support is shared.
    - Test 3: published signing algorithms mirror the same effective allowlist used by the verifier.
  </behavior>
  <action>Clean up the known runtime and metadata drift points per D-26 and D-27. Remove the secret-only post-auth narrowing in `Introspection` so successful shared `ClientAuth` results remain authoritative for confidential callers, and stop suppressing `private_key_jwt` from direct-client metadata in `Discovery`. Update both protocol-level discovery tests and controller-level discovery tests so the old Phase 59 suppression assertions are replaced with runtime-truth assertions. Keep the metadata derived from shared runtime helpers rather than endpoint-local knobs, and preserve thin adapters per D-25.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs</automated>
  </verify>
  <done>Runtime and discovery truth match: the same shared verifier capability is accepted by introspection and published in endpoint metadata without a parallel policy plane.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add representative endpoint proof and CIBA public-error cleanup</name>
  <files>lib/lockspire/web/ciba_authorization_json.ex, test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs, test/lockspire/protocol/backchannel_authentication_test.exs, test/lockspire/web/ciba_authorization_json_test.exs</files>
  <behavior>
    - Test 1: PAR, device authorization, token exchange/token, revocation, introspection, and CIBA backchannel each accept a valid `private_key_jwt` client through their existing adapter path.
    - Test 2: the same invalid assertion fails consistently across representative endpoints with generic public `invalid_client` behavior.
    - Test 3: no endpoint introduces its own `private_key_jwt` allowlist or acceptance carveout.
    - Test 4: CIBA JSON error rendering no longer exposes `reason_code` for shared client-auth failures.
  </behavior>
  <action>Create a shared regression file that exercises the representative direct-client surfaces already listed in PKJWT-06 through their existing public protocol entrypoints, including the CIBA backchannel flow. Per D-27, remove `reason_code` from `Lockspire.Web.CibaAuthorizationJSON` so shared client-auth failures stay generic in public JSON, then add a focused renderer test and a backchannel regression proving the endpoint still preserves useful protocol errors without leaking internal verifier taxonomy. Only touch endpoint modules if a real post-auth semantic difference is discovered during execution; otherwise keep the rollout test-led and adapter-thin per D-24 and D-25.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs test/lockspire/protocol/backchannel_authentication_test.exs test/lockspire/web/ciba_authorization_json_test.exs</automated>
  </verify>
  <done>Representative direct-client surfaces are covered by one shared regression suite proving they inherit verified `private_key_jwt` behavior from `ClientAuth` consistently, and CIBA no longer leaks internal `reason_code` values in public JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared verifier -> endpoint adapters | Endpoints should inherit one auth contract instead of branching independently |
| runtime capability -> discovery metadata | Published auth methods must reflect actual enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-07 | S | introspection caller validation | mitigate | Remove secret-only narrowing so verified `private_key_jwt` callers remain valid confidential clients |
| T-61-08 | I | discovery metadata | mitigate | Publish `private_key_jwt` only from shared runtime capability, preventing underclaim/overclaim drift |
| T-61-09 | T | endpoint adapters | accept | Keep endpoint modules thin; only allow post-auth semantic checks that are protocol-grounded and test-proven |
| T-61-13 | I | CIBA public error JSON | mitigate | Remove `reason_code` from renderer output so shared client-auth failures remain generic to external callers |
</threat_model>

<verification>
Run protocol and controller discovery metadata tests plus the shared endpoint regression suite after Plans 61-01 and 61-02. The suite should demonstrate that runtime truth, published truth, and CIBA public error rendering now match the shared verifier contract.
</verification>

<success_criteria>
- Introspection accepts verified `private_key_jwt` confidential callers through the shared auth seam.
- Discovery stops suppressing `private_key_jwt` where runtime support is shared.
- Representative direct-client surfaces prove consistent shared behavior without endpoint-local verifier branches.
- CIBA public JSON no longer exposes internal `reason_code` data for shared client-auth failures.
</success_criteria>

<output>
After completion, create `.planning/phases/61-shared-private-key-jwt-verification/61-03-SUMMARY.md`
</output>
