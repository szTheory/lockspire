---
phase: 61-shared-private-key-jwt-verification
plan: 61-02
type: execute
wave: 2
depends_on:
  - 61-01
files_modified:
  - lib/lockspire/protocol/client_auth.ex
  - lib/lockspire/protocol/client_auth/private_key_jwt.ex
  - lib/lockspire/protocol/security_profile.ex
  - test/lockspire/protocol/client_auth_test.exs
  - test/lockspire/storage/ecto/repository_used_jti_test.exs
autonomous: true
requirements:
  - PKJWT-02
  - PKJWT-03
  - PKJWT-04
  - PKJWT-05
must_haves:
  truths:
    - "Accepted `private_key_jwt` assertions use only issuer-approved asymmetric signing algorithms."
    - "Trusted claims enforce `iss`, `sub`, issuer-bound `aud`, `exp`, bounded lifetime, and skew-aware `iat`/`nbf`."
    - "Replay state is recorded only after signature and trusted-claim validation succeed."
    - "Replay store failure blocks authentication instead of silently weakening the security posture."
  artifacts:
    - path: lib/lockspire/protocol/client_auth/private_key_jwt.ex
      provides: "Trusted-claim validation, algorithm allowlist enforcement, and replay ordering"
    - path: lib/lockspire/protocol/security_profile.ex
      provides: "Effective signing algorithm source reused by verifier and metadata"
    - path: test/lockspire/storage/ecto/repository_used_jti_test.exs
      provides: "Durable replay uniqueness proof remains `{client_id, jti}`"
  key_links:
    - from: lib/lockspire/protocol/client_auth/private_key_jwt.ex
      to: lib/lockspire/protocol/security_profile.ex
      via: "allowed signing algorithms"
      pattern: "allowed_signing_algorithms"
    - from: lib/lockspire/protocol/client_auth/private_key_jwt.ex
      to: test/lockspire/storage/ecto/repository_used_jti_test.exs
      via: "replay record semantics and fail-closed handling"
      pattern: "record_used_jti"
---

<objective>
Harden the shared verifier with issuer-bound claim policy, algorithm allowlists, and correct replay ordering.

Purpose: Deliver the Phase 61 security contract from D-09 through D-17 so verified assertions are evaluated against one strict issuer posture and cannot poison replay state before trust is established.
Output: Claim-validation and replay-ordering logic added to the shared verifier, plus regression tests for algorithm rejection, audience enforcement, skew handling, and fail-closed replay semantics.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/61-shared-private-key-jwt-verification/61-CONTEXT.md
@.planning/phases/61-shared-private-key-jwt-verification/61-PATTERNS.md
@lib/lockspire/protocol/client_auth.ex
@lib/lockspire/protocol/client_auth/private_key_jwt.ex
@lib/lockspire/protocol/jar.ex
@lib/lockspire/protocol/security_profile.ex
@lib/lockspire/protocol/token_endpoint_dpop.ex
@test/lockspire/protocol/client_auth_test.exs
@test/lockspire/storage/ecto/repository_used_jti_test.exs

<interfaces>
From `lib/lockspire/protocol/security_profile.ex`:

```elixir
@spec allowed_signing_algorithms(ServerPolicy.security_profile()) :: [String.t()]
```

Replay-ordering precedent from `lib/lockspire/protocol/token_endpoint_dpop.ex`:

```elixir
with {:ok, proof} <- validate_proof_with_flag(...),
     :ok <- record_dpop_proof_use(proof, request) do
  {:ok, issuance_context(...)}
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce trusted-claim policy and algorithm allowlists</name>
  <files>lib/lockspire/protocol/client_auth/private_key_jwt.ex, lib/lockspire/protocol/security_profile.ex, test/lockspire/protocol/client_auth_test.exs</files>
  <behavior>
    - Test 1: `alg=none`, symmetric algorithms, and algorithms outside the effective allowlist are rejected.
    - Test 2: `iss` and `sub` must both match the authenticated `client_id`.
    - Test 3: `aud` must match the issuer identifier string, not endpoint URLs.
    - Test 4: expired, future-skewed, and over-long assertions fail with stable internal reasons.
  </behavior>
  <action>Extend the shared verifier per D-09 through D-13 using one effective algorithm source from `SecurityProfile`, not a parallel config plane. Reuse `Jar.validate_claims/2` structure or extracted helpers to enforce issuer-bound audience, bounded lifetime, and controlled skew for `exp`, `iat`, and `nbf`. Keep public failures generic per D-18 while assigning detailed internal `reason_code` atoms per D-20 for algorithm mismatch, invalid audience, missing claims, and expired assertions.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/client_auth_test.exs</automated>
  </verify>
  <done>`private_key_jwt` claim validation is fully trusted-claims-based, audience is issuer-bound, and allowed algorithms are derived only from effective issuer posture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Record replay only after verification and fail closed on store errors</name>
  <files>lib/lockspire/protocol/client_auth/private_key_jwt.ex, test/lockspire/protocol/client_auth_test.exs, test/lockspire/storage/ecto/repository_used_jti_test.exs</files>
  <behavior>
    - Test 1: invalid signatures and invalid claims never write a replay record.
    - Test 2: accepted assertions record `{client_id, jti}` replay state until verified `exp + skew`.
    - Test 3: replay attempts fail with `invalid_client`.
    - Test 4: replay-store write failures fail closed instead of succeeding without replay protection.
  </behavior>
  <action>Implement D-14 through D-17 in the shared verifier: signature verification and trusted-claim validation must complete before building the `UsedJti` record, replay expiry must reflect the effective acceptance window rather than raw `exp` alone, and storage failures must reject authentication. Preserve the existing durable replay uniqueness model in the repository tests, and add verifier tests proving invalid assertions cannot poison replay state.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/client_auth_test.exs test/lockspire/storage/ecto/repository_used_jti_test.exs</automated>
  </verify>
  <done>Replay recording is post-verification only, accepted assertions persist replay state with the correct expiry window, and store failures cannot silently weaken client authentication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verified JWT claims -> auth decision | Time, audience, and issuer claims control acceptance |
| verifier -> durable replay store | Security posture depends on correct post-verification replay recording |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-04 | S | signing algorithm selection | mitigate | Use `SecurityProfile.allowed_signing_algorithms/1`; reject `alg=none`, symmetric, and unsupported algorithms per D-09/D-10 |
| T-61-05 | R | claim validation | mitigate | Enforce `iss`, `sub`, issuer-bound `aud`, TTL, `iat`, and `nbf` only after signature verification |
| T-61-06 | D | replay store | mitigate | Record `{client_id, jti}` only after verification and fail closed on store errors per D-14 to D-17 |
</threat_model>

<verification>
Run the focused `ClientAuth` and `UsedJti` suites together. A valid plan result includes both success-path authentication and negative-path rejection without replay-store poisoning.
</verification>

<success_criteria>
- Shared verifier rejects unsupported algorithms and wrong audiences.
- Time-based claim validation uses bounded lifetime plus controlled skew.
- Replay persistence happens after verification and fails closed on storage failure.
</success_criteria>

<output>
After completion, create `.planning/phases/61-shared-private-key-jwt-verification/61-02-SUMMARY.md`
</output>
