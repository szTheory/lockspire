---
phase: 61-shared-private-key-jwt-verification
plan: 61-04
type: execute
wave: 4
depends_on:
  - 61-02
  - 61-03
files_modified:
  - lib/lockspire/protocol/client_auth.ex
  - lib/lockspire/protocol/client_auth/private_key_jwt.ex
  - lib/lockspire/observability.ex
  - lib/lockspire/redaction.ex
  - lib/lockspire/audit/event.ex
  - test/lockspire/audit/event_test.exs
  - test/lockspire/protocol/client_auth_test.exs
  - test/lockspire/redaction/redaction_test.exs
  - test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs
autonomous: true
requirements:
  - OBS-01
must_haves:
  truths:
    - "Auth failures and replay outcomes emit stable internal telemetry with redaction-safe metadata."
    - "Security-significant `private_key_jwt` failures can be audited without storing raw assertions or remote JWKS material."
    - "Public endpoint error bodies remain generic even when internal observability captures detailed reason codes."
  artifacts:
    - path: lib/lockspire/protocol/client_auth/private_key_jwt.ex
      provides: "Shared failure/outcome emission for verifier and replay events"
    - path: lib/lockspire/redaction.ex
      provides: "Explicit dropping of assertion and key-material fields from telemetry/audit"
    - path: lib/lockspire/audit/event.ex
      provides: "Normalized durable audit shape for replay and client-auth verification outcomes"
    - path: test/lockspire/audit/event_test.exs
      provides: "Focused audit normalization proof for shared verifier events"
  key_links:
    - from: lib/lockspire/protocol/client_auth/private_key_jwt.ex
      to: lib/lockspire/observability.ex
      via: "shared telemetry emission"
      pattern: "Observability\\.emit"
    - from: lib/lockspire/protocol/client_auth/private_key_jwt.ex
      to: lib/lockspire/audit/event.ex
      via: "normalized durable audit event creation"
      pattern: "Audit\\.Event|append_audit_event"
    - from: lib/lockspire/protocol/client_auth/private_key_jwt.ex
      to: lib/lockspire/redaction.ex
      via: "metadata redaction of assertion and JWKS details"
      pattern: "Redaction"
---

<objective>
Close the internal observability, audit, and redaction story for shared `private_key_jwt` verification.

Purpose: Deliver OBS-01 and D-18 through D-22 so operators get stable internal reasons for auth failures and replay outcomes without widening public error leakage or leaking raw assertions anywhere in the stack.
Output: Shared telemetry/audit emission for verifier outcomes, explicit redaction coverage, and regression proof that public errors stay generic while internal metadata stays useful.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/61-shared-private-key-jwt-verification/61-CONTEXT.md
@.planning/phases/61-shared-private-key-jwt-verification/61-PATTERNS.md
@lib/lockspire/protocol/client_auth.ex
@lib/lockspire/protocol/client_auth/private_key_jwt.ex
@lib/lockspire/observability.ex
@lib/lockspire/redaction.ex
@lib/lockspire/audit/event.ex
@lib/lockspire/protocol/revocation.ex
@lib/lockspire/protocol/introspection.ex
@lib/lockspire/protocol/token_exchange.ex
@test/lockspire/redaction/redaction_test.exs

<interfaces>
Observability seam:

```elixir
@spec emit(entity(), action(), measurements(), metadata()) :: :ok
def emit(entity, action, measurements \\\\ %{}, metadata \\\\ %{})
```

Audit normalization seam:

```elixir
@spec normalize(t() | map()) :: t()
```

Existing redaction drop keys already include:

```elixir
@telemetry_drop_keys MapSet.new([:authorization, :client_secret, :params, :payload, :raw_payload, ...])
@audit_drop_keys MapSet.new([:authorization, :client_secret, :params, :payload, :raw_payload, ...])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit shared telemetry and durable audit for verifier failures and replay outcomes</name>
  <files>lib/lockspire/protocol/client_auth.ex, lib/lockspire/protocol/client_auth/private_key_jwt.ex, lib/lockspire/audit/event.ex, test/lockspire/audit/event_test.exs, test/lockspire/protocol/client_auth_test.exs</files>
  <behavior>
    - Test 1: remote JWKS fetch failure, bad signature, invalid audience, and replay each emit stable internal `reason_code` values.
    - Test 2: replay detection and resolved-client verification failures can produce normalized durable audit events when an audit store is available.
    - Test 3: public-facing auth failures remain generic `invalid_client` despite detailed internal telemetry/audit reasons.
  </behavior>
  <action>Centralize Phase 61 failure emission inside the shared verifier per D-20 through D-21 so every direct-client endpoint benefits uniformly. Emit redaction-safe telemetry for auth failures and replay outcomes, and append durable audit events only for security-significant, client-attributable outcomes such as replay detection or post-lookup verification failure. Do not broaden endpoint-local error bodies or leak internal transport details per D-18 and D-19.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/client_auth_test.exs test/lockspire/audit/event_test.exs</automated>
  </verify>
  <done>Shared `private_key_jwt` verification emits stable internal telemetry and focused durable audit events without changing the public `invalid_client` surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove redaction coverage for assertions, payloads, and remote-key material</name>
  <files>lib/lockspire/redaction.ex, test/lockspire/redaction/redaction_test.exs, test/lockspire/protocol/client_auth_test.exs, test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs</files>
  <behavior>
    - Test 1: telemetry and audit metadata never include raw `client_assertion`, decoded claims payloads, JOSE headers, or remote JWKS response bodies.
    - Test 2: endpoint-level regression tests confirm auth failure responses remain generic while internal telemetry retains stable `reason_code` atoms.
  </behavior>
  <action>Extend redaction rules per D-22 if any new metadata keys are introduced by the verifier work, and add proof that internal observability sees only safe identifiers, handles, and reason codes. Use regression tests to assert that endpoint-visible JSON does not widen beyond generic `invalid_client` semantics even when internal telemetry and audit events are richer.</action>
  <verify>
    <automated>mix test test/lockspire/redaction/redaction_test.exs test/lockspire/protocol/client_auth_test.exs test/lockspire/protocol/direct_client_auth_private_key_jwt_test.exs</automated>
  </verify>
  <done>Redaction proof explicitly covers client assertions and remote-key material, and internal observability detail does not leak into public endpoint responses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier outcomes -> telemetry/audit | Internal security signals must be useful without storing secrets |
| internal reasons -> public endpoint responses | External callers must not learn verifier internals from error bodies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-10 | I | telemetry/audit metadata | mitigate | Redact raw assertions, decoded payloads, headers, and JWKS bodies before emission per D-22 |
| T-61-11 | R | audit evidence | mitigate | Normalize durable audit events for replay and verification failures using stable reason codes and client/resource identity |
| T-61-12 | I | public error payloads | mitigate | Preserve generic `401 invalid_client` responses while routing detail to internal observability only |
</threat_model>

<verification>
Run the shared verifier suite, focused audit/redaction suites, and direct-client regression suite with telemetry/audit assertions enabled. A successful result proves both internal signal quality and absence of public leakage.
</verification>

<success_criteria>
- Internal telemetry captures remote-JWKS, signature, audience, and replay failures with stable reasons.
- Durable audit covers security-significant verifier outcomes without recording raw assertions or key material.
- Public endpoint failures remain generic `invalid_client`.
</success_criteria>

<output>
After completion, create `.planning/phases/61-shared-private-key-jwt-verification/61-04-SUMMARY.md`
</output>
