---
phase: 130-readable-code-sustainable-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/lockspire/adoption_demo_docker_contract_test.exs
  - compatibility/phoenix_1_8_live_view_1_1/mix.exs
  - compatibility/phoenix_1_8_live_view_1_1/mix.lock
  - compatibility/phoenix_1_8_live_view_1_1/lib/lockspire_compatibility_fixture.ex
  - test/fixtures/phoenix_1_8_live_view_1_1/mix.exs
  - test/fixtures/phoenix_1_8_live_view_1_1/mix.lock
  - test/fixtures/phoenix_1_8_live_view_1_1/lib/lockspire_compatibility_fixture.ex
  - .github/workflows/ci.yml
  - test/lockspire/compatibility_baseline_contract_test.exs
  - test/lockspire/ci_static_contract_test.exs
autonomous: true
requirements: [TEST-01, TEST-02, CI-02, READ-01, READ-02, CLEAN-01]
must_haves:
  truths:
    - "The ordinary root test command finishes green before structural cleanup begins."
    - "Root test discovery emits no warning for the committed Phoenix/LiveView compatibility project."
    - "The exact Phoenix 1.8.5 and LiveView 1.1.28 compatibility proof remains required in CI."
  artifacts:
    - path: "test/lockspire/adoption_demo_docker_contract_test.exs"
      provides: "Accurate source contract for direct and loop-owned demo volume cleanup"
    - path: "compatibility/phoenix_1_8_live_view_1_1/mix.exs"
      provides: "Standalone compatibility project outside the root test discovery tree"
    - path: ".github/workflows/ci.yml"
      provides: "Compatibility job wired to the relocated fixture"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "compatibility/phoenix_1_8_live_view_1_1/mix.lock"
      via: "cache key, locked dependency install, compile, and drift check"
      pattern: "compatibility/phoenix_1_8_live_view_1_1"
---

<objective>
Restore a clean end-to-end test baseline and remove root test-discovery noise without weakening compatibility proof.

Purpose: Make every later structural edit start from a trustworthy green signal.
Output: A green root suite, an accurately expressed Docker reset contract, and a warning-free compatibility fixture location.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-executable-quality-baselines/127-02-SUMMARY.md
@test/lockspire/adoption_demo_docker_contract_test.exs
@examples/adoption_demo/bin/docker-reset
@.github/workflows/ci.yml
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Restore the ordinary full-suite signal through the failing Docker reset contract</name>
  <files>test/lockspire/adoption_demo_docker_contract_test.exs</files>
  <behavior>
    - The contract recognizes the explicitly named database volume and the two cache-volume suffixes produced by the reset helper.
    - The contract still rejects broad Docker cleanup and volumes outside the three-item allowlist.
  </behavior>
  <action>Replace the word-boundary scan that misses `${project}_db_data` because the underscore is a word character. Assert the database volume target directly, parse the cache suffix loop separately, and retain the broad-cleanup denials. Change test interpretation only; do not alter the already-correct reset script or its public CLI behavior.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/adoption_demo_docker_contract_test.exs</automated>
  </verify>
  <done>The previously failing contract passes while proving the same exact three-volume cleanup boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Relocate the compatibility project outside root test discovery</name>
  <files>compatibility/phoenix_1_8_live_view_1_1/mix.exs, compatibility/phoenix_1_8_live_view_1_1/mix.lock, compatibility/phoenix_1_8_live_view_1_1/lib/lockspire_compatibility_fixture.ex, test/fixtures/phoenix_1_8_live_view_1_1/mix.exs, test/fixtures/phoenix_1_8_live_view_1_1/mix.lock, test/fixtures/phoenix_1_8_live_view_1_1/lib/lockspire_compatibility_fixture.ex, .github/workflows/ci.yml, test/lockspire/compatibility_baseline_contract_test.exs, test/lockspire/ci_static_contract_test.exs</files>
  <action>Move the committed standalone Mix project to `compatibility/phoenix_1_8_live_view_1_1`, correct its relative Lockspire path dependency and tmp build/deps paths, and update every CI cache, working-directory, lock-drift, and contract reference. Preserve exact Phoenix 1.8.5 and LiveView 1.1.28 pins and the PostgreSQL 14 minimum lane. Capture root test output and assert the configured test-load warning is absent.</action>
  <verify>
    <automated>cd compatibility/phoenix_1_8_live_view_1_1 &amp;&amp; mix deps.get --check-locked &amp;&amp; mix compile --warnings-as-errors &amp;&amp; cd ../.. &amp;&amp; MIX_ENV=test mix test --max-failures 1 2&gt;&amp;1 | tee /tmp/lockspire-phase130-suite.log &amp;&amp; ! rg -q 'test_load_filters|test_ignore_filters' /tmp/lockspire-phase130-suite.log</automated>
  </verify>
  <done>The exact-version host fixture compiles from its new location and the root suite is green without fixture discovery warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository source → CI claims | Test and fixture paths determine whether compatibility and cleanup claims are actually exercised. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-01 | Tampering | Docker reset contract | medium | mitigate | Preserve explicit allowlist and broad-cleanup denial assertions while repairing only the parser. |
| T-130-02 | Repudiation | compatibility lane | medium | mitigate | Keep exact lock, compile, and drift checks in CI and executable contracts after relocation. |
| T-130-03 | Denial of service | root test discovery | low | accept | Moving a standalone fixture out of `test/` removes warning noise without adding runtime work. |
</threat_model>

<verification>
Run the compatibility fixture compile, focused Docker contract, root suite, and CI source contracts.
</verification>

<success_criteria>
The repository has a green, warning-free root test baseline and unchanged compatibility guarantees.
</success_criteria>

<output>
Create `.planning/phases/130-readable-code-sustainable-proof/130-01-SUMMARY.md` when done.
</output>
