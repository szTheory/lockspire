---
phase: 44-api-stabilization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/dpop.ex
  - lib/lockspire/workers/backchannel_logout_delivery_worker.ex
  - lib/lockspire/host/context.ex
autonomous: true
requirements:
  - STAB-01

must_haves:
  truths:
    - Dialyzer runs without existing errors in dpop and backchannel logout.
    - The Host Context struct is formally defined and available for callbacks.
  artifacts:
    - path: lib/lockspire/host/context.ex
      provides: Strict types and struct for host interaction context
      contains: "@type t :: %__MODULE__"
  key_links:
    - from: "lib/lockspire/host/account_resolver.ex"
      to: "lib/lockspire/host/context.ex"
      via: "callback context parameter"
      pattern: "Lockspire\\.Host\\.Context\\.t\\(\\)"
---

<objective>
Fix existing Dialyzer errors to establish a clean baseline and define the new `Lockspire.Host.Context` struct.

Purpose: We need a clean Dialyzer slate before strictly typing the public API. We also need the `Context` struct to replace `map()` in host callbacks.
Output: Clean dialyzer run and a new `Context` struct module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/44-api-stabilization/44-CONTEXT.md
@.planning/phases/44-api-stabilization/44-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve existing Dialyzer errors</name>
  <files>
    - lib/lockspire/protocol/dpop.ex
    - lib/lockspire/workers/backchannel_logout_delivery_worker.ex
  </files>
  <action>
    Fix the 4 existing Dialyzer pattern matching/type errors in `dpop.ex` and `backchannel_logout_delivery_worker.ex`.
    Ensure `mix dialyzer` passes cleanly for these modules.
  </action>
  <verify>
    <automated>mix dialyzer</automated>
  </verify>
  <done>Dialyzer reports no errors in these two files.</done>
</task>

<task type="auto">
  <name>Task 2: Define Lockspire.Host.Context</name>
  <files>
    - lib/lockspire/host/context.ex
  </files>
  <action>
    Create the `%Lockspire.Host.Context{}` struct.
    It should include fields like `:return_to`, `:client_id`, `:scopes`, and `:interaction_type` with strong types (`@type t :: ...`).
    This replaces `map()` to provide auto-complete and strict typing for host integrations.

    Follow the struct definition pattern from `lib/lockspire/domain/client.ex`:
    ```elixir
    @type t :: %__MODULE__{
            client_id: String.t(),
            client_type: client_type(),
            # ...
          }

    defstruct [
      :client_id,
      :client_type,
      # ...
    ]
    ```
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Module compiles cleanly and struct is available.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Callback Seam | Host implementations must correctly handle internal struct contexts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Tampering | Lockspire.Host.Context | mitigate | Enforce strict structural types via Dialyzer to prevent malformed data crossing the host boundary. |
</threat_model>

<verification>
Ensure `mix dialyzer` runs successfully on the codebase.
</verification>

<success_criteria>
`mix dialyzer` exits 0 and `lib/lockspire/host/context.ex` defines a valid struct.
</success_criteria>

<output>
After completion, create `.planning/phases/44-api-stabilization/44-01-PLAN-SUMMARY.md`
</output>