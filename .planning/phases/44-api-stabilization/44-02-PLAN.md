---
phase: 44-api-stabilization
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - lib/lockspire.ex
  - lib/lockspire/admin.ex
  - lib/lockspire/clients.ex
  - lib/lockspire/config.ex
  - lib/lockspire/host/account_resolver.ex
  - test/lockspire/host/account_resolver_test.exs
autonomous: true
requirements:
  - STAB-01

must_haves:
  truths:
    - All public facade modules have explicit and complete Typespecs for their functions and delegates.
    - Host integration callbacks rely on explicit union types and the new Context struct.
  artifacts:
    - path: lib/lockspire/admin.ex
      provides: Fully typed public API facade
      contains: "@spec"
    - path: lib/lockspire/host/account_resolver.ex
      provides: Strictly typed callback behaviour
      contains: "Lockspire.Host.Context.t()"
  key_links: []
---

<objective>
Apply strict `@spec` definitions to all public API facade modules and lock the `AccountResolver` callback signatures.

Purpose: Finalize the API contract for the 1.0 GA release by ensuring full type safety and documentation on public boundaries.
Output: Fully typed `Lockspire`, `Lockspire.Admin`, `Lockspire.Clients`, `Lockspire.Config`, and `Lockspire.Host.AccountResolver`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/44-api-stabilization/44-CONTEXT.md
@.planning/phases/44-api-stabilization/44-PATTERNS.md

<interfaces>
From lib/lockspire/host/context.ex:
```elixir
defmodule Lockspire.Host.Context do
  @type t :: %__MODULE__{
    return_to: String.t() | nil,
    client_id: String.t() | nil,
    scopes: [String.t()] | nil,
    interaction_type: :login | :consent | :logout | nil
  }
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Typespecs to Public Facades</name>
  <files>
    - lib/lockspire.ex
    - lib/lockspire/admin.ex
    - lib/lockspire/clients.ex
    - lib/lockspire/config.ex
  </files>
  <action>
    Add explicit `@spec` definitions for every function and `defdelegate` in the public facade modules (`Lockspire`, `Lockspire.Admin`, `Lockspire.Clients`, `Lockspire.Config`).
    Document config accessors to avoid string vs atom footguns.
    Validate that no breaking config key renames are introduced.

    Follow the typespec and delegation pattern from `lib/lockspire/admin/clients.ex`:
    ```elixir
    @spec list_clients(keyword()) :: {:ok, [Client.t()]} | {:error, term()}
    def list_clients(opts \\ []) do
      Repository.list_clients(opts)
    end
    ```
  </action>
  <verify>
    <automated>mix dialyzer</automated>
  </verify>
  <done>All `defdelegate`s and public functions have corresponding accurate `@spec`s.</done>
</task>

<task type="auto">
  <name>Task 2: Lock AccountResolver Signatures</name>
  <files>
    - lib/lockspire/host/account_resolver.ex
  </files>
  <action>
    Update the `@callback`s in `Lockspire.Host.AccountResolver` to replace generic `term()` and `map()` types.
    Use `Plug.Conn.t() | Phoenix.LiveView.Socket.t() | term()` for connections and `Lockspire.Host.Context.t()` for context payloads.

    Follow the callback and custom types pattern from `lib/lockspire/storage/token_store.ex`:
    ```elixir
    @type store_error :: term()
    @callback store_token(Token.t()) :: {:ok, Token.t()} | {:error, store_error()}
    ```
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Callbacks use strictly defined types instead of generics.</done>
</task>

<task type="auto">
  <name>Task 3: Update Test Suite to use Context Struct</name>
  <files>
    - test/lockspire/host/account_resolver_test.exs
  </files>
  <action>
    Update tests covering `AccountResolver` callbacks to pass `%Lockspire.Host.Context{}` structs rather than plain maps, ensuring tests align with the new strict types. If other tests fail due to this change, apply the same fix across the suite.
  </action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>Tests pass cleanly under the new contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Callback Seam | Host implementations must correctly handle internal struct contexts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02 | Elevation of Privilege | Lockspire.Admin | mitigate | Strong static types ensure that internal APIs only receive correctly formatted domain entities, preventing unexpected execution paths. |
</threat_model>

<verification>
Run `mix dialyzer` and `mix test`. Both must pass with zero errors.
</verification>

<success_criteria>
Public API contract is finalized, callbacks use explicit structs, and the test suite passes with full static validation.
</success_criteria>

<output>
After completion, create `.planning/phases/44-api-stabilization/44-02-PLAN-SUMMARY.md`
</output>