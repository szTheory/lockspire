---
phase: 139-required-truth-reconciliation
plan: "02"
type: execute
wave: 2
depends_on: ["139-01"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/support/lockspire/release_proof/package_assertions.ex
  - test/lockspire/release/repository_hygiene_contract_test.exs
autonomous: true
requirements: [QUAL-05, HYGIENE-06]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "QUAL-05: the repository-owned workflow/shell lint lane passes without suppressions, exclusions, or weakening its existing rules."
    - "QUAL-05: active release-proof fixtures pass the proof-quality baseline without embedding phase-numbered labels as brittle source literals."
    - "HYGIENE-06 D-07: only the four observed ShellCheck findings and thirteen observed semantic-label findings are corrected in this plan; no unrelated cleanup campaign is authorized."
    - "HYGIENE-06 unclassified: each correction has a repeatable failing repository-owned gate, an exact source location or semantic attribute, and a focused green regression; anything without that evidence remains unplanned."
    - "D-09: repairs stay inside the existing inventory shell and focused ExUnit support boundary and add no maintainer subsystem or product/runtime surface."
  prohibitions:
    - statement: "Making the local gate green must not add ShellCheck suppressions, weaken proof-quality matching, exclude active files, delete lifecycle assertions, or replace failures with ignored exits."
      verification: flagged-unverified
    - statement: "The narrow gate repair must not become broad refactoring, dependency work, protocol work, or Phase 140 cleanup."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "ShellCheck-clean explicit branching and removal or use of the four demonstrated dead locals without relation-semantic drift."
      contains: "validate_github_rows"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Semantic phase-label composition for all thirteen active proof fixtures plus focused behavior assertions."
      contains: "@baseline_phase_label"
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused regression entry points proving the narrow gate repair."
      contains: "repository hygiene"
  key_links:
    - from: scripts/maintainer/baseline_inventory.sh
      to: scripts/ci/lint_workflows.sh
      via: "existing ShellCheck/actionlint lane observes explicit shell structure with no suppression"
      pattern: "validate_github_rows"
    - from: test/support/lockspire/release_proof/package_assertions.ex
      to: "the repository proof-quality baseline"
      via: "semantic attributes construct fixture phase labels at runtime rather than brittle source literals"
      pattern: "@baseline_phase_number|@next_phase_number|@action_phase_number|@closure_phase_number"
---

<objective>
Restore the existing local acceptance gates by repairing only the demonstrated shell-lint and proof-quality regressions.

Purpose: QUAL-05 cannot become final evidence while workflow lint and the active proof-quality baseline are red; HYGIENE-06 permits only repeatable repository-owned gaps to earn deterministic corrections.
Output: ShellCheck-clean inventory code and semantic phase-label fixtures that preserve every existing safety contract.

Decision coverage: D-07 limits the work to observed deterministic gaps and D-09 keeps every change inside existing repo-local shell and focused ExUnit proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/139-required-truth-reconciliation/139-CONTEXT.md
@.planning/phases/139-required-truth-reconciliation/139-RESEARCH.md
@.planning/phases/139-required-truth-reconciliation/139-PATTERNS.md
@.planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
@scripts/maintainer/baseline_inventory.sh
@scripts/ci/lint_workflows.sh
@test/support/lockspire/release_proof/package_assertions.ex
@test/lockspire/release/repository_hygiene_contract_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the four demonstrated shell-lint findings without semantic drift</name>
  <reversibility rating="reversible">Each repair is a local shell expression or dead-local correction covered by existing behavior contracts.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/support/lockspire/release_proof/package_assertions.ex, test/lockspire/release/repository_hygiene_contract_test.exs</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — inspect validate_github_rows and the observed locals near the research-recorded SC2209/SC2034 sites while preserving collection and relation behavior
    - scripts/ci/lint_workflows.sh — retain the exact ShellCheck/actionlint invocation and failure policy
    - test/support/lockspire/release_proof/package_assertions.ex — reuse current collector and relation fixtures to characterize behavior before source edits
    - test/lockspire/release/repository_hygiene_contract_test.exs — use the full focused suite as the drift backstop
  </read_first>
  <behavior>
    - RED: workflow lint reports SC2209 for the conditional assignment and SC2034 for the three unused locals identified by research.
    - GREEN: validate_github_rows uses explicit if/else assignment and preserves separate pull-request versus issue semantics.
    - GREEN: each unused local is either removed or consumed by the invariant it was intended to prove; no suppression directive is added.
    - GREEN: the complete repository-hygiene contract retains Phase 138 collection, redaction, relation, publication, and recovery behavior.
  </behavior>
  <action>First capture the four exact lint codes and focused repository-hygiene baseline as failing proof. Replace the SC2209 conditional assignment in `validate_github_rows` with explicit if/else control flow. At each SC2034 site recorded in research, trace the value once and either delete genuinely dead assignment state or wire the value into the existing equality/invariant that already owns it; do not invent new output or state. Add the smallest focused assertion needed to prove branch-specific GitHub validation and unchanged relation/publication semantics. Do not add shellcheck directives, `|| true` fallbacks, global lint exclusions, or broad mechanical rewrites.</action>
  <acceptance_criteria>
    - scripts/ci/lint_workflows.sh reports no finding at the four demonstrated sites.
    - Pull-request and issue validation still select their distinct exact kinds and all existing collector/relation fixtures pass.
    - The diff is confined to explicit branching, proven dead-state removal/use, and the focused assertion needed to protect behavior.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; bash scripts/ci/lint_workflows.sh</automated>
    <fails_when>shell syntax or workflow lint exits nonzero, ExUnit exits nonzero or runs zero tests, any ShellCheck suppression appears, PR/issue validation collapses, or an existing collector/relation test fails</fails_when>
  </verify>
  <done>The four observed shell-lint failures are corrected narrowly and the complete inventory behavior remains green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose all active phase proof labels from semantic attributes</name>
  <reversibility rating="reversible">Fixture-label construction is test-only and can be locally adjusted without changing repository evidence or runtime behavior.</reversibility>
  <files>test/support/lockspire/release_proof/package_assertions.ex, test/lockspire/release/repository_hygiene_contract_test.exs</files>
  <read_first>
    - test/support/lockspire/release_proof/package_assertions.ex — use the existing baseline/next/action/closure number, label, and commit-prefix attributes and preserve every lifecycle fixture
    - test/lockspire/release/repository_hygiene_contract_test.exs — keep named behavior entry points and existing timeout tags intact
    - .planning/phases/139-required-truth-reconciliation/139-RESEARCH.md — use the exact thirteen active source locations and do not broaden the repair beyond them
  </read_first>
  <behavior>
    - RED: the repository proof-quality gate reports all thirteen active phase-numbered source literals.
    - GREEN: fixture text, filenames, commit subjects, state labels, and headings compose their phase portions from semantic module attributes.
    - GREEN: generated fixture bytes remain identical to their intended Phase 138/139/140/141 scenarios.
    - GREEN: no lifecycle, currentness, redaction, or transition assertion is deleted or excluded.
  </behavior>
  <action>Replace each of the thirteen research-identified active phase-number literals with concatenation or interpolation of the existing semantic attributes such as `@baseline_phase_number`, `@next_phase_number`, `@action_phase_number`, `@closure_phase_number`, their label forms, and `@baseline_phase_commit_prefix`. Preserve the rendered fixture strings exactly so only source maintainability changes. Where two fixtures need the same semantic fragment, reuse the existing attribute rather than adding another numbered constant. Keep the proof-quality regex and included-source set unchanged. Add a focused assertion only if a rendered string could otherwise drift undetected.</action>
  <acceptance_criteria>
    - The proof-quality baseline finds none of the thirteen active source literals while still scanning package_assertions.ex.
    - Every affected fixture continues to produce the same phase-specific content and all lifecycle tests remain present.
    - No exclusion, regex weakening, or generated-evidence rewrite is introduced.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; mix test test/lockspire/release_readiness_contract_test.exs</automated>
    <fails_when>either Mix command exits nonzero or runs zero tests, a phase-numbered source literal remains at a reported active site, rendered fixture semantics change, or the proof-quality source set/rule is weakened</fails_when>
  </verify>
  <done>All thirteen observed proof-label regressions are repaired through semantic attributes and the focused proof suites are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Shell source -> lint verdict | Gate authority depends on fixing code, not suppressing or bypassing the linter. |
| Test source -> generated fixture | Semantic attributes must render the exact historical/current phase bytes expected by lifecycle tests. |
| Focused green result -> QUAL-05 evidence | A partial or zero-test run cannot stand in for the repository-owned gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-139-07 | Tampering | shell lint policy | high | mitigate | Prohibit suppressions/fallback exits and run the unchanged workflow-lint script plus full focused contracts. |
| T-139-08 | Spoofing | generated phase fixtures | medium | mitigate | Compose from existing semantic attributes and assert rendered scenario bytes. |
| T-139-09 | Repudiation | claimed gate repair | medium | mitigate | Tie each source change to one recorded SC code/location or proof-quality finding and retain command output in the summary. |
| T-139-10 | Denial of service | oversized cleanup diff | low | accept | The exact four plus thirteen finding inventory bounds review and execution cost. |
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: explicit PR/issue kind branching and resolved SC2034 values at the four demonstrated ShellCheck sites; no new CLI or output field.
- `test/support/lockspire/release_proof/package_assertions.ex`: semantic compositions from the existing `@baseline_phase_number`, `@next_phase_number`, `@action_phase_number`, `@closure_phase_number`, phase-label, and commit-prefix symbols; no new phase-number constant.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: only any focused assertion/tag needed to prove the narrow corrections.
- No new subsystem, package, workflow, release artifact, or cleanup target is produced.

<verification>
  <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs &amp;&amp; mix test test/lockspire/release_readiness_contract_test.exs &amp;&amp; bash scripts/ci/lint_workflows.sh</automated>
  <fails_when>any command exits nonzero, ExUnit runs zero tests, any demonstrated lint/proof finding remains, a guardrail is suppressed/weakened, or inventory/release readiness behavior regresses</fails_when>
</verification>

<success_criteria>
- The workflow/shell lint lane is green with its policy intact.
- The proof-quality baseline is green with all active files and regexes intact.
- Every code change maps to a repeatable research-demonstrated failure.
- Existing Phase 138 inventory and release-proof behavior remains green.
</success_criteria>

<output>
Create `.planning/phases/139-required-truth-reconciliation/139-02-SUMMARY.md` recording the exact corrected findings and focused commands.
</output>
