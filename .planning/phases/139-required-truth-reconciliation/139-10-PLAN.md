---
phase: 139-required-truth-reconciliation
plan: "10"
type: execute
wave: 8
depends_on: ["139-09"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/support/lockspire/release_proof/package_assertions.ex
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [CI-08, QUAL-05, HYGIENE-05, TRUTH-03, TRUTH-04]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - The exact Phase 139 acceptance fixtures authenticate a host-sealed candidate and authorize only the expected snapshot relation when their writer descriptors come from one identical GSD runtime root.
    - Forged writer metadata, stale or unavailable snapshot evidence, and failed candidate authentication remain blocked before local or remote ref mutation.
    - The acceptance receipt fixtures continue to prove same-SHA required evidence, historical 1.5.0 traceability, and redacted non-certifying OIDF classification.
  artifacts:
    - scripts/maintainer/baseline_inventory.sh
    - test/support/lockspire/release_proof/package_assertions.ex
    - test/lockspire/release/repository_hygiene_contract_test.exs
    - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
  key_links:
    - The exact GSD tools path selected for fixture sealing is the path whose workflow bytes and file modes the production relation validator checks.
    - The exact acceptance selectors exercise the checked-in finalizer and relation verifier through a temporary repository and fake remote without changing this checkout's refs.
---

<objective>
Repair the Phase 139 exact-acceptance fixture path that currently rejects its own writer descriptor and falls through to `refresh_required` before proving the sealed-candidate success path.

Purpose: Restore current automated proof for the repository-owned acceptance path while retaining fail-closed writer, snapshot, and sealed-candidate checks. The Phase 140 exact-SHA entry gate and its live receipt requirement remain unchanged.
Output: Consistent runtime-root selection in acceptance fixtures and relation validation, regression coverage for the successful and rejected paths, and an updated validation map that still classifies external synchronized-main evidence as deferred.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-required-truth-reconciliation/139-CONTEXT.md
@.planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md
@.planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
@.planning/phases/139-required-truth-reconciliation/139-09-SUMMARY.md
@scripts/maintainer/baseline_inventory.sh
@scripts/maintainer/finalize_phase_139_acceptance.sh
@test/support/lockspire/release_proof/package_assertions.ex
@test/lockspire/release/repository_hygiene_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind sealed acceptance fixtures and relation checks to one exact GSD runtime root</name>
  <files>scripts/maintainer/baseline_inventory.sh, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — inspect `resolve_gsd_tools`, `validate_post_transition_receipt`, and the Phase 139 sealed-candidate relation path; retain exact descriptor and stale-relation rejection.
    - tools/gsd-capabilities/lockspire-phase-finalizer/post-completion-finalizer-state.cjs — compare `resolveCore`, writer descriptor fields, and receipt preparation behavior.
    - test/support/lockspire/release_proof/package_assertions.ex — inspect `build_sealed_phase_139_acceptance_fixture!`, `install_receipt_writer_fixture!`, and acceptance child environment construction.
    - .planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md — use the exact failing fixture environment and current failure evidence; do not rewrite this report.
  </read_first>
  <behavior>
    - The host receipt producer, temporary copied GSD core, finalizer child, and relation validator resolve to the same absolute workflow root for the selected fixture runtime.
    - Workflow descriptors continue to bind exact path, file mode, size, and SHA-256; no descriptor mismatch is reclassified as authorized.
    - `snapshot_relation: refresh_required` remains a hard failure for stale, missing, inconsistent, or unauthenticated evidence.
    - Ambient legacy acceptance test variables cannot make a production finalizer child take a test-only path; the fixture passes only its explicit, supported environment.
  </behavior>
  <acceptance_criteria>
    - The receipt producer and relation validator report identical workflow descriptors for the same absolute fixture GSD core.
    - The valid fake-remote fixture reaches `authorized_bookkeeping` and the expected candidate, while descriptor and stale-relation mutations fail before ref movement.
    - Production still rejects forbidden test-only environment variables and does not accept `refresh_required` as current evidence.
  </acceptance_criteria>
  <action>Per D-01 through D-03, D-07, and D-09, reproduce the `writer descriptor: workflow`, `snapshot_relation: refresh_required`, and sealed-candidate authentication failures in the temporary repository before editing. Resolve the GSD tools file once to an absolute regular-file path in the fixture builder and pass that exact path to every child that creates or validates the host receipt; make `baseline_inventory.sh` honor a valid explicit `GSD_TOOLS` selection consistently with the state helper, while retaining the existing safe fallback search when it is absent and rejecting invalid or unsafe selections. Copy the chosen GSD workflow files and tracked state helper into the fixture from that same resolved root, preserving their bytes and modes. Explicitly clear ambient `LOCKSPIRE_ACCEPTANCE_TEST_MODE`, `LOCKSPIRE_ACCEPTANCE_TEST_STOP_AFTER_LANDING`, `LOCKSPIRE_ACCEPTANCE_TEST_BARRIER`, and `LOCKSPIRE_ACCEPTANCE_HYGIENE_SCRIPT` in the fixture child environment instead of weakening the production rejection at the top of `finalize_phase_139_acceptance.sh`. Keep temporary remotes isolated from this checkout. Add the smallest regression assertions that show matching descriptors authorize the intended fixture and that changing only the workflow descriptor or relation authority still fails before any local or advertised ref moves. Do not edit the ledger, claim a live receipt, alter release ownership, or change the Phase 140 entry gate.</action>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; GSD_TOOLS=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-core/bin/gsd-tools.cjs ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_final_acceptance</automated>
    <fails_when>Shell syntax fails; Mix exits nonzero or runs zero tests; the authorized fixture still reports writer descriptor drift, refresh_required, or sealed-candidate authentication failure; or any forged descriptor, missing receipt, stale relation, or remote/ref adversary is accepted.</fails_when>
  </verify>
  <done>The final-acceptance fixture reaches its exact sealed-candidate success path using matching writer inputs, and every malformed or stale relation remains fail-closed before ref mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the exact-acceptance receipt selectors and record only repository-owned proof</name>
  <files>test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex, .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md</files>
  <read_first>
    - test/lockspire/release/repository_hygiene_contract_test.exs — preserve the existing `phase139_final_acceptance` and `phase139_acceptance_receipt` selector names and timeout budgets.
    - test/support/lockspire/release_proof/package_assertions.ex — retain the fake GitHub/Hex evidence fixtures and assert their exact SHA and allowlisted receipt shape.
    - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md — update current automated proof owners without adding gap IDs or changing the historical report.
    - .planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md — preserve its `gaps_found` result as the record of the failed pre-repair run until canonical verification refreshes it.
  </read_first>
  <behavior>
    - Both exact acceptance tests pass with the prescribed fixture GSD runtime environment and exercise the full receipt success path.
    - Negative writer, topology, lifecycle, historical-evidence, and receipt-security cases preserve the existing failure semantics.
    - The validation map distinguishes restored repository-owned fixture proof from the still-deferred live exact-SHA Phase 140 receipt.
  </behavior>
  <acceptance_criteria>
    - Both exact acceptance selectors execute and pass in the reported fixture environment.
    - The success fixture joins exact candidate and evidence fields; negative cases still reject and preserve refs.
    - The current verification report stays historical until canonical refresh, and no live receipt or Phase 140 readiness is claimed.
  </acceptance_criteria>
  <action>Per D-03 through D-05, D-08, and D-11, keep the test selector contract and the complete fixture evidence shape intact while adding any missing regression assertion needed to make fixture selection deterministic. Confirm success proves that the candidate, local `main`, fake `origin/main`, required-CI SHA, Release no-publish SHA, and receipt baseline are one exact fixture commit; confirm the retained 1.5.0 source/run/tag/checksum stays historical and supplemental OIDF remains `required_gate: false` and non-certifying. Update only the Phase 139 validation proof-map entries for the repaired local acceptance selectors and their exact command/environment; leave the current canonical verification report untouched until `$gsd-verify-work 139` produces fresh evidence. State explicitly that the successful fixture does not create or imply a live synchronized-main receipt and does not close Phase 140 CI-06/CI-07 entry evidence.</action>
  <verify>
    <automated>GSD_TOOLS=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-core/bin/gsd-tools.cjs ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_final_acceptance --only phase139_acceptance_receipt</automated>
    <fails_when>Mix exits nonzero or runs zero tests; either acceptance selector lacks a successful sealed fixture; any hostile evidence or ref-movement case passes; receipt fields exceed the allowlist; historical release values drift; or the validation map claims live acceptance or Phase 140 readiness.</fails_when>
  </verify>
  <done>The two exact-acceptance selector tests pass against the explicit fixture runtime and the validation map records only the repaired local proof while leaving the live entry-gate receipt pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Temporary acceptance fixture → GSD writer metadata | Fixture-selected paths and bytes become part of a host-sealed receipt and must be independently revalidated. |
| Acceptance finalizer → local/fake remote Git refs | Candidate authentication precedes any fixture ref movement; external production refs are outside this plan's tests. |
| Fixture evidence → bounded acceptance receipt | Fake workflow, historical release, warning, and OIDF values must remain exact and redacted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-139-10-01 | Tampering | GSD workflow writer descriptor | high | mitigate | Resolve one explicit runtime root and recheck descriptor path, mode, size, and digest before relation authorization. |
| T-139-10-02 | Spoofing | Test environment and fixture child | medium | mitigate | Pass an absolute fixture path, clear unsupported ambient test variables, and keep production test-seam rejection enabled. |
| T-139-10-03 | Tampering | Snapshot and Git ref authority | high | mitigate | Preserve refresh_required on stale evidence and assert hostile fixtures cannot update local or advertised refs. |
| T-139-10-04 | Information disclosure | Acceptance evidence output | medium | mitigate | Keep only current allowlisted fields in receipts; never emit fake credentials, raw responses, or logs. |
</threat_model>

<verification>
<automated>GSD_TOOLS=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-core/bin/gsd-tools.cjs ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_final_acceptance --only phase139_acceptance_receipt</automated>
<fails_when>Mix exits nonzero or runs zero tests, fixture writers or snapshot relations fail to converge on one exact input, a hostile acceptance case changes refs, or the proof is represented as a production receipt.</fails_when>
</verification>

<success_criteria>
The current exact-acceptance fixture selection passes both tagged tests; the strict writer and snapshot checks still reject hostile inputs; the record keeps live Phase 140 acceptance deferred and makes no release or external state change.
</success_criteria>

<output>
Create `.planning/phases/139-required-truth-reconciliation/139-10-SUMMARY.md` when done.
</output>
