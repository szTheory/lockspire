---
phase: 139-required-truth-reconciliation
plan: "11"
type: execute
wave: 9
depends_on: ["139-10"]
files_modified:
  - .gsd-capabilities.json
  - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
  - scripts/maintainer/finalize_phase_139_acceptance.sh
  - test/support/lockspire/release_proof/package_assertions.ex
  - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [HYGIENE-06, TRUTH-03, TRUTH-05]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - The tracked host fixture explicitly selects a portable lifecycle run even when a developer has global GSD tools installed, while an explicit GSD_TOOLS run continues to verify current installed capability files and rendered hooks.
    - Installed capability parity compares freshly installed files from tracked capability source and exact current hook rendering; stale ignored runtime copies are refreshed through the supported installer, never hand-edited or accepted as authority.
    - The Phase 139 post-transition planning-consistency test runs automatically only inside the existing authenticated Phase 140 plan:pre finalizer, after canonical verification has transitioned state and before finalizer ref mutation.
    - A failed post-transition consistency test blocks final acceptance, preserves the host pending receipt for recovery, and leaves Phase 140's exact-SHA gate and Phase 139's no-publication boundary intact.
  artifacts:
    - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
    - scripts/maintainer/finalize_phase_139_acceptance.sh
    - test/support/lockspire/release_proof/package_assertions.ex
    - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
  key_links:
    - "`LOCKSPIRE_GSD_HOST_FIXTURE` selects the portable contract mode; an explicit `GSD_TOOLS` selects the installed runtime parity mode."
    - The existing blocking Phase 140 `plan:pre` hook authenticates the completed Phase 139 transition before the consistency test is invoked.
    - The planning test exits before `fast_forward_main`; its failure cannot move local or remote main or clear host pending state.
---

<objective>
Repair Phase 139 lifecycle-rendering parity and move the required post-transition planning-consistency proof into the already-blocking Phase 140 entry hook.

Purpose: Make portable hook rendering deterministic in CI and on developer machines, keep installed capability bytes aligned with tracked source, and eliminate the currently unreachable post-transition test handoff without bypassing canonical verification.
Output: Explicit portable/live test modes, a supported installed-capability refresh path, a fail-closed automatic planning proof in the existing plan:pre finalizer, and an updated current validation map.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-required-truth-reconciliation/139-CONTEXT.md
@.planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md
@.planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
@.planning/phases/139-required-truth-reconciliation/139-09-SUMMARY.md
@tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
@tools/gsd-capabilities/lockspire-phase-finalizer/post-completion-finalizer-state.cjs
@scripts/maintainer/finalize_phase_139_acceptance.sh
@test/lockspire/quality/phase_139_planning_consistency_test.exs
@test/support/lockspire/release_proof/package_assertions.ex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make portable and installed lifecycle rendering modes explicit and reproducible</name>
  <files>.gsd-capabilities.json, tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs</files>
  <read_first>
    - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs — inspect `tools`, `hostFixture`, `installedCapability`, and the fresh ordered lifecycle hooks test.
    - tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json — preserve the exact supported points, predicates, timeouts, and tracked runtime file list.
    - tools/gsd-capabilities/lockspire-phase-finalizer/capability.json — keep active capability version 1.2.0 and the existing two halt-capable hook boundaries.
    - .planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md — use both reported Node environment commands and preserve the installed-file drift as a real failure until corrected.
    - .planning/phases/139-required-truth-reconciliation/139-09-SUMMARY.md — preserve existing portable CI coverage and live-runtime parity scope.
  </read_first>
  <behavior>
    - Setting `LOCKSPIRE_GSD_HOST_FIXTURE` alone selects the portable mode regardless of globally discoverable GSD tools, so the same command has one deterministic contract on CI and developer machines.
    - Setting `GSD_TOOLS` explicitly selects live installed-runtime mode and retains capability-list, installed-file byte parity, and rendered execute:post/plan:pre assertions.
    - If active installed files drift, the executor regenerates them with the supported `capability install` operation from tracked source before rerunning parity; the test never copies stale installed files back into tracked source or weakens comparisons.
    - Both modes retain the existing lifecycle, process cancellation, recovery, exact-receipt, and later-phase isolation checks.
  </behavior>
  <acceptance_criteria>
    - Fixture-only mode runs the portable contract even when a global GSD runtime exists.
    - The installed mode sees active capability 1.2.0, byte-identical tracked runtime files, and the exact execute:post and plan:pre hooks after supported installation.
    - No hook predicate, timeout, blocking flag, public command family, or later-phase isolation rule changes.
  </acceptance_criteria>
  <action>Per D-07 and D-09, change only test mode selection: the tracked fixture variable by itself must take precedence over automatic home-directory GSD discovery, while an explicit `GSD_TOOLS` continues to request live mode. Keep exact tracked fixture content checks and exact installed-file comparisons. For live mode, use the installed runtime's supported command to regenerate the project capability from `./tools/gsd-capabilities/lockspire-phase-finalizer` when its ignored mirror is stale, then assert active version 1.2.0 and current hook rendering; never edit `.gsd/capabilities` by hand or make the active copy the source of truth. Preserve all existing Node test cases and skip behavior, and do not change the Phase 140 plan:pre hook predicate, timeout, or blocking status.</action>
  <verify>
    <automated>node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs capability install ./tools/gsd-capabilities/lockspire-phase-finalizer --scope project --yes --raw &amp;&amp; LOCKSPIRE_GSD_HOST_FIXTURE=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; GSD_TOOLS=/Users/jon/.codex/gsd-core/bin/gsd-tools.cjs node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs</automated>
    <fails_when>Capability installation exits nonzero; either Node command exits nonzero or runs zero tests; fixture mode discovers or compares against global runtime; installed byte parity or fresh hook rendering differs; capability version/order changes; or any lifecycle safety assertion is removed or weakened.</fails_when>
  </verify>
  <done>The fixture command reliably proves portable coverage and the explicit GSD_TOOLS command proves live parity from a freshly rendered installation of tracked capability source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the post-transition planning proof only behind the authenticated Phase 140 entry gate</name>
  <files>scripts/maintainer/finalize_phase_139_acceptance.sh, test/support/lockspire/release_proof/package_assertions.ex, tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs, .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md</files>
  <read_first>
    - scripts/maintainer/finalize_phase_139_acceptance.sh — preserve exact argv, host receipt preparation, sealed-candidate authentication, existing main fast-forward sequence, and no-publish behavior.
    - tools/gsd-capabilities/lockspire-phase-finalizer/post-completion-finalizer-state.cjs — confirm pending host state is cleared only after post-transition acceptance succeeds.
    - test/lockspire/quality/phase_139_planning_consistency_test.exs — confirm the module asserts Phase 140 state, all plan/summary counts, verification status, requirements posture, and historical release separation.
    - test/support/lockspire/release_proof/package_assertions.ex — add a fake `mix` fixture that records exact invocation order and can fail without touching Git refs.
    - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs — keep the supported execute:post and plan:pre hook envelope assertions.
    - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md — distinguish the automatic post-transition proof from fresh pre-verification tests and the still-pending live receipt.
  </read_first>
  <behavior>
    - The exact command `ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/quality/phase_139_planning_consistency_test.exs` is called only from the accepted `post-transition --phase 139` path reached by Phase 140 plan:pre.
    - The command runs after the pending host receipt and sealed candidate/relation are authenticated and before `fast_forward_main`; no Phase 139 preverify or failed transition invokes it.
    - A nonzero planning test leaves HEAD/local main/remote main unchanged, preserves the pending receipt for the existing recovery path, and prevents Phase 140 planning from proceeding.
    - On the real Phase 140 plan:pre lifecycle, the test reads the post-transition state and plan/summary set automatically; no manual UAT or separate user command is required.
  </behavior>
  <acceptance_criteria>
    - The real ExUnit command is called once only on an authenticated `plan:pre` Phase 139 post-transition path and is absent from Phase 139 preverify.
    - A fixture failure produces no local or remote ref change and leaves host pending recovery state intact.
    - Phase 140's blocking exact-SHA receipt gate and all no-publication controls remain unchanged.
  </acceptance_criteria>
  <action>Per D-01 through D-03, D-06, D-10, and D-12, insert the exact Phase 139 planning-consistency ExUnit command into `finalize_phase_139_acceptance.sh` only after the script has prepared and validated the host-authenticated `plan:pre` receipt and the sealed-candidate relation, and before `fast_forward_main` or any external acceptance mutation. Use the pinned project Elixir/OTP environment shown in the current verification commands. Treat command failure as a hard finalizer failure; preserve the pending host receipt and leave all refs untouched for same-phase recovery. Re-run the sealed-state/porcelain identity check after the test succeeds and before ref movement. Extend the existing acceptance fixture with a fake `mix` executable that asserts the exact module arguments and environment, records one invocation on the valid authenticated path, and proves zero invocations for forged writer, missing receipt, wrong lifecycle, and relation failures; its failure mode must prove no local or advertised ref changes. The actual planning-consistency module is not run during Phase 139 gap execution because its required post-transition state does not yet exist: the existing blocking Phase 140 `plan:pre` hook is its sole automatic runtime entry point after canonical Phase 139 verification and transition. Update the validation proof map to preserve the eleven existing gap IDs, map the Node failure to its exact portable/live commands, and identify this planning test as automatically gated after transition. Keep G-139-01/G-139-09 and synchronized-main CI-06/CI-07 evidence pending at the unchanged Phase 140 gate; do not edit STATE, claim Phase 139 complete, run human UAT, publish, dispatch workflows, or change release-owned files.</action>
  <verify>
    <automated>GSD_TOOLS=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-core/bin/gsd-tools.cjs ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_final_acceptance --only phase139_acceptance_receipt &amp;&amp; LOCKSPIRE_GSD_HOST_FIXTURE=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; GSD_TOOLS=/Users/jon/.codex/gsd-core/bin/gsd-tools.cjs node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; bash scripts/ci/lint_workflows.sh</automated>
    <fails_when>Any command exits nonzero or either suite runs zero tests; the acceptance child invokes the planning proof before host receipt/relation authentication or after ref movement; a failed fake Mix child changes any ref or clears pending state; the live lifecycle test can be reached before canonical verification passes; or workflow lint finds a regression.</fails_when>
  </verify>
  <done>The automatically gated post-transition planning proof is exercised by fixtures and is scheduled only inside the existing authenticated Phase 140 plan:pre lifecycle, with no human handoff and no change to the exact-SHA entry gate.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| Source | ID | Item | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Restore current repository-owned exact-SHA acceptance, lifecycle rendering, and planning consistency proof. | 139-10, 139-11 | COVERED | Live synchronized-main CI/Release receipt remains the separately deferred Phase 140 entry gate. |
| REQ | CI-08 | Keep OIDF evidence redacted and supplemental. | 139-10 | COVERED | Existing allowlisted fixture assertions remain in the receipt path. |
| REQ | QUAL-05 | Preserve the passing local quality gate while repairing exact acceptance proof. | 139-10 | COVERED | Do not weaken `mix ci` or proof-quality assertions. |
| REQ | HYGIENE-05 | Keep exact candidate, writer, and hygiene relations fail-closed. | 139-10 | COVERED | Matching fixtures pass; hostile descriptors and stale relations still block. |
| REQ | HYGIENE-06 | Keep the supported lifecycle and verification gates automated. | 139-11 | COVERED | Portable and installed Node modes run without manual UAT. |
| REQ | TRUTH-03 | Prove post-transition planning records through the lifecycle gate. | 139-11 | COVERED | The state-dependent proof runs only after canonical verification and Phase 140 transition. |
| REQ | TRUTH-04 | Preserve the historical 1.5.0 evidence chain. | 139-10 | COVERED | Fixture checks retain its separate historical SHA/run/tag/checksum. |
| REQ | TRUTH-05 | Preserve protected release controls and installed hook identity. | 139-11 | COVERED | No workflow dispatch, publish action, hook broadening, or mutable action reference is introduced. |
| RESEARCH | — | Use one exact GSD runtime root for receipt-writer descriptors; keep relation uncertainty as `refresh_required`. | 139-10 | COVERED | Gap-specific fixture and production resolver repair. |
| RESEARCH | — | Keep the portable lifecycle fixture non-authoritative and compare live installed files to tracked source. | 139-11 | COVERED | Explicit mode selection and supported capability installation. |
| CONTEXT | D-01–D-03 | Use one synchronized SHA and require exact same-SHA evidence; fail closed on stale relation. | 139-10, 139-11 | COVERED | No caller-supplied substitute or weakened Phase 140 gate. |
| CONTEXT | D-04–D-05, D-11 | Preserve no-publish semantics, historical release facts, and supplemental OIDF classification. | 139-10 | COVERED | Existing production and receipt fixture invariants remain asserted. |
| CONTEXT | D-06, D-10, D-12 | Keep protected release authority and distinguish active planning from shipped history. | 139-11 | COVERED | Post-transition test checks current planning without rewriting historical records. |
| CONTEXT | D-07–D-09 | Restrict repairs to demonstrated repo-local gaps and avoid a public/runtime surface. | 139-10, 139-11 | COVERED | No new package, service, product API, or release mechanism. |

Deferred items excluded from this gap set: live synchronized-main CI-06/CI-07 receipt creation remains Phase 140's blocking plan:pre responsibility; operational cleanup and final baseline work remain in Phases 140–141; OIDF remediation remains future bounded conformance work.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tracked capability → installed GSD runtime | Ignored runtime files must be freshly derived from tracked source and compared before current hook claims are accepted. |
| Canonical Phase 139 transition → Phase 140 plan:pre finalizer | Only a host-authenticated sealed transition may start the post-transition consistency test. |
| Planning consistency test → Git ref update | A failing or state-mutating test must stop before any local or remote main movement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-139-11-01 | Tampering | Installed capability and hook rendering | high | mitigate | Reinstall from tracked source through the supported GSD command and keep exact file, version, and render assertions. |
| T-139-11-02 | Spoofing | Post-transition lifecycle receipt | high | mitigate | Run the proof only after host writer, hook, transition, and sealed candidate validation at plan:pre. |
| T-139-11-03 | Denial of service | Planning-consistency command | medium | mitigate | Bound it by the existing hook timeout and fail closed before ref movement while retaining pending recovery state. |
| T-139-11-04 | Tampering | Main refs and external acceptance | critical | mitigate | Assert test invocation precedes the existing compare-and-swap/push sequence and recheck sealed identities after it. |
</threat_model>

<verification>
<automated>node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs capability install ./tools/gsd-capabilities/lockspire-phase-finalizer --scope project --yes --raw &amp;&amp; LOCKSPIRE_GSD_HOST_FIXTURE=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; GSD_TOOLS=/Users/jon/.codex/gsd-core/bin/gsd-tools.cjs node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; GSD_TOOLS=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-core/bin/gsd-tools.cjs ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_final_acceptance --only phase139_acceptance_receipt &amp;&amp; bash scripts/ci/lint_workflows.sh</automated>
<fails_when>Capability installation, either Node mode, exact acceptance selectors, or workflow lint exits nonzero; tests execute zero cases; automatic planning proof is not behind the authenticated plan:pre transition; failure can mutate refs or clear recovery state; or any Phase 140 exact-SHA, protected-release, or supplemental-evidence boundary is weakened.</fails_when>
</verification>

<success_criteria>
The portable and current installed-runtime Node suites pass, the exact acceptance fixtures prove that post-transition planning consistency is called only after canonical verification and before ref movement, and Phase 140's exact-SHA receipt gate remains the sole live evidence boundary.
</success_criteria>

<output>
Create `.planning/phases/139-required-truth-reconciliation/139-11-SUMMARY.md` when done.
</output>
