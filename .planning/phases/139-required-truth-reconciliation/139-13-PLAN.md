---
phase: 139-required-truth-reconciliation
plan: "13"
type: execute
wave: 11
depends_on: ["139-12"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - test/support/lockspire/release_proof/package_assertions.ex
  - test/lockspire/release/repository_hygiene_contract_test.exs
autonomous: true
requirements: [TRUTH-03, QUAL-05]
gap_closure: true
estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - The completion classifier accepts the exact installed-GSD Phase 139 transition from the observed 12-plan prestate: a checked top-level Phase 139 row, 9/11 In Progress roadmap row, STATE 49/50 plans and one completed phase, and state.json Phase 139 in_progress with Phase 140 pending.
    - The accepted child records 12/12 Complete, preserves the existing checked top-level row and completion date, marks the outstanding Phase 139 plan bullets complete, advances STATE to 50/50 plans and two completed phases, and changes only Phase 139 to complete in state.json while Phase 140 remains pending.
    - Malformed counts, a changed top-level row, unrelated phase changes, premature Phase 140 status, and arbitrary completion-commit paths remain rejected; Phase 140's exact-SHA plan:pre gate remains blocking and unchanged.
  artifacts:
    - scripts/maintainer/baseline_inventory.sh
    - test/support/lockspire/release_proof/package_assertions.ex
    - test/lockspire/release/repository_hygiene_contract_test.exs
  key_links:
    - The Git-tree PLAN/SUMMARY inventory is checked against both the actual parent roadmap row and the canonical GSD completion child.
    - The completion commit's STATE.md, state.json, ROADMAP.md, and path set are validated together as one bounded parent-to-child transition.
    - Phase 140's existing exact-SHA plan:pre gate remains the next authority boundary after Phase 139 completion.
---

<objective>
Authorize the exact canonical GSD Phase 139 completion transition observed in the live repository, while retaining fail-closed validation and the later Phase 140 exact-SHA entry gate.

Purpose: The current validator rejects the real 12-plan transition because it expects an already reconciled parent roadmap row and an unchecked top-level phase row.
Output: A regression fixture generated in an isolated temporary repository from installed GSD output, plus a narrow classifier correction and hostile near-miss coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md
@.planning/phases/139-required-truth-reconciliation/139-12-SUMMARY.md
@scripts/maintainer/baseline_inventory.sh
@test/support/lockspire/release_proof/package_assertions.ex
@test/lockspire/release/repository_hygiene_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Accept only the observed canonical Phase 139 completion transition</name>
  <files>scripts/maintainer/baseline_inventory.sh, test/support/lockspire/release_proof/package_assertions.ex, test/lockspire/release/repository_hygiene_contract_test.exs</files>
  <behavior>
    - A temporary repository with 12 matching PLAN/SUMMARY pairs and the observed parent records is advanced by the installed canonical GSD phase.complete 139 command; the resulting exact child is accepted by the completion classifier.
    - The roadmap child is 12/12 Complete; its already-checked top-level Phase 139 row and existing completion date remain unchanged, and only the pending Phase 139 plan bullets are completed.
    - STATE advances from 49/50 plans and one completed phase to 50/50 and two completed phases; state.json changes Phase 139 from in_progress to complete and leaves Phase 140 pending.
    - Near-miss children with malformed parent or child counts, a changed top-level row/date, an unrelated phase mutation, Phase 140 no longer pending, or an arbitrary path are rejected.
  </behavior>
  <action>Per D-07, D-09, D-10, and D-12, extend the existing repository-local shell classifier and focused ExUnit fixture only. First make the regression exercise a temporary Git repository containing 12 consecutive Phase 139 PLAN/SUMMARY pairs and the actual observed parent projection: roadmap `9/11 In Progress`, an already checked top-level Phase 139 row with its existing completion date, the outstanding Phase 139 detail bullets, STATE counters 49/50 and one completed phase, and state.json with Phase 139 `in_progress` and Phase 140 `pending`. Invoke the installed GSD `phase.complete 139` command inside that temporary repository and use its emitted child as the positive fixture; do not hand-edit live `.planning/STATE.md`, `.planning/ROADMAP.md`, `.planning/state.json`, or any Phase 140 gate. Update `validate_phase_139_completion_roadmap()` and `validate_phase_139_completion_state()` to recognize only that observed parent-to-child delta while requiring the inventory-derived 12/12 child count, unchanged top-level completion row/date, exact state counter advance, and unchanged Phase 140 status. Keep the exact completion path allowlist and add the listed hostile mutations to the existing `phase139_inventory_relation` matrix. Preserve Phase 140's exact-SHA `plan:pre` implementation and blocking hook unchanged. Do not accept a generic count mismatch, broad status transition, or extra path as bookkeeping authority.</action>
  <verify>
    <automated>GSD_TOOLS=/Users/jon/.codex/gsd-core/bin/gsd-tools.cjs ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_inventory_relation</automated>
    <fails_when>The exact temporary-repository GSD transition is rejected, any listed hostile variant is accepted, Phase 140 ceases to be pending, or a live planning/gate file is mutated by the test.</fails_when>
    <automated>LOCKSPIRE_GSD_HOST_FIXTURE=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs</automated>
    <fails_when>The existing Phase 140 plan:pre route or exact-SHA blocking behavior changes, or the portable lifecycle/router suite reports an unexpected failure.</fails_when>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; git diff --check -- scripts/maintainer/baseline_inventory.sh test/support/lockspire/release_proof/package_assertions.ex test/lockspire/release/repository_hygiene_contract_test.exs</automated>
    <fails_when>Shell syntax is invalid or the authorized implementation/test diff contains whitespace errors.</fails_when>
  </verify>
  <done>The focused integration fixture accepts the installed GSD 12-plan completion output and rejects malformed counts, changed top-level completion metadata, unrelated or premature Phase 140 changes, and arbitrary paths; Phase 140's exact-SHA entry gate remains unchanged and blocking.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git completion commit → lifecycle classifier | Commit paths and planning blobs are untrusted input to the phase transition authority. |
| Phase 139 completion → Phase 140 plan:pre | A local bookkeeping transition must not grant live exact-SHA acceptance authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-13-01 | Tampering | Completion classifier and planning records | medium | mitigate | Validate the exact observed parent-to-child counts, top-level row, state counters, state.json phase statuses, and changed-path allowlist in a temporary-repository integration matrix. |
| T-139-13-02 | Elevation of privilege | Phase 140 exact-SHA entry gate | high | mitigate | Leave the gate implementation untouched and rerun its portable lifecycle/router contract to prove planning still blocks without the required exact-SHA evidence. |
</threat_model>

<verification>
Run the isolated `phase139_inventory_relation` integration selector, the portable Phase 140 lifecycle/router suite, shell syntax validation, and scoped diff validation. No live Phase 139 bookkeeping or Phase 140 gate operation is part of this plan.
</verification>

<success_criteria>
Only the observed installed-GSD completion transition is classified as Phase 139 completion; malformed or broadened transitions remain rejected, and the exact-SHA Phase 140 planning boundary remains blocking.
</success_criteria>

<output>
Create `.planning/phases/139-required-truth-reconciliation/139-13-SUMMARY.md` when done.
</output>
