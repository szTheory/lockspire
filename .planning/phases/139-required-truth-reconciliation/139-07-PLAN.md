---
phase: 139-required-truth-reconciliation
plan: "07"
type: execute
wave: 5
depends_on: ["139-03", "139-04", "139-05", "139-06"]
files_modified:
  - tools/gsd-capabilities/lockspire-phase-finalizer/capability.json
  - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs
  - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs
  - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
  - .gsd-capabilities.json
  - .planning/phases/139-required-truth-reconciliation/COVERAGE.md
autonomous: true
requirements: [CI-06, CI-07, CI-08, QUAL-05, HYGIENE-05, HYGIENE-06, TRUTH-03, TRUTH-04, TRUTH-05]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-01 D-02 D-03: one active project capability refreshes inventory after all summaries/review and performs final exact-main acceptance only after GSD seals every Phase 139 transition write."
    - "At execute:post for Phase 139, code-review precedes `lockspire-finalize pre-verify`; the command publishes one ledger-only replacement and a failure halts before the normal verifier."
    - "At execute:complete:post for Phase 139, `lockspire-finalize post-transition` validates the host receipt, lands final main, waits for exact CI/Release outcomes, runs local acceptance, writes the external receipt, and returns zero before host completion."
    - "Phase 138 retains its exact prior pre-verify/post-transition routing; Phase 140 and later numeric phases are explicit successful no-ops so a Phase 139-specific capability cannot block or mutate later work."
    - "D-04 D-05 D-06: the lifecycle never dispatches publication, keeps the historical 1.5.0 chain separate, and treats executable release controls as authority."
    - "D-07 D-08 D-09: only demonstrated currentness, exact-head, no-publish, gate, and action-pin gaps are enforced through existing repository shell/ExUnit/capability surfaces."
    - "D-10 D-11 D-12: active planning, shipped release, and supplemental conformance truth remain distinct through final acceptance and no archive is rewritten."
    - "Failure at either hook is nonzero and durable: pending host state blocks progress/later-phase routing, and `$gsd-execute-phase 139` recovery reruns only the unfinished post-transition path when applicable."
    - "The phase's sole tracked COVERAGE.md gives the reasoned no-new-external-integration declaration required by api-coverage.verify-pre: existing GitHub/Hex reads remain bounded and read-only, while the normal non-force origin/main fast-forward is classified as repository lifecycle synchronization rather than a new service/API surface."
  prohibitions:
    - statement: "The capability must not run Phase 139 mutation or acceptance logic for Phase 140/141, silently accept malformed phase argv, or leave a Phase 139 failure unable to block later routing."
      verification: flagged-unverified
    - statement: "The installed hook must not publish a release, rewrite history, perform cleanup, commit the live receipt, or add a public Lockspire/runtime surface."
      verification: flagged-unverified
    - statement: "Installed runtime copies must not become source authority or be hand-edited instead of reinstalling from tracked capability source."
      verification: flagged-unverified
  artifacts:
    - path: tools/gsd-capabilities/lockspire-phase-finalizer/capability.json
      provides: "Version 1.1.0 two-hook manifest describing Phase 138 currentness and Phase 139 exact acceptance."
      contains: "lockspire-finalize post-transition"
    - path: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs
      provides: "Phase-aware bounded router for Phase 138, Phase 139, and inert later phases."
      contains: "finalize_phase_139_acceptance.sh"
    - path: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs
      provides: "Exact dispatch, no-op, malformed input, timeout, signal, spawn, and status tests."
      contains: "2400000"
    - path: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
      provides: "Installed-runtime Phase 139 hook order, sealed landing, receipt, recovery, and later-phase no-op integration proof."
      contains: "phase 139"
    - path: .gsd-capabilities.json
      provides: "Tracked explicit-consent activation ledger for capability version 1.1.0."
      contains: "lockspire-phase-finalizer"
  key_links:
    - from: tools/gsd-capabilities/lockspire-phase-finalizer/capability.json
      to: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs
      via: "the same two host commands append the current phase and route through exact phase-aware dispatch"
      pattern: "pre-verify|post-transition|routeLockspireFinalizeCommand"
    - from: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs
      to: "scripts/maintainer/finalize_phase_138_inventory.sh and scripts/maintainer/finalize_phase_139_acceptance.sh"
      via: "Phase 139 pre-verify selects ledger refresh; Phase 139 post-transition selects final acceptance with bounded no-shell child execution"
      pattern: "139|finalize_phase_139_acceptance.sh"
    - from: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
      to: .gsd-capabilities.json
      via: "supported installation, fresh hook rendering, host pending receipt lifecycle, and same-phase recovery"
      pattern: "execute:post|execute:complete:post|pending"
    - from: .planning/phases/139-required-truth-reconciliation/COVERAGE.md
      to: "gsd_run check api-coverage.verify-pre"
      via: "the tracked reasoned declaration classifies the existing read-only GitHub/Hex evidence seam and the guarded origin/main lifecycle synchronization before the verification gate runs"
      pattern: "No external API integration|read-only GitHub/Hex|non-force fast-forward"
---

<objective>
Activate the Phase 139 two-boundary lifecycle so one stock execute-phase run refreshes currentness before verification and completes final exact-main acceptance after all repository bookkeeping.

Purpose: The implementation plans alone cannot run after GSD's verifier/completion/transition commits; extending the already-consented project capability closes that timing gap without a manual second command or self-invalidating tracked receipt.
Output: A phase-aware capability/router, installed lifecycle proof, active version 1.1.0, automatic Phase 139 pre/post finalization with durable same-phase recovery, and the tracked reasoned API-coverage declaration consumed by the verify-pre gate.

Decision coverage: D-01 through D-03 bind the two lifecycle boundaries to final main; D-04 through D-06 preserve no-publish and historical/executable release authority; D-07 through D-09 constrain enforcement; D-10 through D-12 preserve planning/shipped/supplemental truth and historical scope.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/config.json
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-required-truth-reconciliation/139-CONTEXT.md
@.planning/phases/139-required-truth-reconciliation/139-RESEARCH.md
@.planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
@scripts/maintainer/finalize_phase_138_inventory.sh
@scripts/maintainer/finalize_phase_139_acceptance.sh
@tools/gsd-capabilities/lockspire-phase-finalizer/capability.json
@tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs
@tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs
@tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
@/Users/jon/.codex/gsd-core/bin/lib/post-completion-finalizer-state.cjs
@/Users/jon/.codex/gsd-core/workflows/execute-phase.md
@/Users/jon/.codex/gsd-core/workflows/progress.md
@/Users/jon/.codex/gsd-core/workflows/transition.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route Phase 139 pre-verify and post-transition through exact bounded children</name>
  <reversibility rating="reversible">The project capability is an internal tracked integration that can be reinstalled or removed without product data or public API migration.</reversibility>
  <files>tools/gsd-capabilities/lockspire-phase-finalizer/capability.json, tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs, tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs</files>
  <read_first>
    - tools/gsd-capabilities/lockspire-phase-finalizer/capability.json — preserve one command family, exactly two halt-capable lifecycle steps, empty public contributions, and engine/runtime compatibility
    - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs — extend exact raw argv/cwd/spawn/error behavior without shell strings or output parsing
    - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs — retain all Phase 138 positive and failure-class coverage and add Phase 139/later-phase cases
    - scripts/maintainer/finalize_phase_138_inventory.sh — Phase 139 pre-verify target from Plan 05
    - scripts/maintainer/finalize_phase_139_acceptance.sh — Phase 139 post-transition target from Plan 06
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md — retain fresh execute:post rendering and host-appended numeric phase/raw routing
  </read_first>
  <behavior>
    - Phase 138 routes both modes to the existing inventory finalizer with its prior 900000ms bound.
    - Phase 139 pre-verify routes to the inventory finalizer with `pre-verify --phase 139`; post-transition routes to the acceptance finalizer with `post-transition --phase 139` and a 2400000ms bound.
    - Other valid numeric phases emit one bounded not-applicable diagnostic and return zero without spawning, mutation, or hook removal.
    - Invalid raw mode, family, mode, flag, nonnumeric/empty phase, argv cardinality, project root, or missing tracked child rejects with exit 2 and no spawn.
    - Timeout maps to 124, signal to 125, spawn error to 126, missing status to 127, and ordinary child nonzero is preserved.
  </behavior>
  <action>Update the manifest to version 1.1.0 and describe Phase 138 inventory currentness plus Phase 139 exact acceptance while retaining exactly one `lockspire-finalize` command family and the two existing halt-capable step points. Extend `projectRoot` to require both tracked shell children and both phase directories. Refactor the router into exact phase/mode dispatch data: Phase 138 preserves its existing child argv and deadline; Phase 139 selects the Plan 05 pre-verify child or Plan 06 post-transition child; any other strictly numeric phase returns an inert success without spawn so installed phase-specific logic cannot block later phases. Keep raw=true, exact four-element argv, verified real root, `shell:false`, `stdio:inherit`, SIGTERM, bounded timeouts, distinct router diagnostics, and child-status propagation. Add tests for every dispatch/no-op/rejection combination, fixed argv/options, missing child/root, timeout/signal/spawn/missing-status, multiple nonzero statuses, and the longer Phase 139 post-transition deadline.</action>
  <acceptance_criteria>
    - Manifest validation yields exactly two halt-capable lifecycle steps and no public hook/contribution/gate/skill/agent surface.
    - Phase 138 and 139 exact mode matrices route to the intended fixed child once; later phases spawn nothing and malformed input never succeeds.
    - All child failure classes remain distinct and Phase 139 post-transition has enough bounded time for full mix ci plus workflow polling.
  </acceptance_criteria>
  <verify>
    <automated>node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs</automated>
    <fails_when>Node exits nonzero or runs zero tests, manifest/dispatch shape differs, a valid Phase 139 mode selects the wrong child/options, a later phase spawns, malformed input succeeds, or a child failure collapses to success</fails_when>
  </verify>
  <done>The tracked capability routes exactly the required Phase 138/139 lifecycle work and remains inert for later phases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install version 1.1.0, prove the complete Phase 139 host lifecycle, and declare API coverage</name>
  <reversibility rating="reversible">Supported project installation records and can remove the internal capability without altering Lockspire runtime or release state.</reversibility>
  <files>tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs, .gsd-capabilities.json, .planning/phases/139-required-truth-reconciliation/COVERAGE.md</files>
  <read_first>
    - tools/gsd-capabilities/lockspire-phase-finalizer/capability.json — verify Task 1's version, commands, step points, products, and halt policy before installation
    - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs — verify exact phase-aware dispatch and later-phase no-op behavior
    - /Users/jon/.codex/gsd-core/bin/lib/post-completion-finalizer-state.cjs — exercise begin/seal/verify-hooks/status/complete and pending-state ownership in real temporary repositories
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md — assert code review precedes freshly rendered execute:post and recovery precedes every plan filter
    - /Users/jon/.codex/gsd-core/workflows/progress.md — assert pending state blocks later routing
    - /Users/jon/.codex/gsd-core/workflows/transition.md — assert all transition writes are sealed before execute:complete:post and host completion follows hook success
    - .gsd-capabilities.json — update only through supported capability installation from tracked source
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/COVERAGE.md — follow the accepted single-artifact, reasoned API-coverage declaration shape without copying Phase 138's integrated capability matrix into this non-integration phase
  </read_first>
  <behavior>
    - Supported installation activates tracked version 1.1.0 and fresh execute:post rendering orders code-review before the Phase 139 pre-verify halt step.
    - Pre-verify publishes exactly one ledger-only replacement after all summaries/review and before normal verification.
    - The host seals all Phase 139 verifier/completion/transition identities before post-transition; the acceptance child then lands final main and creates the external receipt before host completion.
    - Pre-verify failure stops verification; post-transition failure preserves pending state, blocks progress/Phase 140, and same-phase execute recovery reruns only post-transition with the identical hook hash.
    - Success removes only host pending state; the Phase 139 acceptance receipt survives, no repository commit follows it, and later numeric phases render the hooks but execute inertly.
  </behavior>
  <action>Extend the installed-lifecycle Node test with an isolated Phase 139 real repository and injected finalizer counters. Drive the actual host begin/seal/verify-hooks/status/complete commands and prove ordered fresh pre-verify discovery, one ledger publication, normal verifier/completion/transition identities, post-transition final-main/receipt success, host-owned completion, and zero repository commits after the receipt. Force pre-verify and post-transition child nonzero/timeout/signal, hook reorder/hash change, phase/status/writer/transformation forgery, receipt mismatch, and main race; assert durable pending recovery blocks later routing and re-executes only the unfinished Phase 139 post-transition child. Add Phase 140/141 fixtures proving the installed router is inert and cannot mutate or block them. Create the phase's sole tracked `COVERAGE.md` with a reasoned `No external API integration` declaration: identify exact-SHA GitHub workflow/job and public GitHub/Hex lookups as bounded read-only uses of the existing maintainer evidence seam; state that no service, credential, SDK, endpoint, webhook, product API, workflow dispatch, package publication, or GitHub/Hex object mutation is added; and classify the one guarded non-force `origin/main` fast-forward as repository lifecycle synchronization protected by ancestry, compare-and-swap, race, and post-write identity checks. Install tracked source with `gsd_run capability install ./tools/gsd-capabilities/lockspire-phase-finalizer --scope project --yes --raw`; do not hand-edit the installed runtime copy. Require active version 1.1.0, fresh execute:post order after code-review, exactly one execute:complete:post hook, the two exact command references, and a passing `api-coverage.verify-pre` result against that declaration. Leave the capability active so the immediately following normal execute-phase boundaries use the freshly rendered version.</action>
  <acceptance_criteria>
    - Active project installation records version 1.1.0 from tracked source and both hook envelopes are exact and halt-capable.
    - One normal fixture proves the complete Phase 139 preverify-to-posttransition sequence with no manual command and no receipt-invalidating commit.
    - Every failure preserves recoverable pending state and blocks later routing; recovery reruns no plan, summary, review, verifier, completion, transition, ledger publication, or successful main push.
    - Phase 140/141 dispatch is a tested no-op and cannot inherit Phase 139 mutation authority.
    - The sole tracked COVERAGE.md contains the complete reasoned no-new-external-integration declaration and `api-coverage.verify-pre` accepts it without an unexplained capability or opt-out.
  </acceptance_criteria>
  <verify>
    <automated>gsd_run capability install ./tools/gsd-capabilities/lockspire-phase-finalizer --scope project --yes --raw &amp;&amp; node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; gsd_run capability list --scope project --json | jq -e '.[] | select(.id == "lockspire-phase-finalizer" and .version == "1.1.0" and .status == "active")' &gt;/dev/null &amp;&amp; gsd_run loop render-hooks execute:post --raw | jq -e '([.activeHooks[] | select(.kind == "step") | .capId] | index("code-review") as $c | index("lockspire-phase-finalizer") as $l | ($c != null and $l != null and $c &lt; $l)) and ([.activeHooks[] | select(.capId == "lockspire-phase-finalizer" and .ref.command == "lockspire-finalize pre-verify" and .onError == "halt")] | length == 1)' &gt;/dev/null &amp;&amp; gsd_run loop render-hooks execute:complete:post --raw | jq -e '[.activeHooks[] | select(.capId == "lockspire-phase-finalizer" and .ref.command == "lockspire-finalize post-transition" and .onError == "halt")] | length == 1' &gt;/dev/null &amp;&amp; gsd_run check api-coverage.verify-pre .planning/phases/139-required-truth-reconciliation</automated>
    <fails_when>installation/JSON/API-coverage parsing exits nonzero, Node runs zero tests, active version/order/commands differ, fresh rendering is absent, Phase 139 lifecycle ordering or recovery fails, final receipt is invalidated, a later phase spawns Phase 139 work, or the no-new-integration declaration is absent or malformed</fails_when>
  </verify>
  <done>One normal `$gsd-execute-phase 139` run owns both exact authority boundaries, can recover safely without a manual finalizer or second publication, and carries the tracked API-coverage declaration required by its verification gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tracked capability -> installed execution | GSD validates, consents, installs, and renders commands from tracked source. |
| Hook argv -> phase-aware router | Raw mode, family, mode, flag, numeric phase, cwd, and child existence are untrusted until exact validation. |
| Router -> shell finalizers | Fixed argv arrays, no shell, inherited stdio, timeouts, and distinct exit statuses preserve authority. |
| Host sealed state -> post-transition completion | Pending state remains until exact final-main acceptance returns zero. |
| Phase 139 capability -> later phases | Numeric later phases must be inert and cannot inherit mutation authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-139-32 | Spoofing | capability/version/hook identity | high | mitigate | Tracked source, explicit install, active version check, exact ordered hooks, and sealed hook hash. |
| T-139-33 | Tampering | phase/mode child dispatch | critical | mitigate | Exact raw argv, verified root/children, closed phase matrix, fixed no-shell spawn, and unit tests. |
| T-139-34 | Repudiation | lifecycle ordering/recovery | high | mitigate | Real host state commands, counters, identities, hook hash, pending status, and same-phase retry assertions. |
| T-139-35 | Information disclosure | router/finalizer diagnostics | medium | mitigate | Class-only router messages, inherited redacted child output, and no child output as routing authority. |
| T-139-36 | Denial of service | long final acceptance and retry | medium | mitigate | 2400000ms outer deadline, bounded inner polling, distinct failures, durable pending recovery, and idempotent children. |
| T-139-37 | Elevation of privilege | later-phase execution | high | mitigate | Non-138/139 numeric phases return inert success without spawn; malformed phases reject. |
</threat_model>

## Artifacts this phase produces

- `tools/gsd-capabilities/lockspire-phase-finalizer/capability.json`: version 1.1.0 metadata preserving one command family and exactly two halt-capable lifecycle steps.
- `tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs`: phase-aware dispatch table, inert-later-phase result, and distinct per-child deadlines.
- `tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs`: Phase 138/139 routing, later-phase no-op, invalid input, and failure-class matrix.
- `tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs`: Phase 139 full host lifecycle, final receipt, pending recovery, no-rerun, and later-phase isolation proof.
- `.gsd-capabilities.json`: supported project installation record for active version 1.1.0.
- `.planning/phases/139-required-truth-reconciliation/COVERAGE.md`: reasoned no-new-external-integration declaration for the existing read-only GitHub/Hex evidence seam.
- No ignored installed runtime file is source authority; no new public command family, Lockspire runtime API, release artifact, cleanup action, or tracked final receipt is produced.

## Spec-less edge probe no-silent-drop audit

| Requirement edge | Explicit acceptance disposition |
|---|---|
| CI-06 adjacency | Plan 01 requires workflow/repository/event/branch/status/conclusion/head SHA as one exact record joined to HEAD/main/origin-main. |
| CI-06 empty | Plan 01 blocks zero, malformed, incomplete, or ambiguous canonical CI candidates. |
| CI-06 ordering | Plans 01 and 06 capture before gates, recheck after gates, and perform no later repository commit. |
| CI-07 concurrency | Plans 01 and 06 lock one run id, bound polling, reject rerun substitution, and recheck main movement. |
| CI-08 unclassified/manual review | Plans 04 and 06 use existing repository evidence to state explicitly that redacted OIDF is supplemental, non-certifying, and non-gating. |
| QUAL-05 adjacency | Plan 01 binds mix ci to the same captured HEAD/main/origin SHA. |
| QUAL-05 empty | Plan 01 rejects nonzero, missing command, or zero executed tests. |
| QUAL-05 ordering | Plan 06 runs the full gate only after final main lands and rechecks the SHA afterward. |
| HYGIENE-05 adjacency | Plan 01 joins all PASS/WARN/BLOCK and run receipts to baseline_sha. |
| HYGIENE-05 empty | Plan 01 emits explicit empty WARN dispositions and blocks absent dispositions for nonempty WARN. |
| HYGIENE-05 ordering | Plans 01 and 06 use deterministic receipt ordering and final before/after identity checks. |
| HYGIENE-06 unclassified/manual review | Plans 01-03 repair only research-demonstrated exact-head, lint, proof-label, no-publish, and composite-pin gaps with fail-first tests. |
| TRUTH-03 unclassified/manual review | Plans 04-06 explicitly prove active v1.38 versus shipped v1.37/1.5.0 and exact GSD lifecycle truth. |
| TRUTH-04 unclassified/manual review | Plans 04 and 06 pin maintained values and perform independent read-only GitHub/Hex checks. |
| TRUTH-05 boundary | Plan 03 scans all tracked workflows plus the repository-controlled composite while Plan 04 leaves release-owned files untouched. |
| TRUTH-05 precision | Plan 03 requires exact lowercase 40-hex external action refs and Plan 06 requires exact manifest/checksum/repository identities. |

No-silent-drop equality: 16 unresolved probe items = 16 explicit acceptance dispositions.

## Prohibition recall outcome

Kept phase-specific safety/value prohibitions are the descriptor-less flagged-unverified entries distributed across Plans 01-07: no accidental publication, no historical rewrite or false shipped claim, no destructive/Phase 140 cleanup, no supplemental-certification promotion, no secret-bearing or tracked final receipt, and no later-phase inheritance of Phase 139 authority.

Canon-security breadcrumb: generic command injection, credential handling, and access-control concerns are covered by the STRIDE registers and `$gsd-secure-phase`; they are not minted as duplicate prohibitions.

## Multi-Source Coverage Audit

| Source | ID | Feature / Constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One exact-SHA repository-owned acceptance and release truth across gates, workflows, planning, and release records | 139-01 through 139-07 | COVERED | Final join occurs after sealed transition. |
| REQ | CI-06 | Required repo-owned CI at exact final main | 139-01, 139-06, 139-07 | COVERED | Exact metadata plus final live run. |
| REQ | CI-07 | Successful or intentional no-publish Release at same baseline | 139-01, 139-03, 139-06, 139-07 | COVERED | Static graph plus live jobs. |
| REQ | CI-08 | Required versus supplemental redacted non-certifying OIDF | 139-04, 139-06 | COVERED | No supplemental remediation. |
| REQ | QUAL-05 | Green mix ci at reconciled baseline | 139-01, 139-02, 139-06 | COVERED | Gate blockers repaired first. |
| REQ | HYGIENE-05 | No BLOCK and every WARN disposition | 139-01, 139-05, 139-06 | COVERED | Exact JSON receipt. |
| REQ | HYGIENE-06 | Checks only for demonstrated deterministic gaps | 139-02, 139-03, 139-05 | COVERED | No maintenance subsystem. |
| REQ | TRUTH-03 | Coherent active milestone/release posture | 139-04, 139-05, 139-07 | COVERED | Active and shipped remain distinct. |
| REQ | TRUTH-04 | Immutable public 1.5.0 release chain | 139-04, 139-06 | COVERED | Source tests plus read-only live proof. |
| REQ | TRUTH-05 | Release Please, protected exact ref, pins, manifest proof | 139-03, 139-04, 139-06 | COVERED | Executable authority preserved. |
| RESEARCH | — | Resolve current refresh_required relation through prescribed ledger-only replacement | 139-05, 139-07 | COVERED | execute:post after all summaries/review. |
| RESEARCH | — | Repair four ShellCheck and thirteen proof-label failures | 139-02 | COVERED | No gate weakening. |
| RESEARCH | — | Exact-SHA hygiene with WARN dispositions and complete failure matrix | 139-01 | COVERED | Leading tracer. |
| RESEARCH | — | Push/no-publish and composite-action contracts | 139-03 | COVERED | Source-only proof. |
| RESEARCH | — | Enumerated maintained-prose contradictions only | 139-04 | COVERED | Historical/archived records preserved. |
| RESEARCH | — | Land every repository write on main before the final checkpoint | 139-06, 139-07 | COVERED | execute:complete:post after host seal. |
| RESEARCH | — | Retain live receipt without a self-invalidating commit | 139-06, 139-07 | COVERED | Mode-0600 Git-common-dir receipt for Phase 141. |
| CONTEXT | D-01 | One synchronized final main SHA | 139-01, 139-06, 139-07 | COVERED | |
| CONTEXT | D-02 | Revalidate immutable Phase 138 relation and fail closed | 139-05, 139-06, 139-07 | COVERED | |
| CONTEXT | D-03 | Bind local/hygiene/CI/Release to same SHA | 139-01, 139-06, 139-07 | COVERED | |
| CONTEXT | D-04 | Push success with intentional publication skips | 139-01, 139-03, 139-06 | COVERED | |
| CONTEXT | D-05 | Preserve exact historical 1.5.0 chain | 139-04, 139-06 | COVERED | |
| CONTEXT | D-06 | Executable protected-release authority over prose | 139-03, 139-04 | COVERED | |
| CONTEXT | D-07 | Add checks only for repeatable repository gaps | 139-01, 139-02, 139-03, 139-05 | COVERED | |
| CONTEXT | D-08 | Exact-head evidence and full-SHA pins | 139-01, 139-03 | COVERED | |
| CONTEXT | D-09 | Existing shell plus focused ExUnit only | 139-01 through 139-07 | COVERED | Existing capability only supplies lifecycle timing. |
| CONTEXT | D-10 | Active planning versus shipped truth | 139-04, 139-05 | COVERED | |
| CONTEXT | D-11 | Supplemental OIDF taxonomy | 139-04, 139-06 | COVERED | |
| CONTEXT | D-12 | Only evidence-proven prose edits | 139-04 | COVERED | |
| CONTEXT | Deferred ideas | Dependency/PR/ref cleanup, final baseline publication, OIDF remediation, new subsystem/runtime work | none | EXCLUDED | Reserved to Phase 140, Phase 141, or later milestone. |

<verification>
  <automated>node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; jq -e '.entries["lockspire-phase-finalizer"].version == "1.1.0"' .gsd-capabilities.json &gt;/dev/null &amp;&amp; gsd_run check api-coverage.verify-pre .planning/phases/139-required-truth-reconciliation</automated>
  <fails_when>Node exits nonzero or runs zero tests, active tracked version differs, either lifecycle boundary is missing/misordered, recovery or later-phase isolation fails, final acceptance can complete before all repository writes are sealed, or API-coverage declaration validation blocks</fails_when>
</verification>

<success_criteria>
- Fresh execute:post currentness and sealed execute:complete:post exact-main acceptance run automatically in one Phase 139 execution.
- Every requirement and D-01 through D-12 has explicit task/truth coverage with all 16 fallback edges accounted for.
- Failure remains pending and blocks Phase 140 until the same Phase 139 command recovers.
- Success leaves final main unchanged after the external receipt and later phases cannot execute Phase 139 logic.
</success_criteria>

<output>
Create `.planning/phases/139-required-truth-reconciliation/139-07-SUMMARY.md`. Leave version 1.1.0 active so fresh execute:post and execute:complete:post rendering complete this same Phase 139 invocation; recovery command remains `$gsd-execute-phase 139`.
</output>
