---
phase: 139-required-truth-reconciliation
plan: "12"
type: execute
wave: 10
depends_on: ["139-11"]
files_modified:
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/support/lockspire/release_proof/package_assertions.ex
  - scripts/maintainer/baseline_inventory.sh
autonomous: true
requirements: [QUAL-05, TRUTH-03]
gap_closure: true
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Active Phase 139 fixture labels are composed from the existing semantic phase attributes or are phrased without a literal phase number, so the active numbered-proof selector remains empty.
    - The completion inventory parser retains its current Git tree path/type behavior without an unused local, and workflow lint passes.
  artifacts:
    - test/lockspire/release/repository_hygiene_contract_test.exs
    - test/support/lockspire/release_proof/package_assertions.ex
    - scripts/maintainer/baseline_inventory.sh
  key_links:
    - The existing `QualityBaseline.active_phase_numbered_proof_locations() == []` assertion remains the authority for detecting active numbered proof labels; no locations are added to its allowances.
    - '`phase_139_completion_plan_count()` continues to derive each path from the tab-delimited `git ls-tree` record and validate the same blob/type/mode contract.'
---

<objective>
Close the two remaining executable Phase 139 implementation gaps in the refreshed verifier report.

Purpose: Restore the proof-quality empty-inventory contract and make the required workflow lint gate pass without changing Phase 139/140 bookkeeping or acceptance authority.
Output: Phase-neutral active proof fixtures and a warning-free inventory tree parser.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-required-truth-reconciliation/139-CONTEXT.md
@.planning/phases/139-required-truth-reconciliation/139-PATTERNS.md
@.planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md
@.planning/phases/139-required-truth-reconciliation/139-11-SUMMARY.md
@test/support/lockspire/release_proof/package_assertions.ex
@test/lockspire/release/repository_hygiene_contract_test.exs
@test/lockspire/quality/proof_quality_baseline_test.exs
@scripts/maintainer/baseline_inventory.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove literal active Phase 139 labels from proof fixtures</name>
  <files>test/lockspire/release/repository_hygiene_contract_test.exs, test/support/lockspire/release_proof/package_assertions.ex</files>
  <read_first>
    - test/lockspire/release/repository_hygiene_contract_test.exs — inspect the Phase 139 relation test name and preserve its assertions.
    - test/support/lockspire/release_proof/package_assertions.ex — use the existing `@baseline_phase_number`, `@next_phase_number`, `@action_phase_number`, `@baseline_phase_label`, `@next_phase_label`, `@action_phase_label`, and `@next_phase_slug` definitions when composing fixture text.
    - test/lockspire/quality/proof_quality_baseline_test.exs — preserve the existing empty assertion for `QualityBaseline.active_phase_numbered_proof_locations/0`.
    - .planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md — use the twelve listed source locations as the bounded repair inventory.
  </read_first>
  <behavior>
    - Fixture strings that need phase identities derive them from the existing semantic attributes, so generated fixture bytes still describe the same phase relationships.
    - A test description that does not need a phase identity uses neutral wording instead of a literal numbered phase label.
    - The proof-quality test continues to require `QualityBaseline.active_phase_numbered_proof_locations() == []`; no path or line allowance is added and the selector itself is not weakened.
  </behavior>
  <action>Per D-07 and D-09, repair only the twelve literal active labels reported by verification in these two source files. Compose phase-bearing fixture strings from the existing semantic attributes, preserving their generated contents and distinctions; make the relation test name phase-neutral if it has no existing attribute source. Keep the active-proof selector and its empty assertion unchanged, and do not add exclusions or allowances for the reported paths. Do not rewrite Phase 139/140 state or planning records.</action>
  <verify>
    <automated>ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/release/repository_hygiene_contract_test.exs test/lockspire/quality/proof_quality_baseline_test.exs --only phase139_gap_closure</automated>
    <fails_when>The focused selector fails or reports any active numbered-proof location, the existing empty assertion or any fixture assertion is removed/weakened, or any reported literal is merely added to an allowance.</fails_when>
  </verify>
  <done>The focused Phase 139 gap-closure selector passes with zero active numbered-proof locations while the proof-quality empty assertion and fixture coverage remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the unused object local from the completion tree parser</name>
  <files>scripts/maintainer/baseline_inventory.sh</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — inspect `phase_139_completion_plan_count()` and the adjacent `git ls-tree` record parsing and path validation.
    - scripts/ci/lint_workflows.sh — preserve the repository-owned ShellCheck gate and its existing invocation contract.
    - .planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md — use the exact SC2034 finding at the reported parser assignment.
  </read_first>
  <action>In `phase_139_completion_plan_count()`, remove only the assignment of the unread `object` local from `git ls-tree` record parsing. Retain the existing `mode`, `type`, tab-delimited `path`, suffix, and plan/summary validation behavior; do not add a ShellCheck suppression or otherwise alter parser semantics.</action>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; ASDF_ELIXIR_VERSION=1.19.5-otp-28 ASDF_ERLANG_VERSION=28.1 MIX_ENV=test mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_final_acceptance &amp;&amp; bash scripts/ci/lint_workflows.sh</automated>
    <fails_when>Shell syntax or workflow lint exits nonzero, SC2034 remains, or Git tree path/type validation behavior changes.</fails_when>
  </verify>
  <done>The completion tree parser has no unused `object` local, its valid and hostile completion cases pass through the `phase139_final_acceptance` selector, and `scripts/ci/lint_workflows.sh` passes.</done>
</task>

</tasks>

## Required post-verification orchestration (outside the implementation tasks)

After both implementation tasks pass, run a fresh Phase 139 verifier. Only when its current report has `status: passed`, invoke the canonical GSD transition with `gsd-tools phase complete 139`. Do not hand-edit STATE, ROADMAP, REQUIREMENTS, or `state.json` to imitate this transition.

Accept the transition only when the canonical command succeeds and its resulting records reconcile Phase 139 as 12/12 complete in ROADMAP, advance STATE/state.json consistently to Phase 140, and leave CI-06/CI-07 pending. Preserve the existing blocking Phase 140 exact-SHA `plan:pre` entry gate and its live-evidence requirements; the transition must not mark either requirement satisfied or bypass the gate. Do not invoke `phase complete` before fresh verification passes.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository source → proof selector | Source labels are scanned as evidence; fixture spelling must not hide active phase-numbered proof from the selector. |
| Git tree record → inventory parser | Git-produced mode, type, object, and path fields feed plan/summary classification. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-12-01 | Tampering | Proof-quality selector and fixtures | low | mitigate | Preserve the existing empty inventory assertion and exact fixture assertions; derive labels semantically without adding selector allowances. |
| T-139-12-02 | Tampering | `phase_139_completion_plan_count()` | low | mitigate | Remove only the unread local while retaining the current tree record path/type checks and lint verification. |
</threat_model>

<verification>
Run the focused Phase 139 gap-closure ExUnit selector, the `phase139_final_acceptance` selector for valid and hostile completion tree parsing, and the repository workflow lint gate. After a fresh passed Phase 139 verification report, use the canonical `gsd-tools phase complete 139` transition and verify 12/12 planning reconciliation, pending CI-06/CI-07, and the unchanged Phase 140 exact-SHA entry gate.
</verification>

<success_criteria>
Both reported implementation findings are closed by automated checks: the active numbered-proof inventory remains empty and repository workflow lint passes with no SC2034 warning.
</success_criteria>

<output>
Create `.planning/phases/139-required-truth-reconciliation/139-12-SUMMARY.md` when done.
</output>
