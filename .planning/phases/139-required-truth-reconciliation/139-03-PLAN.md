---
phase: 139-required-truth-reconciliation
plan: "03"
type: execute
wave: 2
depends_on: ["139-01"]
files_modified:
  - test/lockspire/release_ci_evidence_contract_test.exs
  - test/lockspire/workflow_supply_chain_contract_test.exs
autonomous: true
requirements: [CI-07, HYGIENE-06, TRUTH-05]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "CI-07 D-04 D-06: static contract proof preserves the push-only Release Please job and the workflow_dispatch-only validation, proof, publish, and public-install chain."
    - "A successful push no-publish result is an explicit positive baseline outcome, while the protected dispatch publication path remains fully required for actual releases."
    - "TRUTH-05 boundary: immutable external-action reference coverage includes every tracked workflow and the repository-controlled Release Please composite, while workflow-only timeout and service-image checks remain scoped to workflows."
    - "TRUTH-05 precision D-08: every non-local uses reference ends in exactly one lowercase 40-hex commit SHA; local ./ references remain permitted and no tag/branch prefix is accepted."
    - "HYGIENE-06 unclassified: the new assertions correspond exactly to the demonstrated no-publish job-graph and composite-action scan gaps and introduce no new workflow behavior."
  prohibitions:
    - statement: "Contract hardening must not edit the authoritative workflows or composite action merely to satisfy assertions, dispatch a workflow, publish a package, or change Release Please ownership."
      verification: flagged-unverified
    - statement: "The action-pin scan must not stop at workflow files or accept tags, branches, shortened digests, variable expressions, or mutable aliases for external actions."
      verification: flagged-unverified
  artifacts:
    - path: test/lockspire/release_ci_evidence_contract_test.exs
      provides: "Focused push/no-publish versus protected-dispatch release job-graph contract."
      contains: "Maintain Release Please PR"
    - path: test/lockspire/workflow_supply_chain_contract_test.exs
      provides: "Deterministic immutable-action scan across workflows and the repository-controlled composite."
      contains: ".github/actions/release-please/action.yml"
  key_links:
    - from: test/lockspire/release_ci_evidence_contract_test.exs
      to: .github/workflows/release.yml
      via: "positive job names, trigger conditions, needs edges, and negative cross-event assertions"
      pattern: "push|workflow_dispatch|release-please|recovery-validation|publish"
    - from: test/lockspire/workflow_supply_chain_contract_test.exs
      to: .github/actions/release-please/action.yml
      via: "one deterministic tracked-path list applies the exact full-SHA predicate to every external uses reference"
      pattern: "@[0-9a-f]{40}"
---

<objective>
Pin the two missing executable release guardrails: the intentional push/no-publish job graph and full-SHA references inside the repository-controlled composite action.

Purpose: CI-07 and TRUTH-05 depend on existing executable workflow behavior remaining provable without changing or triggering that behavior.
Output: Two focused static contracts covering event-aware release outcomes and the complete tracked action-reference surface.

Decision coverage: D-04 defines no-publish success; D-06 keeps executable workflow ordering authoritative; D-08 requires exact SHA pins; D-09 keeps proof in focused ExUnit.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/139-required-truth-reconciliation/139-CONTEXT.md
@.planning/phases/139-required-truth-reconciliation/139-RESEARCH.md
@.planning/phases/139-required-truth-reconciliation/139-PATTERNS.md
@.github/workflows/release.yml
@.github/actions/release-please/action.yml
@test/lockspire/release_ci_evidence_contract_test.exs
@test/lockspire/workflow_supply_chain_contract_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the push no-publish and protected-dispatch job graph</name>
  <reversibility rating="reversible">This adds static assertions around an existing workflow contract and changes no workflow execution state.</reversibility>
  <files>test/lockspire/release_ci_evidence_contract_test.exs</files>
  <read_first>
    - .github/workflows/release.yml — extract the exact triggers, job ids, display names, if conditions, needs edges, and publish-before-GitHub-release order
    - test/lockspire/release_ci_evidence_contract_test.exs — extend its existing File.read and positive/negative structural assertion style
    - .planning/phases/139-required-truth-reconciliation/139-CONTEXT.md — preserve D-04 no-publish semantics and D-06 executable authority
  </read_first>
  <behavior>
    - Push enables Maintain Release Please PR and does not make any validation or publication job eligible.
    - workflow_dispatch gates exact-head validation, prepublish proof, Hex publication, and public-install verification through their existing needs chain.
    - The protected publisher consumes the verified artifact and publishes the package before creating the matching GitHub release.
    - No test invokes GitHub, dispatches Release, changes workflow YAML, or interprets skipped publication as a shipped release.
  </behavior>
  <action>Add a focused structural test that parses the current release workflow into job regions and asserts the exact event split and dependency graph by job id and display name. Require `release-please` to be push-only; require `recovery-validation` to be workflow_dispatch-only; require `prepublish-proof`, `publish`, and `post-publish-install-truth` to retain their exact validation/proof/publish needs. Assert the four protected job names used by Plan 01's live receipt and preserve package publication before matching GitHub release creation. Add negative assertions preventing the push job from depending on or invoking publication steps. Record assumption-delta `no-change`: push skips are event-inapplicable while protected publication remains mandatory for the existing dispatch release path, so no required release control becomes optional.</action>
  <acceptance_criteria>
    - The test fails if any protected publication job becomes push-eligible or if Release Please becomes dispatch-owned.
    - The test fails if any required needs edge or the package-before-GitHub-release order changes.
    - The test reads source only and performs no external or release-owned mutation.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release_ci_evidence_contract_test.exs</automated>
    <fails_when>Mix exits nonzero or runs zero tests, any event/job/dependency assertion is absent, a push path can reach publication, or protected release ordering differs from the executable authority</fails_when>
  </verify>
  <done>The event-aware Release graph is explicitly protected and the D-04 no-publish result cannot be confused with publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scan the complete tracked action-reference surface for immutable pins</name>
  <reversibility rating="reversible">The deterministic path list and assertion predicate are test-only and can change locally with the tracked action surface.</reversibility>
  <files>test/lockspire/workflow_supply_chain_contract_test.exs</files>
  <read_first>
    - test/lockspire/workflow_supply_chain_contract_test.exs — preserve workflow timeout and PostgreSQL-image assertions while extracting the shared uses-reference check
    - .github/actions/release-please/action.yml — include its actions/setup-node reference in the immutable external-action scan
    - .github/workflows/ci.yml — confirm local composite references remain permitted and workflow-only assertions remain meaningful
  </read_first>
  <behavior>
    - A deterministic sorted path list contains every tracked workflow YAML file plus the Release Please composite action.
    - Every external uses reference ends in one lowercase 40-hex SHA; local repository-relative action references remain allowed.
    - Workflow timeout and service-image checks continue to apply only to workflow files, not to composite-action YAML where those concepts do not exist.
    - A fixture or source mutation to a tag, branch, short SHA, expression, or missing suffix makes the focused test fail.
  </behavior>
  <action>Refactor the current workflow glob into a deterministic sorted workflow list plus a separate immutable-reference path list that appends `.github/actions/release-please/action.yml`. Apply the existing local-reference allowance and exact full-commit-SHA predicate to every file in the second list. Keep timeout-minutes and PostgreSQL image assertions iterating only workflow files. Assert the composite path exists and is scanned exactly once so an absent or duplicated path cannot yield a vacuous pass. Do not alter any workflow or composite action.</action>
  <acceptance_criteria>
    - The focused test identifies every workflow and exactly one repository-controlled composite action.
    - The current full-SHA composite reference passes, while every mutable-reference mutation used by the test fails the predicate.
    - Workflow-only policies retain their previous scope and results.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/workflow_supply_chain_contract_test.exs</automated>
    <fails_when>Mix exits nonzero or runs zero tests, the composite path is absent/duplicated, any external uses reference avoids exact 40-hex validation, or workflow-only checks are weakened or misapplied</fails_when>
  </verify>
  <done>The full tracked GitHub Actions surface is protected against mutable external references without changing executable workflow behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Workflow event -> privileged job eligibility | Push and workflow_dispatch must retain distinct authority. |
| Tracked YAML -> external action code | Every non-local uses reference crosses a supply-chain boundary controlled by its ref. |
| Static contract -> release claim | Source assertions prove structure only; live final-SHA outcomes remain Plan 06 evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-139-11 | Elevation of privilege | release workflow event split | critical | mitigate | Assert push-only maintenance, dispatch-only validation/publication, exact needs, and publish order. |
| T-139-12 | Tampering | external action references | high | mitigate | Scan workflows plus composite and require exact lowercase 40-hex refs for every external use. |
| T-139-13 | Spoofing | static no-publish evidence | medium | mitigate | Keep static graph proof separate from Plan 06's live exact-run receipt. |
| T-139-SC | Tampering | package-manager installs | low | accept | No package install is planned; current tests use the existing Mix/ExUnit stack. |
</threat_model>

## Artifacts this phase produces

- `test/lockspire/release_ci_evidence_contract_test.exs`: one named push/no-publish job-graph test and exact job/needs assertions; no new workflow symbol.
- `test/lockspire/workflow_supply_chain_contract_test.exs`: deterministic `workflow_paths` and `immutable_reference_paths` collections plus one shared exact-SHA reference assertion.
- No workflow YAML, composite-action implementation, package, release-owned file, tag, release, or published artifact is created or modified.

<verification>
  <automated>mix test test/lockspire/release_ci_evidence_contract_test.exs test/lockspire/workflow_supply_chain_contract_test.exs</automated>
  <fails_when>Mix exits nonzero or runs zero tests, push can reach protected publication, the needs/order graph drifts, the composite escapes scanning, or any mutable external action reference passes</fails_when>
</verification>

<success_criteria>
- Push/no-publish and protected dispatch are distinct, executable, tested outcomes.
- Full-SHA action pinning covers workflows and the repository-controlled composite.
- Tests preserve the existing workflow rather than redesigning it.
- Both demonstrated HYGIENE-06 gaps have non-vacuous focused proof.
</success_criteria>

<output>
Create `.planning/phases/139-required-truth-reconciliation/139-03-SUMMARY.md` recording the protected graph and complete immutable-reference path set.
</output>
