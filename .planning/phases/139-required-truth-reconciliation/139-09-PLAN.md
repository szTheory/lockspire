---
phase: 139-required-truth-reconciliation
plan: "09"
type: execute
wave: 7
depends_on: ["139-08"]
files_modified:
  - tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json
  - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
  - .github/workflows/ci.yml
  - test/lockspire/workflow_supply_chain_contract_test.exs
  - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
  - scripts/maintainer/baseline_inventory.sh
autonomous: true
gap_closure: true
gap_ids: [G-139-01, G-139-06, G-139-07, G-139-08, G-139-09, G-139-10, G-139-11]
requirements: [CI-06, CI-07, CI-08, QUAL-05, HYGIENE-05, HYGIENE-06, TRUTH-03, TRUTH-04, TRUTH-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "G-139-06: the active project capability runs the Phase 139 pre-verifier at supported execute:post after code review and before normal verification, producing one current proposal-only ledger replacement per D-02."
    - "G-139-07 and G-139-10: the current host uses supported blocking plan:pre before Phase 140 planning as the post-transition recovery boundary; it authenticates the sealed Phase 139 lifecycle and blocks later routing until success without claiming the removed execute:complete:post point exists."
    - "G-139-08: complete writer, hook, transformation, before/after, transition, and lifecycle authentication occurs before update-ref or push; forged fields leave HEAD, local main, and advertised origin/main unchanged."
    - "G-139-11: signalling only the production router with HUP, INT, or TERM reaps the supervisor, finalizer process group, and resistant descendants before any post-cancellation mutation."
    - "G-139-01 and G-139-09 / CI-06 / CI-07: after gap execution, normal verification, completion, and transition writes, the supported lifecycle synchronizes HEAD/local main/origin main with the exact CI and Release no-publish SHA and retains the mode-0600 Git-common-dir receipt."
    - "Required CI runs the portable repository-owned lifecycle contract in addition to the router unit contract, with no GSD download, credentials, workflow dispatch, release publication, or duplicate ExUnit matrix."
    - "D-04 through D-06 and D-11 preserve the immutable 1.5.0 release chain and supplemental non-certifying OIDF evidence separately from the Phase 139 acceptance SHA."
  artifacts:
    - path: tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json
      provides: "Tracked, bounded supported-host contract for execute:post and plan:pre envelopes used only when a live GSD runtime is unavailable in generic CI."
      contains: "plan:pre"
    - path: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
      provides: "Portable capability/state/finalizer lifecycle contract plus live-runtime parity checks when GSD_TOOLS is available."
      contains: "LOCKSPIRE_GSD_HOST_FIXTURE"
    - path: .github/workflows/ci.yml
      provides: "Distinct recurring lifecycle contract in the existing Release Hygiene Drift job."
      contains: "lockspire-finalize-lifecycle.test.cjs"
    - path: test/lockspire/workflow_supply_chain_contract_test.exs
      provides: "Executable source contract for the protected release-hygiene job identity, permissions, lifecycle-step uniqueness, and step order."
      contains: "portable phase-finalizer lifecycle"
    - path: .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
      provides: "Terminal mapping for G-139-01 and G-139-06 through G-139-11 to portable, live-runtime, and post-transition evidence."
      contains: "G-139-10"
  key_links:
    - from: .github/workflows/ci.yml
      to: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
      via: "the existing release-hygiene job runs the portable host-contract mode once after the router contract"
      pattern: "LOCKSPIRE_GSD_HOST_FIXTURE|node --test"
    - from: test/lockspire/workflow_supply_chain_contract_test.exs
      to: .github/workflows/ci.yml
      via: "a source-level ExUnit assertion isolates release-hygiene and pins its protected identity, read-only permissions, exact step sequence, and one lifecycle command"
      pattern: "release-hygiene|Release Hygiene Drift|contents: read"
    - from: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
      to: tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json
      via: "CI loads only allowlisted supported points, ordered gate envelopes, and state protocol descriptors; local execution compares the same expectations with live render-hooks output"
      pattern: "execute:post|plan:pre|post-completion-finalizer"
    - from: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
      to: "scripts/maintainer/finalize_phase_138_inventory.sh and scripts/maintainer/finalize_phase_139_acceptance.sh"
      via: "a real temporary Git repository exercises preverify publication, sealed authentication, exact fast-forward, receipt retention, failure recovery, and later-phase blocking"
      pattern: "pre-verify|post-transition|lockspire-phase-139-acceptance-v1"
    - from: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
      to: tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs
      via: "lifecycle proof consumes the production router while the router suite independently targets cancellation at the actual router PID"
      pattern: "SIGHUP|SIGINT|SIGTERM"
---

<objective>
Close the host-lifecycle and final exact-SHA gaps through the current supported GSD boundaries, then make the portable part of that contract a required recurring signal.

Purpose: The original implementation targeted a removed loop point and a removed runtime helper. Current source rebases finalization onto execute:post plus blocking plan:pre with a project-owned state helper; this plan makes that repaired architecture hermetic in CI, live-runtime checked during execution, and sufficient for the terminal acceptance receipt.
Output: A bounded host-contract fixture, a portable/live lifecycle suite, required CI coverage, and terminal gap mappings.

Gap closure: G-139-01, G-139-06, G-139-07, G-139-08, G-139-09, G-139-10, and router-cancellation under G-139-11.

Decision coverage: D-01 through D-12. The supported plan:pre recovery gate preserves D-01 through D-03 finality without recreating the invalid execute:complete:post lifecycle point.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-required-truth-reconciliation/139-CONTEXT.md
@.planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md
@.planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
@.planning/phases/139-required-truth-reconciliation/139-08-SUMMARY.md
@tools/gsd-capabilities/lockspire-phase-finalizer/capability.json
@tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.cjs
@tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs
@tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalizer-process-supervisor.cjs
@tools/gsd-capabilities/lockspire-phase-finalizer/post-completion-finalizer-state.cjs
@tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs
@scripts/maintainer/run_lockspire_phase_finalizer.sh
@scripts/maintainer/finalize_phase_138_inventory.sh
@scripts/maintainer/finalize_phase_139_acceptance.sh
@.github/workflows/ci.yml
@test/lockspire/workflow_supply_chain_contract_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one supported Phase 139 lifecycle from preverify to durable receipt</name>
  <reversibility rating="reversible">The tracked fixture and test adapter affect only internal lifecycle proof and can be replaced without changing product data, release state, or the finalizer command contract.</reversibility>
  <files>tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json, tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs, .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md</files>
  <read_first>
    - tools/gsd-capabilities/lockspire-phase-finalizer/capability.json — preserve active 1.2.0 execute:post and plan:pre blocking gates, fixed command lines, timeouts, and empty public surface
    - tools/gsd-capabilities/lockspire-phase-finalizer/post-completion-finalizer-state.cjs — exercise the project-owned begin/prepare/seal/verify/complete state protocol and exact writer descriptors
    - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs — retain installed-file parity, real temporary repositories, pending recovery, final landing/receipt matrix, and live host checks when available
    - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs — retain production-router-only HUP/INT/TERM cancellation proof and distinct failure classes
    - scripts/maintainer/finalize_phase_139_acceptance.sh — preserve complete sealed-candidate authentication before main mutation and mode-0600 out-of-worktree receipt emission
    - .planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md — close the failed host truths without changing the canonical result before re-verification
  </read_first>
  <behavior>
    - In portable mode, a tracked host contract exposes only execute:post and plan:pre, the exact blocking gate predicates, and the project-owned state-helper protocol required by Phase 139.
    - In live mode, the same expectations are compared to current `gsd_run loop render-hooks` output and current host workflow descriptors; disagreement fails instead of updating the fixture automatically.
    - One real temporary repository runs pre-verify after completed summaries/review, produces one ledger-only commit, seals Phase 139 completion, runs post-transition before Phase 140 planning, synchronizes the candidate, and retains the external receipt with no later commit.
    - Forged writer/hook/transformation/before/after/lifecycle fields fail before ref mutation; post-transition failure stays pending and blocks later routing; retry runs only unfinished post-transition work.
    - Router-only HUP, INT, and TERM cancel and reap the supervisor, finalizer group, and descendants with no later mutation.
  </behavior>
  <action>Create `fixtures/gsd-host-contract.json` as an allowlisted test input containing the supported lifecycle point names, exact Phase 139/140 gate predicates, timeout values, state-helper protocol name, and expected tracked runtime files. It must contain no executable package copy, credential, workflow implementation, or installed-runtime authority. Refactor `lockspire-finalize-lifecycle.test.cjs` so `LOCKSPIRE_GSD_HOST_FIXTURE` selects this portable contract when no GSD runtime is present; when `GSD_TOOLS` or a discoverable live runtime exists, keep all current live capability-list, render-hooks, workflow-descriptor, and installed-file parity checks and additionally require agreement with the fixture. Move no project behavior into the fixture. Run the existing real-repository lifecycle, pre-authentication, recovery, receipt, and later-phase isolation tests in both modes. Import or invoke the existing production-router cancellation cases rather than recreating a weaker supervisor-only case. Update 139-VALIDATION.md so G-139-06 through G-139-11 name the precise portable and live entry points. Per D-01 through D-03, the semantic final boundary is the blocking plan:pre gate before Phase 140 work; do not declare or emulate execute:complete:post.</action>
  <acceptance_criteria>
    - The lifecycle suite starts and runs all repository-owned tests in a clean environment with no global GSD installation when the fixture variable is set.
    - A local execution with live GSD tools additionally proves current installed-file parity and current execute:post/plan:pre rendering against the same expectations.
    - Complete sealed authentication precedes any ref mutation, router-targeted cancellation prevents mutation, and pending recovery runs only the unfinished finalizer.
    - The successful fixture leaves one exact candidate, one mode-0600 receipt outside the worktree, and no repository commit after receipt creation.
  </acceptance_criteria>
  <verify>
    <automated>LOCKSPIRE_GSD_HOST_FIXTURE=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs</automated>
    <fails_when>Node exits nonzero or runs zero tests, the fixture broadens supported points, live and portable contracts disagree, authentication occurs after mutation, router cancellation leaves a process/mutation, recovery repeats completed work, or the receipt is tracked/invalidated</fails_when>
  </verify>
  <done>One production-shaped Phase 139 path is proven from supported preverify through blocked recovery to the final external receipt, with live-runtime parity when available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the portable lifecycle contract to required CI and seal the gap audit</name>
  <reversibility rating="reversible">The CI step and validation mapping are repository maintenance controls that can be adjusted without changing the capability command, product API, or release ownership.</reversibility>
  <files>.github/workflows/ci.yml, test/lockspire/workflow_supply_chain_contract_test.exs, .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md</files>
  <read_first>
    - .github/workflows/ci.yml — extend the existing Release Hygiene Drift job after the router test; preserve full-SHA action pins, job name, permissions, and all existing steps
    - test/lockspire/workflow_supply_chain_contract_test.exs — extend the existing workflow source contract with a narrow assertion over the release-hygiene job rather than introducing a second workflow parser
    - tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json — pass the tracked portable fixture explicitly and do not download a runtime
    - tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs — use Task 1's zero-credential portable command; live host parity remains an execute/verify boundary check
    - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md — finish the eleven-gap and nineteen-prohibition mappings only after the named commands exist
    - .planning/phases/139-required-truth-reconciliation/COVERAGE.md — retain the detector-accepted no-new-external-API declaration; do not fabricate an integration matrix
  </read_first>
  <behavior>
    - Release Hygiene Drift runs the self-contained router suite and the portable lifecycle suite exactly once without npm/npx package installation, GitHub/Hex credentials, or mutable-source access.
    - The workflow source contract fails when the lifecycle step is absent, duplicated, or outside the required router-to-release-audit interval, or when top-level read-only permissions and the protected release-hygiene job name, runner, timeout, or prior step sequence drift.
    - The CI lifecycle command proves project-owned hook envelopes, state helper, process supervision, cancellation, recovery, pre-authentication, receipt safety, and later-phase blocking.
    - Live GSD rendering, authenticated GitHub/Hex observations, and final origin/main synchronization remain at the existing execute/verify lifecycle boundaries where their prerequisites exist.
    - 139-VALIDATION.md maps all eleven gap IDs and all nineteen prohibitions to current tracked proof, with G-139-01 remaining pending until the post-transition receipt actually exists.
  </behavior>
  <action>Add one named Release Hygiene Drift step immediately after the current router-contract step and before the release-please runtime dependency audit. Set `LOCKSPIRE_GSD_HOST_FIXTURE` to the tracked fixture and run both Node test files in one exact `node --test` command so shared lifecycle contracts execute once. Extend `workflow_supply_chain_contract_test.exs` with the source-contract test `release hygiene runs the portable phase-finalizer lifecycle exactly once in protected order`; isolate the `release-hygiene` job and assert that top-level `contents: read` permissions remain unique, job key/name plus `ubuntu-latest` runner plus 30-minute timeout remain exact, all protected pre-existing step names remain once and in their existing order, the router and lifecycle commands each occur once, and byte/order positions place router before lifecycle before the release-please audit. Make missing, duplicated, or reordered lifecycle coverage and protected job/permission drift fail ExUnit. Do not install GSD, add a package manager invocation, add credentials, query live APIs, run the subprocess-heavy ExUnit matrix again, or change any workflow trigger/permission/job identity. Update 139-VALIDATION.md with both the CI command and its ExUnit workflow-source owner plus final gap dispositions: repository-owned repairs are automated, current-host rendering is automated by the live execution gate, and G-139-01 becomes closed only after the supported post-transition path writes a valid receipt for the synchronized SHA. Preserve the existing API detector declaration and the locked separation between required acceptance, historical 1.5.0, and supplemental OIDF evidence.</action>
  <acceptance_criteria>
    - CI runs the portable lifecycle suite once in the existing required release-hygiene job and preserves every prior job/step.
    - The focused workflow source test proves lifecycle-step presence, uniqueness, and router-before-lifecycle-before-release-audit order while pinning top-level read-only permissions and the protected job identity, runner, timeout, and prior step sequence.
    - The workflow contains no new package download, secret, dispatch, publication, or external mutation surface.
    - 139-VALIDATION.md contains complete G-139-01 through G-139-11 and nineteen-prohibition mappings without changing 139-VERIFICATION.md.
    - The active capability remains installed from tracked source; the final execute/plan lifecycle is halt-capable and Phase 140 cannot proceed while post-transition acceptance is pending.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/workflow_supply_chain_contract_test.exs &amp;&amp; LOCKSPIRE_GSD_HOST_FIXTURE=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; bash scripts/ci/lint_workflows.sh</automated>
    <fails_when>ExUnit, Node, or workflow lint exits nonzero; either suite runs zero tests; CI lacks, duplicates, or misorders the lifecycle step; the protected release-hygiene job or read-only permissions drift; a package/runtime download or credential is added; or validation claims final receipt success before the receipt exists</fails_when>
  </verify>
  <done>Required CI owns the portable recurring lifecycle risk, while live final acceptance remains an automatic blocking lifecycle operation with no human handoff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Tracked host contract -> lifecycle test | The fixture may describe only allowlisted supported interfaces and cannot become executable runtime authority. |
| Live GSD runtime -> project capability | Current render-hooks and writer descriptors must equal the tracked expectations before the live path is trusted. |
| Sealed host state -> local/origin main | Complete receipt and lifecycle authentication must precede any compare-and-swap or non-force push. |
| Router process -> supervisor/finalizer group | Targeted cancellation must propagate and reap every descendant before mutation. |
| GitHub/Hex evidence -> durable receipt | Only exact allowlisted identity/status/checksum fields may cross into the mode-0600 receipt. |

## STRIDE Threat Register — ASVS L1, block threshold high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-139-G09-01 | Spoofing | host lifecycle contract | high | mitigate | Require live-runtime parity when available and restrict portable fixture fields to supported point/predicate/protocol descriptors. |
| T-139-G09-02 | Tampering | sealed candidate and refs | critical | mitigate | Run complete pre-landing authentication, exact ancestry, compare-and-swap, non-force push, re-fetch, and unchanged-state checks. |
| T-139-G09-03 | Repudiation | final acceptance receipt | high | mitigate | Retain exact SHA, run identities, gate results, warning dispositions, historical chain, repository, and mode-0600 atomic file proof. |
| T-139-G09-04 | Information disclosure | CI/lifecycle diagnostics | high | mitigate | Use fixture-only CI, class-bounded diagnostics, allowlisted receipt fields, and no credentials/raw responses. |
| T-139-G09-05 | Denial of service | cancellation and recovery | high | mitigate | Forward HUP/INT/TERM through the production router boundary, reap process groups, bound timeouts, and retry only pending work. |
| T-139-G09-06 | Elevation of privilege | plan:pre finalization | critical | mitigate | Gate only Phase 140 entry, authenticate sealed Phase 139 state, prohibit release dispatch/publication, and halt later routing on failure. |
| T-139-G09-SC | Tampering | package-manager installs | low | accept | No package install is planned; CI uses repository-owned Node files and a tracked data fixture. |
</threat_model>

## Stable verification-gap IDs

| Gap ID | Report truth | Plan owner |
|---|---|---|
| G-139-01 | Exact acceptance SHA equals HEAD, local main, origin/main, canonical CI, and Release no-publish evidence | 139-09 |
| G-139-02 | Every hostile exact-SHA fixture fails closed | 139-08 |
| G-139-03 | mix ci executes tests at the captured SHA and is green | 139-08 |
| G-139-04 | Workflow/shell lint and semantic phase-label proof both pass | 139-08 |
| G-139-05 | Maintained planning records expose one coherent Phase 139 posture | 139-08 |
| G-139-06 | execute:post publishes one current ledger before verification | 139-09 |
| G-139-07 | Supported post-transition gate authenticates the sealed lifecycle before Phase 140 planning | 139-09 |
| G-139-08 | Main advances only after complete receipt/lifecycle authentication | 139-09 |
| G-139-09 | Durable out-of-worktree acceptance receipt is produced and retained | 139-09 |
| G-139-10 | Installed recovery blocks later routing and replays only unfinished work | 139-09 |
| G-139-11 | Completion and cancellation reject contradictory state and reap the production process tree | 139-08, 139-09 |

## Spec-less edge-probe flagged assumptions

| # | Requirement edge | Flagged assumption carried into execution | Proof owner |
|---:|---|---|---|
| 1 | CI-06 adjacency | Workflow, repository, event, branch, status, conclusion, and head SHA form one exact record joined to HEAD/main/origin-main. | 139-08 exact-SHA matrix |
| 2 | CI-06 empty | Zero, malformed, incomplete, or ambiguous canonical CI candidates block. | 139-08 exact-SHA matrix |
| 3 | CI-06 ordering | Identity is captured before gates, rechecked after gates, and no tracked write follows final acceptance. | 139-09 lifecycle/receipt |
| 4 | CI-07 concurrency | One run id is retained across bounded polling; rerun substitution and main movement block. | 139-08 exact-SHA matrix |
| 5 | CI-08 unclassified | Supplemental OIDF remains redacted, non-certifying, and non-gating. | existing conformance contracts + 139-09 receipt |
| 6 | QUAL-05 adjacency | mix ci belongs to the same captured synchronized SHA. | 139-08 focused proof + finalizer |
| 7 | QUAL-05 empty | Missing command, nonzero exit, or zero executed tests cannot pass. | 139-08 focused proof |
| 8 | QUAL-05 ordering | Full gate runs only after final main landing and SHA is rechecked afterward. | 139-09 lifecycle/receipt |
| 9 | HYGIENE-05 adjacency | PASS/WARN/BLOCK and workflow receipts share baseline_sha. | 139-08 exact-SHA matrix |
| 10 | HYGIENE-05 empty | Zero WARN is explicit and each nonzero WARN has exactly one disposition. | 139-08 exact-SHA matrix |
| 11 | HYGIENE-05 ordering | Receipt order and final before/after identity checks are deterministic. | 139-09 lifecycle/receipt |
| 12 | HYGIENE-06 unclassified | Only demonstrated repository-owned defects earn checks. | 139-08 gap/prohibition audit |
| 13 | TRUTH-03 unclassified | Active v1.38, shipped v1.37/1.5.0, and lifecycle state remain separate. | 139-08 planning consistency |
| 14 | TRUTH-04 unclassified | Historical values remain pinned and independently read-only corroborated. | existing release-readiness contracts + 139-09 receipt |
| 15 | TRUTH-05 boundary | Workflows and the repository-controlled composite remain the complete action-reference surface. | existing supply-chain contract |
| 16 | TRUTH-05 precision | Every external action ref is one lowercase full 40-hex SHA. | existing supply-chain contract |

All sixteen remain explicit execution assumptions until the named commands pass; none is treated as resolved by prose alone.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | One exact-SHA repository-owned acceptance and release truth across gates, workflows, planning, and release records | 139-08, 139-09 | COVERED | Focused repository proof precedes supported live lifecycle acceptance. |
| REQ | CI-06 | Required repo-owned CI at exact final main | 139-08, 139-09 | COVERED | Hostile matrix plus terminal receipt. |
| REQ | CI-07 | Same-SHA successful or intentional no-publish Release | 139-09 | COVERED | Existing job-graph proof plus final exact run evidence. |
| REQ | CI-08 | Required acceptance remains distinct from redacted supplemental OIDF | 139-09 | COVERED | Existing verified truth is preserved and rejoined in the receipt. |
| REQ | QUAL-05 | Green mix ci at reconciled baseline | 139-08, 139-09 | COVERED | Proof-quality closure plus final exact-SHA gate. |
| REQ | HYGIENE-05 | No BLOCK and every WARN disposition | 139-08, 139-09 | COVERED | Hostile matrix plus terminal acceptance. |
| REQ | HYGIENE-06 | Only demonstrated deterministic repository checks | 139-08, 139-09 | COVERED | No package, subsystem, or product surface is added. |
| REQ | TRUTH-03 | Coherent milestone and release posture | 139-08, 139-09 | COVERED | Lifecycle-aware planning contract plus blocked transition. |
| REQ | TRUTH-04 | Immutable public 1.5.0 release chain | 139-09 | COVERED | Existing verified truth is preserved and independently corroborated. |
| REQ | TRUTH-05 | Release Please, protected exact ref, full-SHA pins, manifest proof | 139-09 | COVERED | Existing verified contracts remain unchanged. |
| RESEARCH | — | Use existing shell, ExUnit, Node capability, and exact-SHA evidence seams | 139-08, 139-09 | COVERED | No external package install or new authority. |
| RESEARCH | — | Fail closed on stale ledger, malformed evidence, races, cancellation, and receipt forgery | 139-08, 139-09 | COVERED | Named hostile and lifecycle matrices. |
| RESEARCH | — | Retain live acceptance outside tracked history | 139-09 | COVERED | Mode-0600 Git-common-dir receipt. |
| CONTEXT | D-01 | One synchronized final main SHA | 139-09 | COVERED | |
| CONTEXT | D-02 | Revalidate immutable inventory relation and fail closed | 139-09 | COVERED | |
| CONTEXT | D-03 | Bind mix ci, hygiene, CI, and Release to one SHA | 139-08, 139-09 | COVERED | |
| CONTEXT | D-04 | Push no-publish is a valid non-release outcome | 139-09 | COVERED | |
| CONTEXT | D-05 | Preserve exact historical 1.5.0 chain | 139-09 | COVERED | |
| CONTEXT | D-06 | Executable protected-release authority over prose | 139-09 | COVERED | |
| CONTEXT | D-07 | Add checks only for demonstrated repository gaps | 139-08, 139-09 | COVERED | |
| CONTEXT | D-08 | Exact-head evidence and full-SHA action pins | 139-08, 139-09 | COVERED | |
| CONTEXT | D-09 | Existing repo-local shell and focused tests only | 139-08, 139-09 | COVERED | Node capability surface is pre-existing internal infrastructure. |
| CONTEXT | D-10 | Active planning truth differs from shipped truth | 139-08 | COVERED | |
| CONTEXT | D-11 | Supplemental OIDF stays redacted/non-certifying | 139-09 | COVERED | |
| CONTEXT | D-12 | Only evidence-proven prose changes | 139-08, 139-09 | COVERED | Only VALIDATION proof mapping changes; history remains intact. |
| CONTEXT | Deferred | Dependency/PR/ref cleanup, final dated baseline, OIDF remediation, new product/runtime surface | none | EXCLUDED | Reserved for Phases 140-141 or a later bounded milestone. |

The deterministic API detector reports `none_declared: true`; this plan preserves the accepted no-new-external-integration declaration and does not create an API coverage matrix. No enumerated ORM schema path is modified, so the schema gate does not apply.

<verification>
  <automated>mix test test/lockspire/workflow_supply_chain_contract_test.exs &amp;&amp; LOCKSPIRE_GSD_HOST_FIXTURE=tools/gsd-capabilities/lockspire-phase-finalizer/fixtures/gsd-host-contract.json node --test tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-command-router.test.cjs tools/gsd-capabilities/lockspire-phase-finalizer/lockspire-finalize-lifecycle.test.cjs &amp;&amp; bash scripts/ci/lint_workflows.sh &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/139-required-truth-reconciliation</automated>
  <fails_when>any command exits nonzero, ExUnit or Node runs zero tests, supported lifecycle points or gap mappings are incomplete, CI lifecycle presence/uniqueness/order or protected job/permission identity drifts, CI downloads a runtime/uses credentials, finalization can mutate before authentication or after cancellation, or the API detector declaration becomes invalid</fails_when>
</verification>

<success_criteria>
- G-139-01 and G-139-06 through G-139-10 are exercised through the supported current lifecycle, and G-139-11 is complete across both planning-state and router-cancellation halves.
- Required CI runs the portable lifecycle risk once without duplicating authenticated live evidence or installing GSD, and an executable source contract pins the step order plus protected job and permission identity.
- The automatic terminal lifecycle blocks Phase 140 until exact main, exact CI/Release no-publish, local gates, inventory relation, and the durable receipt agree.
- All nine requirements, D-01 through D-12, sixteen edge assumptions, eleven gap IDs, and nineteen prohibitions remain accounted for with no deferred-scope leakage.
</success_criteria>

<output>
Create `.planning/phases/139-required-truth-reconciliation/139-09-SUMMARY.md`. Leave the tracked Phase 139 finalizer capability active so normal verification/completion and the blocking Phase 140 plan:pre gate produce and validate the final receipt without a manual command.
</output>
