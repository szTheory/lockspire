---
phase: 139-required-truth-reconciliation
plan: "05"
type: execute
wave: 3
depends_on: ["139-02"]
files_modified:
  - scripts/maintainer/baseline_inventory.sh
  - scripts/maintainer/finalize_phase_138_inventory.sh
  - test/support/lockspire/release_proof/package_assertions.ex
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - .planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md
autonomous: true
requirements: [HYGIENE-05, HYGIENE-06, TRUTH-03]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-02: the current refresh_required Phase 138 relation is never treated as acceptance authority; execute:post performs two agreeing live observations and publishes one new ledger-only replacement before the normal Phase 139 verifier."
    - "The Phase 139 pre-verify ledger is the direct child of its recorded clean evidence base, changes only the canonical ledger, retains proposal-only dispositions, and immediately returns the exact current relation verdict."
    - "The Phase 139 post-transition relation accepts only the ledger commit followed by exact Phase 139 verification, phase-completion, and transition bookkeeping bound to the host-sealed receipt."
    - "A final fast-forward of local main and origin/main is authorized only when both old refs are exact ancestors of the sealed Phase 139 candidate and every other ref/worktree/commit identity remains unchanged."
    - "HYGIENE-05: refresh_required, incomplete source receipts, unequal observations, an invalid ledger-only commit, or an unauthorized later commit/ref remains a nonzero BLOCK at each authority boundary."
    - "HYGIENE-06 D-07 D-09: the new relation modes extend the existing collector/finalizer and focused ExUnit proof; they add no second currentness implementation or product/runtime surface."
    - "TRUTH-03: Phase 139 closeout validation requires exact plan-summary, REVIEW, VERIFICATION, ROADMAP, STATE, PROJECT, and REQUIREMENTS lifecycle shapes rather than keyword-based authorization."
  prohibitions:
    - statement: "A stale or mismatched Phase 138 ledger must not be relabeled current, hand-edited in place, appended with acceptance claims, or used to authorize inventory dispositions."
      verification: flagged-unverified
    - statement: "Ledger refresh must not amend, force-push, rewrite earlier ledger commits, change more than the canonical ledger path, or execute any proposal from the inventory."
      verification: flagged-unverified
    - statement: "Post-transition verification must not infer authority from commit subjects alone, allow unrelated ref movement, mutate the ledger, or clear the host-owned lifecycle receipt."
      verification: flagged-unverified
  artifacts:
    - path: scripts/maintainer/baseline_inventory.sh
      provides: "Phase 139 pre-verifier and post-transition currentness relations with exact lifecycle and synchronized-main advancement validation."
      contains: "verify_phase_139_posttransition_relation"
    - path: scripts/maintainer/finalize_phase_138_inventory.sh
      provides: "Backward-compatible Phase 138 finalization plus Phase 139 pre-verifier double-collection ledger replacement."
      contains: "docs(phase-139): refresh baseline inventory before verification"
    - path: test/support/lockspire/release_proof/package_assertions.ex
      provides: "Real-repository Phase 139 ledger publication, lifecycle, ref-advance, forgery, and race fixtures."
      exports: ["assert_phase_139_inventory_relation!/0", "assert_phase_139_preverify_refresh!/0"]
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "Focused Phase 139 inventory relation and pre-verify refresh entry points."
      contains: "phase139_inventory_relation"
    - path: .planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md
      provides: "Conditional immutable ledger-only replacement created by the execute:post hook from two agreeing complete observations."
      contains: "## Post-snapshot currentness relation"
  key_links:
    - from: scripts/maintainer/finalize_phase_138_inventory.sh
      to: .planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md
      via: "two complete normalized-equal observations, atomic replacement, one-path commit, and immediate preverify relation proof"
      pattern: "phase-139|candidate-one|candidate-two"
    - from: scripts/maintainer/baseline_inventory.sh
      to: "Phase 139 GSD lifecycle commits and host receipt"
      via: "exact positive validators bind commit identity, subject, path cardinality, bounded metadata, before/after bytes, and allowed ref movement"
      pattern: "validate_phase_139|verify_phase_139_posttransition_relation"
    - from: test/support/lockspire/release_proof/package_assertions.ex
      to: scripts/maintainer/baseline_inventory.sh
      via: "isolated Git repositories exercise truthful and adversarial pre/post lifecycle histories"
      pattern: "assert_phase_139_inventory_relation!"
---

<objective>
Re-establish the immutable Phase 138 inventory as current Phase 139 input before verification and prove its exact relation through final GSD transition and synchronized-main advancement.

Purpose: D-02 forbids treating today's observed refresh_required result as current; the existing two-hook lifecycle provides the safe point to replace the ledger once and validate later bookkeeping without self-reference.
Output: Phase-aware currentness validators, a backward-compatible pre-verifier finalizer, focused hostile-history fixtures, and one conditional ledger-only replacement at execute:post.

Decision coverage: D-02 governs fail-closed currentness; D-07 permits the demonstrated deterministic extension; D-09 keeps it in existing shell and focused ExUnit; D-10 keeps Phase 139 lifecycle truth exact without altering shipped history.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/139-required-truth-reconciliation/139-CONTEXT.md
@.planning/phases/139-required-truth-reconciliation/139-RESEARCH.md
@.planning/phases/139-required-truth-reconciliation/139-PATTERNS.md
@.planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md
@scripts/maintainer/baseline_inventory.sh
@scripts/maintainer/finalize_phase_138_inventory.sh
@test/support/lockspire/release_proof/package_assertions.ex
@test/lockspire/release/repository_hygiene_contract_test.exs
@/Users/jon/.codex/gsd-core/bin/lib/post-completion-finalizer-state.cjs
@/Users/jon/.codex/gsd-core/workflows/execute-phase.md
@/Users/jon/.codex/gsd-core/workflows/transition.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bind Phase 139 lifecycle and synchronized-main movement to the immutable ledger</name>
  <reversibility rating="costly">Changing an accepted ledger relation later requires another explicit immutable replacement and coordinated validator update, but no history rewrite or data migration.</reversibility>
  <files>scripts/maintainer/baseline_inventory.sh, test/support/lockspire/release_proof/package_assertions.ex, test/lockspire/release/repository_hygiene_contract_test.exs</files>
  <read_first>
    - scripts/maintainer/baseline_inventory.sh — reuse resolve_snapshot_ledger_commit, lifecycle class dispatch, Phase 138 pre/post relation proof, external receipt checks, ref topology, and redaction boundaries
    - .planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md — preserve canonical schema, proposal-only dispositions, evidence-base relation, and prescribed replacement contract
    - test/support/lockspire/release_proof/package_assertions.ex — reuse real-repository commit builders, host receipt fixtures, semantic phase labels, and hostile topology cases
    - test/lockspire/release/repository_hygiene_contract_test.exs — add separate named Phase 139 relation entry points under bounded timeout tags
    - /Users/jon/.codex/gsd-core/bin/lib/post-completion-finalizer-state.cjs — bind to the host's schemaVersion 1 phase, hooks, writer, transformation, before/after identities, and status
  </read_first>
  <behavior>
    - RED: today's production snapshot-relation command returns nonzero refresh_required for the Phase 139 planning commits.
    - GREEN: one fresh ledger-only Phase 139 publication is current at pre-verify HEAD and does not retroactively authorize the superseded ledger.
    - GREEN: post-transition accepts only exact passed verification, phase completion, and transition commits whose bounded contents agree with the host receipt.
    - GREEN: advancing local main and origin/main from exact ancestors to the sealed final candidate is accepted only as one synchronized pair; non-fast-forward, one-sided, extra-ref, tag, worktree, or branch movement is rejected.
    - GREEN: missing/malformed/forged host fields, wrong phase/status/hooks/writer/transformation, dirty worktree, extra commit/path, or changed ledger bytes returns nonzero refresh_required.
  </behavior>
  <action>Create fail-first real-repository fixtures for a Phase 139 pre-verifier ledger and its exact verifier/completion/transition chain, including the host's sealed pending lifecycle receipt and a final synchronized main/origin-main ref advance. Extend `baseline_inventory.sh` with `validate_phase_139_plan_closeout_commit`, `validate_phase_139_verification_commit`, `validate_phase_139_completion_commit`, `validate_phase_139_transition_commit`, `validate_phase_139_main_advance`, `verify_phase_139_preverify_relation`, and `verify_phase_139_posttransition_relation`. Add CLI modes `--verify-phase-139-preverify-relation LEDGER` and `--verify-phase-139-posttransition-relation LEDGER`. Reuse existing bounded regular-file, canonical-frontmatter, author/subject/path-cardinality, before/after identity, receipt-hash, redaction, and exact-ref helpers. Permit the final main/origin-main pair to move only from captured exact ancestors to the host-sealed current candidate, with no force semantics and no other topology drift. Preserve all Phase 138 relation modes byte-for-behavior.</action>
  <acceptance_criteria>
    - Positive fixtures prove current pre-verify publication and exact post-transition lifecycle plus synchronized-main advancement.
    - Every hostile commit, receipt, path, ref, worktree, identity, content, status, ordering, and one-sided-advance fixture returns nonzero.
    - Legacy Phase 138 relation tests remain green and the verifier performs no mutation.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_inventory_relation &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_preverify_relation_gap --only phase138_posttransition_relation_gap</automated>
    <fails_when>shell syntax fails, either Mix invocation exits nonzero or runs zero tests, a hostile history/ref/receipt returns success, a valid synchronized fast-forward is rejected, a legacy Phase 138 relation regresses, or verification mutates Git state</fails_when>
  </verify>
  <done>The production relation can distinguish a truthful Phase 139 ledger/lifecycle/main advance from every stale, forged, partial, or unrelated variant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh the ledger once at the Phase 139 pre-verifier boundary</name>
  <reversibility rating="costly">The ledger-only replacement becomes immutable evidence; correction requires a later replacement commit rather than amendment or deletion.</reversibility>
  <files>scripts/maintainer/finalize_phase_138_inventory.sh, test/support/lockspire/release_proof/package_assertions.ex, test/lockspire/release/repository_hygiene_contract_test.exs, .planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md</files>
  <read_first>
    - scripts/maintainer/finalize_phase_138_inventory.sh — preserve Phase 138's primary-checkout, lock, signal, cleanup, double-observation, atomic move, one-path commit, and retry behavior
    - scripts/maintainer/baseline_inventory.sh — call Task 1's phase-specific preverify relation and retain production collection/redaction semantics
    - .planning/STATE.md — require exact Phase 139 verifying state at execute:post without treating later phase state as authority
    - test/support/lockspire/release_proof/package_assertions.ex — extend existing Phase 138 finalizer fixtures instead of creating a second harness
    - /Users/jon/.codex/gsd-core/workflows/execute-phase.md — rely on fresh execute:post rendering after all plan summaries and code review, before normal verification
  </read_first>
  <behavior>
    - Phase 138 pre-verify/post-transition invocations retain their existing fixed subjects, modes, and relation behavior.
    - Phase 139 pre-verify requires current_phase 139 and status verifying in the primary checkout after every plan summary and code-review artifact exists.
    - Two independently allocated complete collections agree after only authorized timestamp normalization before one ledger-only commit with subject `docs(phase-139): refresh baseline inventory before verification`.
    - The finalizer immediately proves Task 1's Phase 139 preverify relation and returns nonzero on incomplete sources, observation disagreement, wrong state, dirty index, symlink, lock conflict, or extra staged path.
    - Retry at the already-current fixed-subject ledger verifies and returns without a second publication.
  </behavior>
  <action>Generalize the existing finalizer's exact argv/state dispatch so `pre-verify --phase 139` performs the same primary-checkout, lock-stable, signal-safe double collection used by Phase 138 while selecting the Phase 139 fixed subject and Task 1 relation mode. Keep all Phase 138 mode/phase combinations unchanged. Require the canonical ledger to be a non-symlink regular target, normalize only the existing declared timestamps, compare the full normalized candidates, stage exactly one path, commit without amend, and run `--verify-phase-139-preverify-relation` immediately. On retry, recognize only the exact Phase 139 subject plus a green relation. Extend existing finalizer fixtures for source disagreement, partial/unavailable receipts, dirty index, target substitution, interruption, duplicate publication, and exact idempotence. The actual canonical replacement is intentionally deferred to the execute:post hook installed by Plan 07, after all task commits/summaries and code review.</action>
  <acceptance_criteria>
    - Hermetic execution creates exactly one valid ledger-only commit and a second invocation creates none.
    - Every incomplete, unstable, wrong-state, dirty, symlink, lock, and multi-path fixture fails before authority is returned.
    - The real execute:post invocation is the only Phase 139 step permitted to replace the canonical ledger.
  </acceptance_criteria>
  <verify>
    <automated>bash -n scripts/maintainer/finalize_phase_138_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase139_preverify_refresh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs --only phase138_finalizer_gap</automated>
    <fails_when>shell syntax fails, either Mix invocation exits nonzero or runs zero tests, publication count differs from one, retry republishes, incomplete/unequal evidence is accepted, the commit changes another path, or Phase 138 behavior regresses</fails_when>
  </verify>
  <done>The existing finalizer can publish one current Phase 139 ledger-only replacement at execute:post and prove it before normal verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mutable repository -> immutable ledger | Two live complete observations must agree before one-path publication. |
| Ledger commit -> lifecycle currentness | Later commits and refs are untrusted until exact positive semantic validators authorize them. |
| Host pending receipt -> post-transition authority | Phase, hooks, writer, transformation, identities, and current bytes must all agree. |
| Ref advancement -> synchronized main | Only exact fast-forward local/remote main movement to the sealed candidate is permitted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-139-19 | Spoofing | stale inventory currentness | high | mitigate | Require new ledger-only publication from two agreeing complete observations and immediate preverify proof. |
| T-139-20 | Tampering | ledger/history | critical | mitigate | Lock target, require regular file, stage one exact path, prohibit amend/force, and preserve prior commits immutably. |
| T-139-21 | Repudiation | lifecycle authorization | high | mitigate | Validate exact subjects, authors, paths, bounded metadata, receipt hashes, and before/after identities. |
| T-139-22 | Information disclosure | recollected evidence | high | mitigate | Reuse Phase 138 allowlists/redaction and require two normalized outputs to agree. |
| T-139-23 | Denial of service | concurrent finalizer | medium | mitigate | Git-common-dir lock, bounded children, signal cleanup, and idempotent retry. |
| T-139-24 | Elevation of privilege | main/ref authorization | high | mitigate | Allow only paired fast-forward main/origin-main movement to the sealed candidate; reject every other topology change. |
</threat_model>

## Artifacts this phase produces

- `scripts/maintainer/baseline_inventory.sh`: CLI modes `--verify-phase-139-preverify-relation` and `--verify-phase-139-posttransition-relation`; helpers `validate_phase_139_plan_closeout_commit`, `validate_phase_139_verification_commit`, `validate_phase_139_completion_commit`, `validate_phase_139_transition_commit`, `validate_phase_139_main_advance`, `verify_phase_139_preverify_relation`, and `verify_phase_139_posttransition_relation`.
- `scripts/maintainer/finalize_phase_138_inventory.sh`: Phase 139 pre-verify branch, fixed subject `docs(phase-139): refresh baseline inventory before verification`, and phase-aware relation dispatch while preserving Phase 138 modes.
- `test/support/lockspire/release_proof/package_assertions.ex`: `assert_phase_139_inventory_relation!/0`, `assert_phase_139_preverify_refresh!/0`, and real-repository hostile lifecycle/ref fixtures.
- `test/lockspire/release/repository_hygiene_contract_test.exs`: tags `phase139_inventory_relation` and `phase139_preverify_refresh` with focused entry points.
- `.planning/phases/138-baseline-inventory-evidence-taxonomy/baseline-inventory-2026-08-28.md`: one conditional immutable ledger-only replacement created at execute:post, never hand-edited by a task.

<verification>
  <automated>bash -n scripts/maintainer/baseline_inventory.sh &amp;&amp; bash -n scripts/maintainer/finalize_phase_138_inventory.sh &amp;&amp; mix test test/lockspire/release/repository_hygiene_contract_test.exs</automated>
  <fails_when>shell syntax fails, Mix exits nonzero or runs zero tests, currentness accepts stale/forged/partial evidence, Phase 139 publication is not one-path/idempotent, synchronized-main validation is fail-open, or any Phase 138 contract regresses</fails_when>
</verification>

<success_criteria>
- refresh_required is resolved only through a fresh immutable ledger-only replacement.
- Pre-verifier and post-transition boundaries each have exact, non-mutating relation proof.
- Final synchronized-main movement is authorized narrowly without broad ref exceptions.
- The canonical ledger remains proposal-only and historical ledger commits remain immutable.
</success_criteria>

<output>
Create `.planning/phases/139-required-truth-reconciliation/139-05-SUMMARY.md`; the live ledger replacement itself occurs later through Plan 07's freshly installed execute:post hook.
</output>
