---
phase: 139-required-truth-reconciliation
plan: "08"
type: execute
wave: 6
depends_on: ["139-07"]
files_modified:
  - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
  - test/lockspire/release/repository_hygiene_contract_test.exs
  - test/lockspire/quality/proof_quality_baseline_test.exs
  - test/lockspire/quality/phase_139_planning_consistency_test.exs
autonomous: true
gap_closure: true
gap_ids: [G-139-02, G-139-03, G-139-04, G-139-05, G-139-11]
requirements: [CI-06, QUAL-05, HYGIENE-05, HYGIENE-06, TRUTH-03]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "G-139-02 / CI-06 / HYGIENE-05: every ambiguous, stale, malformed, moving, incomplete, and undispositioned exact-SHA fixture has bounded executable proof and fails closed per D-01, D-03, D-07, and D-08."
    - "G-139-03 and G-139-04 / QUAL-05: the semantic phase-label assertion and the canonical mix ci lane pass with the proof-quality rules, source inventory, and zero-test rejection intact per D-07 and D-09."
    - "G-139-05 / TRUTH-03: ROADMAP, REQUIREMENTS, and STATE expose the same current Phase 139 gap-closure posture while v1.37 / Lockspire 1.5.0 remains the latest shipped state per D-10 and D-12."
    - "G-139-11 / TRUTH-03: exact completion-state cardinality rejects duplicate Progress, prefix-valid Phase, and other contradictory valid-shaped rows."
    - "The 8 truths already verified by 139-VERIFICATION.md remain protected by their existing release graph, supply-chain, historical-release, conformance, redaction, and boundary contracts."
    - "All 19 descriptor-less prohibitions from completed Plans 139-01 through 139-07 are mapped to named executable enforcement without editing those completed plans or inventing unsupported coverage."
  artifacts:
    - path: .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
      provides: "Stable G-139-01 through G-139-11 truth-order map plus exact prohibition-to-test enforcement map."
      contains: "G-139-11"
    - path: test/lockspire/release/repository_hygiene_contract_test.exs
      provides: "One focused tag selecting the existing exact-SHA, completion-state, pre-authentication, and receipt adversaries without duplicating their implementations."
      contains: "phase139_gap_closure"
    - path: test/lockspire/quality/proof_quality_baseline_test.exs
      provides: "Focused semantic-label gap selection through the existing proof-quality assertion."
      contains: "phase139_gap_closure"
    - path: test/lockspire/quality/phase_139_planning_consistency_test.exs
      provides: "Lifecycle-aware planning consistency and gap/prohibition coverage assertions."
      contains: "G-139-01"
  key_links:
    - from: .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
      to: test/lockspire/release/repository_hygiene_contract_test.exs
      via: "each failed or behavior-unverified truth names an existing focused test entry point instead of relying on historical prose"
      pattern: "G-139-(0[1-9]|1[01])"
    - from: test/lockspire/quality/phase_139_planning_consistency_test.exs
      to: ".planning/ROADMAP.md, .planning/REQUIREMENTS.md, and .planning/STATE.md"
      via: "derived plan/summary counts and verification status distinguish planned, executed, verified, and shipped truth"
      pattern: "139-.*-(PLAN|SUMMARY)"
    - from: test/lockspire/quality/phase_139_planning_consistency_test.exs
      to: .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
      via: "the source contract requires exactly eleven stable gap IDs and exactly nineteen completed-plan prohibition mappings"
      pattern: "G-139-01|G-139-11|prohibition"
---

<objective>
Turn the canonical Phase 139 verification failures into one deterministic, focused repository proof surface without reimplementing repairs that are already committed.

Purpose: The original verifier observed ten failed truths and one behavior-unverified truth. Later repository commits repaired the code defects, but the canonical report remains failed until those repairs have stable, gap-addressable proof and coherent lifecycle status.
Output: Stable truth-order gap IDs, focused test selection, a checked prohibition map, and lifecycle-aware planning consistency proof.

Gap closure: G-139-02, G-139-03, G-139-04, G-139-05, and the completion-classifier half of G-139-11. G-139-01 and G-139-06 through G-139-10 remain assigned to Plan 139-09; router cancellation under G-139-11 is also assigned there.

Decision coverage: D-01, D-03, D-07, D-08, D-09, D-10, and D-12.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/139-required-truth-reconciliation/139-CONTEXT.md
@.planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md
@.planning/phases/139-required-truth-reconciliation/139-VALIDATION.md
@.planning/phases/139-required-truth-reconciliation/139-REVIEW.md
@test/lockspire/release/repository_hygiene_contract_test.exs
@test/lockspire/quality/proof_quality_baseline_test.exs
@test/lockspire/quality/phase_139_planning_consistency_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Select one end-to-end repository proof path for every non-host gap</name>
  <reversibility rating="reversible">Test tags and validation mappings are local proof metadata over existing behavior and can be changed without touching product or release state.</reversibility>
  <files>.planning/phases/139-required-truth-reconciliation/139-VALIDATION.md, test/lockspire/release/repository_hygiene_contract_test.exs, test/lockspire/quality/proof_quality_baseline_test.exs, test/lockspire/quality/phase_139_planning_consistency_test.exs</files>
  <read_first>
    - .planning/phases/139-required-truth-reconciliation/139-VERIFICATION.md — take the eleven failed/unverified observable properties in report truth order; preserve the eight verified properties
    - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md — retain fresh repaired-command evidence and the explicit external final-acceptance boundary
    - test/lockspire/release/repository_hygiene_contract_test.exs — reuse the existing Phase 139 exact-SHA, inventory relation, final acceptance, receipt, and gate-repair entries; do not add duplicate test bodies
    - test/lockspire/quality/proof_quality_baseline_test.exs — tag the existing active phase-label assertion instead of weakening its source scan
    - test/lockspire/quality/phase_139_planning_consistency_test.exs — replace fixed 7/7 assumptions with derived plan/summary and verification-state assertions
  </read_first>
  <behavior>
    - G-139-02: the exact-SHA fail-closed matrix runs to completion under its repository timeout and every hostile fixture returns a bounded redacted BLOCK.
    - G-139-03 and G-139-04: the existing semantic-label proof remains empty and the same source assertion stays inside mix ci.
    - G-139-05: before re-verification, requirements remain pending, ROADMAP distinguishes seven completed original plans from two pending gap plans, and STATE reports Phase 139 gap execution without claiming release completion.
    - G-139-11: the existing completion-state adversaries include duplicate valid-shaped Progress and prefix-valid Phase rows and both remain rejected.
    - One `phase139_gap_closure` selector runs the existing tests across the three ExUnit files without copying helper execution into new tests.
  </behavior>
  <action>In 139-VALIDATION.md, assign G-139-01 through G-139-11 exactly once in observable-truth order from 139-VERIFICATION.md and record the current named automated owner for each property. Add the `phase139_gap_closure` tag only to existing tests that directly prove G-139-02 through G-139-05 and the completion-classifier portion of G-139-11. Update the planning-consistency test to derive the current plan count from tracked `139-??-PLAN.md` files and completed count from matching summaries, then assert the allowed pre-verification posture: original summaries stay complete, the new gap plans remain pending until executed, Phase 139 requirements do not become complete while canonical verification is gaps_found, and shipped 1.5.0 truth stays distinct from active maintenance planning. Keep the exact-SHA helper matrix, QualityBaseline scan, and completion validators unchanged; this task exposes their proof under one selector rather than creating a parallel implementation. Per D-07 and D-09, add no product API, new maintainer command, package, external call, or broad cleanup.</action>
  <acceptance_criteria>
    - The validation map contains G-139-01 through G-139-11 once each and labels G-139-02 as behavior-unverified in the source report while assigning executable closure proof.
    - The focused selector executes nonzero tests from all three named test modules and includes exact-SHA hostile evidence, semantic-label proof, and lifecycle-aware planning truth.
    - The planning consistency contract accepts the legitimate 7-of-9 gap-planning state and rejects premature requirement completion, stale fixed plan counts, and a false shipped-v1.38 claim.
    - The eight verified truths and all existing helper assertions remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs test/lockspire/quality/proof_quality_baseline_test.exs test/lockspire/quality/phase_139_planning_consistency_test.exs --only phase139_gap_closure</automated>
    <fails_when>Mix exits nonzero or runs zero tests, any hostile exact-SHA fixture passes, semantic-label findings are nonempty, planning records claim completion while verification is gaps_found, or a pre-existing Phase 139 assertion is copied or removed</fails_when>
  </verify>
  <done>The non-host verification failures have one bounded, named, end-to-end proof command and current planning truth remains honest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind all nineteen completed-plan prohibitions to executable enforcement</name>
  <reversibility rating="reversible">The enforcement index is maintained proof metadata and changes no lifecycle, repository ref, release, or public contract.</reversibility>
  <files>.planning/phases/139-required-truth-reconciliation/139-VALIDATION.md, test/lockspire/quality/phase_139_planning_consistency_test.exs</files>
  <read_first>
    - .planning/phases/139-required-truth-reconciliation/139-01-PLAN.md — extract its three descriptor-less prohibition statements as data
    - .planning/phases/139-required-truth-reconciliation/139-02-PLAN.md — extract its two descriptor-less prohibition statements as data
    - .planning/phases/139-required-truth-reconciliation/139-03-PLAN.md — extract its two descriptor-less prohibition statements as data
    - .planning/phases/139-required-truth-reconciliation/139-04-PLAN.md — extract its three descriptor-less prohibition statements as data
    - .planning/phases/139-required-truth-reconciliation/139-05-PLAN.md — extract its three descriptor-less prohibition statements as data
    - .planning/phases/139-required-truth-reconciliation/139-06-PLAN.md — extract its three descriptor-less prohibition statements as data
    - .planning/phases/139-required-truth-reconciliation/139-07-PLAN.md — extract its three descriptor-less prohibition statements as data
    - .planning/phases/139-required-truth-reconciliation/139-VALIDATION.md — preserve its repaired-blocker evidence and map each statement to the narrowest named source/process test
  </read_first>
  <behavior>
    - Exactly 19 prohibition statements are discovered from completed Plans 139-01 through 139-07; completed plan files are not edited.
    - Each statement maps to at least one named automated test, source assertion, or fail-closed process fixture that enforces the prohibited outcome, and the completed table contains zero unresolved rows.
    - Pre-authentication mutation, contradictory completion state, router-only cancellation, accidental publication, historical rewrite, supplemental-certification promotion, secret-bearing receipt, tracked receipt, destructive cleanup, and later-phase authority leakage all have explicit owners.
    - Any statement without an existing executable owner receives a narrow assertion in the planning-consistency contract before its row can be completed; the test rejects missing, prose-only, or `flagged-unverified` dispositions.
  </behavior>
  <action>Add a nineteen-row prohibition enforcement table to 139-VALIDATION.md. Preserve each completed-plan statement verbatim as quoted source data and name the exact tracked test file plus test/helper entry point that enforces it. Resolve every row during this task: when no current owner exists, add the narrowest deterministic assertion to `phase_139_planning_consistency_test.exs` against tracked source or process-fixture data, then cite that assertion as the owner. Extend the planning-consistency test to parse the seven completed PLAN files, count exactly nineteen prohibition statements, require a one-to-one validation row for each, verify every owner path is git-tracked and every named entry point exists, require the completed table to report zero unresolved or `flagged-unverified` rows, and reject classifications backed only by prose. Do not edit 139-01 through 139-07, add a human checkpoint, or assign an owner to a Plan 139-09 test that does not exist yet; statements whose full lifecycle behavior belongs to Plan 139-09 must still receive a concrete, already executable boundary assertion in this task.</action>
  <acceptance_criteria>
    - The source contract discovers 3+2+2+3+3+3+3 prohibition statements and matches all nineteen to distinct validation rows.
    - All nineteen mappings resolve to tracked files and concrete test/helper symbols; the machine-checked unresolved count is exactly zero and no row retains a `flagged-unverified` disposition.
    - No completed plan, canonical verification result, historical artifact, or deferred item is rewritten.
    - The validation artifact distinguishes already executed proof from the final live lifecycle evidence still pending Plan 139-09.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/quality/phase_139_planning_consistency_test.exs</automated>
    <fails_when>Mix exits nonzero or runs zero tests, the prohibition count differs from nineteen, any completed-plan statement lacks a mapping, any owner is untracked or missing, any row is unresolved or flagged-unverified, or prose-only coverage is accepted</fails_when>
  </verify>
  <done>Every one of the nineteen must-NOT statements has an auditable executable owner, the enforced unresolved count is zero, and no completed plan or human verification handoff is introduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical verification -> gap map | Failed, verified, and behavior-unverified truth statuses must retain their report order and meaning. |
| Completed PLAN prose -> prohibition enforcement | Descriptor-less prohibition statements are untrusted source data until matched to tracked executable owners. |
| Planning files -> completion claim | ROADMAP, REQUIREMENTS, STATE, summaries, and verification status jointly control whether Phase 139 may be described as complete. |

## STRIDE Threat Register — ASVS L1, block threshold high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-139-G08-01 | Spoofing | stable gap map | high | mitigate | Parse exactly eleven report-ordered properties and require unique G-139-01 through G-139-11 identifiers. |
| T-139-G08-02 | Tampering | prohibition coverage | high | mitigate | Extract all nineteen statements from tracked completed plans, require tracked concrete executable owners, and fail unless unresolved and flagged-unverified counts are both zero. |
| T-139-G08-03 | Repudiation | closure evidence | medium | mitigate | Record named commands and entry points in 139-VALIDATION.md and make the planning-consistency test verify them. |
| T-139-G08-04 | Information disclosure | hostile fixture output | medium | mitigate | Reuse redacted class-only exact-SHA fixtures; expose no raw API body, environment value, or credential. |
| T-139-G08-05 | Denial of service | hostile matrix | low | accept | Existing per-test timeouts and the focused tag bound execution while retaining the complete matrix. |
| T-139-G08-SC | Tampering | package-manager installs | low | accept | No package install is planned; the existing Mix, ExUnit, shell, and Node surfaces are sufficient. |
</threat_model>

<verification>
  <automated>mix test test/lockspire/release/repository_hygiene_contract_test.exs test/lockspire/quality/proof_quality_baseline_test.exs test/lockspire/quality/phase_139_planning_consistency_test.exs --only phase139_gap_closure</automated>
  <fails_when>Mix exits nonzero or runs zero tests, the eleven-gap map or nineteen-prohibition map is incomplete, any prohibition row is unresolved or lacks a tracked executable owner, any hostile proof becomes fail-open, semantic proof labels regress, or planning truth claims verified completion prematurely</fails_when>
</verification>

<success_criteria>
- G-139-02 through G-139-05 and completion classification under G-139-11 have focused executable proof.
- The canonical report's eight verified truths retain their existing contracts.
- All nineteen completed-plan prohibitions have concrete, machine-checked executable owners and zero unresolved dispositions.
- Planning records remain coherent during the two-plan gap closure and do not claim a release or verified completion early.
</success_criteria>

<output>
Create `.planning/phases/139-required-truth-reconciliation/139-08-SUMMARY.md` with the eleven-gap map, focused command result, and prohibition enforcement count.
</output>
