---
phase: 119-weak-page-application-ia-copy-pass
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/web/live/admin/clients_live/show.ex
  - test/lockspire/web/live/admin/clients_live/show_test.exs
autonomous: true
requirements:
  - FLOW-01
  - FLOW-05
must_haves:
  truths:
    - "FLOW-01: client detail presents identity/current status, effective posture, credentials/assertion keys, endpoints/logout, DCR/RAT context, support pivots, and lifecycle/destructive actions as intentional groups per D-04."
    - "Client detail preserves existing patch destinations, mutation events, copy-once secret/RAT states, and route vocabulary per D-02, D-05, and D-06."
    - "Touched client-detail copy stays calm, domain-accurate, consequence-oriented, and redaction-safe per D-14 and D-15."
  artifacts:
    - path: lib/lockspire/web/live/admin/clients_live/show.ex
      provides: "Client detail entity header, panes, support pivots, and lifecycle action grouping"
      contains: "AdminComponents.entity_header"
    - path: test/lockspire/web/live/admin/clients_live/show_test.exs
      provides: "Rendered proof for client detail panes, preserved actions, vocabulary split, and secret redaction"
      contains: "lockspire-admin-entity-header"
  key_links:
    - from: lib/lockspire/web/live/admin/clients_live/show.ex
      to: lib/lockspire/web/components/admin_components.ex
      via: "AdminComponents.entity_header, pane, status_cluster, lifecycle_row, long_value, and action_group per D-01"
      pattern: "AdminComponents\\.(entity_header|pane|status_cluster|lifecycle_row|long_value|action_group)"
    - from: test/lockspire/web/live/admin/clients_live/show_test.exs
      to: lib/lockspire/web/live/admin/clients_live/show.ex
      via: "LiveView rendered assertions and event/patch contract proof"
      pattern: "toggle_client|logout-propagation|post-logout redirect"
---

<objective>
Apply Phase 118 structural primitives to client detail so the Configure route scans by operator job without changing its existing LiveView behavior.

Purpose: Deliver FLOW-01 and the client-detail portion of FLOW-05 while honoring D-01 through D-06, D-14, D-15, and D-16.
Output: Updated client detail LiveView markup and focused rendered tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-weak-page-application-ia-copy-pass/119-CONTEXT.md
@.planning/phases/119-weak-page-application-ia-copy-pass/119-RESEARCH.md
@.planning/phases/119-weak-page-application-ia-copy-pass/119-VALIDATION.md
@.planning/phases/119-weak-page-application-ia-copy-pass/119-PATTERNS.md
@.planning/phases/119-weak-page-application-ia-copy-pass/119-UI-SPEC.md
@.planning/phases/118-primitive-meta-component-upgrade/118-01-SUMMARY.md
@.planning/phases/118-primitive-meta-component-upgrade/118-02-SUMMARY.md
@.planning/phases/118-primitive-meta-component-upgrade/118-03-SUMMARY.md
@lib/lockspire/web/components/admin_components.ex
@lib/lockspire/web/live/admin/clients_live/show.ex
@test/lockspire/web/live/admin/clients_live/show_test.exs
</context>

## Artifacts this phase produces

- `lib/lockspire/web/live/admin/clients_live/show.ex` materially changes `Lockspire.Web.Live.Admin.ClientsLive.Show.render/1` client detail markup only; existing `handle_event/3`, `show_path/2`, modal routing, and mutation helpers remain behavior owners.
- `test/lockspire/web/live/admin/clients_live/show_test.exs` materially changes client detail rendered assertions for pane/group hierarchy, support-pivot copy, route/action preservation, vocabulary split, and redaction.

<tasks>

<task id="119-01-01" type="auto" tdd="true">
  <name>Task 119-01-01: Recompose client detail around operator panes</name>
  <read_first>
    - `.planning/phases/119-weak-page-application-ia-copy-pass/119-CONTEXT.md` decisions D-01 through D-06 and D-14 through D-16.
    - `.planning/phases/119-weak-page-application-ia-copy-pass/119-UI-SPEC.md` section "Client Detail".
    - `.planning/phases/119-weak-page-application-ia-copy-pass/119-PATTERNS.md` client detail and clients show test assignments.
    - `lib/lockspire/web/components/admin_components.ex`.
    - `lib/lockspire/web/live/admin/clients_live/show.ex`.
    - `test/lockspire/web/live/admin/clients_live/show_test.exs`.
  </read_first>
  <files>lib/lockspire/web/live/admin/clients_live/show.ex, test/lockspire/web/live/admin/clients_live/show_test.exs</files>
  <behavior>
    - Client detail renders `lockspire-admin-entity-header` and `lockspire-admin-pane` groups named `Identity and current status`, `Effective posture`, `Credentials and assertion keys`, `Endpoints and logout`, `DCR and RAT context`, `Support pivots`, and `Lifecycle and destructive actions`.
    - Existing patch destinations remain visible for routine edit, redirect URI edit, post-logout redirect URI edit, logout propagation query workflow, PAR policy edit, security profile edit, secret rotation, and RAT rotation.
    - Existing `phx-click="toggle_client"` remains the only enable/disable client event on the detail page.
    - Rendered HTML continues to omit `client_secret_hash`, token plaintext, RAT plaintext outside the existing reveal state, private keys, verifier material, and raw credential material.
  </behavior>
  <action>First extend `test/lockspire/web/live/admin/clients_live/show_test.exs` for the rendered behaviors above, including assertions for `lockspire-admin-entity-header`, `lockspire-admin-pane`, `lockspire-admin-status-cluster`, `lockspire-admin-long-value`, and the required group headings. Then update `Lockspire.Web.Live.Admin.ClientsLive.Show.render/1` in `lib/lockspire/web/live/admin/clients_live/show.ex` to replace the large local detail-section body with `AdminComponents.entity_header` followed by `AdminComponents.pane` groups per D-04. Use `AdminComponents.status_cluster` for current state/posture badges, `AdminComponents.long_value` for client IDs, URLs, handles, and durable pivots, `AdminComponents.action_group` for routine/secondary/credential/destructive actions, and `AdminComponents.lifecycle_row` for enable/disable and rotation consequence context when it improves scanability. Preserve `show_path/2`, `handle_event("save_client", ...)`, `handle_event("rotate_secret", ...)`, `handle_event("rotate_registration_access_token", ...)`, `handle_event("toggle_client", ...)`, `FormComponent.client_form`, `RotateSecretComponent`, current assigns, URL state, modal routing, and mutation semantics per D-02 and D-05. Keep post-logout redirect URIs described as browser destinations and logout propagation URIs described as RP cleanup endpoints per D-06. Add support pivots only to existing route/filter destinations already backed by current route tests; where no stable filter contract exists, render non-mutating review context instead of inventing a route or query param. Do not create new components, router entries, LiveComponents, CSS architecture, storage fields, or OAuth/OIDC behavior.</action>
  <acceptance_criteria>
    - `test/lockspire/web/live/admin/clients_live/show_test.exs` asserts all seven client detail group names and at least one stable class for `entity_header`, `pane`, `status_cluster`, and `long_value`.
    - Client detail rendered tests assert the post-logout redirect URI versus logout propagation URI vocabulary split and preserve the logout propagation query workflow path.
    - Existing client mutation tests still pass for secret rotation, registration access token rotation, and `toggle_client`.
    - Rendered HTML refutes raw sensitive fields already covered by client detail tests, including `client_secret_hash` and raw credential material.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/clients_live/show_test.exs test/lockspire/web/live/admin/design_system_contract_test.exs</automated>
  </verify>
  <done>Client detail satisfies FLOW-01/FLOW-05 without route, event, form, secret, storage, or protocol drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser -> LiveView | Rendered client detail may reveal sensitive client, credential, or protocol material. |
| LiveView markup -> existing Admin APIs | Existing client lifecycle and credential actions must not gain new events or route semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Information Disclosure | `ClientsLive.Show.render/1` | mitigate | Use `long_value` and existing redaction/copy-once patterns; tests refute raw secret/hash/credential material per D-15. |
| T-119-02 | Tampering | client detail actions | mitigate | Preserve `show_path/2` destinations and existing `toggle_client`, rotation, and edit events; tests assert route/event contract per D-05. |
| T-119-03 | Repudiation | endpoint/logout vocabulary | mitigate | Keep post-logout redirect URIs and logout propagation URIs as separate labels and assertions per D-06. |
| T-119-SC | Tampering | package/dependency surface | accept | No package-manager install task exists; Package Legitimacy Audit says no external packages for Phase 119. |
</threat_model>

<verification>
Run the focused command in the task. After both Wave 1 plans complete, run `mix test test/lockspire/web/live/admin/clients_live/show_test.exs test/lockspire/web/live/admin/policies_live/dcr_test.exs test/lockspire/web/live/admin/design_system_contract_test.exs`.
</verification>

<success_criteria>
FLOW-01 is covered for client detail and FLOW-05 copy/redaction expectations are locally proven for this route.
</success_criteria>

<output>
Create `.planning/phases/119-weak-page-application-ia-copy-pass/119-01-SUMMARY.md` when done.
</output>
