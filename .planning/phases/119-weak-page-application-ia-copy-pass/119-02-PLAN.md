---
phase: 119-weak-page-application-ia-copy-pass
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/web/live/admin/policies_live/dcr.html.heex
  - test/lockspire/web/live/admin/policies_live/dcr_test.exs
autonomous: true
requirements:
  - FLOW-02
  - FLOW-05
must_haves:
  truths:
    - "FLOW-02: DCR policy separates gate, allowlist, lifetime, auth-method, and risk/posture decisions while remaining one submitted policy form per D-07 and D-08."
    - "Current `policy[...]` field names, `phx-submit=\"save_policy\"`, `PolicyForm.changeset/2`, and `Admin.put_dcr_policy/1` semantics remain unchanged per D-07 through D-09."
    - "DCR policy copy stays issuer-registration-policy oriented and does not blur DCR policy with IAT/RAT onboarding per D-06, D-08, D-09, and D-14."
  artifacts:
    - path: lib/lockspire/web/live/admin/policies_live/dcr.html.heex
      provides: "Grouped one-form DCR policy workflow"
      contains: "phx-submit=\"save_policy\""
    - path: test/lockspire/web/live/admin/policies_live/dcr_test.exs
      provides: "LiveView form proof for grouped DCR policy and unchanged persistence"
      contains: "form(\"form[phx-submit=save_policy]\""
  key_links:
    - from: lib/lockspire/web/live/admin/policies_live/dcr.html.heex
      to: lib/lockspire/web/live/admin/policies_live/dcr/policy_form.ex
      via: "unchanged `policy[...]` field names consumed by `PolicyForm.changeset/2`"
      pattern: "policy\\[registration_policy\\]|policy\\[dcr_allowed_scopes\\]"
    - from: lib/lockspire/web/live/admin/policies_live/dcr.ex
      to: Lockspire.Admin.put_dcr_policy/1
      via: "existing `handle_event(\"save_policy\", ...)` persistence path"
      pattern: "Admin\\.put_dcr_policy"
---

<objective>
Convert the DCR policy page from a mixed raw field list into one grouped workflow while preserving every form, validation, and persistence contract.

Purpose: Deliver FLOW-02 and the DCR policy portion of FLOW-05 while honoring D-01, D-02, D-06, D-07, D-08, D-09, D-14, D-15, and D-16.
Output: Updated DCR HEEx form grouping and focused DCR LiveView tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/119-weak-page-application-ia-copy-pass/119-CONTEXT.md
@.planning/phases/119-weak-page-application-ia-copy-pass/119-RESEARCH.md
@.planning/phases/119-weak-page-application-ia-copy-pass/119-VALIDATION.md
@.planning/phases/119-weak-page-application-ia-copy-pass/119-PATTERNS.md
@.planning/phases/119-weak-page-application-ia-copy-pass/119-UI-SPEC.md
@.planning/phases/118-primitive-meta-component-upgrade/118-03-SUMMARY.md
@lib/lockspire/web/components/admin_components.ex
@lib/lockspire/web/live/admin/policies_live/dcr.html.heex
@lib/lockspire/web/live/admin/policies_live/dcr.ex
@lib/lockspire/web/live/admin/policies_live/dcr/policy_form.ex
@test/lockspire/web/live/admin/policies_live/dcr_test.exs
</context>

## Artifacts this phase produces

- `lib/lockspire/web/live/admin/policies_live/dcr.html.heex` materially changes DCR policy markup into grouped `workflow_shell`/`form_field` decision sections inside the existing form.
- `test/lockspire/web/live/admin/policies_live/dcr_test.exs` materially changes DCR route assertions for one-form grouping, unchanged field names, unchanged submit behavior, and calm policy copy.

<tasks>

<task id="119-02-01" type="auto" tdd="true">
  <name>Task 119-02-01: Group DCR policy decisions inside the existing form</name>
  <read_first>
    - `.planning/phases/119-weak-page-application-ia-copy-pass/119-CONTEXT.md` decisions D-01, D-02, and D-07 through D-09.
    - `.planning/phases/119-weak-page-application-ia-copy-pass/119-UI-SPEC.md` section "DCR Policy Workflow".
    - `.planning/phases/119-weak-page-application-ia-copy-pass/119-PATTERNS.md` DCR policy assignments.
    - `lib/lockspire/web/components/admin_components.ex`.
    - `lib/lockspire/web/live/admin/policies_live/dcr.html.heex`.
    - `lib/lockspire/web/live/admin/policies_live/dcr.ex`.
    - `lib/lockspire/web/live/admin/policies_live/dcr/policy_form.ex`.
    - `test/lockspire/web/live/admin/policies_live/dcr_test.exs`.
  </read_first>
  <files>lib/lockspire/web/live/admin/policies_live/dcr.html.heex, test/lockspire/web/live/admin/policies_live/dcr_test.exs</files>
  <behavior>
    - `/admin/policies/dcr` renders exactly one policy form with `phx-submit="save_policy"`.
    - The form still renders current names for `policy[registration_policy]`, `policy[dcr_allowed_scopes]`, `policy[dcr_allowed_grant_types]`, `policy[dcr_allowed_response_types]`, `policy[dcr_allowed_redirect_uri_schemes]`, `policy[dcr_allowed_redirect_uri_hosts]`, `policy[dcr_allowed_token_endpoint_auth_methods]`, `policy[dcr_default_client_lifetime_seconds]`, `policy[dcr_default_client_secret_lifetime_seconds]`, and `policy[dcr_default_registration_access_token_lifetime_seconds]`.
    - Rendered workflow groups are `Registration gate`, `Allowlist decisions`, `Lifetime defaults`, `Token endpoint auth methods`, and `Risk and posture`.
    - Submitting the existing form still persists through `PolicyForm.changeset/2` and `Admin.put_dcr_policy/1`.
  </behavior>
  <action>First extend `test/lockspire/web/live/admin/policies_live/dcr_test.exs` with assertions for exactly one `form[phx-submit=save_policy]`, every current `policy[...]` field name, the five group headings, the primary CTA `Save global DCR policy`, and a successful submit that persists registration policy plus at least one array field. Then update `lib/lockspire/web/live/admin/policies_live/dcr.html.heex` only: keep the single outer form and primary CTA, wrap gate, allowlist, lifetime, auth-method, and risk/posture sections in `Lockspire.Web.Components.AdminComponents.workflow_shell`, and replace remaining raw `lockspire-admin-field` wrappers with `Lockspire.Web.Components.AdminComponents.form_field` where practical. Preserve the explicit inputs/selects, element IDs, field names, `PolicyForm.changeset/2`, `Admin.put_dcr_policy/1`, private-key-jwt/client-secret-jwt posture copy, casts, validations, and existing persistence semantics per D-07 and D-08. Do not change `dcr.ex` or `policy_form.ex` unless a test proves a pre-existing mismatch in the read-only grouping copy; do not add registration modes, auth methods, policy values, automatic risk actions, migrations, storage changes, or onboarding/IAT/RAT behavior per D-09.</action>
  <acceptance_criteria>
    - DCR rendered tests prove one `save_policy` form and all current `policy[...]` names still exist.
    - DCR rendered tests assert the five decision group headings and `lockspire-admin-workflow-shell`.
    - Existing submit test using `form("form[phx-submit=save_policy]", %{policy: ...}) |> render_submit()` still persists through `ServerPolicy.get_server_policy()`.
    - The page copy explains disabled, initial access token, open, private-key-jwt, and client-secret-jwt posture without adding new policy options or fear-led wording.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/policies_live/dcr_test.exs test/lockspire/web/live/admin/design_system_contract_test.exs</automated>
  </verify>
  <done>DCR policy satisfies FLOW-02/FLOW-05 while remaining one existing persisted policy form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator browser -> DCR LiveView form | Operator input crosses into policy validation and persistence. |
| DCR form markup -> `PolicyForm.changeset/2` | Field name drift could silently change validation or persistence semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-02 | Tampering | `dcr.html.heex` one-form policy workflow | mitigate | Tests assert one `save_policy` form and unchanged `policy[...]` names before and after grouping. |
| T-119-04 | Information Disclosure | DCR posture copy | mitigate | Keep copy issuer-policy oriented and secret-free; no credential or private-key material is rendered per D-15. |
| T-119-06 | Repudiation | DCR policy versus onboarding vocabulary | mitigate | Group and copy keep DCR policy separate from IAT/RAT onboarding per D-06 and D-09. |
| T-119-SC | Tampering | package/dependency surface | accept | No package-manager install task exists; Package Legitimacy Audit says no external packages for Phase 119. |
</threat_model>

<verification>
Run the focused command in the task. After both Wave 1 plans complete, run `mix test test/lockspire/web/live/admin/clients_live/show_test.exs test/lockspire/web/live/admin/policies_live/dcr_test.exs test/lockspire/web/live/admin/design_system_contract_test.exs`.
</verification>

<success_criteria>
FLOW-02 is covered and the DCR policy page remains behaviorally identical while becoming easier to scan and safer to operate.
</success_criteria>

<output>
Create `.planning/phases/119-weak-page-application-ia-copy-pass/119-02-SUMMARY.md` when done.
</output>
