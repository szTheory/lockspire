---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/discovery.ex
  - test/lockspire/protocol/discovery_test.exs
autonomous: true
requirements:
  - DCR-16
must_haves:
  truths:
    - "Discovery document only advertises registration_endpoint when DCR is not disabled"
    - "A contract test proves that Discovery and HTTP runtime stay aligned in all three DCR modes"
  artifacts:
    - path: "lib/lockspire/protocol/discovery.ex"
      provides: "Conditional endpoint advertisement"
    - path: "test/lockspire/protocol/discovery_test.exs"
      provides: "Alignment contract test"
  key_links:
    - from: "lib/lockspire/protocol/discovery.ex"
      to: "Lockspire.Storage.Ecto.Repository.get_server_policy/0"
      via: "policy evaluation during metadata generation"
---

<objective>
Align OIDC discovery with the runtime DCR policy state. The `registration_endpoint` must only be advertised if the effective `registration_policy` is NOT `:disabled`.

Purpose: Ensure truthful advertising of supported endpoints and prevent discovery/runtime drift.
Output: Updated `Discovery` module and a contract test guaranteeing alignment between discovery output and HTTP responses for `/register`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/lockspire/protocol/discovery.ex
@test/lockspire/protocol/discovery_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Truthful Discovery Advertising and Alignment Contract Test</name>
  <files>lib/lockspire/protocol/discovery.ex, test/lockspire/protocol/discovery_test.exs</files>
  <behavior>
    - Test 1: When ServerPolicy `registration_policy` is `:disabled`, `registration_endpoint` is missing from `openid_configuration/0` and `POST /register` returns 404 (not 403).
    - Test 2: When `registration_policy` is `:initial_access_token`, `registration_endpoint` is present and `POST /register` returns a relevant HTTP status (e.g. 401/400, not 404).
    - Test 3: When `registration_policy` is `:open`, `registration_endpoint` is present and `POST /register` returns a relevant HTTP status (e.g. 400, not 404).
  </behavior>
  <action>
    Modify `Lockspire.Protocol.Discovery.endpoint_metadata_entry/2` (or the equivalent metadata assembly logic) to fetch the server policy via `Lockspire.Storage.Ecto.Repository.get_server_policy/0`. If the key is `"registration_endpoint"` and the policy is `:disabled`, omit it from the discovery response. 
    Then, write a comprehensive contract test in `discovery_test.exs` that sets the three policy modes (`:disabled`, `:initial_access_token`, `:open`) in the database, calling both `Discovery.openid_configuration/0` and making an HTTP request to `POST /register` in each state to assert their alignment.

    **Analog reference for lib/lockspire/protocol/discovery.ex:**
    Follow the existing pattern from `lib/lockspire/protocol/discovery.ex`:
    ```elixir
      defp endpoint_metadata_entry(issuer, path) do
        Enum.find_value(@endpoint_paths, fn {key, route_path} ->
          if route_path == path do
            {key, issuer_url(issuer, route_path)}
          end
        end)
      end
    ```
    Adapt this pattern to query the policy config to check `registration_policy != :disabled` before advertising `registration_endpoint`.

    **Analog reference for test/lockspire/protocol/discovery_test.exs:**
    Follow the existing pattern from `test/lockspire/protocol/discovery_test.exs`:
    ```elixir
      test "published_token_endpoint_auth_methods_supported/0 reflects the static list when /token is mounted" do
        assert Discovery.published_token_endpoint_auth_methods_supported() == @static_methods
      end
    ```
    Write a contract test asserting discovery and runtime stay aligned for `registration_endpoint` across all three modes (`:disabled`, `:initial_access_token`, `:open`).
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/protocol/discovery_test.exs</automated>
  </verify>
  <done>Discovery hides the endpoint when disabled, and the contract test proves 404 runtime alignment across all modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Discovery API | Unauthenticated clients query supported configuration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information Disclosure | `Discovery` | mitigate | Explicitly omit `registration_endpoint` from discovery payloads when the feature is administratively disabled, preventing reconnaissance. |
</threat_model>

<verification>
Run `mix test` to ensure discovery tests pass and no regressions occur.
</verification>

<success_criteria>
- `openid_configuration` accurately reflects the DCR policy state.
- Contract test explicitly asserts 404 alignment.
</success_criteria>

<output>
After completion, create `.planning/phases/29-truthful-discovery-security-docs-and-milestone-closure/29-01-SUMMARY.md`
</output>
