---
phase: 29
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SECURITY.md
  - docs/dynamic-registration.md
  - mix.exs
autonomous: true
requirements:
  - DCR-24
  - DCR-25
  - DCR-26
must_haves:
  truths:
    - "SECURITY.md explicitly calls out unsupported DCR scope and host responsibilities"
    - "A new dynamic registration guide covers operator setup, IAT lifecycle, and partner integration"
    - "The dynamic registration guide is included in hexdocs configuration"
  artifacts:
    - path: "SECURITY.md"
      provides: "Scope documentation and rate-limiting boundaries"
    - path: "docs/dynamic-registration.md"
      provides: "Operator and partner guide"
    - path: "mix.exs"
      provides: "Hexdocs inclusion"
  key_links:
    - from: "mix.exs"
      to: "docs/dynamic-registration.md"
      via: "groups_for_extras routing"
---

<objective>
Update the documentation surface to accurately describe the shipped v1.5 DCR slice.

Purpose: Prevent operator confusion regarding what is supported, explicitly place the burden of rate-limiting DCR endpoints on the host app, and provide comprehensive usage guides.
Output: Updated `SECURITY.md`, a new `docs/dynamic-registration.md`, and an updated `mix.exs`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@SECURITY.md
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SECURITY.md DCR Scope</name>
  <files>SECURITY.md</files>
  <action>
    Update the `SECURITY.md` out-of-scope/unsupported surfaces section. Explicitly state that software statements (RFC 7591 §2.3), external-IdP federation, FAPI bundles, JAR-04 encryption, and `jwks_uri` outbound fetch are out of scope. 
    Crucially, document that Lockspire does NOT provide built-in rate limiting for DCR endpoints, and it is the host application's responsibility to protect these endpoints via Plug.

    **Analog reference for SECURITY.md:**
    Follow the existing pattern from `SECURITY.md` lines 35-42:
    ```markdown
    Unsupported or out-of-scope surfaces include:

    - host-owned account databases
    - host login/session implementations
    - third-party IdP integrations not shipped in this repo
    - hosted auth as a separate service
    - request-object-by-value support, generic external `request_uri` handling, device flow, and dynamic client registration
    - SAML, LDAP, or generic federation features
    ```
    Explicitly list the new out of scope features.
  </action>
  <verify>
    <automated>grep -qi "rate limit" SECURITY.md && grep -qi "software statements" SECURITY.md</automated>
  </verify>
  <done>SECURITY.md boundaries are accurately documented.</done>
</task>

<task type="auto">
  <name>Task 2: Create Dynamic Registration Guide</name>
  <files>docs/dynamic-registration.md, mix.exs</files>
  <action>
    Create `docs/dynamic-registration.md` outlining how to use Dynamic Client Registration. Include sections for: Operator setup (enabling it), IAT lifecycle (minting/distributing tokens), and partner integration shape (the expected payload and endpoints).
    Update `mix.exs` to include `docs/dynamic-registration.md` in both the `extras` list and the `groups_for_extras` (under "Guides").

    **Analog reference for docs/dynamic-registration.md:**
    Follow the existing pattern from `docs/getting-started.md`:
    ```markdown
    # Getting Started

    Lockspire is for Phoenix teams that need to become an OAuth/OIDC provider inside an existing product.
    ```
  </action>
  <verify>
    <automated>MIX_ENV=docs mix docs</automated>
  </verify>
  <done>Documentation is written, successfully compiles with ex_doc, and is included in the project.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator / Integration | Documentation sets expectations for security boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-02 | Denial of Service | `Documentation` | mitigate | Explicitly document in `SECURITY.md` that rate limiting the `/register` endpoint is a host responsibility, mitigating operator false-assumptions of protection. |
</threat_model>

<verification>
Check hexdocs generation to ensure no broken links or missing files.
</verification>

<success_criteria>
- `SECURITY.md` defines exact limits of DCR scope.
- `mix docs` succeeds with `dynamic-registration.md` present.
</success_criteria>

<output>
After completion, create `.planning/phases/29-truthful-discovery-security-docs-and-milestone-closure/29-02-SUMMARY.md`
</output>
