---
phase: 80-sender-constraining-integration
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/lockspire/protocol/protected_resource_dpop.ex
  - lib/lockspire/plug/enforce_sender_constraints.ex
  - test/lockspire/protocol/protected_resource_dpop_test.exs
  - test/lockspire/plug/enforce_sender_constraints_test.exs
autonomous: true
requirements:
  - VAL-BIND-01
  - VAL-BIND-03
  - VAL-DX-03
must_haves:
  truths:
    - Any token whose normalized binding requirements include DPoP is accepted only when the request presents a matching DPoP proof for the actual method, URI, token hash, and key thumbprint.
    - Sender-constraint enforcement remains a soft plug between `VerifyToken` and `RequireToken`.
    - Missing-proof, malformed-proof, replay, `ath`, wrong-scheme, and wrong-key failures are surfaced as typed token errors rather than ad hoc transport responses.
    - Dual-bound tokens keep their MTLS requirement intact after DPoP validation so Plan 03 can require both bindings, not either/or.
  artifacts:
    - path: lib/lockspire/protocol/protected_resource_dpop.ex
      provides: Generic DPoP protected-resource validator that is no longer hard-coded to `/userinfo`
    - path: lib/lockspire/plug/enforce_sender_constraints.ex
      provides: Soft sender-constraint enforcement plug for DPoP-bound access tokens
    - path: test/lockspire/plug/enforce_sender_constraints_test.exs
      provides: Automated negative-path matrix for DPoP plug enforcement
  key_links:
    - from: lib/lockspire/plug/enforce_sender_constraints.ex
      to: lib/lockspire/protocol/protected_resource_dpop.ex
      via: generic request validator
      pattern: "ProtectedResourceDPoP"
    - from: lib/lockspire/protocol/protected_resource_dpop.ex
      to: "request opts dpop_replay_store"
      via: existing replay-store contract used as the Phase 80 replay-cache seam for compatibility
      pattern: "record_dpop_proof"
---

<objective>
Add explicit DPoP sender-constraint enforcement without collapsing Phase 79’s plug boundary model.

Purpose: Phase 80 needs DPoP proof enforcement for high-security tokens, but the user and phase research both lock in a separate `Lockspire.Plug.EnforceSenderConstraints` after `VerifyToken`. This plan generalizes the existing DPoP protocol helper and wires it into that new soft enforcement step.
Output: Generic protected-resource DPoP validator, new `EnforceSenderConstraints` plug, and automated DPoP enforcement coverage built on the existing `record_dpop_proof/1` replay-store contract as the intentionally chosen replay-cache seam.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-sender-constraining-integration/80-CONTEXT.md
@.planning/phases/80-sender-constraining-integration/80-RESEARCH.md
@.planning/phases/80-sender-constraining-integration/80-VALIDATION.md
@.planning/phases/80-sender-constraining-integration/80-PATTERNS.md
@.planning/phases/79-core-validation-plug/79-03-PLAN.md
@lib/lockspire/protocol/protected_resource_dpop.ex
@lib/lockspire/plug/verify_token.ex

<interfaces>
From lib/lockspire/protocol/protected_resource_dpop.ex:
```elixir
@spec validate_userinfo_access(Token.t(), map()) :: {:ok, DPoP.t()} | {:error, Error.t()}
```

Expected new seam:
```elixir
@spec validate_access(map(), map()) :: {:ok, Lockspire.Protocol.DPoP.t()} | {:error, term()}
```

Expected plug order:
```elixir
plug Lockspire.Plug.VerifyToken
plug Lockspire.Plug.EnforceSenderConstraints,
  dpop_replay_store: ReplayStore
plug Lockspire.Plug.RequireToken
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generalize protected-resource DPoP validation</name>
  <files>lib/lockspire/protocol/protected_resource_dpop.ex, test/lockspire/protocol/protected_resource_dpop_test.exs</files>
  <read_first>
    - .planning/phases/80-sender-constraining-integration/80-RESEARCH.md
    - .planning/phases/80-sender-constraining-integration/80-PATTERNS.md
    - lib/lockspire/protocol/protected_resource_dpop.ex
    - test/lockspire/protocol/protected_resource_dpop_test.exs
  </read_first>
  <action>
    Refactor `ProtectedResourceDPoP` so the core validator accepts an arbitrary protected-resource request shape instead of the hard-coded userinfo URI, while preserving `validate_userinfo_access/2` as a thin wrapper for existing callers. Keep the existing `record_dpop_proof/1` replay-store contract exactly as-is per the user instruction and 80-RESEARCH; in this plan, `dpop_replay_store` is the explicit compatibility name for the injected replay-cache implementation rather than a new persistence abstraction. Preserve typed failure reasons for missing proof, malformed proof, replay, `ath` mismatch, proof/key substitution, and scheme downgrade so downstream logs remain actionable.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/protected_resource_dpop_test.exs</automated>
  </verify>
  <acceptance_criteria>
    The generic validator accepts request method, absolute target URI, authorization scheme, raw access token, DPoP proof, and replay-store opts; existing userinfo coverage still passes; and missing-proof, malformed-proof, replay, `ath`, and `jkt` failures retain stable reason codes.
  </acceptance_criteria>
  <done>`ProtectedResourceDPoP` is reusable by both userinfo and the new resource-server plug without duplicating proof-validation logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce `Lockspire.Plug.EnforceSenderConstraints` for DPoP-bound tokens</name>
  <files>lib/lockspire/plug/enforce_sender_constraints.ex, test/lockspire/plug/enforce_sender_constraints_test.exs</files>
  <read_first>
    - .planning/phases/80-sender-constraining-integration/80-CONTEXT.md
    - .planning/phases/80-sender-constraining-integration/80-VALIDATION.md
    - lib/lockspire/plug/verify_token.ex
    - lib/lockspire/protocol/protected_resource_dpop.ex
    - test/lockspire/plug/require_token_test.exs
  </read_first>
  <action>
    Implement a new soft plug `Lockspire.Plug.EnforceSenderConstraints` per Phase 80 Context section 1 and the user instruction. Place it after `VerifyToken`, validate its explicit options with `NimbleOptions`, and make it inspect `conn.assigns.access_token`. For any token whose normalized binding requirements include `dpop_jkt`, call the generalized `ProtectedResourceDPoP` validator using the real request method, absolute URI, authorization scheme, raw access token, DPoP proof header, and the existing `dpop_replay_store` seam. Missing or malformed proofs must become typed sender-constraint failures here so `RequireToken` can later render RFC 9439 challenges. For dual-bound tokens, record DPoP success without clearing the separate MTLS requirement; do not make the token fully usable unless both bindings eventually pass. Do not halt or emit HTTP responses here; instead mutate the assigned `%AccessToken{}` with typed sender-constraint failures and emit differentiated logs/telemetry for replay, downgrade, missing proof, malformed proof, and proof/key substitution.
  </action>
  <verify>
    <automated>mix test test/lockspire/plug/enforce_sender_constraints_test.exs</automated>
  </verify>
  <acceptance_criteria>
    A new plug exists and is executable immediately after `VerifyToken`; any token requiring DPoP passes only when scheme, proof presence, proof shape, `ath`, `jkt`, and replay state all match; dual-bound tokens still carry an unmet MTLS requirement after DPoP-only processing; unconstrained tokens and already-invalid JWTs pass through unchanged; and the plug never sends a response or halts the connection.
  </acceptance_criteria>
  <done>Phase 80 has the locked three-plug pipeline shape for DPoP enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> `ProtectedResourceDPoP` | Untrusted DPoP proof JWT, authorization scheme, method, and target URI are validated against token binding metadata. |
| `EnforceSenderConstraints` -> replay store | Replay enforcement relies on the injected store honoring the existing `record_dpop_proof/1` contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-04 | Replay / Elevation of Privilege | `ProtectedResourceDPoP` | mitigate | Reuse the existing replay key and `record_dpop_proof/1` contract so each proof `jti` + `jkt` + `htm` + `htu` tuple can be accepted only once within the validity window. |
| T-80-05 | Elevation of Privilege | `EnforceSenderConstraints` | mitigate | Require `Authorization: DPoP` whenever `%AccessToken{binding_type}` includes DPoP so sender-constrained tokens cannot be downgraded to bearer presentation. |
| T-80-06 | Spoofing | `ProtectedResourceDPoP` | mitigate | Enforce `cnf["jkt"] == proof.jkt` and `ath == access_token_ath(raw_token)` before treating the token as usable. |
</threat_model>

<verification>
Create the new plug test file first, keep the protocol unit matrix green while refactoring, then prove the plug leaves transport handling to `RequireToken` by asserting non-halting behavior on all failure paths.
</verification>

<success_criteria>
The codebase exposes a reusable DPoP protected-resource validator and a composable sender-constraint plug that transparently enforces DPoP-bound tokens without changing the Phase 79 strict 401 boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/80-sender-constraining-integration/80-02-SUMMARY.md`
</output>
