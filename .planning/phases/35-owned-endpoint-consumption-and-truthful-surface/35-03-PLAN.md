---
phase: 35-owned-endpoint-consumption-and-truthful-surface
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/registration.ex
  - lib/lockspire/protocol/registration_management.ex
  - lib/lockspire/web/registration_json.ex
  - lib/lockspire/web/router.ex
  - lib/lockspire/web/live/admin/policies_live/dpop.ex
  - lib/lockspire/web/live/admin/clients_live/form_component.ex
  - test/lockspire/protocol/registration_test.exs
  - test/lockspire/protocol/registration_management_test.exs
  - test/lockspire/web/registration_json_test.exs
  - test/lockspire/web/live/admin/policies_live/dpop_test.exs
  - test/lockspire/web/live/admin/clients_live_test.exs
autonomous: true
requirements:
  - DPoP-11
must_haves:
  truths:
    - "Operators can set a global DPoP policy from a narrow admin page that mirrors the shipped PAR policy workflow."
    - "Operators can override an individual client between inherit, bearer, and DPoP without repo-internal edits."
    - "Dynamic Client Registration accepts, persists, returns, and updates `dpop_bound_access_tokens` using RFC 9449 semantics."
    - "Self-registered clients that omit or send `false` for `dpop_bound_access_tokens` become explicit bearer clients instead of silently inheriting DPoP."
  artifacts:
    - path: lib/lockspire/web/live/admin/policies_live/dpop.ex
      provides: global DPoP policy admin page with override summary
    - path: lib/lockspire/protocol/registration.ex
      provides: DCR create-path mapping from `dpop_bound_access_tokens` to durable `client.dpop_policy`
    - path: lib/lockspire/protocol/registration_management.ex
      provides: DCR read/update mapping that keeps RFC metadata aligned with durable client policy
    - path: test/lockspire/web/live/admin/clients_live_test.exs
      provides: executable proof for client override workflow within the existing client edit surface
  key_links:
    - from: lib/lockspire/web/live/admin/policies_live/dpop.ex
      to: lib/lockspire/admin/server_policy.ex
      via: narrow global DPoP page persists `server_policy.dpop_policy`
      pattern: "Admin.put_dpop_policy"
    - from: lib/lockspire/web/live/admin/clients_live/form_component.ex
      to: lib/lockspire/admin/clients.ex
      via: existing client edit workflow persists `dpop_policy`
      pattern: 'dpop_policy: params\\["dpop_policy"\\]'
    - from: lib/lockspire/protocol/registration.ex
      to: lib/lockspire/web/registration_json.ex
      via: create/read/update paths round-trip `dpop_bound_access_tokens`
      pattern: "dpop_bound_access_tokens"
---

<objective>
Add explicit operator and DCR configuration for bearer-vs-DPoP client token mode without widening beyond the narrow Lockspire-owned control surfaces.

Purpose: Complete D-13 through D-17 by exposing the already-durable DPoP policy enums through admin and DCR paths so rollout is explicit, reversible, and standards-shaped.
Output: A global DPoP policy page, a client override workflow, and DCR create/read/update mapping for RFC 9449 `dpop_bound_access_tokens`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-CONTEXT.md
@.planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-RESEARCH.md
@.planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-PATTERNS.md
@.planning/phases/34-token-issuance-and-refresh-device-binding/34-CONTEXT.md
@lib/lockspire/admin/clients.ex
@lib/lockspire/admin/server_policy.ex
@lib/lockspire/protocol/registration.ex
@lib/lockspire/protocol/registration_management.ex
@lib/lockspire/web/live/admin/policies_live/par.ex
@lib/lockspire/web/live/admin/clients_live/form_component.ex
@lib/lockspire/web/registration_json.ex
@test/lockspire/protocol/registration_test.exs
@test/lockspire/protocol/registration_management_test.exs
@test/lockspire/web/live/admin/policies_live/par_test.exs
@test/lockspire/web/live/admin/clients_live_test.exs
@test/lockspire/web/registration_json_test.exs

<interfaces>
From lib/lockspire/admin/server_policy.ex:
```elixir
@spec put_dpop_policy(atom() | String.t()) ::
        {:ok, ServerPolicy.t()} | {:error, [error_detail()]} | {:error, term()}
```

From lib/lockspire/admin/clients.ex:
```elixir
@spec update_client(String.t(), map() | keyword()) ::
        {:ok, Client.t()} | {:error, [error_detail()]} | {:error, term()}
```

From lib/lockspire/web/registration_json.ex:
```elixir
def success_response(%Registration.Success{}), do: map()
def read_response(%Client{}), do: map()
def update_response(%RegistrationManagement.UpdateSuccess{}), do: map()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map RFC 9449 `dpop_bound_access_tokens` across DCR create, read, and update</name>
  <files>lib/lockspire/protocol/registration.ex, lib/lockspire/protocol/registration_management.ex, lib/lockspire/web/registration_json.ex, test/lockspire/protocol/registration_test.exs, test/lockspire/protocol/registration_management_test.exs, test/lockspire/web/registration_json_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/registration.ex
    - lib/lockspire/protocol/registration_management.ex
    - lib/lockspire/web/registration_json.ex
    - test/lockspire/protocol/registration_test.exs
    - test/lockspire/protocol/registration_management_test.exs
    - test/lockspire/web/registration_json_test.exs
    - .planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: DCR create with `dpop_bound_access_tokens: true` persists `client.dpop_policy == :dpop` and returns the RFC field as `true`
    - Test 2: DCR create with `false` or omission persists explicit `client.dpop_policy == :bearer` for the self-registered client and returns the RFC field as `false`
    - Test 3: DCR read derives `dpop_bound_access_tokens` from durable client policy instead of generic metadata blobs
    - Test 4: DCR update can switch between `true` and `false`, rotates the RAT as usual, and keeps read/update JSON aligned
  </behavior>
  <action>Extend the DCR protocol and JSON layers per D-13, D-15, D-16, and D-17 using RFC 9449 `dpop_bound_access_tokens` as first-class mapped state. In `lib/lockspire/protocol/registration.ex`, parse the inbound boolean and persist `client.dpop_policy: :dpop` when true, otherwise persist `:bearer` for self-registered clients even when the field is omitted, instead of leaving them on `:inherit` per D-16. In `lib/lockspire/protocol/registration_management.ex`, mirror that mapping on full-replace update and keep the durable policy authoritative on read. In `lib/lockspire/web/registration_json.ex`, derive `dpop_bound_access_tokens` from `client.dpop_policy` on success/read/update responses instead of stashing it in `client.metadata`. Do not move DPoP mode into arbitrary metadata blobs, and do not widen into non-RFC registration fields.</action>
  <acceptance_criteria>
    - `rg -n 'dpop_bound_access_tokens' lib/lockspire/protocol/registration.ex lib/lockspire/protocol/registration_management.ex lib/lockspire/web/registration_json.ex` finds the RFC field across create/read/update
    - `rg -n 'dpop_policy: :dpop|dpop_policy: :bearer' test/lockspire/protocol/registration_test.exs test/lockspire/protocol/registration_management_test.exs` finds durable policy mapping proof
    - `rg -n 'dpop_bound_access_tokens' test/lockspire/web/registration_json_test.exs` finds response-shape proof
    - `MIX_ENV=test mix test test/lockspire/protocol/registration_test.exs test/lockspire/protocol/registration_management_test.exs test/lockspire/web/registration_json_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test.setup && MIX_ENV=test mix test test/lockspire/protocol/registration_test.exs test/lockspire/protocol/registration_management_test.exs test/lockspire/web/registration_json_test.exs -x</automated>
  </verify>
  <done>DCR create/read/update now round-trip RFC 9449 `dpop_bound_access_tokens` through durable `client.dpop_policy`, and self-registered clients become explicit bearer or DPoP clients without hidden inheritance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror PAR admin patterns for global and client DPoP policy UX</name>
  <files>lib/lockspire/web/router.ex, lib/lockspire/web/live/admin/policies_live/dpop.ex, lib/lockspire/web/live/admin/clients_live/form_component.ex, test/lockspire/web/live/admin/policies_live/dpop_test.exs, test/lockspire/web/live/admin/clients_live_test.exs</files>
  <read_first>
    - lib/lockspire/web/router.ex
    - lib/lockspire/web/live/admin/policies_live/par.ex
    - lib/lockspire/web/live/admin/clients_live/form_component.ex
    - test/lockspire/web/live/admin/policies_live/par_test.exs
    - test/lockspire/web/live/admin/clients_live_test.exs
    - .planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: the router exposes `/admin/policies/dpop`
    - Test 2: the global DPoP page renders current mode, saves `bearer` vs `dpop`, and shows inherit/bearer/dpop client summary counts
    - Test 3: the existing client edit flow renders a DPoP policy selector with `inherit`, `bearer`, and `dpop`
    - Test 4: saving a client DPoP override persists `inherit`, `bearer`, or `dpop` without affecting existing PAR workflows
  </behavior>
  <action>Create `lib/lockspire/web/live/admin/policies_live/dpop.ex` by mirroring the narrow PAR global-policy pattern per D-14, using `Admin.get_server_policy/0` + `Admin.put_dpop_policy/1` and a summary of client overrides split across `inherit`, `bearer`, and `dpop`. Add a matching route at `/admin/policies/dpop`. Reuse the existing client edit workflow instead of adding a second dedicated DPoP route: extend `form_component.ex` with a DPoP policy selector that offers exactly `inherit`, `bearer`, and `dpop` for operator-managed clients, and persist the override through `Admin.update_client/2`. Preserve the narrow control-plane shape: one global DPoP page and one client override workflow only, with no broader sender-constrained console and no default mutation of existing bearer clients.</action>
  <acceptance_criteria>
    - `rg -n '/admin/policies/dpop' lib/lockspire/web/router.ex` confirms the new global policy route
    - `rg -n 'defmodule Lockspire\\.Web\\.Live\\.Admin\\.PoliciesLive\\.Dpop' lib/lockspire/web/live/admin/policies_live/dpop.ex` finds the new LiveView
    - `rg -n 'dpop_policy|Client DPoP override|inherit|bearer|dpop' lib/lockspire/web/live/admin/clients_live/form_component.ex` finds the client override UI on the existing edit flow
    - `rg -n 'Global DPoP policy|Save global DPoP policy|client uses bearer|client requires DPoP' test/lockspire/web/live/admin/policies_live/dpop_test.exs` finds global-page proof
    - `rg -n 'Edit client|dpop_policy|Client DPoP override' test/lockspire/web/live/admin/clients_live_test.exs` finds client workflow proof
    - `MIX_ENV=test mix test test/lockspire/web/live/admin/policies_live/dpop_test.exs test/lockspire/web/live/admin/clients_live_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test.setup && MIX_ENV=test mix test test/lockspire/web/live/admin/policies_live/dpop_test.exs test/lockspire/web/live/admin/clients_live_test.exs -x</automated>
  </verify>
  <done>Operators can manage a narrow global DPoP policy and per-client DPoP override using the same UX shape as PAR, with no repo-internal edits and no unexpected behavior change for existing bearer clients.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin LiveView -> server/client policy persistence | Operator form input becomes durable bearer-vs-DPoP policy truth here |
| DCR request JSON -> client persistence | External client metadata becomes durable `client.dpop_policy` state here |
| durable policy -> response JSON | Stored policy is reflected back to operators and self-registered clients as public truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-07 | Tampering | DCR create/update mapping | mitigate | Map `dpop_bound_access_tokens` directly into `client.dpop_policy` and derive read/update JSON from that durable field per D-13, D-15, and D-16 |
| T-35-08 | Elevation of Privilege | admin DPoP policy UI | mitigate | Mirror the existing PAR narrow-surface pattern so only explicit global and client overrides exist, with allowed values constrained to `inherit | bearer | dpop` |
| T-35-09 | Repudiation | operator/DCR control surfaces | mitigate | Keep all token-mode changes on existing audited admin and DCR protocol seams rather than hidden metadata or repo-internal edits |
</threat_model>

<verification>
Run the admin + DCR configuration suite after this plan:

`MIX_ENV=test mix test.setup && MIX_ENV=test mix test test/lockspire/protocol/registration_test.exs test/lockspire/protocol/registration_management_test.exs test/lockspire/web/registration_json_test.exs test/lockspire/web/live/admin/policies_live/dpop_test.exs test/lockspire/web/live/admin/clients_live_test.exs -x`
</verification>

<success_criteria>
- Operators can change the global DPoP policy from a dedicated admin page that mirrors PAR’s narrow UX.
- Operators can override an individual client to `inherit`, `bearer`, or `dpop` through the existing client edit workflow.
- DCR create/read/update accept and return RFC 9449 `dpop_bound_access_tokens` and keep durable client policy authoritative.
- Existing operator-managed bearer clients remain unchanged until explicitly edited.
</success_criteria>

<output>
After completion, create `.planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-03-SUMMARY.md`
</output>
