---
phase: 35-owned-endpoint-consumption-and-truthful-surface
plan: "02"
type: execute
wave: 2
depends_on:
  - "35-01"
files_modified:
  - lib/lockspire/protocol/discovery.ex
  - test/lockspire/protocol/discovery_test.exs
  - test/lockspire/web/discovery_controller_test.exs
  - test/lockspire/release_readiness_contract_test.exs
  - docs/supported-surface.md
autonomous: true
requirements:
  - DPoP-10
must_haves:
  truths:
    - "Discovery publishes DPoP metadata only when the repo-owned DPoP slice is actually mounted and proved."
    - "The published DPoP algorithm metadata comes from the validator allowlist, not a hand-maintained second list."
    - "Public support docs say Lockspire proves DPoP only on `/token` and Lockspire-owned `userinfo`, not generic host protected resources."
    - "Release contract tests fail if discovery/docs drift ahead of the shipped DPoP slice."
  artifacts:
    - path: lib/lockspire/protocol/discovery.ex
      provides: truth-gated publication of `dpop_signing_alg_values_supported`
    - path: docs/supported-surface.md
      provides: canonical preview wording for the shipped DPoP surface and its explicit limits
    - path: test/lockspire/release_readiness_contract_test.exs
      provides: enforcement backstop for public DPoP wording and support-surface truth
  key_links:
    - from: lib/lockspire/protocol/discovery.ex
      to: lib/lockspire/protocol/dpop.ex
      via: published metadata reads the validator allowlist from `signing_alg_values_supported/0`
      pattern: "DPoP.signing_alg_values_supported"
    - from: lib/lockspire/protocol/discovery.ex
      to: lib/lockspire/web/router.ex
      via: DPoP metadata only appears when `/token` and `/userinfo` are both mounted
      pattern: 'Map.has_key\\(endpoint_metadata, "token_endpoint"\\).+Map.has_key\\(endpoint_metadata, "userinfo_endpoint"\\)'
    - from: docs/supported-surface.md
      to: test/lockspire/release_readiness_contract_test.exs
      via: release docs contract pins the exact public DPoP wording
      pattern: "DPoP"
---

<objective>
Make discovery and public support docs advertise only the DPoP slice the repo now proves on owned surfaces.

Purpose: Lock public claims to executable repo truth per D-09 through D-12 so metadata and docs cannot get ahead of the shipped `userinfo` consumption path.
Output: Truth-gated DPoP discovery metadata, updated supported-surface wording, and release-contract tests that pin the narrow DPoP claim set.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-CONTEXT.md
@.planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-RESEARCH.md
@.planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-PATTERNS.md
@.planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-01-PLAN.md
@lib/lockspire/protocol/discovery.ex
@lib/lockspire/protocol/dpop.ex
@test/lockspire/protocol/discovery_test.exs
@test/lockspire/web/discovery_controller_test.exs
@test/lockspire/release_readiness_contract_test.exs
@docs/supported-surface.md

<interfaces>
From lib/lockspire/protocol/discovery.ex:
```elixir
@spec openid_configuration() :: map()
def published_token_endpoint_auth_methods_supported()
```

From lib/lockspire/protocol/dpop.ex:
```elixir
def signing_alg_values_supported() :: [String.t()]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish truth-gated DPoP discovery metadata from mounted owned-surface reality</name>
  <files>lib/lockspire/protocol/discovery.ex, test/lockspire/protocol/discovery_test.exs, test/lockspire/web/discovery_controller_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/discovery.ex
    - lib/lockspire/protocol/dpop.ex
    - test/lockspire/protocol/discovery_test.exs
    - test/lockspire/web/discovery_controller_test.exs
    - .planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: `openid_configuration/0` publishes `dpop_signing_alg_values_supported` when both `/token` and `/userinfo` are mounted
    - Test 2: the published DPoP algorithms exactly equal `DPoP.signing_alg_values_supported/0`
    - Test 3: discovery omits DPoP metadata when the required owned DPoP surface is not mounted
    - Test 4: the HTTP `/.well-known/openid-configuration` response mirrors the same present/refute truth
  </behavior>
  <action>Extend `Lockspire.Protocol.Discovery` per D-09 and D-10 so DPoP metadata is published only when the mounted surface proves the shipped slice: the token endpoint and Lockspire-owned `userinfo` are both present. Add a small truth gate function inside `discovery.ex` that checks mounted endpoint metadata rather than static config and then conditionally inserts `dpop_signing_alg_values_supported` from `Lockspire.Protocol.DPoP.signing_alg_values_supported/0`. Do not publish protected-resource metadata beyond this milestone’s slice, do not mention generic host-resource middleware, and do not add hand-maintained algorithm constants. Update both the pure discovery tests and the HTTP controller tests to assert present/refute behavior, exact algorithm list reuse, and continued omission when the route truth is incomplete.</action>
  <acceptance_criteria>
    - `rg -n 'dpop_signing_alg_values_supported' lib/lockspire/protocol/discovery.ex test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs` finds metadata wiring and proof
    - `rg -n 'DPoP\\.signing_alg_values_supported\\(\\)' lib/lockspire/protocol/discovery.ex` confirms discovery consumes the validator export directly
    - `rg -n 'refute Map.has_key\\(config, \"dpop_signing_alg_values_supported\"\\)' test/lockspire/protocol/discovery_test.exs` confirms truth-gated omission coverage
    - `rg -n 'body\\[\"dpop_signing_alg_values_supported\"\\]' test/lockspire/web/discovery_controller_test.exs` confirms HTTP publication proof
    - `MIX_ENV=test mix test test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test.setup && MIX_ENV=test mix test test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs -x</automated>
  </verify>
  <done>Discovery publishes DPoP algorithms only when the shipped owned-surface DPoP slice is really present, and the published values come from the validator’s allowlist rather than a duplicated constant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the supported-surface contract and pin it with release-readiness tests</name>
  <files>docs/supported-surface.md, test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - docs/supported-surface.md
    - test/lockspire/release_readiness_contract_test.exs
    - .planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-CONTEXT.md
    - .planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: the release contract asserts that supported-surface docs describe DPoP only on `/token` and Lockspire-owned `userinfo`
    - Test 2: the release contract asserts docs keep generic host protected-resource middleware out of scope
    - Test 3: the docs explicitly state bearer clients remain unchanged by default while DPoP is explicit and narrow
  </behavior>
  <action>Update `docs/supported-surface.md` per D-11 and D-12 so the preview contract explicitly says Lockspire now proves DPoP on token requests and the Lockspire-owned `userinfo` endpoint, while generic host protected-resource validation remains out of scope. Keep the wording narrow and repo-truthful: no nonce claims, no generic middleware, no broad resource-server posture, and no stronger support wording than the tests can prove. Extend `test/lockspire/release_readiness_contract_test.exs` to pin the new DPoP language, including that bearer clients remain unchanged by default, `userinfo` is the owned protected-resource proof surface, and unsupported host-resource claims remain explicitly rejected.</action>
  <acceptance_criteria>
    - `rg -n 'DPoP|userinfo|protected-resource|bearer clients remain unchanged' docs/supported-surface.md` finds the narrowed public wording
    - `rg -n 'DPoP|userinfo|generic host protected-resource middleware|bearer clients remain unchanged' test/lockspire/release_readiness_contract_test.exs` finds release-contract assertions for the same wording
    - `MIX_ENV=test mix test test/lockspire/release_readiness_contract_test.exs -x` passes
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test.setup && MIX_ENV=test mix test test/lockspire/release_readiness_contract_test.exs -x</automated>
  </verify>
  <done>The canonical supported-surface doc now describes only the shipped DPoP slice, and release-readiness tests will fail if public wording drifts ahead of owned-surface repo proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| discovery builder -> public metadata | Repo-internal route truth becomes an external protocol contract here |
| supported-surface docs -> release claims | Human-facing docs become public support posture and release evidence |
| release contract tests -> docs metadata | Tests enforce that repo claims do not outrun implemented DPoP behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-04 | Information Disclosure | `lib/lockspire/protocol/discovery.ex` | mitigate | Publish DPoP metadata only when `/token` and `/userinfo` are both mounted and use the actual validator allowlist per D-09 and D-10 |
| T-35-05 | Tampering | `docs/supported-surface.md` public claims | mitigate | Pin the DPoP wording in release-readiness tests so docs cannot overclaim owned-surface support per D-11 and D-12 |
| T-35-06 | Repudiation | release posture evidence | mitigate | Keep discovery tests, HTTP tests, and release contract tests aligned so the repo has executable proof for every public DPoP claim |
</threat_model>

<verification>
Run the discovery/docs truth suite after this plan:

`MIX_ENV=test mix test.setup && MIX_ENV=test mix test test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs test/lockspire/release_readiness_contract_test.exs -x`
</verification>

<success_criteria>
- Discovery publishes `dpop_signing_alg_values_supported` only when the shipped owned DPoP slice is truly mounted.
- The published algorithm list matches the validator allowlist exactly.
- `docs/supported-surface.md` explicitly limits DPoP claims to `/token` and Lockspire-owned `userinfo`.
- Release-readiness tests guard both discovery metadata and the public support wording against drift.
</success_criteria>

<output>
After completion, create `.planning/phases/35-owned-endpoint-consumption-and-truthful-surface/35-02-SUMMARY.md`
</output>
