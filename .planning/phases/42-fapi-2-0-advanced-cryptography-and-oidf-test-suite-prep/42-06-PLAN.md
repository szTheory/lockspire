---
phase: 42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep
plan: 06
type: execute
wave: 3
depends_on: [03]
files_modified:
  - lib/lockspire/protocol/registration.ex
  - lib/lockspire/protocol/registration_management.ex
  - lib/lockspire/web/controllers/registration_controller.ex
  - test/lockspire/protocol/registration_test.exs
  - test/lockspire/protocol/registration_management_test.exs
  - test/lockspire/web/controllers/registration_controller_test.exs
autonomous: true
requirements: [FAPI-04]
must_haves:
  truths:
    - "DCR intake and RFC 7592 registration-management updates fail fast when FAPI-effective metadata is incompatible or the server signing posture is not ready, per D-06/D-09."
    - "Registration HTTP surfaces reuse the same remediation-friendly readiness contract as the admin client path instead of inventing a second policy system, per D-10."
    - "Non-FAPI clients may still persist legacy metadata when their effective profile remains `:none`, preserving mixed-mode semantics per D-07/D-08."
  artifacts:
    - path: "lib/lockspire/protocol/registration.ex"
      provides: "DCR intake rejection path that reuses the Phase 42 client-readiness contract"
      exports: ["register/2"]
    - path: "lib/lockspire/protocol/registration_management.ex"
      provides: "RFC 7592 update rejection path aligned with admin/client FAPI readiness semantics"
      exports: ["update/3"]
    - path: "lib/lockspire/web/controllers/registration_controller.ex"
      provides: "HTTP registration surfaces that expose the same remediation-friendly FAPI errors as protocol/admin flows"
      contains: "register"
  key_links:
    - from: "lib/lockspire/protocol/registration.ex"
      to: "lib/lockspire/admin/clients.ex"
      via: "reuses the client-readiness and remediation contract established in Plan 03"
      pattern: "security_profile|id_token_signed_response_alg|fapi_2_0_security"
    - from: "lib/lockspire/protocol/registration_management.ex"
      to: "lib/lockspire/storage/ecto/client_record.ex"
      via: "durable update paths reuse the same FAPI metadata validation semantics"
      pattern: "update_changeset|id_token_signed_response_alg"
    - from: "lib/lockspire/web/controllers/registration_controller.ex"
      to: "lib/lockspire/protocol/registration.ex"
      via: "controller responses surface protocol-level remediation guidance without translation drift"
      pattern: "register|error"
---

<objective>
Wire DCR and registration-management surfaces to the Phase 42 readiness contract without reopening the larger admin/storage slice.

Purpose: D-06 through D-10 require remote registration paths to fail fast on the same incompatible metadata and missing-signing-readiness cases as the admin path. Splitting these controller-facing surfaces into their own dependent plan keeps the repo boundary clean: Plan 03 establishes the durable readiness contract, and this plan applies it to protocol and HTTP registration flows.

Output:
- DCR and RFC 7592 update paths that reject broken FAPI-effective metadata before runtime.
- Registration controller responses that carry the same remediation guidance as the admin path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-CONTEXT.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-RESEARCH.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-PATTERNS.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-VALIDATION.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-03-PLAN.md

<interfaces>
From `lib/lockspire/protocol/registration.ex`:
```elixir
@spec register(map(), keyword()) :: {:ok, Success.t()} | {:error, list(error_detail())}
```

From `lib/lockspire/protocol/registration_management.ex`:
```elixir
@spec update(Client.t(), map(), keyword()) :: {:ok, Success.t()} | {:error, list(error_detail())}
```

From `lib/lockspire/web/controllers/registration_controller.ex`:
```elixir
def create(conn, params)
def update(conn, params)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reuse the admin/client readiness contract in DCR and RFC 7592 update paths</name>
  <files>lib/lockspire/protocol/registration.ex, lib/lockspire/protocol/registration_management.ex, test/lockspire/protocol/registration_test.exs, test/lockspire/protocol/registration_management_test.exs</files>
  <behavior>
    - Test 1: DCR intake rejects `security_profile: :fapi_2_0_security` when client algorithm metadata is incompatible or the server signing posture is not FAPI-ready, per D-06/D-09.
    - Test 2: RFC 7592 registration-management updates reject the same incompatible FAPI metadata and missing-signing-readiness combinations as the admin client path, per D-06/D-10.
    - Test 3: non-FAPI clients may still store legacy metadata such as RS256/EdDSA without becoming FAPI-effective, per D-07/D-08.
  </behavior>
  <action>
    Update the DCR intake and registration-management flows to reuse the readiness/remediation contract established in Plan 03 instead of introducing controller-local or protocol-local policy logic. Keep the repo truthful about unsupported areas: validations and error messages should steer maintainers toward the shipped ES256/PS256 + DPoP path and must not imply mTLS or `private_key_jwt` support that does not yet exist.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/registration_test.exs test/lockspire/protocol/registration_management_test.exs</automated>
  </verify>
  <done>DCR and RFC 7592 updates fail fast on broken FAPI-effective metadata using the same semantics as the admin client path, while mixed-mode legacy storage remains possible for `:none` clients.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface remediation-friendly FAPI registration errors through the HTTP controller</name>
  <files>lib/lockspire/web/controllers/registration_controller.ex, test/lockspire/web/controllers/registration_controller_test.exs</files>
  <behavior>
    - Test 1: registration HTTP responses expose the same remediation-friendly FAPI readiness and metadata errors as the protocol/admin path, per D-10.
    - Test 2: controller-facing DCR failures do not drift into generic or misleading messages that imply support for unbuilt auth methods.
  </behavior>
  <action>
    Update the registration controller only as needed to preserve the protocol-level error contract through HTTP. Do not add new validation logic here; the controller should translate or render the existing remediation guidance without changing its meaning.
  </action>
  <verify>
    <automated>mix test test/lockspire/web/controllers/registration_controller_test.exs</automated>
  </verify>
  <done>The HTTP registration surface stays aligned with protocol/admin remediation semantics and does not invent or obscure FAPI readiness failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote DCR clients -> registration protocol | Client-supplied metadata can try to persist incompatible FAPI posture |
| registration controller -> public HTTP surface | Error rendering can drift from protocol truth and mislead integrators |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-16 | T | `lib/lockspire/protocol/registration.ex` / `lib/lockspire/protocol/registration_management.ex` | mitigate | Reuse the Plan 03 readiness contract so remote clients cannot bypass FAPI metadata or signing-readiness checks. |
| T-42-17 | R | `lib/lockspire/web/controllers/registration_controller.ex` | mitigate | Preserve structured remediation guidance through HTTP so failures remain auditable and truthful. |
| T-42-18 | E | registration-facing metadata validation | mitigate | Keep mixed-mode storage possible only for `:none` clients and reject broken FAPI opt-ins before persistence. |
</threat_model>

<verification>
- Run the targeted registration tests from `42-VALIDATION.md` for Task `42-06-01`.
- Confirm DCR and registration-management failure paths reuse the Plan 03 readiness contract and do not imply unimplemented auth methods.
</verification>

<success_criteria>
- DCR and RFC 7592 update paths are fail-fast and remediation-friendly under FAPI-effective behavior.
- Controller-facing registration errors stay aligned with protocol/admin truth.
- The registration slice is isolated from admin/storage changes, reducing plan scope while preserving D-06 through D-10 coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-06-SUMMARY.md`
</output>
