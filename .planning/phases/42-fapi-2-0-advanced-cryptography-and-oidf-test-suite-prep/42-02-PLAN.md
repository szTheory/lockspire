---
phase: 42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/lockspire/protocol/id_token.ex
  - lib/lockspire/protocol/jar.ex
  - test/lockspire/protocol/id_token_test.exs
  - test/lockspire/protocol/jar_test.exs
autonomous: true
requirements: [FAPI-04]
must_haves:
  truths:
    - "When FAPI-effective behavior is active, JAR request-object verification accepts only `ES256` / `PS256` and rejects `RS256` / `EdDSA` per the canonical policy."
    - "ID token signing uses the same canonical allow-list as other FAPI-effective runtime surfaces, so non-compliant signing keys are rejected before JOSE signing."
    - "JAR and ID token runtime behavior stays aligned to the canonical profile truth without depending on per-surface constants."
  artifacts:
    - path: "lib/lockspire/protocol/jar.ex"
      provides: "Profile-aware JAR request-object verification"
      exports: ["verify_signature/3"]
    - path: "lib/lockspire/protocol/id_token.ex"
      provides: "Profile-aware ID token signing preflight and emission"
      exports: ["sign/1"]
  key_links:
    - from: "lib/lockspire/protocol/jar.ex"
      to: "lib/lockspire/protocol/security_profile.ex"
      via: "request-object verification allow-list"
      pattern: "allowed_signing_algorithms"
    - from: "lib/lockspire/protocol/id_token.ex"
      to: "lib/lockspire/protocol/security_profile.ex"
      via: "signer preflight stays aligned with the canonical runtime policy"
      pattern: "allowed_signing_algorithms|ensure_allowed_alg"
---

<objective>
Align the highest-throughput JAR and ID token runtime paths to the canonical Phase 42 algorithm policy established in Plan 01.

Purpose: D-04 and D-05 require acceptance and emission surfaces to agree on the same resolved-profile truth. This plan isolates the JAR and ID token paths into a smaller runtime slice so the follow-up logout/end-session/DPoP cleanup can stay separate and the former 10-file scope warning is eliminated instead of merely accepted.

Output:
- JAR verification and ID token signing both consume the same canonical algorithm policy.
- Focused protocol tests pin the ES256/PS256-only runtime contract before the dependent logout/end-session/DPoP cleanup and publication slices.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-CONTEXT.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-RESEARCH.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-PATTERNS.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-VALIDATION.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-01-PLAN.md

<interfaces>
From `lib/lockspire/protocol/dpop.ex`:
```elixir
@spec signing_alg_values_supported() :: [String.t()]
@spec signing_alg_values_supported(SecurityProfile.Resolved.t() | :fapi_2_0_security | :none) ::
        [String.t()]
```

From `lib/lockspire/protocol/id_token.ex`:
```elixir
@spec sign(map()) :: {:ok, String.t()} | {:error, atom()}
```

From `lib/lockspire/protocol/jar.ex`:
```elixir
@spec verify_signature(String.t(), Client.t(), [String.t()]) ::
        {:ok, t()} | {:error, :invalid_signature | :no_matching_key | :invalid_client_keys | :invalid_typ}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make JAR verification consume the canonical profile-aware allow-list</name>
  <files>lib/lockspire/protocol/jar.ex, test/lockspire/protocol/jar_test.exs</files>
  <behavior>
    - Test 1: FAPI-effective JAR verification rejects `RS256` / `EdDSA` request objects and accepts `ES256` / `PS256` per D-04/D-05.
    - Test 2: non-FAPI JAR verification continues to honor the broader legacy allow-list only where the effective profile is `:none`.
  </behavior>
  <action>
    Update `lib/lockspire/protocol/jar.ex` so request-object verification resolves its allow-list from the Plan 01 canonical policy rather than any local or caller-maintained constant. Keep JOSE verification protocol-owned and explicit, and preserve the existing typed JAR error envelope while making FAPI-effective behavior reject `RS256` / `EdDSA` per D-04/D-05.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/jar_test.exs</automated>
  </verify>
  <done>JAR verification uses the canonical allow-list, rejects `RS256` / `EdDSA` under FAPI-effective behavior, and preserves the existing typed protocol errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make ID token signing enforce the canonical FAPI signer policy</name>
  <files>lib/lockspire/protocol/id_token.ex, test/lockspire/protocol/id_token_test.exs</files>
  <behavior>
    - Test 1: ID token signing rejects non-compliant key algorithms under FAPI and signs successfully with `ES256` / `PS256` per D-04/D-05.
    - Test 2: non-FAPI ID token signing preserves the broader legacy list only where the effective profile is `:none`.
  </behavior>
  <action>
    Tighten `lib/lockspire/protocol/id_token.ex` so signer preflight, active-key validation, and emitted headers all stay aligned to the Plan 01 canonical policy instead of any broader implicit fallback. Preserve the existing claims flow and typed failure behavior, but make FAPI-effective ID token signing reject `RS256` / `EdDSA` before JOSE signing begins, per D-04/D-05.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/id_token_test.exs</automated>
  </verify>
  <done>ID token signing consumes the canonical signer policy, rejects non-compliant key algorithms under FAPI-effective behavior, and preserves non-FAPI legacy behavior only where the effective profile is `:none`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external clients -> request-object verification | Request objects bring attacker-controlled JOSE headers and algorithms |
| key store -> ID token signer | Active signing keys can drift from FAPI posture if signer preflight is not explicit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-04 | S | `lib/lockspire/protocol/jar.ex` | mitigate | Use explicit ES256/PS256-only allow-lists under FAPI-effective behavior and reject RS256/alg drift. |
| T-42-05 | T | `lib/lockspire/protocol/id_token.ex` | mitigate | Preflight signer algorithms against canonical policy before JOSE signing. |
| T-42-06 | T | JAR and ID token runtime policy seam | mitigate | Route both surfaces through the Plan 01 canonical policy so no per-surface constant can drift. |
</threat_model>

<verification>
  - Run the targeted protocol tests from `42-VALIDATION.md` for Tasks `42-02-01` and `42-02-02`.
  - Confirm JAR verification and ID token signing both resolve their FAPI-effective allow-list from the canonical Plan 01 policy.
</verification>

<success_criteria>
- JAR verification and ID token signing consume the canonical algorithm truth without local allow-list drift.
- FAPI-effective runtime behavior rejects `RS256` and `EdDSA` on these high-throughput paths before publication surfaces are updated.
- This plan stays below the prior scope-warning threshold while preserving the locked Phase 42 boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-02-SUMMARY.md`
</output>
