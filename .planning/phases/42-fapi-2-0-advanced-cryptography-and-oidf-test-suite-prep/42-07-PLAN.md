---
phase: 42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep
plan: 07
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/lockspire/protocol/dpop.ex
  - lib/lockspire/protocol/logout_token.ex
  - lib/lockspire/protocol/end_session.ex
  - test/lockspire/protocol/dpop_test.exs
  - test/lockspire/protocol/logout_token_test.exs
  - test/lockspire/protocol/end_session_test.exs
autonomous: true
requirements: [FAPI-04]
must_haves:
  truths:
    - "Logout token signing accepts and emits only `ES256` / `PS256` under FAPI-effective behavior."
    - "End-session `id_token_hint` verification rejects `RS256` / `EdDSA` under FAPI-effective behavior by using the canonical allow-list instead of a hardcoded constant."
    - "DPoP validation uses the same canonical allow-list as the other runtime verification paths, so FAPI-effective enforcement does not drift from signer policy."
  artifacts:
    - path: "lib/lockspire/protocol/logout_token.ex"
      provides: "Profile-aware logout token signing"
      exports: ["sign/1"]
    - path: "lib/lockspire/protocol/end_session.ex"
      provides: "Profile-aware `id_token_hint` verification using publishable keys and canonical allow-lists"
      exports: ["validate/1"]
    - path: "lib/lockspire/protocol/dpop.ex"
      provides: "DPoP verification and advertised validator truth aligned to the canonical signing policy"
      exports: ["signing_alg_values_supported/0", "signing_alg_values_supported/1"]
  key_links:
    - from: "lib/lockspire/protocol/logout_token.ex"
      to: "lib/lockspire/protocol/security_profile.ex"
      via: "logout signing preflight uses the canonical allow-list"
      pattern: "allowed_signing_algorithms|ensure_allowed_alg"
    - from: "lib/lockspire/protocol/end_session.ex"
      to: "lib/lockspire/protocol/security_profile.ex"
      via: "`id_token_hint` verification resolves the same FAPI-effective allow-list as other verifiers"
      pattern: "allowed_signing_algorithms|verify_strict"
    - from: "lib/lockspire/protocol/dpop.ex"
      to: "lib/lockspire/protocol/security_profile.ex"
      via: "proof verification allow-list stays aligned with signer policy"
      pattern: "allowed_signing_algorithms|signing_alg_values_supported"
---

<objective>
Finish the runtime cryptography cleanup for logout, end-session, and DPoP without reopening the broader JAR/ID-token slice.

Purpose: D-04 and D-05 require all Lockspire-owned acceptance and emission surfaces to agree on the same resolved-profile truth. This follow-on runtime slice removes the remaining hardcoded `RS256` assumptions and stale allow-lists from logout, end-session, and DPoP while keeping the work small enough to avoid the prior scope warning.

Output:
- Logout token signing, end-session hint verification, and DPoP proof validation all consume the same canonical algorithm policy.
- Focused protocol tests pin the ES256/PS256-only runtime contract before discovery/JWKS/challenge publication is updated in Plan 05.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-CONTEXT.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-RESEARCH.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-PATTERNS.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-VALIDATION.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-01-PLAN.md

<interfaces>
From `lib/lockspire/protocol/dpop.ex`:
```elixir
@spec signing_alg_values_supported() :: [String.t()]
@spec signing_alg_values_supported(SecurityProfile.Resolved.t() | :fapi_2_0_security | :none) ::
        [String.t()]
```

From `lib/lockspire/protocol/logout_token.ex`:
```elixir
@spec sign(map()) :: {:ok, String.t()} | {:error, atom() | tuple()}
```

From `lib/lockspire/protocol/end_session.ex`:
```elixir
@spec validate(map()) :: {:ok, map()} | {:error, term()}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove hardcoded RS256 assumptions from logout and end-session runtime paths</name>
  <files>lib/lockspire/protocol/logout_token.ex, lib/lockspire/protocol/end_session.ex, test/lockspire/protocol/logout_token_test.exs, test/lockspire/protocol/end_session_test.exs</files>
  <behavior>
    - Test 1: logout token signing rejects non-compliant key algorithms under FAPI and signs successfully with `ES256` / `PS256` per D-04/D-05.
    - Test 2: `EndSession.validate/1` verifies `id_token_hint` against the canonical allow-list instead of the hardcoded `RS256` constant.
    - Test 3: non-FAPI runtime paths continue to allow the broader legacy list where the effective profile is `:none`.
  </behavior>
  <action>
    Replace the local `RS256` assumptions in `lib/lockspire/protocol/logout_token.ex` and `lib/lockspire/protocol/end_session.ex` with the Plan 01 canonical policy, using the existing `IdToken` signer-preflight and `DPoP.verify_strict` patterns as the implementation model. Preserve the current claims flow and typed error envelopes; the change here is the signer/verifier policy mechanics required by D-04/D-05.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/logout_token_test.exs test/lockspire/protocol/end_session_test.exs</automated>
  </verify>
  <done>Logout token signing and end-session hint verification no longer rely on hardcoded `RS256` assumptions, and FAPI-effective runtime behavior rejects non-compliant algorithms while preserving non-FAPI legacy behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align DPoP verification with the canonical FAPI runtime policy</name>
  <files>lib/lockspire/protocol/dpop.ex, test/lockspire/protocol/dpop_test.exs</files>
  <behavior>
    - Test 1: DPoP verification rejects `RS256` / `EdDSA` proofs under FAPI-effective behavior and preserves the typed error envelope.
    - Test 2: non-FAPI DPoP verification continues to allow the broader legacy list only where the effective profile is `:none`.
    - Test 3: `signing_alg_values_supported/1` remains the single validator truth that later publication surfaces can reuse in Plan 05.
  </behavior>
  <action>
    Update `lib/lockspire/protocol/dpop.ex` so proof verification and the exported validator-truth helper both consume the Plan 01 canonical policy instead of any stale allow-list constant. Keep JOSE verification protocol-owned and preserve the existing typed errors; the change is to make FAPI-effective DPoP reject `RS256` / `EdDSA` everywhere the validator inspects JOSE headers, per D-04/D-05.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/dpop_test.exs</automated>
  </verify>
  <done>DPoP verification stays aligned with the canonical FAPI runtime policy, rejects non-compliant proofs under FAPI-effective behavior, and continues to expose one validator-truth helper for downstream publication surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external clients -> JWT verification surfaces | Logout hints and DPoP proofs bring attacker-controlled JOSE headers and algorithms |
| key store -> logout signer | Active signing keys can drift from FAPI posture if logout signing is not preflighted explicitly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-19 | S | `lib/lockspire/protocol/end_session.ex` | mitigate | Use explicit ES256/PS256-only allow-lists under FAPI-effective behavior and reject `RS256` / `EdDSA` hints. |
| T-42-20 | T | `lib/lockspire/protocol/logout_token.ex` | mitigate | Preflight logout signer algorithms against the canonical policy before JOSE signing. |
| T-42-21 | T | `lib/lockspire/protocol/dpop.ex` | mitigate | Reuse the same allow-list for proof verification so DPoP enforcement cannot drift from signer/verifier truth. |
</threat_model>

<verification>
- Run the targeted protocol tests from `42-VALIDATION.md` for Tasks `42-07-01` and `42-07-02`.
- Confirm logout, end-session, and DPoP no longer carry hardcoded `RS256` or stale FAPI allow-list assumptions.
</verification>

<success_criteria>
- Logout token signing, end-session verification, and DPoP validation all consume the canonical algorithm truth.
- FAPI-effective runtime behavior rejects `RS256` and `EdDSA` on these remaining protocol surfaces.
- The runtime cleanup is isolated from JAR/ID-token and publication work, eliminating the former `42-02` scope warning instead of merely documenting it.
</success_criteria>

<output>
After completion, create `.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-07-SUMMARY.md`
</output>
