---
phase: 42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 07]
files_modified:
  - lib/lockspire/protocol/discovery.ex
  - lib/lockspire/protocol/jwks.ex
  - lib/lockspire/web/controllers/userinfo_controller.ex
  - test/lockspire/protocol/discovery_test.exs
  - test/lockspire/web/discovery_controller_test.exs
  - test/lockspire/web/jwks_controller_test.exs
  - test/lockspire/protocol/dpop_test.exs
autonomous: true
requirements: [FAPI-04]
must_haves:
  truths:
    - "Discovery publishes only the signing algorithms the runtime actually supports under the resolved profile, per D-11/D-12."
    - "JWKS output excludes keys that are not publishable for FAPI-effective behavior, so publication truth matches runtime signer selection."
    - "DPoP `WWW-Authenticate` challenge metadata advertises only the algorithms the validator actually allows."
  artifacts:
    - path: "lib/lockspire/protocol/discovery.ex"
      provides: "Truthful algorithm metadata for discovery under FAPI-effective and non-FAPI behavior"
      exports: ["openid_configuration/0"]
    - path: "lib/lockspire/protocol/jwks.ex"
      provides: "JWKS publication filtered to the runtime/publication policy established in Plan 01"
      exports: ["public_jwk_set/1"]
    - path: "lib/lockspire/web/controllers/userinfo_controller.ex"
      provides: "DPoP challenge metadata derived from validator truth"
      contains: "WWW-Authenticate"
  key_links:
    - from: "lib/lockspire/protocol/discovery.ex"
      to: "lib/lockspire/protocol/security_profile.ex"
      via: "published signing algorithms derive from canonical policy"
      pattern: "allowed_signing_algorithms|openid_configuration"
    - from: "lib/lockspire/protocol/jwks.ex"
      to: "lib/lockspire/storage/ecto/repository.ex"
      via: "published keys reflect FAPI-eligible selection from Plan 01"
      pattern: "list_publishable_keys"
    - from: "lib/lockspire/web/controllers/userinfo_controller.ex"
      to: "lib/lockspire/protocol/dpop.ex"
      via: "challenge algs derive from validator truth updated in Plan 02"
      pattern: "signing_alg_values_supported"
---

<objective>
Align discovery, JWKS, and DPoP challenge publication with the runtime truth established by Plans 01 and 02.

Purpose: D-11 and D-12 require publication surfaces to stay truthful to runtime support. With JAR/ID-token runtime alignment now isolated in Plan 02 and logout/end-session/DPoP cleanup isolated in Plan 07, this plan can focus strictly on publication truth without reopening either runtime slice.

Output:
- Discovery metadata that advertises only the resolved-profile signing subset.
- JWKS publication filtered to keys that are actually eligible for FAPI-effective runtime/publication use.
- DPoP challenge metadata that advertises only validator-supported algorithms.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-CONTEXT.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-RESEARCH.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-PATTERNS.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-VALIDATION.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-01-PLAN.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-02-PLAN.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-07-PLAN.md

<interfaces>
From `lib/lockspire/protocol/discovery.ex`:
```elixir
@spec openid_configuration() :: map()
```

From `lib/lockspire/protocol/jwks.ex`:
```elixir
@spec public_jwk_set(keyword()) :: {:ok, map()} | {:error, term()}
```

From `lib/lockspire/protocol/dpop.ex`:
```elixir
@spec signing_alg_values_supported() :: [String.t()]
@spec signing_alg_values_supported(SecurityProfile.Resolved.t() | :fapi_2_0_security | :none) ::
        [String.t()]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make discovery and JWKS publication truthful to the resolved-profile policy</name>
  <files>lib/lockspire/protocol/discovery.ex, lib/lockspire/protocol/jwks.ex, test/lockspire/protocol/discovery_test.exs, test/lockspire/web/discovery_controller_test.exs, test/lockspire/web/jwks_controller_test.exs</files>
  <behavior>
    - Test 1: discovery publishes `id_token_signing_alg_values_supported` from the same canonical truth used by runtime per D-11/D-12.
    - Test 2: FAPI-effective discovery no longer advertises RS256 or EdDSA, while non-FAPI publication still reflects the broader legacy list where the effective profile is `:none`.
    - Test 3: JWKS output contains only keys eligible for the runtime/publication policy established in Plan 01.
  </behavior>
  <action>
    Update discovery and JWKS publication so they derive algorithm truth and publishable keys from the same canonical policy and repository-filtered selection used by runtime. Keep the metadata truthful rather than compatibility-biased: per D-11, FAPI-effective publication should advertise only ES256/PS256, while non-FAPI publication may continue to expose the broader legacy list. Do not duplicate allow-lists locally.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/discovery_test.exs test/lockspire/web/discovery_controller_test.exs test/lockspire/web/jwks_controller_test.exs</automated>
  </verify>
  <done>Discovery and JWKS publish only what the runtime actually supports under the resolved profile, with RS256/EdDSA removed from FAPI-effective publication.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make DPoP challenge metadata advertise only validator-supported algorithms</name>
  <files>lib/lockspire/web/controllers/userinfo_controller.ex, test/lockspire/protocol/dpop_test.exs</files>
  <behavior>
    - Test 1: `WWW-Authenticate` DPoP challenge metadata reflects the validator’s actual allowed algorithms rather than a broader stale list.
    - Test 2: FAPI-effective challenge metadata advertises only ES256/PS256, and non-FAPI challenge metadata continues to reflect the broader non-FAPI validator truth.
  </behavior>
  <action>
    Update the userinfo controller’s challenge builder to call the same `DPoP.signing_alg_values_supported/1` truth the validator uses after Plan 02. Preserve the existing error structure and realm text; only the algorithm publication contract changes here.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/dpop_test.exs</automated>
  </verify>
  <done>DPoP challenge metadata stays truthful to validator behavior under both FAPI and non-FAPI effective profiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| discovery/JWKS publication -> relying parties | Misstated metadata would cause interoperability drift and false compliance claims |
| userinfo challenge publication -> API clients | Stale challenge algorithm hints can mislead clients into sending proofs the validator will reject |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-13 | R | `lib/lockspire/protocol/discovery.ex` | mitigate | Publish signing algorithms from the same canonical policy the runtime uses so discovery cannot over-claim support. |
| T-42-14 | I | `lib/lockspire/protocol/jwks.ex` | mitigate | Filter JWKS publication to FAPI-eligible keys so relying parties do not learn or cache unusable signing material. |
| T-42-15 | T | `lib/lockspire/web/controllers/userinfo_controller.ex` | mitigate | Build DPoP challenge alg hints from validator truth so clients are not induced into algorithm-downgrade or mismatch retries. |
</threat_model>

<verification>
  - Run the targeted publication-surface tests from `42-VALIDATION.md` for Tasks `42-05-01` and `42-05-02`.
- Confirm discovery, JWKS, and DPoP challenge assertions derive from runtime truth instead of hardcoded algorithm constants.
</verification>

<success_criteria>
- Discovery, JWKS, and DPoP challenge publication surfaces are truthful to resolved-profile runtime support.
- FAPI-effective publication stops advertising RS256 and EdDSA.
- The publication contract is isolated from the runtime JOSE refactor, reducing plan risk while preserving D-11/D-12 coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-05-SUMMARY.md`
</output>
