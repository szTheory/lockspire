---
phase: 42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/security_profile.ex
  - lib/lockspire/security/policy.ex
  - lib/lockspire/admin/keys.ex
  - lib/lockspire/storage/key_store.ex
  - lib/lockspire/storage/ecto/repository.ex
  - test/lockspire/protocol/security_profile_test.exs
  - test/lockspire/protocol/security_policy_test.exs
  - test/lockspire/admin/keys_test.exs
autonomous: true
requirements: [FAPI-04]
must_haves:
  truths:
    - "When the effective security profile is `:fapi_2_0_security`, Lockspire exposes one canonical signing-algorithm policy and that policy returns only `ES256` and `PS256`."
    - "A FAPI-effective signing key cannot be generated, activated, fetched as active, or listed as publishable if its algorithm or key strength would violate the FAPI contract."
    - "Non-FAPI durable keys may still exist for mixed-mode clients, but the canonical policy blocks them from FAPI-effective selection paths."
  artifacts:
    - path: "lib/lockspire/protocol/security_profile.ex"
      provides: "Protocol-owned canonical algorithm policy reused by all signing and verification surfaces"
      exports: ["resolve_effective_profile/2", "allowed_signing_algorithms/1"]
    - path: "lib/lockspire/security/policy.ex"
      provides: "FAPI key compliance and boot/runtime invariant helpers aligned to ES256/PS256 only"
      contains: "ensure_fapi_compliant_alg"
    - path: "lib/lockspire/storage/ecto/repository.ex"
      provides: "Fail-fast filtering and/or rejection for active/publishable signing-key selection under FAPI-effective posture"
      contains: "list_publishable_keys"
  key_links:
    - from: "lib/lockspire/admin/keys.ex"
      to: "lib/lockspire/security/policy.ex"
      via: "generate_key/1 and activate_key/2 enforce the same FAPI subset"
      pattern: "Policy\\.validate_key_compliance|allowed_signing_algorithms"
    - from: "lib/lockspire/storage/ecto/repository.ex"
      to: "lib/lockspire/protocol/security_profile.ex"
      via: "publishable/active selection stays aligned to resolved profile truth"
      pattern: "allowed_signing_algorithms|fapi_2_0_security"
---

<objective>
Establish the canonical Phase 42 cryptographic truth source and push it through the key lifecycle seams that can fail fast before invalid state reaches runtime.

Purpose: D-01 through D-10 require FAPI-effective behavior to be a real cryptographic contract, not a hint. This plan creates the one algorithm-policy seam, narrows it to `ES256` and `PS256` per D-02/D-03, and enforces it at generation, activation, storage-selection, and compliance boundaries.

Output:
- One protocol-owned algorithm-policy source for FAPI-effective behavior.
- Key generation, compliance, and storage-selection paths that reject or filter non-FAPI signing posture early.
- Focused tests proving ES256/PS256-only truth and fail-fast key enforcement.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-CONTEXT.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-RESEARCH.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-PATTERNS.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-VALIDATION.md
@.planning/phases/41-fapi-2-0-profile-configuration/41-01-PLAN.md

<interfaces>
From `lib/lockspire/protocol/security_profile.ex`:
```elixir
@spec resolve_effective_profile(ServerPolicy.t(), struct() | map() | nil) :: Resolved.t()
@spec allowed_signing_algorithms(ServerPolicy.security_profile()) :: [String.t()]
```

From `lib/lockspire/security/policy.ex`:
```elixir
@spec validate_key_compliance(SigningKey.t(), :fapi_2_0_security | :none) :: :ok | {:error, term()}
```

From `lib/lockspire/storage/key_store.ex`:
```elixir
@callback list_publishable_keys() :: {:ok, [SigningKey.t()]} | {:error, store_error()}
@callback fetch_active_signing_key() :: {:ok, SigningKey.t() | nil} | {:error, store_error()}
@callback activate_signing_key(integer(), DateTime.t()) ::
            {:ok, %{activated_key: SigningKey.t(), retiring_key: SigningKey.t() | nil}}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Narrow the canonical FAPI algorithm truth and compliance helpers</name>
  <files>lib/lockspire/protocol/security_profile.ex, lib/lockspire/security/policy.ex, test/lockspire/protocol/security_profile_test.exs, test/lockspire/protocol/security_policy_test.exs</files>
  <behavior>
    - Test 1: `allowed_signing_algorithms(:fapi_2_0_security)` returns exactly `["ES256", "PS256"]` per D-02.
    - Test 2: `allowed_signing_algorithms(:none)` remains the broader non-FAPI list so mixed-mode stays intact per D-07.
    - Test 3: `validate_key_compliance/2` accepts `ES256` and `PS256` keys with valid strength under FAPI.
    - Test 4: `validate_key_compliance/2` rejects `RS256`, `EdDSA`, weak RSA, and unsupported curves under FAPI with typed errors.
  </behavior>
  <action>
    Update `lib/lockspire/protocol/security_profile.ex` so the exported algorithm truth source is the only place that defines the FAPI subset and it returns only `ES256`/`PS256` per D-02/D-03. Update `lib/lockspire/security/policy.ex` to consume that truth instead of maintaining a divergent list, preserving non-FAPI behavior while rejecting `RS256` and `EdDSA` under FAPI-effective posture per D-01/D-05. Extend the two protocol/security tests so the narrowed subset and typed rejection reasons are pinned before later plans consume the seam.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/security_profile_test.exs test/lockspire/protocol/security_policy_test.exs</automated>
  </verify>
  <done>There is one canonical FAPI signing-algorithm truth source, FAPI resolves to ES256/PS256 only, and compliance helpers reject non-compliant algorithms and weak key material with stable typed failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce fail-fast FAPI key posture in admin and repository seams</name>
  <files>lib/lockspire/admin/keys.ex, lib/lockspire/storage/key_store.ex, lib/lockspire/storage/ecto/repository.ex, test/lockspire/admin/keys_test.exs</files>
  <behavior>
    - Test 1: `Admin.Keys.generate_key/1` defaults to a FAPI-compliant signing key when the server profile is `:fapi_2_0_security` per D-06/D-09.
    - Test 2: `Admin.Keys.activate_key/2` rejects stored signing keys whose algorithm or strength is not FAPI-eligible with remediation-friendly errors per D-10.
    - Test 3: publishable/active key selection cannot surface non-compliant signing keys for FAPI-effective use even if legacy rows remain in storage per D-07/D-08.
  </behavior>
  <action>
    Make `lib/lockspire/admin/keys.ex` and `lib/lockspire/storage/ecto/repository.ex` fail fast at the existing key lifecycle seams instead of allowing unusable posture to survive into runtime. Reuse existing durable state per D-08; do not invent a quarantine system. If repository contracts need profile-aware options or helper functions, add them in `lib/lockspire/storage/key_store.ex` and repository code so later runtime surfaces can ask for FAPI-eligible keys without hand-rolled filtering. Keep remediation explicit in returned errors per D-10.
  </action>
  <verify>
    <automated>mix test test/lockspire/admin/keys_test.exs test/lockspire/protocol/security_policy_test.exs</automated>
  </verify>
  <done>FAPI-effective key generation, activation, and active/publishable selection paths are fail-fast and never surface RS256/EdDSA or weak keys for runtime use, while mixed-mode legacy rows can still remain durably stored outside FAPI-effective use.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/admin -> key lifecycle | Operator input can attempt to activate or generate non-compliant signing posture |
| durable key store -> protocol runtime | Stored legacy keys must not leak into FAPI-effective runtime selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | T | `lib/lockspire/admin/keys.ex` | mitigate | Enforce canonical ES256/PS256-only compliance at generation/activation boundaries per D-06/D-09. |
| T-42-02 | E | `lib/lockspire/storage/ecto/repository.ex` | mitigate | Prevent non-compliant stored keys from becoming active/publishable for FAPI-effective paths. |
| T-42-03 | I | `lib/lockspire/security/policy.ex` | mitigate | Return typed compliance errors without leaking private key material; preserve redaction conventions. |
</threat_model>

<verification>
- Run the targeted unit/admin tests from `42-VALIDATION.md` for Task `42-01-01`.
- Confirm no code path under FAPI-effective state still returns `EdDSA` or `RS256` from the canonical policy.
</verification>

<success_criteria>
- Canonical FAPI signing truth is exported from one protocol-owned source and reused by key/compliance seams.
- FAPI-effective key lifecycle paths fail fast on RS256, EdDSA, weak RSA, and unsupported curves.
- Mixed-mode durable storage remains possible without leaking non-compliant keys into FAPI-effective runtime posture.
</success_criteria>

<output>
After completion, create `.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-01-SUMMARY.md`
</output>
