---
phase: 42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep
plan: 03
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/lockspire/admin/server_policy.ex
  - lib/lockspire/admin/clients.ex
  - lib/lockspire/storage/ecto/client_record.ex
  - lib/lockspire/storage/ecto/repository.ex
  - test/lockspire/admin/server_policy_test.exs
  - test/lockspire/admin/clients_test.exs
  - test/lockspire/storage/ecto/client_record_test.exs
  - test/lockspire/storage/repository_test.exs
autonomous: true
requirements: [FAPI-04]
must_haves:
  truths:
    - "Turning on global FAPI mode fails clearly if the server’s effective signing posture is not ready per D-09."
    - "Opting a client into FAPI from the admin path fails clearly when either its algorithm metadata is incompatible or the server lacks a compliant active/publishable signing posture, while opting a client out to `:none` still preserves the mixed-mode escape hatch per D-07/D-09."
    - "The admin/client readiness contract is durable and reusable so later DCR and registration-management surfaces can fail with the same remediation semantics instead of inventing a parallel policy system, per D-06/D-10."
  artifacts:
    - path: "lib/lockspire/admin/server_policy.ex"
      provides: "Server-policy mutation path that checks FAPI readiness before allowing strict mode"
      exports: ["put_security_profile/1"]
    - path: "lib/lockspire/admin/clients.ex"
      provides: "Admin client update validation for FAPI-incompatible metadata and signing-readiness dependencies with remediation-friendly errors"
      exports: ["update_client/2"]
    - path: "lib/lockspire/storage/ecto/client_record.ex"
      provides: "Durable client metadata validation contract for FAPI-effective admin updates and downstream registration reuse"
      contains: "update_changeset"
  key_links:
    - from: "lib/lockspire/admin/server_policy.ex"
      to: "lib/lockspire/storage/ecto/repository.ex"
      via: "server-profile updates consult durable key posture before enabling FAPI"
      pattern: "put_security_profile|get_server_policy"
    - from: "lib/lockspire/admin/clients.ex"
      to: "lib/lockspire/storage/ecto/repository.ex"
      via: "client FAPI opt-in consults runtime signing readiness before persisting strict mode"
      pattern: "security_profile|fetch_active_signing_key|list_publishable_keys"
    - from: "lib/lockspire/storage/ecto/client_record.ex"
      to: "lib/lockspire/admin/clients.ex"
      via: "durable FAPI metadata validation is shared by admin updates and later registration surfaces"
      pattern: "update_changeset|security_profile|id_token_signed_response_alg"
---

<objective>
Close the server-policy and admin-client rejection paths so FAPI-effective state cannot be enabled or persisted into an already-broken algorithm posture.

Purpose: D-06 through D-10 require durable rejection at server-policy and client-metadata boundaries, while preserving the Phase 41 mixed-mode semantics. This plan establishes the reusable readiness and remediation contract first, leaving DCR and registration-management HTTP wiring to a smaller dependent plan so the slice stays repo-shaped and does not mix admin/storage work with controller-facing registration work.

Output:
- Global and admin-client FAPI opt-in paths that fail fast on incompatible key/client metadata or missing signing readiness.
- Durable client metadata validation and repository readiness helpers that downstream registration surfaces can reuse without reopening the admin slice.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-CONTEXT.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-RESEARCH.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-PATTERNS.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-VALIDATION.md
@.planning/phases/41-fapi-2-0-profile-configuration/41-CONTEXT.md
@.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-01-PLAN.md

<interfaces>
From `lib/lockspire/admin/server_policy.ex`:
```elixir
@spec put_security_profile(atom() | String.t()) ::
        {:ok, ServerPolicy.t()} | {:error, [error_detail()]} | {:error, term()}
```

From `lib/lockspire/admin/clients.ex`:
```elixir
@spec update_client(String.t(), map() | keyword()) ::
        {:ok, Client.t()} | {:error, [error_detail()]} | {:error, term()}
```

From `lib/lockspire/storage/ecto/client_record.ex`:
```elixir
field(:security_profile, Ecto.Enum, values: [:inherit, :fapi_2_0_security, :none], default: :inherit)
field(:id_token_signed_response_alg, Ecto.Enum, values: [:RS256, :ES256, :EdDSA])
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add server-policy and operator fail-fast checks for FAPI readiness</name>
  <files>lib/lockspire/admin/server_policy.ex, lib/lockspire/storage/ecto/repository.ex, test/lockspire/admin/server_policy_test.exs, test/lockspire/storage/repository_test.exs</files>
  <behavior>
    - Test 1: enabling global `:fapi_2_0_security` fails when there is no compliant active/publishable signing posture, with a remediation-friendly error per D-09/D-10.
    - Test 2: enabling global FAPI succeeds once a compliant signing posture exists.
    - Test 3: the readiness check proves both active-key and publishable-key posture, so FAPI is blocked when runtime could not sign or discovery/JWKS could not publish compliant material per D-09/D-11.
  </behavior>
  <action>
    Extend the server-policy mutation path so enabling global FAPI validates durable signing readiness before persisting the stricter mode. Reuse the Plan 01 policy helpers plus repository truth about active and publishable signing keys rather than inventing separate heuristics. Return structured errors that tell operators whether they need to generate, publish, or activate a compliant key, per D-09/D-10. Keep this logic inside Lockspire’s embedded-library boundary; do not introduce hosted-service assumptions or certificate-based auth work.
  </action>
  <verify>
    <automated>mix test test/lockspire/admin/server_policy_test.exs test/lockspire/storage/repository_test.exs</automated>
  </verify>
  <done>Global FAPI opt-in is blocked until the server has compliant active and publishable signing posture, and the failure path points operators at the concrete next fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject incompatible admin client metadata and expose a reusable readiness contract</name>
  <files>lib/lockspire/admin/clients.ex, lib/lockspire/storage/ecto/client_record.ex, lib/lockspire/storage/ecto/repository.ex, test/lockspire/admin/clients_test.exs, test/lockspire/storage/ecto/client_record_test.exs, test/lockspire/storage/repository_test.exs</files>
  <behavior>
    - Test 1: `Admin.Clients.update_client/2` rejects `security_profile: :fapi_2_0_security` when `id_token_signed_response_alg` is incompatible or no compliant active/publishable signing posture exists, per D-06/D-09.
    - Test 2: the durable validation path in `client_record` / repository exposes the same incompatible-metadata and missing-signing-readiness outcomes that downstream registration surfaces will reuse, per D-06/D-10.
    - Test 3: non-FAPI clients may still store legacy metadata such as RS256/EdDSA without being upgraded into FAPI-effective state per D-07/D-08.
    - Test 4: returned admin errors explain the remediation path, such as activating a compliant signing key, changing the client profile to `:none`, or rotating to a supported algorithm, per D-10.
  </behavior>
  <action>
    Tighten the existing admin client update, durable changeset, and repository readiness paths so FAPI-effective client state cannot persist incompatible algorithm metadata or opt into strict mode without a compliant runtime signing posture. Use the current admin/repository/update_changeset flow and make the resulting validation/remediation contract reusable by downstream registration surfaces; do not add a parallel policy system. Keep the repo truthful about unsupported areas: since Lockspire does not yet support mTLS or `private_key_jwt`, the validations and messages should steer maintainers toward the shipped ES256/PS256 + DPoP path, not imply unbuilt auth methods are acceptable.
  </action>
  <verify>
    <automated>mix test test/lockspire/admin/clients_test.exs test/lockspire/storage/ecto/client_record_test.exs test/lockspire/storage/repository_test.exs</automated>
  </verify>
  <done>Admin client updates fail fast when FAPI-effective state would be broken by incompatible metadata or missing signing readiness, mixed-mode legacy storage remains possible for `:none` clients, and the durable validation contract is ready for DCR reuse in the next plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/admin -> server policy | A strict-mode toggle can create an unusable or falsely compliant posture if not validated |
| operator/DCR client metadata -> durable client state | Client algorithm metadata is partially user-controlled and can conflict with FAPI posture |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-07 | T | `lib/lockspire/admin/server_policy.ex` | mitigate | Block FAPI enablement when durable signing posture is non-compliant; return fix-oriented errors. |
| T-42-08 | E | `lib/lockspire/admin/clients.ex` / `lib/lockspire/storage/ecto/client_record.ex` | mitigate | Reject incompatible client metadata and missing-signing-readiness opt-ins before they become FAPI-effective. |
| T-42-09 | T | `lib/lockspire/protocol/registration.ex` / `lib/lockspire/protocol/registration_management.ex` | mitigate | Reuse the same readiness and algorithm validation in DCR surfaces so remote clients cannot sneak broken FAPI metadata past admin guards. |
| T-42-10 | R | operator and DCR remediation surfaces | mitigate | Use structured, actionable error reasons so failures are auditable and not hand-waved as “strict mode broken.” |
</threat_model>

<verification>
- Run the targeted admin/storage tests from `42-VALIDATION.md` for Task `42-03-01`.
- Confirm mixed-mode `:none` overrides still pass while server-policy and admin client FAPI opt-in paths reject incompatible algorithm posture and missing signing readiness.
</verification>

<success_criteria>
- Global and admin-client FAPI opt-in paths are fail-fast and remediation-friendly.
- Mixed-mode non-FAPI durable rows remain allowed without becoming FAPI-effective.
- The readiness/remediation contract is ready for DCR reuse without reopening storage/admin files in the dependent registration slice.
</success_criteria>

<output>
After completion, create `.planning/phases/42-fapi-2-0-advanced-cryptography-and-oidf-test-suite-prep/42-03-SUMMARY.md`
</output>
