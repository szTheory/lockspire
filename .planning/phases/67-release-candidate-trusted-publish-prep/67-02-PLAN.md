---
phase: 67-release-candidate-trusted-publish-prep
plan: 67-02
type: execute
wave: 2
depends_on:
  - 67-01
files_modified:
  - .github/workflows/release.yml
  - .github/actions/release-please/action.yml
  - docs/maintainer-release.md
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
must_haves:
  truths:
    - "Maintainer runbook and checked-in workflow define every repo-owned step required before protected publish approval."
    - "Trusted publish prep preserves the expected root release tag behavior and one protected `hex-publish` boundary."
    - "Phase 67 stays preparatory: runbook and workflow changes do not claim Hex-public visibility, install-from-Hex proof, or other Phase 68 outcomes."
  artifacts:
    - path: .github/workflows/release.yml
      provides: "checked-in trusted publish lane contract and protected boundary"
    - path: .github/actions/release-please/action.yml
      provides: "repo-controlled root release tag behavior and Release Please invocation"
    - path: docs/maintainer-release.md
      provides: "maintainer release guidance that remains subordinate to the canonical support contract"
  key_links:
    - from: .github/workflows/release.yml
      to: .github/actions/release-please/action.yml
      via: "workflow still calls the repo-controlled root release action"
      pattern: "uses: \\./\\.github/actions/release-please|tag_name|release_created"
    - from: docs/maintainer-release.md
      to: .github/workflows/release.yml
      via: "maintainer checklist references the same trusted publish boundary and release candidate steps the workflow enforces"
      pattern: "hex-publish|Release candidate checklist|workflow_dispatch|supported-surface"
---

<objective>
Turn the Phase 67 release-prep posture into an executable drift fence that prevents support-contract widening, undocumented release steps, and premature publish claims.

Purpose: REL-02 requires the trusted publish lane to be executable without undocumented manual steps. After 67-01 aligns the checked-in release candidate artifacts, this plan makes the maintainer runbook and checked-in workflow explicit about the release-candidate checklist, root-tag expectations, repo-controlled Release Please path, recovery-only replay semantics, and the exact point where protected-environment proof begins.
Output: a trusted-publish-prep runbook and workflow contract that define every non-secret release step while staying subordinate to the canonical support contract and distinct from Phase 68 publish verification.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/67-release-candidate-trusted-publish-prep/67-RESEARCH.md
@.planning/phases/67-release-candidate-trusted-publish-prep/67-01-PLAN.md
@mix.exs
@.release-please-manifest.json
@CHANGELOG.md
@.github/workflows/release.yml
@.github/actions/release-please/action.yml
@docs/maintainer-release.md
@docs/supported-surface.md
@.github/workflows/release.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the trusted publish lane and release-candidate checklist explicit</name>
  <files>.github/workflows/release.yml, .github/actions/release-please/action.yml, docs/maintainer-release.md</files>
  <read_first>
    - .github/workflows/release.yml
    - .github/actions/release-please/action.yml
    - docs/maintainer-release.md
    - docs/supported-surface.md
    - mix.exs
    - .release-please-manifest.json
    - CHANGELOG.md
  </read_first>
  <action>Update the checked-in trusted release lane and maintainer guide only where needed so a maintainer can review, merge, and enter the protected publish lane without undocumented manual steps. Preserve the current evidence buckets: repo-owned proof, GitHub settings proof, and workflow-run proof. Add or tighten an explicit release-candidate checklist in `docs/maintainer-release.md` that names the required file reviews, root-tag expectations, repo-owned commands, and the exact point where proof stops being checked-in and starts being protected-environment evidence. Keep `workflow_dispatch` recovery-only, keep `.github/workflows/release.yml` pointed at the repo-controlled composite action, and explicitly preserve the root release tag contract rather than leaving it implicit.</action>
  <acceptance_criteria>
    - `.github/workflows/release.yml` still defines one protected `hex-publish` lane with recovery-only manual replay semantics
    - `.github/actions/release-please/action.yml` remains the repo-controlled Release Please shim and preserves root release outputs such as `tag_name`
    - `docs/maintainer-release.md` contains an explicit release-candidate checklist, evidence buckets, and root-tag expectations
    - the maintainer guide remains subordinate to `docs/supported-surface.md` and does not create a second support matrix
  </acceptance_criteria>
  <verify>
    <automated>rg 'environment: hex-publish|workflow_dispatch:|recovery_reason|recovery_ref|mix release.preflight|mix hex.publish --yes' .github/workflows/release.yml && rg 'uses: composite|node-version: "24"|node \\.github/actions/release-please/runtime/index.js|tag_name|release_created' .github/actions/release-please/action.yml && rg 'Repo-owned proof|GitHub settings proof|Workflow-run proof|Release candidate checklist|tag target|supported-surface' docs/maintainer-release.md</automated>
  </verify>
  <done>The trusted lane and maintainer runbook are explicit, reviewable, and still narrow to repo-owned release prep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer runbook/workflow -> protected publish lane | Ambiguity here creates undocumented release steps or false assumptions about authenticated proof |
| checked-in release config -> root tag target | If root release outputs drift, the release candidate can target the wrong tag even with matching version files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-02-01 | R | maintainer checklist | mitigate | Make repo-owned steps, evidence buckets, and tag-target expectations explicit in `docs/maintainer-release.md` |
| T-67-02-02 | T | trusted publish workflow | mitigate | Keep `.github/workflows/release.yml` pinned to one protected `hex-publish` lane with recovery-only replay semantics |
| T-67-02-03 | T | root release outputs | mitigate | Preserve repo-controlled Release Please output behavior, including root `tag_name`, so tag-target truth stays explicit |
</threat_model>

<verification>
Completion requires the maintainer guide and trusted workflow to prove the Phase 67 boundary in checked-in artifacts. If release steps remain implicit, tag-target behavior remains unspoken, or the workflow blurs repo-owned proof with protected proof, this plan is not done.
</verification>

<success_criteria>
- The maintainer guide names every repo-owned pre-publish step and where protected-environment proof begins.
- The trusted publish lane remains one narrow, repo-controlled boundary with explicit root-tag behavior.
- Phase 67 stays strictly pre-publish and does not absorb Phase 68 verification claims.
- Release-only workflow/runbook cleanup can no longer rely on maintainer folklore.
</success_criteria>

<output>
After completion, create `.planning/phases/67-release-candidate-trusted-publish-prep/67-02-SUMMARY.md`
</output>
