---
phase: 67-release-candidate-trusted-publish-prep
plan: 67-03
type: execute
wave: 3
depends_on:
  - 67-01
  - 67-02
files_modified:
  - test/lockspire/release_readiness_contract_test.exs
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
must_haves:
  truths:
    - "Release-candidate metadata, workflow guidance, and support-boundary posture fail fast in repo-owned tests if they drift."
    - "New release prep language cannot turn protected-environment proof or Hex-public proof into a checked-in claim."
    - "`docs/supported-surface.md` remains the canonical support contract even after release-prep updates."
  artifacts:
    - path: test/lockspire/release_readiness_contract_test.exs
      provides: "repo-owned drift fences for release candidate, trusted publish, and support-boundary truth"
  key_links:
    - from: test/lockspire/release_readiness_contract_test.exs
      to: mix.exs
      via: "package metadata and version-chain assertions"
      pattern: "Supported surface|1\\.0\\.0|package"
    - from: test/lockspire/release_readiness_contract_test.exs
      to: docs/maintainer-release.md
      via: "evidence-boundary and checklist assertions"
      pattern: "GitHub settings proof|Workflow-run proof|release candidate"
    - from: test/lockspire/release_readiness_contract_test.exs
      to: .github/workflows/release.yml
      via: "workflow boundary and pre-publish validation assertions"
      pattern: "hex-publish|workflow_dispatch|Validate"
---

<objective>
Turn the Phase 67 release-prep contract into executable drift fences.

Purpose: After release artifacts and maintainer guidance change, the repo needs one place that fails fast if metadata, workflow boundaries, or support-contract language drift. This plan keeps Phase 67 preparatory and repo-owned by encoding its claims in the existing release-readiness contract test instead of relying on memory.
Output: expanded `test/lockspire/release_readiness_contract_test.exs` coverage for release-candidate truth, trusted publish prep, and support-boundary drift.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/67-release-candidate-trusted-publish-prep/67-RESEARCH.md
@.planning/phases/65-release-truth-support-contract-reconciliation/65-03-SUMMARY.md
@mix.exs
@docs/maintainer-release.md
@docs/supported-surface.md
@README.md
@SECURITY.md
@.github/workflows/release.yml
@release-please-config.json
@.release-please-manifest.json
@CHANGELOG.md
@test/lockspire/release_readiness_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend release-candidate and package-metadata assertions</name>
  <files>test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - test/lockspire/release_readiness_contract_test.exs
    - mix.exs
    - release-please-config.json
    - .release-please-manifest.json
    - CHANGELOG.md
  </read_first>
  <action>Expand `test/lockspire/release_readiness_contract_test.exs` so it enforces the new Phase 67 `REL-01` contract: the test should assert the release-candidate version chain stays aligned, any new `mix.exs` package links point to canonical docs, and no checked-in release artifact introduces `-rc`, alternate package naming, or package metadata drift. Keep the assertions string- or structure-based against checked-in files only; do not attempt live Hex, tag, or GitHub API verification in this phase.</action>
  <acceptance_criteria>
    - the test covers any new `mix.exs` package links or docs pointers added by Plan 67-01
    - the test fails on `1.0.0-rc`, alternate package-name drift, or mismatched checked-in release versions
    - the test remains repo-owned and does not require external credentials or network access
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <done>The release-candidate artifact chain and package metadata now have executable repo-owned drift fences.</done>
</task>

<task type="auto">
  <name>Task 2: Add evidence-boundary and support-contract drift fences</name>
  <files>test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - test/lockspire/release_readiness_contract_test.exs
    - docs/maintainer-release.md
    - docs/supported-surface.md
    - README.md
    - SECURITY.md
    - .github/workflows/release.yml
  </read_first>
  <action>Extend the same contract test so `REL-02` and `REL-03` stay enforceable. Add assertions that the maintainer guide keeps `docs/supported-surface.md` canonical, names the release-candidate checklist, preserves separate repo-owned / GitHub-settings / workflow-run evidence buckets, and explicitly avoids Phase 68 claims such as successful publish proof, Hex-public verification, or install-from-Hex proof. Assert the workflow still keeps protected steps inside `hex-publish` and that release-prep docs do not become a second support matrix or broaden the embedded-library contract.</action>
  <acceptance_criteria>
    - the test covers the new release-candidate checklist and evidence-boundary wording
    - the test fails if checked-in docs start claiming publish success, Hex-public proof, or a second support contract
    - the workflow boundary assertions continue to pin `hex-publish`, recovery-only manual dispatch, and protected preflight/publish commands
  </acceptance_criteria>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <done>Phase 67 release-prep guidance is guarded by executable tests instead of maintainer memory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in release guidance -> future edits | Without tests, later doc or workflow edits can silently change the release story or support boundary |
| repo-owned truth -> Phase 68 protected/public proof | Test coverage must prevent Phase 67 from smuggling Phase 68 publish verification claims into checked-in docs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03-01 | T | release readiness contract test | mitigate | Add assertions for release-candidate metadata, package links, and workflow validation introduced in Plans 67-01 and 67-02 |
| T-67-03-02 | R | release guidance and support-boundary posture | mitigate | Add explicit failures for shadow support matrices, Hex-public proof claims, and protected-proof claims in checked-in docs |
</threat_model>

<verification>
Run the focused release-readiness contract test after every change. Completion requires the test to catch metadata drift, workflow-boundary drift, and support-contract drift without touching live publish systems.
</verification>

<success_criteria>
- Phase 67 release-prep claims are encoded in repo-owned tests.
- Protected-environment proof and Hex-public proof remain impossible to claim accidentally in checked-in docs.
- The canonical supported-surface contract remains the only public support matrix.
</success_criteria>

<output>
After completion, create `.planning/phases/67-release-candidate-trusted-publish-prep/67-03-SUMMARY.md`
</output>
