---
phase: 67-release-candidate-trusted-publish-prep
plan: 67-01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - .release-please-manifest.json
  - CHANGELOG.md
  - release-please-config.json
autonomous: true
requirements:
  - REL-01
  - REL-02
must_haves:
  truths:
    - "The checked-in release candidate version story stays singular across package metadata, manifest state, changelog posture, and trusted-lane wiring."
    - "The checked-in release candidate includes explicit root-tag expectations so version, changelog, tag target, and package metadata stay aligned."
  artifacts:
    - path: mix.exs
      provides: "authoritative package version and package metadata for the release candidate"
    - path: .release-please-manifest.json
      provides: "Release Please tracked version aligned with package truth"
    - path: CHANGELOG.md
      provides: "release-candidate changelog posture aligned with the shipped `1.0.0` surface"
  key_links:
    - from: mix.exs
      to: .release-please-manifest.json
      via: "same semantic version and package posture"
      pattern: "1\\.0\\.0"
    - from: release-please-config.json
      to: .release-please-manifest.json
      via: "root-component release metadata and version state remain aligned for the expected tag target"
      pattern: "package-name|release-type|\"\\.\": \"1\\.0\\.0\""
---

<objective>
Turn Lockspire's already-aligned `1.0.0` repo posture into one explicit, reviewable release-candidate contract before any real publish proof is attempted.

Purpose: REL-01 requires maintainers to cut one coherent release candidate whose version, changelog, tag target, and package metadata all describe the same embedded surface. The repo already has the core `1.0.0` truth chain, but execution still needs a final artifact pass that keeps package metadata, changelog language, manifest state, and root-tag expectations aligned.
Output: a checked-in release-candidate artifact chain across metadata, changelog, manifest state, and tag-target expectations that can be reviewed before entering the trusted publish lane.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/67-release-candidate-trusted-publish-prep/67-RESEARCH.md
@.planning/phases/65-release-truth-support-contract-reconciliation/65-01-SUMMARY.md
@.planning/phases/65-release-truth-support-contract-reconciliation/65-02-SUMMARY.md
@.planning/phases/65-release-truth-support-contract-reconciliation/65-03-SUMMARY.md
@mix.exs
@.release-please-manifest.json
@CHANGELOG.md
@release-please-config.json
@docs/supported-surface.md
@README.md
@SECURITY.md
@test/lockspire/release_readiness_contract_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and tighten the checked-in release-candidate artifact chain</name>
  <files>mix.exs, .release-please-manifest.json, CHANGELOG.md, release-please-config.json</files>
  <read_first>
    - mix.exs
    - .release-please-manifest.json
    - CHANGELOG.md
    - release-please-config.json
    - .planning/phases/67-release-candidate-trusted-publish-prep/67-RESEARCH.md
    - test/lockspire/release_readiness_contract_test.exs
  </read_first>
  <action>Review the checked-in `1.0.0` release-candidate chain and make any necessary narrow corrections so `mix.exs`, `.release-please-manifest.json`, `CHANGELOG.md`, and `release-please-config.json` all describe the same shipped embedded-library surface. Keep the changelog factual and avoid any `-rc`, "GA-ready", preview-regression, or shadow-package wording. If package metadata in `mix.exs` or related release configuration still leaves ambiguity about what Phoenix maintainers are about to install, fix that ambiguity in the smallest truthful way possible without claiming a real publish has happened yet.</action>
  <acceptance_criteria>
    - `mix.exs`, `.release-please-manifest.json`, and the newest `CHANGELOG.md` heading still agree on one version story
    - `release-please-config.json` stays manifest-driven and Elixir-specific
    - no checked-in artifact introduces release-candidate-only version labels or wording that conflicts with the public `1.0.0` support contract
    - any package metadata edits remain inside the existing embedded-library support boundary
  </acceptance_criteria>
  <verify>
    <automated>VERSION=$(sed -n 's/.*version: "\\([0-9][0-9.]*\\)".*/\\1/p' mix.exs | head -1) && MANIFEST=$(sed -n 's/.*"\\.": "\\([0-9][0-9.]*\\)".*/\\1/p' .release-please-manifest.json | head -1) && test -n "$VERSION" && test "$VERSION" = "$MANIFEST" && rg "^## \\[$VERSION\\]" CHANGELOG.md && ! rg '1\\.0\\.0-rc|GA-ready|preview release candidate' CHANGELOG.md</automated>
  </verify>
  <done>The checked-in release candidate artifacts tell one factual pre-publish story.</done>
</task>

<task type="auto">
  <name>Task 2: Make root-tag and Hex-facing package expectations explicit in checked-in artifacts</name>
  <files>mix.exs, .release-please-manifest.json, CHANGELOG.md, release-please-config.json</files>
  <read_first>
    - mix.exs
    - .release-please-manifest.json
    - CHANGELOG.md
    - release-please-config.json
    - docs/supported-surface.md
    - test/lockspire/release_readiness_contract_test.exs
  </read_first>
  <action>Make the release-candidate artifact chain explicit about root-tag expectations and Hex-facing package truth. Tighten any checked-in metadata, changelog wording, or release configuration needed so the expected root release tag still resolves to the same `1.0.0` story as `mix.exs`, `.release-please-manifest.json`, and `CHANGELOG.md`. Keep this work repo-owned and structural: validate the root-component release posture from checked-in configuration and outputs, but do not attempt a live tag creation or any publish action.</action>
  <acceptance_criteria>
    - checked-in artifacts explicitly preserve root-component release posture for the expected `1.0.0` tag target
    - `release-please-config.json` still names the root package correctly and does not introduce alternate component or package naming
    - the artifact chain remains aligned with `docs/supported-surface.md` and does not imply a second package or product shape
    - no task attempts live tag creation or authenticated publish work
  </acceptance_criteria>
  <verify>
    <automated>rg '"package-name": "lockspire"|"\.": "1\.0\.0"|release-type": "elixir"' release-please-config.json .release-please-manifest.json && rg '^## \[1\.0\.0\]' CHANGELOG.md && ! rg '1\.0\.0-rc|component tag|alternate package' CHANGELOG.md release-please-config.json .release-please-manifest.json</automated>
  </verify>
  <done>The checked-in release candidate now names the same root-tag and package truth across all artifact files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in release artifacts -> maintainer release decision | Drift here can create a false release candidate or conflicting package truth |
| checked-in release artifacts -> root tag target | If tag-target expectations drift from metadata or changelog truth, the release candidate becomes ambiguous before publish starts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01-01 | R | version and metadata chain | mitigate | Keep `mix.exs`, `.release-please-manifest.json`, `CHANGELOG.md`, and release config on one factual `1.0.0` story with no RC shadow labels |
| T-67-01-02 | T | root tag target | mitigate | Keep root-component release metadata and manifest state aligned so tag-target expectations match the checked-in `1.0.0` artifact chain |
| T-67-01-03 | R | package identity | mitigate | Reject alternate package/component naming or RC shadow labels that would create a second release story |
</threat_model>

<verification>
Completion requires one coherent checked-in release-candidate contract across package metadata, changelog posture, manifest state, and root-tag expectations. Do not treat the plan as complete if the files still leave the tag target ambiguous or imply a real publish already happened.
</verification>

<success_criteria>
- The checked-in release candidate still resolves to one factual `1.0.0` story.
- The release-candidate artifact chain makes tag-target expectations explicit and consistent.
- The plan stays strictly pre-publish and repo-owned.
</success_criteria>

<output>
After completion, create `.planning/phases/67-release-candidate-trusted-publish-prep/67-01-SUMMARY.md`
</output>
