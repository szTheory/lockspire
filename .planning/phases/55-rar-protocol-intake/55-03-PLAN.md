---
phase: 55-rar-protocol-intake
plan: 03
type: execute
wave: 3
depends_on: [55-02]
files_modified:
  - test/integration/phase55_rar_intake_e2e_test.exs
autonomous: true
requirements: [RAR-01]

must_haves:
  truths:
    - "Clients can successfully push RAR details via PAR and see them in the Interaction record"
    - "Direct Authorization requests with small RAR JSON are accepted and parsed"
    - "Direct Authorization requests with RAR JSON > 2048 characters are rejected"
  artifacts:
    - path: "test/integration/phase55_rar_intake_e2e_test.exs"
      provides: "End-to-end integration tests for RAR intake"
  key_links:
    - from: "test/integration/phase55_rar_intake_e2e_test.exs"
      to: "lib/lockspire/protocol/authorization_request.ex"
      via: "Integration test"
    - from: "test/integration/phase55_rar_intake_e2e_test.exs"
      to: "lib/lockspire/protocol/pushed_authorization_request.ex"
      via: "Integration test"
---

<objective>
Verify the end-to-end Rich Authorization Request (RAR) intake flow via PAR and direct Authorization requests.

Purpose: Ensure the implementation correctly handles varied RAR intake scenarios and enforces security boundaries.
Output: Integration test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-rar-protocol-intake/55-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create integration tests for RAR intake</name>
  <files>test/integration/phase55_rar_intake_e2e_test.exs</files>
  <action>
    Create a new integration test file that covers:
    1. **PAR Intake**: Client pushes `authorization_details` (JSON array) to `/par`. Verify 201 Created and that the stored PAR record contains the decoded maps.
    2. **Interaction Carry-through**: Follow the PAR `request_uri` to `/authorize`. Verify the created `Interaction` record contains the correct `authorization_details`.
    3. **Direct Intake (Success)**: Client sends small `authorization_details` directly to `/authorize`. Verify acceptance and storage in `Interaction`.
    4. **Direct Intake (Rejection)**: Client sends `authorization_details` string > 2048 chars directly to `/authorize`. Verify `invalid_request` redirect error.
    5. **Malformed JSON**: Verify that invalid JSON in `authorization_details` results in an appropriate protocol error.
  </action>
  <verify>
    <automated>mix test test/integration/phase55_rar_intake_e2e_test.exs</automated>
  </verify>
  <done>Integration tests pass for all RAR intake scenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Test Environment | Verified flows ensure security mitigations (length check) are effective. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | Information Disclosure | Error Responses | mitigate | Ensure length rejection doesn't leak internal state, only standard OIDC error codes. |
</threat_model>

<verification>
Run the new integration test suite.
</verification>

<success_criteria>
- All integration tests pass.
- Coverage includes PAR, Direct, and Error scenarios for RAR.
</success_criteria>

<output>
After completion, create .planning/phases/55-rar-protocol-intake/55-03-SUMMARY.md
</output>
