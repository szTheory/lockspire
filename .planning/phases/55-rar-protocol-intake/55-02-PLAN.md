---
phase: 55-rar-protocol-intake
plan: 02
type: execute
wave: 2
depends_on: [55-01]
files_modified:
  - lib/lockspire/protocol/authorization_request.ex
  - lib/lockspire/protocol/pushed_authorization_request.ex
  - lib/lockspire/protocol/authorization_flow.ex
autonomous: true
requirements: [RAR-01]

must_haves:
  truths:
    - "Lockspire parses authorization_details from binary JSON in direct requests"
    - "Lockspire parses authorization_details from already-decoded lists (e.g. Request Objects)"
    - "Lockspire rejects direct GET/POST requests if authorization_details exceeds 2048 characters"
    - "RAR details are carried from PAR into the Interaction state"
  artifacts:
    - path: "lib/lockspire/protocol/authorization_request.ex"
      provides: "RAR validation and parsing logic"
    - path: "lib/lockspire/protocol/pushed_authorization_request.ex"
      provides: "PAR persistence with RAR"
    - path: "lib/lockspire/protocol/authorization_flow.ex"
      provides: "Interaction creation with RAR"
  key_links:
    - from: "lib/lockspire/protocol/authorization_request.ex"
      to: "lib/lockspire/protocol/pushed_authorization_request.ex"
      via: "Validated struct"
    - from: "lib/lockspire/protocol/authorization_request.ex"
      to: "lib/lockspire/protocol/authorization_flow.ex"
      via: "Validated struct"
---

<objective>
Implement the protocol logic for RAR intake, including parsing, URI length protection, and cross-pipeline persistence.

Purpose: Ensure the `authorization_details` parameter is correctly validated at the protocol edge and preserved through the PAR and Authorization flows.
Output: Updated protocol modules with RAR support.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/55-rar-protocol-intake/55-RESEARCH.md
@.planning/phases/55-rar-protocol-intake/55-01-SUMMARY.md

<interfaces>
From lib/lockspire/protocol/authorization_request.ex:
```elixir
defmodule Validated do
  defstruct [
    :client, :client_id, :redirect_uri, :nonce, :state, :max_age,
    :code_challenge, :code_challenge_method,
    scopes: [], resources: [], prompt: [], auth_time_requested?: false
  ]
end

def validate(params)
def validate_pushed(params, %Client{} = client)
```

From lib/lockspire/protocol/pushed_authorization_request.ex:
```elixir
def push(request)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AuthorizationRequest validation and parsing</name>
  <files>lib/lockspire/protocol/authorization_request.ex</files>
  <action>
    - Add `:authorization_details` field to `Lockspire.Protocol.AuthorizationRequest.Validated` struct (default `[]`).
    - Implement `validate_authorization_details/2` (args: `params`, `pushed?` boolean).
    - If `pushed?` is false and `authorization_details` is a string, check if length > 2048. If so, return `:redirect_error` with `invalid_request` (reason: `:authorization_details_too_large`).
    - Decode `authorization_details` using `Jason.decode!/1` (or `Jason.decode/1`) if it's a binary.
    - If it's already a list (e.g. from a Request Object), use it as is.
    - Ensure it's a list of maps; otherwise return error.
    - Update `validate_with_client/3` to call `validate_authorization_details/2`.
    - Update `build_validated/8` and its callers to include `authorization_details`.
    - Update `pushed_request_to_params/1` to include `"authorization_details" => request.authorization_details`.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/authorization_request_test.exs</automated>
  </verify>
  <done>AuthorizationRequest correctly parses and validates RAR details.</done>
</task>

<task type="auto">
  <name>Task 2: Update PAR protocol logic and persistence</name>
  <files>lib/lockspire/protocol/pushed_authorization_request.ex</files>
  <action>
    - Update `persist_pushed_request/3` to take `authorization_details` from the `validated` struct and pass it to `PushedAuthorizationRequestState.issue/2`.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/pushed_authorization_request_test.exs</automated>
  </verify>
  <done>PAR successfully persists RAR details.</done>
</task>

<task type="auto">
  <name>Task 3: Update AuthorizationFlow to carry RAR to Interaction</name>
  <files>lib/lockspire/protocol/authorization_flow.ex</files>
  <action>
    - Update `build_interaction/5` to include `authorization_details: validated.authorization_details` in the created `Interaction` struct.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/authorization_flow_test.exs</automated>
  </verify>
  <done>AuthorizationFlow carries RAR details into the Interaction state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted JSON input crosses here. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-02 | Denial of Service | AuthorizationRequest | mitigate | Implement 2048 character limit on raw authorization_details parameter. |
| T-55-03 | Tampering | Jason.decode | mitigate | Use Jason for robust JSON parsing; subsequent phases will validate content schema. |
</threat_model>

<verification>
Verify that RAR details move from PAR params to DB, and then from /authorize params to Interaction DB.
</verification>

<success_criteria>
- Large RAR in direct requests are rejected.
- Valid RAR JSON is parsed into list of maps.
- Interaction state contains the RAR details.
</success_criteria>

<output>
After completion, create .planning/phases/55-rar-protocol-intake/55-02-SUMMARY.md
</output>
