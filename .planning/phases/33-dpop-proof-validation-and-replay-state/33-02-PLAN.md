---
phase: "33"
plan: "02"
type: execute
wave: 2
depends_on:
  - 33-01
files_modified:
  - lib/lockspire/domain/dpop_replay.ex
  - lib/lockspire/storage/dpop_replay_store.ex
  - lib/lockspire/storage/ecto/dpop_replay_record.ex
  - priv/repo/migrations/20260428150000_add_lockspire_dpop_replay_state.exs
  - lib/lockspire/storage/ecto/repository.ex
  - lib/lockspire/protocol/token_exchange.ex
  - test/lockspire/storage/ecto/repository_dpop_replay_test.exs
  - test/lockspire/protocol/token_exchange_test.exs
autonomous: true
requirements:
  - DPoP-02
  - DPoP-03
must_haves:
  truths:
    - "Replay detection for DPoP proofs is durable across nodes and restarts for the supported replay window"
    - "First proof use is accepted and a replayed proof is rejected deterministically through repository-owned typed outcomes"
    - "Replay-state storage stays narrow and phase-local rather than leaking generic cache or infrastructure assumptions into Lockspire"
  artifacts:
    - path: "lib/lockspire/storage/dpop_replay_store.ex"
      provides: "Narrow replay persistence contract"
      contains: "@callback"
    - path: "lib/lockspire/storage/ecto/dpop_replay_record.ex"
      provides: "Durable DPoP replay-state schema"
      contains: "schema"
    - path: "lib/lockspire/storage/ecto/repository.ex"
      provides: "Repository-backed replay insertion and deterministic replay classification"
      contains: "def record_dpop_proof"
    - path: "lib/lockspire/protocol/token_exchange.ex"
      provides: "Protocol-owned replay rejection seam on a real Lockspire request path"
      contains: "invalid_dpop_proof"
    - path: "test/lockspire/storage/ecto/repository_dpop_replay_test.exs"
      provides: "Executable proof for replay acceptance, rejection, and expiry-window behavior"
      contains: "invalid_dpop_proof"
  key_links:
    - from: "lib/lockspire/storage/dpop_replay_store.ex"
      to: "lib/lockspire/storage/ecto/repository.ex"
      via: "typed record/check replay callbacks"
      pattern: "record_dpop_proof"
    - from: "lib/lockspire/storage/ecto/repository.ex"
      to: "lib/lockspire/storage/ecto/dpop_replay_record.ex"
      via: "durable replay insert/query behavior"
      pattern: "lockspire_dpop_replay"
    - from: "lib/lockspire/protocol/token_exchange.ex"
      to: "lib/lockspire/storage/dpop_replay_store.ex"
      via: "proof validation plus replay recording on a real token-endpoint seam"
      pattern: "record_dpop_proof"
---

<objective>
Add the durable replay-state contract and repository-backed enforcement that makes DPoP proof reuse detectable across nodes and process restarts.

Purpose: Phase 33 cannot make trustworthy DPoP claims if replay protection depends on request-local or process-local memory. This plan creates the Postgres/Ecto truth later token and resource work will depend on.
Output: a first-class replay domain/store/schema, additive migration, repository enforcement, and repository tests proving deterministic replay behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-dpop-proof-validation-and-replay-state/33-RESEARCH.md
@.planning/phases/33-dpop-proof-validation-and-replay-state/33-PATTERNS.md
@.planning/phases/33-dpop-proof-validation-and-replay-state/33-01-PLAN.md
@lib/lockspire/storage/ecto/repository.ex
@lib/lockspire/storage/ecto/token_record.ex
@test/lockspire/storage/ecto/repository_device_authorization_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a first-class DPoP replay domain, store contract, and schema</name>
  <files>lib/lockspire/domain/dpop_replay.ex, lib/lockspire/storage/dpop_replay_store.ex, lib/lockspire/storage/ecto/dpop_replay_record.ex, priv/repo/migrations/20260428150000_add_lockspire_dpop_replay_state.exs, test/lockspire/storage/ecto/repository_dpop_replay_test.exs</files>
  <read_first>
    - lib/lockspire/storage/ecto/token_record.ex
    - lib/lockspire/storage/device_authorization_store.ex
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-RESEARCH.md
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-PATTERNS.md
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-VALIDATION.md
  </read_first>
  <behavior>
    - DPoP replay state has an explicit durable shape rather than being hidden in ad hoc maps.
    - Replay identity and expiry are stored explicitly enough to support deterministic repository outcomes.
    - The migration is additive and does not weaken existing token or client tables.
  </behavior>
  <action>
    Add a first-class DPoP replay state model and persistence seam.

    Recommended durable state:
    - a unique replay key or proof fingerprint
    - `jti`
    - normalized `htm`
    - normalized `htu`
    - `jkt`
    - `seen_at`
    - `expires_at`

    Create a dedicated behavior for replay persistence so later protocol code can depend on typed outcomes instead of raw Ecto/database details.

    Add an additive migration that creates the replay table and unique index needed for conflict-as-replay semantics. Keep the table narrow, durable, and easy to prune by TTL later.

    In the same task, add a minimal repository test scaffold so the verify command is immediately valid after Task 1 instead of depending on a later task.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/storage/ecto/repository_dpop_replay_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'defmodule Lockspire.Domain.DpopReplay' lib/lockspire/domain/dpop_replay.ex` succeeds.
    - `grep -q '@callback record_dpop_proof' lib/lockspire/storage/dpop_replay_store.ex` succeeds.
    - `grep -q 'schema \"lockspire_dpop_replay' lib/lockspire/storage/ecto/dpop_replay_record.ex` succeeds.
    - `grep -q 'create table(:lockspire_dpop_replay' priv/repo/migrations/20260428150000_add_lockspire_dpop_replay_state.exs` succeeds.
    - `test -f test/lockspire/storage/ecto/repository_dpop_replay_test.exs` succeeds.
    - `grep -q 'unique_index' priv/repo/migrations/20260428150000_add_lockspire_dpop_replay_state.exs` succeeds.
  </acceptance_criteria>
  <done>Lockspire now has a first-class durable schema and storage contract for DPoP replay state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement repository-backed replay acceptance, rejection, and TTL-window proof</name>
  <files>lib/lockspire/storage/ecto/repository.ex, test/lockspire/storage/ecto/repository_dpop_replay_test.exs, lib/lockspire/protocol/token_exchange.ex, test/lockspire/protocol/token_exchange_test.exs</files>
  <read_first>
    - lib/lockspire/storage/ecto/repository.ex
    - test/lockspire/storage/ecto/repository_device_authorization_test.exs
    - lib/lockspire/protocol/token_exchange.ex
    - test/lockspire/protocol/token_exchange_test.exs
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-RESEARCH.md
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-PATTERNS.md
  </read_first>
  <behavior>
    - The first presentation of a proof in-window is accepted.
    - A duplicate presentation of the same replay identity in-window is rejected deterministically.
    - Replay enforcement remains durable and typed even after process restarts or multi-node races.
    - A real Lockspire protocol seam rejects replayed proofs as `invalid_dpop_proof` without pulling full Phase 34 token binding forward.
  </behavior>
  <action>
    Implement repository-backed replay recording in `Lockspire.Storage.Ecto.Repository`.

    Use the repo's existing transaction/locking/typed-outcome discipline. Prefer a narrow API such as `record_dpop_proof/2` or `record_dpop_proof/3` that receives normalized proof identity plus timing context and returns typed results like:
    - `{:ok, :accepted}`
    - `{:ok, :replay}`
    - `{:error, term()}`

    Keep cleanup strategy pragmatic for Phase 33. The repository logic should rely on `expires_at` / TTL semantics for correctness, not on a background worker. If the implementation opportunistically prunes expired rows, keep that as a local storage detail rather than a new milestone dependency.

    Add repository tests proving:
    - first proof accepted
    - immediate duplicate rejected
    - persisted replay record survives a fresh repository call
    - expired replay state no longer blocks a new proof after the supported window

    Also add the first composed protocol seam for replay rejection on a real Lockspire request path. The narrow target is the token-endpoint preflight inside `Lockspire.Protocol.TokenExchange`:
    - preserve bearer-default behavior when DPoP is not required
    - accept a validated non-replayed proof when the caller supplies one
    - reject missing or replayed proofs with public `invalid_dpop_proof` once a later policy-resolved client path marks DPoP as required

    Keep this seam preflight-only. Do not yet implement Phase 34 token binding, `cnf` persistence, or DPoP token-type responses here.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/storage/ecto/repository_dpop_replay_test.exs test/lockspire/protocol/token_exchange_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def record_dpop_proof' lib/lockspire/storage/ecto/repository.ex` succeeds.
    - `grep -q 'accepted' test/lockspire/storage/ecto/repository_dpop_replay_test.exs` succeeds.
    - `grep -q 'replay' test/lockspire/storage/ecto/repository_dpop_replay_test.exs` succeeds.
    - `grep -q 'expires_at' test/lockspire/storage/ecto/repository_dpop_replay_test.exs` succeeds.
    - `grep -q 'invalid_dpop_proof' test/lockspire/protocol/token_exchange_test.exs` succeeds.
    - `grep -q 'invalid_dpop_proof' lib/lockspire/protocol/token_exchange.ex` succeeds.
    - `mix test test/lockspire/storage/ecto/repository_dpop_replay_test.exs test/lockspire/protocol/token_exchange_test.exs` passes.
  </acceptance_criteria>
  <done>Replay detection is now durable, typed, and repository-proven for the supported DPoP replay window.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Protocol validator -> durable replay store | Validated proof identity crosses into persistence that must resist duplicate use and concurrency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-04 | Denial of Service | replay-state growth | mitigate | Keep replay rows TTL-bounded and store only the minimum durable proof identity needed for replay enforcement. |
| T-33-05 | Tampering | duplicate proof presentation | mitigate | Use repository-owned conflict/replay semantics backed by unique durable state rather than in-memory caches. |
| T-33-06 | Repudiation | replay classification | mitigate | Return deterministic typed outcomes and back them with executable repository tests. |
</threat_model>

<verification>
MIX_ENV=test mix test test/lockspire/storage/ecto/repository_dpop_replay_test.exs test/lockspire/protocol/token_exchange_test.exs
</verification>

<success_criteria>
Lockspire can deterministically reject replayed DPoP proofs within a supported replay window using durable Postgres-backed state, and that rejection is visible on a real Lockspire protocol seam instead of existing only as an isolated storage primitive.
</success_criteria>

<output>
After completion, create `.planning/phases/33-dpop-proof-validation-and-replay-state/33-02-SUMMARY.md`
</output>
