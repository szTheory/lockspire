---
phase: "33"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/dpop.ex
  - test/lockspire/protocol/dpop_test.exs
  - test/support/jar_test_helpers.ex
autonomous: true
requirements:
  - DPoP-01
  - DPoP-02
must_haves:
  truths:
    - "Lockspire has one protocol-owned DPoP validator that parses proof JWTs, verifies signatures, validates JOSE/header semantics, and checks method/URI/time claims"
    - "The validated proof key can be converted into a stable RFC 7638 thumbprint for later token binding"
    - "Phase 33 establishes a reusable proof-validation seam without collapsing Phase 34 token issuance work into this plan"
  artifacts:
    - path: "lib/lockspire/protocol/dpop.ex"
      provides: "DPoP proof parser/validator and JWK thumbprint helpers"
      contains: "def validate_proof"
    - path: "test/lockspire/protocol/dpop_test.exs"
      provides: "Executable proof for valid, invalid, stale, and malformed DPoP proofs"
      contains: "invalid_dpop_proof"
    - path: "test/support/jar_test_helpers.ex"
      provides: "Reusable JOSE key/signing helpers for DPoP protocol tests"
      contains: "JOSE.JWK.generate_key"
  key_links:
    - from: "lib/lockspire/protocol/dpop.ex"
      to: "lib/lockspire/protocol/jar.ex"
      via: "shared JOSE decode/verify/claims-check pipeline style"
      pattern: "verify_strict"
    - from: "lib/lockspire/protocol/dpop.ex"
      to: "lib/lockspire/domain/token.ex"
      via: "future cnf.jkt binding seam"
      pattern: "cnf"
---

<objective>
Add the protocol-owned DPoP proof validator and thumbprint helpers that later token issuance and protected-resource work can trust.

Purpose: Phase 33 should first establish a single reusable source of truth for DPoP proof parsing, signature verification, method/URI validation, and JWK thumbprint derivation before later phases wire it into grant execution and protected-resource consumption.
Output: a new `Lockspire.Protocol.DPoP` module with focused protocol tests and helper support for creating proof JWTs in test code.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-dpop-proof-validation-and-replay-state/33-RESEARCH.md
@.planning/phases/33-dpop-proof-validation-and-replay-state/33-PATTERNS.md
@lib/lockspire/protocol/jar.ex
@lib/lockspire/protocol/request_object.ex
@lib/lockspire/domain/token.ex
@test/support/jar_test_helpers.ex

<interfaces>
From `lib/lockspire/protocol/jar.ex`:
```elixir
@spec decode(String.t()) :: {:ok, t()} | {:error, :invalid_jwt}
@spec verify_signature(String.t(), Client.t()) :: {:ok, t()} | {:error, term()}
@spec validate_claims(t(), keyword()) :: :ok | {:error, term()}
```

From `lib/lockspire/domain/token.ex`:
```elixir
@type t :: %__MODULE__{
  cnf: map() | nil
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DPoP proof decode, verification, and thumbprint helpers</name>
  <files>lib/lockspire/protocol/dpop.ex, test/support/jar_test_helpers.ex, test/lockspire/protocol/dpop_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/jar.ex
    - lib/lockspire/protocol/request_object.ex
    - test/support/jar_test_helpers.ex
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-RESEARCH.md
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-PATTERNS.md
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-VALIDATION.md
  </read_first>
  <behavior>
    - A compact DPoP JWT can be decoded into protected-header and claim maps without leaking raw internals into caller code.
    - Signature verification accepts only asymmetric JOSE algorithms and rejects `alg=none`.
    - The validated public JWK yields a stable RFC 7638 thumbprint suitable for later `cnf.jkt` persistence.
  </behavior>
  <action>
    Add `Lockspire.Protocol.DPoP` as the single protocol-owned validator for DPoP proof JWTs.

    Model it after the repo's JAR pipeline:
    - decode the compact JWT
    - inspect protected JOSE headers
    - require `typ` to be the DPoP form the repo chooses for strictness
    - require an embedded public `jwk`
    - reject private-key material and symmetric keys in the header
    - verify the signature with JOSE using an explicit asymmetric allowlist

    Add helpers that derive the public JWK thumbprint in RFC 7638 format. Keep the returned value immediately usable as later `cnf["jkt"]` token state.

    Extend `test/support/jar_test_helpers.ex` only as needed so tests can generate proof JWTs and key material without copying JOSE setup inline.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/protocol/dpop_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'def validate_proof' lib/lockspire/protocol/dpop.ex` succeeds.
    - `grep -q 'def thumbprint' lib/lockspire/protocol/dpop.ex` succeeds.
    - `grep -q 'alg=none' test/lockspire/protocol/dpop_test.exs` succeeds.
    - `grep -q 'thumbprint' test/lockspire/protocol/dpop_test.exs` succeeds.
    - `mix test test/lockspire/protocol/dpop_test.exs` passes.
  </acceptance_criteria>
  <done>Lockspire now has a reusable DPoP parser/validator and thumbprint helper for later grant and resource enforcement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove required claim checks against method, URI, and bounded issuance time</name>
  <files>lib/lockspire/protocol/dpop.ex, test/lockspire/protocol/dpop_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/dpop.ex
    - test/lockspire/protocol/dpop_test.exs
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-RESEARCH.md
  </read_first>
  <behavior>
    - Valid proofs pass only when `htm`, `htu`, `iat`, and `jti` satisfy the request context.
    - Stale, future, mismatched-method, mismatched-URI, and missing-claim proofs fail with typed reasons.
    - The validator remains protocol-focused and does not yet own replay persistence or token issuance.
  </behavior>
  <action>
    Extend `Lockspire.Protocol.DPoP` with explicit claim validation against a caller-supplied request context that includes:
    - request method
    - canonical absolute target URI
    - current time
    - allowed clock skew / max age

    Keep the validator generic enough that later `TokenExchange` and `userinfo` work can call it without controller-specific knowledge.

    Add focused tests for:
    - valid proof
    - invalid signature
    - bad `htm`
    - bad `htu`
    - stale `iat`
    - future `iat` beyond allowed skew
    - missing `jti`
    - missing `jwk`

    Return typed protocol failure reasons that later phases can map into RFC-shaped public errors such as `invalid_dpop_proof`.
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/protocol/dpop_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'htm' test/lockspire/protocol/dpop_test.exs` succeeds.
    - `grep -q 'htu' test/lockspire/protocol/dpop_test.exs` succeeds.
    - `grep -q 'iat' test/lockspire/protocol/dpop_test.exs` succeeds.
    - `grep -q 'jti' test/lockspire/protocol/dpop_test.exs` succeeds.
    - `grep -q 'invalid_dpop_proof' test/lockspire/protocol/dpop_test.exs` succeeds.
    - `mix test test/lockspire/protocol/dpop_test.exs` passes.
  </acceptance_criteria>
  <done>DPoP proof validation now has executable protocol proof for the required method, URI, and claim semantics Phase 33 promises.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public token/resource request -> protocol validator | Untrusted JWT material and HTTP context cross into Lockspire proof validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Spoofing | proof signature validation | mitigate | Verify only against the embedded public JWK with an explicit asymmetric algorithm allowlist; reject `alg=none` and invalid signatures. |
| T-33-02 | Tampering | `htm` / `htu` / `iat` claims | mitigate | Compare claims against protocol-owned request context and bounded issuance-time rules. |
| T-33-03 | Information Disclosure | proof parsing failures | mitigate | Return typed internal reasons without exposing raw JWT internals or unredacted proof contents. |
</threat_model>

<verification>
MIX_ENV=test mix test test/lockspire/protocol/dpop_test.exs
</verification>

<success_criteria>
Lockspire has a reusable protocol validator that proves possession of a DPoP key against a concrete request context and can derive the proof-key thumbprint for later binding work.
</success_criteria>

<output>
After completion, create `.planning/phases/33-dpop-proof-validation-and-replay-state/33-01-SUMMARY.md`
</output>
