---
phase: "33"
plan: "03"
type: execute
wave: 3
depends_on:
  - 33-01
  - 33-02
files_modified:
  - lib/lockspire/domain/client.ex
  - lib/lockspire/storage/ecto/client_record.ex
  - lib/lockspire/domain/server_policy.ex
  - lib/lockspire/storage/ecto/server_policy_record.ex
  - priv/repo/migrations/20260428153000_add_dpop_policy_fields.exs
  - lib/lockspire/admin/clients.ex
  - lib/lockspire/admin/server_policy.ex
  - lib/lockspire/protocol/dpop_policy.ex
  - test/lockspire/admin/clients_test.exs
  - test/lockspire/admin/server_policy_test.exs
  - test/lockspire/protocol/dpop_policy_test.exs
autonomous: true
requirements:
  - DPoP-04
must_haves:
  truths:
    - "Client and server policy state can explicitly opt into DPoP mode without changing existing bearer clients by default"
    - "Effective DPoP mode resolution is explicit, durable, and test-proven rather than inferred from metadata or runtime guesses"
    - "Phase 33 ends with a clean policy seam that later token-issuance and userinfo work can consume"
  artifacts:
    - path: "lib/lockspire/domain/client.ex"
      provides: "Explicit per-client DPoP mode state"
      contains: "dpop"
    - path: "lib/lockspire/domain/server_policy.ex"
      provides: "Explicit global DPoP default state"
      contains: "dpop"
    - path: "lib/lockspire/protocol/dpop_policy.ex"
      provides: "Effective DPoP mode resolution"
      contains: "resolve_effective_policy"
    - path: "test/lockspire/protocol/dpop_policy_test.exs"
      provides: "Executable proof for bearer-default and explicit opt-in behavior"
      contains: "inherit"
  key_links:
    - from: "lib/lockspire/protocol/dpop_policy.ex"
      to: "lib/lockspire/domain/server_policy.ex"
      via: "global default resolution"
      pattern: "ServerPolicy"
    - from: "lib/lockspire/protocol/dpop_policy.ex"
      to: "lib/lockspire/domain/client.ex"
      via: "client override resolution"
      pattern: "Client"
---

<objective>
Add explicit, durable DPoP policy state on server and client records and prove that bearer remains the default unless operators opt a client into DPoP mode.

Purpose: Phase 33 must finish with a trustworthy policy seam for later grant and resource enforcement. Existing clients cannot silently change behavior, and later phases need a deterministic way to decide when DPoP is required.
Output: explicit client/server DPoP fields, normalized admin write paths, a policy resolver module, and tests proving bearer-default behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/33-dpop-proof-validation-and-replay-state/33-RESEARCH.md
@.planning/phases/33-dpop-proof-validation-and-replay-state/33-PATTERNS.md
@.planning/phases/33-dpop-proof-validation-and-replay-state/33-01-PLAN.md
@.planning/phases/33-dpop-proof-validation-and-replay-state/33-02-PLAN.md
@lib/lockspire/domain/client.ex
@lib/lockspire/domain/server_policy.ex
@lib/lockspire/storage/ecto/client_record.ex
@lib/lockspire/storage/ecto/server_policy_record.ex
@lib/lockspire/admin/clients.ex
@lib/lockspire/admin/server_policy.ex
@lib/lockspire/protocol/par_policy.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit durable DPoP mode fields to client and server policy state</name>
  <files>lib/lockspire/domain/client.ex, lib/lockspire/storage/ecto/client_record.ex, lib/lockspire/domain/server_policy.ex, lib/lockspire/storage/ecto/server_policy_record.ex, priv/repo/migrations/20260428153000_add_dpop_policy_fields.exs, lib/lockspire/admin/clients.ex, lib/lockspire/admin/server_policy.ex, test/lockspire/admin/clients_test.exs, test/lockspire/admin/server_policy_test.exs</files>
  <read_first>
    - lib/lockspire/domain/client.ex
    - lib/lockspire/storage/ecto/client_record.ex
    - lib/lockspire/domain/server_policy.ex
    - lib/lockspire/storage/ecto/server_policy_record.ex
    - lib/lockspire/admin/clients.ex
    - lib/lockspire/admin/server_policy.ex
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-RESEARCH.md
  </read_first>
  <behavior>
    - Existing records default to bearer behavior after the migration/code change.
    - DPoP mode is represented by explicit fields, not free-form metadata.
    - Invalid policy values are rejected through the same normalized admin pathways used elsewhere in Lockspire.
  </behavior>
  <action>
    Extend the `Client` and `ServerPolicy` durable models with explicit DPoP mode state.

    Follow the repo's existing enum/normalized-field style rather than metadata blobs. Preserve current behavior by making bearer the effective default for existing clients and existing server policy rows.

    Add an additive migration `20260428153000_add_dpop_policy_fields.exs` that persists the new client/server policy fields with bearer-safe defaults/backfill for existing rows.

    Widen the corresponding Ecto records and changesets, plus the admin write paths, so these fields can be validated and persisted through normal repo-owned APIs.

    Add tests proving:
    - existing defaults remain bearer
    - invalid field values are rejected
    - explicit DPoP values round-trip through admin/persistence paths
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/admin/clients_test.exs test/lockspire/admin/server_policy_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'dpop' lib/lockspire/domain/client.ex` succeeds.
    - `grep -q 'dpop' lib/lockspire/storage/ecto/client_record.ex` succeeds.
    - `grep -q 'dpop' lib/lockspire/domain/server_policy.ex` succeeds.
    - `grep -q 'dpop' lib/lockspire/storage/ecto/server_policy_record.ex` succeeds.
    - `test -f priv/repo/migrations/20260428153000_add_dpop_policy_fields.exs` succeeds.
    - `mix test test/lockspire/admin/clients_test.exs test/lockspire/admin/server_policy_test.exs` passes.
  </acceptance_criteria>
  <done>Lockspire now has durable explicit DPoP mode state on the client and server policy seams.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add effective DPoP policy resolution with bearer-default proof</name>
  <files>lib/lockspire/admin/clients.ex, lib/lockspire/admin/server_policy.ex, lib/lockspire/protocol/dpop_policy.ex, test/lockspire/protocol/dpop_policy_test.exs</files>
  <read_first>
    - lib/lockspire/protocol/par_policy.ex
    - lib/lockspire/admin/clients.ex
    - lib/lockspire/admin/server_policy.ex
    - .planning/phases/33-dpop-proof-validation-and-replay-state/33-PATTERNS.md
  </read_first>
  <behavior>
    - Global default plus client override produce a deterministic effective DPoP mode.
    - Clients that inherit from a bearer-default server policy remain bearer without any migration-time special cases.
    - Later token-issuance work can resolve effective DPoP mode through one protocol seam.
  </behavior>
  <action>
    Add a small resolver module, likely `Lockspire.Protocol.DpopPolicy`, modeled after the repo's PAR policy resolver.

    The resolver should make these properties explicit:
    - server default controls the inherited mode
    - client override can narrow or opt into DPoP
    - effective mode is deterministic and easy for later `TokenExchange` work to consume

    Extend admin-layer tests and add dedicated resolver tests proving:
    - bearer-default + inherit => bearer
    - dpop-default + inherit => dpop
    - explicit client override wins where allowed
    - malformed values do not silently coerce into insecure behavior
  </action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/protocol/dpop_policy_test.exs test/lockspire/admin/clients_test.exs test/lockspire/admin/server_policy_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'defmodule Lockspire.Protocol.DpopPolicy' lib/lockspire/protocol/dpop_policy.ex` succeeds.
    - `grep -q 'resolve_effective_policy' lib/lockspire/protocol/dpop_policy.ex` succeeds.
    - `grep -q 'inherit' test/lockspire/protocol/dpop_policy_test.exs` succeeds.
    - `grep -q 'bearer' test/lockspire/protocol/dpop_policy_test.exs` succeeds.
    - `grep -q 'dpop' test/lockspire/protocol/dpop_policy_test.exs` succeeds.
    - `mix test test/lockspire/protocol/dpop_policy_test.exs test/lockspire/admin/clients_test.exs test/lockspire/admin/server_policy_test.exs` passes.
  </acceptance_criteria>
  <done>Lockspire now has a deterministic DPoP policy seam that preserves bearer-by-default while allowing explicit opt-in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator/admin inputs -> durable policy state | Human-controlled configuration crosses into protocol-critical client/server behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-07 | Elevation of Privilege | implicit DPoP enablement | mitigate | Keep bearer as the explicit default and require validated durable state for DPoP opt-in. |
| T-33-08 | Tampering | malformed policy writes | mitigate | Validate and normalize values through admin-layer APIs and Ecto-backed record constraints. |
| T-33-09 | Repudiation | ambiguous effective mode | mitigate | Centralize resolution in one protocol module with dedicated tests. |
</threat_model>

<verification>
MIX_ENV=test mix test test/lockspire/protocol/dpop_policy_test.exs test/lockspire/admin/clients_test.exs test/lockspire/admin/server_policy_test.exs
</verification>

<success_criteria>
Lockspire has explicit client and server DPoP policy state with a deterministic resolver, and every unchanged client continues to behave as a bearer client by default.
</success_criteria>

<output>
After completion, create `.planning/phases/33-dpop-proof-validation-and-replay-state/33-03-SUMMARY.md`
</output>
