---
phase: 100-sender-constraint-end-to-end-proof
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - test/lockspire/release_readiness_contract_test.exs
  - test/lockspire/plug/verify_token_test.exs
autonomous: true
requirements: [BIND-03, BIND-01, BIND-02]
must_haves:
  truths:
    - "release_readiness_contract_test asserts all four RECIPE-01 canonical pipeline sites order VerifyToken -> EnforceSenderConstraints -> RequireToken, failing genuinely if Enforce/Require are transposed in any file"
    - "The ordering clause reuses the existing extract_canonical_pipeline!/2 helper and the four-file {path, kind} iteration — no parallel extraction path"
    - "A list-valued aud (the shape AccessTokenSigner emits) passes VerifyToken's audience matcher when audience: is set (the A1 spike de-risks BIND-01/02 before Wave 2)"
  artifacts:
    - path: "test/lockspire/release_readiness_contract_test.exs"
      provides: "BIND-03/D-05 ordering clause + byte_offset/2 private helper"
      contains: "EnforceSenderConstraints"
    - path: "test/lockspire/plug/verify_token_test.exs"
      provides: "A1 list-aud acceptance assertion"
      contains: "aud"
  key_links:
    - from: "test/lockspire/release_readiness_contract_test.exs"
      to: "the four RECIPE-01 canonical pipeline files"
      via: "extract_canonical_pipeline!/2 over the four {path, kind} tuples"
      pattern: "extract_canonical_pipeline!"
---

<objective>
Add the BIND-03 contract-test layer (D-05, defense-in-depth mirroring Phase 98's both-mechanisms VERIFIER-06 posture): a `release_readiness_contract_test` clause asserting all four RECIPE-01 canonical pipeline sites order `VerifyToken → EnforceSenderConstraints → RequireToken`. Also run the A1 Wave-0 spike: prove a list-valued `aud` (the wire shape `AccessTokenSigner` emits) passes `VerifyToken`'s audience matcher, de-risking the BIND-01/02 integration proof in Wave 2.

Purpose: Make the misordered/omitting pipeline unreachable in the blessed path at the structural layer too — even if the runtime guard (Plan 01) were bypassed, a shipped pipeline that transposed the plugs would fail CI. The A1 spike confirms the single open assumption (signer emits list `aud`, phase81 used string `aud`) before the integration test relies on it.

Output: 1 contract-test clause + 1 private helper; 1 audience-matcher assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-CONTEXT.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-RESEARCH.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-PATTERNS.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-VALIDATION.md

<interfaces>
<!-- Live contracts. Verified against the tree this session. -->

From test/lockspire/release_readiness_contract_test.exs:
  - Four path module attrs (lines 71, 84, 88, 92): @protect_phoenix_api_routes_path, @adoption_demo_router_path, @install_template_router_path, @adoption_smoke_script_path.
  - extract_canonical_pipeline!/2 (lines 140-157): the SINGLE extraction path — reads the `# BEGIN/END LOCKSPIRE_PROTECTED_PIPELINE` markers and normalizes per `kind`. Do NOT add a parallel extractor (D-05).
  - The audience-substring clause (lines 761-791) is the structural twin: same four-file {path, kind} iteration (kinds: :elixir_in_markdown_fence, :elixir, :elixir_in_commented_heredoc, :python_commented), same extract_canonical_pipeline!/2 call, per-file assertion loop.
  - There is NO byte_offset/2 helper in this file (grep-confirmed absent) — it must be added.

From the four RECIPE-01 canonical pipeline files (ALL already order Verify→Enforce→Require today, so the clause passes against current content and does NOT ripple the content hash):
  - docs/protect-phoenix-api-routes.md (lines 16-23)
  - examples/adoption_demo/lib/adoption_demo_web/router.ex (lines 23-30)
  - priv/templates/lockspire.install/router.ex (lines 11-18, commented heredoc)
  - scripts/demo/adoption_smoke.py (lines 244-251, python comment)

From the VerifyToken audience-matcher path (read-only):
  - lib/lockspire/plug/verify_token.ex audience check (Phase 98-hardened). RFC 9068/7519 allow aud as list-or-string.
  - test/lockspire/plug/verify_token_test.exs generate_key_and_token/2 (lines 39-91) is the KeyCache publish-then-sign recipe; existing audience tests are the analog for the A1 assertion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the D-05 BIND-03 pipeline-ordering clause + byte_offset/2 helper to the contract test</name>
  <files>test/lockspire/release_readiness_contract_test.exs</files>
  <read_first>
    - test/lockspire/release_readiness_contract_test.exs (the four path attrs 71-92; extract_canonical_pipeline!/2 140-157; the audience clause 761-791 — the structural twin to mirror)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-PATTERNS.md (the release_readiness_contract_test.exs section, lines 473-518 — the clause shape, the four-tuple iteration, and the byte_offset/2 helper to add)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-RESEARCH.md (Code Examples: D-05 contract ordering clause, lines 378-398)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-CONTEXT.md (D-05, lines 27; canonical_refs lines 92-96)
  </read_first>
  <action>
    Per D-05, add ONE new test clause to release_readiness_contract_test.exs asserting all four RECIPE-01 sites order `VerifyToken → EnforceSenderConstraints → RequireToken`. Reuse the EXISTING four-file `{path, kind}` iteration VERBATIM from the audience clause (lines 761-770): the tuples `{@protect_phoenix_api_routes_path, :elixir_in_markdown_fence}`, `{@adoption_demo_router_path, :elixir}`, `{@install_template_router_path, :elixir_in_commented_heredoc}`, `{@adoption_smoke_script_path, :python_commented}`. For each, call `extract_canonical_pipeline!(path, kind)` (the SINGLE extraction path — do NOT add a parallel one, D-05), then assert byte-offset ordering: offset of `"Lockspire.Plug.VerifyToken"` < offset of `"Lockspire.Plug.EnforceSenderConstraints"` < offset of `"Lockspire.Plug.RequireToken"`, with a failure message naming the file (`Path.relative_to_cwd(path)`).
    Add the missing private helper `byte_offset/2` (grep-confirmed absent): `:binary.match(bytes, needle)` returning the start offset on `{start, _len}` and `flunk("expected #{needle} in canonical pipeline block")` on `:nomatch`. (A `~s` multiline-regex alternative is acceptable per Claude's-Discretion provided it genuinely fails if Enforce/Require are transposed — offset is simpler and self-evidently correct; prefer it.) The anti-cheat lens: the assertion MUST genuinely fail if Enforce and Require are transposed in any of the four files — do NOT write a regex that would pass on wrong order.
    This clause is satisfied by current content (all four already correctly ordered) so it passes immediately and does NOT change the four-file content hash.
  </action>
  <verify>
    <automated>mix test test/lockspire/release_readiness_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lockspire/release_readiness_contract_test.exs` exits 0
    - Source assertion: the new clause calls `extract_canonical_pipeline!/2` (no new/parallel extraction function added) and iterates the same four `{path, kind}` tuples the audience clause uses
    - Source assertion: a `byte_offset/2` (or equivalent) helper exists and the ordering assertion is `v < e and e < r`
    - Anti-cheat: manually reasoning about the assertion confirms it would FAIL if EnforceSenderConstraints and RequireToken were swapped in any file (offset comparison, not a permissive regex)
    - The four-file content-hash clause (existing) still passes — this clause adds no content drift
  </acceptance_criteria>
  <done>The contract test asserts canonical-pipeline ordering across all four RECIPE-01 sites via the shared extractor + a byte_offset helper; the file is green.</done>
</task>

<task type="auto">
  <name>Task 2: A1 Wave-0 spike — prove VerifyToken's audience matcher accepts a list-valued aud</name>
  <files>test/lockspire/plug/verify_token_test.exs</files>
  <read_first>
    - test/lockspire/plug/verify_token_test.exs (generate_key_and_token/2 39-91 KeyCache publish-then-sign recipe; the existing audience-acceptance tests for the assertion analog)
    - lib/lockspire/plug/verify_token.ex (the audience-matcher path — Phase 98-hardened; confirm it handles list aud)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-RESEARCH.md (Pitfall 6 lines 328-332; A1 lines 421, 428-431; Open Questions 1)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-PATTERNS.md (the A1 watch note, lines 353)
  </read_first>
  <action>
    Per the A1 open assumption: the signer emits `aud => ["billing-api"]` (LIST), while phase81's hand-signed happy path used a STRING `aud`. Add ONE assertion (reusing the existing `generate_key_and_token/2` KeyCache recipe and the existing audience-test structure in this file) that mints/crafts a token whose `aud` claim is a single-element LIST and runs it through `VerifyToken` with `audience: "billing-api"` set — asserting it is NOT rejected with `invalid_audience` (i.e. `conn.assigns.access_token.error` is nil on the audience dimension; reaching past the audience check is sufficient). This de-risks BIND-01/02 in Wave 2.
    If the matcher REJECTS list `aud` (unexpected — Phase 98 hardening makes acceptance very likely): do NOT silently work around it. Record the finding in the SUMMARY and flag that Plan 03 (BIND-01/02) must either mint with a string `aud` the matcher unwraps or pass an explicit `audiences:` option — this is a tiny prerequisite for Wave 2, surfaced now rather than discovered mid-integration. Do NOT change verify_token.ex behavior (read-only for Phase 100).
  </action>
  <verify>
    <automated>mix test test/lockspire/plug/verify_token_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/lockspire/plug/verify_token_test.exs` exits 0
    - Behavior: an assertion exists proving a list-valued `aud` token with matching `audience:` is accepted (not `invalid_audience`)
    - If list `aud` is NOT accepted: the SUMMARY explicitly records the BIND-01/02 mitigation (string aud or `audiences:` option) — no silent workaround
    - verify_token.ex is unchanged (read-only)
  </acceptance_criteria>
  <done>The A1 assumption is resolved with an automated assertion; BIND-01/02's list-aud reliance is either confirmed safe or its mitigation is recorded for Plan 03; file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped canonical pipeline declarations → adopters | The four RECIPE-01 files are copied by adopters / the install template; a transposed plug order would ship a bypass precondition |
| client → VerifyToken audience check | Untrusted `aud` claim shape (list vs string) crosses the audience matcher |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-05 | Elevation / Tampering | The four RECIPE-01 canonical pipeline blocks | mitigate | D-05 contract clause: CI fails loudly if any shipped pipeline transposes EnforceSenderConstraints/RequireToken — structural defense-in-depth behind the Plan 01 runtime guard. Anti-cheat: offset comparison genuinely fails on transposition. |
| T-100-06 | Elevation (cross-API token reuse) | VerifyToken audience matcher with list aud | mitigate | A1 spike confirms list-aud acceptance so the Phase 98 `enforce_audience: true` + `audience:` mitigation continues to bite for signer-minted tokens (which emit list aud); BIND-01/02 target `audience: "billing-api"`. |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `mix test test/lockspire/release_readiness_contract_test.exs test/lockspire/plug/verify_token_test.exs` green.
- `mix test` (full default non-integration suite) green.
- The four-file content-hash contract clause still passes (no content drift from the additive ordering clause).
</verification>

<success_criteria>
- BIND-03 structural layer exists: ordering asserted across all four RECIPE-01 sites via the shared extractor, genuinely failing on transposition.
- The A1 list-`aud` assumption is resolved (confirmed accepted, or mitigation recorded for Plan 03).
- No content-hash drift; both files green.
</success_criteria>

<output>
Create `.planning/phases/100-sender-constraint-end-to-end-proof/100-02-SUMMARY.md` when done
</output>
