---
phase: 100-sender-constraint-end-to-end-proof
plan: 03
type: execute
wave: 2
depends_on: ["100-01", "100-02"]
files_modified:
  - test/integration/phase100_sender_constraint_e2e_test.exs
  - mix.exs
autonomous: true
requirements: [BIND-01, BIND-02]
must_haves:
  truths:
    - "A DPoP-bound at+jwt minted by AccessTokenSigner.issue/3 (cnf.jkt) traverses the real GeneratedHostAppWeb 3-plug pipeline through the genuine nonce-retry dance and returns 200 with conn.assigns.access_token populated, binding_type: dpop, binding_requirements: %{dpop_jkt: jkt}"
    - "An mTLS-bound at+jwt minted by AccessTokenSigner.issue/3 (cnf x5t#S256) with the matching cert presented via conn.private[:lockspire_mtls_cert] returns 200 with binding_type: mtls"
    - "Both proofs exercise Phase 99's maybe_put_cnf/2 carry-through — the token is minted by the real signer, NOT hand-signed (the refinement over phase81)"
    - "Because EnforceSenderConstraints now sets binding_verified: true (Plan 01), the bound tokens reach 200 through RequireToken's new guard rather than fail-closing"
  artifacts:
    - path: "test/integration/phase100_sender_constraint_e2e_test.exs"
      provides: "BIND-01 DPoP + BIND-02 mTLS happy-path end-to-end proofs"
      contains: "AccessTokenSigner.issue"
    - path: "mix.exs"
      provides: "optional test.phase100.e2e alias mirroring test.phase30.e2e"
  key_links:
    - from: "test/integration/phase100_sender_constraint_e2e_test.exs"
      to: "lib/lockspire/protocol/access_token_signer.ex"
      via: "AccessTokenSigner.issue/3 mints the cnf-carrying at+jwt"
      pattern: "AccessTokenSigner.issue"
    - from: "test/integration/phase100_sender_constraint_e2e_test.exs"
      to: "GeneratedHostAppWeb.Endpoint :lockspire_protected_api pipeline"
      via: "Phoenix.ConnTest get/2 against the canonical 3-plug chain"
      pattern: "get\\(@protected_route"
    - from: "test/integration/phase100_sender_constraint_e2e_test.exs"
      to: "Lockspire.Storage KeyCache"
      via: "publish_key + send(KeyCache, :refresh) + :sys.get_state(KeyCache)"
      pattern: ":sys.get_state\\(KeyCache\\)"
---

<objective>
Prove BIND-01 and BIND-02 end-to-end: a DPoP-bound and an mTLS-bound `at+jwt` — both minted by the real `AccessTokenSigner.issue/3` (Phase 99's `maybe_put_cnf/2` carry-through, NOT hand-signed) — traverse the canonical `VerifyToken → EnforceSenderConstraints → RequireToken` pipeline on the real `GeneratedHostAppWeb.Endpoint` and arrive as a usable `%AccessToken{}` at the host controller (200), with the correct `binding_type`.

Purpose: This is the phase's core proof — that a Phase-99-issued sender-constrained token survives the blessed pipeline. BIND-01 lifts the phase81 DPoP harness and refines it (signer mint + nonce-retry dance); BIND-02 is genuinely new (no prior full-pipeline mTLS→200 test).

Output: 1 new integration test file with the two happy-path proofs side-by-side; optionally a `test.phase100.e2e` mix alias.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-CONTEXT.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-RESEARCH.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-PATTERNS.md
@.planning/phases/100-sender-constraint-end-to-end-proof/100-VALIDATION.md

# Plan 02's A1 spike resolves whether the signer's list-aud is accepted; read its SUMMARY before relying on list aud.
@.planning/phases/100-sender-constraint-end-to-end-proof/100-02-SUMMARY.md

<interfaces>
<!-- Live contracts. Verified against the tree this session. -->

From lib/lockspire/protocol/access_token_signer.ex:
  - issue/3 (lines 55-66): {:ok, raw_at_jwt, hash} = AccessTokenSigner.issue(token, client, request)
  - base_claims/3 (130-148) reads exactly: account_id (->sub), audience (->aud LIST), scopes (->scope string), cnf (->maybe_put_cnf), issued_at (->iat). Sets iss => Config.issuer!().
  - maybe_put_cnf/2 (144,147-148): conditionally copies token.cnf into the JWT — THE thing BIND-01/02 prove survives.
  - fetch_signing_key (196-216): reads private_jwk_encrypted from Config.repo!() by kid.
  - resolve_format(%Client{access_token_format: nil}, nil) -> :jwt (line 98) — server_policy_store may be omitted.

From lib/lockspire/domain/token.ex — minimal %Token{} fields issue/3 needs (A4):
  required no-default keys: token_hash, token_type, client_id, expires_at;
  read by base_claims: account_id, scopes, audience, cnf, issued_at.

From test/integration/phase81_generated_host_route_protection_e2e_test.exs (the harness to LIFT):
  - module header / setup_all / setup (lines 1-51): @endpoint GeneratedHostAppWeb.Endpoint, @issuer, async: false, Application.put_env(:lockspire, :issuer, @issuer), TestRepo + Endpoint start_supervised, SQL Sandbox.
  - publish_signing_key/1 (257-283): generate RSA key -> Repository.publish_key(%SigningKey{public_jwk + private_jwk_encrypted, status: :active}) -> send(KeyCache, :refresh) -> :sys.get_state(KeyCache). Publishes BOTH halves (public for VerifyToken/KeyCache, private for the signer). COPY VERBATIM.
  - the nonce-retry dance (150-215) + generate_dpop_proof/3 (305-319) + protected_conn/0 (250-255, pins host: "api.example.test", port: 80).
  - phase81 hand-signs via issue_access_token/3 (285-303) — D-07 REPLACES this with AccessTokenSigner.issue/3.

From lib/lockspire/protocol/dpop.ex: thumbprint/1 (jkt from public JWK); access_token_ath/1 (57-62, ath binding proof to token).
From lib/lockspire/protocol/mtls_token_binding.ex: thumbprint/1 (7-17, x5t#S256 from cert string).
From test/support/jar_test_helpers.ex: generate_ec_keys/0 (46), sign_dpop_proof/2,3 (82).
From test/support/generated_host_app_web/router.ex (19-27): the canonical 3-plug pipeline; VerifyToken declares audience: "billing-api" (string) at line 20.
From test/support/generated_host_app_web/controllers/protected_api_controller.ex: surfaces binding_type / binding_requirements in the 200 JSON; ProtectedApiReplayStore returns {:ok, :accepted}.

From mix.exs: test.phase30.e2e alias (lines 79-80) is the template for an optional test.phase100.e2e alias; test.setup alias (line 67); integration excluded by default in test_helper.exs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BIND-01 — DPoP-bound at+jwt minted via AccessTokenSigner.issue/3 through the full pipeline to 200</name>
  <files>test/integration/phase100_sender_constraint_e2e_test.exs, mix.exs</files>
  <read_first>
    - test/integration/phase81_generated_host_route_protection_e2e_test.exs (IN FULL — the harness to lift: header/setup 1-51, nonce dance 150-215, publish_signing_key/1 257-283, generate_dpop_proof/3 305-319, protected_conn/0 250-255, issue_access_token/3 285-303 which D-07 replaces)
    - lib/lockspire/protocol/access_token_signer.ex (issue/3 55-66, base_claims/3 + maybe_put_cnf/2 130-148, resolve_format 98, fetch_signing_key 196-216)
    - lib/lockspire/domain/token.ex (the cnf field ~53 and the required no-default struct keys)
    - lib/lockspire/protocol/dpop.ex (thumbprint/1; access_token_ath/1 57-62)
    - test/support/jar_test_helpers.ex (generate_ec_keys/0 46; sign_dpop_proof/2,3 82)
    - test/support/generated_host_app_web/router.ex (canonical pipeline 19-27; audience: "billing-api" at 20)
    - test/support/generated_host_app_web/controllers/protected_api_controller.ex (the 200 JSON shape; ProtectedApiReplayStore)
    - mix.exs (test.phase30.e2e alias 79-80; test.setup 67)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-PATTERNS.md (the phase100 integration-test section, lines 220-353 — module header, key-publish helper, signer mint, nonce dance, all to copy)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-RESEARCH.md (Pitfalls 1, 2, 3, 6; Pattern 1, 2; A1, A2, A4)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-02-SUMMARY.md (the A1 list-aud resolution from Plan 02)
  </read_first>
  <behavior>
    - Mint: %Token{cnf: %{"jkt" => jkt}} -> AccessTokenSigner.issue/3 -> raw at+jwt whose cnf survived maybe_put_cnf/2
    - Request 1 (DPoP proof WITHOUT nonce): status 401, error="use_dpop_nonce", DPoP-Nonce response header present
    - Request 2 (DPoP proof WITH the returned nonce): status 200
    - 200 body: conn.assigns.access_token populated; access_token.binding_type == "dpop"; binding_requirements == %{"dpop_jkt" => jkt}
    - The proof's ath is DPoP.access_token_ath(raw_at_jwt) on the ACTUAL minted token (anti-cheat: not a stub)
  </behavior>
  <action>
    Create test/integration/phase100_sender_constraint_e2e_test.exs as `Lockspire.Integration.Phase100SenderConstraintE2ETest`, `use ExUnit.Case, async: false`, `@moduletag :integration`, `@endpoint GeneratedHostAppWeb.Endpoint`. COPY VERBATIM from phase81: the module header/aliases, the `setup_all` (MUST `Application.put_env(:lockspire, :issuer, @issuer)` so the signer's `iss` matches what VerifyToken pins — Pitfall/Pattern 1), the `setup` that publishes a unique-kid key, `publish_signing_key/1` (257-283, publishes BOTH public_jwk + private_jwk_encrypted), `protected_conn/0` (host "api.example.test" port 80 — Pitfall 3), and `generate_dpop_proof/3` (305-319).
    THE D-07 REFINEMENT: do NOT lift phase81's hand-signing `issue_access_token/3`. Instead mint via the real signer: build `%Lockspire.Domain.Token{token_hash: "unused", token_type: :access_token, client_id: "generated-host-api-client", account_id: "generated-host-user", scopes: ["read:billing"], audience: ["billing-api"], cnf: %{"jkt" => jkt}, issued_at: ..., expires_at: ...+3600s}`; a `%Lockspire.Domain.Client{client_id: "generated-host-api-client", access_token_format: :jwt}`; and `request = %{opts: [key_store: Lockspire.Config.repo!()]}` (server_policy_store omitted -> :jwt fallback per resolve_format/98, A2). `{:ok, raw_at_jwt, _hash} = AccessTokenSigner.issue(token, client, request)`. Derive `jkt` via `DPoP.thumbprint/1` on the EC proof key's public JWK (JarTestHelpers.generate_ec_keys/0).
    Run the MANDATORY three-request nonce dance (Pitfall 1 — a single proof without nonce returns 401 use_dpop_nonce, never 200): (1) DPoP proof without nonce -> assert 401 + extract `dpop-nonce` header; (2) build a fresh proof embedding that nonce (and `ath` = DPoP.access_token_ath(raw_at_jwt)); (3) re-request -> assert 200 and decode the body asserting `binding_type: "dpop"` and `binding_requirements: %{"dpop_jkt" => jkt}`.
    Audience: pass the token `audience: ["billing-api"]` (LIST — the signer shape). The router declares `audience: "billing-api"` on the plug. Honor the Plan 02 A1 SUMMARY: if it confirmed list-aud is accepted, proceed; if it flagged a mismatch, apply the recorded mitigation (string aud or the matcher's unwrap path) — do NOT silently fail audience (Pitfall 6).
    Optionally add a `test.phase100.e2e` alias to mix.exs mirroring `test.phase30.e2e` (79-80): `mix test.setup` then `mix test --include integration test/integration/phase100_sender_constraint_e2e_test.exs`.
    Anti-patterns (forbidden): hand-signing with JOSE.JWT.sign (defeats the proof — D-07); skipping/stubbing the nonce dance or the ProtectedApiReplayStore (D-06 faithful proof); omitting `:sys.get_state(KeyCache)` after the refresh (Pitfall 2 — async race -> flaky unknown-kid).
  </action>
  <verify>
    <automated>mix test.setup && mix test --include integration test/integration/phase100_sender_constraint_e2e_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test.setup && mix test --include integration test/integration/phase100_sender_constraint_e2e_test.exs` exits 0 with the BIND-01 case green
    - Faithful proof (not shallow): the token is minted by `AccessTokenSigner.issue/3` from a `%Token{cnf: %{"jkt" => jkt}}` (grep the file for `AccessTokenSigner.issue` and confirm NO `JOSE.JWT.sign` hand-sign of the access token)
    - The genuine nonce dance runs: a first request asserts 401 `use_dpop_nonce` and a second (with nonce) asserts 200 — not a single-shot 200
    - 200 body asserts `binding_type: "dpop"` AND `binding_requirements: %{"dpop_jkt" => jkt}` (the cnf carry-through survived)
    - `:sys.get_state(KeyCache)` follows `send(KeyCache, :refresh)` in the key-publish helper
  </acceptance_criteria>
  <done>BIND-01 proves a signer-minted DPoP-bound at+jwt traverses the real 3-plug pipeline through the nonce dance to a 200 with the cnf-derived binding metadata; the integration file's DPoP case is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BIND-02 — mTLS-bound at+jwt minted via AccessTokenSigner.issue/3 through the full pipeline to 200</name>
  <files>test/integration/phase100_sender_constraint_e2e_test.exs</files>
  <read_first>
    - test/integration/phase100_sender_constraint_e2e_test.exs (the file created in Task 1 — reuse its setup, publish_signing_key/1, protected_conn/0, signer-mint pattern)
    - lib/lockspire/protocol/mtls_token_binding.ex (thumbprint/1 7-17; confirmation_matches?/2 22-29)
    - lib/lockspire/plug/enforce_sender_constraints.ex (fetch_mtls_cert/2 178-189 — conn.private[:lockspire_mtls_cert] is the primary path)
    - test/lockspire/plug/enforce_sender_constraints_test.exs (the put_private(:lockspire_mtls_cert, ...) + MTLSTokenBinding.thumbprint/1 pattern, 192-216)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-PATTERNS.md (the BIND-02 mTLS section, lines 335-351)
    - .planning/phases/100-sender-constraint-end-to-end-proof/100-RESEARCH.md (BIND-02 row; Code Example mTLS happy-path 336-361; D-08)
  </read_first>
  <behavior>
    - cert = a synthetic cert string; x5t = MTLSTokenBinding.thumbprint(cert) (the SAME source for both sides)
    - Mint: %Token{cnf: %{"x5t#S256" => x5t}} -> AccessTokenSigner.issue/3 -> raw at+jwt
    - Request: Authorization "Bearer <raw>" + conn.private[:lockspire_mtls_cert] = cert (no nonce dance for mTLS)
    - Result: status 200; access_token.binding_type == "mtls"; conn.assigns.access_token populated
  </behavior>
  <action>
    Add the BIND-02 case to the file created in Task 1 (D-10: both proofs side-by-side in one file). Per D-08: derive `x5t` via `Lockspire.Protocol.MTLSTokenBinding.thumbprint("phase100-mtls-client-cert")` (synthetic string cert — D-08 confirms this is sufficient; a real DER-cert/`:mtls_extractor` path is explicitly deferred and NOT required). Mint a `%Token{}` (same skeleton as Task 1) with `cnf: %{"x5t#S256" => x5t}` via `AccessTokenSigner.issue/3` (per D-07 — signer mint, NOT hand-signed). CRITICAL: the presented cert and the token's `cnf["x5t#S256"]` MUST be derived from the SAME cert string so `confirmation_matches?/2` (mtls_token_binding.ex:22-29) passes.
    Build the request via `protected_conn/0` |> `put_req_header("authorization", "Bearer #{raw}")` |> `Plug.Conn.put_private(:lockspire_mtls_cert, cert)` (the primary fetch_mtls_cert/2 path, enforce_sender_constraints.ex:178-189) |> `get(@protected_route)`. No nonce dance (mTLS has none). Assert `conn.status == 200` and decode the body asserting `binding_type: "mtls"` and a populated access_token.
    Reuse the Task-1 `publish_signing_key/1`, `setup`, and audience handling (list aud per the Plan 02 A1 SUMMARY). Do NOT stand up a TLS chain or `:mtls_extractor` (deferred).
  </action>
  <verify>
    <automated>mix test.setup && mix test --include integration test/integration/phase100_sender_constraint_e2e_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test.setup && mix test --include integration test/integration/phase100_sender_constraint_e2e_test.exs` exits 0 with BOTH BIND-01 and BIND-02 cases green
    - Faithful proof (not shallow): asserts 200 AT THE CONTROLLER and `binding_type: "mtls"` (not merely "the plug didn't error"); the token's `x5t#S256` is derived from the SAME cert string presented via `conn.private[:lockspire_mtls_cert]`
    - The token is minted by `AccessTokenSigner.issue/3` (cnf x5t#S256 carry-through), NOT hand-signed
    - No TLS chain / `:mtls_extractor` path introduced (synthetic string cert only — D-08)
  </acceptance_criteria>
  <done>BIND-02 proves a signer-minted mTLS-bound at+jwt with a matching presented cert traverses the full pipeline to a 200 with binding_type mtls; both proofs live side-by-side and the file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GeneratedHostAppWeb 3-plug pipeline | Untrusted Authorization header + DPoP proof / mTLS cert crosses into the real endpoint under test |
| issuance fixture → KeyCache / repo | The test-published signing key (public to KeyCache, private to the signer) — the kid must resolve on BOTH sides |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-07 | Tampering / Repudiation | DPoP proof replay (BIND-01) | mitigate | The faithful proof runs the genuine nonce-retry dance against the wired ProtectedApiReplayStore and binds the proof to the token via DPoP.access_token_ath(raw_at_jwt) — exercises ath + replay-store + nonce end-to-end (RFC 9449). |
| T-100-08 | Spoofing | mTLS cert/thumbprint mismatch (BIND-02) | mitigate | The token's cnf x5t#S256 is derived from the SAME cert string presented via conn.private; confirmation_matches?/2 enforces agreement (RFC 8705 §3). |
| T-100-09 | Tampering | alg:none / client-controlled signing | mitigate | AccessTokenSigner takes alg/kid ONLY from the active key, never client input; the issuance fixture inherits this guarantee. |
| T-100-10 | Elevation (cross-API reuse) | wrong-audience token | mitigate | The pipeline declares audience: "billing-api"; both bound tokens target it (list aud per Plan 02 A1). |
| T-100-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `mix test.setup && mix test --include integration test/integration/phase100_sender_constraint_e2e_test.exs` fully green (both BIND-01 + BIND-02).
- Phase gate: `mix test.setup && mix test --include integration` fully green (entire suite incl. integration) + `mix compile --warnings-as-errors` clean (CI also runs credo/dialyzer/sobelow).
- Confirms the dependency on Plan 01: the bound tokens reach 200 ONLY because EnforceSenderConstraints now sets binding_verified: true before RequireToken's new guard.
</verification>

<success_criteria>
- BIND-01: signer-minted DPoP-bound at+jwt -> real pipeline -> nonce dance -> 200 with binding_type dpop and cnf-derived binding_requirements.
- BIND-02: signer-minted mTLS-bound at+jwt + matching cert -> 200 with binding_type mtls.
- Both proofs are faithful (real signer, real nonce dance, real cert-thumbprint agreement) — not hand-signed or stubbed.
</success_criteria>

<output>
Create `.planning/phases/100-sender-constraint-end-to-end-proof/100-03-SUMMARY.md` when done
</output>
