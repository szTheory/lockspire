---
phase: 124-configure-onboarding-propagation-pass
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/web/live/admin/clients_live/index.ex
  - lib/lockspire/web/live/admin/clients_live/form_component.ex
  - lib/lockspire/web/live/admin/clients_live/show.ex
  - lib/lockspire/web/live/admin/clients_live/rotate_secret_component.ex
  - test/lockspire/web/live/admin/clients_live_test.exs
  - test/lockspire/web/live/admin/clients_live/show_test.exs
autonomous: true
requirements:
  - CONFIG-01
  - CONFIG-02
  - CONFIG-03
must_haves:
  truths:
    - "Configure operator can filter and select clients with posture, selected context, and route-specific actions visible before dense detail."
    - "Client detail keeps current posture, support pivots, safe/secondary/destructive action grouping, and copy-once credential/RAT handoff separated."
    - "Client lifecycle and credential actions use concrete consequence copy without plaintext recovery or host-owned policy implications."
  artifacts:
    - path: "lib/lockspire/web/live/admin/clients_live/index.ex"
      provides: "Client inventory route hierarchy and Filter clients action copy"
    - path: "lib/lockspire/web/live/admin/clients_live/show.ex"
      provides: "Client detail posture, action grouping, lifecycle confirmation, and RAT copy-once handoff"
    - path: "lib/lockspire/web/live/admin/clients_live/form_component.ex"
      provides: "Focused client Configure form titles, help, and save labels"
    - path: "lib/lockspire/web/live/admin/clients_live/rotate_secret_component.ex"
      provides: "Copy-once client secret rotation handoff copy and confirmation label"
    - path: "test/lockspire/web/live/admin/clients_live_test.exs"
      provides: "Rendered client inventory and client creation copy-once proof"
    - path: "test/lockspire/web/live/admin/clients_live/show_test.exs"
      provides: "Rendered client detail, lifecycle, RAT, and credential proof"
  key_links:
    - from: "lib/lockspire/web/live/admin/clients_live/index.ex"
      to: "Lockspire.Admin.create_client/1"
      via: "existing save_client event creates clients and renders copy_once_secret_panel only from create result"
      pattern: "Admin\\.create_client"
    - from: "lib/lockspire/web/live/admin/clients_live/show.ex"
      to: "Lockspire.Admin.update_client/2 and Lockspire.Protocol.RegistrationManagement.rotate_registration_access_token/2"
      via: "existing show_path and LiveView events keep client mutations inside current Admin/protocol APIs"
      pattern: "rotate_registration_access_token|Admin\\.update_client"
---

<objective>
Propagate the Phase 124 Configure interaction model into client inventory, client detail, client forms, and client credential/RAT handoff without adding public APIs, routes, schemas, migrations, packages, or host-owned seams.

Purpose: CONFIG-01, CONFIG-02, and CONFIG-03 require clients to share the Configure hierarchy, copy-once handoff, and action semantics used by stronger Support/Operate pages while honoring D-01 through D-10.
Output: Existing client LiveViews/components/tests updated in place; no new public symbols or files.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/124-configure-onboarding-propagation-pass/124-CONTEXT.md
@.planning/phases/124-configure-onboarding-propagation-pass/124-UI-SPEC.md
@.planning/phases/124-configure-onboarding-propagation-pass/124-RESEARCH.md
@.planning/phases/124-configure-onboarding-propagation-pass/124-VALIDATION.md
@.planning/phases/124-configure-onboarding-propagation-pass/124-PATTERNS.md
@.planning/phases/121-route-scorecards-judgment-contract/121-ROUTE-SCORECARDS.md
@.planning/phases/122-support-investigation-flow-polish/122-01-SUMMARY.md
@.planning/phases/122-support-investigation-flow-polish/122-02-SUMMARY.md
@.planning/phases/123-operate-queue-flow-polish/123-04-SUMMARY.md
@lib/lockspire/web/live/admin/clients_live/index.ex
@lib/lockspire/web/live/admin/clients_live/form_component.ex
@lib/lockspire/web/live/admin/clients_live/show.ex
@lib/lockspire/web/live/admin/clients_live/rotate_secret_component.ex
@test/lockspire/web/live/admin/clients_live_test.exs
@test/lockspire/web/live/admin/clients_live/show_test.exs
</context>

## Artifacts This Phase Produces

Created files: None.
Created public symbols: None.
Required private symbols if the current source does not already have equivalent helpers: `client_inventory_decision_items/2`, `client_handoff_copy/1`, `client_secret_rotation_copy/1`, and `rat_rotation_copy/1`. These helpers must remain private to the touched LiveView/component modules.

<tasks>

<task type="auto" tdd="true">
  <name>Task 124-01-01: Add client Configure hierarchy and handoff proof</name>
  <files>test/lockspire/web/live/admin/clients_live_test.exs, test/lockspire/web/live/admin/clients_live/show_test.exs</files>
  <read_first>
    - `test/lockspire/web/live/admin/clients_live_test.exs`
    - `test/lockspire/web/live/admin/clients_live/show_test.exs`
    - `git diff -- test/lockspire/web/live/admin/clients_live/show_test.exs`
    - `.planning/phases/124-configure-onboarding-propagation-pass/124-CONTEXT.md`
    - `.planning/phases/124-configure-onboarding-propagation-pass/124-UI-SPEC.md`
    - `.planning/phases/124-configure-onboarding-propagation-pass/124-VALIDATION.md`
  </read_first>
  <action>Add focused RED coverage for CONFIG-01, CONFIG-02, and CONFIG-03 before implementation. In `clients_live_test.exs`, assert `/admin/clients` renders journey label `Configure`, selected filter context, total/matching client count, `Filter clients` submit copy, `Create client`, no generic client inventory copy, and copy-once client-secret output only from the create result. In dirty `clients_live/show_test.exs`, first inspect and preserve unrelated/user-owned hunks, then add assertions that client detail renders `Review client configuration`, `Effective posture`, `DCR and RAT context`, `Support pivots`, `Lifecycle and destructive actions`, `AdminComponents.action_group` markup, `Rotate client secret`, `Rotate registration access token`, `Confirm client disable`, `name="toggle[confirm]"`, redacted durable credential/RAT posture, and no plaintext secret/hash text. Reference D-01, D-02, D-03, D-04, D-06, D-07, D-08, D-09, and D-10 in test names or comments only if comments already exist nearby; otherwise keep citations in assertion messages. Stage only Phase 124 hunks from the dirty test file.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/web/live/admin/clients_live_test.exs test/lockspire/web/live/admin/clients_live/show_test.exs --max-failures 1</automated>
    <automated>mix format --check-formatted test/lockspire/web/live/admin/clients_live_test.exs test/lockspire/web/live/admin/clients_live/show_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The focused client test command fails before implementation only on the newly asserted Phase 124 client hierarchy, copy, action grouping, or copy-once expectations.
    - Test assertions cover `Filter clients`, `Rotate client secret`, `Rotate registration access token`, `Confirm client disable`, `name="toggle[confirm]"`, redacted durable credential/RAT state, and absence of plaintext secret/hash samples.
    - Existing dirty hunks in `clients_live/show_test.exs` that are unrelated to Phase 124 remain preserved and unstaged.
  </acceptance_criteria>
  <done>Client Configure route tests describe the required Phase 124 behaviors before source implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 124-01-02: Implement client Configure hierarchy, labels, and copy-once consequences</name>
  <files>lib/lockspire/web/live/admin/clients_live/index.ex, lib/lockspire/web/live/admin/clients_live/form_component.ex, lib/lockspire/web/live/admin/clients_live/show.ex, lib/lockspire/web/live/admin/clients_live/rotate_secret_component.ex</files>
  <read_first>
    - `lib/lockspire/web/live/admin/clients_live/index.ex`
    - `lib/lockspire/web/live/admin/clients_live/form_component.ex`
    - `git diff -- lib/lockspire/web/live/admin/clients_live/form_component.ex`
    - `lib/lockspire/web/live/admin/clients_live/show.ex`
    - `git diff -- lib/lockspire/web/live/admin/clients_live/show.ex`
    - `lib/lockspire/web/live/admin/clients_live/rotate_secret_component.ex`
    - `lib/lockspire/web/components/admin_components.ex`
    - `.planning/phases/124-configure-onboarding-propagation-pass/124-PATTERNS.md`
  </read_first>
  <action>Make the RED client tests pass using existing Admin-backed behavior only. In `clients_live/index.ex`, keep the existing `save_client` event and `Admin.create_client/1` path, replace the inventory filter submit label with `Filter clients`, and put selected filter/total context before dense client rows. In dirty `form_component.ex`, preserve unrelated hunks and keep `button_for/1` labels route-specific: `Create client`, `Save metadata`, `Save redirect URIs`, `Save post-logout redirect URIs`, `Save logout propagation`, `Save PAR policy`, and `Save security profile`. In dirty `show.ex`, preserve existing `show_path/2`, `toggle_client`, `rotate_secret`, and `rotate_rat` event semantics while ensuring posture content precedes mutation panes, safe/secondary/destructive controls are grouped with `AdminComponents.action_group`, lifecycle confirmation remains an inline `AdminComponents.confirmation_panel`, and RAT copy-once copy says the previous RAT stops being current and plaintext is shown once. In `rotate_secret_component.ex`, change the submit button copy from `Rotate secret` to `Rotate client secret` and ensure the body says plaintext is shown once and Lockspire does not store or re-show it. Do not add routes, Admin API delegates, schemas, migrations, packages, public components, reveal/export controls, or developer-portal UX per D-01, D-02, D-05, D-06, and D-07. Stage only Phase 124 hunks from dirty files.</action>
  <verify>
    <automated>MIX_ENV=test mix test test/lockspire/web/live/admin/clients_live_test.exs test/lockspire/web/live/admin/clients_live/show_test.exs --max-failures 1</automated>
    <automated>mix format --check-formatted lib/lockspire/web/live/admin/clients_live/index.ex lib/lockspire/web/live/admin/clients_live/form_component.ex lib/lockspire/web/live/admin/clients_live/show.ex lib/lockspire/web/live/admin/clients_live/rotate_secret_component.ex test/lockspire/web/live/admin/clients_live_test.exs test/lockspire/web/live/admin/clients_live/show_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `/admin/clients` renders `Filter clients`, selected context, client count, safe create path, and no generic inventory submit label.
    - Client detail and credential flows render route-specific labels, inline lifecycle confirmation, grouped safe/secondary/destructive controls, and copy-once secret/RAT consequence copy.
    - Client source still uses existing `Admin.create_client/1`, `Admin.update_client/2`, `toggle_client`, `rotate_secret`, `rotate_rat`, and `show_path/2`; no route/API/schema/package expansion appears in the diff.
    - Focused client tests and format checks pass.
  </acceptance_criteria>
  <done>Client Configure surfaces satisfy CONFIG-01, CONFIG-02, and CONFIG-03 with existing route and Admin API boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host staff guard -> AdminRouter | Host app owns staff authentication and mount protection; Phase 124 must not add routes or auth seams. |
| LiveView form -> Lockspire.Admin | Client create/update/lifecycle/rotation inputs cross from operator UI into existing Admin/protocol functions. |
| Admin/protocol secret result -> LiveView assigns | Plaintext client secret/RAT exists only in immediate copy-once render state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-01 | Information Disclosure | `clients_live/index.ex`, `clients_live/show.ex`, `rotate_secret_component.ex` | mitigate | Render plaintext only through `copy_once_secret_panel` immediately after create/rotate; tests assert durable client pages omit plaintext secrets, RATs, hashes, and forbidden secret samples. |
| T-124-02 | Elevation of Privilege | `Lockspire.Web.AdminRouter` boundary | mitigate | Keep all work inside existing client routes and existing Admin/protocol functions; Plan 06 route/source contracts recheck no new public route/API surface. |
| T-124-03 | Tampering | client lifecycle and credential rotation forms | mitigate | Preserve checkbox-backed `confirmation_panel` semantics and concrete consequence copy before destructive/risky mutations. |
| T-124-04 | Repudiation | operator copy for client lifecycle/credential changes | mitigate | Copy names the concrete consequence: disabled clients block future OAuth/OIDC requests, rotated secrets/RATs stop the previous credential from being current, and plaintext is shown once. |
| T-124-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks are planned; executor must not add package dependencies or registry artifacts. |

## ASVS / High-Threshold Boundary Coverage

| Concern | Applicability | Plan Treatment |
|---------|---------------|----------------|
| route/API expansion | Applicable | Preserve existing client routes, `show_path/2`, `Admin.create_client/1`, `Admin.update_client/2`, `toggle_client`, `rotate_secret`, and `rotate_rat`; no route, Admin API, schema, migration, package, lab, or public theming expansion is allowed. |
| host seam ownership | Preserved by boundary | Client UI must not claim Lockspire owns staff authentication, host layouts/branding, tenant policy, developer portal delivery, account resolution, or host login redirects. |
| copy-once leaks | Applicable | Client secret and RAT plaintext may render only in immediate create/rotate `copy_once_secret_panel` state; durable client detail and inventory remain redacted. |
| sensitive internals | Applicable | Tests deny plaintext secrets, RATs, hashes, private protocol material, backend internals, and forbidden secret samples in durable client UI. |
| destructive confirmation | Applicable | Client lifecycle and credential rotation actions use checkbox-backed `confirmation_panel` or equivalent existing grouped confirmation semantics with concrete consequence copy. |
</threat_model>

<verification>
Plan 01 verification is the focused client LiveView route suite plus format check. Full cross-route Configure contracts run in Plan 06 after all route-local Wave 1 plans finish.
</verification>

<success_criteria>
- CONFIG-01: client inventory/detail hierarchy shows current posture, next safe action, support/configure pivots, and safe route-specific labels.
- CONFIG-02: client create, client-secret rotation, and RAT rotation keep copy-once plaintext limited to immediate create/rotate state and durable UI redacted.
- CONFIG-03: client lifecycle and credential controls distinguish safe, secondary, and destructive actions with confirmation and concrete consequence copy.
</success_criteria>

<output>
Create `.planning/phases/124-configure-onboarding-propagation-pass/124-01-SUMMARY.md` when done.
</output>
