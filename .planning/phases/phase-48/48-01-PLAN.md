---
phase: 48
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/domain/client.ex
  - lib/lockspire/protocol/token_exchange.ex
  - lib/lockspire/protocol/rfc8693_exchange.ex
autonomous: true
requirements:
  - TE-01
must_haves:
  truths:
    - The server recognizes the token-exchange grant type as valid.
    - Incoming token-exchange requests are routed to the specific RFC 8693 handler.
    - The new handler parses subject_token and actor_token.
  artifacts:
    - path: lib/lockspire/protocol/rfc8693_exchange.ex
      provides: Token exchange request parsing
    - path: test/lockspire/protocol/rfc8693_exchange_test.exs
      provides: Tests for token parsing
  key_links:
    - from: lib/lockspire/protocol/token_exchange.ex
      to: lib/lockspire/protocol/rfc8693_exchange.ex
      via: exchange_rfc8693
---

<objective>
Configure the application to accept the OAuth 2.0 Token Exchange grant type and route requests to a new parser handler.

Purpose: Foundation for RFC 8693. We must accept the grant type, route it correctly, and parse the required tokens out of the request payload.
Output: System accepts `urn:ietf:params:oauth:grant-type:token-exchange` and calls the new `Lockspire.Protocol.Rfc8693Exchange.exchange/2` handler.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-48/RESEARCH.md
@.planning/phases/phase-48/PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Allow Token Exchange Grant Type</name>
  <files>lib/lockspire/domain/client.ex, test/lockspire/clients_test.exs</files>
  <action>Add `urn:ietf:params:oauth:grant-type:token-exchange` to the allowed grant types validation in `Lockspire.Domain.Client`. Ensure clients can be configured with this grant type. Update `test/lockspire/clients_test.exs` or the relevant tests to cover this grant type validation.</action>
  <verify>
    <automated>mix test test/lockspire/clients_test.exs</automated>
  </verify>
  <done>Client struct can accept the new grant type without validation errors.</done>
</task>

<task type="auto">
  <name>Task 2: Route request in TokenExchange</name>
  <files>lib/lockspire/protocol/token_exchange.ex</files>
  <action>In `Lockspire.Protocol.TokenExchange.exchange/1`, add a pattern match for `"urn:ietf:params:oauth:grant-type:token-exchange"`. Have it extract the client context and call `Lockspire.Protocol.Rfc8693Exchange.exchange(client, request)`.</action>
  <verify>
    <automated>mix test</automated>
  </verify>
  <done>The router delegates the specific grant type to the new module.</done>
</task>

<task type="auto">
  <name>Task 3: Create RFC 8693 Handler Skeleton</name>
  <files>lib/lockspire/protocol/rfc8693_exchange.ex, test/lockspire/protocol/rfc8693_exchange_test.exs</files>
  <action>Create the new module `Lockspire.Protocol.Rfc8693Exchange` with an `exchange(client, request)` function. It must parse the `subject_token` and `actor_token` from the request parameters. If `subject_token` is missing, return an oauth error (`invalid_request`). For now, if present, return a placeholder `{:error, %Error{error: "invalid_grant"}}` so it compiles and handles the basic parse. Add basic unit tests for missing subject_token.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/rfc8693_exchange_test.exs</automated>
  </verify>
  <done>Handler exists, parses input, and rejects missing subject tokens.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted OAuth clients sending tokens |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Spoofing | Client Validation | mitigate | TokenExchange already authenticates clients before calling handlers. |
</threat_model>

<verification>
Automated tests confirm the grant type is accepted and routed, and missing parameters are correctly rejected by the new handler.
</verification>

<success_criteria>
- `Lockspire.Domain.Client` allows the new grant type.
- `Lockspire.Protocol.TokenExchange` routes the new grant type.
- `Lockspire.Protocol.Rfc8693Exchange` rejects requests missing `subject_token`.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-48/48-01-SUMMARY.md`
</output>
