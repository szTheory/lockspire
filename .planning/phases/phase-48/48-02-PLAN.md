---
phase: 48
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lib/lockspire/protocol/rfc8693_exchange.ex
  - test/lockspire/protocol/rfc8693_exchange_test.exs
autonomous: true
requirements:
  - TE-02
must_haves:
  truths:
    - The server rejects invalid or expired subject tokens.
    - The server rejects requests where requested scopes exceed the subject token scopes.
    - If no scopes are requested, the new token inherits the subject token's scopes exactly.
  artifacts:
    - path: lib/lockspire/protocol/rfc8693_exchange.ex
      provides: Token validation and scope enforcement logic
  key_links:
    - from: lib/lockspire/protocol/rfc8693_exchange.ex
      to: lockspire_tokens (via TokenStore)
      via: fetch_lifecycle_token/fetch_active_access_token
---

<objective>
Implement token validation and strict downscoping rules for OAuth 2.0 Token Exchange.

Purpose: Security and authorization enforcement. We must guarantee that a token exchange cannot result in privilege escalation.
Output: The `Rfc8693Exchange` handler validates the subject token against the database and correctly bounds the requested scopes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-48/RESEARCH.md
@.planning/phases/phase-48/PATTERNS.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Subject Token Validation</name>
  <files>lib/lockspire/protocol/rfc8693_exchange.ex, test/lockspire/protocol/rfc8693_exchange_test.exs</files>
  <action>In `exchange/2`, hash the provided `subject_token` using `Lockspire.Security.Policy.hash_token/1`. Use `TokenStore.fetch_lifecycle_token/1` or `TokenStore.fetch_active_access_token/1` to retrieve the token. If it is not found, expired, or revoked, return an `invalid_grant` OAuth error. Add tests covering these invalid token scenarios.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/rfc8693_exchange_test.exs</automated>
  </verify>
  <done>Invalid, expired, or revoked subject tokens are rejected securely.</done>
</task>

<task type="auto">
  <name>Task 2: Strict Downscoping</name>
  <files>lib/lockspire/protocol/rfc8693_exchange.ex, test/lockspire/protocol/rfc8693_exchange_test.exs</files>
  <action>Implement downscoping logic. Parse the `scope` parameter from the request (if provided). Check if the requested scopes are a strict subset of the subject token's scopes (using `MapSet.subset?/2`). If not, return an `invalid_scope` OAuth error. If the `scope` parameter is omitted, default to the subject token's full scope list. Add unit tests for successful downscoping and scope exceedance rejection.</action>
  <verify>
    <automated>mix test test/lockspire/protocol/rfc8693_exchange_test.exs</automated>
  </verify>
  <done>Scope escalation is blocked; downscoping or equal scoping succeeds.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> API | Untrusted OAuth clients requesting downscoped tokens |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-02 | Elevation of Privilege | Downscoping Logic | mitigate | Strict MapSet subset enforcement ensures new scopes cannot exceed original scopes. |
| T-48-03 | Spoofing/Replay | Token Validation | mitigate | Hashing and checking DB ensures revoked/expired tokens cannot be exchanged. |
</threat_model>

<verification>
Automated tests confirm that token validation blocks invalid tokens and scope downscoping rules are strictly enforced.
</verification>

<success_criteria>
- Exchange fails with `invalid_grant` for invalid subject tokens.
- Exchange fails with `invalid_scope` if requested scopes exceed subject scopes.
- Missing requested scope defaults to subject scopes.
</success_criteria>

<output>
After completion, create `.planning/phases/phase-48/48-02-SUMMARY.md`
</output>
