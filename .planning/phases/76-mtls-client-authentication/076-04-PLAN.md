---
phase: 76-mtls-client-authentication
plan: 04
type: execute
wave: 3
depends_on: ["03"]
files_modified:
  - lib/lockspire/protocol/client_auth.ex
  - lib/lockspire/web/controllers/token_controller.ex
  - lib/lockspire/web/controllers/introspection_controller.ex
  - lib/lockspire/web/controllers/revocation_controller.ex
autonomous: true
requirements:
  - MTLS-AUTH-01
  - MTLS-AUTH-02
  - MTLS-AUTH-03
must_haves:
  truths:
    - "Token endpoint passes the MTLS cert from connection context to the protocol logic."
    - "Introspection and Revocation endpoints also pass the MTLS cert."
    - "Protocol defaults to implicit client authentication fallback when standard credentials are not provided but an MTLS cert is present."
  artifacts:
    - path: "lib/lockspire/protocol/client_auth.ex"
      provides: "ClientAuth method dispatch for MTLS"
  key_links:
    - from: "lib/lockspire/web/controllers/token_controller.ex"
      to: "lib/lockspire/protocol/token_exchange.ex"
      via: "opts[:mtls_cert]"
      pattern: "Keyword.put(:mtls_cert, conn.private\[:lockspire_mtls_cert\])"
    - from: "lib/lockspire/protocol/client_auth.ex"
      to: "lib/lockspire/protocol/client_auth/mtls.ex"
      via: "MTLS.verify/4"
      pattern: "Lockspire.Protocol.ClientAuth.MTLS.verify(client, opts[:mtls_cert], method, opts)"
---

<objective>
Wire the MTLS validation pipeline into the core Client Authentication dispatcher and update Plug controllers to supply the out-of-band certificate.

Purpose: Provide end-to-end integration of the MTLS feature across the protocol boundary without leaking HTTP connection context.
Output: Controllers threading the certificate as options and `ClientAuth` correctly routing authentication resolution.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-mtls-client-authentication/Phase76-STRATEGY.md
@.planning/phases/76-mtls-client-authentication/76-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Client Authentication Core Dispatch</name>
  <files>lib/lockspire/protocol/client_auth.ex</files>
  <behavior>
    - `evaluate_client_credentials` returns a unified `{:ok, :implicit_client_id, id, nil}` when only a `client_id` is provided (no secret/assertion).
    - `validate_client_secret` delegates to `MTLS.verify/4` for `:tls_client_auth` and `:self_signed_tls_client_auth` using `opts[:mtls_cert]`.
  </behavior>
  <action>
    Modify `lib/lockspire/protocol/client_auth.ex` to:
    - Update `evaluate_client_credentials/1` to return `{:ok, :implicit_client_id, id, nil}` instead of `{:ok, :none, id, nil}` if body contains only `client_id`. This allows differentiating between no credentials (fallback) and explicit "none" method.
    - Add dispatch clauses in `validate_client_secret/4` for `:tls_client_auth` and `:self_signed_tls_client_auth`.
    - In the MTLS dispatch clauses, call `Lockspire.Protocol.ClientAuth.MTLS.verify(client, opts[:mtls_cert], method, opts)`.
    - Map `MTLS.verify` success to `:ok` and format errors using `invalid_client("Client authentication failed", reason_code)`.
  </action>
  <verify>
    <automated>mix test test/lockspire/protocol/client_auth_test.exs</automated>
  </verify>
  <done>ClientAuth routes to MTLS and resolves implicit clients.</done>
</task>

<task type="auto">
  <name>Task 2: Inject MTLS Cert via Plug Controllers</name>
  <files>lib/lockspire/web/controllers/token_controller.ex, lib/lockspire/web/controllers/introspection_controller.ex, lib/lockspire/web/controllers/revocation_controller.ex</files>
  <action>
    Modify the 3 controllers (`TokenController`, `IntrospectionController`, `RevocationController`) to:
    - Extract `conn.private[:lockspire_mtls_cert]`.
    - Append it to the `opts` keyword list passed to `TokenExchange.exchange`, `Introspection.introspect`, and `Revocation.revoke` respectively, using `Keyword.put(:mtls_cert, conn.private[:lockspire_mtls_cert])`.
  </action>
  <verify>
    <automated>mix test --stale</automated>
  </verify>
  <done>Controllers correctly pass the out-of-band certificate to the protocol options.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP Controller to Protocol | Isolates raw connection environment (Plug/Cowboy context) from standard application payload logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-05 | Information Disclosure | `ClientAuth.ex` | mitigate | Ensure explicit logging does not leak parsed certificates or full raw DER bytes. |
</threat_model>

<verification>
mix test
</verification>

<success_criteria>
- Token endpoint successfully validates MTLS client authentication entirely via the connection context fallback and protocol validation mapping.
</success_criteria>

<output>
After completion, create `.planning/phases/76-mtls-client-authentication/76-04-SUMMARY.md`
</output>
