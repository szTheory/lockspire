---
phase: 76-mtls-client-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/domain/client.ex
  - lib/lockspire/storage/ecto/client_record.ex
  - lib/lockspire/security/policy.ex
autonomous: true
requirements:
  - MTLS-AUTH-01
  - MTLS-AUTH-02
must_haves:
  truths:
    - "Client domain struct and Ecto schema support RFC 8705 PKI attributes."
    - "Security policy explicitly allows tls_client_auth and self_signed_tls_client_auth methods."
  artifacts:
    - path: "lib/lockspire/domain/client.ex"
      provides: "Updated typespecs for client entity"
    - path: "lib/lockspire/storage/ecto/client_record.ex"
      provides: "Ecto schema fields for MTLS attributes"
    - path: "lib/lockspire/security/policy.ex"
      provides: "Updated list of supported auth methods"
  key_links: []
---

<objective>
Update the Client domain model and Ecto schema to include the five RFC 8705 MTLS PKI attributes and extend the supported client authentication methods list.

Purpose: Implement the required domain foundation to map MTLS attributes for clients without incurring technical debt later.
Output: Extended Client domain struct, updated Client Ecto schema and changeset, and Policy configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-mtls-client-authentication/Phase76-STRATEGY.md
@.planning/phases/76-mtls-client-authentication/76-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Domain Struct and Policy Config</name>
  <files>lib/lockspire/domain/client.ex, lib/lockspire/security/policy.ex</files>
  <behavior>
    - Policy module exposes `:tls_client_auth` and `:self_signed_tls_client_auth` as supported methods.
    - Domain Client struct can hold MTLS attributes: `tls_client_auth_subject_dn`, `tls_client_auth_san_dns`, `tls_client_auth_san_uri`, `tls_client_auth_san_ip`, `tls_client_auth_san_email`.
  </behavior>
  <action>
    Modify `lib/lockspire/domain/client.ex` to:
    - Add `:tls_client_auth | :self_signed_tls_client_auth` to the `token_endpoint_auth_method` typespec enum.
    - Add the 5 PKI string attributes (`tls_client_auth_subject_dn`, `tls_client_auth_san_dns`, `tls_client_auth_san_uri`, `tls_client_auth_san_ip`, `tls_client_auth_san_email`) to `@type t` and the `defstruct`.

    Modify `lib/lockspire/security/policy.ex` to:
    - Add `:tls_client_auth` and `:self_signed_tls_client_auth` to `@supported_token_endpoint_auth_methods`.
  </action>
  <verify>
    <automated>mix test --stale</automated>
  </verify>
  <done>Typespec and struct updated, policy contains new methods.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update ClientRecord Ecto Schema</name>
  <files>lib/lockspire/storage/ecto/client_record.ex</files>
  <behavior>
    - `ClientRecord` can cast and store the 5 MTLS string attributes.
    - `token_endpoint_auth_method` enum column supports `:tls_client_auth` and `:self_signed_tls_client_auth`.
  </behavior>
  <action>
    Modify `lib/lockspire/storage/ecto/client_record.ex` to:
    - Add `:tls_client_auth` and `:self_signed_tls_client_auth` to the `token_endpoint_auth_method` enum values.
    - Add the 5 MTLS attributes (`tls_client_auth_subject_dn`, etc.) as `:string` fields in the Ecto schema.
    - Add the 5 MTLS attributes to the `cast/3` permitted fields list in `changeset/2`.
  </action>
  <verify>
    <automated>mix test --stale</automated>
  </verify>
  <done>Changeset correctly casts MTLS properties and accepts new auth methods.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB schema mapping | Client entity mapping validates proper attributes during persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Input Validation | `ClientRecord.changeset/2` | mitigate | Explicitly cast only the defined MTLS fields to prevent mass-assignment vulnerabilities. |
</threat_model>

<verification>
mix compile --warnings-as-errors
</verification>

<success_criteria>
- Domain and Schema fully model the 5 RFC 8705 PKI attributes.
- New MTLS auth methods are properly defined in enumerations and policy config.
</success_criteria>

<output>
After completion, create `.planning/phases/76-mtls-client-authentication/76-01-SUMMARY.md`
</output>
