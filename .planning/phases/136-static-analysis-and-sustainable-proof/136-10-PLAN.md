---
phase: 136-static-analysis-and-sustainable-proof
plan: 10
type: execute
wave: 2
depends_on: [136-01]
files_modified:
  - lib/lockspire/application.ex
  - lib/lockspire/key_cache.ex
  - config/test.exs
  - test/lockspire/key_cache_test.exs
  - test/support/telemetry_capture.ex
  - test/lockspire/protocol/jarm_test.exs
  - test/lockspire/protocol/device_authorization_test.exs
  - test/lockspire/protocol/dcr_telemetry_redaction_test.exs
  - scripts/ci/check_test_runtime_noise.sh
autonomous: true
requirements: [QUAL-03, QUAL-04]
must_haves:
  truths:
    - "A successful routine test run emits no KeyCache startup error, Ecto query-debug flood, or telemetry local-handler warning."
    - "Unexpected KeyCache failures, sensitive-query suppression, and telemetry failure/redaction behavior remain explicitly observable."
  artifacts:
    - path: test/support/telemetry_capture.ex
      provides: unique remote callback attach/detach lifecycle for test telemetry
    - path: scripts/ci/check_test_runtime_noise.sh
      provides: executable successful-run noise contract
    - path: lib/lockspire/key_cache.ex
      provides: readiness-aware initial refresh without hiding operational failure
  key_links:
    - from: lib/lockspire/application.ex
      to: lib/lockspire/key_cache.ex
      via: explicit cache startup/readiness options
      pattern: "KeyCache"
---
<objective>
Make successful test execution quiet while retaining explicit failure and redaction evidence.
</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/136-static-analysis-and-sustainable-proof/136-01-SUMMARY.md
@lib/lockspire/application.ex
@lib/lockspire/key_cache.ex
@config/test.exs
@test/support/token_exchange_case.ex</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Start KeyCache quietly through the real repository-readiness transition</name>
  <files>lib/lockspire/application.ex, lib/lockspire/key_cache.ex, config/test.exs, test/lockspire/key_cache_test.exs, scripts/ci/check_test_runtime_noise.sh</files>
  <behavior>
    - Initial cache startup before the configured host repo process is ready defers refresh without logging an error, then loads keys once ready.
    - A ready repo returning an unexpected error still logs a sanitized operational failure and retains the prior cache.
    - Ordinary test SQL queries are not logged; tests that explicitly raise logger/repo verbosity can still capture sensitive-query suppression behavior.
  </behavior>
  <action>Characterize the actual current startup condition (`Lockspire.TestRepo` not started), then add an explicit readiness-aware initial refresh/retry seam with injectable loader/readiness functions for deterministic tests. Treat only the configured repo-not-running bootstrap state as deferred; once ready, all storage errors remain logged. Apply `log: false` only to the test repo configuration. Create a script that captures successful focused/fast output to a temporary file, rejects the three known noise categories with region-aware patterns, redacts displayed diagnostics, and always cleans up.</action>
  <verify><automated>mix test test/lockspire/key_cache_test.exs test/lockspire/storage/repository_test.exs &amp;&amp; bash scripts/ci/check_test_runtime_noise.sh --focused</automated></verify>
  <done>Real startup becomes quiet and deterministic, while unexpected refresh failures and sensitive-query tests stay observable.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Replace local telemetry callbacks with unique remote capture lifecycle</name>
  <files>test/support/telemetry_capture.ex, test/lockspire/protocol/jarm_test.exs, test/lockspire/protocol/device_authorization_test.exs, test/lockspire/protocol/dcr_telemetry_redaction_test.exs, scripts/ci/check_test_runtime_noise.sh</files>
  <behavior>
    - JARM remote-key failure and device-authorization telemetry tests attach unique handler IDs using a module-qualified callback and detach through `on_exit`.
    - DCR failure telemetry still proves secret/redaction behavior and no handler leakage occurs between async tests.
  </behavior>
  <action>Add one test-support capture module with `attach/2` and `attach_many/2` that creates a unique ID, uses a public module callback, forwards safe event data to the owning test process, and registers idempotent `on_exit` detach. Migrate the three warning-producing/negative-evidence suites without changing event names or assertions. Extend the noise script to reject telemetry's local-function warning but not application telemetry events.</action>
  <verify><automated>mix test test/lockspire/protocol/jarm_test.exs test/lockspire/protocol/device_authorization_test.exs test/lockspire/protocol/dcr_telemetry_redaction_test.exs &amp;&amp; bash scripts/ci/check_test_runtime_noise.sh --focused</automated></verify>
  <done>Successful telemetry tests are warning-free and failure/redaction observations remain exact.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| host repo readiness -> key cache | Startup ordering must not turn a real key-storage outage into silence. |
| telemetry/logger -> test assertions | Diagnostic capture may leak secrets or handlers across tests. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-136-27 | Denial of service | KeyCache readiness retry | high | mitigate | Bounded scheduled retry without busy loop; normal interval retained. |
| T-136-28 | Repudiation | refresh failure logging | high | mitigate | Only repo-not-running startup defers; unexpected ready-repo failure is captured. |
| T-136-29 | Information disclosure | logs/telemetry | critical | mitigate | Preserve redaction assertions and display only sanitized checker excerpts. |
</threat_model>
<verification>`bash scripts/ci/check_test_runtime_noise.sh &amp;&amp; mix test test/lockspire/key_cache_test.exs test/lockspire/protocol/jarm_test.exs test/lockspire/protocol/device_authorization_test.exs test/lockspire/protocol/dcr_telemetry_redaction_test.exs test/lockspire/storage/repository_test.exs`</verification>
<success_criteria>Successful runs are quiet, deterministic, and signal-rich; explicit failure/redaction proof remains green.</success_criteria>
<output>Create `.planning/phases/136-static-analysis-and-sustainable-proof/136-10-SUMMARY.md` when done.</output>
