---
phase: "28"
plan: "03"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - lib/lockspire/web/live/admin/clients_live/index.ex
  - lib/lockspire/web/live/admin/clients_live/index.html.heex
  - lib/lockspire/web/live/admin/clients_live/show.ex
  - lib/lockspire/web/live/admin/clients_live/show.html.heex
  - lib/lockspire/web/router.ex
  - test/lockspire/web/live/admin/clients_live_test.exs
autonomous: true
requirements:
  - DCR-20
must_haves:
  truths:
    - Operator can distinguish self-registered from operator-created clients in the index
    - Operator can force-rotate a Registration Access Token (RAT)
  artifacts:
    - path: lib/lockspire/web/live/admin/clients_live/index.ex
      provides: Provenance faceted filtering
    - path: lib/lockspire/web/live/admin/clients_live/show.ex
      provides: DCR info panel and RAT rotation action
  key_links:
    - from: lib/lockspire/web/live/admin/clients_live/show.ex
      to: Lockspire.Protocol.RegistrationManagement
      via: rotate RAT action
---

<objective>
Enhance the Client Admin UI to clearly expose client provenance (operator vs self-registered via DCR) and provide an operator-driven workflow to forcibly rotate a client's Registration Access Token (RAT).

Purpose: Operators need visibility into how a client arrived and an escape hatch to rotate a RAT if the partner loses theirs.
Output: Provenance filter on `ClientsLive.Index`, self-registration panel and copy-once RAT rotation flow on `ClientsLive.Show`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-operator-admin-ui-and-telemetry/28-RESEARCH.md
@.planning/phases/28-operator-admin-ui-and-telemetry/28-PATTERNS.md
@lib/lockspire/web/live/admin/clients_live/index.ex
@lib/lockspire/web/live/admin/clients_live/show.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Provenance Filter to Clients Index</name>
  <files>lib/lockspire/web/live/admin/clients_live/index.ex, lib/lockspire/web/live/admin/clients_live/index.html.heex</files>
  <action>Update `load_clients` to accept a provenance filter. Add a UI badge showing `operator_created` or `self_registered`. Add the faceted filter dropdown to the HEEx template alongside existing filters.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Client list clearly distinguishes provenance visually and logically.</done>
</task>

<task type="auto">
  <name>Task 2: Add Self-Registered Panel and RAT Rotate Modal</name>
  <files>lib/lockspire/web/live/admin/clients_live/show.ex, lib/lockspire/web/live/admin/clients_live/show.html.heex, lib/lockspire/web/router.ex</files>
  <action>In `ClientsLive.Show`, conditionally render a "Self-registered client" panel if provenance is `self_registered`. Add a `:rotate_registration_access_token` live action. Implement a copy-once flow identical to IAT minting: generate the new RAT using `Protocol.RegistrationManagement.rotate_registration_access_token`, display plaintext in `assigns`, and require explicit acknowledgement to clear it. Mount the new live action route in `lib/lockspire/web/router.ex`.</action>
  <verify>
    <automated>mix test test/lockspire/web/router_test.exs || mix compile</automated>
  </verify>
  <done>RAT rotation works and successfully uses the copy-once pattern.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test Provenance and Rotation UI</name>
  <files>test/lockspire/web/live/admin/clients_live_test.exs</files>
  <behavior>
    - Test 1: Provenance filter narrows client list correctly
    - Test 2: Show page for self-registered client renders DCR info panel
    - Test 3: RAT rotation replaces the DB hash and displays plaintext copy-once
  </behavior>
  <action>Add integration tests asserting filter behavior and the RAT rotation explicit-clear workflow on the show page.</action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/clients_live_test.exs</automated>
  </verify>
  <done>All tests confirm features operate as intended without leaking plaintext.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Memory -> Operator | RAT secrets are shown once upon rotation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Information Disclosure | RAT Rotation | mitigate | Apply the identical copy-once explicit state-clear mechanism used in IATs to prevent RAT plaintext from lingering. |
</threat_model>

<verification>
mix test test/lockspire/web/live/admin/clients_live_test.exs
</verification>

<success_criteria>
Operators can identify DCR clients and securely rotate their RAT via the UI.
</success_criteria>

<output>
After completion, create `.planning/phases/28-operator-admin-ui-and-telemetry/28-03-SUMMARY.md`
</output>