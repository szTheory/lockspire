---
phase: "28"
plan: "04"
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - lib/lockspire/observability.ex
  - lib/lockspire/protocol/registration.ex
  - lib/lockspire/protocol/registration_management.ex
  - lib/lockspire/protocol/initial_access_token.ex
  - lib/lockspire/admin/initial_access_tokens.ex
  - test/integration/phase28_e2e_test.exs
autonomous: true
requirements:
  - DCR-21
must_haves:
  truths:
    - DCR actions (register, read, update, delete, rotate, unauthorized) emit telemetry
    - IAT actions (mint, use, revoke) emit telemetry
    - Secrets are redacted from all telemetry payloads
  artifacts:
    - path: lib/lockspire/observability.ex
      provides: Telemetry definition for DCR and IAT
    - path: test/integration/phase28_e2e_test.exs
      provides: Full E2E telemetry trace proof
  key_links:
    - from: lib/lockspire/protocol/registration.ex
      to: :telemetry
      via: Lockspire.Observability.emit
---

<objective>
Wire the full DCR and IAT lifecycle telemetry surface across protocol, HTTP, and admin paths, ensuring absolute redact-by-default safety for plaintext secrets, and prove it via an end-to-end scenario test.

Purpose: Operators need deep observability into partner registration lifecycles for auditing and metrics, but secrets must never leak to logs or APMs.
Output: Telemetry emission integrated into all DCR/IAT domain functions and a comprehensive integration test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-operator-admin-ui-and-telemetry/28-RESEARCH.md
@.planning/phases/28-operator-admin-ui-and-telemetry/28-PATTERNS.md
@lib/lockspire/observability.ex
@lib/lockspire/protocol/initial_access_token.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define DCR and IAT Telemetry Namespaces</name>
  <files>lib/lockspire/observability.ex</files>
  <action>Update `Lockspire.Observability` (if necessary) to ensure event prefixes `[:lockspire, :dcr, *]` and `[:lockspire, :iat, *]` are supported and properly redact any keys named `registration_access_token`, `client_secret`, or `iat_secret`. If the system uses flat events, create specific wrapper functions that expand to these hierarchical events.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Observability boundaries are ready for strict hierarchical DCR/IAT events.</done>
</task>

<task type="auto">
  <name>Task 2: Inject Event Emission in Protocol/Domain Logic</name>
  <files>lib/lockspire/protocol/registration.ex, lib/lockspire/protocol/registration_management.ex, lib/lockspire/protocol/initial_access_token.ex, lib/lockspire/admin/initial_access_tokens.ex</files>
  <action>Emit `[:lockspire, :dcr, :register]`, `:read`, `:update`, `:delete`, `:rotate`, and `:unauthorized` in the respective functions inside `Registration` and `RegistrationManagement`. Emit `[:lockspire, :iat, :mint]`, `:use`, `:revoke` in `Admin.InitialAccessTokens` and `Protocol.InitialAccessToken.redeem/1` (migrating any existing `:iat_redeemed` flat event to the namespace if needed, or maintaining compatibility).</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>All state mutations and reads in the DCR flow trigger telemetry.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create E2E Lifecycle Scenario Test</name>
  <files>test/integration/phase28_e2e_test.exs</files>
  <behavior>
    - Test 1: Full flow triggers every expected event: mint -> register -> read -> rotate -> update -> delete -> revoke -> unauthorized
  </behavior>
  <action>Use `:telemetry.attach_many` to capture emitted events. Execute a complete integration flow: mint an IAT, use it to register via HTTP/Protocol, read the client, rotate RAT, update the client, delete the client, and attempt unauthorized access. Assert that the exact expected telemetry event names were fired, and perform a redaction assertion on the collected payloads ensuring no plaintext leaked.</action>
  <verify>
    <automated>mix test test/integration/phase28_e2e_test.exs</automated>
  </verify>
  <done>E2E test successfully proves full observability coverage without secret leaks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Domain -> Telemetry | Events sent to observability systems. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Information Disclosure | Telemetry Payloads | mitigate | All payloads pass through `Lockspire.Observability.emit` where a redaction filter strips `client_secret` and access tokens from maps before execution. |
</threat_model>

<verification>
mix test test/integration/phase28_e2e_test.exs
</verification>

<success_criteria>
DCR and IAT lifecycle events are thoroughly captured via telemetry and proven safe for production logs.
</success_criteria>

<output>
After completion, create `.planning/phases/28-operator-admin-ui-and-telemetry/28-04-SUMMARY.md`
</output>