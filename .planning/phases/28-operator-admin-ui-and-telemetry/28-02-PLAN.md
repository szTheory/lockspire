---
phase: "28"
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - lib/lockspire/web/live/admin/iat_live/index.ex
  - lib/lockspire/web/live/admin/iat_live/index.html.heex
  - lib/lockspire/web/live/admin/iat_live/new.ex
  - lib/lockspire/web/live/admin/iat_live/new.html.heex
  - lib/lockspire/web/router.ex
  - test/lockspire/web/live/admin/iat_live_test.exs
  - lib/lockspire/admin/initial_access_tokens.ex
autonomous: true
requirements:
  - DCR-12
  - DCR-19
must_haves:
  truths:
    - Operator can list IATs
    - Operator can mint a new IAT with copy-once display
    - Operator can revoke an IAT
  artifacts:
    - path: lib/lockspire/web/live/admin/iat_live/new.ex
      provides: IAT Minting UI
    - path: lib/lockspire/admin/initial_access_tokens.ex
      provides: Admin core logic for IATs
  key_links:
    - from: lib/lockspire/web/live/admin/iat_live/new.ex
      to: assign(socket, iat_secret: nil)
      via: copy-once acknowledge button
---

<objective>
Build the Initial Access Token (IAT) lifecycle UI allowing operators to list, mint, and revoke IATs.

Purpose: Operators need to generate IATs to distribute to partners for DCR. The plaintext secret must be shown only once and explicitly cleared.
Output: Admin backend module for IATs, and LiveViews for listing and minting IATs with copy-once mechanism.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-operator-admin-ui-and-telemetry/28-RESEARCH.md
@.planning/phases/28-operator-admin-ui-and-telemetry/28-PATTERNS.md
@lib/lockspire/web/live/admin/tokens_live/index.ex
@lib/lockspire/web/live/admin/clients_live/index.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Admin IAT Domain Core</name>
  <files>lib/lockspire/admin/initial_access_tokens.ex</files>
  <action>Create `Lockspire.Admin.InitialAccessTokens` with `list_iats/1`, `mint_iat/1`, and `revoke_iat/1` delegating to `Lockspire.Storage.Ecto.Repository` functions. `mint_iat` should generate a 32-byte secret, hash it via `Lockspire.Security.Policy.hash_token/1`, save it, and return `{:ok, iat, plaintext_secret}`. Ensure telemetry events `[:lockspire, :iat, :mint]` and `[:lockspire, :iat, :revoke]` are wrapped here or in the repository appropriately.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Admin context logic is present for UI consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Build IAT Index and New LiveViews</name>
  <files>lib/lockspire/web/live/admin/iat_live/index.ex, lib/lockspire/web/live/admin/iat_live/index.html.heex, lib/lockspire/web/live/admin/iat_live/new.ex, lib/lockspire/web/live/admin/iat_live/new.html.heex, lib/lockspire/web/router.ex</files>
  <action>Create `IatLive.Index` mirroring `TokensLive.Index` to list tokens and handle `revoke` events. Create `IatLive.New` with a form to mint an IAT. In `IatLive.New`, the minted plaintext secret MUST be assigned to socket state, displayed with a warning, and accompanied by an "I have copied this" button that explicitly zeroes out the assign (`assign(socket, revealed_secret: nil)`). Mount both at `/admin/iats` and `/admin/iats/new`.</action>
  <verify>
    <automated>mix test test/lockspire/web/router_test.exs || mix compile</automated>
  </verify>
  <done>LiveViews built and mounted, copy-once UI logic implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test IAT UI and Copy-Once Mechanism</name>
  <files>test/lockspire/web/live/admin/iat_live_test.exs</files>
  <behavior>
    - Test 1: IAT index lists active tokens and allows revocation
    - Test 2: Minting an IAT shows the secret exactly once
    - Test 3: Clicking the acknowledge button clears the secret from UI
  </behavior>
  <action>Write integration tests covering the listing, minting, and the explicit UI state-clear behavior.</action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/iat_live_test.exs</automated>
  </verify>
  <done>All tests pass confirming copy-once logic is secure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Memory -> Operator | IAT secrets are transiently shown in LiveView state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Information Disclosure | IAT Mint View | mitigate | Enforce explicit copy acknowledgment that zeroes out the plaintext secret assign, preventing it from lingering in socket state or DOM. |
</threat_model>

<verification>
mix test test/lockspire/web/live/admin/iat_live_test.exs
</verification>

<success_criteria>
IATs can be minted and revoked, and plaintext secrets are safely displayed once and explicitly removed from state.
</success_criteria>

<output>
After completion, create `.planning/phases/28-operator-admin-ui-and-telemetry/28-02-SUMMARY.md`
</output>