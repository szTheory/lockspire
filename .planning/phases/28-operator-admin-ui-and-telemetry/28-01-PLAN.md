---
phase: "28"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/web/live/admin/policies_live/dcr/policy_form.ex
  - lib/lockspire/web/live/admin/policies_live/dcr.ex
  - lib/lockspire/web/live/admin/policies_live/dcr.html.heex
  - lib/lockspire/web/router.ex
  - test/lockspire/web/live/admin/policies_live/dcr_test.exs
autonomous: true
requirements:
  - DCR-18
must_haves:
  truths:
    - Operator can view global DCR mode
    - Operator can edit DCR allowlists and defaults
  artifacts:
    - path: lib/lockspire/web/live/admin/policies_live/dcr.ex
      provides: DCR Policy management UI
  key_links:
    - from: lib/lockspire/web/live/admin/policies_live/dcr.ex
      to: Lockspire.Admin.ServerPolicy
      via: put_dcr_policy
---

<objective>
Build the DCR Policy Admin UI using an embedded Ecto schema for complex form validation, mirroring the existing PAR policy page.

Purpose: Operators need a graphical interface to toggle Dynamic Client Registration modes and configure allowlists/defaults safely.
Output: DCR Policy LiveView and form schema, mounted in the admin router.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/28-operator-admin-ui-and-telemetry/28-RESEARCH.md
@.planning/phases/28-operator-admin-ui-and-telemetry/28-PATTERNS.md
@lib/lockspire/web/live/admin/policies_live/par.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DCR Policy Form Schema</name>
  <files>lib/lockspire/web/live/admin/policies_live/dcr/policy_form.ex</files>
  <action>Create an `Ecto.Schema` with `@primary_key false` and `embedded_schema` defining all DCR fields (`registration_policy`, allowlists for scopes/grant_types/response_types/schemes/hosts, defaults for client/secret/RAT lifetimes). Add a `changeset/2` function handling casting and `validate_required` for the policy mode. Do not hand-roll map validation; use Ecto as this is a LiveView form constraint.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>Embedded schema successfully compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build and Mount DCR Policy LiveView</name>
  <files>lib/lockspire/web/live/admin/policies_live/dcr.ex, lib/lockspire/web/live/admin/policies_live/dcr.html.heex, lib/lockspire/web/router.ex</files>
  <action>Create the LiveView and template mirroring `PoliciesLive.Par` pattern. On `mount`, load the existing policy via `Lockspire.Admin.ServerPolicy`. On `handle_event("save_policy")`, validate against `PolicyForm` and call `Admin.ServerPolicy.put_dcr_policy`. Mount it in `lib/lockspire/web/router.ex` at `/admin/policies/dcr`.</action>
  <verify>
    <automated>mix test test/lockspire/web/router_test.exs || mix compile</automated>
  </verify>
  <done>LiveView renders, form saves policy, and router is mounted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Test DCR Policy UI</name>
  <files>test/lockspire/web/live/admin/policies_live/dcr_test.exs</files>
  <behavior>
    - Test 1: Loading page displays current DCR mode
    - Test 2: Saving policy persists changes via `Admin.ServerPolicy.put_dcr_policy`
    - Test 3: Invalid input shows form errors
  </behavior>
  <action>Write LiveView integration tests proving the form loads and saves correctly. Mirror the assertions found in `par_test.exs`.</action>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/policies_live/dcr_test.exs</automated>
  </verify>
  <done>All UI integration tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Operator -> Admin UI | Operators updating server configuration. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering | DCR Policy Form | mitigate | Use Ecto.Changeset to rigorously cast and validate all submitted JSON-like configurations to prevent malicious data persistence. |
</threat_model>

<verification>
mix test test/lockspire/web/live/admin/policies_live/dcr_test.exs
</verification>

<success_criteria>
`PoliciesLive.Dcr` allows reading and writing the global DCR policy mode and all associated lists.
</success_criteria>

<output>
After completion, create `.planning/phases/28-operator-admin-ui-and-telemetry/28-01-SUMMARY.md`
</output>