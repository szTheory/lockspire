---
phase: "45-observability-operator-seams"
plan: "03"
type: execute
wave: 2
depends_on: ["45-02"]
files_modified:
  - lib/lockspire/web/live/admin/device_authorizations_live/index.ex
  - test/lockspire/web/live/admin/device_authorizations_live_test.exs
  - lib/lockspire/web/router.ex
  - lib/lockspire/web/live/admin_layout_live.ex
  - docs/telemetry.md
autonomous: true
requirements: [STAB-03, STAB-04]
must_haves:
  truths:
    - "Operator can view a list of device authorizations in the Admin UI."
    - "Developer can read telemetry documentation in the docs folder."
  artifacts:
    - path: "lib/lockspire/web/live/admin/device_authorizations_live/index.ex"
      provides: "Device authorizations list UI"
      contains: "AdminLayoutLive.shell"
    - path: "docs/telemetry.md"
      provides: "Documentation for telemetry events"
      contains: "[:lockspire,"
  key_links:
    - from: "lib/lockspire/web/router.ex"
      to: "lib/lockspire/web/live/admin/device_authorizations_live/index.ex"
      via: "live route"
      pattern: "live.*device_authorizations.*DeviceAuthorizationsLive"
---

<objective>
Implement the final LiveView operator panel for Device Authorizations and document all telemetry events officially.

Purpose: Operators need a seam to monitor active/pending device flow requests, and developers need accurate documentation of telemetry payloads.
Output: Working Device Authorizations LiveView panel, its tests, and `docs/telemetry.md`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From lib/lockspire/web/live/admin/clients_live/index.ex:
```elixir
<AdminLayoutLive.shell current_section={@current_section} page_title={@page_title}>
  <AdminComponents.section_card title="Client inventory" subtitle="...">
    ...
  </AdminComponents.section_card>
</AdminLayoutLive.shell>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Device Authorizations LiveView Panel</name>
  <files>lib/lockspire/web/live/admin/device_authorizations_live/index.ex, test/lockspire/web/live/admin/device_authorizations_live_test.exs, lib/lockspire/web/router.ex, lib/lockspire/web/live/admin_layout_live.ex</files>
  <behavior>
    - GET `/admin/device_authorizations` renders a list of active device authorization requests.
    - Test ensures the page mounts and renders properly.
  </behavior>
  <implementation>
    Create the `Lockspire.Web.Live.Admin.DeviceAuthorizationsLive.Index` module mimicking `ClientsLive.Index`. It should fetch and display `DeviceAuthorizationState` records. Add the route `/admin/device_authorizations` to `lib/lockspire/web/router.ex` under the admin scope. Add the link to the navigation in `admin_layout_live.ex`. Create an integration test to verify it.
  </implementation>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/device_authorizations_live_test.exs</automated>
  </verify>
  <done>Device authorizations panel is accessible via router and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Telemetry Documentation</name>
  <files>docs/telemetry.md</files>
  <implementation>
    Create a formal user-facing `docs/telemetry.md` file. It should document all telemetry events emitted by `Lockspire.Observability.emit/4` across the codebase (e.g. `[:lockspire, entity, action]`), including measurements (e.g. `%{count: 1}`) and available metadata maps for operators. Refer to existing domain logic for accurate payloads.
  </implementation>
  <verify>
    <automated>ls docs/telemetry.md</automated>
  </verify>
  <done>Documentation contains exhaustive list of telemetry events.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → Domain | Operator access to internal state. Must require admin authentication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Elevation of Privilege | Admin Routes | mitigate | Ensure new routes in `router.ex` are placed within the protected admin scope pipeline. |
</threat_model>

<verification>
`mix test test/lockspire/web/live/admin/device_authorizations_live_test.exs` passes and `docs/telemetry.md` exists.
</verification>

<success_criteria>
Device Authorization records are accessible via Admin UI LiveViews, and telemetry is comprehensively documented.
</success_criteria>

<output>
After completion, create `.planning/phases/45-observability-operator-seams/45-03-SUMMARY.md`
</output>
