---
phase: "45-observability-operator-seams"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/web/live/admin/interactions_live/index.ex
  - test/lockspire/web/live/admin/interactions_live_test.exs
  - lib/lockspire/web/live/admin/logout_deliveries_live/index.ex
  - test/lockspire/web/live/admin/logout_deliveries_live_test.exs
  - lib/lockspire/web/router.ex
  - lib/lockspire/web/live/admin_layout_live.ex
autonomous: true
requirements: [STAB-04]
must_haves:
  truths:
    - "Operator can view a list of interactions in the Admin UI."
    - "Operator can view a list of logout deliveries in the Admin UI."
  artifacts:
    - path: "lib/lockspire/web/live/admin/interactions_live/index.ex"
      provides: "Interactions list UI"
      contains: "AdminLayoutLive.shell"
    - path: "lib/lockspire/web/live/admin/logout_deliveries_live/index.ex"
      provides: "Logout deliveries list UI"
      contains: "AdminLayoutLive.shell"
    - path: "lib/lockspire/web/router.ex"
      provides: "Routing for the new panels"
      contains: "InteractionsLive.Index"
  key_links:
    - from: "lib/lockspire/web/router.ex"
      to: "lib/lockspire/web/live/admin/interactions_live/index.ex"
      via: "live route"
      pattern: "live.*interactions.*InteractionsLive"
    - from: "lib/lockspire/web/router.ex"
      to: "lib/lockspire/web/live/admin/logout_deliveries_live/index.ex"
      via: "live route"
      pattern: "live.*logout.*LogoutDeliveriesLive"
---

<objective>
Implement missing LiveView operator panels for Interactions and Logout Deliveries to allow operators to debug active logins and logout propagation.

Purpose: Operators need visibility into ongoing user interactions and backchannel logout events to effectively administer the system.
Output: Working Interactions and Logout Deliveries LiveView panels integrated into the Admin router and layout.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From lib/lockspire/web/live/admin/clients_live/index.ex:
```elixir
<AdminLayoutLive.shell current_section={@current_section} page_title={@page_title}>
  <AdminComponents.section_card title="Client inventory" subtitle="...">
    ...
  </AdminComponents.section_card>
</AdminLayoutLive.shell>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Interactions LiveView Panel</name>
  <files>lib/lockspire/web/live/admin/interactions_live/index.ex, test/lockspire/web/live/admin/interactions_live_test.exs, lib/lockspire/web/router.ex</files>
  <behavior>
    - GET `/admin/interactions` renders a list of active interactions.
    - Test ensures that the page loads successfully and contains expected layout elements.
  </behavior>
  <implementation>
    Create the `Lockspire.Web.Live.Admin.InteractionsLive.Index` module mimicking `ClientsLive.Index`. It should fetch and display `Interaction` records. Add the route `/admin/interactions` to `lib/lockspire/web/router.ex` under the admin scope. Create an integration test to verify the route mounts and renders successfully. Add the sidebar link to `lib/lockspire/web/live/admin_layout_live.ex` if applicable.
  </implementation>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/interactions_live_test.exs</automated>
  </verify>
  <done>Interactions LiveView is accessible, tests pass, and it's linked in the layout/router.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Logout Deliveries LiveView Panel</name>
  <files>lib/lockspire/web/live/admin/logout_deliveries_live/index.ex, test/lockspire/web/live/admin/logout_deliveries_live_test.exs, lib/lockspire/web/router.ex, lib/lockspire/web/live/admin_layout_live.ex</files>
  <behavior>
    - GET `/admin/logouts` (or `/admin/logout_deliveries`) renders a list of logout delivery records.
    - Test ensures the page mounts and renders properly.
  </behavior>
  <implementation>
    Create the `Lockspire.Web.Live.Admin.LogoutDeliveriesLive.Index` module to fetch and display `LogoutDelivery` and/or `LogoutEvent` records. Add the route `/admin/logouts` to `lib/lockspire/web/router.ex`. Add it to the sidebar navigation in `admin_layout_live.ex`. Create the corresponding integration test.
  </implementation>
  <verify>
    <automated>mix test test/lockspire/web/live/admin/logout_deliveries_live_test.exs</automated>
  </verify>
  <done>Logout deliveries panel is accessible via router and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin UI → Domain | Operator access to internal state. Must require admin authentication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-02 | Elevation of Privilege | Admin Routes | mitigate | Ensure new routes in `router.ex` are placed within the protected admin scope pipeline (e.g. `require_authenticated_admin`). |
</threat_model>

<verification>
`mix test test/lockspire/web/live/admin/interactions_live_test.exs test/lockspire/web/live/admin/logout_deliveries_live_test.exs` passes.
</verification>

<success_criteria>
Interactions and Logout Delivery records are accessible via Admin UI LiveViews.
</success_criteria>

<output>
After completion, create `.planning/phases/45-observability-operator-seams/45-02-SUMMARY.md`
</output>
