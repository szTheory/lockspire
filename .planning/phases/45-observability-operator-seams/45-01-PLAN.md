---
phase: "45-observability-operator-seams"
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/lockspire/protocol/device_authorization.ex
  - lib/lockspire/protocol/device_verification.ex
  - test/lockspire/protocol/device_verification_test.exs
  - test/lockspire/protocol/device_authorization_test.exs
autonomous: true
requirements: [STAB-03]
must_haves:
  truths:
    - "Device code creation emits standardized telemetry."
    - "Device verification approvals and denials emit standardized telemetry."
  artifacts:
    - path: "lib/lockspire/protocol/device_authorization.ex"
      provides: "Device authorization domain logic"
      contains: "Observability.emit"
    - path: "lib/lockspire/protocol/device_verification.ex"
      provides: "Device verification logic"
      contains: "Observability.emit"
  key_links:
    - from: "lib/lockspire/protocol/device_authorization.ex"
      to: "lib/lockspire/observability.ex"
      via: "Function call"
      pattern: "Observability.emit"
---

<objective>
Implement missing telemetry emission in the Device Authorization and Device Verification domain logic to ensure consistent observability.

Purpose: Operators need visibility into Device Authorization drop-offs and success rates. Currently, it fails silently because these protocol actions don't emit telemetry.
Output: Working telemetry emitted from `DeviceAuthorization` and `DeviceVerification` modules, backed by tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
From lib/lockspire/observability.ex:
```elixir
def emit(entity, action, measurements \\ %{}, metadata \\ %{}) when is_atom(entity) and is_atom(action)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Device Authorization Telemetry</name>
  <files>lib/lockspire/protocol/device_authorization.ex, lib/lockspire/protocol/device_verification.ex, test/lockspire/protocol/device_authorization_test.exs, test/lockspire/protocol/device_verification_test.exs</files>
  <behavior>
    - When `DeviceAuthorization` creates a device/user code, it emits `[:lockspire, :device_authorization, :created]` (and its audit equivalent) via `Observability.emit/4`.
    - When `DeviceVerification` approves a request, it emits `[:lockspire, :device_authorization, :approved]`.
    - When `DeviceVerification` denies a request, it emits `[:lockspire, :device_authorization, :denied]`.
    - Metadata must include relevant `client_id` or `device_code` references.
  </behavior>
  <implementation>
    Use `Lockspire.Observability.emit/4` following the shared telemetry/audit mirroring pattern established in `lib/lockspire/admin/clients.ex`. Ensure that standard actor metadata is included in the emitted payload. Attach to `:telemetry` events in tests to ensure that the events are properly executed during the respective functions' execution flows.
  </implementation>
  <verify>
    <automated>mix test test/lockspire/protocol/device_authorization_test.exs test/lockspire/protocol/device_verification_test.exs</automated>
  </verify>
  <done>Telemetry is emitted and all related tests pass successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System → Logs/Telemetry | Telemetry emission must not log sensitive user codes or device codes in plaintext metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information Disclosure | Observability | mitigate | Only emit non-sensitive IDs or hashes in metadata. Do not log plaintext user codes. `Observability.redact/1` will handle some, but avoid passing sensitive fields entirely. |
</threat_model>

<verification>
`mix test test/lockspire/protocol/device_authorization_test.exs test/lockspire/protocol/device_verification_test.exs` passes.
</verification>

<success_criteria>
`Observability.emit/4` is called on all critical Device Authorization and Verification transitions and tests verify the emissions.
</success_criteria>

<output>
After completion, create `.planning/phases/45-observability-operator-seams/45-01-SUMMARY.md`
</output>
